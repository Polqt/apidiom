---
title: apidiom
description: Generate type-safe Python API clients from clean specs or messy API docs.
---

import { CardGrid, LinkCard } from '@astrojs/starlight/components';

`apidiom` generates type-safe Python API clients from messy docs that have no
OpenAPI spec, not just from already-clean OpenAPI documents.

It is for developers who need a usable `httpx` client from API documentation
they do not control: public docs pages, internal API notes, or an existing
OpenAPI file that should be normalized and passed through client generation.

The tool is conservative by design. When the input docs do not prove a field,
`apidiom` marks it as unverified instead of guessing.

## Get started

<CardGrid>
  <LinkCard
    title="Quickstart"
    description="Install apidiom and generate a client from a clean spec or messy docs."
    href="/quickstart/"
  />
  <LinkCard
    title="Providers & privacy"
    description="Choose null, Gemini, or Ollama and understand the privacy tradeoff."
    href="/providers-privacy/"
  />
  <LinkCard
    title="Unverified fields"
    description="See how apidiom marks what it cannot verify instead of guessing."
    href="/unverified-fields/"
  />
  <LinkCard
    title="MCP server"
    description="Connect agents to the same pipeline used by the CLI and web UI."
    href="/mcp-server/"
  />
</CardGrid>

## What it does today

`apidiom` reads OpenAPI JSON or YAML from a file path or URL, extracts OpenAPI
from unstructured docs through a selected LLM provider, validates and normalizes
the spec, and delegates Python client generation to existing tools.
