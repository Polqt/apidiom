"use strict";
// Generated by apidiom — https://github.com/poyhi/apidiom
// GitHub v3 REST API v1.1.4

const https = require("https");
const http = require("http");


// --- HTTP helper ---
function _request(method, url, headers, body) {
  return new Promise(function(resolve, reject) {
    var u = new URL(url);
    var lib = u.protocol === "https:" ? https : http;
    var opts = {
      hostname: u.hostname,
      port: u.port || undefined,
      path: u.pathname + u.search,
      method: method,
      headers: Object.assign({}, body !== undefined ? { "Content-Type": "application/json" } : {}, headers)
    };
    var req = lib.request(opts, function(res) {
      var d = "";
      res.on("data", function(c) { d += c; });
      res.on("end", function() {
        try { resolve(JSON.parse(d)); } catch(e) { resolve(d); }
      });
    });
    req.on("error", reject);
    if (body !== undefined) req.write(JSON.stringify(body));
    req.end();
  });
}

// --- Tools ---
var _TOOLS = [
  {
    name: "meta/root",
    description: "GitHub API Root. Get Hypermedia links to resources accessible in GitHub's REST API.",
    inputSchema: {"type":"object","properties":{},"required":[]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/") + _qs, {}, undefined);
    }
  },
  {
    name: "security-advisories/list-global-advisories",
    description: "List global security advisories. Lists all global security advisories that match the specified parameters.",
    inputSchema: {"type":"object","properties":{"ghsa_id":{"type":"string","description":"If specified, only advisories with this GHSA (GitHub Security Advisory) identifier will be returned."},"type":{"type":"string","enum":["reviewed","malware","unreviewed"],"default":"reviewed","description":"If specified, only advisories of this type will be returned. By default, a request with no other parameters defined will only return reviewed advisories that are not malware."},"cve_id":{"type":"string","description":"If specified, only advisories with this CVE (Common Vulnerabilities and Exposures) identifier will be returned."},"ecosystem":{"$ref":"#/components/schemas/security-advisory-ecosystems","description":"If specified, only advisories for these ecosystems will be returned."},"severity":{"type":"string","enum":["unknown","low","medium","high","critical"],"description":"If specified, only advisories with these severities will be returned."},"cwes":{"oneOf":[{"type":"string"},{"type":"array","items":{"type":"string"}}],"description":"If specified, only advisories with these Common Weakness Enumerations (CWEs) will be returned.\n\nExample: `cwes=79,284,22` or `cwes[]=79&cwes[]=284&cwes[]=22`"},"is_withdrawn":{"type":"boolean","description":"Whether to only return advisories that have been withdrawn."},"affects":{"oneOf":[{"type":"string"},{"type":"array","maxItems":1000,"items":{"type":"string"}}],"description":"If specified, only return advisories that affect any of `package` or `package@version`. A maximum of 1000 packages can be specified.\nIf the query parameter causes the URL to exceed the maximum URL length supported by your client, you must specify fewer packages.\n\nExample: `affects=package1,package2@1.0.0,package3@2.0.0` or `affects[]=package1&affects[]=package2@1.0.0`"},"published":{"type":"string","description":"If specified, only return advisories that were published on a date or date range.\n\nFor more information on the syntax of the date range, see \"[Understanding the search syntax](https://docs.github.com/search-github/getting-started-with-searching-on-github/understanding-the-search-syntax#query-for-dates).\""},"updated":{"type":"string","description":"If specified, only return advisories that were updated on a date or date range.\n\nFor more information on the syntax of the date range, see \"[Understanding the search syntax](https://docs.github.com/search-github/getting-started-with-searching-on-github/understanding-the-search-syntax#query-for-dates).\""},"modified":{"type":"string","description":"If specified, only show advisories that were updated or published on a date or date range.\n\nFor more information on the syntax of the date range, see \"[Understanding the search syntax](https://docs.github.com/search-github/getting-started-with-searching-on-github/understanding-the-search-syntax#query-for-dates).\""},"epss_percentage":{"type":"string","description":"If specified, only return advisories that have an EPSS percentage score that matches the provided value.\nThe EPSS percentage represents the likelihood of a CVE being exploited."},"epss_percentile":{"type":"string","description":"If specified, only return advisories that have an EPSS percentile score that matches the provided value.\nThe EPSS percentile represents the relative rank of the CVE's likelihood of being exploited compared to other CVEs."},"before":{"type":"string","description":"A cursor, as given in the [Link header](https://docs.github.com/rest/guides/using-pagination-in-the-rest-api#using-link-headers). If specified, the query only searches for results before this cursor. For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""},"after":{"type":"string","description":"A cursor, as given in the [Link header](https://docs.github.com/rest/guides/using-pagination-in-the-rest-api#using-link-headers). If specified, the query only searches for results after this cursor. For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""},"direction":{"type":"string","enum":["asc","desc"],"default":"desc","description":"The direction to sort the results by."},"per_page":{"type":"integer","minimum":1,"maximum":100,"default":30,"description":"The number of results per page (max 100). For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""},"sort":{"type":"string","enum":["updated","published","epss_percentage","epss_percentile"],"default":"published","description":"The property to sort the results by."}},"required":[]},
    call: function(args) {
    var _q = new URLSearchParams();
    if (args["ghsa_id"] !== undefined) _q.set("ghsa_id", String(args["ghsa_id"]));
    if (args["type"] !== undefined) _q.set("type", String(args["type"]));
    if (args["cve_id"] !== undefined) _q.set("cve_id", String(args["cve_id"]));
    if (args["ecosystem"] !== undefined) _q.set("ecosystem", String(args["ecosystem"]));
    if (args["severity"] !== undefined) _q.set("severity", String(args["severity"]));
    if (args["cwes"] !== undefined) _q.set("cwes", String(args["cwes"]));
    if (args["is_withdrawn"] !== undefined) _q.set("is_withdrawn", String(args["is_withdrawn"]));
    if (args["affects"] !== undefined) _q.set("affects", String(args["affects"]));
    if (args["published"] !== undefined) _q.set("published", String(args["published"]));
    if (args["updated"] !== undefined) _q.set("updated", String(args["updated"]));
    if (args["modified"] !== undefined) _q.set("modified", String(args["modified"]));
    if (args["epss_percentage"] !== undefined) _q.set("epss_percentage", String(args["epss_percentage"]));
    if (args["epss_percentile"] !== undefined) _q.set("epss_percentile", String(args["epss_percentile"]));
    if (args["before"] !== undefined) _q.set("before", String(args["before"]));
    if (args["after"] !== undefined) _q.set("after", String(args["after"]));
    if (args["direction"] !== undefined) _q.set("direction", String(args["direction"]));
    if (args["per_page"] !== undefined) _q.set("per_page", String(args["per_page"]));
    if (args["sort"] !== undefined) _q.set("sort", String(args["sort"]));
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/advisories") + _qs, {}, undefined);
    }
  },
  {
    name: "security-advisories/get-global-advisory",
    description: "Get a global security advisory. Gets a global security advisory using its GitHub Security Advisory (GHSA) identifier. Params: ghsa_id (required).",
    inputSchema: {"type":"object","properties":{"ghsa_id":{"type":"string","description":"The GHSA (GitHub Security Advisory) identifier of the advisory."}},"required":["ghsa_id"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/advisories/" + String(args["ghsa_id"])) + _qs, {}, undefined);
    }
  },
  {
    name: "agent-tasks/list-tasks-for-repo",
    description: "List tasks for repository. > [!NOTE]\n> This endpoint is in public preview and is subject to change. Params: owner (required), repo (required).",
    inputSchema: {"type":"object","properties":{"owner":{"type":"string","description":"The account owner of the repository. The name is not case sensitive."},"repo":{"type":"string","description":"The name of the repository. The name is not case sensitive."},"per_page":{"type":"integer","default":30,"minimum":1,"maximum":100,"description":"The number of results per page (max 100)."},"page":{"type":"integer","default":1,"minimum":1,"description":"The page number of the results to fetch."},"sort":{"type":"string","default":"updated_at","enum":["updated_at","created_at"],"description":"The field to sort results by. Can be `updated_at` or `created_at`."},"direction":{"type":"string","default":"desc","enum":["asc","desc"],"description":"The direction to sort results. Can be `asc` or `desc`."},"state":{"type":"string","description":"Comma-separated list of task states to filter by. Can be any combination of: `queued`, `in_progress`, `completed`, `failed`, `idle`, `waiting_for_user`, `timed_out`, `cancelled`."},"is_archived":{"type":"boolean","default":false,"description":"Filter by archived status. When `true`, returns only archived tasks. When `false` or omitted, returns only non-archived tasks. Defaults to `false`."},"since":{"type":"string","format":"date-time","description":"Only show tasks updated at or after this time (ISO 8601 timestamp)"},"creator_id":{"type":"array","items":{"type":"integer"},"description":"Filter tasks by creator user ID. Accepts one or more user IDs."}},"required":["owner","repo"]},
    call: function(args) {
    var _q = new URLSearchParams();
    if (args["per_page"] !== undefined) _q.set("per_page", String(args["per_page"]));
    if (args["page"] !== undefined) _q.set("page", String(args["page"]));
    if (args["sort"] !== undefined) _q.set("sort", String(args["sort"]));
    if (args["direction"] !== undefined) _q.set("direction", String(args["direction"]));
    if (args["state"] !== undefined) _q.set("state", String(args["state"]));
    if (args["is_archived"] !== undefined) _q.set("is_archived", String(args["is_archived"]));
    if (args["since"] !== undefined) _q.set("since", String(args["since"]));
    if (args["creator_id"] !== undefined) _q.set("creator_id", String(args["creator_id"]));
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/agents/repos/" + String(args["owner"]) + "/" + String(args["repo"]) + "/tasks") + _qs, {}, undefined);
    }
  },
  {
    name: "agent-tasks/create-task-in-repo",
    description: "Start a task. > [!NOTE]\n> This endpoint is in public preview and is subject to change. Params: owner (required), repo (required).",
    inputSchema: {"type":"object","properties":{"owner":{"type":"string","description":"The account owner of the repository. The name is not case sensitive."},"repo":{"type":"string","description":"The name of the repository. The name is not case sensitive."},"body":{"type":"object","description":"The task creation parameters, including the user's prompt and optional agent settings."}},"required":["owner","repo","body"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("POST", ("https://api.github.com/agents/repos/" + String(args["owner"]) + "/" + String(args["repo"]) + "/tasks") + _qs, {}, args["body"]);
    }
  },
  {
    name: "agent-tasks/get-task-by-repo-and-id",
    description: "Get a task by repo. > [!NOTE]\n> This endpoint is in public preview and is subject to change. Params: owner (required), repo (required), task_id (required).",
    inputSchema: {"type":"object","properties":{"owner":{"type":"string","description":"The account owner of the repository. The name is not case sensitive."},"repo":{"type":"string","description":"The name of the repository. The name is not case sensitive."},"task_id":{"type":"string","description":"The unique identifier of the task."}},"required":["owner","repo","task_id"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/agents/repos/" + String(args["owner"]) + "/" + String(args["repo"]) + "/tasks/" + String(args["task_id"])) + _qs, {}, undefined);
    }
  },
  {
    name: "agent-tasks/list-tasks",
    description: "List tasks. > [!NOTE]\n> This endpoint is in public preview and is subject to change.",
    inputSchema: {"type":"object","properties":{"per_page":{"type":"integer","default":30,"minimum":1,"maximum":100,"description":"The number of results per page (max 100)."},"page":{"type":"integer","default":1,"minimum":1,"description":"The page number of the results to fetch."},"sort":{"type":"string","default":"updated_at","enum":["updated_at","created_at"],"description":"The field to sort results by. Can be `updated_at` or `created_at`."},"direction":{"type":"string","default":"desc","enum":["asc","desc"],"description":"The direction to sort results. Can be `asc` or `desc`."},"state":{"type":"string","description":"Comma-separated list of task states to filter by. Can be any combination of: `queued`, `in_progress`, `completed`, `failed`, `idle`, `waiting_for_user`, `timed_out`, `cancelled`."},"is_archived":{"type":"boolean","default":false,"description":"Filter by archived status. When `true`, returns only archived tasks. When `false` or omitted, returns only non-archived tasks. Defaults to `false`."},"since":{"type":"string","format":"date-time","description":"Only show tasks updated at or after this time (ISO 8601 timestamp)"}},"required":[]},
    call: function(args) {
    var _q = new URLSearchParams();
    if (args["per_page"] !== undefined) _q.set("per_page", String(args["per_page"]));
    if (args["page"] !== undefined) _q.set("page", String(args["page"]));
    if (args["sort"] !== undefined) _q.set("sort", String(args["sort"]));
    if (args["direction"] !== undefined) _q.set("direction", String(args["direction"]));
    if (args["state"] !== undefined) _q.set("state", String(args["state"]));
    if (args["is_archived"] !== undefined) _q.set("is_archived", String(args["is_archived"]));
    if (args["since"] !== undefined) _q.set("since", String(args["since"]));
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/agents/tasks") + _qs, {}, undefined);
    }
  },
  {
    name: "agent-tasks/get-task-by-id",
    description: "Get a task by ID. > [!NOTE]\n> This endpoint is in public preview and is subject to change. Params: task_id (required).",
    inputSchema: {"type":"object","properties":{"task_id":{"type":"string","description":"The unique identifier of the task."}},"required":["task_id"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/agents/tasks/" + String(args["task_id"])) + _qs, {}, undefined);
    }
  },
  {
    name: "apps/get-authenticated",
    description: "Get the authenticated app. Returns the GitHub App associated with the authentication credentials used.",
    inputSchema: {"type":"object","properties":{},"required":[]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/app") + _qs, {}, undefined);
    }
  },
  {
    name: "apps/create-from-manifest",
    description: "Create a GitHub App from a manifest. Use this endpoint to complete the handshake necessary when implementing the [GitHub App Manifest flow](https://docs.github.com/apps/building-github-apps/creating-github-apps-from-a-manifest/). Params: code (required).",
    inputSchema: {"type":"object","properties":{"code":{"type":"string"}},"required":["code"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("POST", ("https://api.github.com/app-manifests/" + String(args["code"]) + "/conversions") + _qs, {}, undefined);
    }
  },
  {
    name: "apps/get-webhook-config-for-app",
    description: "Get a webhook configuration for an app. Returns the webhook configuration for a GitHub App.",
    inputSchema: {"type":"object","properties":{},"required":[]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/app/hook/config") + _qs, {}, undefined);
    }
  },
  {
    name: "apps/update-webhook-config-for-app",
    description: "Update a webhook configuration for an app. Updates the webhook configuration for a GitHub App.",
    inputSchema: {"type":"object","properties":{"body":{"type":"object","description":"Request body"}},"required":["body"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("PATCH", ("https://api.github.com/app/hook/config") + _qs, {}, args["body"]);
    }
  },
  {
    name: "apps/list-webhook-deliveries",
    description: "List deliveries for an app webhook. Returns a list of webhook deliveries for the webhook configured for a GitHub App.",
    inputSchema: {"type":"object","properties":{"per_page":{"type":"integer","default":30,"description":"The number of results per page (max 100). For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""},"cursor":{"type":"string","description":"Used for pagination: the starting delivery from which the page of deliveries is fetched. Refer to the `link` header for the next and previous page cursors."},"status":{"type":"string","enum":["success","failure"],"description":"Returns webhook deliveries filtered by delivery outcome classification based on `status_code` range. A `status` of `success` returns deliveries with a `status_code` in the 200-399 range (inclusive). A `status` of `failure` returns deliveries with a `status_code` in the 400-599 range (inclusive)."}},"required":[]},
    call: function(args) {
    var _q = new URLSearchParams();
    if (args["per_page"] !== undefined) _q.set("per_page", String(args["per_page"]));
    if (args["cursor"] !== undefined) _q.set("cursor", String(args["cursor"]));
    if (args["status"] !== undefined) _q.set("status", String(args["status"]));
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/app/hook/deliveries") + _qs, {}, undefined);
    }
  },
  {
    name: "apps/get-webhook-delivery",
    description: "Get a delivery for an app webhook. Returns a delivery for the webhook configured for a GitHub App. Params: delivery_id (required).",
    inputSchema: {"type":"object","properties":{"delivery_id":{"type":"integer"}},"required":["delivery_id"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/app/hook/deliveries/" + String(args["delivery_id"])) + _qs, {}, undefined);
    }
  },
  {
    name: "apps/redeliver-webhook-delivery",
    description: "Redeliver a delivery for an app webhook. Redeliver a delivery for the webhook configured for a GitHub App. Params: delivery_id (required).",
    inputSchema: {"type":"object","properties":{"delivery_id":{"type":"integer"}},"required":["delivery_id"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("POST", ("https://api.github.com/app/hook/deliveries/" + String(args["delivery_id"]) + "/attempts") + _qs, {}, undefined);
    }
  },
  {
    name: "apps/list-installation-requests-for-authenticated-app",
    description: "List installation requests for the authenticated app. Lists all the pending installation requests for the authenticated GitHub App.",
    inputSchema: {"type":"object","properties":{"per_page":{"type":"integer","default":30,"description":"The number of results per page (max 100). For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""},"page":{"type":"integer","default":1,"description":"The page number of the results to fetch. For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""}},"required":[]},
    call: function(args) {
    var _q = new URLSearchParams();
    if (args["per_page"] !== undefined) _q.set("per_page", String(args["per_page"]));
    if (args["page"] !== undefined) _q.set("page", String(args["page"]));
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/app/installation-requests") + _qs, {}, undefined);
    }
  },
  {
    name: "apps/list-installations",
    description: "List installations for the authenticated app. The permissions the installation has are included under the `permissions` key.",
    inputSchema: {"type":"object","properties":{"per_page":{"type":"integer","default":30,"description":"The number of results per page (max 100). For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""},"page":{"type":"integer","default":1,"description":"The page number of the results to fetch. For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""},"since":{"type":"string","format":"date-time","description":"Only show results that were last updated after the given time. This is a timestamp in [ISO 8601](https://en.wikipedia.org/wiki/ISO_8601) format: `YYYY-MM-DDTHH:MM:SSZ`."},"outdated":{"type":"string"}},"required":[]},
    call: function(args) {
    var _q = new URLSearchParams();
    if (args["per_page"] !== undefined) _q.set("per_page", String(args["per_page"]));
    if (args["page"] !== undefined) _q.set("page", String(args["page"]));
    if (args["since"] !== undefined) _q.set("since", String(args["since"]));
    if (args["outdated"] !== undefined) _q.set("outdated", String(args["outdated"]));
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/app/installations") + _qs, {}, undefined);
    }
  },
  {
    name: "apps/get-installation",
    description: "Get an installation for the authenticated app. Enables an authenticated GitHub App to find an installation's information using the installation id. Params: installation_id (required).",
    inputSchema: {"type":"object","properties":{"installation_id":{"type":"integer","description":"The unique identifier of the installation."}},"required":["installation_id"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/app/installations/" + String(args["installation_id"])) + _qs, {}, undefined);
    }
  },
  {
    name: "apps/delete-installation",
    description: "Delete an installation for the authenticated app. Uninstalls a GitHub App on a user, organization, or enterprise account. Params: installation_id (required).",
    inputSchema: {"type":"object","properties":{"installation_id":{"type":"integer","description":"The unique identifier of the installation."}},"required":["installation_id"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("DELETE", ("https://api.github.com/app/installations/" + String(args["installation_id"])) + _qs, {}, undefined);
    }
  },
  {
    name: "apps/create-installation-access-token",
    description: "Create an installation access token for an app. Creates an installation access token that enables a GitHub App to make authenticated API requests for the app's installation on an organization or individual account. Params: installation_id (required).",
    inputSchema: {"type":"object","properties":{"installation_id":{"type":"integer","description":"The unique identifier of the installation."},"body":{"type":"object","description":"Request body"}},"required":["installation_id"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("POST", ("https://api.github.com/app/installations/" + String(args["installation_id"]) + "/access_tokens") + _qs, {}, args["body"]);
    }
  },
  {
    name: "apps/suspend-installation",
    description: "Suspend an app installation. Suspends a GitHub App on a user, organization, or enterprise account, which blocks the app from accessing the account's resources. Params: installation_id (required).",
    inputSchema: {"type":"object","properties":{"installation_id":{"type":"integer","description":"The unique identifier of the installation."}},"required":["installation_id"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("PUT", ("https://api.github.com/app/installations/" + String(args["installation_id"]) + "/suspended") + _qs, {}, undefined);
    }
  },
  {
    name: "apps/unsuspend-installation",
    description: "Unsuspend an app installation. Removes a GitHub App installation suspension. Params: installation_id (required).",
    inputSchema: {"type":"object","properties":{"installation_id":{"type":"integer","description":"The unique identifier of the installation."}},"required":["installation_id"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("DELETE", ("https://api.github.com/app/installations/" + String(args["installation_id"]) + "/suspended") + _qs, {}, undefined);
    }
  },
  {
    name: "apps/delete-authorization",
    description: "Delete an app authorization. OAuth and GitHub application owners can revoke a grant for their application and a specific user. Params: client_id (required).",
    inputSchema: {"type":"object","properties":{"client_id":{"type":"string","description":"The client ID of the GitHub app."},"body":{"type":"object","description":"Request body"}},"required":["client_id","body"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("DELETE", ("https://api.github.com/applications/" + String(args["client_id"]) + "/grant") + _qs, {}, args["body"]);
    }
  },
  {
    name: "apps/check-token",
    description: "Check a token. OAuth applications and GitHub applications with OAuth authorizations can use this API method for checking OAuth token validity without exceeding the normal rate limits for failed login attempts. Params: client_id (required).",
    inputSchema: {"type":"object","properties":{"client_id":{"type":"string","description":"The client ID of the GitHub app."},"body":{"type":"object","description":"Request body"}},"required":["client_id","body"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("POST", ("https://api.github.com/applications/" + String(args["client_id"]) + "/token") + _qs, {}, args["body"]);
    }
  },
  {
    name: "apps/reset-token",
    description: "Reset a token. OAuth applications and GitHub applications with OAuth authorizations can use this API method to reset a valid OAuth token without end-user involvement. Params: client_id (required).",
    inputSchema: {"type":"object","properties":{"client_id":{"type":"string","description":"The client ID of the GitHub app."},"body":{"type":"object","description":"Request body"}},"required":["client_id","body"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("PATCH", ("https://api.github.com/applications/" + String(args["client_id"]) + "/token") + _qs, {}, args["body"]);
    }
  },
  {
    name: "apps/delete-token",
    description: "Delete an app token. OAuth  or GitHub application owners can revoke a single token for an OAuth application or a GitHub application with an OAuth authorization. Params: client_id (required).",
    inputSchema: {"type":"object","properties":{"client_id":{"type":"string","description":"The client ID of the GitHub app."},"body":{"type":"object","description":"Request body"}},"required":["client_id","body"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("DELETE", ("https://api.github.com/applications/" + String(args["client_id"]) + "/token") + _qs, {}, args["body"]);
    }
  },
  {
    name: "apps/scope-token",
    description: "Create a scoped access token. Use a non-scoped user access token to create a repository-scoped and/or permission-scoped user access token. Params: client_id (required).",
    inputSchema: {"type":"object","properties":{"client_id":{"type":"string","description":"The client ID of the GitHub app."},"body":{"type":"object","description":"Request body"}},"required":["client_id","body"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("POST", ("https://api.github.com/applications/" + String(args["client_id"]) + "/token/scoped") + _qs, {}, args["body"]);
    }
  },
  {
    name: "apps/get-by-slug",
    description: "Get an app. > [!NOTE]\n> The `:app_slug` is just the URL-friendly name of your GitHub App. Params: app_slug (required).",
    inputSchema: {"type":"object","properties":{"app_slug":{"type":"string"}},"required":["app_slug"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/apps/" + String(args["app_slug"])) + _qs, {}, undefined);
    }
  },
  {
    name: "classroom/get-an-assignment",
    description: "Get an assignment. Gets a GitHub Classroom assignment. Params: assignment_id (required).",
    inputSchema: {"type":"object","properties":{"assignment_id":{"type":"integer","description":"The unique identifier of the classroom assignment."}},"required":["assignment_id"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/assignments/" + String(args["assignment_id"])) + _qs, {}, undefined);
    }
  },
  {
    name: "classroom/list-accepted-assignments-for-an-assignment",
    description: "List accepted assignments for an assignment. Lists any assignment repositories that have been created by students accepting a GitHub Classroom assignment. Params: assignment_id (required).",
    inputSchema: {"type":"object","properties":{"assignment_id":{"type":"integer","description":"The unique identifier of the classroom assignment."},"page":{"type":"integer","default":1,"description":"The page number of the results to fetch. For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""},"per_page":{"type":"integer","default":30,"description":"The number of results per page (max 100). For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""}},"required":["assignment_id"]},
    call: function(args) {
    var _q = new URLSearchParams();
    if (args["page"] !== undefined) _q.set("page", String(args["page"]));
    if (args["per_page"] !== undefined) _q.set("per_page", String(args["per_page"]));
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/assignments/" + String(args["assignment_id"]) + "/accepted_assignments") + _qs, {}, undefined);
    }
  },
  {
    name: "classroom/get-assignment-grades",
    description: "Get assignment grades. Gets grades for a GitHub Classroom assignment. Params: assignment_id (required).",
    inputSchema: {"type":"object","properties":{"assignment_id":{"type":"integer","description":"The unique identifier of the classroom assignment."}},"required":["assignment_id"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/assignments/" + String(args["assignment_id"]) + "/grades") + _qs, {}, undefined);
    }
  },
  {
    name: "classroom/list-classrooms",
    description: "List classrooms. Lists GitHub Classroom classrooms for the current user.",
    inputSchema: {"type":"object","properties":{"page":{"type":"integer","default":1,"description":"The page number of the results to fetch. For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""},"per_page":{"type":"integer","default":30,"description":"The number of results per page (max 100). For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""}},"required":[]},
    call: function(args) {
    var _q = new URLSearchParams();
    if (args["page"] !== undefined) _q.set("page", String(args["page"]));
    if (args["per_page"] !== undefined) _q.set("per_page", String(args["per_page"]));
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/classrooms") + _qs, {}, undefined);
    }
  },
  {
    name: "classroom/get-a-classroom",
    description: "Get a classroom. Gets a GitHub Classroom classroom for the current user. Params: classroom_id (required).",
    inputSchema: {"type":"object","properties":{"classroom_id":{"type":"integer","description":"The unique identifier of the classroom."}},"required":["classroom_id"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/classrooms/" + String(args["classroom_id"])) + _qs, {}, undefined);
    }
  },
  {
    name: "classroom/list-assignments-for-a-classroom",
    description: "List assignments for a classroom. Lists GitHub Classroom assignments for a classroom. Params: classroom_id (required).",
    inputSchema: {"type":"object","properties":{"classroom_id":{"type":"integer","description":"The unique identifier of the classroom."},"page":{"type":"integer","default":1,"description":"The page number of the results to fetch. For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""},"per_page":{"type":"integer","default":30,"description":"The number of results per page (max 100). For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""}},"required":["classroom_id"]},
    call: function(args) {
    var _q = new URLSearchParams();
    if (args["page"] !== undefined) _q.set("page", String(args["page"]));
    if (args["per_page"] !== undefined) _q.set("per_page", String(args["per_page"]));
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/classrooms/" + String(args["classroom_id"]) + "/assignments") + _qs, {}, undefined);
    }
  },
  {
    name: "codes-of-conduct/get-all-codes-of-conduct",
    description: "Get all codes of conduct. Returns array of all GitHub's codes of conduct.",
    inputSchema: {"type":"object","properties":{},"required":[]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/codes_of_conduct") + _qs, {}, undefined);
    }
  },
  {
    name: "codes-of-conduct/get-conduct-code",
    description: "Get a code of conduct. Returns information about the specified GitHub code of conduct. Params: key (required).",
    inputSchema: {"type":"object","properties":{"key":{"type":"string"}},"required":["key"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/codes_of_conduct/" + String(args["key"])) + _qs, {}, undefined);
    }
  },
  {
    name: "credentials/revoke",
    description: "Revoke a list of credentials. Submit a list of credentials to be revoked.",
    inputSchema: {"type":"object","properties":{"body":{"type":"object","description":"Request body"}},"required":["body"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("POST", ("https://api.github.com/credentials/revoke") + _qs, {}, args["body"]);
    }
  },
  {
    name: "emojis/get",
    description: "Get emojis. Lists all the emojis available to use on GitHub.",
    inputSchema: {"type":"object","properties":{},"required":[]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/emojis") + _qs, {}, undefined);
    }
  },
  {
    name: "actions/get-actions-cache-retention-limit-for-enterprise",
    description: "Get GitHub Actions cache retention limit for an enterprise. Gets GitHub Actions cache retention limit for an enterprise. Params: enterprise (required).",
    inputSchema: {"type":"object","properties":{"enterprise":{"type":"string","description":"The slug version of the enterprise name."}},"required":["enterprise"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/enterprises/" + String(args["enterprise"]) + "/actions/cache/retention-limit") + _qs, {}, undefined);
    }
  },
  {
    name: "actions/set-actions-cache-retention-limit-for-enterprise",
    description: "Set GitHub Actions cache retention limit for an enterprise. Sets GitHub Actions cache retention limit for an enterprise. Params: enterprise (required).",
    inputSchema: {"type":"object","properties":{"enterprise":{"type":"string","description":"The slug version of the enterprise name."},"body":{"type":"object","description":"Request body"}},"required":["enterprise","body"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("PUT", ("https://api.github.com/enterprises/" + String(args["enterprise"]) + "/actions/cache/retention-limit") + _qs, {}, args["body"]);
    }
  },
  {
    name: "actions/get-actions-cache-storage-limit-for-enterprise",
    description: "Get GitHub Actions cache storage limit for an enterprise. Gets GitHub Actions cache storage limit for an enterprise. Params: enterprise (required).",
    inputSchema: {"type":"object","properties":{"enterprise":{"type":"string","description":"The slug version of the enterprise name."}},"required":["enterprise"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/enterprises/" + String(args["enterprise"]) + "/actions/cache/storage-limit") + _qs, {}, undefined);
    }
  },
  {
    name: "actions/set-actions-cache-storage-limit-for-enterprise",
    description: "Set GitHub Actions cache storage limit for an enterprise. Sets GitHub Actions cache storage limit for an enterprise. Params: enterprise (required).",
    inputSchema: {"type":"object","properties":{"enterprise":{"type":"string","description":"The slug version of the enterprise name."},"body":{"type":"object","description":"Request body"}},"required":["enterprise","body"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("PUT", ("https://api.github.com/enterprises/" + String(args["enterprise"]) + "/actions/cache/storage-limit") + _qs, {}, args["body"]);
    }
  },
  {
    name: "oidc/list-oidc-custom-property-inclusions-for-enterprise",
    description: "List OIDC custom property inclusions for an enterprise. Lists the repository custom properties that are included in the OIDC token for repository actions in an enterprise. Params: enterprise (required).",
    inputSchema: {"type":"object","properties":{"enterprise":{"type":"string","description":"The slug version of the enterprise name."}},"required":["enterprise"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/enterprises/" + String(args["enterprise"]) + "/actions/oidc/customization/properties/repo") + _qs, {}, undefined);
    }
  },
  {
    name: "oidc/create-oidc-custom-property-inclusion-for-enterprise",
    description: "Create an OIDC custom property inclusion for an enterprise. Adds a repository custom property to be included in the OIDC token for repository actions in an enterprise. Params: enterprise (required).",
    inputSchema: {"type":"object","properties":{"enterprise":{"type":"string","description":"The slug version of the enterprise name."},"body":{"type":"object","description":"Request body"}},"required":["enterprise","body"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("POST", ("https://api.github.com/enterprises/" + String(args["enterprise"]) + "/actions/oidc/customization/properties/repo") + _qs, {}, args["body"]);
    }
  },
  {
    name: "oidc/delete-oidc-custom-property-inclusion-for-enterprise",
    description: "Delete an OIDC custom property inclusion for an enterprise. Removes a repository custom property from being included in the OIDC token for repository actions in an enterprise. Params: enterprise (required), custom_property_name (required).",
    inputSchema: {"type":"object","properties":{"enterprise":{"type":"string","description":"The slug version of the enterprise name."},"custom_property_name":{"type":"string","description":"The name of the custom property to remove from OIDC token inclusion"}},"required":["enterprise","custom_property_name"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("DELETE", ("https://api.github.com/enterprises/" + String(args["enterprise"]) + "/actions/oidc/customization/properties/repo/" + String(args["custom_property_name"])) + _qs, {}, undefined);
    }
  },
  {
    name: "code-security/get-configurations-for-enterprise",
    description: "Get code security configurations for an enterprise. Lists all code security configurations available in an enterprise. Params: enterprise (required).",
    inputSchema: {"type":"object","properties":{"enterprise":{"type":"string","description":"The slug version of the enterprise name."},"per_page":{"type":"integer","default":30,"description":"The number of results per page (max 100). For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""},"before":{"type":"string","description":"A cursor, as given in the [Link header](https://docs.github.com/rest/guides/using-pagination-in-the-rest-api#using-link-headers). If specified, the query only searches for results before this cursor. For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""},"after":{"type":"string","description":"A cursor, as given in the [Link header](https://docs.github.com/rest/guides/using-pagination-in-the-rest-api#using-link-headers). If specified, the query only searches for results after this cursor. For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""}},"required":["enterprise"]},
    call: function(args) {
    var _q = new URLSearchParams();
    if (args["per_page"] !== undefined) _q.set("per_page", String(args["per_page"]));
    if (args["before"] !== undefined) _q.set("before", String(args["before"]));
    if (args["after"] !== undefined) _q.set("after", String(args["after"]));
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/enterprises/" + String(args["enterprise"]) + "/code-security/configurations") + _qs, {}, undefined);
    }
  },
  {
    name: "code-security/create-configuration-for-enterprise",
    description: "Create a code security configuration for an enterprise. Creates a code security configuration in an enterprise. Params: enterprise (required).",
    inputSchema: {"type":"object","properties":{"enterprise":{"type":"string","description":"The slug version of the enterprise name."},"body":{"type":"object","description":"Request body"}},"required":["enterprise","body"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("POST", ("https://api.github.com/enterprises/" + String(args["enterprise"]) + "/code-security/configurations") + _qs, {}, args["body"]);
    }
  },
  {
    name: "code-security/get-default-configurations-for-enterprise",
    description: "Get default code security configurations for an enterprise. Lists the default code security configurations for an enterprise. Params: enterprise (required).",
    inputSchema: {"type":"object","properties":{"enterprise":{"type":"string","description":"The slug version of the enterprise name."}},"required":["enterprise"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/enterprises/" + String(args["enterprise"]) + "/code-security/configurations/defaults") + _qs, {}, undefined);
    }
  },
  {
    name: "code-security/get-single-configuration-for-enterprise",
    description: "Retrieve a code security configuration of an enterprise. Gets a code security configuration available in an enterprise. Params: enterprise (required), configuration_id (required).",
    inputSchema: {"type":"object","properties":{"enterprise":{"type":"string","description":"The slug version of the enterprise name."},"configuration_id":{"type":"integer","description":"The unique identifier of the code security configuration."}},"required":["enterprise","configuration_id"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/enterprises/" + String(args["enterprise"]) + "/code-security/configurations/" + String(args["configuration_id"])) + _qs, {}, undefined);
    }
  },
  {
    name: "code-security/update-enterprise-configuration",
    description: "Update a custom code security configuration for an enterprise. Updates a code security configuration in an enterprise. Params: enterprise (required), configuration_id (required).",
    inputSchema: {"type":"object","properties":{"enterprise":{"type":"string","description":"The slug version of the enterprise name."},"configuration_id":{"type":"integer","description":"The unique identifier of the code security configuration."},"body":{"type":"object","description":"Request body"}},"required":["enterprise","configuration_id","body"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("PATCH", ("https://api.github.com/enterprises/" + String(args["enterprise"]) + "/code-security/configurations/" + String(args["configuration_id"])) + _qs, {}, args["body"]);
    }
  },
  {
    name: "code-security/delete-configuration-for-enterprise",
    description: "Delete a code security configuration for an enterprise. Deletes a code security configuration from an enterprise. Params: enterprise (required), configuration_id (required).",
    inputSchema: {"type":"object","properties":{"enterprise":{"type":"string","description":"The slug version of the enterprise name."},"configuration_id":{"type":"integer","description":"The unique identifier of the code security configuration."}},"required":["enterprise","configuration_id"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("DELETE", ("https://api.github.com/enterprises/" + String(args["enterprise"]) + "/code-security/configurations/" + String(args["configuration_id"])) + _qs, {}, undefined);
    }
  },
  {
    name: "code-security/attach-enterprise-configuration",
    description: "Attach an enterprise configuration to repositories. Attaches an enterprise code security configuration to repositories. Params: enterprise (required), configuration_id (required).",
    inputSchema: {"type":"object","properties":{"enterprise":{"type":"string","description":"The slug version of the enterprise name."},"configuration_id":{"type":"integer","description":"The unique identifier of the code security configuration."},"body":{"type":"object","description":"Request body"}},"required":["enterprise","configuration_id","body"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("POST", ("https://api.github.com/enterprises/" + String(args["enterprise"]) + "/code-security/configurations/" + String(args["configuration_id"]) + "/attach") + _qs, {}, args["body"]);
    }
  },
  {
    name: "code-security/set-configuration-as-default-for-enterprise",
    description: "Set a code security configuration as a default for an enterprise. Sets a code security configuration as a default to be applied to new repositories in your enterprise. Params: enterprise (required), configuration_id (required).",
    inputSchema: {"type":"object","properties":{"enterprise":{"type":"string","description":"The slug version of the enterprise name."},"configuration_id":{"type":"integer","description":"The unique identifier of the code security configuration."},"body":{"type":"object","description":"Request body"}},"required":["enterprise","configuration_id","body"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("PUT", ("https://api.github.com/enterprises/" + String(args["enterprise"]) + "/code-security/configurations/" + String(args["configuration_id"]) + "/defaults") + _qs, {}, args["body"]);
    }
  },
  {
    name: "code-security/get-repositories-for-enterprise-configuration",
    description: "Get repositories associated with an enterprise code security configuration. Lists the repositories associated with an enterprise code security configuration in an organization. Params: enterprise (required), configuration_id (required).",
    inputSchema: {"type":"object","properties":{"enterprise":{"type":"string","description":"The slug version of the enterprise name."},"configuration_id":{"type":"integer","description":"The unique identifier of the code security configuration."},"per_page":{"type":"integer","default":30,"description":"The number of results per page (max 100). For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""},"before":{"type":"string","description":"A cursor, as given in the [Link header](https://docs.github.com/rest/guides/using-pagination-in-the-rest-api#using-link-headers). If specified, the query only searches for results before this cursor. For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""},"after":{"type":"string","description":"A cursor, as given in the [Link header](https://docs.github.com/rest/guides/using-pagination-in-the-rest-api#using-link-headers). If specified, the query only searches for results after this cursor. For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""},"status":{"type":"string","default":"all","description":"A comma-separated list of statuses. If specified, only repositories with these attachment statuses will be returned.\n\nCan be: `all`, `attached`, `attaching`, `removed`, `enforced`, `failed`, `updating`, `removed_by_enterprise`"}},"required":["enterprise","configuration_id"]},
    call: function(args) {
    var _q = new URLSearchParams();
    if (args["per_page"] !== undefined) _q.set("per_page", String(args["per_page"]));
    if (args["before"] !== undefined) _q.set("before", String(args["before"]));
    if (args["after"] !== undefined) _q.set("after", String(args["after"]));
    if (args["status"] !== undefined) _q.set("status", String(args["status"]));
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/enterprises/" + String(args["enterprise"]) + "/code-security/configurations/" + String(args["configuration_id"]) + "/repositories") + _qs, {}, undefined);
    }
  },
  {
    name: "copilot/copilot-enterprise-one-day-usage-metrics",
    description: "Get Copilot enterprise usage metrics for a specific day. Use this endpoint to retrieve download links for the Copilot enterprise usage metrics report for a specific day. Params: enterprise (required), day (required).",
    inputSchema: {"type":"object","properties":{"enterprise":{"type":"string","description":"The slug version of the enterprise name."},"day":{"type":"string","format":"date","example":"2025-10-13","description":"The day to request data for, in `YYYY-MM-DD` format."}},"required":["enterprise","day"]},
    call: function(args) {
    var _q = new URLSearchParams();
    if (args["day"] !== undefined) _q.set("day", String(args["day"]));
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/enterprises/" + String(args["enterprise"]) + "/copilot/metrics/reports/enterprise-1-day") + _qs, {}, undefined);
    }
  },
  {
    name: "copilot/copilot-enterprise-usage-metrics",
    description: "Get Copilot enterprise usage metrics. Use this endpoint to retrieve download links for the latest 28-day enterprise Copilot usage metrics report. Params: enterprise (required).",
    inputSchema: {"type":"object","properties":{"enterprise":{"type":"string","description":"The slug version of the enterprise name."}},"required":["enterprise"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/enterprises/" + String(args["enterprise"]) + "/copilot/metrics/reports/enterprise-28-day/latest") + _qs, {}, undefined);
    }
  },
  {
    name: "copilot/copilot-enterprise-user-teams-one-day-report",
    description: "Get Copilot enterprise user-teams report for a specific day. Use this endpoint to retrieve download links for the Copilot enterprise user-teams report for a specific day. Params: enterprise (required), day (required).",
    inputSchema: {"type":"object","properties":{"enterprise":{"type":"string","description":"The slug version of the enterprise name."},"day":{"type":"string","format":"date","example":"2025-10-13","description":"The day to request data for, in `YYYY-MM-DD` format."}},"required":["enterprise","day"]},
    call: function(args) {
    var _q = new URLSearchParams();
    if (args["day"] !== undefined) _q.set("day", String(args["day"]));
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/enterprises/" + String(args["enterprise"]) + "/copilot/metrics/reports/user-teams-1-day") + _qs, {}, undefined);
    }
  },
  {
    name: "copilot/copilot-users-one-day-usage-metrics",
    description: "Get Copilot users usage metrics for a specific day. Use this endpoint to retrieve download links for the Copilot user usage metrics report for a specific day. Params: enterprise (required), day (required).",
    inputSchema: {"type":"object","properties":{"enterprise":{"type":"string","description":"The slug version of the enterprise name."},"day":{"type":"string","format":"date","example":"2025-10-13","description":"The day to request data for, in `YYYY-MM-DD` format."}},"required":["enterprise","day"]},
    call: function(args) {
    var _q = new URLSearchParams();
    if (args["day"] !== undefined) _q.set("day", String(args["day"]));
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/enterprises/" + String(args["enterprise"]) + "/copilot/metrics/reports/users-1-day") + _qs, {}, undefined);
    }
  },
  {
    name: "copilot/copilot-users-usage-metrics",
    description: "Get Copilot users usage metrics. Use this endpoint to retrieve download links for the latest 28-day enterprise users Copilot usage metrics report. Params: enterprise (required).",
    inputSchema: {"type":"object","properties":{"enterprise":{"type":"string","description":"The slug version of the enterprise name."}},"required":["enterprise"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/enterprises/" + String(args["enterprise"]) + "/copilot/metrics/reports/users-28-day/latest") + _qs, {}, undefined);
    }
  },
  {
    name: "copilot/set-enterprise-coding-agent-policy",
    description: "Set the coding agent policy for an enterprise. Sets the policy for Copilot cloud agent usage across an enterprise. Params: enterprise (required).",
    inputSchema: {"type":"object","properties":{"enterprise":{"type":"string","description":"The slug version of the enterprise name."},"body":{"type":"object","description":"The coding agent policy configuration"}},"required":["enterprise","body"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("PUT", ("https://api.github.com/enterprises/" + String(args["enterprise"]) + "/copilot/policies/coding_agent") + _qs, {}, args["body"]);
    }
  },
  {
    name: "copilot/add-organizations-to-enterprise-coding-agent-policy",
    description: "Add organizations to the enterprise coding agent policy. Enables Copilot cloud agent for the specified organizations within the enterprise. Params: enterprise (required).",
    inputSchema: {"type":"object","properties":{"enterprise":{"type":"string","description":"The slug version of the enterprise name."},"body":{"type":"object","description":"The organizations to enable Copilot cloud agent for"}},"required":["enterprise","body"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("POST", ("https://api.github.com/enterprises/" + String(args["enterprise"]) + "/copilot/policies/coding_agent/organizations") + _qs, {}, args["body"]);
    }
  },
  {
    name: "copilot/remove-organizations-from-enterprise-coding-agent-policy",
    description: "Remove organizations from the enterprise coding agent policy. Disables Copilot cloud agent for the specified organizations within the enterprise. Params: enterprise (required).",
    inputSchema: {"type":"object","properties":{"enterprise":{"type":"string","description":"The slug version of the enterprise name."},"body":{"type":"object","description":"The organizations to disable Copilot cloud agent for"}},"required":["enterprise","body"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("DELETE", ("https://api.github.com/enterprises/" + String(args["enterprise"]) + "/copilot/policies/coding_agent/organizations") + _qs, {}, args["body"]);
    }
  },
  {
    name: "dependabot/list-alerts-for-enterprise",
    description: "List Dependabot alerts for an enterprise. Lists Dependabot alerts for repositories that are owned by the specified enterprise. Params: enterprise (required).",
    inputSchema: {"type":"object","properties":{"enterprise":{"type":"string","description":"The slug version of the enterprise name."},"classification":{"type":"string","description":"A comma-separated list of vulnerability classifications. If specified, only alerts for vulnerabilities with these classifications will be returned.\n\nCan be: `malware`, `general`"},"state":{"type":"string","description":"A comma-separated list of states. If specified, only alerts with these states will be returned.\n\nCan be: `auto_dismissed`, `dismissed`, `fixed`, `open`"},"severity":{"type":"string","description":"A comma-separated list of severities. If specified, only alerts with these severities will be returned.\n\nCan be: `low`, `medium`, `high`, `critical`"},"ecosystem":{"type":"string","description":"A comma-separated list of ecosystems. If specified, only alerts for these ecosystems will be returned.\n\nCan be: `composer`, `go`, `maven`, `npm`, `nuget`, `pip`, `pub`, `rubygems`, `rust`"},"package":{"type":"string","description":"A comma-separated list of package names. If specified, only alerts for these packages will be returned."},"epss_percentage":{"type":"string","description":"CVE Exploit Prediction Scoring System (EPSS) percentage. Can be specified as:\n- An exact number (`n`)\n- Comparators such as `>n`, `<n`, `>=n`, `<=n`\n- A range like `n..n`, where `n` is a number from 0.0 to 1.0\n\nFilters the list of alerts based on EPSS percentages. If specified, only alerts with the provided EPSS percentages will be returned."},"has":{"oneOf":[{"type":"string"},{"type":"array","items":{"type":"string","enum":["patch"]}}],"description":"Filters the list of alerts based on whether the alert has the given value. If specified, only alerts meeting this criterion will be returned.\nMultiple `has` filters can be passed to filter for alerts that have all of the values. Currently, only `patch` is supported."},"assignee":{"type":"string","description":"Filter alerts by assignees.\nProvide a comma-separated list of user handles (e.g., `octocat` or `octocat,hubot`) to return alerts assigned to any of the specified users.\nUse `*` to list alerts with at least one assignee or `none` to list alerts with no assignees."},"scope":{"type":"string","enum":["development","runtime"],"description":"The scope of the vulnerable dependency. If specified, only alerts with this scope will be returned."},"sort":{"type":"string","enum":["created","updated","epss_percentage"],"default":"created","description":"The property by which to sort the results.\n`created` means when the alert was created.\n`updated` means when the alert's state last changed.\n`epss_percentage` sorts alerts by the Exploit Prediction Scoring System (EPSS) percentage."},"direction":{"type":"string","enum":["asc","desc"],"default":"desc","description":"The direction to sort the results by."},"before":{"type":"string","description":"A cursor, as given in the [Link header](https://docs.github.com/rest/guides/using-pagination-in-the-rest-api#using-link-headers). If specified, the query only searches for results before this cursor. For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""},"after":{"type":"string","description":"A cursor, as given in the [Link header](https://docs.github.com/rest/guides/using-pagination-in-the-rest-api#using-link-headers). If specified, the query only searches for results after this cursor. For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""},"per_page":{"type":"integer","default":30,"description":"The number of results per page (max 100). For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""}},"required":["enterprise"]},
    call: function(args) {
    var _q = new URLSearchParams();
    if (args["classification"] !== undefined) _q.set("classification", String(args["classification"]));
    if (args["state"] !== undefined) _q.set("state", String(args["state"]));
    if (args["severity"] !== undefined) _q.set("severity", String(args["severity"]));
    if (args["ecosystem"] !== undefined) _q.set("ecosystem", String(args["ecosystem"]));
    if (args["package"] !== undefined) _q.set("package", String(args["package"]));
    if (args["epss_percentage"] !== undefined) _q.set("epss_percentage", String(args["epss_percentage"]));
    if (args["has"] !== undefined) _q.set("has", String(args["has"]));
    if (args["assignee"] !== undefined) _q.set("assignee", String(args["assignee"]));
    if (args["scope"] !== undefined) _q.set("scope", String(args["scope"]));
    if (args["sort"] !== undefined) _q.set("sort", String(args["sort"]));
    if (args["direction"] !== undefined) _q.set("direction", String(args["direction"]));
    if (args["before"] !== undefined) _q.set("before", String(args["before"]));
    if (args["after"] !== undefined) _q.set("after", String(args["after"]));
    if (args["per_page"] !== undefined) _q.set("per_page", String(args["per_page"]));
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/enterprises/" + String(args["enterprise"]) + "/dependabot/alerts") + _qs, {}, undefined);
    }
  },
  {
    name: "dependabot/repository-access-for-enterprise",
    description: "Lists the repositories Dependabot can access in an enterprise. Lists repositories that enterprise admins have allowed Dependabot to access when updating dependencies across organizations in the enterprise. Params: enterprise (required).",
    inputSchema: {"type":"object","properties":{"enterprise":{"type":"string","description":"The slug version of the enterprise name."},"page":{"type":"integer","minimum":1,"default":1,"description":"The page number of results to fetch."},"per_page":{"type":"integer","minimum":1,"maximum":100,"default":30,"description":"Number of results per page."}},"required":["enterprise"]},
    call: function(args) {
    var _q = new URLSearchParams();
    if (args["page"] !== undefined) _q.set("page", String(args["page"]));
    if (args["per_page"] !== undefined) _q.set("per_page", String(args["per_page"]));
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/enterprises/" + String(args["enterprise"]) + "/dependabot/repository-access") + _qs, {}, undefined);
    }
  },
  {
    name: "dependabot/update-repository-access-for-enterprise",
    description: "Updates Dependabot's repository access list for an enterprise. Updates repositories according to the list of repositories that enterprise admins have given Dependabot access to when they've updated dependencies across organizations in the enterprise. Params: enterprise (required).",
    inputSchema: {"type":"object","properties":{"enterprise":{"type":"string","description":"The slug version of the enterprise name."},"body":{"type":"object","description":"Request body"}},"required":["enterprise","body"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("PATCH", ("https://api.github.com/enterprises/" + String(args["enterprise"]) + "/dependabot/repository-access") + _qs, {}, args["body"]);
    }
  },
  {
    name: "dependabot/set-repository-access-default-level-for-enterprise",
    description: "Set the default repository access level for Dependabot in an enterprise. Sets the default level of repository access Dependabot will have while performing an update across organizations in the enterprise. Params: enterprise (required).",
    inputSchema: {"type":"object","properties":{"enterprise":{"type":"string","description":"The slug version of the enterprise name."},"body":{"type":"object","description":"Request body"}},"required":["enterprise","body"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("PUT", ("https://api.github.com/enterprises/" + String(args["enterprise"]) + "/dependabot/repository-access/default-level") + _qs, {}, args["body"]);
    }
  },
  {
    name: "enterprise-teams/list",
    description: "List enterprise teams. List all teams in the enterprise for the authenticated user. Params: enterprise (required).",
    inputSchema: {"type":"object","properties":{"enterprise":{"type":"string","description":"The slug version of the enterprise name."},"per_page":{"type":"integer","default":30,"description":"The number of results per page (max 100). For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""},"page":{"type":"integer","default":1,"description":"The page number of the results to fetch. For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""}},"required":["enterprise"]},
    call: function(args) {
    var _q = new URLSearchParams();
    if (args["per_page"] !== undefined) _q.set("per_page", String(args["per_page"]));
    if (args["page"] !== undefined) _q.set("page", String(args["page"]));
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/enterprises/" + String(args["enterprise"]) + "/teams") + _qs, {}, undefined);
    }
  },
  {
    name: "enterprise-teams/create",
    description: "Create an enterprise team. To create an enterprise team, the authenticated user must be an owner of the enterprise. Params: enterprise (required).",
    inputSchema: {"type":"object","properties":{"enterprise":{"type":"string","description":"The slug version of the enterprise name."},"body":{"type":"object","description":"Request body"}},"required":["enterprise","body"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("POST", ("https://api.github.com/enterprises/" + String(args["enterprise"]) + "/teams") + _qs, {}, args["body"]);
    }
  },
  {
    name: "enterprise-team-memberships/list",
    description: "List members in an enterprise team. Lists all team members in an enterprise team. Params: enterprise (required), enterprise-team (required).",
    inputSchema: {"type":"object","properties":{"enterprise":{"type":"string","description":"The slug version of the enterprise name."},"enterprise-team":{"type":"string","description":"The slug version of the enterprise team name. You can also substitute this value with the enterprise team id."},"per_page":{"type":"integer","default":30,"description":"The number of results per page (max 100). For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""},"page":{"type":"integer","default":1,"description":"The page number of the results to fetch. For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""}},"required":["enterprise","enterprise-team"]},
    call: function(args) {
    var _q = new URLSearchParams();
    if (args["per_page"] !== undefined) _q.set("per_page", String(args["per_page"]));
    if (args["page"] !== undefined) _q.set("page", String(args["page"]));
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/enterprises/" + String(args["enterprise"]) + "/teams/" + String(args["enterprise-team"]) + "/memberships") + _qs, {}, undefined);
    }
  },
  {
    name: "enterprise-team-memberships/bulk-add",
    description: "Bulk add team members. Add multiple team members to an enterprise team. Params: enterprise (required), enterprise-team (required).",
    inputSchema: {"type":"object","properties":{"enterprise":{"type":"string","description":"The slug version of the enterprise name."},"enterprise-team":{"type":"string","description":"The slug version of the enterprise team name. You can also substitute this value with the enterprise team id."},"body":{"type":"object","description":"Request body"}},"required":["enterprise","enterprise-team","body"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("POST", ("https://api.github.com/enterprises/" + String(args["enterprise"]) + "/teams/" + String(args["enterprise-team"]) + "/memberships/add") + _qs, {}, args["body"]);
    }
  },
  {
    name: "enterprise-team-memberships/bulk-remove",
    description: "Bulk remove team members. Remove multiple team members from an enterprise team. Params: enterprise (required), enterprise-team (required).",
    inputSchema: {"type":"object","properties":{"enterprise":{"type":"string","description":"The slug version of the enterprise name."},"enterprise-team":{"type":"string","description":"The slug version of the enterprise team name. You can also substitute this value with the enterprise team id."},"body":{"type":"object","description":"Request body"}},"required":["enterprise","enterprise-team","body"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("POST", ("https://api.github.com/enterprises/" + String(args["enterprise"]) + "/teams/" + String(args["enterprise-team"]) + "/memberships/remove") + _qs, {}, args["body"]);
    }
  },
  {
    name: "enterprise-team-memberships/get",
    description: "Get enterprise team membership. Returns whether the user is a member of the enterprise team. Params: enterprise (required), enterprise-team (required), username (required).",
    inputSchema: {"type":"object","properties":{"enterprise":{"type":"string","description":"The slug version of the enterprise name."},"enterprise-team":{"type":"string","description":"The slug version of the enterprise team name. You can also substitute this value with the enterprise team id."},"username":{"type":"string","description":"The handle for the GitHub user account."}},"required":["enterprise","enterprise-team","username"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/enterprises/" + String(args["enterprise"]) + "/teams/" + String(args["enterprise-team"]) + "/memberships/" + String(args["username"])) + _qs, {}, undefined);
    }
  },
  {
    name: "enterprise-team-memberships/add",
    description: "Add team member. Add a team member to an enterprise team. Params: enterprise (required), enterprise-team (required), username (required).",
    inputSchema: {"type":"object","properties":{"enterprise":{"type":"string","description":"The slug version of the enterprise name."},"enterprise-team":{"type":"string","description":"The slug version of the enterprise team name. You can also substitute this value with the enterprise team id."},"username":{"type":"string","description":"The handle for the GitHub user account."}},"required":["enterprise","enterprise-team","username"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("PUT", ("https://api.github.com/enterprises/" + String(args["enterprise"]) + "/teams/" + String(args["enterprise-team"]) + "/memberships/" + String(args["username"])) + _qs, {}, undefined);
    }
  },
  {
    name: "enterprise-team-memberships/remove",
    description: "Remove team membership. Remove membership of a specific user from a particular team in an enterprise. Params: enterprise (required), enterprise-team (required), username (required).",
    inputSchema: {"type":"object","properties":{"enterprise":{"type":"string","description":"The slug version of the enterprise name."},"enterprise-team":{"type":"string","description":"The slug version of the enterprise team name. You can also substitute this value with the enterprise team id."},"username":{"type":"string","description":"The handle for the GitHub user account."}},"required":["enterprise","enterprise-team","username"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("DELETE", ("https://api.github.com/enterprises/" + String(args["enterprise"]) + "/teams/" + String(args["enterprise-team"]) + "/memberships/" + String(args["username"])) + _qs, {}, undefined);
    }
  },
  {
    name: "enterprise-team-organizations/get-assignments",
    description: "Get organization assignments. Get all organizations assigned to an enterprise team. Params: enterprise (required), enterprise-team (required).",
    inputSchema: {"type":"object","properties":{"enterprise":{"type":"string","description":"The slug version of the enterprise name."},"enterprise-team":{"type":"string","description":"The slug version of the enterprise team name. You can also substitute this value with the enterprise team id."},"per_page":{"type":"integer","default":30,"description":"The number of results per page (max 100). For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""},"page":{"type":"integer","default":1,"description":"The page number of the results to fetch. For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""}},"required":["enterprise","enterprise-team"]},
    call: function(args) {
    var _q = new URLSearchParams();
    if (args["per_page"] !== undefined) _q.set("per_page", String(args["per_page"]));
    if (args["page"] !== undefined) _q.set("page", String(args["page"]));
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/enterprises/" + String(args["enterprise"]) + "/teams/" + String(args["enterprise-team"]) + "/organizations") + _qs, {}, undefined);
    }
  },
  {
    name: "enterprise-team-organizations/bulk-add",
    description: "Add organization assignments. Assign an enterprise team to multiple organizations. Params: enterprise (required), enterprise-team (required).",
    inputSchema: {"type":"object","properties":{"enterprise":{"type":"string","description":"The slug version of the enterprise name."},"enterprise-team":{"type":"string","description":"The slug version of the enterprise team name. You can also substitute this value with the enterprise team id."},"body":{"type":"object","description":"Request body"}},"required":["enterprise","enterprise-team","body"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("POST", ("https://api.github.com/enterprises/" + String(args["enterprise"]) + "/teams/" + String(args["enterprise-team"]) + "/organizations/add") + _qs, {}, args["body"]);
    }
  },
  {
    name: "enterprise-team-organizations/bulk-remove",
    description: "Remove organization assignments. Unassign an enterprise team from multiple organizations. Params: enterprise (required), enterprise-team (required).",
    inputSchema: {"type":"object","properties":{"enterprise":{"type":"string","description":"The slug version of the enterprise name."},"enterprise-team":{"type":"string","description":"The slug version of the enterprise team name. You can also substitute this value with the enterprise team id."},"body":{"type":"object","description":"Request body"}},"required":["enterprise","enterprise-team","body"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("POST", ("https://api.github.com/enterprises/" + String(args["enterprise"]) + "/teams/" + String(args["enterprise-team"]) + "/organizations/remove") + _qs, {}, args["body"]);
    }
  },
  {
    name: "enterprise-team-organizations/get-assignment",
    description: "Get organization assignment. Check if an enterprise team is assigned to an organization. Params: enterprise (required), enterprise-team (required), org (required).",
    inputSchema: {"type":"object","properties":{"enterprise":{"type":"string","description":"The slug version of the enterprise name."},"enterprise-team":{"type":"string","description":"The slug version of the enterprise team name. You can also substitute this value with the enterprise team id."},"org":{"type":"string","description":"The organization name. The name is not case sensitive."}},"required":["enterprise","enterprise-team","org"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/enterprises/" + String(args["enterprise"]) + "/teams/" + String(args["enterprise-team"]) + "/organizations/" + String(args["org"])) + _qs, {}, undefined);
    }
  },
  {
    name: "enterprise-team-organizations/add",
    description: "Add an organization assignment. Assign an enterprise team to an organization. Params: enterprise (required), enterprise-team (required), org (required).",
    inputSchema: {"type":"object","properties":{"enterprise":{"type":"string","description":"The slug version of the enterprise name."},"enterprise-team":{"type":"string","description":"The slug version of the enterprise team name. You can also substitute this value with the enterprise team id."},"org":{"type":"string","description":"The organization name. The name is not case sensitive."}},"required":["enterprise","enterprise-team","org"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("PUT", ("https://api.github.com/enterprises/" + String(args["enterprise"]) + "/teams/" + String(args["enterprise-team"]) + "/organizations/" + String(args["org"])) + _qs, {}, undefined);
    }
  },
  {
    name: "enterprise-team-organizations/delete",
    description: "Delete an organization assignment. Unassign an enterprise team from an organization. Params: enterprise (required), enterprise-team (required), org (required).",
    inputSchema: {"type":"object","properties":{"enterprise":{"type":"string","description":"The slug version of the enterprise name."},"enterprise-team":{"type":"string","description":"The slug version of the enterprise team name. You can also substitute this value with the enterprise team id."},"org":{"type":"string","description":"The organization name. The name is not case sensitive."}},"required":["enterprise","enterprise-team","org"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("DELETE", ("https://api.github.com/enterprises/" + String(args["enterprise"]) + "/teams/" + String(args["enterprise-team"]) + "/organizations/" + String(args["org"])) + _qs, {}, undefined);
    }
  },
  {
    name: "enterprise-teams/get",
    description: "Get an enterprise team. Gets a team using the team's slug. Params: enterprise (required), team_slug (required).",
    inputSchema: {"type":"object","properties":{"enterprise":{"type":"string","description":"The slug version of the enterprise name."},"team_slug":{"type":"string","description":"The slug of the team name."}},"required":["enterprise","team_slug"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/enterprises/" + String(args["enterprise"]) + "/teams/" + String(args["team_slug"])) + _qs, {}, undefined);
    }
  },
  {
    name: "enterprise-teams/update",
    description: "Update an enterprise team. To edit a team, the authenticated user must be an enterprise owner. Params: enterprise (required), team_slug (required).",
    inputSchema: {"type":"object","properties":{"enterprise":{"type":"string","description":"The slug version of the enterprise name."},"team_slug":{"type":"string","description":"The slug of the team name."},"body":{"type":"object","description":"Request body"}},"required":["enterprise","team_slug","body"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("PATCH", ("https://api.github.com/enterprises/" + String(args["enterprise"]) + "/teams/" + String(args["team_slug"])) + _qs, {}, args["body"]);
    }
  },
  {
    name: "enterprise-teams/delete",
    description: "Delete an enterprise team. To delete an enterprise team, the authenticated user must be an enterprise owner. Params: enterprise (required), team_slug (required).",
    inputSchema: {"type":"object","properties":{"enterprise":{"type":"string","description":"The slug version of the enterprise name."},"team_slug":{"type":"string","description":"The slug of the team name."}},"required":["enterprise","team_slug"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("DELETE", ("https://api.github.com/enterprises/" + String(args["enterprise"]) + "/teams/" + String(args["team_slug"])) + _qs, {}, undefined);
    }
  },
  {
    name: "activity/list-public-events",
    description: "List public events. > [!NOTE]\n> This API is not built to serve real-time use cases.",
    inputSchema: {"type":"object","properties":{"per_page":{"type":"integer","default":15,"description":"The number of results per page (max 100). For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""},"page":{"type":"integer","default":1,"description":"The page number of the results to fetch. For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""}},"required":[]},
    call: function(args) {
    var _q = new URLSearchParams();
    if (args["per_page"] !== undefined) _q.set("per_page", String(args["per_page"]));
    if (args["page"] !== undefined) _q.set("page", String(args["page"]));
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/events") + _qs, {}, undefined);
    }
  },
  {
    name: "activity/get-feeds",
    description: "Get feeds. Lists the feeds available to the authenticated user.",
    inputSchema: {"type":"object","properties":{},"required":[]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/feeds") + _qs, {}, undefined);
    }
  },
  {
    name: "gists/list",
    description: "List gists for the authenticated user. Lists the authenticated user's gists or if called anonymously, this endpoint returns all public gists:.",
    inputSchema: {"type":"object","properties":{"since":{"type":"string","format":"date-time","description":"Only show results that were last updated after the given time. This is a timestamp in [ISO 8601](https://en.wikipedia.org/wiki/ISO_8601) format: `YYYY-MM-DDTHH:MM:SSZ`."},"per_page":{"type":"integer","default":30,"description":"The number of results per page (max 100). For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""},"page":{"type":"integer","default":1,"description":"The page number of the results to fetch. For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""}},"required":[]},
    call: function(args) {
    var _q = new URLSearchParams();
    if (args["since"] !== undefined) _q.set("since", String(args["since"]));
    if (args["per_page"] !== undefined) _q.set("per_page", String(args["per_page"]));
    if (args["page"] !== undefined) _q.set("page", String(args["page"]));
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/gists") + _qs, {}, undefined);
    }
  },
  {
    name: "gists/create",
    description: "Create a gist. Allows you to add a new gist with one or more files.",
    inputSchema: {"type":"object","properties":{"body":{"type":"object","description":"Request body"}},"required":["body"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("POST", ("https://api.github.com/gists") + _qs, {}, args["body"]);
    }
  },
  {
    name: "gists/list-public",
    description: "List public gists. List public gists sorted by most recently updated to least recently updated.",
    inputSchema: {"type":"object","properties":{"since":{"type":"string","format":"date-time","description":"Only show results that were last updated after the given time. This is a timestamp in [ISO 8601](https://en.wikipedia.org/wiki/ISO_8601) format: `YYYY-MM-DDTHH:MM:SSZ`."},"per_page":{"type":"integer","default":30,"description":"The number of results per page (max 100). For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""},"page":{"type":"integer","default":1,"description":"The page number of the results to fetch. For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""}},"required":[]},
    call: function(args) {
    var _q = new URLSearchParams();
    if (args["since"] !== undefined) _q.set("since", String(args["since"]));
    if (args["per_page"] !== undefined) _q.set("per_page", String(args["per_page"]));
    if (args["page"] !== undefined) _q.set("page", String(args["page"]));
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/gists/public") + _qs, {}, undefined);
    }
  },
  {
    name: "gists/list-starred",
    description: "List starred gists. List the authenticated user's starred gists:.",
    inputSchema: {"type":"object","properties":{"since":{"type":"string","format":"date-time","description":"Only show results that were last updated after the given time. This is a timestamp in [ISO 8601](https://en.wikipedia.org/wiki/ISO_8601) format: `YYYY-MM-DDTHH:MM:SSZ`."},"per_page":{"type":"integer","default":30,"description":"The number of results per page (max 100). For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""},"page":{"type":"integer","default":1,"description":"The page number of the results to fetch. For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""}},"required":[]},
    call: function(args) {
    var _q = new URLSearchParams();
    if (args["since"] !== undefined) _q.set("since", String(args["since"]));
    if (args["per_page"] !== undefined) _q.set("per_page", String(args["per_page"]));
    if (args["page"] !== undefined) _q.set("page", String(args["page"]));
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/gists/starred") + _qs, {}, undefined);
    }
  },
  {
    name: "gists/get",
    description: "Get a gist. Gets a specified gist. Params: gist_id (required).",
    inputSchema: {"type":"object","properties":{"gist_id":{"type":"string","description":"The unique identifier of the gist."}},"required":["gist_id"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/gists/" + String(args["gist_id"])) + _qs, {}, undefined);
    }
  },
  {
    name: "gists/update",
    description: "Update a gist. Allows you to update a gist's description and to update, delete, or rename gist files. Params: gist_id (required).",
    inputSchema: {"type":"object","properties":{"gist_id":{"type":"string","description":"The unique identifier of the gist."},"body":{"type":"object","description":"Request body"}},"required":["gist_id","body"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("PATCH", ("https://api.github.com/gists/" + String(args["gist_id"])) + _qs, {}, args["body"]);
    }
  },
  {
    name: "gists/delete",
    description: "Delete a gist. Params: gist_id (required).",
    inputSchema: {"type":"object","properties":{"gist_id":{"type":"string","description":"The unique identifier of the gist."}},"required":["gist_id"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("DELETE", ("https://api.github.com/gists/" + String(args["gist_id"])) + _qs, {}, undefined);
    }
  },
  {
    name: "gists/list-comments",
    description: "List gist comments. Lists the comments on a gist. Params: gist_id (required).",
    inputSchema: {"type":"object","properties":{"gist_id":{"type":"string","description":"The unique identifier of the gist."},"per_page":{"type":"integer","default":30,"description":"The number of results per page (max 100). For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""},"page":{"type":"integer","default":1,"description":"The page number of the results to fetch. For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""}},"required":["gist_id"]},
    call: function(args) {
    var _q = new URLSearchParams();
    if (args["per_page"] !== undefined) _q.set("per_page", String(args["per_page"]));
    if (args["page"] !== undefined) _q.set("page", String(args["page"]));
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/gists/" + String(args["gist_id"]) + "/comments") + _qs, {}, undefined);
    }
  },
  {
    name: "gists/create-comment",
    description: "Create a gist comment. Creates a comment on a gist. Params: gist_id (required).",
    inputSchema: {"type":"object","properties":{"gist_id":{"type":"string","description":"The unique identifier of the gist."},"body":{"type":"object","description":"Request body"}},"required":["gist_id","body"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("POST", ("https://api.github.com/gists/" + String(args["gist_id"]) + "/comments") + _qs, {}, args["body"]);
    }
  },
  {
    name: "gists/get-comment",
    description: "Get a gist comment. Gets a comment on a gist. Params: gist_id (required), comment_id (required).",
    inputSchema: {"type":"object","properties":{"gist_id":{"type":"string","description":"The unique identifier of the gist."},"comment_id":{"type":"integer","format":"int64","description":"The unique identifier of the comment."}},"required":["gist_id","comment_id"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/gists/" + String(args["gist_id"]) + "/comments/" + String(args["comment_id"])) + _qs, {}, undefined);
    }
  },
  {
    name: "gists/update-comment",
    description: "Update a gist comment. Updates a comment on a gist. Params: gist_id (required), comment_id (required).",
    inputSchema: {"type":"object","properties":{"gist_id":{"type":"string","description":"The unique identifier of the gist."},"comment_id":{"type":"integer","format":"int64","description":"The unique identifier of the comment."},"body":{"type":"object","description":"Request body"}},"required":["gist_id","comment_id","body"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("PATCH", ("https://api.github.com/gists/" + String(args["gist_id"]) + "/comments/" + String(args["comment_id"])) + _qs, {}, args["body"]);
    }
  },
  {
    name: "gists/delete-comment",
    description: "Delete a gist comment. Params: gist_id (required), comment_id (required).",
    inputSchema: {"type":"object","properties":{"gist_id":{"type":"string","description":"The unique identifier of the gist."},"comment_id":{"type":"integer","format":"int64","description":"The unique identifier of the comment."}},"required":["gist_id","comment_id"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("DELETE", ("https://api.github.com/gists/" + String(args["gist_id"]) + "/comments/" + String(args["comment_id"])) + _qs, {}, undefined);
    }
  },
  {
    name: "gists/list-commits",
    description: "List gist commits. Params: gist_id (required).",
    inputSchema: {"type":"object","properties":{"gist_id":{"type":"string","description":"The unique identifier of the gist."},"per_page":{"type":"integer","default":30,"description":"The number of results per page (max 100). For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""},"page":{"type":"integer","default":1,"description":"The page number of the results to fetch. For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""}},"required":["gist_id"]},
    call: function(args) {
    var _q = new URLSearchParams();
    if (args["per_page"] !== undefined) _q.set("per_page", String(args["per_page"]));
    if (args["page"] !== undefined) _q.set("page", String(args["page"]));
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/gists/" + String(args["gist_id"]) + "/commits") + _qs, {}, undefined);
    }
  },
  {
    name: "gists/list-forks",
    description: "List gist forks. Params: gist_id (required).",
    inputSchema: {"type":"object","properties":{"gist_id":{"type":"string","description":"The unique identifier of the gist."},"per_page":{"type":"integer","default":30,"description":"The number of results per page (max 100). For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""},"page":{"type":"integer","default":1,"description":"The page number of the results to fetch. For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""}},"required":["gist_id"]},
    call: function(args) {
    var _q = new URLSearchParams();
    if (args["per_page"] !== undefined) _q.set("per_page", String(args["per_page"]));
    if (args["page"] !== undefined) _q.set("page", String(args["page"]));
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/gists/" + String(args["gist_id"]) + "/forks") + _qs, {}, undefined);
    }
  },
  {
    name: "gists/fork",
    description: "Fork a gist. Params: gist_id (required).",
    inputSchema: {"type":"object","properties":{"gist_id":{"type":"string","description":"The unique identifier of the gist."}},"required":["gist_id"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("POST", ("https://api.github.com/gists/" + String(args["gist_id"]) + "/forks") + _qs, {}, undefined);
    }
  },
  {
    name: "gists/check-is-starred",
    description: "Check if a gist is starred. Params: gist_id (required).",
    inputSchema: {"type":"object","properties":{"gist_id":{"type":"string","description":"The unique identifier of the gist."}},"required":["gist_id"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/gists/" + String(args["gist_id"]) + "/star") + _qs, {}, undefined);
    }
  },
  {
    name: "gists/star",
    description: "Star a gist. Note that you'll need to set `Content-Length` to zero when calling out to this endpoint. Params: gist_id (required).",
    inputSchema: {"type":"object","properties":{"gist_id":{"type":"string","description":"The unique identifier of the gist."}},"required":["gist_id"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("PUT", ("https://api.github.com/gists/" + String(args["gist_id"]) + "/star") + _qs, {}, undefined);
    }
  },
  {
    name: "gists/unstar",
    description: "Unstar a gist. Params: gist_id (required).",
    inputSchema: {"type":"object","properties":{"gist_id":{"type":"string","description":"The unique identifier of the gist."}},"required":["gist_id"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("DELETE", ("https://api.github.com/gists/" + String(args["gist_id"]) + "/star") + _qs, {}, undefined);
    }
  },
  {
    name: "gists/get-revision",
    description: "Get a gist revision. Gets a specified gist revision. Params: gist_id (required), sha (required).",
    inputSchema: {"type":"object","properties":{"gist_id":{"type":"string","description":"The unique identifier of the gist."},"sha":{"type":"string"}},"required":["gist_id","sha"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/gists/" + String(args["gist_id"]) + "/" + String(args["sha"])) + _qs, {}, undefined);
    }
  },
  {
    name: "gitignore/get-all-templates",
    description: "Get all gitignore templates. List all templates available to pass as an option when [creating a repository](https://docs.github.com/rest/repos/repos#create-a-repository-for-the-authenticated-user).",
    inputSchema: {"type":"object","properties":{},"required":[]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/gitignore/templates") + _qs, {}, undefined);
    }
  },
  {
    name: "gitignore/get-template",
    description: "Get a gitignore template. Get the content of a gitignore template. Params: name (required).",
    inputSchema: {"type":"object","properties":{"name":{"type":"string"}},"required":["name"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/gitignore/templates/" + String(args["name"])) + _qs, {}, undefined);
    }
  },
  {
    name: "apps/list-repos-accessible-to-installation",
    description: "List repositories accessible to the app installation. List repositories that an app installation can access.",
    inputSchema: {"type":"object","properties":{"per_page":{"type":"integer","default":30,"description":"The number of results per page (max 100). For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""},"page":{"type":"integer","default":1,"description":"The page number of the results to fetch. For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""}},"required":[]},
    call: function(args) {
    var _q = new URLSearchParams();
    if (args["per_page"] !== undefined) _q.set("per_page", String(args["per_page"]));
    if (args["page"] !== undefined) _q.set("page", String(args["page"]));
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/installation/repositories") + _qs, {}, undefined);
    }
  },
  {
    name: "apps/revoke-installation-access-token",
    description: "Revoke an installation access token. Revokes the installation token you're using to authenticate as an installation and access this endpoint.",
    inputSchema: {"type":"object","properties":{},"required":[]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("DELETE", ("https://api.github.com/installation/token") + _qs, {}, undefined);
    }
  },
  {
    name: "issues/list",
    description: "List issues assigned to the authenticated user. List issues assigned to the authenticated user across all visible repositories including owned repositories, member\nrepositories, and organization repositories.",
    inputSchema: {"type":"object","properties":{"filter":{"type":"string","enum":["assigned","created","mentioned","subscribed","repos","all"],"default":"assigned","description":"Indicates which sorts of issues to return. `assigned` means issues assigned to you. `created` means issues created by you. `mentioned` means issues mentioning you. `subscribed` means issues you're subscribed to updates for. `all` or `repos` means all issues you can see, regardless of participation or creation."},"state":{"type":"string","enum":["open","closed","all"],"default":"open","description":"Indicates the state of the issues to return."},"labels":{"type":"string","description":"A list of comma separated label names. Example: `bug,ui,@high`"},"sort":{"type":"string","enum":["created","updated","comments"],"default":"created","description":"What to sort results by."},"direction":{"type":"string","enum":["asc","desc"],"default":"desc","description":"The direction to sort the results by."},"since":{"type":"string","format":"date-time","description":"Only show results that were last updated after the given time. This is a timestamp in [ISO 8601](https://en.wikipedia.org/wiki/ISO_8601) format: `YYYY-MM-DDTHH:MM:SSZ`."},"collab":{"type":"boolean"},"orgs":{"type":"boolean"},"owned":{"type":"boolean"},"pulls":{"type":"boolean"},"per_page":{"type":"integer","default":30,"description":"The number of results per page (max 100). For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""},"page":{"type":"integer","default":1,"description":"The page number of the results to fetch. For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""}},"required":[]},
    call: function(args) {
    var _q = new URLSearchParams();
    if (args["filter"] !== undefined) _q.set("filter", String(args["filter"]));
    if (args["state"] !== undefined) _q.set("state", String(args["state"]));
    if (args["labels"] !== undefined) _q.set("labels", String(args["labels"]));
    if (args["sort"] !== undefined) _q.set("sort", String(args["sort"]));
    if (args["direction"] !== undefined) _q.set("direction", String(args["direction"]));
    if (args["since"] !== undefined) _q.set("since", String(args["since"]));
    if (args["collab"] !== undefined) _q.set("collab", String(args["collab"]));
    if (args["orgs"] !== undefined) _q.set("orgs", String(args["orgs"]));
    if (args["owned"] !== undefined) _q.set("owned", String(args["owned"]));
    if (args["pulls"] !== undefined) _q.set("pulls", String(args["pulls"]));
    if (args["per_page"] !== undefined) _q.set("per_page", String(args["per_page"]));
    if (args["page"] !== undefined) _q.set("page", String(args["page"]));
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/issues") + _qs, {}, undefined);
    }
  },
  {
    name: "licenses/get-all-commonly-used",
    description: "Get all commonly used licenses. Lists the most commonly used licenses on GitHub.",
    inputSchema: {"type":"object","properties":{"featured":{"type":"boolean"},"per_page":{"type":"integer","default":30,"description":"The number of results per page (max 100). For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""},"page":{"type":"integer","default":1,"description":"The page number of the results to fetch. For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""}},"required":[]},
    call: function(args) {
    var _q = new URLSearchParams();
    if (args["featured"] !== undefined) _q.set("featured", String(args["featured"]));
    if (args["per_page"] !== undefined) _q.set("per_page", String(args["per_page"]));
    if (args["page"] !== undefined) _q.set("page", String(args["page"]));
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/licenses") + _qs, {}, undefined);
    }
  },
  {
    name: "licenses/get",
    description: "Get a license. Gets information about a specific license. Params: license (required).",
    inputSchema: {"type":"object","properties":{"license":{"type":"string"}},"required":["license"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/licenses/" + String(args["license"])) + _qs, {}, undefined);
    }
  },
  {
    name: "markdown/render",
    description: "Render a Markdown document. Depending on what is rendered in the Markdown, you may need to provide additional token scopes for labels, such as `issues:read` or `pull_requests:read`.",
    inputSchema: {"type":"object","properties":{"body":{"type":"object","description":"Request body"}},"required":["body"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("POST", ("https://api.github.com/markdown") + _qs, {}, args["body"]);
    }
  },
  {
    name: "markdown/render-raw",
    description: "Render a Markdown document in raw mode. You must send Markdown as plain text (using a `Content-Type` header of `text/plain` or `text/x-markdown`) to this endpoint, rather than using JSON format.",
    inputSchema: {"type":"object","properties":{"body":{"type":"object","description":"Request body"}},"required":[]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("POST", ("https://api.github.com/markdown/raw") + _qs, {}, args["body"]);
    }
  },
  {
    name: "apps/get-subscription-plan-for-account",
    description: "Get a subscription plan for an account. Shows whether the user or organization account actively subscribes to a plan listed by the authenticated GitHub App. Params: account_id (required).",
    inputSchema: {"type":"object","properties":{"account_id":{"type":"integer","description":"account_id parameter"}},"required":["account_id"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/marketplace_listing/accounts/" + String(args["account_id"])) + _qs, {}, undefined);
    }
  },
  {
    name: "apps/list-plans",
    description: "List plans. Lists all plans that are part of your GitHub Marketplace listing.",
    inputSchema: {"type":"object","properties":{"per_page":{"type":"integer","default":30,"description":"The number of results per page (max 100). For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""},"page":{"type":"integer","default":1,"description":"The page number of the results to fetch. For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""}},"required":[]},
    call: function(args) {
    var _q = new URLSearchParams();
    if (args["per_page"] !== undefined) _q.set("per_page", String(args["per_page"]));
    if (args["page"] !== undefined) _q.set("page", String(args["page"]));
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/marketplace_listing/plans") + _qs, {}, undefined);
    }
  },
  {
    name: "apps/list-accounts-for-plan",
    description: "List accounts for a plan. Returns user and organization accounts associated with the specified plan, including free plans. Params: plan_id (required).",
    inputSchema: {"type":"object","properties":{"plan_id":{"type":"integer","description":"The unique identifier of the plan."},"sort":{"type":"string","enum":["created","updated"],"default":"created","description":"The property to sort the results by."},"direction":{"type":"string","enum":["asc","desc"],"description":"To return the oldest accounts first, set to `asc`. Ignored without the `sort` parameter."},"per_page":{"type":"integer","default":30,"description":"The number of results per page (max 100). For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""},"page":{"type":"integer","default":1,"description":"The page number of the results to fetch. For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""}},"required":["plan_id"]},
    call: function(args) {
    var _q = new URLSearchParams();
    if (args["sort"] !== undefined) _q.set("sort", String(args["sort"]));
    if (args["direction"] !== undefined) _q.set("direction", String(args["direction"]));
    if (args["per_page"] !== undefined) _q.set("per_page", String(args["per_page"]));
    if (args["page"] !== undefined) _q.set("page", String(args["page"]));
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/marketplace_listing/plans/" + String(args["plan_id"]) + "/accounts") + _qs, {}, undefined);
    }
  },
  {
    name: "apps/get-subscription-plan-for-account-stubbed",
    description: "Get a subscription plan for an account (stubbed). Shows whether the user or organization account actively subscribes to a plan listed by the authenticated GitHub App. Params: account_id (required).",
    inputSchema: {"type":"object","properties":{"account_id":{"type":"integer","description":"account_id parameter"}},"required":["account_id"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/marketplace_listing/stubbed/accounts/" + String(args["account_id"])) + _qs, {}, undefined);
    }
  },
  {
    name: "apps/list-plans-stubbed",
    description: "List plans (stubbed). Lists all plans that are part of your GitHub Marketplace listing.",
    inputSchema: {"type":"object","properties":{"per_page":{"type":"integer","default":30,"description":"The number of results per page (max 100). For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""},"page":{"type":"integer","default":1,"description":"The page number of the results to fetch. For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""}},"required":[]},
    call: function(args) {
    var _q = new URLSearchParams();
    if (args["per_page"] !== undefined) _q.set("per_page", String(args["per_page"]));
    if (args["page"] !== undefined) _q.set("page", String(args["page"]));
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/marketplace_listing/stubbed/plans") + _qs, {}, undefined);
    }
  },
  {
    name: "apps/list-accounts-for-plan-stubbed",
    description: "List accounts for a plan (stubbed). Returns repository and organization accounts associated with the specified plan, including free plans. Params: plan_id (required).",
    inputSchema: {"type":"object","properties":{"plan_id":{"type":"integer","description":"The unique identifier of the plan."},"sort":{"type":"string","enum":["created","updated"],"default":"created","description":"The property to sort the results by."},"direction":{"type":"string","enum":["asc","desc"],"description":"To return the oldest accounts first, set to `asc`. Ignored without the `sort` parameter."},"per_page":{"type":"integer","default":30,"description":"The number of results per page (max 100). For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""},"page":{"type":"integer","default":1,"description":"The page number of the results to fetch. For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""}},"required":["plan_id"]},
    call: function(args) {
    var _q = new URLSearchParams();
    if (args["sort"] !== undefined) _q.set("sort", String(args["sort"]));
    if (args["direction"] !== undefined) _q.set("direction", String(args["direction"]));
    if (args["per_page"] !== undefined) _q.set("per_page", String(args["per_page"]));
    if (args["page"] !== undefined) _q.set("page", String(args["page"]));
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/marketplace_listing/stubbed/plans/" + String(args["plan_id"]) + "/accounts") + _qs, {}, undefined);
    }
  },
  {
    name: "meta/get",
    description: "Get GitHub meta information. Returns meta information about GitHub, including a list of GitHub's IP addresses.",
    inputSchema: {"type":"object","properties":{},"required":[]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/meta") + _qs, {}, undefined);
    }
  },
  {
    name: "activity/list-public-events-for-repo-network",
    description: "List public events for a network of repositories. > [!NOTE]\n> This API is not built to serve real-time use cases. Params: owner (required), repo (required).",
    inputSchema: {"type":"object","properties":{"owner":{"type":"string","description":"The account owner of the repository. The name is not case sensitive."},"repo":{"type":"string","description":"The name of the repository without the `.git` extension. The name is not case sensitive."},"per_page":{"type":"integer","default":30,"description":"The number of results per page (max 100). For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""},"page":{"type":"integer","default":1,"description":"The page number of the results to fetch. For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""}},"required":["owner","repo"]},
    call: function(args) {
    var _q = new URLSearchParams();
    if (args["per_page"] !== undefined) _q.set("per_page", String(args["per_page"]));
    if (args["page"] !== undefined) _q.set("page", String(args["page"]));
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/networks/" + String(args["owner"]) + "/" + String(args["repo"]) + "/events") + _qs, {}, undefined);
    }
  },
  {
    name: "activity/list-notifications-for-authenticated-user",
    description: "List notifications for the authenticated user. List all notifications for the current user, sorted by most recently updated.",
    inputSchema: {"type":"object","properties":{"all":{"type":"boolean","default":false,"description":"If `true`, show notifications marked as read."},"participating":{"type":"boolean","default":false,"description":"If `true`, only shows notifications in which the user is directly participating or mentioned."},"since":{"type":"string","format":"date-time","description":"Only show results that were last updated after the given time. This is a timestamp in [ISO 8601](https://en.wikipedia.org/wiki/ISO_8601) format: `YYYY-MM-DDTHH:MM:SSZ`."},"before":{"type":"string","format":"date-time","description":"Only show notifications updated before the given time. This is a timestamp in [ISO 8601](https://en.wikipedia.org/wiki/ISO_8601) format: `YYYY-MM-DDTHH:MM:SSZ`."},"page":{"type":"integer","default":1,"description":"The page number of the results to fetch. For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""},"per_page":{"type":"integer","default":50,"description":"The number of results per page (max 50). For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""}},"required":[]},
    call: function(args) {
    var _q = new URLSearchParams();
    if (args["all"] !== undefined) _q.set("all", String(args["all"]));
    if (args["participating"] !== undefined) _q.set("participating", String(args["participating"]));
    if (args["since"] !== undefined) _q.set("since", String(args["since"]));
    if (args["before"] !== undefined) _q.set("before", String(args["before"]));
    if (args["page"] !== undefined) _q.set("page", String(args["page"]));
    if (args["per_page"] !== undefined) _q.set("per_page", String(args["per_page"]));
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/notifications") + _qs, {}, undefined);
    }
  },
  {
    name: "activity/mark-notifications-as-read",
    description: "Mark notifications as read. Marks all notifications as \"read\" for the current user.",
    inputSchema: {"type":"object","properties":{"body":{"type":"object","description":"Request body"}},"required":[]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("PUT", ("https://api.github.com/notifications") + _qs, {}, args["body"]);
    }
  },
  {
    name: "activity/get-thread",
    description: "Get a thread. Gets information about a notification thread. Params: thread_id (required).",
    inputSchema: {"type":"object","properties":{"thread_id":{"type":"integer","description":"The unique identifier of the notification thread. This corresponds to the value returned in the `id` field when you retrieve notifications (for example with the [`GET /notifications` operation](https://docs.github.com/rest/activity/notifications#list-notifications-for-the-authenticated-user))."}},"required":["thread_id"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/notifications/threads/" + String(args["thread_id"])) + _qs, {}, undefined);
    }
  },
  {
    name: "activity/mark-thread-as-read",
    description: "Mark a thread as read. Marks a thread as \"read.\" Marking a thread as \"read\" is equivalent to clicking a notification in your notification inbox on GitHub: https://github.com/notifications. Params: thread_id (required).",
    inputSchema: {"type":"object","properties":{"thread_id":{"type":"integer","description":"The unique identifier of the notification thread. This corresponds to the value returned in the `id` field when you retrieve notifications (for example with the [`GET /notifications` operation](https://docs.github.com/rest/activity/notifications#list-notifications-for-the-authenticated-user))."}},"required":["thread_id"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("PATCH", ("https://api.github.com/notifications/threads/" + String(args["thread_id"])) + _qs, {}, undefined);
    }
  },
  {
    name: "activity/mark-thread-as-done",
    description: "Mark a thread as done. Marks a thread as \"done.\" Marking a thread as \"done\" is equivalent to marking a notification in your notification inbox on GitHub as done: https://github.com/notifications. Params: thread_id (required).",
    inputSchema: {"type":"object","properties":{"thread_id":{"type":"integer","description":"The unique identifier of the notification thread. This corresponds to the value returned in the `id` field when you retrieve notifications (for example with the [`GET /notifications` operation](https://docs.github.com/rest/activity/notifications#list-notifications-for-the-authenticated-user))."}},"required":["thread_id"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("DELETE", ("https://api.github.com/notifications/threads/" + String(args["thread_id"])) + _qs, {}, undefined);
    }
  },
  {
    name: "activity/get-thread-subscription-for-authenticated-user",
    description: "Get a thread subscription for the authenticated user. This checks to see if the current user is subscribed to a thread. Params: thread_id (required).",
    inputSchema: {"type":"object","properties":{"thread_id":{"type":"integer","description":"The unique identifier of the notification thread. This corresponds to the value returned in the `id` field when you retrieve notifications (for example with the [`GET /notifications` operation](https://docs.github.com/rest/activity/notifications#list-notifications-for-the-authenticated-user))."}},"required":["thread_id"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/notifications/threads/" + String(args["thread_id"]) + "/subscription") + _qs, {}, undefined);
    }
  },
  {
    name: "activity/set-thread-subscription",
    description: "Set a thread subscription. If you are watching a repository, you receive notifications for all threads by default. Params: thread_id (required).",
    inputSchema: {"type":"object","properties":{"thread_id":{"type":"integer","description":"The unique identifier of the notification thread. This corresponds to the value returned in the `id` field when you retrieve notifications (for example with the [`GET /notifications` operation](https://docs.github.com/rest/activity/notifications#list-notifications-for-the-authenticated-user))."},"body":{"type":"object","description":"Request body"}},"required":["thread_id"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("PUT", ("https://api.github.com/notifications/threads/" + String(args["thread_id"]) + "/subscription") + _qs, {}, args["body"]);
    }
  },
  {
    name: "activity/delete-thread-subscription",
    description: "Delete a thread subscription. Mutes all future notifications for a conversation until you comment on the thread or get an **@mention**. Params: thread_id (required).",
    inputSchema: {"type":"object","properties":{"thread_id":{"type":"integer","description":"The unique identifier of the notification thread. This corresponds to the value returned in the `id` field when you retrieve notifications (for example with the [`GET /notifications` operation](https://docs.github.com/rest/activity/notifications#list-notifications-for-the-authenticated-user))."}},"required":["thread_id"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("DELETE", ("https://api.github.com/notifications/threads/" + String(args["thread_id"]) + "/subscription") + _qs, {}, undefined);
    }
  },
  {
    name: "meta/get-octocat",
    description: "Get Octocat. Get the octocat as ASCII art.",
    inputSchema: {"type":"object","properties":{"s":{"type":"string","description":"The words to show in Octocat's speech bubble"}},"required":[]},
    call: function(args) {
    var _q = new URLSearchParams();
    if (args["s"] !== undefined) _q.set("s", String(args["s"]));
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/octocat") + _qs, {}, undefined);
    }
  },
  {
    name: "orgs/list",
    description: "List organizations. Lists all organizations, in the order that they were created.",
    inputSchema: {"type":"object","properties":{"since":{"type":"integer","description":"An organization ID. Only return organizations with an ID greater than this ID."},"per_page":{"type":"integer","default":30,"description":"The number of results per page (max 100). For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""}},"required":[]},
    call: function(args) {
    var _q = new URLSearchParams();
    if (args["since"] !== undefined) _q.set("since", String(args["since"]));
    if (args["per_page"] !== undefined) _q.set("per_page", String(args["per_page"]));
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/organizations") + _qs, {}, undefined);
    }
  },
  {
    name: "actions/get-actions-cache-retention-limit-for-organization",
    description: "Get GitHub Actions cache retention limit for an organization. Gets GitHub Actions cache retention limit for an organization. Params: org (required).",
    inputSchema: {"type":"object","properties":{"org":{"type":"string","description":"The organization name. The name is not case sensitive."}},"required":["org"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/organizations/" + String(args["org"]) + "/actions/cache/retention-limit") + _qs, {}, undefined);
    }
  },
  {
    name: "actions/set-actions-cache-retention-limit-for-organization",
    description: "Set GitHub Actions cache retention limit for an organization. Sets GitHub Actions cache retention limit for an organization. Params: org (required).",
    inputSchema: {"type":"object","properties":{"org":{"type":"string","description":"The organization name. The name is not case sensitive."},"body":{"type":"object","description":"Request body"}},"required":["org","body"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("PUT", ("https://api.github.com/organizations/" + String(args["org"]) + "/actions/cache/retention-limit") + _qs, {}, args["body"]);
    }
  },
  {
    name: "actions/get-actions-cache-storage-limit-for-organization",
    description: "Get GitHub Actions cache storage limit for an organization. Gets GitHub Actions cache storage limit for an organization. Params: org (required).",
    inputSchema: {"type":"object","properties":{"org":{"type":"string","description":"The organization name. The name is not case sensitive."}},"required":["org"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/organizations/" + String(args["org"]) + "/actions/cache/storage-limit") + _qs, {}, undefined);
    }
  },
  {
    name: "actions/set-actions-cache-storage-limit-for-organization",
    description: "Set GitHub Actions cache storage limit for an organization. Sets GitHub Actions cache storage limit for an organization. Params: org (required).",
    inputSchema: {"type":"object","properties":{"org":{"type":"string","description":"The organization name. The name is not case sensitive."},"body":{"type":"object","description":"Request body"}},"required":["org","body"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("PUT", ("https://api.github.com/organizations/" + String(args["org"]) + "/actions/cache/storage-limit") + _qs, {}, args["body"]);
    }
  },
  {
    name: "billing/get-github-billing-ai-credit-usage-report-org",
    description: "Get billing AI credit usage report for an organization. Gets a report of AI credit usage for an organization. Params: org (required).",
    inputSchema: {"type":"object","properties":{"org":{"type":"string","description":"The organization name. The name is not case sensitive."},"year":{"type":"integer","description":"If specified, only return results for a single year. The value of `year` is an integer with four digits representing a year. For example, `2025`. Default value is the current year."},"month":{"type":"integer","description":"If specified, only return results for a single month. The value of `month` is an integer between `1` and `12`. Default value is the current month. If no year is specified the default `year` is used."},"day":{"type":"integer","description":"If specified, only return results for a single day. The value of `day` is an integer between `1` and `31`. If no `year` or `month` is specified, the default `year` and `month` are used."},"user":{"type":"string","description":"The user name to query usage for. The name is not case sensitive."},"model":{"type":"string","description":"The model name to query usage for. The name is not case sensitive."},"product":{"type":"string","description":"The product name to query usage for. The name is not case sensitive."}},"required":["org"]},
    call: function(args) {
    var _q = new URLSearchParams();
    if (args["year"] !== undefined) _q.set("year", String(args["year"]));
    if (args["month"] !== undefined) _q.set("month", String(args["month"]));
    if (args["day"] !== undefined) _q.set("day", String(args["day"]));
    if (args["user"] !== undefined) _q.set("user", String(args["user"]));
    if (args["model"] !== undefined) _q.set("model", String(args["model"]));
    if (args["product"] !== undefined) _q.set("product", String(args["product"]));
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/organizations/" + String(args["org"]) + "/settings/billing/ai_credit/usage") + _qs, {}, undefined);
    }
  },
  {
    name: "billing/get-all-budgets-org",
    description: "Get all budgets for an organization. Gets all budgets for an organization. Params: org (required).",
    inputSchema: {"type":"object","properties":{"org":{"type":"string","description":"The organization name. The name is not case sensitive."},"page":{"type":"integer","default":1,"description":"The page number of the results to fetch."},"per_page":{"type":"integer","default":10,"description":"The number of results per page (max 100)."},"scope":{"type":"string","enum":["enterprise","organization","repository","cost_center","multi_user_customer","user"],"description":"Filter budgets by scope type."},"user":{"type":"string","description":"Filter consumed amount details for budgets by the specified user login."}},"required":["org"]},
    call: function(args) {
    var _q = new URLSearchParams();
    if (args["page"] !== undefined) _q.set("page", String(args["page"]));
    if (args["per_page"] !== undefined) _q.set("per_page", String(args["per_page"]));
    if (args["scope"] !== undefined) _q.set("scope", String(args["scope"]));
    if (args["user"] !== undefined) _q.set("user", String(args["user"]));
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/organizations/" + String(args["org"]) + "/settings/billing/budgets") + _qs, {}, undefined);
    }
  },
  {
    name: "billing/create-organization-budget",
    description: "Create a budget for an organization. Creates a new budget for an organization. Params: org (required).",
    inputSchema: {"type":"object","properties":{"org":{"type":"string","description":"The organization name. The name is not case sensitive."},"body":{"type":"object","description":"Request body"}},"required":["org"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("POST", ("https://api.github.com/organizations/" + String(args["org"]) + "/settings/billing/budgets") + _qs, {}, args["body"]);
    }
  },
  {
    name: "billing/get-budget-org",
    description: "Get a budget by ID for an organization. Gets a budget by ID. Params: org (required), budget_id (required).",
    inputSchema: {"type":"object","properties":{"org":{"type":"string","description":"The organization name. The name is not case sensitive."},"budget_id":{"type":"string","description":"The ID corresponding to the budget."}},"required":["org","budget_id"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/organizations/" + String(args["org"]) + "/settings/billing/budgets/" + String(args["budget_id"])) + _qs, {}, undefined);
    }
  },
  {
    name: "billing/update-budget-org",
    description: "Update a budget for an organization. Updates an existing budget for an organization. Params: org (required), budget_id (required).",
    inputSchema: {"type":"object","properties":{"org":{"type":"string","description":"The organization name. The name is not case sensitive."},"budget_id":{"type":"string","description":"The ID corresponding to the budget."},"body":{"type":"object","description":"Request body"}},"required":["org","budget_id","body"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("PATCH", ("https://api.github.com/organizations/" + String(args["org"]) + "/settings/billing/budgets/" + String(args["budget_id"])) + _qs, {}, args["body"]);
    }
  },
  {
    name: "billing/delete-budget-org",
    description: "Delete a budget for an organization. Deletes a budget by ID for an organization. Params: org (required), budget_id (required).",
    inputSchema: {"type":"object","properties":{"org":{"type":"string","description":"The organization name. The name is not case sensitive."},"budget_id":{"type":"string","description":"The ID corresponding to the budget."}},"required":["org","budget_id"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("DELETE", ("https://api.github.com/organizations/" + String(args["org"]) + "/settings/billing/budgets/" + String(args["budget_id"])) + _qs, {}, undefined);
    }
  },
  {
    name: "billing/get-github-billing-premium-request-usage-report-org",
    description: "Get billing premium request usage report for an organization. Gets a report of premium request usage for an organization. Params: org (required).",
    inputSchema: {"type":"object","properties":{"org":{"type":"string","description":"The organization name. The name is not case sensitive."},"year":{"type":"integer","description":"If specified, only return results for a single year. The value of `year` is an integer with four digits representing a year. For example, `2025`. Default value is the current year."},"month":{"type":"integer","description":"If specified, only return results for a single month. The value of `month` is an integer between `1` and `12`. Default value is the current month. If no year is specified the default `year` is used."},"day":{"type":"integer","description":"If specified, only return results for a single day. The value of `day` is an integer between `1` and `31`. If no `year` or `month` is specified, the default `year` and `month` are used."},"user":{"type":"string","description":"The user name to query usage for. The name is not case sensitive."},"model":{"type":"string","description":"The model name to query usage for. The name is not case sensitive."},"product":{"type":"string","description":"The product name to query usage for. The name is not case sensitive."}},"required":["org"]},
    call: function(args) {
    var _q = new URLSearchParams();
    if (args["year"] !== undefined) _q.set("year", String(args["year"]));
    if (args["month"] !== undefined) _q.set("month", String(args["month"]));
    if (args["day"] !== undefined) _q.set("day", String(args["day"]));
    if (args["user"] !== undefined) _q.set("user", String(args["user"]));
    if (args["model"] !== undefined) _q.set("model", String(args["model"]));
    if (args["product"] !== undefined) _q.set("product", String(args["product"]));
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/organizations/" + String(args["org"]) + "/settings/billing/premium_request/usage") + _qs, {}, undefined);
    }
  },
  {
    name: "billing/get-github-billing-usage-report-org",
    description: "Get billing usage report for an organization. Gets a report of the total usage for an organization. Params: org (required).",
    inputSchema: {"type":"object","properties":{"org":{"type":"string","description":"The organization name. The name is not case sensitive."},"year":{"type":"integer","description":"If specified, only return results for a single year. The value of `year` is an integer with four digits representing a year. For example, `2025`. Default value is the current year."},"month":{"type":"integer","description":"If specified, only return results for a single month. The value of `month` is an integer between `1` and `12`. If no year is specified the default `year` is used."},"day":{"type":"integer","description":"If specified, only return results for a single day. The value of `day` is an integer between `1` and `31`. If no `year` or `month` is specified, the default `year` and `month` are used."}},"required":["org"]},
    call: function(args) {
    var _q = new URLSearchParams();
    if (args["year"] !== undefined) _q.set("year", String(args["year"]));
    if (args["month"] !== undefined) _q.set("month", String(args["month"]));
    if (args["day"] !== undefined) _q.set("day", String(args["day"]));
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/organizations/" + String(args["org"]) + "/settings/billing/usage") + _qs, {}, undefined);
    }
  },
  {
    name: "billing/get-github-billing-usage-summary-report-org",
    description: "Get billing usage summary for an organization. > [!NOTE]\n> This endpoint is in public preview and is subject to change. Params: org (required).",
    inputSchema: {"type":"object","properties":{"org":{"type":"string","description":"The organization name. The name is not case sensitive."},"year":{"type":"integer","description":"If specified, only return results for a single year. The value of `year` is an integer with four digits representing a year. For example, `2025`. Default value is the current year."},"month":{"type":"integer","description":"If specified, only return results for a single month. The value of `month` is an integer between `1` and `12`. Default value is the current month. If no year is specified the default `year` is used."},"day":{"type":"integer","description":"If specified, only return results for a single day. The value of `day` is an integer between `1` and `31`. If no `year` or `month` is specified, the default `year` and `month` are used."},"repository":{"type":"string","description":"The repository name to query for usage in the format owner/repository."},"product":{"type":"string","description":"The product name to query usage for. The name is not case sensitive."},"sku":{"type":"string","description":"The SKU to query for usage."}},"required":["org"]},
    call: function(args) {
    var _q = new URLSearchParams();
    if (args["year"] !== undefined) _q.set("year", String(args["year"]));
    if (args["month"] !== undefined) _q.set("month", String(args["month"]));
    if (args["day"] !== undefined) _q.set("day", String(args["day"]));
    if (args["repository"] !== undefined) _q.set("repository", String(args["repository"]));
    if (args["product"] !== undefined) _q.set("product", String(args["product"]));
    if (args["sku"] !== undefined) _q.set("sku", String(args["sku"]));
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/organizations/" + String(args["org"]) + "/settings/billing/usage/summary") + _qs, {}, undefined);
    }
  },
  {
    name: "orgs/get",
    description: "Get an organization. Gets information about an organization. Params: org (required).",
    inputSchema: {"type":"object","properties":{"org":{"type":"string","description":"The organization name. The name is not case sensitive."}},"required":["org"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/orgs/" + String(args["org"])) + _qs, {}, undefined);
    }
  },
  {
    name: "orgs/update",
    description: "Update an organization. > [!WARNING]\n> **Closing down notice:** GitHub will replace and discontinue `members_allowed_repository_creation_type` in favor of more granular permissions. Params: org (required).",
    inputSchema: {"type":"object","properties":{"org":{"type":"string","description":"The organization name. The name is not case sensitive."},"body":{"type":"object","description":"Request body"}},"required":["org"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("PATCH", ("https://api.github.com/orgs/" + String(args["org"])) + _qs, {}, args["body"]);
    }
  },
  {
    name: "orgs/delete",
    description: "Delete an organization. Deletes an organization and all its repositories. Params: org (required).",
    inputSchema: {"type":"object","properties":{"org":{"type":"string","description":"The organization name. The name is not case sensitive."}},"required":["org"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("DELETE", ("https://api.github.com/orgs/" + String(args["org"])) + _qs, {}, undefined);
    }
  },
  {
    name: "actions/get-actions-cache-usage-for-org",
    description: "Get GitHub Actions cache usage for an organization. Gets the total GitHub Actions cache usage for an organization. Params: org (required).",
    inputSchema: {"type":"object","properties":{"org":{"type":"string","description":"The organization name. The name is not case sensitive."}},"required":["org"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/orgs/" + String(args["org"]) + "/actions/cache/usage") + _qs, {}, undefined);
    }
  },
  {
    name: "actions/get-actions-cache-usage-by-repo-for-org",
    description: "List repositories with GitHub Actions cache usage for an organization. Lists repositories and their GitHub Actions cache usage for an organization. Params: org (required).",
    inputSchema: {"type":"object","properties":{"org":{"type":"string","description":"The organization name. The name is not case sensitive."},"per_page":{"type":"integer","default":30,"description":"The number of results per page (max 100). For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""},"page":{"type":"integer","default":1,"description":"The page number of the results to fetch. For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""}},"required":["org"]},
    call: function(args) {
    var _q = new URLSearchParams();
    if (args["per_page"] !== undefined) _q.set("per_page", String(args["per_page"]));
    if (args["page"] !== undefined) _q.set("page", String(args["page"]));
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/orgs/" + String(args["org"]) + "/actions/cache/usage-by-repository") + _qs, {}, undefined);
    }
  },
  {
    name: "actions/list-hosted-runners-for-org",
    description: "List GitHub-hosted runners for an organization. Lists all GitHub-hosted runners configured in an organization. Params: org (required).",
    inputSchema: {"type":"object","properties":{"org":{"type":"string","description":"The organization name. The name is not case sensitive."},"per_page":{"type":"integer","default":30,"description":"The number of results per page (max 100). For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""},"page":{"type":"integer","default":1,"description":"The page number of the results to fetch. For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""}},"required":["org"]},
    call: function(args) {
    var _q = new URLSearchParams();
    if (args["per_page"] !== undefined) _q.set("per_page", String(args["per_page"]));
    if (args["page"] !== undefined) _q.set("page", String(args["page"]));
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/orgs/" + String(args["org"]) + "/actions/hosted-runners") + _qs, {}, undefined);
    }
  },
  {
    name: "actions/create-hosted-runner-for-org",
    description: "Create a GitHub-hosted runner for an organization. Creates a GitHub-hosted runner for an organization. Params: org (required).",
    inputSchema: {"type":"object","properties":{"org":{"type":"string","description":"The organization name. The name is not case sensitive."},"body":{"type":"object","description":"Request body"}},"required":["org","body"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("POST", ("https://api.github.com/orgs/" + String(args["org"]) + "/actions/hosted-runners") + _qs, {}, args["body"]);
    }
  },
  {
    name: "actions/list-custom-images-for-org",
    description: "List custom images for an organization. Params: org (required).",
    inputSchema: {"type":"object","properties":{"org":{"type":"string","description":"The organization name. The name is not case sensitive."}},"required":["org"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/orgs/" + String(args["org"]) + "/actions/hosted-runners/images/custom") + _qs, {}, undefined);
    }
  },
  {
    name: "actions/get-custom-image-for-org",
    description: "Get a custom image definition for GitHub Actions Hosted Runners. Params: org (required), image_definition_id (required).",
    inputSchema: {"type":"object","properties":{"org":{"type":"string","description":"The organization name. The name is not case sensitive."},"image_definition_id":{"type":"integer","description":"Image definition ID of custom image"}},"required":["org","image_definition_id"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/orgs/" + String(args["org"]) + "/actions/hosted-runners/images/custom/" + String(args["image_definition_id"])) + _qs, {}, undefined);
    }
  },
  {
    name: "actions/delete-custom-image-from-org",
    description: "Delete a custom image from the organization. Params: org (required), image_definition_id (required).",
    inputSchema: {"type":"object","properties":{"org":{"type":"string","description":"The organization name. The name is not case sensitive."},"image_definition_id":{"type":"integer","description":"Image definition ID of custom image"}},"required":["org","image_definition_id"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("DELETE", ("https://api.github.com/orgs/" + String(args["org"]) + "/actions/hosted-runners/images/custom/" + String(args["image_definition_id"])) + _qs, {}, undefined);
    }
  },
  {
    name: "actions/list-custom-image-versions-for-org",
    description: "List image versions of a custom image for an organization. Params: image_definition_id (required), org (required).",
    inputSchema: {"type":"object","properties":{"image_definition_id":{"type":"integer","description":"Image definition ID of custom image"},"org":{"type":"string","description":"The organization name. The name is not case sensitive."}},"required":["image_definition_id","org"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/orgs/" + String(args["org"]) + "/actions/hosted-runners/images/custom/" + String(args["image_definition_id"]) + "/versions") + _qs, {}, undefined);
    }
  },
  {
    name: "actions/get-custom-image-version-for-org",
    description: "Get an image version of a custom image for GitHub Actions Hosted Runners. Params: org (required), image_definition_id (required), version (required).",
    inputSchema: {"type":"object","properties":{"org":{"type":"string","description":"The organization name. The name is not case sensitive."},"image_definition_id":{"type":"integer","description":"Image definition ID of custom image"},"version":{"type":"string","pattern":"^\\d+\\.\\d+\\.\\d+$","description":"Version of a custom image"}},"required":["org","image_definition_id","version"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/orgs/" + String(args["org"]) + "/actions/hosted-runners/images/custom/" + String(args["image_definition_id"]) + "/versions/" + String(args["version"])) + _qs, {}, undefined);
    }
  },
  {
    name: "actions/delete-custom-image-version-from-org",
    description: "Delete an image version of custom image from the organization. Params: org (required), image_definition_id (required), version (required).",
    inputSchema: {"type":"object","properties":{"org":{"type":"string","description":"The organization name. The name is not case sensitive."},"image_definition_id":{"type":"integer","description":"Image definition ID of custom image"},"version":{"type":"string","pattern":"^\\d+\\.\\d+\\.\\d+$","description":"Version of a custom image"}},"required":["org","image_definition_id","version"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("DELETE", ("https://api.github.com/orgs/" + String(args["org"]) + "/actions/hosted-runners/images/custom/" + String(args["image_definition_id"]) + "/versions/" + String(args["version"])) + _qs, {}, undefined);
    }
  },
  {
    name: "actions/get-hosted-runners-github-owned-images-for-org",
    description: "Get GitHub-owned images for GitHub-hosted runners in an organization. Get the list of GitHub-owned images available for GitHub-hosted runners for an organization. Params: org (required).",
    inputSchema: {"type":"object","properties":{"org":{"type":"string","description":"The organization name. The name is not case sensitive."}},"required":["org"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/orgs/" + String(args["org"]) + "/actions/hosted-runners/images/github-owned") + _qs, {}, undefined);
    }
  },
  {
    name: "actions/get-hosted-runners-partner-images-for-org",
    description: "Get partner images for GitHub-hosted runners in an organization. Get the list of partner images available for GitHub-hosted runners for an organization. Params: org (required).",
    inputSchema: {"type":"object","properties":{"org":{"type":"string","description":"The organization name. The name is not case sensitive."}},"required":["org"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/orgs/" + String(args["org"]) + "/actions/hosted-runners/images/partner") + _qs, {}, undefined);
    }
  },
  {
    name: "actions/get-hosted-runners-limits-for-org",
    description: "Get limits on GitHub-hosted runners for an organization. Get the GitHub-hosted runners limits for an organization. Params: org (required).",
    inputSchema: {"type":"object","properties":{"org":{"type":"string","description":"The organization name. The name is not case sensitive."}},"required":["org"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/orgs/" + String(args["org"]) + "/actions/hosted-runners/limits") + _qs, {}, undefined);
    }
  },
  {
    name: "actions/get-hosted-runners-machine-specs-for-org",
    description: "Get GitHub-hosted runners machine specs for an organization. Get the list of machine specs available for GitHub-hosted runners for an organization. Params: org (required).",
    inputSchema: {"type":"object","properties":{"org":{"type":"string","description":"The organization name. The name is not case sensitive."}},"required":["org"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/orgs/" + String(args["org"]) + "/actions/hosted-runners/machine-sizes") + _qs, {}, undefined);
    }
  },
  {
    name: "actions/get-hosted-runners-platforms-for-org",
    description: "Get platforms for GitHub-hosted runners in an organization. Get the list of platforms available for GitHub-hosted runners for an organization. Params: org (required).",
    inputSchema: {"type":"object","properties":{"org":{"type":"string","description":"The organization name. The name is not case sensitive."}},"required":["org"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/orgs/" + String(args["org"]) + "/actions/hosted-runners/platforms") + _qs, {}, undefined);
    }
  },
  {
    name: "actions/get-hosted-runner-for-org",
    description: "Get a GitHub-hosted runner for an organization. Gets a GitHub-hosted runner configured in an organization. Params: org (required), hosted_runner_id (required).",
    inputSchema: {"type":"object","properties":{"org":{"type":"string","description":"The organization name. The name is not case sensitive."},"hosted_runner_id":{"type":"integer","description":"Unique identifier of the GitHub-hosted runner."}},"required":["org","hosted_runner_id"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/orgs/" + String(args["org"]) + "/actions/hosted-runners/" + String(args["hosted_runner_id"])) + _qs, {}, undefined);
    }
  },
  {
    name: "actions/update-hosted-runner-for-org",
    description: "Update a GitHub-hosted runner for an organization. Updates a GitHub-hosted runner for an organization. Params: org (required), hosted_runner_id (required).",
    inputSchema: {"type":"object","properties":{"org":{"type":"string","description":"The organization name. The name is not case sensitive."},"hosted_runner_id":{"type":"integer","description":"Unique identifier of the GitHub-hosted runner."},"body":{"type":"object","description":"Request body"}},"required":["org","hosted_runner_id","body"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("PATCH", ("https://api.github.com/orgs/" + String(args["org"]) + "/actions/hosted-runners/" + String(args["hosted_runner_id"])) + _qs, {}, args["body"]);
    }
  },
  {
    name: "actions/delete-hosted-runner-for-org",
    description: "Delete a GitHub-hosted runner for an organization. Deletes a GitHub-hosted runner for an organization. Params: org (required), hosted_runner_id (required).",
    inputSchema: {"type":"object","properties":{"org":{"type":"string","description":"The organization name. The name is not case sensitive."},"hosted_runner_id":{"type":"integer","description":"Unique identifier of the GitHub-hosted runner."}},"required":["org","hosted_runner_id"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("DELETE", ("https://api.github.com/orgs/" + String(args["org"]) + "/actions/hosted-runners/" + String(args["hosted_runner_id"])) + _qs, {}, undefined);
    }
  },
  {
    name: "oidc/list-oidc-custom-property-inclusions-for-org",
    description: "List OIDC custom property inclusions for an organization. Lists the repository custom properties that are included in the OIDC token for repository actions in an organization. Params: org (required).",
    inputSchema: {"type":"object","properties":{"org":{"type":"string","description":"The organization name. The name is not case sensitive."}},"required":["org"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/orgs/" + String(args["org"]) + "/actions/oidc/customization/properties/repo") + _qs, {}, undefined);
    }
  },
  {
    name: "oidc/create-oidc-custom-property-inclusion-for-org",
    description: "Create an OIDC custom property inclusion for an organization. Adds a repository custom property to be included in the OIDC token for repository actions in an organization. Params: org (required).",
    inputSchema: {"type":"object","properties":{"org":{"type":"string","description":"The organization name. The name is not case sensitive."},"body":{"type":"object","description":"Request body"}},"required":["org","body"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("POST", ("https://api.github.com/orgs/" + String(args["org"]) + "/actions/oidc/customization/properties/repo") + _qs, {}, args["body"]);
    }
  },
  {
    name: "oidc/delete-oidc-custom-property-inclusion-for-org",
    description: "Delete an OIDC custom property inclusion for an organization. Removes a repository custom property from being included in the OIDC token for repository actions in an organization. Params: org (required), custom_property_name (required).",
    inputSchema: {"type":"object","properties":{"org":{"type":"string","description":"The organization name. The name is not case sensitive."},"custom_property_name":{"type":"string","description":"The name of the custom property to remove from OIDC token inclusion"}},"required":["org","custom_property_name"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("DELETE", ("https://api.github.com/orgs/" + String(args["org"]) + "/actions/oidc/customization/properties/repo/" + String(args["custom_property_name"])) + _qs, {}, undefined);
    }
  },
  {
    name: "oidc/get-oidc-custom-sub-template-for-org",
    description: "Get the customization template for an OIDC subject claim for an organization. Gets the customization template for an OpenID Connect (OIDC) subject claim. Params: org (required).",
    inputSchema: {"type":"object","properties":{"org":{"type":"string","description":"The organization name. The name is not case sensitive."}},"required":["org"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/orgs/" + String(args["org"]) + "/actions/oidc/customization/sub") + _qs, {}, undefined);
    }
  },
  {
    name: "oidc/update-oidc-custom-sub-template-for-org",
    description: "Set the customization template for an OIDC subject claim for an organization. Creates or updates the customization template for an OpenID Connect (OIDC) subject claim. Params: org (required).",
    inputSchema: {"type":"object","properties":{"org":{"type":"string","description":"The organization name. The name is not case sensitive."},"body":{"type":"object","description":"Request body"}},"required":["org","body"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("PUT", ("https://api.github.com/orgs/" + String(args["org"]) + "/actions/oidc/customization/sub") + _qs, {}, args["body"]);
    }
  },
  {
    name: "actions/get-github-actions-permissions-organization",
    description: "Get GitHub Actions permissions for an organization. Gets the GitHub Actions permissions policy for repositories and allowed actions and reusable workflows in an organization. Params: org (required).",
    inputSchema: {"type":"object","properties":{"org":{"type":"string","description":"The organization name. The name is not case sensitive."}},"required":["org"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/orgs/" + String(args["org"]) + "/actions/permissions") + _qs, {}, undefined);
    }
  },
  {
    name: "actions/set-github-actions-permissions-organization",
    description: "Set GitHub Actions permissions for an organization. Sets the GitHub Actions permissions policy for repositories and allowed actions and reusable workflows in an organization. Params: org (required).",
    inputSchema: {"type":"object","properties":{"org":{"type":"string","description":"The organization name. The name is not case sensitive."},"body":{"type":"object","description":"Request body"}},"required":["org","body"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("PUT", ("https://api.github.com/orgs/" + String(args["org"]) + "/actions/permissions") + _qs, {}, args["body"]);
    }
  },
  {
    name: "actions/get-artifact-and-log-retention-settings-organization",
    description: "Get artifact and log retention settings for an organization. Gets artifact and log retention settings for an organization. Params: org (required).",
    inputSchema: {"type":"object","properties":{"org":{"type":"string","description":"The organization name. The name is not case sensitive."}},"required":["org"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/orgs/" + String(args["org"]) + "/actions/permissions/artifact-and-log-retention") + _qs, {}, undefined);
    }
  },
  {
    name: "actions/set-artifact-and-log-retention-settings-organization",
    description: "Set artifact and log retention settings for an organization. Sets artifact and log retention settings for an organization. Params: org (required).",
    inputSchema: {"type":"object","properties":{"org":{"type":"string","description":"The organization name. The name is not case sensitive."},"body":{"type":"object","description":"Request body"}},"required":["org","body"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("PUT", ("https://api.github.com/orgs/" + String(args["org"]) + "/actions/permissions/artifact-and-log-retention") + _qs, {}, args["body"]);
    }
  },
  {
    name: "actions/get-fork-pr-contributor-approval-permissions-organization",
    description: "Get fork PR contributor approval permissions for an organization. Gets the fork PR contributor approval policy for an organization. Params: org (required).",
    inputSchema: {"type":"object","properties":{"org":{"type":"string","description":"The organization name. The name is not case sensitive."}},"required":["org"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/orgs/" + String(args["org"]) + "/actions/permissions/fork-pr-contributor-approval") + _qs, {}, undefined);
    }
  },
  {
    name: "actions/set-fork-pr-contributor-approval-permissions-organization",
    description: "Set fork PR contributor approval permissions for an organization. Sets the fork PR contributor approval policy for an organization. Params: org (required).",
    inputSchema: {"type":"object","properties":{"org":{"type":"string","description":"The organization name. The name is not case sensitive."},"body":{"type":"object","description":"Request body"}},"required":["org","body"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("PUT", ("https://api.github.com/orgs/" + String(args["org"]) + "/actions/permissions/fork-pr-contributor-approval") + _qs, {}, args["body"]);
    }
  },
  {
    name: "actions/get-private-repo-fork-pr-workflows-settings-organization",
    description: "Get private repo fork PR workflow settings for an organization. Gets the settings for whether workflows from fork pull requests can run on private repositories in an organization. Params: org (required).",
    inputSchema: {"type":"object","properties":{"org":{"type":"string","description":"The organization name. The name is not case sensitive."}},"required":["org"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/orgs/" + String(args["org"]) + "/actions/permissions/fork-pr-workflows-private-repos") + _qs, {}, undefined);
    }
  },
  {
    name: "actions/set-private-repo-fork-pr-workflows-settings-organization",
    description: "Set private repo fork PR workflow settings for an organization. Sets the settings for whether workflows from fork pull requests can run on private repositories in an organization. Params: org (required).",
    inputSchema: {"type":"object","properties":{"org":{"type":"string","description":"The organization name. The name is not case sensitive."},"body":{"type":"object","description":"Request body"}},"required":["org","body"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("PUT", ("https://api.github.com/orgs/" + String(args["org"]) + "/actions/permissions/fork-pr-workflows-private-repos") + _qs, {}, args["body"]);
    }
  },
  {
    name: "actions/list-selected-repositories-enabled-github-actions-organization",
    description: "List selected repositories enabled for GitHub Actions in an organization. Lists the selected repositories that are enabled for GitHub Actions in an organization. Params: org (required).",
    inputSchema: {"type":"object","properties":{"org":{"type":"string","description":"The organization name. The name is not case sensitive."},"per_page":{"type":"integer","default":30,"description":"The number of results per page (max 100). For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""},"page":{"type":"integer","default":1,"description":"The page number of the results to fetch. For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""}},"required":["org"]},
    call: function(args) {
    var _q = new URLSearchParams();
    if (args["per_page"] !== undefined) _q.set("per_page", String(args["per_page"]));
    if (args["page"] !== undefined) _q.set("page", String(args["page"]));
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/orgs/" + String(args["org"]) + "/actions/permissions/repositories") + _qs, {}, undefined);
    }
  },
  {
    name: "actions/set-selected-repositories-enabled-github-actions-organization",
    description: "Set selected repositories enabled for GitHub Actions in an organization. Replaces the list of selected repositories that are enabled for GitHub Actions in an organization. Params: org (required).",
    inputSchema: {"type":"object","properties":{"org":{"type":"string","description":"The organization name. The name is not case sensitive."},"body":{"type":"object","description":"Request body"}},"required":["org","body"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("PUT", ("https://api.github.com/orgs/" + String(args["org"]) + "/actions/permissions/repositories") + _qs, {}, args["body"]);
    }
  },
  {
    name: "actions/enable-selected-repository-github-actions-organization",
    description: "Enable a selected repository for GitHub Actions in an organization. Adds a repository to the list of selected repositories that are enabled for GitHub Actions in an organization. Params: org (required), repository_id (required).",
    inputSchema: {"type":"object","properties":{"org":{"type":"string","description":"The organization name. The name is not case sensitive."},"repository_id":{"type":"integer","description":"The unique identifier of the repository."}},"required":["org","repository_id"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("PUT", ("https://api.github.com/orgs/" + String(args["org"]) + "/actions/permissions/repositories/" + String(args["repository_id"])) + _qs, {}, undefined);
    }
  },
  {
    name: "actions/disable-selected-repository-github-actions-organization",
    description: "Disable a selected repository for GitHub Actions in an organization. Removes a repository from the list of selected repositories that are enabled for GitHub Actions in an organization. Params: org (required), repository_id (required).",
    inputSchema: {"type":"object","properties":{"org":{"type":"string","description":"The organization name. The name is not case sensitive."},"repository_id":{"type":"integer","description":"The unique identifier of the repository."}},"required":["org","repository_id"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("DELETE", ("https://api.github.com/orgs/" + String(args["org"]) + "/actions/permissions/repositories/" + String(args["repository_id"])) + _qs, {}, undefined);
    }
  },
  {
    name: "actions/get-allowed-actions-organization",
    description: "Get allowed actions and reusable workflows for an organization. Gets the selected actions and reusable workflows that are allowed in an organization. Params: org (required).",
    inputSchema: {"type":"object","properties":{"org":{"type":"string","description":"The organization name. The name is not case sensitive."}},"required":["org"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/orgs/" + String(args["org"]) + "/actions/permissions/selected-actions") + _qs, {}, undefined);
    }
  },
  {
    name: "actions/set-allowed-actions-organization",
    description: "Set allowed actions and reusable workflows for an organization. Sets the actions and reusable workflows that are allowed in an organization. Params: org (required).",
    inputSchema: {"type":"object","properties":{"org":{"type":"string","description":"The organization name. The name is not case sensitive."},"body":{"type":"object","description":"Request body"}},"required":["org"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("PUT", ("https://api.github.com/orgs/" + String(args["org"]) + "/actions/permissions/selected-actions") + _qs, {}, args["body"]);
    }
  },
  {
    name: "actions/get-self-hosted-runners-permissions-organization",
    description: "Get self-hosted runners settings for an organization. Gets the settings for self-hosted runners for an organization. Params: org (required).",
    inputSchema: {"type":"object","properties":{"org":{"type":"string","description":"The organization name. The name is not case sensitive."}},"required":["org"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/orgs/" + String(args["org"]) + "/actions/permissions/self-hosted-runners") + _qs, {}, undefined);
    }
  },
  {
    name: "actions/set-self-hosted-runners-permissions-organization",
    description: "Set self-hosted runners settings for an organization. Sets the settings for self-hosted runners for an organization. Params: org (required).",
    inputSchema: {"type":"object","properties":{"org":{"type":"string","description":"The organization name. The name is not case sensitive."},"body":{"type":"object","description":"Request body"}},"required":["org","body"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("PUT", ("https://api.github.com/orgs/" + String(args["org"]) + "/actions/permissions/self-hosted-runners") + _qs, {}, args["body"]);
    }
  },
  {
    name: "actions/list-selected-repositories-self-hosted-runners-organization",
    description: "List repositories allowed to use self-hosted runners in an organization. Lists repositories that are allowed to use self-hosted runners in an organization. Params: org (required).",
    inputSchema: {"type":"object","properties":{"org":{"type":"string","description":"The organization name. The name is not case sensitive."},"per_page":{"type":"integer","default":30,"description":"The number of results per page (max 100). For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""},"page":{"type":"integer","default":1,"description":"The page number of the results to fetch. For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""}},"required":["org"]},
    call: function(args) {
    var _q = new URLSearchParams();
    if (args["per_page"] !== undefined) _q.set("per_page", String(args["per_page"]));
    if (args["page"] !== undefined) _q.set("page", String(args["page"]));
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/orgs/" + String(args["org"]) + "/actions/permissions/self-hosted-runners/repositories") + _qs, {}, undefined);
    }
  },
  {
    name: "actions/set-selected-repositories-self-hosted-runners-organization",
    description: "Set repositories allowed to use self-hosted runners in an organization. Sets repositories that are allowed to use self-hosted runners in an organization. Params: org (required).",
    inputSchema: {"type":"object","properties":{"org":{"type":"string","description":"The organization name. The name is not case sensitive."},"body":{"type":"object","description":"Request body"}},"required":["org","body"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("PUT", ("https://api.github.com/orgs/" + String(args["org"]) + "/actions/permissions/self-hosted-runners/repositories") + _qs, {}, args["body"]);
    }
  },
  {
    name: "actions/enable-selected-repository-self-hosted-runners-organization",
    description: "Add a repository to the list of repositories allowed to use self-hosted runners in an organization. Adds a repository to the list of repositories that are allowed to use self-hosted runners in an organization. Params: org (required), repository_id (required).",
    inputSchema: {"type":"object","properties":{"org":{"type":"string","description":"The organization name. The name is not case sensitive."},"repository_id":{"type":"integer","description":"The unique identifier of the repository."}},"required":["org","repository_id"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("PUT", ("https://api.github.com/orgs/" + String(args["org"]) + "/actions/permissions/self-hosted-runners/repositories/" + String(args["repository_id"])) + _qs, {}, undefined);
    }
  },
  {
    name: "actions/disable-selected-repository-self-hosted-runners-organization",
    description: "Remove a repository from the list of repositories allowed to use self-hosted runners in an organization. Removes a repository from the list of repositories that are allowed to use self-hosted runners in an organization. Params: org (required), repository_id (required).",
    inputSchema: {"type":"object","properties":{"org":{"type":"string","description":"The organization name. The name is not case sensitive."},"repository_id":{"type":"integer","description":"The unique identifier of the repository."}},"required":["org","repository_id"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("DELETE", ("https://api.github.com/orgs/" + String(args["org"]) + "/actions/permissions/self-hosted-runners/repositories/" + String(args["repository_id"])) + _qs, {}, undefined);
    }
  },
  {
    name: "actions/get-github-actions-default-workflow-permissions-organization",
    description: "Get default workflow permissions for an organization. Gets the default workflow permissions granted to the `GITHUB_TOKEN` when running workflows in an organization,\nas well as whether GitHub Actions can submit approving pull request reviews. Params: org (required).",
    inputSchema: {"type":"object","properties":{"org":{"type":"string","description":"The organization name. The name is not case sensitive."}},"required":["org"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/orgs/" + String(args["org"]) + "/actions/permissions/workflow") + _qs, {}, undefined);
    }
  },
  {
    name: "actions/set-github-actions-default-workflow-permissions-organization",
    description: "Set default workflow permissions for an organization. Sets the default workflow permissions granted to the `GITHUB_TOKEN` when running workflows in an organization, and sets if GitHub Actions\ncan submit approving pull request reviews. Params: org (required).",
    inputSchema: {"type":"object","properties":{"org":{"type":"string","description":"The organization name. The name is not case sensitive."},"body":{"type":"object","description":"Request body"}},"required":["org"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("PUT", ("https://api.github.com/orgs/" + String(args["org"]) + "/actions/permissions/workflow") + _qs, {}, args["body"]);
    }
  },
  {
    name: "actions/list-self-hosted-runner-groups-for-org",
    description: "List self-hosted runner groups for an organization. Lists all self-hosted runner groups configured in an organization and inherited from an enterprise. Params: org (required).",
    inputSchema: {"type":"object","properties":{"org":{"type":"string","description":"The organization name. The name is not case sensitive."},"per_page":{"type":"integer","default":30,"description":"The number of results per page (max 100). For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""},"page":{"type":"integer","default":1,"description":"The page number of the results to fetch. For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""},"visible_to_repository":{"type":"string","description":"Only return runner groups that are allowed to be used by this repository."}},"required":["org"]},
    call: function(args) {
    var _q = new URLSearchParams();
    if (args["per_page"] !== undefined) _q.set("per_page", String(args["per_page"]));
    if (args["page"] !== undefined) _q.set("page", String(args["page"]));
    if (args["visible_to_repository"] !== undefined) _q.set("visible_to_repository", String(args["visible_to_repository"]));
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/orgs/" + String(args["org"]) + "/actions/runner-groups") + _qs, {}, undefined);
    }
  },
  {
    name: "actions/create-self-hosted-runner-group-for-org",
    description: "Create a self-hosted runner group for an organization. Creates a new self-hosted runner group for an organization. Params: org (required).",
    inputSchema: {"type":"object","properties":{"org":{"type":"string","description":"The organization name. The name is not case sensitive."},"body":{"type":"object","description":"Request body"}},"required":["org","body"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("POST", ("https://api.github.com/orgs/" + String(args["org"]) + "/actions/runner-groups") + _qs, {}, args["body"]);
    }
  },
  {
    name: "actions/get-self-hosted-runner-group-for-org",
    description: "Get a self-hosted runner group for an organization. Gets a specific self-hosted runner group for an organization. Params: org (required), runner_group_id (required).",
    inputSchema: {"type":"object","properties":{"org":{"type":"string","description":"The organization name. The name is not case sensitive."},"runner_group_id":{"type":"integer","description":"Unique identifier of the self-hosted runner group."}},"required":["org","runner_group_id"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/orgs/" + String(args["org"]) + "/actions/runner-groups/" + String(args["runner_group_id"])) + _qs, {}, undefined);
    }
  },
  {
    name: "actions/update-self-hosted-runner-group-for-org",
    description: "Update a self-hosted runner group for an organization. Updates the `name` and `visibility` of a self-hosted runner group in an organization. Params: org (required), runner_group_id (required).",
    inputSchema: {"type":"object","properties":{"org":{"type":"string","description":"The organization name. The name is not case sensitive."},"runner_group_id":{"type":"integer","description":"Unique identifier of the self-hosted runner group."},"body":{"type":"object","description":"Request body"}},"required":["org","runner_group_id","body"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("PATCH", ("https://api.github.com/orgs/" + String(args["org"]) + "/actions/runner-groups/" + String(args["runner_group_id"])) + _qs, {}, args["body"]);
    }
  },
  {
    name: "actions/delete-self-hosted-runner-group-from-org",
    description: "Delete a self-hosted runner group from an organization. Deletes a self-hosted runner group for an organization. Params: org (required), runner_group_id (required).",
    inputSchema: {"type":"object","properties":{"org":{"type":"string","description":"The organization name. The name is not case sensitive."},"runner_group_id":{"type":"integer","description":"Unique identifier of the self-hosted runner group."}},"required":["org","runner_group_id"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("DELETE", ("https://api.github.com/orgs/" + String(args["org"]) + "/actions/runner-groups/" + String(args["runner_group_id"])) + _qs, {}, undefined);
    }
  },
  {
    name: "actions/list-github-hosted-runners-in-group-for-org",
    description: "List GitHub-hosted runners in a group for an organization. Lists the GitHub-hosted runners in an organization group. Params: org (required), runner_group_id (required).",
    inputSchema: {"type":"object","properties":{"org":{"type":"string","description":"The organization name. The name is not case sensitive."},"runner_group_id":{"type":"integer","description":"Unique identifier of the self-hosted runner group."},"per_page":{"type":"integer","default":30,"description":"The number of results per page (max 100). For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""},"page":{"type":"integer","default":1,"description":"The page number of the results to fetch. For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""}},"required":["org","runner_group_id"]},
    call: function(args) {
    var _q = new URLSearchParams();
    if (args["per_page"] !== undefined) _q.set("per_page", String(args["per_page"]));
    if (args["page"] !== undefined) _q.set("page", String(args["page"]));
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/orgs/" + String(args["org"]) + "/actions/runner-groups/" + String(args["runner_group_id"]) + "/hosted-runners") + _qs, {}, undefined);
    }
  },
  {
    name: "actions/list-repo-access-to-self-hosted-runner-group-in-org",
    description: "List repository access to a self-hosted runner group in an organization. Lists the repositories with access to a self-hosted runner group configured in an organization. Params: org (required), runner_group_id (required).",
    inputSchema: {"type":"object","properties":{"org":{"type":"string","description":"The organization name. The name is not case sensitive."},"runner_group_id":{"type":"integer","description":"Unique identifier of the self-hosted runner group."},"page":{"type":"integer","default":1,"description":"The page number of the results to fetch. For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""},"per_page":{"type":"integer","default":30,"description":"The number of results per page (max 100). For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""}},"required":["org","runner_group_id"]},
    call: function(args) {
    var _q = new URLSearchParams();
    if (args["page"] !== undefined) _q.set("page", String(args["page"]));
    if (args["per_page"] !== undefined) _q.set("per_page", String(args["per_page"]));
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/orgs/" + String(args["org"]) + "/actions/runner-groups/" + String(args["runner_group_id"]) + "/repositories") + _qs, {}, undefined);
    }
  },
  {
    name: "actions/set-repo-access-to-self-hosted-runner-group-in-org",
    description: "Set repository access for a self-hosted runner group in an organization. Replaces the list of repositories that have access to a self-hosted runner group configured in an organization. Params: org (required), runner_group_id (required).",
    inputSchema: {"type":"object","properties":{"org":{"type":"string","description":"The organization name. The name is not case sensitive."},"runner_group_id":{"type":"integer","description":"Unique identifier of the self-hosted runner group."},"body":{"type":"object","description":"Request body"}},"required":["org","runner_group_id","body"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("PUT", ("https://api.github.com/orgs/" + String(args["org"]) + "/actions/runner-groups/" + String(args["runner_group_id"]) + "/repositories") + _qs, {}, args["body"]);
    }
  },
  {
    name: "actions/add-repo-access-to-self-hosted-runner-group-in-org",
    description: "Add repository access to a self-hosted runner group in an organization. Adds a repository to the list of repositories that can access a self-hosted runner group. Params: org (required), runner_group_id (required), repository_id (required).",
    inputSchema: {"type":"object","properties":{"org":{"type":"string","description":"The organization name. The name is not case sensitive."},"runner_group_id":{"type":"integer","description":"Unique identifier of the self-hosted runner group."},"repository_id":{"type":"integer","description":"The unique identifier of the repository."}},"required":["org","runner_group_id","repository_id"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("PUT", ("https://api.github.com/orgs/" + String(args["org"]) + "/actions/runner-groups/" + String(args["runner_group_id"]) + "/repositories/" + String(args["repository_id"])) + _qs, {}, undefined);
    }
  },
  {
    name: "actions/remove-repo-access-to-self-hosted-runner-group-in-org",
    description: "Remove repository access to a self-hosted runner group in an organization. Removes a repository from the list of selected repositories that can access a self-hosted runner group. Params: org (required), runner_group_id (required), repository_id (required).",
    inputSchema: {"type":"object","properties":{"org":{"type":"string","description":"The organization name. The name is not case sensitive."},"runner_group_id":{"type":"integer","description":"Unique identifier of the self-hosted runner group."},"repository_id":{"type":"integer","description":"The unique identifier of the repository."}},"required":["org","runner_group_id","repository_id"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("DELETE", ("https://api.github.com/orgs/" + String(args["org"]) + "/actions/runner-groups/" + String(args["runner_group_id"]) + "/repositories/" + String(args["repository_id"])) + _qs, {}, undefined);
    }
  },
  {
    name: "actions/list-self-hosted-runners-in-group-for-org",
    description: "List self-hosted runners in a group for an organization. Lists self-hosted runners that are in a specific organization group. Params: org (required), runner_group_id (required).",
    inputSchema: {"type":"object","properties":{"org":{"type":"string","description":"The organization name. The name is not case sensitive."},"runner_group_id":{"type":"integer","description":"Unique identifier of the self-hosted runner group."},"per_page":{"type":"integer","default":30,"description":"The number of results per page (max 100). For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""},"page":{"type":"integer","default":1,"description":"The page number of the results to fetch. For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""}},"required":["org","runner_group_id"]},
    call: function(args) {
    var _q = new URLSearchParams();
    if (args["per_page"] !== undefined) _q.set("per_page", String(args["per_page"]));
    if (args["page"] !== undefined) _q.set("page", String(args["page"]));
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/orgs/" + String(args["org"]) + "/actions/runner-groups/" + String(args["runner_group_id"]) + "/runners") + _qs, {}, undefined);
    }
  },
  {
    name: "actions/set-self-hosted-runners-in-group-for-org",
    description: "Set self-hosted runners in a group for an organization. Replaces the list of self-hosted runners that are part of an organization runner group. Params: org (required), runner_group_id (required).",
    inputSchema: {"type":"object","properties":{"org":{"type":"string","description":"The organization name. The name is not case sensitive."},"runner_group_id":{"type":"integer","description":"Unique identifier of the self-hosted runner group."},"body":{"type":"object","description":"Request body"}},"required":["org","runner_group_id","body"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("PUT", ("https://api.github.com/orgs/" + String(args["org"]) + "/actions/runner-groups/" + String(args["runner_group_id"]) + "/runners") + _qs, {}, args["body"]);
    }
  },
  {
    name: "actions/add-self-hosted-runner-to-group-for-org",
    description: "Add a self-hosted runner to a group for an organization. Adds a self-hosted runner to a runner group configured in an organization. Params: org (required), runner_group_id (required), runner_id (required).",
    inputSchema: {"type":"object","properties":{"org":{"type":"string","description":"The organization name. The name is not case sensitive."},"runner_group_id":{"type":"integer","description":"Unique identifier of the self-hosted runner group."},"runner_id":{"type":"integer","description":"Unique identifier of the self-hosted runner."}},"required":["org","runner_group_id","runner_id"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("PUT", ("https://api.github.com/orgs/" + String(args["org"]) + "/actions/runner-groups/" + String(args["runner_group_id"]) + "/runners/" + String(args["runner_id"])) + _qs, {}, undefined);
    }
  },
  {
    name: "actions/remove-self-hosted-runner-from-group-for-org",
    description: "Remove a self-hosted runner from a group for an organization. Removes a self-hosted runner from a group configured in an organization. Params: org (required), runner_group_id (required), runner_id (required).",
    inputSchema: {"type":"object","properties":{"org":{"type":"string","description":"The organization name. The name is not case sensitive."},"runner_group_id":{"type":"integer","description":"Unique identifier of the self-hosted runner group."},"runner_id":{"type":"integer","description":"Unique identifier of the self-hosted runner."}},"required":["org","runner_group_id","runner_id"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("DELETE", ("https://api.github.com/orgs/" + String(args["org"]) + "/actions/runner-groups/" + String(args["runner_group_id"]) + "/runners/" + String(args["runner_id"])) + _qs, {}, undefined);
    }
  },
  {
    name: "actions/list-self-hosted-runners-for-org",
    description: "List self-hosted runners for an organization. Lists all self-hosted runners configured in an organization. Params: org (required).",
    inputSchema: {"type":"object","properties":{"name":{"type":"string","description":"The name of a self-hosted runner."},"org":{"type":"string","description":"The organization name. The name is not case sensitive."},"per_page":{"type":"integer","default":30,"description":"The number of results per page (max 100). For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""},"page":{"type":"integer","default":1,"description":"The page number of the results to fetch. For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""}},"required":["org"]},
    call: function(args) {
    var _q = new URLSearchParams();
    if (args["name"] !== undefined) _q.set("name", String(args["name"]));
    if (args["per_page"] !== undefined) _q.set("per_page", String(args["per_page"]));
    if (args["page"] !== undefined) _q.set("page", String(args["page"]));
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/orgs/" + String(args["org"]) + "/actions/runners") + _qs, {}, undefined);
    }
  },
  {
    name: "actions/list-runner-applications-for-org",
    description: "List runner applications for an organization. Lists binaries for the runner application that you can download and run. Params: org (required).",
    inputSchema: {"type":"object","properties":{"org":{"type":"string","description":"The organization name. The name is not case sensitive."}},"required":["org"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/orgs/" + String(args["org"]) + "/actions/runners/downloads") + _qs, {}, undefined);
    }
  },
  {
    name: "actions/generate-runner-jitconfig-for-org",
    description: "Create configuration for a just-in-time runner for an organization. Generates a configuration that can be passed to the runner application at startup. Params: org (required).",
    inputSchema: {"type":"object","properties":{"org":{"type":"string","description":"The organization name. The name is not case sensitive."},"body":{"type":"object","description":"Request body"}},"required":["org","body"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("POST", ("https://api.github.com/orgs/" + String(args["org"]) + "/actions/runners/generate-jitconfig") + _qs, {}, args["body"]);
    }
  },
  {
    name: "actions/create-registration-token-for-org",
    description: "Create a registration token for an organization. Returns a token that you can pass to the `config` script. Params: org (required).",
    inputSchema: {"type":"object","properties":{"org":{"type":"string","description":"The organization name. The name is not case sensitive."}},"required":["org"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("POST", ("https://api.github.com/orgs/" + String(args["org"]) + "/actions/runners/registration-token") + _qs, {}, undefined);
    }
  },
  {
    name: "actions/create-remove-token-for-org",
    description: "Create a remove token for an organization. Returns a token that you can pass to the `config` script to remove a self-hosted runner from an organization. Params: org (required).",
    inputSchema: {"type":"object","properties":{"org":{"type":"string","description":"The organization name. The name is not case sensitive."}},"required":["org"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("POST", ("https://api.github.com/orgs/" + String(args["org"]) + "/actions/runners/remove-token") + _qs, {}, undefined);
    }
  },
  {
    name: "actions/get-self-hosted-runner-for-org",
    description: "Get a self-hosted runner for an organization. Gets a specific self-hosted runner configured in an organization. Params: org (required), runner_id (required).",
    inputSchema: {"type":"object","properties":{"org":{"type":"string","description":"The organization name. The name is not case sensitive."},"runner_id":{"type":"integer","description":"Unique identifier of the self-hosted runner."}},"required":["org","runner_id"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/orgs/" + String(args["org"]) + "/actions/runners/" + String(args["runner_id"])) + _qs, {}, undefined);
    }
  },
  {
    name: "actions/delete-self-hosted-runner-from-org",
    description: "Delete a self-hosted runner from an organization. Forces the removal of a self-hosted runner from an organization. Params: org (required), runner_id (required).",
    inputSchema: {"type":"object","properties":{"org":{"type":"string","description":"The organization name. The name is not case sensitive."},"runner_id":{"type":"integer","description":"Unique identifier of the self-hosted runner."}},"required":["org","runner_id"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("DELETE", ("https://api.github.com/orgs/" + String(args["org"]) + "/actions/runners/" + String(args["runner_id"])) + _qs, {}, undefined);
    }
  },
  {
    name: "actions/list-labels-for-self-hosted-runner-for-org",
    description: "List labels for a self-hosted runner for an organization. Lists all labels for a self-hosted runner configured in an organization. Params: org (required), runner_id (required).",
    inputSchema: {"type":"object","properties":{"org":{"type":"string","description":"The organization name. The name is not case sensitive."},"runner_id":{"type":"integer","description":"Unique identifier of the self-hosted runner."}},"required":["org","runner_id"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/orgs/" + String(args["org"]) + "/actions/runners/" + String(args["runner_id"]) + "/labels") + _qs, {}, undefined);
    }
  },
  {
    name: "actions/add-custom-labels-to-self-hosted-runner-for-org",
    description: "Add custom labels to a self-hosted runner for an organization. Adds custom labels to a self-hosted runner configured in an organization. Params: org (required), runner_id (required).",
    inputSchema: {"type":"object","properties":{"org":{"type":"string","description":"The organization name. The name is not case sensitive."},"runner_id":{"type":"integer","description":"Unique identifier of the self-hosted runner."},"body":{"type":"object","description":"Request body"}},"required":["org","runner_id","body"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("POST", ("https://api.github.com/orgs/" + String(args["org"]) + "/actions/runners/" + String(args["runner_id"]) + "/labels") + _qs, {}, args["body"]);
    }
  },
  {
    name: "actions/set-custom-labels-for-self-hosted-runner-for-org",
    description: "Set custom labels for a self-hosted runner for an organization. Remove all previous custom labels and set the new custom labels for a specific\nself-hosted runner configured in an organization. Params: org (required), runner_id (required).",
    inputSchema: {"type":"object","properties":{"org":{"type":"string","description":"The organization name. The name is not case sensitive."},"runner_id":{"type":"integer","description":"Unique identifier of the self-hosted runner."},"body":{"type":"object","description":"Request body"}},"required":["org","runner_id","body"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("PUT", ("https://api.github.com/orgs/" + String(args["org"]) + "/actions/runners/" + String(args["runner_id"]) + "/labels") + _qs, {}, args["body"]);
    }
  },
  {
    name: "actions/remove-all-custom-labels-from-self-hosted-runner-for-org",
    description: "Remove all custom labels from a self-hosted runner for an organization. Remove all custom labels from a self-hosted runner configured in an\norganization. Params: org (required), runner_id (required).",
    inputSchema: {"type":"object","properties":{"org":{"type":"string","description":"The organization name. The name is not case sensitive."},"runner_id":{"type":"integer","description":"Unique identifier of the self-hosted runner."}},"required":["org","runner_id"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("DELETE", ("https://api.github.com/orgs/" + String(args["org"]) + "/actions/runners/" + String(args["runner_id"]) + "/labels") + _qs, {}, undefined);
    }
  },
  {
    name: "actions/remove-custom-label-from-self-hosted-runner-for-org",
    description: "Remove a custom label from a self-hosted runner for an organization. Remove a custom label from a self-hosted runner configured\nin an organization. Params: org (required), runner_id (required), name (required).",
    inputSchema: {"type":"object","properties":{"org":{"type":"string","description":"The organization name. The name is not case sensitive."},"runner_id":{"type":"integer","description":"Unique identifier of the self-hosted runner."},"name":{"type":"string","description":"The name of a self-hosted runner's custom label."}},"required":["org","runner_id","name"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("DELETE", ("https://api.github.com/orgs/" + String(args["org"]) + "/actions/runners/" + String(args["runner_id"]) + "/labels/" + String(args["name"])) + _qs, {}, undefined);
    }
  },
  {
    name: "actions/list-org-secrets",
    description: "List organization secrets. Lists all secrets available in an organization without revealing their\nencrypted values. Params: org (required).",
    inputSchema: {"type":"object","properties":{"org":{"type":"string","description":"The organization name. The name is not case sensitive."},"per_page":{"type":"integer","default":30,"description":"The number of results per page (max 100). For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""},"page":{"type":"integer","default":1,"description":"The page number of the results to fetch. For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""}},"required":["org"]},
    call: function(args) {
    var _q = new URLSearchParams();
    if (args["per_page"] !== undefined) _q.set("per_page", String(args["per_page"]));
    if (args["page"] !== undefined) _q.set("page", String(args["page"]));
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/orgs/" + String(args["org"]) + "/actions/secrets") + _qs, {}, undefined);
    }
  },
  {
    name: "actions/get-org-public-key",
    description: "Get an organization public key. Gets your public key, which you need to encrypt secrets. Params: org (required).",
    inputSchema: {"type":"object","properties":{"org":{"type":"string","description":"The organization name. The name is not case sensitive."}},"required":["org"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/orgs/" + String(args["org"]) + "/actions/secrets/public-key") + _qs, {}, undefined);
    }
  },
  {
    name: "actions/get-org-secret",
    description: "Get an organization secret. Gets a single organization secret without revealing its encrypted value. Params: org (required), secret_name (required).",
    inputSchema: {"type":"object","properties":{"org":{"type":"string","description":"The organization name. The name is not case sensitive."},"secret_name":{"type":"string","description":"The name of the secret."}},"required":["org","secret_name"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/orgs/" + String(args["org"]) + "/actions/secrets/" + String(args["secret_name"])) + _qs, {}, undefined);
    }
  },
  {
    name: "actions/create-or-update-org-secret",
    description: "Create or update an organization secret. Creates or updates an organization secret with an encrypted value. Params: org (required), secret_name (required).",
    inputSchema: {"type":"object","properties":{"org":{"type":"string","description":"The organization name. The name is not case sensitive."},"secret_name":{"type":"string","description":"The name of the secret."},"body":{"type":"object","description":"Request body"}},"required":["org","secret_name","body"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("PUT", ("https://api.github.com/orgs/" + String(args["org"]) + "/actions/secrets/" + String(args["secret_name"])) + _qs, {}, args["body"]);
    }
  },
  {
    name: "actions/delete-org-secret",
    description: "Delete an organization secret. Deletes a secret in an organization using the secret name. Params: org (required), secret_name (required).",
    inputSchema: {"type":"object","properties":{"org":{"type":"string","description":"The organization name. The name is not case sensitive."},"secret_name":{"type":"string","description":"The name of the secret."}},"required":["org","secret_name"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("DELETE", ("https://api.github.com/orgs/" + String(args["org"]) + "/actions/secrets/" + String(args["secret_name"])) + _qs, {}, undefined);
    }
  },
  {
    name: "actions/list-selected-repos-for-org-secret",
    description: "List selected repositories for an organization secret. Lists all repositories that have been selected when the `visibility`\nfor repository access to a secret is set to `selected`. Params: org (required), secret_name (required).",
    inputSchema: {"type":"object","properties":{"org":{"type":"string","description":"The organization name. The name is not case sensitive."},"secret_name":{"type":"string","description":"The name of the secret."},"page":{"type":"integer","default":1,"description":"The page number of the results to fetch. For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""},"per_page":{"type":"integer","default":30,"description":"The number of results per page (max 100). For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""}},"required":["org","secret_name"]},
    call: function(args) {
    var _q = new URLSearchParams();
    if (args["page"] !== undefined) _q.set("page", String(args["page"]));
    if (args["per_page"] !== undefined) _q.set("per_page", String(args["per_page"]));
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/orgs/" + String(args["org"]) + "/actions/secrets/" + String(args["secret_name"]) + "/repositories") + _qs, {}, undefined);
    }
  },
  {
    name: "actions/set-selected-repos-for-org-secret",
    description: "Set selected repositories for an organization secret. Replaces all repositories for an organization secret when the `visibility`\nfor repository access is set to `selected`. Params: org (required), secret_name (required).",
    inputSchema: {"type":"object","properties":{"org":{"type":"string","description":"The organization name. The name is not case sensitive."},"secret_name":{"type":"string","description":"The name of the secret."},"body":{"type":"object","description":"Request body"}},"required":["org","secret_name","body"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("PUT", ("https://api.github.com/orgs/" + String(args["org"]) + "/actions/secrets/" + String(args["secret_name"]) + "/repositories") + _qs, {}, args["body"]);
    }
  },
  {
    name: "actions/add-selected-repo-to-org-secret",
    description: "Add selected repository to an organization secret. Adds a repository to an organization secret when the `visibility` for\nrepository access is set to `selected`. Params: org (required), secret_name (required), repository_id (required).",
    inputSchema: {"type":"object","properties":{"org":{"type":"string","description":"The organization name. The name is not case sensitive."},"secret_name":{"type":"string","description":"The name of the secret."},"repository_id":{"type":"integer"}},"required":["org","secret_name","repository_id"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("PUT", ("https://api.github.com/orgs/" + String(args["org"]) + "/actions/secrets/" + String(args["secret_name"]) + "/repositories/" + String(args["repository_id"])) + _qs, {}, undefined);
    }
  },
  {
    name: "actions/remove-selected-repo-from-org-secret",
    description: "Remove selected repository from an organization secret. Removes a repository from an organization secret when the `visibility`\nfor repository access is set to `selected`. Params: org (required), secret_name (required), repository_id (required).",
    inputSchema: {"type":"object","properties":{"org":{"type":"string","description":"The organization name. The name is not case sensitive."},"secret_name":{"type":"string","description":"The name of the secret."},"repository_id":{"type":"integer"}},"required":["org","secret_name","repository_id"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("DELETE", ("https://api.github.com/orgs/" + String(args["org"]) + "/actions/secrets/" + String(args["secret_name"]) + "/repositories/" + String(args["repository_id"])) + _qs, {}, undefined);
    }
  },
  {
    name: "actions/list-org-variables",
    description: "List organization variables. Lists all organization variables. Params: org (required).",
    inputSchema: {"type":"object","properties":{"org":{"type":"string","description":"The organization name. The name is not case sensitive."},"per_page":{"type":"integer","default":10,"description":"The number of results per page (max 30). For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""},"page":{"type":"integer","default":1,"description":"The page number of the results to fetch. For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""}},"required":["org"]},
    call: function(args) {
    var _q = new URLSearchParams();
    if (args["per_page"] !== undefined) _q.set("per_page", String(args["per_page"]));
    if (args["page"] !== undefined) _q.set("page", String(args["page"]));
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/orgs/" + String(args["org"]) + "/actions/variables") + _qs, {}, undefined);
    }
  },
  {
    name: "actions/create-org-variable",
    description: "Create an organization variable. Creates an organization variable that you can reference in a GitHub Actions workflow. Params: org (required).",
    inputSchema: {"type":"object","properties":{"org":{"type":"string","description":"The organization name. The name is not case sensitive."},"body":{"type":"object","description":"Request body"}},"required":["org","body"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("POST", ("https://api.github.com/orgs/" + String(args["org"]) + "/actions/variables") + _qs, {}, args["body"]);
    }
  },
  {
    name: "actions/get-org-variable",
    description: "Get an organization variable. Gets a specific variable in an organization. Params: org (required), name (required).",
    inputSchema: {"type":"object","properties":{"org":{"type":"string","description":"The organization name. The name is not case sensitive."},"name":{"type":"string","description":"The name of the variable."}},"required":["org","name"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/orgs/" + String(args["org"]) + "/actions/variables/" + String(args["name"])) + _qs, {}, undefined);
    }
  },
  {
    name: "actions/update-org-variable",
    description: "Update an organization variable. Updates an organization variable that you can reference in a GitHub Actions workflow. Params: org (required), name (required).",
    inputSchema: {"type":"object","properties":{"org":{"type":"string","description":"The organization name. The name is not case sensitive."},"name":{"type":"string","description":"The name of the variable."},"body":{"type":"object","description":"Request body"}},"required":["org","name","body"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("PATCH", ("https://api.github.com/orgs/" + String(args["org"]) + "/actions/variables/" + String(args["name"])) + _qs, {}, args["body"]);
    }
  },
  {
    name: "actions/delete-org-variable",
    description: "Delete an organization variable. Deletes an organization variable using the variable name. Params: org (required), name (required).",
    inputSchema: {"type":"object","properties":{"org":{"type":"string","description":"The organization name. The name is not case sensitive."},"name":{"type":"string","description":"The name of the variable."}},"required":["org","name"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("DELETE", ("https://api.github.com/orgs/" + String(args["org"]) + "/actions/variables/" + String(args["name"])) + _qs, {}, undefined);
    }
  },
  {
    name: "actions/list-selected-repos-for-org-variable",
    description: "List selected repositories for an organization variable. Lists all repositories that can access an organization variable\nthat is available to selected repositories. Params: org (required), name (required).",
    inputSchema: {"type":"object","properties":{"org":{"type":"string","description":"The organization name. The name is not case sensitive."},"name":{"type":"string","description":"The name of the variable."},"page":{"type":"integer","default":1,"description":"The page number of the results to fetch. For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""},"per_page":{"type":"integer","default":30,"description":"The number of results per page (max 100). For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""}},"required":["org","name"]},
    call: function(args) {
    var _q = new URLSearchParams();
    if (args["page"] !== undefined) _q.set("page", String(args["page"]));
    if (args["per_page"] !== undefined) _q.set("per_page", String(args["per_page"]));
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/orgs/" + String(args["org"]) + "/actions/variables/" + String(args["name"]) + "/repositories") + _qs, {}, undefined);
    }
  },
  {
    name: "actions/set-selected-repos-for-org-variable",
    description: "Set selected repositories for an organization variable. Replaces all repositories for an organization variable that is available\nto selected repositories. Params: org (required), name (required).",
    inputSchema: {"type":"object","properties":{"org":{"type":"string","description":"The organization name. The name is not case sensitive."},"name":{"type":"string","description":"The name of the variable."},"body":{"type":"object","description":"Request body"}},"required":["org","name","body"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("PUT", ("https://api.github.com/orgs/" + String(args["org"]) + "/actions/variables/" + String(args["name"]) + "/repositories") + _qs, {}, args["body"]);
    }
  },
  {
    name: "actions/add-selected-repo-to-org-variable",
    description: "Add selected repository to an organization variable. Adds a repository to an organization variable that is available to selected repositories. Params: org (required), name (required), repository_id (required).",
    inputSchema: {"type":"object","properties":{"org":{"type":"string","description":"The organization name. The name is not case sensitive."},"name":{"type":"string","description":"The name of the variable."},"repository_id":{"type":"integer"}},"required":["org","name","repository_id"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("PUT", ("https://api.github.com/orgs/" + String(args["org"]) + "/actions/variables/" + String(args["name"]) + "/repositories/" + String(args["repository_id"])) + _qs, {}, undefined);
    }
  },
  {
    name: "actions/remove-selected-repo-from-org-variable",
    description: "Remove selected repository from an organization variable. Removes a repository from an organization variable that is\navailable to selected repositories. Params: org (required), name (required), repository_id (required).",
    inputSchema: {"type":"object","properties":{"org":{"type":"string","description":"The organization name. The name is not case sensitive."},"name":{"type":"string","description":"The name of the variable."},"repository_id":{"type":"integer"}},"required":["org","name","repository_id"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("DELETE", ("https://api.github.com/orgs/" + String(args["org"]) + "/actions/variables/" + String(args["name"]) + "/repositories/" + String(args["repository_id"])) + _qs, {}, undefined);
    }
  },
  {
    name: "agents/list-org-secrets",
    description: "List organization secrets. Lists all secrets available in an organization without revealing their\nencrypted values. Params: org (required).",
    inputSchema: {"type":"object","properties":{"org":{"type":"string","description":"The organization name. The name is not case sensitive."},"per_page":{"type":"integer","default":30,"description":"The number of results per page (max 100). For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""},"page":{"type":"integer","default":1,"description":"The page number of the results to fetch. For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""}},"required":["org"]},
    call: function(args) {
    var _q = new URLSearchParams();
    if (args["per_page"] !== undefined) _q.set("per_page", String(args["per_page"]));
    if (args["page"] !== undefined) _q.set("page", String(args["page"]));
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/orgs/" + String(args["org"]) + "/agents/secrets") + _qs, {}, undefined);
    }
  },
  {
    name: "agents/get-org-public-key",
    description: "Get an organization public key. Gets your public key, which you need to encrypt secrets. Params: org (required).",
    inputSchema: {"type":"object","properties":{"org":{"type":"string","description":"The organization name. The name is not case sensitive."}},"required":["org"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/orgs/" + String(args["org"]) + "/agents/secrets/public-key") + _qs, {}, undefined);
    }
  },
  {
    name: "agents/get-org-secret",
    description: "Get an organization secret. Gets a single organization secret without revealing its encrypted value. Params: org (required), secret_name (required).",
    inputSchema: {"type":"object","properties":{"org":{"type":"string","description":"The organization name. The name is not case sensitive."},"secret_name":{"type":"string","description":"The name of the secret."}},"required":["org","secret_name"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/orgs/" + String(args["org"]) + "/agents/secrets/" + String(args["secret_name"])) + _qs, {}, undefined);
    }
  },
  {
    name: "agents/create-or-update-org-secret",
    description: "Create or update an organization secret. Creates or updates an organization secret with an encrypted value. Params: org (required), secret_name (required).",
    inputSchema: {"type":"object","properties":{"org":{"type":"string","description":"The organization name. The name is not case sensitive."},"secret_name":{"type":"string","description":"The name of the secret."},"body":{"type":"object","description":"Request body"}},"required":["org","secret_name","body"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("PUT", ("https://api.github.com/orgs/" + String(args["org"]) + "/agents/secrets/" + String(args["secret_name"])) + _qs, {}, args["body"]);
    }
  },
  {
    name: "agents/delete-org-secret",
    description: "Delete an organization secret. Deletes a secret in an organization using the secret name. Params: org (required), secret_name (required).",
    inputSchema: {"type":"object","properties":{"org":{"type":"string","description":"The organization name. The name is not case sensitive."},"secret_name":{"type":"string","description":"The name of the secret."}},"required":["org","secret_name"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("DELETE", ("https://api.github.com/orgs/" + String(args["org"]) + "/agents/secrets/" + String(args["secret_name"])) + _qs, {}, undefined);
    }
  },
  {
    name: "agents/list-selected-repos-for-org-secret",
    description: "List selected repositories for an organization secret. Lists all repositories that have been selected when the `visibility`\nfor repository access to a secret is set to `selected`. Params: org (required), secret_name (required).",
    inputSchema: {"type":"object","properties":{"org":{"type":"string","description":"The organization name. The name is not case sensitive."},"secret_name":{"type":"string","description":"The name of the secret."},"page":{"type":"integer","default":1,"description":"The page number of the results to fetch. For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""},"per_page":{"type":"integer","default":30,"description":"The number of results per page (max 100). For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""}},"required":["org","secret_name"]},
    call: function(args) {
    var _q = new URLSearchParams();
    if (args["page"] !== undefined) _q.set("page", String(args["page"]));
    if (args["per_page"] !== undefined) _q.set("per_page", String(args["per_page"]));
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/orgs/" + String(args["org"]) + "/agents/secrets/" + String(args["secret_name"]) + "/repositories") + _qs, {}, undefined);
    }
  },
  {
    name: "agents/set-selected-repos-for-org-secret",
    description: "Set selected repositories for an organization secret. Replaces all repositories for an organization secret when the `visibility`\nfor repository access is set to `selected`. Params: org (required), secret_name (required).",
    inputSchema: {"type":"object","properties":{"org":{"type":"string","description":"The organization name. The name is not case sensitive."},"secret_name":{"type":"string","description":"The name of the secret."},"body":{"type":"object","description":"Request body"}},"required":["org","secret_name","body"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("PUT", ("https://api.github.com/orgs/" + String(args["org"]) + "/agents/secrets/" + String(args["secret_name"]) + "/repositories") + _qs, {}, args["body"]);
    }
  },
  {
    name: "agents/add-selected-repo-to-org-secret",
    description: "Add selected repository to an organization secret. Adds a repository to an organization secret when the `visibility` for\nrepository access is set to `selected`. Params: org (required), secret_name (required), repository_id (required).",
    inputSchema: {"type":"object","properties":{"org":{"type":"string","description":"The organization name. The name is not case sensitive."},"secret_name":{"type":"string","description":"The name of the secret."},"repository_id":{"type":"integer"}},"required":["org","secret_name","repository_id"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("PUT", ("https://api.github.com/orgs/" + String(args["org"]) + "/agents/secrets/" + String(args["secret_name"]) + "/repositories/" + String(args["repository_id"])) + _qs, {}, undefined);
    }
  },
  {
    name: "agents/remove-selected-repo-from-org-secret",
    description: "Remove selected repository from an organization secret. Removes a repository from an organization secret when the `visibility`\nfor repository access is set to `selected`. Params: org (required), secret_name (required), repository_id (required).",
    inputSchema: {"type":"object","properties":{"org":{"type":"string","description":"The organization name. The name is not case sensitive."},"secret_name":{"type":"string","description":"The name of the secret."},"repository_id":{"type":"integer"}},"required":["org","secret_name","repository_id"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("DELETE", ("https://api.github.com/orgs/" + String(args["org"]) + "/agents/secrets/" + String(args["secret_name"]) + "/repositories/" + String(args["repository_id"])) + _qs, {}, undefined);
    }
  },
  {
    name: "agents/list-org-variables",
    description: "List organization variables. Lists all agent variables available in an organization. Params: org (required).",
    inputSchema: {"type":"object","properties":{"org":{"type":"string","description":"The organization name. The name is not case sensitive."},"per_page":{"type":"integer","default":10,"description":"The number of results per page (max 30). For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""},"page":{"type":"integer","default":1,"description":"The page number of the results to fetch. For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""}},"required":["org"]},
    call: function(args) {
    var _q = new URLSearchParams();
    if (args["per_page"] !== undefined) _q.set("per_page", String(args["per_page"]));
    if (args["page"] !== undefined) _q.set("page", String(args["page"]));
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/orgs/" + String(args["org"]) + "/agents/variables") + _qs, {}, undefined);
    }
  },
  {
    name: "agents/create-org-variable",
    description: "Create an organization variable. Creates an organization agent variable that you can reference in a GitHub Actions workflow. Params: org (required).",
    inputSchema: {"type":"object","properties":{"org":{"type":"string","description":"The organization name. The name is not case sensitive."},"body":{"type":"object","description":"Request body"}},"required":["org","body"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("POST", ("https://api.github.com/orgs/" + String(args["org"]) + "/agents/variables") + _qs, {}, args["body"]);
    }
  },
  {
    name: "agents/get-org-variable",
    description: "Get an organization variable. Gets a specific agent variable in an organization. Params: org (required), name (required).",
    inputSchema: {"type":"object","properties":{"org":{"type":"string","description":"The organization name. The name is not case sensitive."},"name":{"type":"string","description":"The name of the variable."}},"required":["org","name"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/orgs/" + String(args["org"]) + "/agents/variables/" + String(args["name"])) + _qs, {}, undefined);
    }
  },
  {
    name: "agents/update-org-variable",
    description: "Update an organization variable. Updates an organization agent variable that you can reference in a GitHub Actions workflow. Params: org (required), name (required).",
    inputSchema: {"type":"object","properties":{"org":{"type":"string","description":"The organization name. The name is not case sensitive."},"name":{"type":"string","description":"The name of the variable."},"body":{"type":"object","description":"Request body"}},"required":["org","name","body"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("PATCH", ("https://api.github.com/orgs/" + String(args["org"]) + "/agents/variables/" + String(args["name"])) + _qs, {}, args["body"]);
    }
  },
  {
    name: "agents/delete-org-variable",
    description: "Delete an organization variable. Deletes an organization agent variable using the variable name. Params: org (required), name (required).",
    inputSchema: {"type":"object","properties":{"org":{"type":"string","description":"The organization name. The name is not case sensitive."},"name":{"type":"string","description":"The name of the variable."}},"required":["org","name"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("DELETE", ("https://api.github.com/orgs/" + String(args["org"]) + "/agents/variables/" + String(args["name"])) + _qs, {}, undefined);
    }
  },
  {
    name: "agents/list-selected-repos-for-org-variable",
    description: "List selected repositories for an organization variable. Lists all repositories that can access an organization agent variable\nthat is available to selected repositories. Params: org (required), name (required).",
    inputSchema: {"type":"object","properties":{"org":{"type":"string","description":"The organization name. The name is not case sensitive."},"name":{"type":"string","description":"The name of the variable."},"page":{"type":"integer","default":1,"description":"The page number of the results to fetch. For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""},"per_page":{"type":"integer","default":30,"description":"The number of results per page (max 100). For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""}},"required":["org","name"]},
    call: function(args) {
    var _q = new URLSearchParams();
    if (args["page"] !== undefined) _q.set("page", String(args["page"]));
    if (args["per_page"] !== undefined) _q.set("per_page", String(args["per_page"]));
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/orgs/" + String(args["org"]) + "/agents/variables/" + String(args["name"]) + "/repositories") + _qs, {}, undefined);
    }
  },
  {
    name: "agents/set-selected-repos-for-org-variable",
    description: "Set selected repositories for an organization variable. Replaces all repositories for an organization agent variable that is available\nto selected repositories. Params: org (required), name (required).",
    inputSchema: {"type":"object","properties":{"org":{"type":"string","description":"The organization name. The name is not case sensitive."},"name":{"type":"string","description":"The name of the variable."},"body":{"type":"object","description":"Request body"}},"required":["org","name","body"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("PUT", ("https://api.github.com/orgs/" + String(args["org"]) + "/agents/variables/" + String(args["name"]) + "/repositories") + _qs, {}, args["body"]);
    }
  },
  {
    name: "agents/add-selected-repo-to-org-variable",
    description: "Add selected repository to an organization variable. Adds a repository to an organization agent variable that is available to selected repositories. Params: org (required), name (required), repository_id (required).",
    inputSchema: {"type":"object","properties":{"org":{"type":"string","description":"The organization name. The name is not case sensitive."},"name":{"type":"string","description":"The name of the variable."},"repository_id":{"type":"integer"}},"required":["org","name","repository_id"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("PUT", ("https://api.github.com/orgs/" + String(args["org"]) + "/agents/variables/" + String(args["name"]) + "/repositories/" + String(args["repository_id"])) + _qs, {}, undefined);
    }
  },
  {
    name: "agents/remove-selected-repo-from-org-variable",
    description: "Remove selected repository from an organization variable. Removes a repository from an organization agent variable that is\navailable to selected repositories. Params: org (required), name (required), repository_id (required).",
    inputSchema: {"type":"object","properties":{"org":{"type":"string","description":"The organization name. The name is not case sensitive."},"name":{"type":"string","description":"The name of the variable."},"repository_id":{"type":"integer"}},"required":["org","name","repository_id"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("DELETE", ("https://api.github.com/orgs/" + String(args["org"]) + "/agents/variables/" + String(args["name"]) + "/repositories/" + String(args["repository_id"])) + _qs, {}, undefined);
    }
  },
  {
    name: "orgs/create-artifact-deployment-record",
    description: "Create an artifact deployment record. Create or update deployment records for an artifact associated\nwith an organization. Params: org (required).",
    inputSchema: {"type":"object","properties":{"org":{"type":"string","description":"The organization name. The name is not case sensitive."},"body":{"type":"object","description":"Request body"}},"required":["org","body"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("POST", ("https://api.github.com/orgs/" + String(args["org"]) + "/artifacts/metadata/deployment-record") + _qs, {}, args["body"]);
    }
  },
  {
    name: "orgs/set-cluster-deployment-records",
    description: "Set cluster deployment records. Set deployment records for a given cluster. Params: org (required), cluster (required).",
    inputSchema: {"type":"object","properties":{"org":{"type":"string","description":"The organization name. The name is not case sensitive."},"cluster":{"type":"string","minLength":1,"maxLength":128,"pattern":"^[a-zA-Z0-9._-]+$","description":"The cluster name."},"body":{"type":"object","description":"Request body"}},"required":["org","cluster","body"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("POST", ("https://api.github.com/orgs/" + String(args["org"]) + "/artifacts/metadata/deployment-record/cluster/" + String(args["cluster"])) + _qs, {}, args["body"]);
    }
  },
  {
    name: "orgs/create-artifact-storage-record",
    description: "Create artifact metadata storage record. Create metadata storage records for artifacts associated with an organization. Params: org (required).",
    inputSchema: {"type":"object","properties":{"org":{"type":"string","description":"The organization name. The name is not case sensitive."},"body":{"type":"object","description":"Request body"}},"required":["org","body"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("POST", ("https://api.github.com/orgs/" + String(args["org"]) + "/artifacts/metadata/storage-record") + _qs, {}, args["body"]);
    }
  },
  {
    name: "orgs/list-artifact-deployment-records",
    description: "List artifact deployment records. List deployment records for an artifact metadata associated with an organization. Params: org (required), subject_digest (required).",
    inputSchema: {"type":"object","properties":{"org":{"type":"string","description":"The organization name. The name is not case sensitive."},"subject_digest":{"type":"string","minLength":71,"maxLength":71,"pattern":"^sha256:[a-f0-9]{64}$","description":"The SHA256 digest of the artifact, in the form `sha256:HEX_DIGEST`."}},"required":["org","subject_digest"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/orgs/" + String(args["org"]) + "/artifacts/" + String(args["subject_digest"]) + "/metadata/deployment-records") + _qs, {}, undefined);
    }
  },
  {
    name: "orgs/list-artifact-storage-records",
    description: "List artifact storage records. List artifact storage records with a given subject digest for repositories owned by an organization. Params: org (required), subject_digest (required).",
    inputSchema: {"type":"object","properties":{"org":{"type":"string","description":"The organization name. The name is not case sensitive."},"subject_digest":{"type":"string","minLength":71,"maxLength":71,"pattern":"^sha256:[a-f0-9]{64}$","description":"The parameter should be set to the attestation's subject's SHA256 digest, in the form `sha256:HEX_DIGEST`."}},"required":["org","subject_digest"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/orgs/" + String(args["org"]) + "/artifacts/" + String(args["subject_digest"]) + "/metadata/storage-records") + _qs, {}, undefined);
    }
  },
  {
    name: "orgs/list-attestations-bulk",
    description: "List attestations by bulk subject digests. List a collection of artifact attestations associated with any entry in a list of subject digests owned by an organization. Params: org (required).",
    inputSchema: {"type":"object","properties":{"per_page":{"type":"integer","default":30,"description":"The number of results per page (max 100). For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""},"before":{"type":"string","description":"A cursor, as given in the [Link header](https://docs.github.com/rest/guides/using-pagination-in-the-rest-api#using-link-headers). If specified, the query only searches for results before this cursor. For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""},"after":{"type":"string","description":"A cursor, as given in the [Link header](https://docs.github.com/rest/guides/using-pagination-in-the-rest-api#using-link-headers). If specified, the query only searches for results after this cursor. For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""},"org":{"type":"string","description":"The organization name. The name is not case sensitive."},"body":{"type":"object","description":"Request body"}},"required":["org","body"]},
    call: function(args) {
    var _q = new URLSearchParams();
    if (args["per_page"] !== undefined) _q.set("per_page", String(args["per_page"]));
    if (args["before"] !== undefined) _q.set("before", String(args["before"]));
    if (args["after"] !== undefined) _q.set("after", String(args["after"]));
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("POST", ("https://api.github.com/orgs/" + String(args["org"]) + "/attestations/bulk-list") + _qs, {}, args["body"]);
    }
  },
  {
    name: "orgs/delete-attestations-bulk",
    description: "Delete attestations in bulk. Delete artifact attestations in bulk by either subject digests or unique ID. Params: org (required).",
    inputSchema: {"type":"object","properties":{"org":{"type":"string","description":"The organization name. The name is not case sensitive."},"body":{"type":"object","description":"Request body"}},"required":["org","body"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("POST", ("https://api.github.com/orgs/" + String(args["org"]) + "/attestations/delete-request") + _qs, {}, args["body"]);
    }
  },
  {
    name: "orgs/delete-attestations-by-subject-digest",
    description: "Delete attestations by subject digest. Delete an artifact attestation by subject digest. Params: org (required), subject_digest (required).",
    inputSchema: {"type":"object","properties":{"org":{"type":"string","description":"The organization name. The name is not case sensitive."},"subject_digest":{"type":"string","description":"Subject Digest"}},"required":["org","subject_digest"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("DELETE", ("https://api.github.com/orgs/" + String(args["org"]) + "/attestations/digest/" + String(args["subject_digest"])) + _qs, {}, undefined);
    }
  },
  {
    name: "orgs/list-attestation-repositories",
    description: "List attestation repositories. List repositories owned by the provided organization that have created at least one attested artifact\nResults will be sorted in ascending order by repository ID. Params: org (required).",
    inputSchema: {"type":"object","properties":{"per_page":{"type":"integer","default":30,"description":"The number of results per page (max 100). For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""},"before":{"type":"string","description":"A cursor, as given in the [Link header](https://docs.github.com/rest/guides/using-pagination-in-the-rest-api#using-link-headers). If specified, the query only searches for results before this cursor. For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""},"after":{"type":"string","description":"A cursor, as given in the [Link header](https://docs.github.com/rest/guides/using-pagination-in-the-rest-api#using-link-headers). If specified, the query only searches for results after this cursor. For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""},"org":{"type":"string","description":"The organization name. The name is not case sensitive."},"predicate_type":{"type":"string","description":"Optional filter for fetching attestations with a given predicate type.\nThis option accepts `provenance`, `sbom`, `release`, or freeform text\nfor custom predicate types."}},"required":["org"]},
    call: function(args) {
    var _q = new URLSearchParams();
    if (args["per_page"] !== undefined) _q.set("per_page", String(args["per_page"]));
    if (args["before"] !== undefined) _q.set("before", String(args["before"]));
    if (args["after"] !== undefined) _q.set("after", String(args["after"]));
    if (args["predicate_type"] !== undefined) _q.set("predicate_type", String(args["predicate_type"]));
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/orgs/" + String(args["org"]) + "/attestations/repositories") + _qs, {}, undefined);
    }
  },
  {
    name: "orgs/delete-attestations-by-id",
    description: "Delete attestations by ID. Delete an artifact attestation by unique ID that is associated with a repository owned by an org. Params: org (required), attestation_id (required).",
    inputSchema: {"type":"object","properties":{"org":{"type":"string","description":"The organization name. The name is not case sensitive."},"attestation_id":{"type":"integer","description":"Attestation ID"}},"required":["org","attestation_id"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("DELETE", ("https://api.github.com/orgs/" + String(args["org"]) + "/attestations/" + String(args["attestation_id"])) + _qs, {}, undefined);
    }
  },
  {
    name: "orgs/list-attestations",
    description: "List attestations. List a collection of artifact attestations with a given subject digest that are associated with repositories owned by an organization. Params: org (required), subject_digest (required).",
    inputSchema: {"type":"object","properties":{"per_page":{"type":"integer","default":30,"description":"The number of results per page (max 100). For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""},"before":{"type":"string","description":"A cursor, as given in the [Link header](https://docs.github.com/rest/guides/using-pagination-in-the-rest-api#using-link-headers). If specified, the query only searches for results before this cursor. For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""},"after":{"type":"string","description":"A cursor, as given in the [Link header](https://docs.github.com/rest/guides/using-pagination-in-the-rest-api#using-link-headers). If specified, the query only searches for results after this cursor. For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""},"org":{"type":"string","description":"The organization name. The name is not case sensitive."},"subject_digest":{"type":"string","description":"The parameter should be set to the attestation's subject's SHA256 digest, in the form `sha256:HEX_DIGEST`."},"predicate_type":{"type":"string","description":"Optional filter for fetching attestations with a given predicate type.\nThis option accepts `provenance`, `sbom`, `release`, or freeform text\nfor custom predicate types."}},"required":["org","subject_digest"]},
    call: function(args) {
    var _q = new URLSearchParams();
    if (args["per_page"] !== undefined) _q.set("per_page", String(args["per_page"]));
    if (args["before"] !== undefined) _q.set("before", String(args["before"]));
    if (args["after"] !== undefined) _q.set("after", String(args["after"]));
    if (args["predicate_type"] !== undefined) _q.set("predicate_type", String(args["predicate_type"]));
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/orgs/" + String(args["org"]) + "/attestations/" + String(args["subject_digest"])) + _qs, {}, undefined);
    }
  },
  {
    name: "orgs/list-blocked-users",
    description: "List users blocked by an organization. List the users blocked by an organization. Params: org (required).",
    inputSchema: {"type":"object","properties":{"org":{"type":"string","description":"The organization name. The name is not case sensitive."},"per_page":{"type":"integer","default":30,"description":"The number of results per page (max 100). For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""},"page":{"type":"integer","default":1,"description":"The page number of the results to fetch. For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""}},"required":["org"]},
    call: function(args) {
    var _q = new URLSearchParams();
    if (args["per_page"] !== undefined) _q.set("per_page", String(args["per_page"]));
    if (args["page"] !== undefined) _q.set("page", String(args["page"]));
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/orgs/" + String(args["org"]) + "/blocks") + _qs, {}, undefined);
    }
  },
  {
    name: "orgs/check-blocked-user",
    description: "Check if a user is blocked by an organization. Returns a 204 if the given user is blocked by the given organization. Params: org (required), username (required).",
    inputSchema: {"type":"object","properties":{"org":{"type":"string","description":"The organization name. The name is not case sensitive."},"username":{"type":"string","description":"The handle for the GitHub user account."}},"required":["org","username"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/orgs/" + String(args["org"]) + "/blocks/" + String(args["username"])) + _qs, {}, undefined);
    }
  },
  {
    name: "orgs/block-user",
    description: "Block a user from an organization. Blocks the given user on behalf of the specified organization and returns a 204. Params: org (required), username (required).",
    inputSchema: {"type":"object","properties":{"org":{"type":"string","description":"The organization name. The name is not case sensitive."},"username":{"type":"string","description":"The handle for the GitHub user account."}},"required":["org","username"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("PUT", ("https://api.github.com/orgs/" + String(args["org"]) + "/blocks/" + String(args["username"])) + _qs, {}, undefined);
    }
  },
  {
    name: "orgs/unblock-user",
    description: "Unblock a user from an organization. Unblocks the given user on behalf of the specified organization. Params: org (required), username (required).",
    inputSchema: {"type":"object","properties":{"org":{"type":"string","description":"The organization name. The name is not case sensitive."},"username":{"type":"string","description":"The handle for the GitHub user account."}},"required":["org","username"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("DELETE", ("https://api.github.com/orgs/" + String(args["org"]) + "/blocks/" + String(args["username"])) + _qs, {}, undefined);
    }
  },
  {
    name: "campaigns/list-org-campaigns",
    description: "List campaigns for an organization. Lists campaigns in an organization. Params: org (required).",
    inputSchema: {"type":"object","properties":{"org":{"type":"string","description":"The organization name. The name is not case sensitive."},"page":{"type":"integer","default":1,"description":"The page number of the results to fetch. For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""},"per_page":{"type":"integer","default":30,"description":"The number of results per page (max 100). For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""},"direction":{"type":"string","enum":["asc","desc"],"default":"desc","description":"The direction to sort the results by."},"state":{"$ref":"#/components/schemas/campaign-state","description":"If specified, only campaigns with this state will be returned."},"sort":{"type":"string","enum":["created","updated","ends_at","published"],"default":"created","description":"The property by which to sort the results."}},"required":["org"]},
    call: function(args) {
    var _q = new URLSearchParams();
    if (args["page"] !== undefined) _q.set("page", String(args["page"]));
    if (args["per_page"] !== undefined) _q.set("per_page", String(args["per_page"]));
    if (args["direction"] !== undefined) _q.set("direction", String(args["direction"]));
    if (args["state"] !== undefined) _q.set("state", String(args["state"]));
    if (args["sort"] !== undefined) _q.set("sort", String(args["sort"]));
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/orgs/" + String(args["org"]) + "/campaigns") + _qs, {}, undefined);
    }
  },
  {
    name: "campaigns/create-campaign",
    description: "Create a campaign for an organization. Params: org (required).",
    inputSchema: {"type":"object","properties":{"org":{"type":"string","description":"The organization name. The name is not case sensitive."},"body":{"type":"object","description":"Request body"}},"required":["org","body"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("POST", ("https://api.github.com/orgs/" + String(args["org"]) + "/campaigns") + _qs, {}, args["body"]);
    }
  },
  {
    name: "campaigns/get-campaign-summary",
    description: "Get a campaign for an organization. Gets a campaign for an organization. Params: org (required), campaign_number (required).",
    inputSchema: {"type":"object","properties":{"org":{"type":"string","description":"The organization name. The name is not case sensitive."},"campaign_number":{"type":"integer","description":"The campaign number."}},"required":["org","campaign_number"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/orgs/" + String(args["org"]) + "/campaigns/" + String(args["campaign_number"])) + _qs, {}, undefined);
    }
  },
  {
    name: "campaigns/update-campaign",
    description: "Update a campaign. Updates a campaign in an organization. Params: org (required), campaign_number (required).",
    inputSchema: {"type":"object","properties":{"org":{"type":"string","description":"The organization name. The name is not case sensitive."},"campaign_number":{"type":"integer","description":"The campaign number."},"body":{"type":"object","description":"Request body"}},"required":["org","campaign_number","body"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("PATCH", ("https://api.github.com/orgs/" + String(args["org"]) + "/campaigns/" + String(args["campaign_number"])) + _qs, {}, args["body"]);
    }
  },
  {
    name: "campaigns/delete-campaign",
    description: "Delete a campaign for an organization. Deletes a campaign in an organization. Params: org (required), campaign_number (required).",
    inputSchema: {"type":"object","properties":{"org":{"type":"string","description":"The organization name. The name is not case sensitive."},"campaign_number":{"type":"integer","description":"The campaign number."}},"required":["org","campaign_number"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("DELETE", ("https://api.github.com/orgs/" + String(args["org"]) + "/campaigns/" + String(args["campaign_number"])) + _qs, {}, undefined);
    }
  },
  {
    name: "code-scanning/list-alerts-for-org",
    description: "List code scanning alerts for an organization. Lists code scanning alerts for the default branch for all eligible repositories in an organization. Params: org (required).",
    inputSchema: {"type":"object","properties":{"org":{"type":"string","description":"The organization name. The name is not case sensitive."},"tool_name":{"$ref":"#/components/schemas/code-scanning-analysis-tool-name","description":"The name of a code scanning tool. Only results by this tool will be listed. You can specify the tool by using either `tool_name` or `tool_guid`, but not both."},"tool_guid":{"$ref":"#/components/schemas/code-scanning-analysis-tool-guid","description":"The GUID of a code scanning tool. Only results by this tool will be listed. Note that some code scanning tools may not include a GUID in their analysis data. You can specify the tool by using either `tool_guid` or `tool_name`, but not both."},"before":{"type":"string","description":"A cursor, as given in the [Link header](https://docs.github.com/rest/guides/using-pagination-in-the-rest-api#using-link-headers). If specified, the query only searches for results before this cursor. For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""},"after":{"type":"string","description":"A cursor, as given in the [Link header](https://docs.github.com/rest/guides/using-pagination-in-the-rest-api#using-link-headers). If specified, the query only searches for results after this cursor. For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""},"page":{"type":"integer","default":1,"description":"The page number of the results to fetch. For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""},"per_page":{"type":"integer","default":30,"description":"The number of results per page (max 100). For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""},"direction":{"type":"string","enum":["asc","desc"],"default":"desc","description":"The direction to sort the results by."},"state":{"$ref":"#/components/schemas/code-scanning-alert-state-query","description":"If specified, only code scanning alerts with this state will be returned."},"sort":{"type":"string","enum":["created","updated"],"default":"created","description":"The property by which to sort the results."},"severity":{"$ref":"#/components/schemas/code-scanning-alert-severity","description":"If specified, only code scanning alerts with this severity will be returned."},"assignees":{"type":"string","description":"Filter alerts by assignees. Provide a comma-separated list of user handles (e.g., `octocat` or `octocat,hubot`).\nUse `*` to list alerts with at least one assignee or `none` to list alerts with no assignees.\n"}},"required":["org"]},
    call: function(args) {
    var _q = new URLSearchParams();
    if (args["tool_name"] !== undefined) _q.set("tool_name", String(args["tool_name"]));
    if (args["tool_guid"] !== undefined) _q.set("tool_guid", String(args["tool_guid"]));
    if (args["before"] !== undefined) _q.set("before", String(args["before"]));
    if (args["after"] !== undefined) _q.set("after", String(args["after"]));
    if (args["page"] !== undefined) _q.set("page", String(args["page"]));
    if (args["per_page"] !== undefined) _q.set("per_page", String(args["per_page"]));
    if (args["direction"] !== undefined) _q.set("direction", String(args["direction"]));
    if (args["state"] !== undefined) _q.set("state", String(args["state"]));
    if (args["sort"] !== undefined) _q.set("sort", String(args["sort"]));
    if (args["severity"] !== undefined) _q.set("severity", String(args["severity"]));
    if (args["assignees"] !== undefined) _q.set("assignees", String(args["assignees"]));
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/orgs/" + String(args["org"]) + "/code-scanning/alerts") + _qs, {}, undefined);
    }
  },
  {
    name: "code-security/get-configurations-for-org",
    description: "Get code security configurations for an organization. Lists all code security configurations available in an organization. Params: org (required).",
    inputSchema: {"type":"object","properties":{"org":{"type":"string","description":"The organization name. The name is not case sensitive."},"target_type":{"type":"string","enum":["global","all"],"default":"all","description":"The target type of the code security configuration"},"per_page":{"type":"integer","default":30,"description":"The number of results per page (max 100). For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""},"before":{"type":"string","description":"A cursor, as given in the [Link header](https://docs.github.com/rest/guides/using-pagination-in-the-rest-api#using-link-headers). If specified, the query only searches for results before this cursor. For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""},"after":{"type":"string","description":"A cursor, as given in the [Link header](https://docs.github.com/rest/guides/using-pagination-in-the-rest-api#using-link-headers). If specified, the query only searches for results after this cursor. For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""}},"required":["org"]},
    call: function(args) {
    var _q = new URLSearchParams();
    if (args["target_type"] !== undefined) _q.set("target_type", String(args["target_type"]));
    if (args["per_page"] !== undefined) _q.set("per_page", String(args["per_page"]));
    if (args["before"] !== undefined) _q.set("before", String(args["before"]));
    if (args["after"] !== undefined) _q.set("after", String(args["after"]));
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/orgs/" + String(args["org"]) + "/code-security/configurations") + _qs, {}, undefined);
    }
  },
  {
    name: "code-security/create-configuration",
    description: "Create a code security configuration. Creates a code security configuration in an organization. Params: org (required).",
    inputSchema: {"type":"object","properties":{"org":{"type":"string","description":"The organization name. The name is not case sensitive."},"body":{"type":"object","description":"Request body"}},"required":["org","body"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("POST", ("https://api.github.com/orgs/" + String(args["org"]) + "/code-security/configurations") + _qs, {}, args["body"]);
    }
  },
  {
    name: "code-security/get-default-configurations",
    description: "Get default code security configurations. Lists the default code security configurations for an organization. Params: org (required).",
    inputSchema: {"type":"object","properties":{"org":{"type":"string","description":"The organization name. The name is not case sensitive."}},"required":["org"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/orgs/" + String(args["org"]) + "/code-security/configurations/defaults") + _qs, {}, undefined);
    }
  },
  {
    name: "code-security/detach-configuration",
    description: "Detach configurations from repositories. Detach code security configuration(s) from a set of repositories. Params: org (required).",
    inputSchema: {"type":"object","properties":{"org":{"type":"string","description":"The organization name. The name is not case sensitive."},"body":{"type":"object","description":"Request body"}},"required":["org","body"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("DELETE", ("https://api.github.com/orgs/" + String(args["org"]) + "/code-security/configurations/detach") + _qs, {}, args["body"]);
    }
  },
  {
    name: "code-security/get-configuration",
    description: "Get a code security configuration. Gets a code security configuration available in an organization. Params: org (required), configuration_id (required).",
    inputSchema: {"type":"object","properties":{"org":{"type":"string","description":"The organization name. The name is not case sensitive."},"configuration_id":{"type":"integer","description":"The unique identifier of the code security configuration."}},"required":["org","configuration_id"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/orgs/" + String(args["org"]) + "/code-security/configurations/" + String(args["configuration_id"])) + _qs, {}, undefined);
    }
  },
  {
    name: "code-security/update-configuration",
    description: "Update a code security configuration. Updates a code security configuration in an organization. Params: org (required), configuration_id (required).",
    inputSchema: {"type":"object","properties":{"org":{"type":"string","description":"The organization name. The name is not case sensitive."},"configuration_id":{"type":"integer","description":"The unique identifier of the code security configuration."},"body":{"type":"object","description":"Request body"}},"required":["org","configuration_id","body"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("PATCH", ("https://api.github.com/orgs/" + String(args["org"]) + "/code-security/configurations/" + String(args["configuration_id"])) + _qs, {}, args["body"]);
    }
  },
  {
    name: "code-security/delete-configuration",
    description: "Delete a code security configuration. Deletes the desired code security configuration from an organization. Params: org (required), configuration_id (required).",
    inputSchema: {"type":"object","properties":{"org":{"type":"string","description":"The organization name. The name is not case sensitive."},"configuration_id":{"type":"integer","description":"The unique identifier of the code security configuration."}},"required":["org","configuration_id"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("DELETE", ("https://api.github.com/orgs/" + String(args["org"]) + "/code-security/configurations/" + String(args["configuration_id"])) + _qs, {}, undefined);
    }
  },
  {
    name: "code-security/attach-configuration",
    description: "Attach a configuration to repositories. Attach a code security configuration to a set of repositories. Params: org (required), configuration_id (required).",
    inputSchema: {"type":"object","properties":{"org":{"type":"string","description":"The organization name. The name is not case sensitive."},"configuration_id":{"type":"integer","description":"The unique identifier of the code security configuration."},"body":{"type":"object","description":"Request body"}},"required":["org","configuration_id","body"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("POST", ("https://api.github.com/orgs/" + String(args["org"]) + "/code-security/configurations/" + String(args["configuration_id"]) + "/attach") + _qs, {}, args["body"]);
    }
  },
  {
    name: "code-security/set-configuration-as-default",
    description: "Set a code security configuration as a default for an organization. Sets a code security configuration as a default to be applied to new repositories in your organization. Params: org (required), configuration_id (required).",
    inputSchema: {"type":"object","properties":{"org":{"type":"string","description":"The organization name. The name is not case sensitive."},"configuration_id":{"type":"integer","description":"The unique identifier of the code security configuration."},"body":{"type":"object","description":"Request body"}},"required":["org","configuration_id","body"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("PUT", ("https://api.github.com/orgs/" + String(args["org"]) + "/code-security/configurations/" + String(args["configuration_id"]) + "/defaults") + _qs, {}, args["body"]);
    }
  },
  {
    name: "code-security/get-repositories-for-configuration",
    description: "Get repositories associated with a code security configuration. Lists the repositories associated with a code security configuration in an organization. Params: org (required), configuration_id (required).",
    inputSchema: {"type":"object","properties":{"org":{"type":"string","description":"The organization name. The name is not case sensitive."},"configuration_id":{"type":"integer","description":"The unique identifier of the code security configuration."},"per_page":{"type":"integer","default":30,"description":"The number of results per page (max 100). For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""},"before":{"type":"string","description":"A cursor, as given in the [Link header](https://docs.github.com/rest/guides/using-pagination-in-the-rest-api#using-link-headers). If specified, the query only searches for results before this cursor. For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""},"after":{"type":"string","description":"A cursor, as given in the [Link header](https://docs.github.com/rest/guides/using-pagination-in-the-rest-api#using-link-headers). If specified, the query only searches for results after this cursor. For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""},"status":{"type":"string","default":"all","description":"A comma-separated list of statuses. If specified, only repositories with these attachment statuses will be returned.\n\nCan be: `all`, `attached`, `attaching`, `detached`, `removed`, `enforced`, `failed`, `updating`, `removed_by_enterprise`"}},"required":["org","configuration_id"]},
    call: function(args) {
    var _q = new URLSearchParams();
    if (args["per_page"] !== undefined) _q.set("per_page", String(args["per_page"]));
    if (args["before"] !== undefined) _q.set("before", String(args["before"]));
    if (args["after"] !== undefined) _q.set("after", String(args["after"]));
    if (args["status"] !== undefined) _q.set("status", String(args["status"]));
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/orgs/" + String(args["org"]) + "/code-security/configurations/" + String(args["configuration_id"]) + "/repositories") + _qs, {}, undefined);
    }
  },
  {
    name: "codespaces/list-in-organization",
    description: "List codespaces for the organization. Lists the codespaces associated to a specified organization. Params: org (required).",
    inputSchema: {"type":"object","properties":{"per_page":{"type":"integer","default":30,"description":"The number of results per page (max 100). For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""},"page":{"type":"integer","default":1,"description":"The page number of the results to fetch. For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""},"org":{"type":"string","description":"The organization name. The name is not case sensitive."}},"required":["org"]},
    call: function(args) {
    var _q = new URLSearchParams();
    if (args["per_page"] !== undefined) _q.set("per_page", String(args["per_page"]));
    if (args["page"] !== undefined) _q.set("page", String(args["page"]));
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/orgs/" + String(args["org"]) + "/codespaces") + _qs, {}, undefined);
    }
  },
  {
    name: "codespaces/set-codespaces-access",
    description: "Manage access control for organization codespaces. Sets which users can access codespaces in an organization. Params: org (required).",
    inputSchema: {"type":"object","properties":{"org":{"type":"string","description":"The organization name. The name is not case sensitive."},"body":{"type":"object","description":"Request body"}},"required":["org","body"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("PUT", ("https://api.github.com/orgs/" + String(args["org"]) + "/codespaces/access") + _qs, {}, args["body"]);
    }
  },
  {
    name: "codespaces/set-codespaces-access-users",
    description: "Add users to Codespaces access for an organization. Codespaces for the specified users will be billed to the organization. Params: org (required).",
    inputSchema: {"type":"object","properties":{"org":{"type":"string","description":"The organization name. The name is not case sensitive."},"body":{"type":"object","description":"Request body"}},"required":["org","body"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("POST", ("https://api.github.com/orgs/" + String(args["org"]) + "/codespaces/access/selected_users") + _qs, {}, args["body"]);
    }
  },
  {
    name: "codespaces/delete-codespaces-access-users",
    description: "Remove users from Codespaces access for an organization. Codespaces for the specified users will no longer be billed to the organization. Params: org (required).",
    inputSchema: {"type":"object","properties":{"org":{"type":"string","description":"The organization name. The name is not case sensitive."},"body":{"type":"object","description":"Request body"}},"required":["org","body"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("DELETE", ("https://api.github.com/orgs/" + String(args["org"]) + "/codespaces/access/selected_users") + _qs, {}, args["body"]);
    }
  },
  {
    name: "codespaces/list-org-secrets",
    description: "List organization secrets. Lists all Codespaces development environment secrets available at the organization-level without revealing their encrypted\nvalues. Params: org (required).",
    inputSchema: {"type":"object","properties":{"org":{"type":"string","description":"The organization name. The name is not case sensitive."},"per_page":{"type":"integer","default":30,"description":"The number of results per page (max 100). For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""},"page":{"type":"integer","default":1,"description":"The page number of the results to fetch. For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""}},"required":["org"]},
    call: function(args) {
    var _q = new URLSearchParams();
    if (args["per_page"] !== undefined) _q.set("per_page", String(args["per_page"]));
    if (args["page"] !== undefined) _q.set("page", String(args["page"]));
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/orgs/" + String(args["org"]) + "/codespaces/secrets") + _qs, {}, undefined);
    }
  },
  {
    name: "codespaces/get-org-public-key",
    description: "Get an organization public key. Gets a public key for an organization, which is required in order to encrypt secrets. Params: org (required).",
    inputSchema: {"type":"object","properties":{"org":{"type":"string","description":"The organization name. The name is not case sensitive."}},"required":["org"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/orgs/" + String(args["org"]) + "/codespaces/secrets/public-key") + _qs, {}, undefined);
    }
  },
  {
    name: "codespaces/get-org-secret",
    description: "Get an organization secret. Gets an organization development environment secret without revealing its encrypted value. Params: org (required), secret_name (required).",
    inputSchema: {"type":"object","properties":{"org":{"type":"string","description":"The organization name. The name is not case sensitive."},"secret_name":{"type":"string","description":"The name of the secret."}},"required":["org","secret_name"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/orgs/" + String(args["org"]) + "/codespaces/secrets/" + String(args["secret_name"])) + _qs, {}, undefined);
    }
  },
  {
    name: "codespaces/create-or-update-org-secret",
    description: "Create or update an organization secret. Creates or updates an organization development environment secret with an encrypted value. Params: org (required), secret_name (required).",
    inputSchema: {"type":"object","properties":{"org":{"type":"string","description":"The organization name. The name is not case sensitive."},"secret_name":{"type":"string","description":"The name of the secret."},"body":{"type":"object","description":"Request body"}},"required":["org","secret_name","body"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("PUT", ("https://api.github.com/orgs/" + String(args["org"]) + "/codespaces/secrets/" + String(args["secret_name"])) + _qs, {}, args["body"]);
    }
  },
  {
    name: "codespaces/delete-org-secret",
    description: "Delete an organization secret. Deletes an organization development environment secret using the secret name. Params: org (required), secret_name (required).",
    inputSchema: {"type":"object","properties":{"org":{"type":"string","description":"The organization name. The name is not case sensitive."},"secret_name":{"type":"string","description":"The name of the secret."}},"required":["org","secret_name"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("DELETE", ("https://api.github.com/orgs/" + String(args["org"]) + "/codespaces/secrets/" + String(args["secret_name"])) + _qs, {}, undefined);
    }
  },
  {
    name: "codespaces/list-selected-repos-for-org-secret",
    description: "List selected repositories for an organization secret. Lists all repositories that have been selected when the `visibility`\nfor repository access to a secret is set to `selected`. Params: org (required), secret_name (required).",
    inputSchema: {"type":"object","properties":{"org":{"type":"string","description":"The organization name. The name is not case sensitive."},"secret_name":{"type":"string","description":"The name of the secret."},"page":{"type":"integer","default":1,"description":"The page number of the results to fetch. For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""},"per_page":{"type":"integer","default":30,"description":"The number of results per page (max 100). For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""}},"required":["org","secret_name"]},
    call: function(args) {
    var _q = new URLSearchParams();
    if (args["page"] !== undefined) _q.set("page", String(args["page"]));
    if (args["per_page"] !== undefined) _q.set("per_page", String(args["per_page"]));
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/orgs/" + String(args["org"]) + "/codespaces/secrets/" + String(args["secret_name"]) + "/repositories") + _qs, {}, undefined);
    }
  },
  {
    name: "codespaces/set-selected-repos-for-org-secret",
    description: "Set selected repositories for an organization secret. Replaces all repositories for an organization development environment secret when the `visibility`\nfor repository access is set to `selected`. Params: org (required), secret_name (required).",
    inputSchema: {"type":"object","properties":{"org":{"type":"string","description":"The organization name. The name is not case sensitive."},"secret_name":{"type":"string","description":"The name of the secret."},"body":{"type":"object","description":"Request body"}},"required":["org","secret_name","body"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("PUT", ("https://api.github.com/orgs/" + String(args["org"]) + "/codespaces/secrets/" + String(args["secret_name"]) + "/repositories") + _qs, {}, args["body"]);
    }
  },
  {
    name: "codespaces/add-selected-repo-to-org-secret",
    description: "Add selected repository to an organization secret. Adds a repository to an organization development environment secret when the `visibility` for repository access is set to `selected`. Params: org (required), secret_name (required), repository_id (required).",
    inputSchema: {"type":"object","properties":{"org":{"type":"string","description":"The organization name. The name is not case sensitive."},"secret_name":{"type":"string","description":"The name of the secret."},"repository_id":{"type":"integer"}},"required":["org","secret_name","repository_id"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("PUT", ("https://api.github.com/orgs/" + String(args["org"]) + "/codespaces/secrets/" + String(args["secret_name"]) + "/repositories/" + String(args["repository_id"])) + _qs, {}, undefined);
    }
  },
  {
    name: "codespaces/remove-selected-repo-from-org-secret",
    description: "Remove selected repository from an organization secret. Removes a repository from an organization development environment secret when the `visibility`\nfor repository access is set to `selected`. Params: org (required), secret_name (required), repository_id (required).",
    inputSchema: {"type":"object","properties":{"org":{"type":"string","description":"The organization name. The name is not case sensitive."},"secret_name":{"type":"string","description":"The name of the secret."},"repository_id":{"type":"integer"}},"required":["org","secret_name","repository_id"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("DELETE", ("https://api.github.com/orgs/" + String(args["org"]) + "/codespaces/secrets/" + String(args["secret_name"]) + "/repositories/" + String(args["repository_id"])) + _qs, {}, undefined);
    }
  },
  {
    name: "copilot-spaces/list-for-org",
    description: "List organization Copilot Spaces. Lists Copilot Spaces owned by an organization. Params: org (required).",
    inputSchema: {"type":"object","properties":{"org":{"type":"string","description":"The organization name. The name is not case sensitive."},"per_page":{"type":"integer","minimum":1,"maximum":100,"default":30,"description":"The number of results per page (max 100)."},"before":{"type":"string","description":"A cursor, as given in the [Link header](https://docs.github.com/rest/guides/using-pagination-in-the-rest-api#using-link-headers). If specified, the query only searches for results before this cursor."},"after":{"type":"string","description":"A cursor, as given in the [Link header](https://docs.github.com/rest/guides/using-pagination-in-the-rest-api#using-link-headers). If specified, the query only searches for results after this cursor."}},"required":["org"]},
    call: function(args) {
    var _q = new URLSearchParams();
    if (args["per_page"] !== undefined) _q.set("per_page", String(args["per_page"]));
    if (args["before"] !== undefined) _q.set("before", String(args["before"]));
    if (args["after"] !== undefined) _q.set("after", String(args["after"]));
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/orgs/" + String(args["org"]) + "/copilot-spaces") + _qs, {}, undefined);
    }
  },
  {
    name: "copilot-spaces/create-for-org",
    description: "Create an organization Copilot Space. Creates a new Copilot Space owned by an organization. Params: org (required).",
    inputSchema: {"type":"object","properties":{"org":{"type":"string","description":"The organization name. The name is not case sensitive."},"body":{"type":"object","description":"Request body"}},"required":["org","body"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("POST", ("https://api.github.com/orgs/" + String(args["org"]) + "/copilot-spaces") + _qs, {}, args["body"]);
    }
  },
  {
    name: "copilot-spaces/get-for-org",
    description: "Get an organization Copilot Space. Gets details about a specific Copilot Space owned by an organization. Params: org (required), space_number (required).",
    inputSchema: {"type":"object","properties":{"org":{"type":"string","description":"The organization name. The name is not case sensitive."},"space_number":{"type":"integer","description":"The unique identifier of the Copilot Space."}},"required":["org","space_number"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/orgs/" + String(args["org"]) + "/copilot-spaces/" + String(args["space_number"])) + _qs, {}, undefined);
    }
  },
  {
    name: "copilot-spaces/update-for-org",
    description: "Set an organization Copilot Space. Updates a Copilot Space owned by an organization. Params: org (required), space_number (required).",
    inputSchema: {"type":"object","properties":{"org":{"type":"string","description":"The organization name. The name is not case sensitive."},"space_number":{"type":"integer","description":"The unique identifier of the Copilot Space."},"body":{"type":"object","description":"Request body"}},"required":["org","space_number","body"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("PUT", ("https://api.github.com/orgs/" + String(args["org"]) + "/copilot-spaces/" + String(args["space_number"])) + _qs, {}, args["body"]);
    }
  },
  {
    name: "copilot-spaces/delete-for-org",
    description: "Delete an organization Copilot Space. Deletes a Copilot Space owned by an organization. Params: org (required), space_number (required).",
    inputSchema: {"type":"object","properties":{"org":{"type":"string","description":"The organization name. The name is not case sensitive."},"space_number":{"type":"integer","description":"The unique identifier of the Copilot Space."}},"required":["org","space_number"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("DELETE", ("https://api.github.com/orgs/" + String(args["org"]) + "/copilot-spaces/" + String(args["space_number"])) + _qs, {}, undefined);
    }
  },
  {
    name: "copilot-spaces/list-collaborators-for-org",
    description: "List collaborators for an organization Copilot Space. Lists all collaborators for a specific Copilot Space owned by an organization. Params: org (required), space_number (required).",
    inputSchema: {"type":"object","properties":{"org":{"type":"string","description":"The organization name. The name is not case sensitive."},"space_number":{"type":"integer","description":"The unique identifier of the Copilot Space."}},"required":["org","space_number"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/orgs/" + String(args["org"]) + "/copilot-spaces/" + String(args["space_number"]) + "/collaborators") + _qs, {}, undefined);
    }
  },
  {
    name: "copilot-spaces/add-collaborator-for-org",
    description: "Add a collaborator to an organization Copilot Space. Adds a collaborator (user or team) to a specific Copilot Space owned by an organization. Params: org (required), space_number (required).",
    inputSchema: {"type":"object","properties":{"org":{"type":"string","description":"The organization name. The name is not case sensitive."},"space_number":{"type":"integer","description":"The unique identifier of the Copilot Space."},"body":{"type":"object","description":"Request body"}},"required":["org","space_number","body"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("POST", ("https://api.github.com/orgs/" + String(args["org"]) + "/copilot-spaces/" + String(args["space_number"]) + "/collaborators") + _qs, {}, args["body"]);
    }
  },
  {
    name: "copilot-spaces/update-collaborator-for-org",
    description: "Set a collaborator role for an organization Copilot Space. Updates the role of a collaborator for a specific Copilot Space owned by an organization. Params: org (required), space_number (required), actor_type (required), actor_identifier (required).",
    inputSchema: {"type":"object","properties":{"org":{"type":"string","description":"The organization name. The name is not case sensitive."},"space_number":{"type":"integer","description":"The unique identifier of the Copilot Space."},"actor_type":{"type":"string","enum":["User","Team"],"description":"The type of actor (user or team)."},"actor_identifier":{"type":"string","description":"The username (for users) or team slug (for teams). The numeric ID of a user or team is also accepted."},"body":{"type":"object","description":"Request body"}},"required":["org","space_number","actor_type","actor_identifier","body"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("PUT", ("https://api.github.com/orgs/" + String(args["org"]) + "/copilot-spaces/" + String(args["space_number"]) + "/collaborators/" + String(args["actor_type"]) + "/" + String(args["actor_identifier"])) + _qs, {}, args["body"]);
    }
  },
  {
    name: "copilot-spaces/remove-collaborator-for-org",
    description: "Remove a collaborator from an organization Copilot Space. Removes a collaborator from a specific Copilot Space owned by an organization. Params: org (required), space_number (required), actor_type (required), actor_identifier (required).",
    inputSchema: {"type":"object","properties":{"org":{"type":"string","description":"The organization name. The name is not case sensitive."},"space_number":{"type":"integer","description":"The unique identifier of the Copilot Space."},"actor_type":{"type":"string","enum":["User","Team"],"description":"The type of actor (user or team)."},"actor_identifier":{"type":"string","description":"The username (for users) or team slug (for teams). The numeric ID of a user or team is also accepted."}},"required":["org","space_number","actor_type","actor_identifier"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("DELETE", ("https://api.github.com/orgs/" + String(args["org"]) + "/copilot-spaces/" + String(args["space_number"]) + "/collaborators/" + String(args["actor_type"]) + "/" + String(args["actor_identifier"])) + _qs, {}, undefined);
    }
  },
  {
    name: "copilot-spaces/list-resources-for-org",
    description: "List resources for an organization Copilot Space. Lists all resources attached to a specific Copilot Space owned by an organization. Params: org (required), space_number (required).",
    inputSchema: {"type":"object","properties":{"org":{"type":"string","description":"The organization name. The name is not case sensitive."},"space_number":{"type":"integer","description":"The unique identifier of the Copilot Space."}},"required":["org","space_number"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/orgs/" + String(args["org"]) + "/copilot-spaces/" + String(args["space_number"]) + "/resources") + _qs, {}, undefined);
    }
  },
  {
    name: "copilot-spaces/create-resource-for-org",
    description: "Create a resource for an organization Copilot Space. Creates a new resource in a specific Copilot Space owned by an organization. Params: org (required), space_number (required).",
    inputSchema: {"type":"object","properties":{"org":{"type":"string","description":"The organization name. The name is not case sensitive."},"space_number":{"type":"integer","description":"The unique identifier of the Copilot Space."},"body":{"type":"object","description":"Request body"}},"required":["org","space_number","body"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("POST", ("https://api.github.com/orgs/" + String(args["org"]) + "/copilot-spaces/" + String(args["space_number"]) + "/resources") + _qs, {}, args["body"]);
    }
  },
  {
    name: "copilot-spaces/get-resource-for-org",
    description: "Get a resource for an organization Copilot Space. Gets a specific resource attached to a Copilot Space owned by an organization. Params: org (required), space_number (required), space_resource_id (required).",
    inputSchema: {"type":"object","properties":{"org":{"type":"string","description":"The organization name. The name is not case sensitive."},"space_number":{"type":"integer","description":"The unique identifier of the Copilot Space."},"space_resource_id":{"type":"integer","description":"The unique identifier of the resource."}},"required":["org","space_number","space_resource_id"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/orgs/" + String(args["org"]) + "/copilot-spaces/" + String(args["space_number"]) + "/resources/" + String(args["space_resource_id"])) + _qs, {}, undefined);
    }
  },
  {
    name: "copilot-spaces/update-resource-for-org",
    description: "Set a resource for an organization Copilot Space. Updates the metadata of a resource in a specific Copilot Space owned by an organization. Params: org (required), space_number (required), space_resource_id (required).",
    inputSchema: {"type":"object","properties":{"org":{"type":"string","description":"The organization name. The name is not case sensitive."},"space_number":{"type":"integer","description":"The unique identifier of the Copilot Space."},"space_resource_id":{"type":"integer","description":"The unique identifier of the resource."},"body":{"type":"object","description":"Request body"}},"required":["org","space_number","space_resource_id","body"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("PUT", ("https://api.github.com/orgs/" + String(args["org"]) + "/copilot-spaces/" + String(args["space_number"]) + "/resources/" + String(args["space_resource_id"])) + _qs, {}, args["body"]);
    }
  },
  {
    name: "copilot-spaces/delete-resource-for-org",
    description: "Delete a resource from an organization Copilot Space. Deletes a resource from a specific Copilot Space owned by an organization. Params: org (required), space_number (required), space_resource_id (required).",
    inputSchema: {"type":"object","properties":{"org":{"type":"string","description":"The organization name. The name is not case sensitive."},"space_number":{"type":"integer","description":"The unique identifier of the Copilot Space."},"space_resource_id":{"type":"integer","description":"The unique identifier of the resource."}},"required":["org","space_number","space_resource_id"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("DELETE", ("https://api.github.com/orgs/" + String(args["org"]) + "/copilot-spaces/" + String(args["space_number"]) + "/resources/" + String(args["space_resource_id"])) + _qs, {}, undefined);
    }
  },
  {
    name: "copilot/get-copilot-organization-details",
    description: "Get Copilot seat information and settings for an organization. > [!NOTE]\n> This endpoint is in public preview and is subject to change. Params: org (required).",
    inputSchema: {"type":"object","properties":{"org":{"type":"string","description":"The organization name. The name is not case sensitive."}},"required":["org"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/orgs/" + String(args["org"]) + "/copilot/billing") + _qs, {}, undefined);
    }
  },
  {
    name: "copilot/list-copilot-seats",
    description: "List all Copilot seat assignments for an organization. > [!NOTE]\n> This endpoint is in public preview and is subject to change. Params: org (required).",
    inputSchema: {"type":"object","properties":{"org":{"type":"string","description":"The organization name. The name is not case sensitive."},"page":{"type":"integer","default":1,"description":"The page number of the results to fetch. For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""},"per_page":{"type":"integer","default":50,"description":"The number of results per page (max 100). For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""}},"required":["org"]},
    call: function(args) {
    var _q = new URLSearchParams();
    if (args["page"] !== undefined) _q.set("page", String(args["page"]));
    if (args["per_page"] !== undefined) _q.set("per_page", String(args["per_page"]));
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/orgs/" + String(args["org"]) + "/copilot/billing/seats") + _qs, {}, undefined);
    }
  },
  {
    name: "copilot/add-copilot-seats-for-teams",
    description: "Add teams to the Copilot subscription for an organization. > [!NOTE]\n> This endpoint is in public preview and is subject to change. Params: org (required).",
    inputSchema: {"type":"object","properties":{"org":{"type":"string","description":"The organization name. The name is not case sensitive."},"body":{"type":"object","description":"Request body"}},"required":["org","body"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("POST", ("https://api.github.com/orgs/" + String(args["org"]) + "/copilot/billing/selected_teams") + _qs, {}, args["body"]);
    }
  },
  {
    name: "copilot/cancel-copilot-seat-assignment-for-teams",
    description: "Remove teams from the Copilot subscription for an organization. > [!NOTE]\n> This endpoint is in public preview and is subject to change. Params: org (required).",
    inputSchema: {"type":"object","properties":{"org":{"type":"string","description":"The organization name. The name is not case sensitive."},"body":{"type":"object","description":"Request body"}},"required":["org","body"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("DELETE", ("https://api.github.com/orgs/" + String(args["org"]) + "/copilot/billing/selected_teams") + _qs, {}, args["body"]);
    }
  },
  {
    name: "copilot/add-copilot-seats-for-users",
    description: "Add users to the Copilot subscription for an organization. > [!NOTE]\n> This endpoint is in public preview and is subject to change. Params: org (required).",
    inputSchema: {"type":"object","properties":{"org":{"type":"string","description":"The organization name. The name is not case sensitive."},"body":{"type":"object","description":"Request body"}},"required":["org","body"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("POST", ("https://api.github.com/orgs/" + String(args["org"]) + "/copilot/billing/selected_users") + _qs, {}, args["body"]);
    }
  },
  {
    name: "copilot/cancel-copilot-seat-assignment-for-users",
    description: "Remove users from the Copilot subscription for an organization. > [!NOTE]\n> This endpoint is in public preview and is subject to change. Params: org (required).",
    inputSchema: {"type":"object","properties":{"org":{"type":"string","description":"The organization name. The name is not case sensitive."},"body":{"type":"object","description":"Request body"}},"required":["org","body"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("DELETE", ("https://api.github.com/orgs/" + String(args["org"]) + "/copilot/billing/selected_users") + _qs, {}, args["body"]);
    }
  },
  {
    name: "copilot/get-copilot-coding-agent-permissions-organization",
    description: "Get Copilot cloud agent permissions for an organization. > [!NOTE]\n> This endpoint is in public preview and is subject to change. Params: org (required).",
    inputSchema: {"type":"object","properties":{"org":{"type":"string","description":"The organization name. The name is not case sensitive."}},"required":["org"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/orgs/" + String(args["org"]) + "/copilot/coding-agent/permissions") + _qs, {}, undefined);
    }
  },
  {
    name: "copilot/set-copilot-coding-agent-permissions-organization",
    description: "Set Copilot cloud agent permissions for an organization. > [!NOTE]\n> This endpoint is in public preview and is subject to change. Params: org (required).",
    inputSchema: {"type":"object","properties":{"org":{"type":"string","description":"The organization name. The name is not case sensitive."},"body":{"type":"object","description":"Request body"}},"required":["org","body"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("PUT", ("https://api.github.com/orgs/" + String(args["org"]) + "/copilot/coding-agent/permissions") + _qs, {}, args["body"]);
    }
  },
  {
    name: "copilot/list-copilot-coding-agent-selected-repositories-for-organization",
    description: "List repositories enabled for Copilot cloud agent in an organization. > [!NOTE]\n> This endpoint is in public preview and is subject to change. Params: org (required).",
    inputSchema: {"type":"object","properties":{"org":{"type":"string","description":"The organization name. The name is not case sensitive."},"per_page":{"type":"integer","default":30,"description":"The number of results per page (max 100). For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""},"page":{"type":"integer","default":1,"description":"The page number of the results to fetch. For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""}},"required":["org"]},
    call: function(args) {
    var _q = new URLSearchParams();
    if (args["per_page"] !== undefined) _q.set("per_page", String(args["per_page"]));
    if (args["page"] !== undefined) _q.set("page", String(args["page"]));
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/orgs/" + String(args["org"]) + "/copilot/coding-agent/permissions/repositories") + _qs, {}, undefined);
    }
  },
  {
    name: "copilot/set-copilot-coding-agent-selected-repositories-for-organization",
    description: "Set selected repositories for Copilot cloud agent in an organization. > [!NOTE]\n> This endpoint is in public preview and is subject to change. Params: org (required).",
    inputSchema: {"type":"object","properties":{"org":{"type":"string","description":"The organization name. The name is not case sensitive."},"body":{"type":"object","description":"Request body"}},"required":["org","body"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("PUT", ("https://api.github.com/orgs/" + String(args["org"]) + "/copilot/coding-agent/permissions/repositories") + _qs, {}, args["body"]);
    }
  },
  {
    name: "copilot/enable-copilot-coding-agent-for-repository-in-organization",
    description: "Enable a repository for Copilot cloud agent in an organization. > [!NOTE]\n> This endpoint is in public preview and is subject to change. Params: org (required), repository_id (required).",
    inputSchema: {"type":"object","properties":{"org":{"type":"string","description":"The organization name. The name is not case sensitive."},"repository_id":{"type":"integer","description":"The unique identifier of the repository."}},"required":["org","repository_id"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("PUT", ("https://api.github.com/orgs/" + String(args["org"]) + "/copilot/coding-agent/permissions/repositories/" + String(args["repository_id"])) + _qs, {}, undefined);
    }
  },
  {
    name: "copilot/disable-copilot-coding-agent-for-repository-in-organization",
    description: "Disable a repository for Copilot cloud agent in an organization. > [!NOTE]\n> This endpoint is in public preview and is subject to change. Params: org (required), repository_id (required).",
    inputSchema: {"type":"object","properties":{"org":{"type":"string","description":"The organization name. The name is not case sensitive."},"repository_id":{"type":"integer","description":"The unique identifier of the repository."}},"required":["org","repository_id"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("DELETE", ("https://api.github.com/orgs/" + String(args["org"]) + "/copilot/coding-agent/permissions/repositories/" + String(args["repository_id"])) + _qs, {}, undefined);
    }
  },
  {
    name: "copilot/copilot-content-exclusion-for-organization",
    description: "Get Copilot content exclusion rules for an organization. > [!NOTE]\n> This endpoint is in public preview and is subject to change. Params: org (required).",
    inputSchema: {"type":"object","properties":{"org":{"type":"string","description":"The organization name. The name is not case sensitive."}},"required":["org"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/orgs/" + String(args["org"]) + "/copilot/content_exclusion") + _qs, {}, undefined);
    }
  },
  {
    name: "copilot/set-copilot-content-exclusion-for-organization",
    description: "Set Copilot content exclusion rules for an organization. > [!NOTE]\n> This endpoint is in public preview and is subject to change. Params: org (required).",
    inputSchema: {"type":"object","properties":{"org":{"type":"string","description":"The organization name. The name is not case sensitive."},"body":{"type":"object","description":"The content exclusion rules to set"}},"required":["org","body"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("PUT", ("https://api.github.com/orgs/" + String(args["org"]) + "/copilot/content_exclusion") + _qs, {}, args["body"]);
    }
  },
  {
    name: "copilot/copilot-organization-one-day-usage-metrics",
    description: "Get Copilot organization usage metrics for a specific day. Use this endpoint to retrieve download links for the Copilot organization usage metrics report for a specific day. Params: org (required), day (required).",
    inputSchema: {"type":"object","properties":{"org":{"type":"string","description":"The organization name. The name is not case sensitive."},"day":{"type":"string","format":"date","example":"2025-10-13","description":"The day to request data for, in `YYYY-MM-DD` format."}},"required":["org","day"]},
    call: function(args) {
    var _q = new URLSearchParams();
    if (args["day"] !== undefined) _q.set("day", String(args["day"]));
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/orgs/" + String(args["org"]) + "/copilot/metrics/reports/organization-1-day") + _qs, {}, undefined);
    }
  },
  {
    name: "copilot/copilot-organization-usage-metrics",
    description: "Get Copilot organization usage metrics. Use this endpoint to retrieve download links for the latest 28-day organization Copilot usage metrics report. Params: org (required).",
    inputSchema: {"type":"object","properties":{"org":{"type":"string","description":"The organization name. The name is not case sensitive."}},"required":["org"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/orgs/" + String(args["org"]) + "/copilot/metrics/reports/organization-28-day/latest") + _qs, {}, undefined);
    }
  },
  {
    name: "copilot/copilot-organization-user-teams-one-day-report",
    description: "Get Copilot organization user-teams report for a specific day. Use this endpoint to retrieve download links for the Copilot organization user-teams report for a specific day. Params: org (required), day (required).",
    inputSchema: {"type":"object","properties":{"org":{"type":"string","description":"The organization name. The name is not case sensitive."},"day":{"type":"string","format":"date","example":"2025-10-13","description":"The day to request data for, in `YYYY-MM-DD` format."}},"required":["org","day"]},
    call: function(args) {
    var _q = new URLSearchParams();
    if (args["day"] !== undefined) _q.set("day", String(args["day"]));
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/orgs/" + String(args["org"]) + "/copilot/metrics/reports/user-teams-1-day") + _qs, {}, undefined);
    }
  },
  {
    name: "copilot/copilot-organization-users-one-day-usage-metrics",
    description: "Get Copilot organization users usage metrics for a specific day. Use this endpoint to retrieve download links for the Copilot organization user usage metrics report for a specific day. Params: org (required), day (required).",
    inputSchema: {"type":"object","properties":{"org":{"type":"string","description":"The organization name. The name is not case sensitive."},"day":{"type":"string","format":"date","example":"2025-10-13","description":"The day to request data for, in `YYYY-MM-DD` format."}},"required":["org","day"]},
    call: function(args) {
    var _q = new URLSearchParams();
    if (args["day"] !== undefined) _q.set("day", String(args["day"]));
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/orgs/" + String(args["org"]) + "/copilot/metrics/reports/users-1-day") + _qs, {}, undefined);
    }
  },
  {
    name: "copilot/copilot-organization-users-usage-metrics",
    description: "Get Copilot organization users usage metrics. Use this endpoint to retrieve download links for the latest 28-day organization users Copilot usage metrics report. Params: org (required).",
    inputSchema: {"type":"object","properties":{"org":{"type":"string","description":"The organization name. The name is not case sensitive."}},"required":["org"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/orgs/" + String(args["org"]) + "/copilot/metrics/reports/users-28-day/latest") + _qs, {}, undefined);
    }
  },
  {
    name: "dependabot/list-alerts-for-org",
    description: "List Dependabot alerts for an organization. Lists Dependabot alerts for an organization. Params: org (required).",
    inputSchema: {"type":"object","properties":{"org":{"type":"string","description":"The organization name. The name is not case sensitive."},"classification":{"type":"string","description":"A comma-separated list of vulnerability classifications. If specified, only alerts for vulnerabilities with these classifications will be returned.\n\nCan be: `malware`, `general`"},"state":{"type":"string","description":"A comma-separated list of states. If specified, only alerts with these states will be returned.\n\nCan be: `auto_dismissed`, `dismissed`, `fixed`, `open`"},"severity":{"type":"string","description":"A comma-separated list of severities. If specified, only alerts with these severities will be returned.\n\nCan be: `low`, `medium`, `high`, `critical`"},"ecosystem":{"type":"string","description":"A comma-separated list of ecosystems. If specified, only alerts for these ecosystems will be returned.\n\nCan be: `composer`, `go`, `maven`, `npm`, `nuget`, `pip`, `pub`, `rubygems`, `rust`"},"package":{"type":"string","description":"A comma-separated list of package names. If specified, only alerts for these packages will be returned."},"epss_percentage":{"type":"string","description":"CVE Exploit Prediction Scoring System (EPSS) percentage. Can be specified as:\n- An exact number (`n`)\n- Comparators such as `>n`, `<n`, `>=n`, `<=n`\n- A range like `n..n`, where `n` is a number from 0.0 to 1.0\n\nFilters the list of alerts based on EPSS percentages. If specified, only alerts with the provided EPSS percentages will be returned."},"artifact_registry_url":{"type":"string","description":"A comma-separated list of artifact registry URLs. If specified, only alerts for repositories with storage records matching these URLs will be returned."},"artifact_registry":{"type":"string","description":"A comma-separated list of Artifact Registry name strings. If specified, only alerts for repositories with storage records matching these registries will be returned.\n\nCan be: `jfrog-artifactory`"},"has":{"oneOf":[{"type":"string"},{"type":"array","items":{"type":"string","enum":["patch","deployment"]}}],"description":"Filters the list of alerts based on whether the alert has the given value. If specified, only alerts meeting this criterion will be returned.\nMultiple `has` filters can be passed to filter for alerts that have all of the values."},"assignee":{"type":"string","description":"Filter alerts by assignees.\nProvide a comma-separated list of user handles (e.g., `octocat` or `octocat,hubot`) to return alerts assigned to any of the specified users.\nUse `*` to list alerts with at least one assignee or `none` to list alerts with no assignees."},"runtime_risk":{"type":"string","description":"A comma-separated list of runtime risk strings. If specified, only alerts for repositories with deployment records matching these risks will be returned.\n\nCan be: `critical-resource`, `internet-exposed`, `sensitive-data`, `lateral-movement`"},"scope":{"type":"string","enum":["development","runtime"],"description":"The scope of the vulnerable dependency. If specified, only alerts with this scope will be returned."},"sort":{"type":"string","enum":["created","updated","epss_percentage"],"default":"created","description":"The property by which to sort the results.\n`created` means when the alert was created.\n`updated` means when the alert's state last changed.\n`epss_percentage` sorts alerts by the Exploit Prediction Scoring System (EPSS) percentage."},"direction":{"type":"string","enum":["asc","desc"],"default":"desc","description":"The direction to sort the results by."},"before":{"type":"string","description":"A cursor, as given in the [Link header](https://docs.github.com/rest/guides/using-pagination-in-the-rest-api#using-link-headers). If specified, the query only searches for results before this cursor. For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""},"after":{"type":"string","description":"A cursor, as given in the [Link header](https://docs.github.com/rest/guides/using-pagination-in-the-rest-api#using-link-headers). If specified, the query only searches for results after this cursor. For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""},"per_page":{"type":"integer","default":30,"description":"The number of results per page (max 100). For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""}},"required":["org"]},
    call: function(args) {
    var _q = new URLSearchParams();
    if (args["classification"] !== undefined) _q.set("classification", String(args["classification"]));
    if (args["state"] !== undefined) _q.set("state", String(args["state"]));
    if (args["severity"] !== undefined) _q.set("severity", String(args["severity"]));
    if (args["ecosystem"] !== undefined) _q.set("ecosystem", String(args["ecosystem"]));
    if (args["package"] !== undefined) _q.set("package", String(args["package"]));
    if (args["epss_percentage"] !== undefined) _q.set("epss_percentage", String(args["epss_percentage"]));
    if (args["artifact_registry_url"] !== undefined) _q.set("artifact_registry_url", String(args["artifact_registry_url"]));
    if (args["artifact_registry"] !== undefined) _q.set("artifact_registry", String(args["artifact_registry"]));
    if (args["has"] !== undefined) _q.set("has", String(args["has"]));
    if (args["assignee"] !== undefined) _q.set("assignee", String(args["assignee"]));
    if (args["runtime_risk"] !== undefined) _q.set("runtime_risk", String(args["runtime_risk"]));
    if (args["scope"] !== undefined) _q.set("scope", String(args["scope"]));
    if (args["sort"] !== undefined) _q.set("sort", String(args["sort"]));
    if (args["direction"] !== undefined) _q.set("direction", String(args["direction"]));
    if (args["before"] !== undefined) _q.set("before", String(args["before"]));
    if (args["after"] !== undefined) _q.set("after", String(args["after"]));
    if (args["per_page"] !== undefined) _q.set("per_page", String(args["per_page"]));
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/orgs/" + String(args["org"]) + "/dependabot/alerts") + _qs, {}, undefined);
    }
  },
  {
    name: "dependabot/repository-access-for-org",
    description: "Lists the repositories Dependabot can access in an organization. Lists repositories that organization admins have allowed Dependabot to access when updating dependencies. Params: org (required).",
    inputSchema: {"type":"object","properties":{"org":{"type":"string","description":"The organization name. The name is not case sensitive."},"page":{"type":"integer","minimum":1,"default":1,"description":"The page number of results to fetch."},"per_page":{"type":"integer","minimum":1,"maximum":100,"default":30,"description":"Number of results per page."}},"required":["org"]},
    call: function(args) {
    var _q = new URLSearchParams();
    if (args["page"] !== undefined) _q.set("page", String(args["page"]));
    if (args["per_page"] !== undefined) _q.set("per_page", String(args["per_page"]));
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/orgs/" + String(args["org"]) + "/dependabot/repository-access") + _qs, {}, undefined);
    }
  },
  {
    name: "dependabot/update-repository-access-for-org",
    description: "Updates Dependabot's repository access list for an organization. Updates repositories according to the list of repositories that organization admins have given Dependabot access to when they've updated dependencies. Params: org (required).",
    inputSchema: {"type":"object","properties":{"org":{"type":"string","description":"The organization name. The name is not case sensitive."},"body":{"type":"object","description":"Request body"}},"required":["org","body"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("PATCH", ("https://api.github.com/orgs/" + String(args["org"]) + "/dependabot/repository-access") + _qs, {}, args["body"]);
    }
  },
  {
    name: "dependabot/set-repository-access-default-level",
    description: "Set the default repository access level for Dependabot. Sets the default level of repository access Dependabot will have while performing an update. Params: org (required).",
    inputSchema: {"type":"object","properties":{"org":{"type":"string","description":"The organization name. The name is not case sensitive."},"body":{"type":"object","description":"Request body"}},"required":["org","body"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("PUT", ("https://api.github.com/orgs/" + String(args["org"]) + "/dependabot/repository-access/default-level") + _qs, {}, args["body"]);
    }
  },
  {
    name: "dependabot/list-org-secrets",
    description: "List organization secrets. Lists all secrets available in an organization without revealing their\nencrypted values. Params: org (required).",
    inputSchema: {"type":"object","properties":{"org":{"type":"string","description":"The organization name. The name is not case sensitive."},"per_page":{"type":"integer","default":30,"description":"The number of results per page (max 100). For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""},"page":{"type":"integer","default":1,"description":"The page number of the results to fetch. For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""}},"required":["org"]},
    call: function(args) {
    var _q = new URLSearchParams();
    if (args["per_page"] !== undefined) _q.set("per_page", String(args["per_page"]));
    if (args["page"] !== undefined) _q.set("page", String(args["page"]));
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/orgs/" + String(args["org"]) + "/dependabot/secrets") + _qs, {}, undefined);
    }
  },
  {
    name: "dependabot/get-org-public-key",
    description: "Get an organization public key. Gets your public key, which you need to encrypt secrets. Params: org (required).",
    inputSchema: {"type":"object","properties":{"org":{"type":"string","description":"The organization name. The name is not case sensitive."}},"required":["org"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/orgs/" + String(args["org"]) + "/dependabot/secrets/public-key") + _qs, {}, undefined);
    }
  },
  {
    name: "dependabot/get-org-secret",
    description: "Get an organization secret. Gets a single organization secret without revealing its encrypted value. Params: org (required), secret_name (required).",
    inputSchema: {"type":"object","properties":{"org":{"type":"string","description":"The organization name. The name is not case sensitive."},"secret_name":{"type":"string","description":"The name of the secret."}},"required":["org","secret_name"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/orgs/" + String(args["org"]) + "/dependabot/secrets/" + String(args["secret_name"])) + _qs, {}, undefined);
    }
  },
  {
    name: "dependabot/create-or-update-org-secret",
    description: "Create or update an organization secret. Creates or updates an organization secret with an encrypted value. Params: org (required), secret_name (required).",
    inputSchema: {"type":"object","properties":{"org":{"type":"string","description":"The organization name. The name is not case sensitive."},"secret_name":{"type":"string","description":"The name of the secret."},"body":{"type":"object","description":"Request body"}},"required":["org","secret_name","body"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("PUT", ("https://api.github.com/orgs/" + String(args["org"]) + "/dependabot/secrets/" + String(args["secret_name"])) + _qs, {}, args["body"]);
    }
  },
  {
    name: "dependabot/delete-org-secret",
    description: "Delete an organization secret. Deletes a secret in an organization using the secret name. Params: org (required), secret_name (required).",
    inputSchema: {"type":"object","properties":{"org":{"type":"string","description":"The organization name. The name is not case sensitive."},"secret_name":{"type":"string","description":"The name of the secret."}},"required":["org","secret_name"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("DELETE", ("https://api.github.com/orgs/" + String(args["org"]) + "/dependabot/secrets/" + String(args["secret_name"])) + _qs, {}, undefined);
    }
  },
  {
    name: "dependabot/list-selected-repos-for-org-secret",
    description: "List selected repositories for an organization secret. Lists all repositories that have been selected when the `visibility`\nfor repository access to a secret is set to `selected`. Params: org (required), secret_name (required).",
    inputSchema: {"type":"object","properties":{"org":{"type":"string","description":"The organization name. The name is not case sensitive."},"secret_name":{"type":"string","description":"The name of the secret."},"page":{"type":"integer","default":1,"description":"The page number of the results to fetch. For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""},"per_page":{"type":"integer","default":30,"description":"The number of results per page (max 100). For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""}},"required":["org","secret_name"]},
    call: function(args) {
    var _q = new URLSearchParams();
    if (args["page"] !== undefined) _q.set("page", String(args["page"]));
    if (args["per_page"] !== undefined) _q.set("per_page", String(args["per_page"]));
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/orgs/" + String(args["org"]) + "/dependabot/secrets/" + String(args["secret_name"]) + "/repositories") + _qs, {}, undefined);
    }
  },
  {
    name: "dependabot/set-selected-repos-for-org-secret",
    description: "Set selected repositories for an organization secret. Replaces all repositories for an organization secret when the `visibility`\nfor repository access is set to `selected`. Params: org (required), secret_name (required).",
    inputSchema: {"type":"object","properties":{"org":{"type":"string","description":"The organization name. The name is not case sensitive."},"secret_name":{"type":"string","description":"The name of the secret."},"body":{"type":"object","description":"Request body"}},"required":["org","secret_name","body"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("PUT", ("https://api.github.com/orgs/" + String(args["org"]) + "/dependabot/secrets/" + String(args["secret_name"]) + "/repositories") + _qs, {}, args["body"]);
    }
  },
  {
    name: "dependabot/add-selected-repo-to-org-secret",
    description: "Add selected repository to an organization secret. Adds a repository to an organization secret when the `visibility` for\nrepository access is set to `selected`. Params: org (required), secret_name (required), repository_id (required).",
    inputSchema: {"type":"object","properties":{"org":{"type":"string","description":"The organization name. The name is not case sensitive."},"secret_name":{"type":"string","description":"The name of the secret."},"repository_id":{"type":"integer"}},"required":["org","secret_name","repository_id"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("PUT", ("https://api.github.com/orgs/" + String(args["org"]) + "/dependabot/secrets/" + String(args["secret_name"]) + "/repositories/" + String(args["repository_id"])) + _qs, {}, undefined);
    }
  },
  {
    name: "dependabot/remove-selected-repo-from-org-secret",
    description: "Remove selected repository from an organization secret. Removes a repository from an organization secret when the `visibility`\nfor repository access is set to `selected`. Params: org (required), secret_name (required), repository_id (required).",
    inputSchema: {"type":"object","properties":{"org":{"type":"string","description":"The organization name. The name is not case sensitive."},"secret_name":{"type":"string","description":"The name of the secret."},"repository_id":{"type":"integer"}},"required":["org","secret_name","repository_id"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("DELETE", ("https://api.github.com/orgs/" + String(args["org"]) + "/dependabot/secrets/" + String(args["secret_name"]) + "/repositories/" + String(args["repository_id"])) + _qs, {}, undefined);
    }
  },
  {
    name: "packages/list-docker-migration-conflicting-packages-for-organization",
    description: "Get list of conflicting packages during Docker migration for organization. Lists all packages that are in a specific organization, are readable by the requesting user, and that encountered a conflict during a Docker migration. Params: org (required).",
    inputSchema: {"type":"object","properties":{"org":{"type":"string","description":"The organization name. The name is not case sensitive."}},"required":["org"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/orgs/" + String(args["org"]) + "/docker/conflicts") + _qs, {}, undefined);
    }
  },
  {
    name: "activity/list-public-org-events",
    description: "List public organization events. > [!NOTE]\n> This API is not built to serve real-time use cases. Params: org (required).",
    inputSchema: {"type":"object","properties":{"org":{"type":"string","description":"The organization name. The name is not case sensitive."},"per_page":{"type":"integer","default":30,"description":"The number of results per page (max 100). For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""},"page":{"type":"integer","default":1,"description":"The page number of the results to fetch. For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""}},"required":["org"]},
    call: function(args) {
    var _q = new URLSearchParams();
    if (args["per_page"] !== undefined) _q.set("per_page", String(args["per_page"]));
    if (args["page"] !== undefined) _q.set("page", String(args["page"]));
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/orgs/" + String(args["org"]) + "/events") + _qs, {}, undefined);
    }
  },
  {
    name: "orgs/list-failed-invitations",
    description: "List failed organization invitations. The return hash contains `failed_at` and `failed_reason` fields which represent the time at which the invitation failed and the reason for the failure. Params: org (required).",
    inputSchema: {"type":"object","properties":{"org":{"type":"string","description":"The organization name. The name is not case sensitive."},"per_page":{"type":"integer","default":30,"description":"The number of results per page (max 100). For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""},"page":{"type":"integer","default":1,"description":"The page number of the results to fetch. For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""}},"required":["org"]},
    call: function(args) {
    var _q = new URLSearchParams();
    if (args["per_page"] !== undefined) _q.set("per_page", String(args["per_page"]));
    if (args["page"] !== undefined) _q.set("page", String(args["page"]));
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/orgs/" + String(args["org"]) + "/failed_invitations") + _qs, {}, undefined);
    }
  },
  {
    name: "orgs/list-webhooks",
    description: "List organization webhooks. List webhooks for an organization. Params: org (required).",
    inputSchema: {"type":"object","properties":{"org":{"type":"string","description":"The organization name. The name is not case sensitive."},"per_page":{"type":"integer","default":30,"description":"The number of results per page (max 100). For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""},"page":{"type":"integer","default":1,"description":"The page number of the results to fetch. For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""}},"required":["org"]},
    call: function(args) {
    var _q = new URLSearchParams();
    if (args["per_page"] !== undefined) _q.set("per_page", String(args["per_page"]));
    if (args["page"] !== undefined) _q.set("page", String(args["page"]));
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/orgs/" + String(args["org"]) + "/hooks") + _qs, {}, undefined);
    }
  },
  {
    name: "orgs/create-webhook",
    description: "Create an organization webhook. Create a hook that posts payloads in JSON format. Params: org (required).",
    inputSchema: {"type":"object","properties":{"org":{"type":"string","description":"The organization name. The name is not case sensitive."},"body":{"type":"object","description":"Request body"}},"required":["org","body"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("POST", ("https://api.github.com/orgs/" + String(args["org"]) + "/hooks") + _qs, {}, args["body"]);
    }
  },
  {
    name: "orgs/get-webhook",
    description: "Get an organization webhook. Returns a webhook configured in an organization. Params: org (required), hook_id (required).",
    inputSchema: {"type":"object","properties":{"org":{"type":"string","description":"The organization name. The name is not case sensitive."},"hook_id":{"type":"integer","description":"The unique identifier of the hook. You can find this value in the `X-GitHub-Hook-ID` header of a webhook delivery."}},"required":["org","hook_id"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/orgs/" + String(args["org"]) + "/hooks/" + String(args["hook_id"])) + _qs, {}, undefined);
    }
  },
  {
    name: "orgs/update-webhook",
    description: "Update an organization webhook. Updates a webhook configured in an organization. Params: org (required), hook_id (required).",
    inputSchema: {"type":"object","properties":{"org":{"type":"string","description":"The organization name. The name is not case sensitive."},"hook_id":{"type":"integer","description":"The unique identifier of the hook. You can find this value in the `X-GitHub-Hook-ID` header of a webhook delivery."},"body":{"type":"object","description":"Request body"}},"required":["org","hook_id"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("PATCH", ("https://api.github.com/orgs/" + String(args["org"]) + "/hooks/" + String(args["hook_id"])) + _qs, {}, args["body"]);
    }
  },
  {
    name: "orgs/delete-webhook",
    description: "Delete an organization webhook. Delete a webhook for an organization. Params: org (required), hook_id (required).",
    inputSchema: {"type":"object","properties":{"org":{"type":"string","description":"The organization name. The name is not case sensitive."},"hook_id":{"type":"integer","description":"The unique identifier of the hook. You can find this value in the `X-GitHub-Hook-ID` header of a webhook delivery."}},"required":["org","hook_id"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("DELETE", ("https://api.github.com/orgs/" + String(args["org"]) + "/hooks/" + String(args["hook_id"])) + _qs, {}, undefined);
    }
  },
  {
    name: "orgs/get-webhook-config-for-org",
    description: "Get a webhook configuration for an organization. Returns the webhook configuration for an organization. Params: org (required), hook_id (required).",
    inputSchema: {"type":"object","properties":{"org":{"type":"string","description":"The organization name. The name is not case sensitive."},"hook_id":{"type":"integer","description":"The unique identifier of the hook. You can find this value in the `X-GitHub-Hook-ID` header of a webhook delivery."}},"required":["org","hook_id"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/orgs/" + String(args["org"]) + "/hooks/" + String(args["hook_id"]) + "/config") + _qs, {}, undefined);
    }
  },
  {
    name: "orgs/update-webhook-config-for-org",
    description: "Update a webhook configuration for an organization. Updates the webhook configuration for an organization. Params: org (required), hook_id (required).",
    inputSchema: {"type":"object","properties":{"org":{"type":"string","description":"The organization name. The name is not case sensitive."},"hook_id":{"type":"integer","description":"The unique identifier of the hook. You can find this value in the `X-GitHub-Hook-ID` header of a webhook delivery."},"body":{"type":"object","description":"Request body"}},"required":["org","hook_id"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("PATCH", ("https://api.github.com/orgs/" + String(args["org"]) + "/hooks/" + String(args["hook_id"]) + "/config") + _qs, {}, args["body"]);
    }
  },
  {
    name: "orgs/list-webhook-deliveries",
    description: "List deliveries for an organization webhook. Returns a list of webhook deliveries for a webhook configured in an organization. Params: org (required), hook_id (required).",
    inputSchema: {"type":"object","properties":{"org":{"type":"string","description":"The organization name. The name is not case sensitive."},"hook_id":{"type":"integer","description":"The unique identifier of the hook. You can find this value in the `X-GitHub-Hook-ID` header of a webhook delivery."},"per_page":{"type":"integer","default":30,"description":"The number of results per page (max 100). For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""},"cursor":{"type":"string","description":"Used for pagination: the starting delivery from which the page of deliveries is fetched. Refer to the `link` header for the next and previous page cursors."},"status":{"type":"string","enum":["success","failure"],"description":"Returns webhook deliveries filtered by delivery outcome classification based on `status_code` range. A `status` of `success` returns deliveries with a `status_code` in the 200-399 range (inclusive). A `status` of `failure` returns deliveries with a `status_code` in the 400-599 range (inclusive)."}},"required":["org","hook_id"]},
    call: function(args) {
    var _q = new URLSearchParams();
    if (args["per_page"] !== undefined) _q.set("per_page", String(args["per_page"]));
    if (args["cursor"] !== undefined) _q.set("cursor", String(args["cursor"]));
    if (args["status"] !== undefined) _q.set("status", String(args["status"]));
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/orgs/" + String(args["org"]) + "/hooks/" + String(args["hook_id"]) + "/deliveries") + _qs, {}, undefined);
    }
  },
  {
    name: "orgs/get-webhook-delivery",
    description: "Get a webhook delivery for an organization webhook. Returns a delivery for a webhook configured in an organization. Params: org (required), hook_id (required), delivery_id (required).",
    inputSchema: {"type":"object","properties":{"org":{"type":"string","description":"The organization name. The name is not case sensitive."},"hook_id":{"type":"integer","description":"The unique identifier of the hook. You can find this value in the `X-GitHub-Hook-ID` header of a webhook delivery."},"delivery_id":{"type":"integer"}},"required":["org","hook_id","delivery_id"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/orgs/" + String(args["org"]) + "/hooks/" + String(args["hook_id"]) + "/deliveries/" + String(args["delivery_id"])) + _qs, {}, undefined);
    }
  },
  {
    name: "orgs/redeliver-webhook-delivery",
    description: "Redeliver a delivery for an organization webhook. Redeliver a delivery for a webhook configured in an organization. Params: org (required), hook_id (required), delivery_id (required).",
    inputSchema: {"type":"object","properties":{"org":{"type":"string","description":"The organization name. The name is not case sensitive."},"hook_id":{"type":"integer","description":"The unique identifier of the hook. You can find this value in the `X-GitHub-Hook-ID` header of a webhook delivery."},"delivery_id":{"type":"integer"}},"required":["org","hook_id","delivery_id"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("POST", ("https://api.github.com/orgs/" + String(args["org"]) + "/hooks/" + String(args["hook_id"]) + "/deliveries/" + String(args["delivery_id"]) + "/attempts") + _qs, {}, undefined);
    }
  },
  {
    name: "orgs/ping-webhook",
    description: "Ping an organization webhook. This will trigger a [ping event](https://docs.github.com/webhooks/#ping-event)\nto be sent to the hook. Params: org (required), hook_id (required).",
    inputSchema: {"type":"object","properties":{"org":{"type":"string","description":"The organization name. The name is not case sensitive."},"hook_id":{"type":"integer","description":"The unique identifier of the hook. You can find this value in the `X-GitHub-Hook-ID` header of a webhook delivery."}},"required":["org","hook_id"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("POST", ("https://api.github.com/orgs/" + String(args["org"]) + "/hooks/" + String(args["hook_id"]) + "/pings") + _qs, {}, undefined);
    }
  },
  {
    name: "api-insights/get-route-stats-by-actor",
    description: "Get route stats by actor. Get API request count statistics for an actor broken down by route within a specified time frame. Params: org (required), actor_type (required), actor_id (required), min_timestamp (required).",
    inputSchema: {"type":"object","properties":{"org":{"type":"string","description":"The organization name. The name is not case sensitive."},"actor_type":{"type":"string","enum":["installation","classic_pat","fine_grained_pat","oauth_app","github_app_user_to_server"],"description":"The type of the actor"},"actor_id":{"type":"integer","description":"The ID of the actor"},"min_timestamp":{"type":"string","description":"The minimum timestamp to query for stats. This is a timestamp in [ISO 8601](https://en.wikipedia.org/wiki/ISO_8601) format: `YYYY-MM-DDTHH:MM:SSZ`."},"max_timestamp":{"type":"string","description":"The maximum timestamp to query for stats. Defaults to the time 30 days ago. This is a timestamp in [ISO 8601](https://en.wikipedia.org/wiki/ISO_8601) format: `YYYY-MM-DDTHH:MM:SSZ`."},"page":{"type":"integer","default":1,"description":"The page number of the results to fetch. For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""},"per_page":{"type":"integer","default":30,"description":"The number of results per page (max 100). For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""},"direction":{"type":"string","enum":["asc","desc"],"default":"desc","description":"The direction to sort the results by."},"sort":{"type":"array","items":{"type":"string","enum":["last_rate_limited_timestamp","last_request_timestamp","rate_limited_request_count","http_method","api_route","total_request_count"],"default":"total_request_count"},"description":"The property to sort the results by."},"api_route_substring":{"type":"string","description":"Providing a substring will filter results where the API route contains the substring. This is a case-insensitive search."}},"required":["org","actor_type","actor_id","min_timestamp"]},
    call: function(args) {
    var _q = new URLSearchParams();
    if (args["min_timestamp"] !== undefined) _q.set("min_timestamp", String(args["min_timestamp"]));
    if (args["max_timestamp"] !== undefined) _q.set("max_timestamp", String(args["max_timestamp"]));
    if (args["page"] !== undefined) _q.set("page", String(args["page"]));
    if (args["per_page"] !== undefined) _q.set("per_page", String(args["per_page"]));
    if (args["direction"] !== undefined) _q.set("direction", String(args["direction"]));
    if (args["sort"] !== undefined) _q.set("sort", String(args["sort"]));
    if (args["api_route_substring"] !== undefined) _q.set("api_route_substring", String(args["api_route_substring"]));
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/orgs/" + String(args["org"]) + "/insights/api/route-stats/" + String(args["actor_type"]) + "/" + String(args["actor_id"])) + _qs, {}, undefined);
    }
  },
  {
    name: "api-insights/get-subject-stats",
    description: "Get subject stats. Get API request statistics for all subjects within an organization within a specified time frame. Params: org (required), min_timestamp (required).",
    inputSchema: {"type":"object","properties":{"org":{"type":"string","description":"The organization name. The name is not case sensitive."},"min_timestamp":{"type":"string","description":"The minimum timestamp to query for stats. This is a timestamp in [ISO 8601](https://en.wikipedia.org/wiki/ISO_8601) format: `YYYY-MM-DDTHH:MM:SSZ`."},"max_timestamp":{"type":"string","description":"The maximum timestamp to query for stats. Defaults to the time 30 days ago. This is a timestamp in [ISO 8601](https://en.wikipedia.org/wiki/ISO_8601) format: `YYYY-MM-DDTHH:MM:SSZ`."},"page":{"type":"integer","default":1,"description":"The page number of the results to fetch. For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""},"per_page":{"type":"integer","default":30,"description":"The number of results per page (max 100). For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""},"direction":{"type":"string","enum":["asc","desc"],"default":"desc","description":"The direction to sort the results by."},"sort":{"type":"array","items":{"type":"string","enum":["last_rate_limited_timestamp","last_request_timestamp","rate_limited_request_count","subject_name","total_request_count"],"default":"total_request_count"},"description":"The property to sort the results by."},"subject_name_substring":{"type":"string","description":"Providing a substring will filter results where the subject name contains the substring. This is a case-insensitive search."}},"required":["org","min_timestamp"]},
    call: function(args) {
    var _q = new URLSearchParams();
    if (args["min_timestamp"] !== undefined) _q.set("min_timestamp", String(args["min_timestamp"]));
    if (args["max_timestamp"] !== undefined) _q.set("max_timestamp", String(args["max_timestamp"]));
    if (args["page"] !== undefined) _q.set("page", String(args["page"]));
    if (args["per_page"] !== undefined) _q.set("per_page", String(args["per_page"]));
    if (args["direction"] !== undefined) _q.set("direction", String(args["direction"]));
    if (args["sort"] !== undefined) _q.set("sort", String(args["sort"]));
    if (args["subject_name_substring"] !== undefined) _q.set("subject_name_substring", String(args["subject_name_substring"]));
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/orgs/" + String(args["org"]) + "/insights/api/subject-stats") + _qs, {}, undefined);
    }
  },
  {
    name: "api-insights/get-summary-stats",
    description: "Get summary stats. Get overall statistics of API requests made within an organization by all users and apps within a specified time frame. Params: org (required), min_timestamp (required).",
    inputSchema: {"type":"object","properties":{"org":{"type":"string","description":"The organization name. The name is not case sensitive."},"min_timestamp":{"type":"string","description":"The minimum timestamp to query for stats. This is a timestamp in [ISO 8601](https://en.wikipedia.org/wiki/ISO_8601) format: `YYYY-MM-DDTHH:MM:SSZ`."},"max_timestamp":{"type":"string","description":"The maximum timestamp to query for stats. Defaults to the time 30 days ago. This is a timestamp in [ISO 8601](https://en.wikipedia.org/wiki/ISO_8601) format: `YYYY-MM-DDTHH:MM:SSZ`."}},"required":["org","min_timestamp"]},
    call: function(args) {
    var _q = new URLSearchParams();
    if (args["min_timestamp"] !== undefined) _q.set("min_timestamp", String(args["min_timestamp"]));
    if (args["max_timestamp"] !== undefined) _q.set("max_timestamp", String(args["max_timestamp"]));
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/orgs/" + String(args["org"]) + "/insights/api/summary-stats") + _qs, {}, undefined);
    }
  },
  {
    name: "api-insights/get-summary-stats-by-user",
    description: "Get summary stats by user. Get overall statistics of API requests within the organization for a user. Params: org (required), user_id (required), min_timestamp (required).",
    inputSchema: {"type":"object","properties":{"org":{"type":"string","description":"The organization name. The name is not case sensitive."},"user_id":{"type":"string","description":"The ID of the user to query for stats"},"min_timestamp":{"type":"string","description":"The minimum timestamp to query for stats. This is a timestamp in [ISO 8601](https://en.wikipedia.org/wiki/ISO_8601) format: `YYYY-MM-DDTHH:MM:SSZ`."},"max_timestamp":{"type":"string","description":"The maximum timestamp to query for stats. Defaults to the time 30 days ago. This is a timestamp in [ISO 8601](https://en.wikipedia.org/wiki/ISO_8601) format: `YYYY-MM-DDTHH:MM:SSZ`."}},"required":["org","user_id","min_timestamp"]},
    call: function(args) {
    var _q = new URLSearchParams();
    if (args["min_timestamp"] !== undefined) _q.set("min_timestamp", String(args["min_timestamp"]));
    if (args["max_timestamp"] !== undefined) _q.set("max_timestamp", String(args["max_timestamp"]));
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/orgs/" + String(args["org"]) + "/insights/api/summary-stats/users/" + String(args["user_id"])) + _qs, {}, undefined);
    }
  },
  {
    name: "api-insights/get-summary-stats-by-actor",
    description: "Get summary stats by actor. Get overall statistics of API requests within the organization made by a specific actor. Params: org (required), min_timestamp (required), actor_type (required), actor_id (required).",
    inputSchema: {"type":"object","properties":{"org":{"type":"string","description":"The organization name. The name is not case sensitive."},"min_timestamp":{"type":"string","description":"The minimum timestamp to query for stats. This is a timestamp in [ISO 8601](https://en.wikipedia.org/wiki/ISO_8601) format: `YYYY-MM-DDTHH:MM:SSZ`."},"max_timestamp":{"type":"string","description":"The maximum timestamp to query for stats. Defaults to the time 30 days ago. This is a timestamp in [ISO 8601](https://en.wikipedia.org/wiki/ISO_8601) format: `YYYY-MM-DDTHH:MM:SSZ`."},"actor_type":{"type":"string","enum":["installation","classic_pat","fine_grained_pat","oauth_app","github_app_user_to_server"],"description":"The type of the actor"},"actor_id":{"type":"integer","description":"The ID of the actor"}},"required":["org","min_timestamp","actor_type","actor_id"]},
    call: function(args) {
    var _q = new URLSearchParams();
    if (args["min_timestamp"] !== undefined) _q.set("min_timestamp", String(args["min_timestamp"]));
    if (args["max_timestamp"] !== undefined) _q.set("max_timestamp", String(args["max_timestamp"]));
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/orgs/" + String(args["org"]) + "/insights/api/summary-stats/" + String(args["actor_type"]) + "/" + String(args["actor_id"])) + _qs, {}, undefined);
    }
  },
  {
    name: "api-insights/get-time-stats",
    description: "Get time stats. Get the number of API requests and rate-limited requests made within an organization over a specified time period. Params: org (required), min_timestamp (required), timestamp_increment (required).",
    inputSchema: {"type":"object","properties":{"org":{"type":"string","description":"The organization name. The name is not case sensitive."},"min_timestamp":{"type":"string","description":"The minimum timestamp to query for stats. This is a timestamp in [ISO 8601](https://en.wikipedia.org/wiki/ISO_8601) format: `YYYY-MM-DDTHH:MM:SSZ`."},"max_timestamp":{"type":"string","description":"The maximum timestamp to query for stats. Defaults to the time 30 days ago. This is a timestamp in [ISO 8601](https://en.wikipedia.org/wiki/ISO_8601) format: `YYYY-MM-DDTHH:MM:SSZ`."},"timestamp_increment":{"type":"string","description":"The increment of time used to breakdown the query results (5m, 10m, 1h, etc.)"}},"required":["org","min_timestamp","timestamp_increment"]},
    call: function(args) {
    var _q = new URLSearchParams();
    if (args["min_timestamp"] !== undefined) _q.set("min_timestamp", String(args["min_timestamp"]));
    if (args["max_timestamp"] !== undefined) _q.set("max_timestamp", String(args["max_timestamp"]));
    if (args["timestamp_increment"] !== undefined) _q.set("timestamp_increment", String(args["timestamp_increment"]));
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/orgs/" + String(args["org"]) + "/insights/api/time-stats") + _qs, {}, undefined);
    }
  },
  {
    name: "api-insights/get-time-stats-by-user",
    description: "Get time stats by user. Get the number of API requests and rate-limited requests made within an organization by a specific user over a specified time period. Params: org (required), user_id (required), min_timestamp (required), timestamp_increment (required).",
    inputSchema: {"type":"object","properties":{"org":{"type":"string","description":"The organization name. The name is not case sensitive."},"user_id":{"type":"string","description":"The ID of the user to query for stats"},"min_timestamp":{"type":"string","description":"The minimum timestamp to query for stats. This is a timestamp in [ISO 8601](https://en.wikipedia.org/wiki/ISO_8601) format: `YYYY-MM-DDTHH:MM:SSZ`."},"max_timestamp":{"type":"string","description":"The maximum timestamp to query for stats. Defaults to the time 30 days ago. This is a timestamp in [ISO 8601](https://en.wikipedia.org/wiki/ISO_8601) format: `YYYY-MM-DDTHH:MM:SSZ`."},"timestamp_increment":{"type":"string","description":"The increment of time used to breakdown the query results (5m, 10m, 1h, etc.)"}},"required":["org","user_id","min_timestamp","timestamp_increment"]},
    call: function(args) {
    var _q = new URLSearchParams();
    if (args["min_timestamp"] !== undefined) _q.set("min_timestamp", String(args["min_timestamp"]));
    if (args["max_timestamp"] !== undefined) _q.set("max_timestamp", String(args["max_timestamp"]));
    if (args["timestamp_increment"] !== undefined) _q.set("timestamp_increment", String(args["timestamp_increment"]));
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/orgs/" + String(args["org"]) + "/insights/api/time-stats/users/" + String(args["user_id"])) + _qs, {}, undefined);
    }
  },
  {
    name: "api-insights/get-time-stats-by-actor",
    description: "Get time stats by actor. Get the number of API requests and rate-limited requests made within an organization by a specific actor within a specified time period. Params: org (required), actor_type (required), actor_id (required), min_timestamp (required), timestamp_increment (required).",
    inputSchema: {"type":"object","properties":{"org":{"type":"string","description":"The organization name. The name is not case sensitive."},"actor_type":{"type":"string","enum":["installation","classic_pat","fine_grained_pat","oauth_app","github_app_user_to_server"],"description":"The type of the actor"},"actor_id":{"type":"integer","description":"The ID of the actor"},"min_timestamp":{"type":"string","description":"The minimum timestamp to query for stats. This is a timestamp in [ISO 8601](https://en.wikipedia.org/wiki/ISO_8601) format: `YYYY-MM-DDTHH:MM:SSZ`."},"max_timestamp":{"type":"string","description":"The maximum timestamp to query for stats. Defaults to the time 30 days ago. This is a timestamp in [ISO 8601](https://en.wikipedia.org/wiki/ISO_8601) format: `YYYY-MM-DDTHH:MM:SSZ`."},"timestamp_increment":{"type":"string","description":"The increment of time used to breakdown the query results (5m, 10m, 1h, etc.)"}},"required":["org","actor_type","actor_id","min_timestamp","timestamp_increment"]},
    call: function(args) {
    var _q = new URLSearchParams();
    if (args["min_timestamp"] !== undefined) _q.set("min_timestamp", String(args["min_timestamp"]));
    if (args["max_timestamp"] !== undefined) _q.set("max_timestamp", String(args["max_timestamp"]));
    if (args["timestamp_increment"] !== undefined) _q.set("timestamp_increment", String(args["timestamp_increment"]));
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/orgs/" + String(args["org"]) + "/insights/api/time-stats/" + String(args["actor_type"]) + "/" + String(args["actor_id"])) + _qs, {}, undefined);
    }
  },
  {
    name: "api-insights/get-user-stats",
    description: "Get user stats. Get API usage statistics within an organization for a user broken down by the type of access. Params: org (required), user_id (required), min_timestamp (required).",
    inputSchema: {"type":"object","properties":{"org":{"type":"string","description":"The organization name. The name is not case sensitive."},"user_id":{"type":"string","description":"The ID of the user to query for stats"},"min_timestamp":{"type":"string","description":"The minimum timestamp to query for stats. This is a timestamp in [ISO 8601](https://en.wikipedia.org/wiki/ISO_8601) format: `YYYY-MM-DDTHH:MM:SSZ`."},"max_timestamp":{"type":"string","description":"The maximum timestamp to query for stats. Defaults to the time 30 days ago. This is a timestamp in [ISO 8601](https://en.wikipedia.org/wiki/ISO_8601) format: `YYYY-MM-DDTHH:MM:SSZ`."},"page":{"type":"integer","default":1,"description":"The page number of the results to fetch. For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""},"per_page":{"type":"integer","default":30,"description":"The number of results per page (max 100). For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""},"direction":{"type":"string","enum":["asc","desc"],"default":"desc","description":"The direction to sort the results by."},"sort":{"type":"array","items":{"type":"string","enum":["last_rate_limited_timestamp","last_request_timestamp","rate_limited_request_count","subject_name","total_request_count"],"default":"total_request_count"},"description":"The property to sort the results by."},"actor_name_substring":{"type":"string","description":"Providing a substring will filter results where the actor name contains the substring. This is a case-insensitive search."}},"required":["org","user_id","min_timestamp"]},
    call: function(args) {
    var _q = new URLSearchParams();
    if (args["min_timestamp"] !== undefined) _q.set("min_timestamp", String(args["min_timestamp"]));
    if (args["max_timestamp"] !== undefined) _q.set("max_timestamp", String(args["max_timestamp"]));
    if (args["page"] !== undefined) _q.set("page", String(args["page"]));
    if (args["per_page"] !== undefined) _q.set("per_page", String(args["per_page"]));
    if (args["direction"] !== undefined) _q.set("direction", String(args["direction"]));
    if (args["sort"] !== undefined) _q.set("sort", String(args["sort"]));
    if (args["actor_name_substring"] !== undefined) _q.set("actor_name_substring", String(args["actor_name_substring"]));
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/orgs/" + String(args["org"]) + "/insights/api/user-stats/" + String(args["user_id"])) + _qs, {}, undefined);
    }
  },
  {
    name: "apps/get-org-installation",
    description: "Get an organization installation for the authenticated app. Enables an authenticated GitHub App to find the organization's installation information. Params: org (required).",
    inputSchema: {"type":"object","properties":{"org":{"type":"string","description":"The organization name. The name is not case sensitive."}},"required":["org"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/orgs/" + String(args["org"]) + "/installation") + _qs, {}, undefined);
    }
  },
  {
    name: "orgs/list-app-installations",
    description: "List app installations for an organization. Lists all GitHub Apps in an organization. Params: org (required).",
    inputSchema: {"type":"object","properties":{"org":{"type":"string","description":"The organization name. The name is not case sensitive."},"per_page":{"type":"integer","default":30,"description":"The number of results per page (max 100). For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""},"page":{"type":"integer","default":1,"description":"The page number of the results to fetch. For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""}},"required":["org"]},
    call: function(args) {
    var _q = new URLSearchParams();
    if (args["per_page"] !== undefined) _q.set("per_page", String(args["per_page"]));
    if (args["page"] !== undefined) _q.set("page", String(args["page"]));
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/orgs/" + String(args["org"]) + "/installations") + _qs, {}, undefined);
    }
  },
  {
    name: "interactions/get-restrictions-for-org",
    description: "Get interaction restrictions for an organization. Shows which type of GitHub user can interact with this organization and when the restriction expires. Params: org (required).",
    inputSchema: {"type":"object","properties":{"org":{"type":"string","description":"The organization name. The name is not case sensitive."}},"required":["org"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/orgs/" + String(args["org"]) + "/interaction-limits") + _qs, {}, undefined);
    }
  },
  {
    name: "interactions/set-restrictions-for-org",
    description: "Set interaction restrictions for an organization. Temporarily restricts interactions to a certain type of GitHub user in any public repository in the given organization. Params: org (required).",
    inputSchema: {"type":"object","properties":{"org":{"type":"string","description":"The organization name. The name is not case sensitive."},"body":{"type":"object","description":"Request body"}},"required":["org","body"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("PUT", ("https://api.github.com/orgs/" + String(args["org"]) + "/interaction-limits") + _qs, {}, args["body"]);
    }
  },
  {
    name: "interactions/remove-restrictions-for-org",
    description: "Remove interaction restrictions for an organization. Removes all interaction restrictions from public repositories in the given organization. Params: org (required).",
    inputSchema: {"type":"object","properties":{"org":{"type":"string","description":"The organization name. The name is not case sensitive."}},"required":["org"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("DELETE", ("https://api.github.com/orgs/" + String(args["org"]) + "/interaction-limits") + _qs, {}, undefined);
    }
  },
  {
    name: "orgs/list-pending-invitations",
    description: "List pending organization invitations. The return hash contains a `role` field which refers to the Organization\nInvitation role and will be one of the following values: `direct_member`, `admin`,\n`billing_manager`, or `hiring_manager`. Params: org (required).",
    inputSchema: {"type":"object","properties":{"org":{"type":"string","description":"The organization name. The name is not case sensitive."},"per_page":{"type":"integer","default":30,"description":"The number of results per page (max 100). For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""},"page":{"type":"integer","default":1,"description":"The page number of the results to fetch. For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""},"role":{"type":"string","enum":["all","admin","direct_member","billing_manager","hiring_manager"],"default":"all","description":"Filter invitations by their member role."},"invitation_source":{"type":"string","enum":["all","member","scim"],"default":"all","description":"Filter invitations by their invitation source."}},"required":["org"]},
    call: function(args) {
    var _q = new URLSearchParams();
    if (args["per_page"] !== undefined) _q.set("per_page", String(args["per_page"]));
    if (args["page"] !== undefined) _q.set("page", String(args["page"]));
    if (args["role"] !== undefined) _q.set("role", String(args["role"]));
    if (args["invitation_source"] !== undefined) _q.set("invitation_source", String(args["invitation_source"]));
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/orgs/" + String(args["org"]) + "/invitations") + _qs, {}, undefined);
    }
  },
  {
    name: "orgs/create-invitation",
    description: "Create an organization invitation. Invite people to an organization by using their GitHub user ID or their email address. Params: org (required).",
    inputSchema: {"type":"object","properties":{"org":{"type":"string","description":"The organization name. The name is not case sensitive."},"body":{"type":"object","description":"Request body"}},"required":["org"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("POST", ("https://api.github.com/orgs/" + String(args["org"]) + "/invitations") + _qs, {}, args["body"]);
    }
  },
  {
    name: "orgs/cancel-invitation",
    description: "Cancel an organization invitation. Params: org (required), invitation_id (required).",
    inputSchema: {"type":"object","properties":{"org":{"type":"string","description":"The organization name. The name is not case sensitive."},"invitation_id":{"type":"integer","description":"The unique identifier of the invitation."}},"required":["org","invitation_id"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("DELETE", ("https://api.github.com/orgs/" + String(args["org"]) + "/invitations/" + String(args["invitation_id"])) + _qs, {}, undefined);
    }
  },
  {
    name: "orgs/list-invitation-teams",
    description: "List organization invitation teams. List all teams associated with an invitation. Params: org (required), invitation_id (required).",
    inputSchema: {"type":"object","properties":{"org":{"type":"string","description":"The organization name. The name is not case sensitive."},"invitation_id":{"type":"integer","description":"The unique identifier of the invitation."},"per_page":{"type":"integer","default":30,"description":"The number of results per page (max 100). For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""},"page":{"type":"integer","default":1,"description":"The page number of the results to fetch. For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""}},"required":["org","invitation_id"]},
    call: function(args) {
    var _q = new URLSearchParams();
    if (args["per_page"] !== undefined) _q.set("per_page", String(args["per_page"]));
    if (args["page"] !== undefined) _q.set("page", String(args["page"]));
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/orgs/" + String(args["org"]) + "/invitations/" + String(args["invitation_id"]) + "/teams") + _qs, {}, undefined);
    }
  },
  {
    name: "orgs/list-issue-fields",
    description: "List issue fields for an organization. Lists all issue fields for an organization. Params: org (required).",
    inputSchema: {"type":"object","properties":{"org":{"type":"string","description":"The organization name. The name is not case sensitive."}},"required":["org"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/orgs/" + String(args["org"]) + "/issue-fields") + _qs, {}, undefined);
    }
  },
  {
    name: "orgs/create-issue-field",
    description: "Create issue field for an organization. Creates a new issue field for an organization. Params: org (required).",
    inputSchema: {"type":"object","properties":{"org":{"type":"string","description":"The organization name. The name is not case sensitive."},"body":{"type":"object","description":"Request body"}},"required":["org","body"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("POST", ("https://api.github.com/orgs/" + String(args["org"]) + "/issue-fields") + _qs, {}, args["body"]);
    }
  },
  {
    name: "orgs/update-issue-field",
    description: "Update issue field for an organization. Updates an issue field for an organization. Params: org (required), issue_field_id (required).",
    inputSchema: {"type":"object","properties":{"org":{"type":"string","description":"The organization name. The name is not case sensitive."},"issue_field_id":{"type":"integer","description":"The unique identifier of the issue field."},"body":{"type":"object","description":"Request body"}},"required":["org","issue_field_id","body"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("PATCH", ("https://api.github.com/orgs/" + String(args["org"]) + "/issue-fields/" + String(args["issue_field_id"])) + _qs, {}, args["body"]);
    }
  },
  {
    name: "orgs/delete-issue-field",
    description: "Delete issue field for an organization. Deletes an issue field for an organization. Params: org (required), issue_field_id (required).",
    inputSchema: {"type":"object","properties":{"org":{"type":"string","description":"The organization name. The name is not case sensitive."},"issue_field_id":{"type":"integer","description":"The unique identifier of the issue field."}},"required":["org","issue_field_id"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("DELETE", ("https://api.github.com/orgs/" + String(args["org"]) + "/issue-fields/" + String(args["issue_field_id"])) + _qs, {}, undefined);
    }
  },
  {
    name: "orgs/list-issue-types",
    description: "List issue types for an organization. Lists all issue types for an organization. Params: org (required).",
    inputSchema: {"type":"object","properties":{"org":{"type":"string","description":"The organization name. The name is not case sensitive."}},"required":["org"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/orgs/" + String(args["org"]) + "/issue-types") + _qs, {}, undefined);
    }
  },
  {
    name: "orgs/create-issue-type",
    description: "Create issue type for an organization. Create a new issue type for an organization. Params: org (required).",
    inputSchema: {"type":"object","properties":{"org":{"type":"string","description":"The organization name. The name is not case sensitive."},"body":{"type":"object","description":"Request body"}},"required":["org","body"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("POST", ("https://api.github.com/orgs/" + String(args["org"]) + "/issue-types") + _qs, {}, args["body"]);
    }
  },
  {
    name: "orgs/update-issue-type",
    description: "Update issue type for an organization. Updates an issue type for an organization. Params: org (required), issue_type_id (required).",
    inputSchema: {"type":"object","properties":{"org":{"type":"string","description":"The organization name. The name is not case sensitive."},"issue_type_id":{"type":"integer","description":"The unique identifier of the issue type."},"body":{"type":"object","description":"Request body"}},"required":["org","issue_type_id","body"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("PUT", ("https://api.github.com/orgs/" + String(args["org"]) + "/issue-types/" + String(args["issue_type_id"])) + _qs, {}, args["body"]);
    }
  },
  {
    name: "orgs/delete-issue-type",
    description: "Delete issue type for an organization. Deletes an issue type for an organization. Params: org (required), issue_type_id (required).",
    inputSchema: {"type":"object","properties":{"org":{"type":"string","description":"The organization name. The name is not case sensitive."},"issue_type_id":{"type":"integer","description":"The unique identifier of the issue type."}},"required":["org","issue_type_id"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("DELETE", ("https://api.github.com/orgs/" + String(args["org"]) + "/issue-types/" + String(args["issue_type_id"])) + _qs, {}, undefined);
    }
  },
  {
    name: "issues/list-for-org",
    description: "List organization issues assigned to the authenticated user. List issues in an organization assigned to the authenticated user. Params: org (required).",
    inputSchema: {"type":"object","properties":{"org":{"type":"string","description":"The organization name. The name is not case sensitive."},"filter":{"type":"string","enum":["assigned","created","mentioned","subscribed","repos","all"],"default":"assigned","description":"Indicates which sorts of issues to return. `assigned` means issues assigned to you. `created` means issues created by you. `mentioned` means issues mentioning you. `subscribed` means issues you're subscribed to updates for. `all` or `repos` means all issues you can see, regardless of participation or creation."},"state":{"type":"string","enum":["open","closed","all"],"default":"open","description":"Indicates the state of the issues to return."},"labels":{"type":"string","description":"A list of comma separated label names. Example: `bug,ui,@high`"},"type":{"type":"string","description":"Can be the name of an issue type."},"sort":{"type":"string","enum":["created","updated","comments"],"default":"created","description":"What to sort results by."},"direction":{"type":"string","enum":["asc","desc"],"default":"desc","description":"The direction to sort the results by."},"since":{"type":"string","format":"date-time","description":"Only show results that were last updated after the given time. This is a timestamp in [ISO 8601](https://en.wikipedia.org/wiki/ISO_8601) format: `YYYY-MM-DDTHH:MM:SSZ`."},"per_page":{"type":"integer","default":30,"description":"The number of results per page (max 100). For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""},"page":{"type":"integer","default":1,"description":"The page number of the results to fetch. For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""}},"required":["org"]},
    call: function(args) {
    var _q = new URLSearchParams();
    if (args["filter"] !== undefined) _q.set("filter", String(args["filter"]));
    if (args["state"] !== undefined) _q.set("state", String(args["state"]));
    if (args["labels"] !== undefined) _q.set("labels", String(args["labels"]));
    if (args["type"] !== undefined) _q.set("type", String(args["type"]));
    if (args["sort"] !== undefined) _q.set("sort", String(args["sort"]));
    if (args["direction"] !== undefined) _q.set("direction", String(args["direction"]));
    if (args["since"] !== undefined) _q.set("since", String(args["since"]));
    if (args["per_page"] !== undefined) _q.set("per_page", String(args["per_page"]));
    if (args["page"] !== undefined) _q.set("page", String(args["page"]));
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/orgs/" + String(args["org"]) + "/issues") + _qs, {}, undefined);
    }
  },
  {
    name: "orgs/list-members",
    description: "List organization members. List all users who are members of an organization. Params: org (required).",
    inputSchema: {"type":"object","properties":{"org":{"type":"string","description":"The organization name. The name is not case sensitive."},"filter":{"type":"string","enum":["2fa_disabled","2fa_insecure","all"],"default":"all","description":"Filter members returned in the list. `2fa_disabled` means that only members without [two-factor authentication](https://github.com/blog/1614-two-factor-authentication) enabled will be returned. `2fa_insecure` means that only members with [insecure 2FA methods](https://docs.github.com/organizations/keeping-your-organization-secure/managing-two-factor-authentication-for-your-organization/requiring-two-factor-authentication-in-your-organization#requiring-secure-methods-of-two-factor-authentication-in-your-organization) will be returned. These options are only available for organization owners."},"role":{"type":"string","enum":["all","admin","member"],"default":"all","description":"Filter members returned by their role."},"per_page":{"type":"integer","default":30,"description":"The number of results per page (max 100). For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""},"page":{"type":"integer","default":1,"description":"The page number of the results to fetch. For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""}},"required":["org"]},
    call: function(args) {
    var _q = new URLSearchParams();
    if (args["filter"] !== undefined) _q.set("filter", String(args["filter"]));
    if (args["role"] !== undefined) _q.set("role", String(args["role"]));
    if (args["per_page"] !== undefined) _q.set("per_page", String(args["per_page"]));
    if (args["page"] !== undefined) _q.set("page", String(args["page"]));
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/orgs/" + String(args["org"]) + "/members") + _qs, {}, undefined);
    }
  },
  {
    name: "orgs/check-membership-for-user",
    description: "Check organization membership for a user. Check if a user is, publicly or privately, a member of the organization. Params: org (required), username (required).",
    inputSchema: {"type":"object","properties":{"org":{"type":"string","description":"The organization name. The name is not case sensitive."},"username":{"type":"string","description":"The handle for the GitHub user account."}},"required":["org","username"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/orgs/" + String(args["org"]) + "/members/" + String(args["username"])) + _qs, {}, undefined);
    }
  },
  {
    name: "orgs/remove-member",
    description: "Remove an organization member. Removing a user from this list will remove them from all teams and they will no longer have any access to the organization's repositories. Params: org (required), username (required).",
    inputSchema: {"type":"object","properties":{"org":{"type":"string","description":"The organization name. The name is not case sensitive."},"username":{"type":"string","description":"The handle for the GitHub user account."}},"required":["org","username"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("DELETE", ("https://api.github.com/orgs/" + String(args["org"]) + "/members/" + String(args["username"])) + _qs, {}, undefined);
    }
  },
  {
    name: "codespaces/get-codespaces-for-user-in-org",
    description: "List codespaces for a user in organization. Lists the codespaces that a member of an organization has for repositories in that organization. Params: org (required), username (required).",
    inputSchema: {"type":"object","properties":{"per_page":{"type":"integer","default":30,"description":"The number of results per page (max 100). For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""},"page":{"type":"integer","default":1,"description":"The page number of the results to fetch. For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""},"org":{"type":"string","description":"The organization name. The name is not case sensitive."},"username":{"type":"string","description":"The handle for the GitHub user account."}},"required":["org","username"]},
    call: function(args) {
    var _q = new URLSearchParams();
    if (args["per_page"] !== undefined) _q.set("per_page", String(args["per_page"]));
    if (args["page"] !== undefined) _q.set("page", String(args["page"]));
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/orgs/" + String(args["org"]) + "/members/" + String(args["username"]) + "/codespaces") + _qs, {}, undefined);
    }
  },
  {
    name: "codespaces/delete-from-organization",
    description: "Delete a codespace from the organization. Deletes a user's codespace. Params: org (required), username (required), codespace_name (required).",
    inputSchema: {"type":"object","properties":{"org":{"type":"string","description":"The organization name. The name is not case sensitive."},"username":{"type":"string","description":"The handle for the GitHub user account."},"codespace_name":{"type":"string","description":"The name of the codespace."}},"required":["org","username","codespace_name"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("DELETE", ("https://api.github.com/orgs/" + String(args["org"]) + "/members/" + String(args["username"]) + "/codespaces/" + String(args["codespace_name"])) + _qs, {}, undefined);
    }
  },
  {
    name: "codespaces/stop-in-organization",
    description: "Stop a codespace for an organization user. Stops a user's codespace. Params: org (required), username (required), codespace_name (required).",
    inputSchema: {"type":"object","properties":{"org":{"type":"string","description":"The organization name. The name is not case sensitive."},"username":{"type":"string","description":"The handle for the GitHub user account."},"codespace_name":{"type":"string","description":"The name of the codespace."}},"required":["org","username","codespace_name"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("POST", ("https://api.github.com/orgs/" + String(args["org"]) + "/members/" + String(args["username"]) + "/codespaces/" + String(args["codespace_name"]) + "/stop") + _qs, {}, undefined);
    }
  },
  {
    name: "copilot/get-copilot-seat-details-for-user",
    description: "Get Copilot seat assignment details for a user. > [!NOTE]\n> This endpoint is in public preview and is subject to change. Params: org (required), username (required).",
    inputSchema: {"type":"object","properties":{"org":{"type":"string","description":"The organization name. The name is not case sensitive."},"username":{"type":"string","description":"The handle for the GitHub user account."}},"required":["org","username"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/orgs/" + String(args["org"]) + "/members/" + String(args["username"]) + "/copilot") + _qs, {}, undefined);
    }
  },
  {
    name: "orgs/get-membership-for-user",
    description: "Get organization membership for a user. In order to get a user's membership with an organization, the authenticated user must be an organization member. Params: org (required), username (required).",
    inputSchema: {"type":"object","properties":{"org":{"type":"string","description":"The organization name. The name is not case sensitive."},"username":{"type":"string","description":"The handle for the GitHub user account."}},"required":["org","username"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/orgs/" + String(args["org"]) + "/memberships/" + String(args["username"])) + _qs, {}, undefined);
    }
  },
  {
    name: "orgs/set-membership-for-user",
    description: "Set organization membership for a user. Only authenticated organization owners can add a member to the organization or update the member's role. Params: org (required), username (required).",
    inputSchema: {"type":"object","properties":{"org":{"type":"string","description":"The organization name. The name is not case sensitive."},"username":{"type":"string","description":"The handle for the GitHub user account."},"body":{"type":"object","description":"Request body"}},"required":["org","username"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("PUT", ("https://api.github.com/orgs/" + String(args["org"]) + "/memberships/" + String(args["username"])) + _qs, {}, args["body"]);
    }
  },
  {
    name: "orgs/remove-membership-for-user",
    description: "Remove organization membership for a user. In order to remove a user's membership with an organization, the authenticated user must be an organization owner. Params: org (required), username (required).",
    inputSchema: {"type":"object","properties":{"org":{"type":"string","description":"The organization name. The name is not case sensitive."},"username":{"type":"string","description":"The handle for the GitHub user account."}},"required":["org","username"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("DELETE", ("https://api.github.com/orgs/" + String(args["org"]) + "/memberships/" + String(args["username"])) + _qs, {}, undefined);
    }
  },
  {
    name: "migrations/list-for-org",
    description: "List organization migrations. Lists the most recent migrations, including both exports (which can be started through the REST API) and imports (which cannot be started using the REST API). Params: org (required).",
    inputSchema: {"type":"object","properties":{"org":{"type":"string","description":"The organization name. The name is not case sensitive."},"per_page":{"type":"integer","default":30,"description":"The number of results per page (max 100). For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""},"page":{"type":"integer","default":1,"description":"The page number of the results to fetch. For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""},"exclude":{"type":"array","items":{"description":"Allowed values that can be passed to the exclude param.","enum":["repositories"],"example":"repositories","type":"string"},"description":"Exclude attributes from the API response to improve performance"}},"required":["org"]},
    call: function(args) {
    var _q = new URLSearchParams();
    if (args["per_page"] !== undefined) _q.set("per_page", String(args["per_page"]));
    if (args["page"] !== undefined) _q.set("page", String(args["page"]));
    if (args["exclude"] !== undefined) _q.set("exclude", String(args["exclude"]));
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/orgs/" + String(args["org"]) + "/migrations") + _qs, {}, undefined);
    }
  },
  {
    name: "migrations/start-for-org",
    description: "Start an organization migration. Initiates the generation of a migration archive. Params: org (required).",
    inputSchema: {"type":"object","properties":{"org":{"type":"string","description":"The organization name. The name is not case sensitive."},"body":{"type":"object","description":"Request body"}},"required":["org","body"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("POST", ("https://api.github.com/orgs/" + String(args["org"]) + "/migrations") + _qs, {}, args["body"]);
    }
  },
  {
    name: "migrations/get-status-for-org",
    description: "Get an organization migration status. Fetches the status of a migration. Params: org (required), migration_id (required).",
    inputSchema: {"type":"object","properties":{"org":{"type":"string","description":"The organization name. The name is not case sensitive."},"migration_id":{"type":"integer","description":"The unique identifier of the migration."},"exclude":{"type":"array","items":{"description":"Allowed values that can be passed to the exclude param.","enum":["repositories"],"example":"repositories","type":"string"},"description":"Exclude attributes from the API response to improve performance"}},"required":["org","migration_id"]},
    call: function(args) {
    var _q = new URLSearchParams();
    if (args["exclude"] !== undefined) _q.set("exclude", String(args["exclude"]));
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/orgs/" + String(args["org"]) + "/migrations/" + String(args["migration_id"])) + _qs, {}, undefined);
    }
  },
  {
    name: "migrations/download-archive-for-org",
    description: "Download an organization migration archive. Fetches the URL to a migration archive. Params: org (required), migration_id (required).",
    inputSchema: {"type":"object","properties":{"org":{"type":"string","description":"The organization name. The name is not case sensitive."},"migration_id":{"type":"integer","description":"The unique identifier of the migration."}},"required":["org","migration_id"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/orgs/" + String(args["org"]) + "/migrations/" + String(args["migration_id"]) + "/archive") + _qs, {}, undefined);
    }
  },
  {
    name: "migrations/delete-archive-for-org",
    description: "Delete an organization migration archive. Deletes a previous migration archive. Params: org (required), migration_id (required).",
    inputSchema: {"type":"object","properties":{"org":{"type":"string","description":"The organization name. The name is not case sensitive."},"migration_id":{"type":"integer","description":"The unique identifier of the migration."}},"required":["org","migration_id"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("DELETE", ("https://api.github.com/orgs/" + String(args["org"]) + "/migrations/" + String(args["migration_id"]) + "/archive") + _qs, {}, undefined);
    }
  },
  {
    name: "migrations/unlock-repo-for-org",
    description: "Unlock an organization repository. Unlocks a repository that was locked for migration. Params: org (required), migration_id (required), repo_name (required).",
    inputSchema: {"type":"object","properties":{"org":{"type":"string","description":"The organization name. The name is not case sensitive."},"migration_id":{"type":"integer","description":"The unique identifier of the migration."},"repo_name":{"type":"string","description":"repo_name parameter"}},"required":["org","migration_id","repo_name"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("DELETE", ("https://api.github.com/orgs/" + String(args["org"]) + "/migrations/" + String(args["migration_id"]) + "/repos/" + String(args["repo_name"]) + "/lock") + _qs, {}, undefined);
    }
  },
  {
    name: "migrations/list-repos-for-org",
    description: "List repositories in an organization migration. List all the repositories for this organization migration. Params: org (required), migration_id (required).",
    inputSchema: {"type":"object","properties":{"org":{"type":"string","description":"The organization name. The name is not case sensitive."},"migration_id":{"type":"integer","description":"The unique identifier of the migration."},"per_page":{"type":"integer","default":30,"description":"The number of results per page (max 100). For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""},"page":{"type":"integer","default":1,"description":"The page number of the results to fetch. For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""}},"required":["org","migration_id"]},
    call: function(args) {
    var _q = new URLSearchParams();
    if (args["per_page"] !== undefined) _q.set("per_page", String(args["per_page"]));
    if (args["page"] !== undefined) _q.set("page", String(args["page"]));
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/orgs/" + String(args["org"]) + "/migrations/" + String(args["migration_id"]) + "/repositories") + _qs, {}, undefined);
    }
  },
  {
    name: "orgs/list-org-roles",
    description: "Get all organization roles for an organization. Lists the organization roles available in this organization. Params: org (required).",
    inputSchema: {"type":"object","properties":{"org":{"type":"string","description":"The organization name. The name is not case sensitive."}},"required":["org"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/orgs/" + String(args["org"]) + "/organization-roles") + _qs, {}, undefined);
    }
  },
  {
    name: "orgs/revoke-all-org-roles-team",
    description: "Remove all organization roles for a team. Removes all assigned organization roles from a team. Params: org (required), team_slug (required).",
    inputSchema: {"type":"object","properties":{"org":{"type":"string","description":"The organization name. The name is not case sensitive."},"team_slug":{"type":"string","description":"The slug of the team name."}},"required":["org","team_slug"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("DELETE", ("https://api.github.com/orgs/" + String(args["org"]) + "/organization-roles/teams/" + String(args["team_slug"])) + _qs, {}, undefined);
    }
  },
  {
    name: "orgs/assign-team-to-org-role",
    description: "Assign an organization role to a team. Assigns an organization role to a team in an organization. Params: org (required), team_slug (required), role_id (required).",
    inputSchema: {"type":"object","properties":{"org":{"type":"string","description":"The organization name. The name is not case sensitive."},"team_slug":{"type":"string","description":"The slug of the team name."},"role_id":{"type":"integer","description":"The unique identifier of the role."}},"required":["org","team_slug","role_id"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("PUT", ("https://api.github.com/orgs/" + String(args["org"]) + "/organization-roles/teams/" + String(args["team_slug"]) + "/" + String(args["role_id"])) + _qs, {}, undefined);
    }
  },
  {
    name: "orgs/revoke-org-role-team",
    description: "Remove an organization role from a team. Removes an organization role from a team. Params: org (required), team_slug (required), role_id (required).",
    inputSchema: {"type":"object","properties":{"org":{"type":"string","description":"The organization name. The name is not case sensitive."},"team_slug":{"type":"string","description":"The slug of the team name."},"role_id":{"type":"integer","description":"The unique identifier of the role."}},"required":["org","team_slug","role_id"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("DELETE", ("https://api.github.com/orgs/" + String(args["org"]) + "/organization-roles/teams/" + String(args["team_slug"]) + "/" + String(args["role_id"])) + _qs, {}, undefined);
    }
  },
  {
    name: "orgs/revoke-all-org-roles-user",
    description: "Remove all organization roles for a user. Revokes all assigned organization roles from a user. Params: org (required), username (required).",
    inputSchema: {"type":"object","properties":{"org":{"type":"string","description":"The organization name. The name is not case sensitive."},"username":{"type":"string","description":"The handle for the GitHub user account."}},"required":["org","username"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("DELETE", ("https://api.github.com/orgs/" + String(args["org"]) + "/organization-roles/users/" + String(args["username"])) + _qs, {}, undefined);
    }
  },
  {
    name: "orgs/assign-user-to-org-role",
    description: "Assign an organization role to a user. Assigns an organization role to a member of an organization. Params: org (required), username (required), role_id (required).",
    inputSchema: {"type":"object","properties":{"org":{"type":"string","description":"The organization name. The name is not case sensitive."},"username":{"type":"string","description":"The handle for the GitHub user account."},"role_id":{"type":"integer","description":"The unique identifier of the role."}},"required":["org","username","role_id"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("PUT", ("https://api.github.com/orgs/" + String(args["org"]) + "/organization-roles/users/" + String(args["username"]) + "/" + String(args["role_id"])) + _qs, {}, undefined);
    }
  },
  {
    name: "orgs/revoke-org-role-user",
    description: "Remove an organization role from a user. Params: org (required), username (required), role_id (required).",
    inputSchema: {"type":"object","properties":{"org":{"type":"string","description":"The organization name. The name is not case sensitive."},"username":{"type":"string","description":"The handle for the GitHub user account."},"role_id":{"type":"integer","description":"The unique identifier of the role."}},"required":["org","username","role_id"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("DELETE", ("https://api.github.com/orgs/" + String(args["org"]) + "/organization-roles/users/" + String(args["username"]) + "/" + String(args["role_id"])) + _qs, {}, undefined);
    }
  },
  {
    name: "orgs/get-org-role",
    description: "Get an organization role. Gets an organization role that is available to this organization. Params: org (required), role_id (required).",
    inputSchema: {"type":"object","properties":{"org":{"type":"string","description":"The organization name. The name is not case sensitive."},"role_id":{"type":"integer","description":"The unique identifier of the role."}},"required":["org","role_id"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/orgs/" + String(args["org"]) + "/organization-roles/" + String(args["role_id"])) + _qs, {}, undefined);
    }
  },
  {
    name: "orgs/list-org-role-teams",
    description: "List teams that are assigned to an organization role. Lists the teams that are assigned to an organization role. Params: org (required), role_id (required).",
    inputSchema: {"type":"object","properties":{"org":{"type":"string","description":"The organization name. The name is not case sensitive."},"role_id":{"type":"integer","description":"The unique identifier of the role."},"per_page":{"type":"integer","default":30,"description":"The number of results per page (max 100). For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""},"page":{"type":"integer","default":1,"description":"The page number of the results to fetch. For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""}},"required":["org","role_id"]},
    call: function(args) {
    var _q = new URLSearchParams();
    if (args["per_page"] !== undefined) _q.set("per_page", String(args["per_page"]));
    if (args["page"] !== undefined) _q.set("page", String(args["page"]));
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/orgs/" + String(args["org"]) + "/organization-roles/" + String(args["role_id"]) + "/teams") + _qs, {}, undefined);
    }
  },
  {
    name: "orgs/list-org-role-users",
    description: "List users that are assigned to an organization role. Lists organization members that are assigned to an organization role. Params: org (required), role_id (required).",
    inputSchema: {"type":"object","properties":{"org":{"type":"string","description":"The organization name. The name is not case sensitive."},"role_id":{"type":"integer","description":"The unique identifier of the role."},"per_page":{"type":"integer","default":30,"description":"The number of results per page (max 100). For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""},"page":{"type":"integer","default":1,"description":"The page number of the results to fetch. For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""}},"required":["org","role_id"]},
    call: function(args) {
    var _q = new URLSearchParams();
    if (args["per_page"] !== undefined) _q.set("per_page", String(args["per_page"]));
    if (args["page"] !== undefined) _q.set("page", String(args["page"]));
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/orgs/" + String(args["org"]) + "/organization-roles/" + String(args["role_id"]) + "/users") + _qs, {}, undefined);
    }
  },
  {
    name: "orgs/list-outside-collaborators",
    description: "List outside collaborators for an organization. List all users who are outside collaborators of an organization. Params: org (required).",
    inputSchema: {"type":"object","properties":{"org":{"type":"string","description":"The organization name. The name is not case sensitive."},"filter":{"type":"string","enum":["2fa_disabled","2fa_insecure","all"],"default":"all","description":"Filter the list of outside collaborators. `2fa_disabled` means that only outside collaborators without [two-factor authentication](https://github.com/blog/1614-two-factor-authentication) enabled will be returned. `2fa_insecure` means that only outside collaborators with [insecure 2FA methods](https://docs.github.com/organizations/keeping-your-organization-secure/managing-two-factor-authentication-for-your-organization/requiring-two-factor-authentication-in-your-organization#requiring-secure-methods-of-two-factor-authentication-in-your-organization) will be returned."},"per_page":{"type":"integer","default":30,"description":"The number of results per page (max 100). For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""},"page":{"type":"integer","default":1,"description":"The page number of the results to fetch. For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""}},"required":["org"]},
    call: function(args) {
    var _q = new URLSearchParams();
    if (args["filter"] !== undefined) _q.set("filter", String(args["filter"]));
    if (args["per_page"] !== undefined) _q.set("per_page", String(args["per_page"]));
    if (args["page"] !== undefined) _q.set("page", String(args["page"]));
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/orgs/" + String(args["org"]) + "/outside_collaborators") + _qs, {}, undefined);
    }
  },
  {
    name: "orgs/convert-member-to-outside-collaborator",
    description: "Convert an organization member to outside collaborator. When an organization member is converted to an outside collaborator, they'll only have access to the repositories that their current team membership allows. Params: org (required), username (required).",
    inputSchema: {"type":"object","properties":{"org":{"type":"string","description":"The organization name. The name is not case sensitive."},"username":{"type":"string","description":"The handle for the GitHub user account."},"body":{"type":"object","description":"Request body"}},"required":["org","username"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("PUT", ("https://api.github.com/orgs/" + String(args["org"]) + "/outside_collaborators/" + String(args["username"])) + _qs, {}, args["body"]);
    }
  },
  {
    name: "orgs/remove-outside-collaborator",
    description: "Remove outside collaborator from an organization. Removing a user from this list will remove them from all the organization's repositories. Params: org (required), username (required).",
    inputSchema: {"type":"object","properties":{"org":{"type":"string","description":"The organization name. The name is not case sensitive."},"username":{"type":"string","description":"The handle for the GitHub user account."}},"required":["org","username"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("DELETE", ("https://api.github.com/orgs/" + String(args["org"]) + "/outside_collaborators/" + String(args["username"])) + _qs, {}, undefined);
    }
  },
  {
    name: "packages/list-packages-for-organization",
    description: "List packages for an organization. Lists packages in an organization readable by the user. Params: package_type (required), org (required).",
    inputSchema: {"type":"object","properties":{"package_type":{"type":"string","enum":["npm","maven","rubygems","docker","nuget","container"],"description":"The type of supported package. Packages in GitHub's Gradle registry have the type `maven`. Docker images pushed to GitHub's Container registry (`ghcr.io`) have the type `container`. You can use the type `docker` to find images that were pushed to GitHub's Docker registry (`docker.pkg.github.com`), even if these have now been migrated to the Container registry."},"org":{"type":"string","description":"The organization name. The name is not case sensitive."},"visibility":{"type":"string","enum":["public","private","internal"],"description":"The selected visibility of the packages.  This parameter is optional and only filters an existing result set.\n\nThe `internal` visibility is only supported for GitHub Packages registries that allow for granular permissions. For other ecosystems `internal` is synonymous with `private`.\nFor the list of GitHub Packages registries that support granular permissions, see \"[About permissions for GitHub Packages](https://docs.github.com/packages/learn-github-packages/about-permissions-for-github-packages#granular-permissions-for-userorganization-scoped-packages).\""},"page":{"type":"integer","default":1,"description":"The page number of the results to fetch. For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""},"per_page":{"type":"integer","default":30,"description":"The number of results per page (max 100). For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""}},"required":["package_type","org"]},
    call: function(args) {
    var _q = new URLSearchParams();
    if (args["package_type"] !== undefined) _q.set("package_type", String(args["package_type"]));
    if (args["visibility"] !== undefined) _q.set("visibility", String(args["visibility"]));
    if (args["page"] !== undefined) _q.set("page", String(args["page"]));
    if (args["per_page"] !== undefined) _q.set("per_page", String(args["per_page"]));
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/orgs/" + String(args["org"]) + "/packages") + _qs, {}, undefined);
    }
  },
  {
    name: "packages/get-package-for-organization",
    description: "Get a package for an organization. Gets a specific package in an organization. Params: package_type (required), package_name (required), org (required).",
    inputSchema: {"type":"object","properties":{"package_type":{"type":"string","enum":["npm","maven","rubygems","docker","nuget","container"],"description":"The type of supported package. Packages in GitHub's Gradle registry have the type `maven`. Docker images pushed to GitHub's Container registry (`ghcr.io`) have the type `container`. You can use the type `docker` to find images that were pushed to GitHub's Docker registry (`docker.pkg.github.com`), even if these have now been migrated to the Container registry."},"package_name":{"type":"string","description":"The name of the package."},"org":{"type":"string","description":"The organization name. The name is not case sensitive."}},"required":["package_type","package_name","org"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/orgs/" + String(args["org"]) + "/packages/" + String(args["package_type"]) + "/" + String(args["package_name"])) + _qs, {}, undefined);
    }
  },
  {
    name: "packages/delete-package-for-org",
    description: "Delete a package for an organization. Deletes an entire package in an organization. Params: package_type (required), package_name (required), org (required).",
    inputSchema: {"type":"object","properties":{"package_type":{"type":"string","enum":["npm","maven","rubygems","docker","nuget","container"],"description":"The type of supported package. Packages in GitHub's Gradle registry have the type `maven`. Docker images pushed to GitHub's Container registry (`ghcr.io`) have the type `container`. You can use the type `docker` to find images that were pushed to GitHub's Docker registry (`docker.pkg.github.com`), even if these have now been migrated to the Container registry."},"package_name":{"type":"string","description":"The name of the package."},"org":{"type":"string","description":"The organization name. The name is not case sensitive."}},"required":["package_type","package_name","org"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("DELETE", ("https://api.github.com/orgs/" + String(args["org"]) + "/packages/" + String(args["package_type"]) + "/" + String(args["package_name"])) + _qs, {}, undefined);
    }
  },
  {
    name: "packages/restore-package-for-org",
    description: "Restore a package for an organization. Restores an entire package in an organization. Params: package_type (required), package_name (required), org (required).",
    inputSchema: {"type":"object","properties":{"package_type":{"type":"string","enum":["npm","maven","rubygems","docker","nuget","container"],"description":"The type of supported package. Packages in GitHub's Gradle registry have the type `maven`. Docker images pushed to GitHub's Container registry (`ghcr.io`) have the type `container`. You can use the type `docker` to find images that were pushed to GitHub's Docker registry (`docker.pkg.github.com`), even if these have now been migrated to the Container registry."},"package_name":{"type":"string","description":"The name of the package."},"org":{"type":"string","description":"The organization name. The name is not case sensitive."},"token":{"type":"string","description":"package token"}},"required":["package_type","package_name","org"]},
    call: function(args) {
    var _q = new URLSearchParams();
    if (args["token"] !== undefined) _q.set("token", String(args["token"]));
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("POST", ("https://api.github.com/orgs/" + String(args["org"]) + "/packages/" + String(args["package_type"]) + "/" + String(args["package_name"]) + "/restore") + _qs, {}, undefined);
    }
  },
  {
    name: "packages/get-all-package-versions-for-package-owned-by-org",
    description: "List package versions for a package owned by an organization. Lists package versions for a package owned by an organization. Params: package_type (required), package_name (required), org (required).",
    inputSchema: {"type":"object","properties":{"package_type":{"type":"string","enum":["npm","maven","rubygems","docker","nuget","container"],"description":"The type of supported package. Packages in GitHub's Gradle registry have the type `maven`. Docker images pushed to GitHub's Container registry (`ghcr.io`) have the type `container`. You can use the type `docker` to find images that were pushed to GitHub's Docker registry (`docker.pkg.github.com`), even if these have now been migrated to the Container registry."},"package_name":{"type":"string","description":"The name of the package."},"org":{"type":"string","description":"The organization name. The name is not case sensitive."},"page":{"type":"integer","default":1,"description":"The page number of the results to fetch. For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""},"per_page":{"type":"integer","default":30,"description":"The number of results per page (max 100). For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""},"state":{"type":"string","enum":["active","deleted"],"default":"active","description":"The state of the package, either active or deleted."}},"required":["package_type","package_name","org"]},
    call: function(args) {
    var _q = new URLSearchParams();
    if (args["page"] !== undefined) _q.set("page", String(args["page"]));
    if (args["per_page"] !== undefined) _q.set("per_page", String(args["per_page"]));
    if (args["state"] !== undefined) _q.set("state", String(args["state"]));
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/orgs/" + String(args["org"]) + "/packages/" + String(args["package_type"]) + "/" + String(args["package_name"]) + "/versions") + _qs, {}, undefined);
    }
  },
  {
    name: "packages/get-package-version-for-organization",
    description: "Get a package version for an organization. Gets a specific package version in an organization. Params: package_type (required), package_name (required), org (required), package_version_id (required).",
    inputSchema: {"type":"object","properties":{"package_type":{"type":"string","enum":["npm","maven","rubygems","docker","nuget","container"],"description":"The type of supported package. Packages in GitHub's Gradle registry have the type `maven`. Docker images pushed to GitHub's Container registry (`ghcr.io`) have the type `container`. You can use the type `docker` to find images that were pushed to GitHub's Docker registry (`docker.pkg.github.com`), even if these have now been migrated to the Container registry."},"package_name":{"type":"string","description":"The name of the package."},"org":{"type":"string","description":"The organization name. The name is not case sensitive."},"package_version_id":{"type":"integer","description":"Unique identifier of the package version."}},"required":["package_type","package_name","org","package_version_id"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/orgs/" + String(args["org"]) + "/packages/" + String(args["package_type"]) + "/" + String(args["package_name"]) + "/versions/" + String(args["package_version_id"])) + _qs, {}, undefined);
    }
  },
  {
    name: "packages/delete-package-version-for-org",
    description: "Delete package version for an organization. Deletes a specific package version in an organization. Params: package_type (required), package_name (required), org (required), package_version_id (required).",
    inputSchema: {"type":"object","properties":{"package_type":{"type":"string","enum":["npm","maven","rubygems","docker","nuget","container"],"description":"The type of supported package. Packages in GitHub's Gradle registry have the type `maven`. Docker images pushed to GitHub's Container registry (`ghcr.io`) have the type `container`. You can use the type `docker` to find images that were pushed to GitHub's Docker registry (`docker.pkg.github.com`), even if these have now been migrated to the Container registry."},"package_name":{"type":"string","description":"The name of the package."},"org":{"type":"string","description":"The organization name. The name is not case sensitive."},"package_version_id":{"type":"integer","description":"Unique identifier of the package version."}},"required":["package_type","package_name","org","package_version_id"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("DELETE", ("https://api.github.com/orgs/" + String(args["org"]) + "/packages/" + String(args["package_type"]) + "/" + String(args["package_name"]) + "/versions/" + String(args["package_version_id"])) + _qs, {}, undefined);
    }
  },
  {
    name: "packages/restore-package-version-for-org",
    description: "Restore package version for an organization. Restores a specific package version in an organization. Params: package_type (required), package_name (required), org (required), package_version_id (required).",
    inputSchema: {"type":"object","properties":{"package_type":{"type":"string","enum":["npm","maven","rubygems","docker","nuget","container"],"description":"The type of supported package. Packages in GitHub's Gradle registry have the type `maven`. Docker images pushed to GitHub's Container registry (`ghcr.io`) have the type `container`. You can use the type `docker` to find images that were pushed to GitHub's Docker registry (`docker.pkg.github.com`), even if these have now been migrated to the Container registry."},"package_name":{"type":"string","description":"The name of the package."},"org":{"type":"string","description":"The organization name. The name is not case sensitive."},"package_version_id":{"type":"integer","description":"Unique identifier of the package version."}},"required":["package_type","package_name","org","package_version_id"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("POST", ("https://api.github.com/orgs/" + String(args["org"]) + "/packages/" + String(args["package_type"]) + "/" + String(args["package_name"]) + "/versions/" + String(args["package_version_id"]) + "/restore") + _qs, {}, undefined);
    }
  },
  {
    name: "orgs/list-pat-grant-requests",
    description: "List requests to access organization resources with fine-grained personal access tokens. Lists requests from organization members to access organization resources with a fine-grained personal access token. Params: org (required).",
    inputSchema: {"type":"object","properties":{"org":{"type":"string","description":"The organization name. The name is not case sensitive."},"per_page":{"type":"integer","default":30,"description":"The number of results per page (max 100). For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""},"page":{"type":"integer","default":1,"description":"The page number of the results to fetch. For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""},"sort":{"type":"string","enum":["created_at"],"default":"created_at","description":"The property by which to sort the results."},"direction":{"type":"string","enum":["asc","desc"],"default":"desc","description":"The direction to sort the results by."},"owner":{"type":"array","maxItems":10,"items":{"type":"string"},"example":"owner[]=octocat1,owner[]=octocat2","description":"A list of owner usernames to use to filter the results."},"repository":{"type":"string","example":"Hello-World","description":"The name of the repository to use to filter the results."},"permission":{"type":"string","example":"issues_read","description":"The permission to use to filter the results."},"last_used_before":{"type":"string","format":"date-time","description":"Only show fine-grained personal access tokens used before the given time. This is a timestamp in [ISO 8601](https://en.wikipedia.org/wiki/ISO_8601) format: `YYYY-MM-DDTHH:MM:SSZ`."},"last_used_after":{"type":"string","format":"date-time","description":"Only show fine-grained personal access tokens used after the given time. This is a timestamp in [ISO 8601](https://en.wikipedia.org/wiki/ISO_8601) format: `YYYY-MM-DDTHH:MM:SSZ`."},"token_id":{"type":"array","maxItems":50,"items":{"type":"string"},"example":"token_id[]=1,token_id[]=2","description":"The ID of the token"}},"required":["org"]},
    call: function(args) {
    var _q = new URLSearchParams();
    if (args["per_page"] !== undefined) _q.set("per_page", String(args["per_page"]));
    if (args["page"] !== undefined) _q.set("page", String(args["page"]));
    if (args["sort"] !== undefined) _q.set("sort", String(args["sort"]));
    if (args["direction"] !== undefined) _q.set("direction", String(args["direction"]));
    if (args["owner"] !== undefined) _q.set("owner", String(args["owner"]));
    if (args["repository"] !== undefined) _q.set("repository", String(args["repository"]));
    if (args["permission"] !== undefined) _q.set("permission", String(args["permission"]));
    if (args["last_used_before"] !== undefined) _q.set("last_used_before", String(args["last_used_before"]));
    if (args["last_used_after"] !== undefined) _q.set("last_used_after", String(args["last_used_after"]));
    if (args["token_id"] !== undefined) _q.set("token_id", String(args["token_id"]));
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/orgs/" + String(args["org"]) + "/personal-access-token-requests") + _qs, {}, undefined);
    }
  },
  {
    name: "orgs/review-pat-grant-requests-in-bulk",
    description: "Review requests to access organization resources with fine-grained personal access tokens. Approves or denies multiple pending requests to access organization resources via a fine-grained personal access token. Params: org (required).",
    inputSchema: {"type":"object","properties":{"org":{"type":"string","description":"The organization name. The name is not case sensitive."},"body":{"type":"object","description":"Request body"}},"required":["org","body"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("POST", ("https://api.github.com/orgs/" + String(args["org"]) + "/personal-access-token-requests") + _qs, {}, args["body"]);
    }
  },
  {
    name: "orgs/review-pat-grant-request",
    description: "Review a request to access organization resources with a fine-grained personal access token. Approves or denies a pending request to access organization resources via a fine-grained personal access token. Params: org (required), pat_request_id (required).",
    inputSchema: {"type":"object","properties":{"org":{"type":"string","description":"The organization name. The name is not case sensitive."},"pat_request_id":{"type":"integer","description":"Unique identifier of the request for access via fine-grained personal access token."},"body":{"type":"object","description":"Request body"}},"required":["org","pat_request_id","body"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("POST", ("https://api.github.com/orgs/" + String(args["org"]) + "/personal-access-token-requests/" + String(args["pat_request_id"])) + _qs, {}, args["body"]);
    }
  },
  {
    name: "orgs/list-pat-grant-request-repositories",
    description: "List repositories requested to be accessed by a fine-grained personal access token. Lists the repositories a fine-grained personal access token request is requesting access to. Params: org (required), pat_request_id (required).",
    inputSchema: {"type":"object","properties":{"org":{"type":"string","description":"The organization name. The name is not case sensitive."},"pat_request_id":{"type":"integer","description":"Unique identifier of the request for access via fine-grained personal access token."},"per_page":{"type":"integer","default":30,"description":"The number of results per page (max 100). For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""},"page":{"type":"integer","default":1,"description":"The page number of the results to fetch. For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""}},"required":["org","pat_request_id"]},
    call: function(args) {
    var _q = new URLSearchParams();
    if (args["per_page"] !== undefined) _q.set("per_page", String(args["per_page"]));
    if (args["page"] !== undefined) _q.set("page", String(args["page"]));
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/orgs/" + String(args["org"]) + "/personal-access-token-requests/" + String(args["pat_request_id"]) + "/repositories") + _qs, {}, undefined);
    }
  },
  {
    name: "orgs/list-pat-grants",
    description: "List fine-grained personal access tokens with access to organization resources. Lists approved fine-grained personal access tokens owned by organization members that can access organization resources. Params: org (required).",
    inputSchema: {"type":"object","properties":{"org":{"type":"string","description":"The organization name. The name is not case sensitive."},"per_page":{"type":"integer","default":30,"description":"The number of results per page (max 100). For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""},"page":{"type":"integer","default":1,"description":"The page number of the results to fetch. For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""},"sort":{"type":"string","enum":["created_at"],"default":"created_at","description":"The property by which to sort the results."},"direction":{"type":"string","enum":["asc","desc"],"default":"desc","description":"The direction to sort the results by."},"owner":{"type":"array","maxItems":10,"items":{"type":"string"},"example":"owner[]=octocat1,owner[]=octocat2","description":"A list of owner usernames to use to filter the results."},"repository":{"type":"string","example":"Hello-World","description":"The name of the repository to use to filter the results."},"permission":{"type":"string","example":"issues_read","description":"The permission to use to filter the results."},"last_used_before":{"type":"string","format":"date-time","description":"Only show fine-grained personal access tokens used before the given time. This is a timestamp in [ISO 8601](https://en.wikipedia.org/wiki/ISO_8601) format: `YYYY-MM-DDTHH:MM:SSZ`."},"last_used_after":{"type":"string","format":"date-time","description":"Only show fine-grained personal access tokens used after the given time. This is a timestamp in [ISO 8601](https://en.wikipedia.org/wiki/ISO_8601) format: `YYYY-MM-DDTHH:MM:SSZ`."},"token_id":{"type":"array","maxItems":50,"items":{"type":"string"},"example":"token_id[]=1,token_id[]=2","description":"The ID of the token"}},"required":["org"]},
    call: function(args) {
    var _q = new URLSearchParams();
    if (args["per_page"] !== undefined) _q.set("per_page", String(args["per_page"]));
    if (args["page"] !== undefined) _q.set("page", String(args["page"]));
    if (args["sort"] !== undefined) _q.set("sort", String(args["sort"]));
    if (args["direction"] !== undefined) _q.set("direction", String(args["direction"]));
    if (args["owner"] !== undefined) _q.set("owner", String(args["owner"]));
    if (args["repository"] !== undefined) _q.set("repository", String(args["repository"]));
    if (args["permission"] !== undefined) _q.set("permission", String(args["permission"]));
    if (args["last_used_before"] !== undefined) _q.set("last_used_before", String(args["last_used_before"]));
    if (args["last_used_after"] !== undefined) _q.set("last_used_after", String(args["last_used_after"]));
    if (args["token_id"] !== undefined) _q.set("token_id", String(args["token_id"]));
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/orgs/" + String(args["org"]) + "/personal-access-tokens") + _qs, {}, undefined);
    }
  },
  {
    name: "orgs/update-pat-accesses",
    description: "Update the access to organization resources via fine-grained personal access tokens. Updates the access organization members have to organization resources via fine-grained personal access tokens. Params: org (required).",
    inputSchema: {"type":"object","properties":{"org":{"type":"string","description":"The organization name. The name is not case sensitive."},"body":{"type":"object","description":"Request body"}},"required":["org","body"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("POST", ("https://api.github.com/orgs/" + String(args["org"]) + "/personal-access-tokens") + _qs, {}, args["body"]);
    }
  },
  {
    name: "orgs/update-pat-access",
    description: "Update the access a fine-grained personal access token has to organization resources. Updates the access an organization member has to organization resources via a fine-grained personal access token. Params: org (required), pat_id (required).",
    inputSchema: {"type":"object","properties":{"org":{"type":"string","description":"The organization name. The name is not case sensitive."},"pat_id":{"type":"integer","description":"The unique identifier of the fine-grained personal access token."},"body":{"type":"object","description":"Request body"}},"required":["org","pat_id","body"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("POST", ("https://api.github.com/orgs/" + String(args["org"]) + "/personal-access-tokens/" + String(args["pat_id"])) + _qs, {}, args["body"]);
    }
  },
  {
    name: "orgs/list-pat-grant-repositories",
    description: "List repositories a fine-grained personal access token has access to. Lists the repositories a fine-grained personal access token has access to. Params: org (required), pat_id (required).",
    inputSchema: {"type":"object","properties":{"org":{"type":"string","description":"The organization name. The name is not case sensitive."},"pat_id":{"type":"integer","description":"Unique identifier of the fine-grained personal access token."},"per_page":{"type":"integer","default":30,"description":"The number of results per page (max 100). For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""},"page":{"type":"integer","default":1,"description":"The page number of the results to fetch. For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""}},"required":["org","pat_id"]},
    call: function(args) {
    var _q = new URLSearchParams();
    if (args["per_page"] !== undefined) _q.set("per_page", String(args["per_page"]));
    if (args["page"] !== undefined) _q.set("page", String(args["page"]));
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/orgs/" + String(args["org"]) + "/personal-access-tokens/" + String(args["pat_id"]) + "/repositories") + _qs, {}, undefined);
    }
  },
  {
    name: "private-registries/list-org-private-registries",
    description: "List private registries for an organization. Lists all private registry configurations available at the organization-level without revealing their encrypted\nvalues. Params: org (required).",
    inputSchema: {"type":"object","properties":{"org":{"type":"string","description":"The organization name. The name is not case sensitive."},"per_page":{"type":"integer","default":30,"description":"The number of results per page (max 100). For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""},"page":{"type":"integer","default":1,"description":"The page number of the results to fetch. For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""}},"required":["org"]},
    call: function(args) {
    var _q = new URLSearchParams();
    if (args["per_page"] !== undefined) _q.set("per_page", String(args["per_page"]));
    if (args["page"] !== undefined) _q.set("page", String(args["page"]));
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/orgs/" + String(args["org"]) + "/private-registries") + _qs, {}, undefined);
    }
  },
  {
    name: "private-registries/create-org-private-registry",
    description: "Create a private registry for an organization. Creates a private registry configuration with an encrypted value for an organization. Params: org (required).",
    inputSchema: {"type":"object","properties":{"org":{"type":"string","description":"The organization name. The name is not case sensitive."},"body":{"type":"object","description":"Request body"}},"required":["org","body"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("POST", ("https://api.github.com/orgs/" + String(args["org"]) + "/private-registries") + _qs, {}, args["body"]);
    }
  },
  {
    name: "private-registries/get-org-public-key",
    description: "Get private registries public key for an organization. Gets the org public key, which is needed to encrypt private registry secrets. Params: org (required).",
    inputSchema: {"type":"object","properties":{"org":{"type":"string","description":"The organization name. The name is not case sensitive."}},"required":["org"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/orgs/" + String(args["org"]) + "/private-registries/public-key") + _qs, {}, undefined);
    }
  },
  {
    name: "private-registries/get-org-private-registry",
    description: "Get a private registry for an organization. Get the configuration of a single private registry defined for an organization, omitting its encrypted value. Params: org (required), secret_name (required).",
    inputSchema: {"type":"object","properties":{"org":{"type":"string","description":"The organization name. The name is not case sensitive."},"secret_name":{"type":"string","description":"The name of the secret."}},"required":["org","secret_name"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/orgs/" + String(args["org"]) + "/private-registries/" + String(args["secret_name"])) + _qs, {}, undefined);
    }
  },
  {
    name: "private-registries/update-org-private-registry",
    description: "Update a private registry for an organization. Updates a private registry configuration with an encrypted value for an organization. Params: org (required), secret_name (required).",
    inputSchema: {"type":"object","properties":{"org":{"type":"string","description":"The organization name. The name is not case sensitive."},"secret_name":{"type":"string","description":"The name of the secret."},"body":{"type":"object","description":"Request body"}},"required":["org","secret_name","body"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("PATCH", ("https://api.github.com/orgs/" + String(args["org"]) + "/private-registries/" + String(args["secret_name"])) + _qs, {}, args["body"]);
    }
  },
  {
    name: "private-registries/delete-org-private-registry",
    description: "Delete a private registry for an organization. Delete a private registry configuration at the organization-level. Params: org (required), secret_name (required).",
    inputSchema: {"type":"object","properties":{"org":{"type":"string","description":"The organization name. The name is not case sensitive."},"secret_name":{"type":"string","description":"The name of the secret."}},"required":["org","secret_name"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("DELETE", ("https://api.github.com/orgs/" + String(args["org"]) + "/private-registries/" + String(args["secret_name"])) + _qs, {}, undefined);
    }
  },
  {
    name: "projects/list-for-org",
    description: "List projects for organization. List all projects owned by a specific organization accessible by the authenticated user. Params: org (required).",
    inputSchema: {"type":"object","properties":{"org":{"type":"string","description":"The organization name. The name is not case sensitive."},"q":{"type":"string","description":"Limit results to projects of the specified type."},"before":{"type":"string","description":"A cursor, as given in the [Link header](https://docs.github.com/rest/guides/using-pagination-in-the-rest-api#using-link-headers). If specified, the query only searches for results before this cursor. For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""},"after":{"type":"string","description":"A cursor, as given in the [Link header](https://docs.github.com/rest/guides/using-pagination-in-the-rest-api#using-link-headers). If specified, the query only searches for results after this cursor. For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""},"per_page":{"type":"integer","default":30,"description":"The number of results per page (max 100). For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""}},"required":["org"]},
    call: function(args) {
    var _q = new URLSearchParams();
    if (args["q"] !== undefined) _q.set("q", String(args["q"]));
    if (args["before"] !== undefined) _q.set("before", String(args["before"]));
    if (args["after"] !== undefined) _q.set("after", String(args["after"]));
    if (args["per_page"] !== undefined) _q.set("per_page", String(args["per_page"]));
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/orgs/" + String(args["org"]) + "/projectsV2") + _qs, {}, undefined);
    }
  },
  {
    name: "projects/get-for-org",
    description: "Get project for organization. Get a specific organization-owned project. Params: project_number (required), org (required).",
    inputSchema: {"type":"object","properties":{"project_number":{"type":"integer","description":"The project's number."},"org":{"type":"string","description":"The organization name. The name is not case sensitive."}},"required":["project_number","org"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/orgs/" + String(args["org"]) + "/projectsV2/" + String(args["project_number"])) + _qs, {}, undefined);
    }
  },
  {
    name: "projects/create-draft-item-for-org",
    description: "Create draft item for organization owned project. Create draft issue item for the specified organization owned project. Params: org (required), project_number (required).",
    inputSchema: {"type":"object","properties":{"org":{"type":"string","description":"The organization name. The name is not case sensitive."},"project_number":{"type":"integer","description":"The project's number."},"body":{"type":"object","description":"Details of the draft item to create in the project."}},"required":["org","project_number","body"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("POST", ("https://api.github.com/orgs/" + String(args["org"]) + "/projectsV2/" + String(args["project_number"]) + "/drafts") + _qs, {}, args["body"]);
    }
  },
  {
    name: "projects/list-fields-for-org",
    description: "List project fields for organization. List all fields for a specific organization-owned project. Params: project_number (required), org (required).",
    inputSchema: {"type":"object","properties":{"project_number":{"type":"integer","description":"The project's number."},"org":{"type":"string","description":"The organization name. The name is not case sensitive."},"per_page":{"type":"integer","default":30,"description":"The number of results per page (max 100). For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""},"before":{"type":"string","description":"A cursor, as given in the [Link header](https://docs.github.com/rest/guides/using-pagination-in-the-rest-api#using-link-headers). If specified, the query only searches for results before this cursor. For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""},"after":{"type":"string","description":"A cursor, as given in the [Link header](https://docs.github.com/rest/guides/using-pagination-in-the-rest-api#using-link-headers). If specified, the query only searches for results after this cursor. For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""}},"required":["project_number","org"]},
    call: function(args) {
    var _q = new URLSearchParams();
    if (args["per_page"] !== undefined) _q.set("per_page", String(args["per_page"]));
    if (args["before"] !== undefined) _q.set("before", String(args["before"]));
    if (args["after"] !== undefined) _q.set("after", String(args["after"]));
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/orgs/" + String(args["org"]) + "/projectsV2/" + String(args["project_number"]) + "/fields") + _qs, {}, undefined);
    }
  },
  {
    name: "projects/add-field-for-org",
    description: "Add a field to an organization-owned project. Params: project_number (required), org (required).",
    inputSchema: {"type":"object","properties":{"project_number":{"type":"integer","description":"The project's number."},"org":{"type":"string","description":"The organization name. The name is not case sensitive."},"body":{"type":"object","description":"Request body"}},"required":["project_number","org","body"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("POST", ("https://api.github.com/orgs/" + String(args["org"]) + "/projectsV2/" + String(args["project_number"]) + "/fields") + _qs, {}, args["body"]);
    }
  },
  {
    name: "projects/get-field-for-org",
    description: "Get project field for organization. Get a specific field for an organization-owned project. Params: project_number (required), field_id (required), org (required).",
    inputSchema: {"type":"object","properties":{"project_number":{"type":"integer","description":"The project's number."},"field_id":{"type":"integer","description":"The unique identifier of the field."},"org":{"type":"string","description":"The organization name. The name is not case sensitive."}},"required":["project_number","field_id","org"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/orgs/" + String(args["org"]) + "/projectsV2/" + String(args["project_number"]) + "/fields/" + String(args["field_id"])) + _qs, {}, undefined);
    }
  },
  {
    name: "projects/list-items-for-org",
    description: "List items for an organization owned project. List all items for a specific organization-owned project accessible by the authenticated user. Params: project_number (required), org (required).",
    inputSchema: {"type":"object","properties":{"project_number":{"type":"integer","description":"The project's number."},"org":{"type":"string","description":"The organization name. The name is not case sensitive."},"q":{"type":"string","description":"Search query to filter items, see [Filtering projects](https://docs.github.com/issues/planning-and-tracking-with-projects/customizing-views-in-your-project/filtering-projects) for more information."},"fields":{"oneOf":[{"type":"string"},{"type":"array","maxItems":50,"items":{"type":"string"}}],"description":"Limit results to specific fields, by their IDs. If not specified, the title field will be returned.\n\nExample: `fields[]=123&fields[]=456&fields[]=789` or `fields=123,456,789`"},"before":{"type":"string","description":"A cursor, as given in the [Link header](https://docs.github.com/rest/guides/using-pagination-in-the-rest-api#using-link-headers). If specified, the query only searches for results before this cursor. For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""},"after":{"type":"string","description":"A cursor, as given in the [Link header](https://docs.github.com/rest/guides/using-pagination-in-the-rest-api#using-link-headers). If specified, the query only searches for results after this cursor. For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""},"per_page":{"type":"integer","default":30,"description":"The number of results per page (max 100). For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""}},"required":["project_number","org"]},
    call: function(args) {
    var _q = new URLSearchParams();
    if (args["q"] !== undefined) _q.set("q", String(args["q"]));
    if (args["fields"] !== undefined) _q.set("fields", String(args["fields"]));
    if (args["before"] !== undefined) _q.set("before", String(args["before"]));
    if (args["after"] !== undefined) _q.set("after", String(args["after"]));
    if (args["per_page"] !== undefined) _q.set("per_page", String(args["per_page"]));
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/orgs/" + String(args["org"]) + "/projectsV2/" + String(args["project_number"]) + "/items") + _qs, {}, undefined);
    }
  },
  {
    name: "projects/add-item-for-org",
    description: "Add item to organization owned project. Add an issue or pull request item to the specified organization owned project. Params: org (required), project_number (required).",
    inputSchema: {"type":"object","properties":{"org":{"type":"string","description":"The organization name. The name is not case sensitive."},"project_number":{"type":"integer","description":"The project's number."},"body":{"type":"object","description":"Details of the item to add to the project. You can specify either the unique ID or the repository owner, name, and issue/PR number."}},"required":["org","project_number","body"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("POST", ("https://api.github.com/orgs/" + String(args["org"]) + "/projectsV2/" + String(args["project_number"]) + "/items") + _qs, {}, args["body"]);
    }
  },
  {
    name: "projects/get-org-item",
    description: "Get an item for an organization owned project. Get a specific item from an organization-owned project. Params: project_number (required), org (required), item_id (required).",
    inputSchema: {"type":"object","properties":{"project_number":{"type":"integer","description":"The project's number."},"org":{"type":"string","description":"The organization name. The name is not case sensitive."},"item_id":{"type":"integer","description":"The unique identifier of the project item."},"fields":{"oneOf":[{"type":"string"},{"type":"array","maxItems":50,"items":{"type":"string"}}],"description":"Limit results to specific fields, by their IDs. If not specified, the title field will be returned.\n\nExample: fields[]=123&fields[]=456&fields[]=789 or fields=123,456,789"}},"required":["project_number","org","item_id"]},
    call: function(args) {
    var _q = new URLSearchParams();
    if (args["fields"] !== undefined) _q.set("fields", String(args["fields"]));
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/orgs/" + String(args["org"]) + "/projectsV2/" + String(args["project_number"]) + "/items/" + String(args["item_id"])) + _qs, {}, undefined);
    }
  },
  {
    name: "projects/update-item-for-org",
    description: "Update project item for organization. Update a specific item in an organization-owned project. Params: project_number (required), org (required), item_id (required).",
    inputSchema: {"type":"object","properties":{"project_number":{"type":"integer","description":"The project's number."},"org":{"type":"string","description":"The organization name. The name is not case sensitive."},"item_id":{"type":"integer","description":"The unique identifier of the project item."},"body":{"type":"object","description":"Field updates to apply to the project item. Only text, number, date, single select, and iteration fields are supported."}},"required":["project_number","org","item_id","body"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("PATCH", ("https://api.github.com/orgs/" + String(args["org"]) + "/projectsV2/" + String(args["project_number"]) + "/items/" + String(args["item_id"])) + _qs, {}, args["body"]);
    }
  },
  {
    name: "projects/delete-item-for-org",
    description: "Delete project item for organization. Delete a specific item from an organization-owned project. Params: project_number (required), org (required), item_id (required).",
    inputSchema: {"type":"object","properties":{"project_number":{"type":"integer","description":"The project's number."},"org":{"type":"string","description":"The organization name. The name is not case sensitive."},"item_id":{"type":"integer","description":"The unique identifier of the project item."}},"required":["project_number","org","item_id"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("DELETE", ("https://api.github.com/orgs/" + String(args["org"]) + "/projectsV2/" + String(args["project_number"]) + "/items/" + String(args["item_id"])) + _qs, {}, undefined);
    }
  },
  {
    name: "projects/create-view-for-org",
    description: "Create a view for an organization-owned project. Create a new view in an organization-owned project. Params: org (required), project_number (required).",
    inputSchema: {"type":"object","properties":{"org":{"type":"string","description":"The organization name. The name is not case sensitive."},"project_number":{"type":"integer","description":"The project's number."},"body":{"type":"object","description":"Request body"}},"required":["org","project_number","body"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("POST", ("https://api.github.com/orgs/" + String(args["org"]) + "/projectsV2/" + String(args["project_number"]) + "/views") + _qs, {}, args["body"]);
    }
  },
  {
    name: "projects/list-view-items-for-org",
    description: "List items for an organization project view. List items in an organization project with the saved view's filter applied. Params: project_number (required), org (required), view_number (required).",
    inputSchema: {"type":"object","properties":{"project_number":{"type":"integer","description":"The project's number."},"org":{"type":"string","description":"The organization name. The name is not case sensitive."},"view_number":{"type":"integer","description":"The number that identifies the project view."},"fields":{"oneOf":[{"type":"string"},{"type":"array","maxItems":50,"items":{"type":"string"}}],"description":"Limit results to specific fields, by their IDs. If not specified, the\ntitle field will be returned.\n\nExample: `fields[]=123&fields[]=456&fields[]=789` or `fields=123,456,789`"},"before":{"type":"string","description":"A cursor, as given in the [Link header](https://docs.github.com/rest/guides/using-pagination-in-the-rest-api#using-link-headers). If specified, the query only searches for results before this cursor. For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""},"after":{"type":"string","description":"A cursor, as given in the [Link header](https://docs.github.com/rest/guides/using-pagination-in-the-rest-api#using-link-headers). If specified, the query only searches for results after this cursor. For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""},"per_page":{"type":"integer","default":30,"description":"The number of results per page (max 100). For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""}},"required":["project_number","org","view_number"]},
    call: function(args) {
    var _q = new URLSearchParams();
    if (args["fields"] !== undefined) _q.set("fields", String(args["fields"]));
    if (args["before"] !== undefined) _q.set("before", String(args["before"]));
    if (args["after"] !== undefined) _q.set("after", String(args["after"]));
    if (args["per_page"] !== undefined) _q.set("per_page", String(args["per_page"]));
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/orgs/" + String(args["org"]) + "/projectsV2/" + String(args["project_number"]) + "/views/" + String(args["view_number"]) + "/items") + _qs, {}, undefined);
    }
  },
  {
    name: "orgs/custom-properties-for-repos-get-organization-definitions",
    description: "Get all custom properties for an organization. Gets all custom properties defined for an organization. Params: org (required).",
    inputSchema: {"type":"object","properties":{"org":{"type":"string","description":"The organization name. The name is not case sensitive."}},"required":["org"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/orgs/" + String(args["org"]) + "/properties/schema") + _qs, {}, undefined);
    }
  },
  {
    name: "orgs/custom-properties-for-repos-create-or-update-organization-definitions",
    description: "Create or update custom properties for an organization. Creates new or updates existing custom properties defined for an organization in a batch. Params: org (required).",
    inputSchema: {"type":"object","properties":{"org":{"type":"string","description":"The organization name. The name is not case sensitive."},"body":{"type":"object","description":"Request body"}},"required":["org","body"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("PATCH", ("https://api.github.com/orgs/" + String(args["org"]) + "/properties/schema") + _qs, {}, args["body"]);
    }
  },
  {
    name: "orgs/custom-properties-for-repos-get-organization-definition",
    description: "Get a custom property for an organization. Gets a custom property that is defined for an organization. Params: org (required), custom_property_name (required).",
    inputSchema: {"type":"object","properties":{"org":{"type":"string","description":"The organization name. The name is not case sensitive."},"custom_property_name":{"type":"string","description":"The custom property name"}},"required":["org","custom_property_name"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/orgs/" + String(args["org"]) + "/properties/schema/" + String(args["custom_property_name"])) + _qs, {}, undefined);
    }
  },
  {
    name: "orgs/custom-properties-for-repos-create-or-update-organization-definition",
    description: "Create or update a custom property for an organization. Creates a new or updates an existing custom property that is defined for an organization. Params: org (required), custom_property_name (required).",
    inputSchema: {"type":"object","properties":{"org":{"type":"string","description":"The organization name. The name is not case sensitive."},"custom_property_name":{"type":"string","description":"The custom property name"},"body":{"type":"object","description":"Request body"}},"required":["org","custom_property_name","body"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("PUT", ("https://api.github.com/orgs/" + String(args["org"]) + "/properties/schema/" + String(args["custom_property_name"])) + _qs, {}, args["body"]);
    }
  },
  {
    name: "orgs/custom-properties-for-repos-delete-organization-definition",
    description: "Remove a custom property for an organization. Removes a custom property that is defined for an organization. Params: org (required), custom_property_name (required).",
    inputSchema: {"type":"object","properties":{"org":{"type":"string","description":"The organization name. The name is not case sensitive."},"custom_property_name":{"type":"string","description":"The custom property name"}},"required":["org","custom_property_name"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("DELETE", ("https://api.github.com/orgs/" + String(args["org"]) + "/properties/schema/" + String(args["custom_property_name"])) + _qs, {}, undefined);
    }
  },
  {
    name: "orgs/custom-properties-for-repos-get-organization-values",
    description: "List custom property values for organization repositories. Lists organization repositories with all of their custom property values. Params: org (required).",
    inputSchema: {"type":"object","properties":{"org":{"type":"string","description":"The organization name. The name is not case sensitive."},"per_page":{"type":"integer","default":30,"description":"The number of results per page (max 100). For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""},"page":{"type":"integer","default":1,"description":"The page number of the results to fetch. For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""},"repository_query":{"type":"string","description":"Finds repositories in the organization with a query containing one or more search keywords and qualifiers. Qualifiers allow you to limit your search to specific areas of GitHub. The REST API supports the same qualifiers as the web interface for GitHub. To learn more about the format of the query, see [Constructing a search query](https://docs.github.com/rest/search/search#constructing-a-search-query). See \"[Searching for repositories](https://docs.github.com/articles/searching-for-repositories/)\" for a detailed list of qualifiers."}},"required":["org"]},
    call: function(args) {
    var _q = new URLSearchParams();
    if (args["per_page"] !== undefined) _q.set("per_page", String(args["per_page"]));
    if (args["page"] !== undefined) _q.set("page", String(args["page"]));
    if (args["repository_query"] !== undefined) _q.set("repository_query", String(args["repository_query"]));
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/orgs/" + String(args["org"]) + "/properties/values") + _qs, {}, undefined);
    }
  },
  {
    name: "orgs/custom-properties-for-repos-create-or-update-organization-values",
    description: "Create or update custom property values for organization repositories. Create new or update existing custom property values for repositories in a batch that belong to an organization. Params: org (required).",
    inputSchema: {"type":"object","properties":{"org":{"type":"string","description":"The organization name. The name is not case sensitive."},"body":{"type":"object","description":"Request body"}},"required":["org","body"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("PATCH", ("https://api.github.com/orgs/" + String(args["org"]) + "/properties/values") + _qs, {}, args["body"]);
    }
  },
  {
    name: "orgs/list-public-members",
    description: "List public organization members. Members of an organization can choose to have their membership publicized or not. Params: org (required).",
    inputSchema: {"type":"object","properties":{"org":{"type":"string","description":"The organization name. The name is not case sensitive."},"per_page":{"type":"integer","default":30,"description":"The number of results per page (max 100). For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""},"page":{"type":"integer","default":1,"description":"The page number of the results to fetch. For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""}},"required":["org"]},
    call: function(args) {
    var _q = new URLSearchParams();
    if (args["per_page"] !== undefined) _q.set("per_page", String(args["per_page"]));
    if (args["page"] !== undefined) _q.set("page", String(args["page"]));
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/orgs/" + String(args["org"]) + "/public_members") + _qs, {}, undefined);
    }
  },
  {
    name: "orgs/check-public-membership-for-user",
    description: "Check public organization membership for a user. Check if the provided user is a public member of the organization. Params: org (required), username (required).",
    inputSchema: {"type":"object","properties":{"org":{"type":"string","description":"The organization name. The name is not case sensitive."},"username":{"type":"string","description":"The handle for the GitHub user account."}},"required":["org","username"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/orgs/" + String(args["org"]) + "/public_members/" + String(args["username"])) + _qs, {}, undefined);
    }
  },
  {
    name: "orgs/set-public-membership-for-authenticated-user",
    description: "Set public organization membership for the authenticated user. The user can publicize their own membership. Params: org (required), username (required).",
    inputSchema: {"type":"object","properties":{"org":{"type":"string","description":"The organization name. The name is not case sensitive."},"username":{"type":"string","description":"The handle for the GitHub user account."}},"required":["org","username"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("PUT", ("https://api.github.com/orgs/" + String(args["org"]) + "/public_members/" + String(args["username"])) + _qs, {}, undefined);
    }
  },
  {
    name: "orgs/remove-public-membership-for-authenticated-user",
    description: "Remove public organization membership for the authenticated user. Removes the public membership for the authenticated user from the specified organization, unless public visibility is enforced by default. Params: org (required), username (required).",
    inputSchema: {"type":"object","properties":{"org":{"type":"string","description":"The organization name. The name is not case sensitive."},"username":{"type":"string","description":"The handle for the GitHub user account."}},"required":["org","username"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("DELETE", ("https://api.github.com/orgs/" + String(args["org"]) + "/public_members/" + String(args["username"])) + _qs, {}, undefined);
    }
  },
  {
    name: "repos/list-for-org",
    description: "List organization repositories. Lists repositories for the specified organization. Params: org (required).",
    inputSchema: {"type":"object","properties":{"org":{"type":"string","description":"The organization name. The name is not case sensitive."},"type":{"type":"string","enum":["all","public","private","forks","sources","member"],"default":"all","description":"Specifies the types of repositories you want returned."},"sort":{"type":"string","enum":["created","updated","pushed","full_name"],"default":"created","description":"The property to sort the results by."},"direction":{"type":"string","enum":["asc","desc"],"description":"The order to sort by. Default: `asc` when using `full_name`, otherwise `desc`."},"per_page":{"type":"integer","default":30,"description":"The number of results per page (max 100). For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""},"page":{"type":"integer","default":1,"description":"The page number of the results to fetch. For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""}},"required":["org"]},
    call: function(args) {
    var _q = new URLSearchParams();
    if (args["type"] !== undefined) _q.set("type", String(args["type"]));
    if (args["sort"] !== undefined) _q.set("sort", String(args["sort"]));
    if (args["direction"] !== undefined) _q.set("direction", String(args["direction"]));
    if (args["per_page"] !== undefined) _q.set("per_page", String(args["per_page"]));
    if (args["page"] !== undefined) _q.set("page", String(args["page"]));
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/orgs/" + String(args["org"]) + "/repos") + _qs, {}, undefined);
    }
  },
  {
    name: "repos/create-in-org",
    description: "Create an organization repository. Creates a new repository in the specified organization. Params: org (required).",
    inputSchema: {"type":"object","properties":{"org":{"type":"string","description":"The organization name. The name is not case sensitive."},"body":{"type":"object","description":"Request body"}},"required":["org","body"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("POST", ("https://api.github.com/orgs/" + String(args["org"]) + "/repos") + _qs, {}, args["body"]);
    }
  },
  {
    name: "repos/get-org-rulesets",
    description: "Get all organization repository rulesets. Get all the repository rulesets for an organization. Params: org (required).",
    inputSchema: {"type":"object","properties":{"org":{"type":"string","description":"The organization name. The name is not case sensitive."},"per_page":{"type":"integer","default":30,"description":"The number of results per page (max 100). For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""},"page":{"type":"integer","default":1,"description":"The page number of the results to fetch. For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""},"targets":{"type":"string","example":"branch,tag,push","description":"A comma-separated list of rule targets to filter by.\nIf provided, only rulesets that apply to the specified targets will be returned.\nFor example, `branch,tag,push`.\n"}},"required":["org"]},
    call: function(args) {
    var _q = new URLSearchParams();
    if (args["per_page"] !== undefined) _q.set("per_page", String(args["per_page"]));
    if (args["page"] !== undefined) _q.set("page", String(args["page"]));
    if (args["targets"] !== undefined) _q.set("targets", String(args["targets"]));
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/orgs/" + String(args["org"]) + "/rulesets") + _qs, {}, undefined);
    }
  },
  {
    name: "repos/create-org-ruleset",
    description: "Create an organization repository ruleset. Create a repository ruleset for an organization. Params: org (required).",
    inputSchema: {"type":"object","properties":{"org":{"type":"string","description":"The organization name. The name is not case sensitive."},"body":{"type":"object","description":"Request body"}},"required":["org","body"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("POST", ("https://api.github.com/orgs/" + String(args["org"]) + "/rulesets") + _qs, {}, args["body"]);
    }
  },
  {
    name: "repos/get-org-rule-suites",
    description: "List organization rule suites. Lists suites of rule evaluations at the organization level. Params: org (required).",
    inputSchema: {"type":"object","properties":{"org":{"type":"string","description":"The organization name. The name is not case sensitive."},"ref":{"type":"string","description":"The name of the ref. Cannot contain wildcard characters. Optionally prefix with `refs/heads/` to limit to branches or `refs/tags/` to limit to tags. Omit the prefix to search across all refs. When specified, only rule evaluations triggered for this ref will be returned."},"repository_name":{"type":"string","description":"The name of the repository to filter on."},"time_period":{"type":"string","enum":["hour","day","week","month"],"default":"day","description":"The time period to filter by.\n\nFor example, `day` will filter for rule suites that occurred in the past 24 hours, and `week` will filter for rule suites that occurred in the past 7 days (168 hours)."},"actor_name":{"type":"string","description":"The handle for the GitHub user account to filter on. When specified, only rule evaluations triggered by this actor will be returned."},"rule_suite_result":{"type":"string","enum":["pass","fail","bypass","all"],"default":"all","description":"The rule suite results to filter on. When specified, only suites with this result will be returned."},"evaluate_status":{"type":"string","enum":["all","active","evaluate"],"default":"all","description":"The evaluate status to filter on. When specified, only rule suites resulting from rulesets with the specified evaluate status will be returned.\n  - `all` - all rule suites will be returned.\n  - `active` - only rule suites resulting from rulesets in active (non-evaluate) mode will be returned.\n  - `evaluate` - only rule suites resulting from rulesets in evaluate mode will be returned."},"per_page":{"type":"integer","default":30,"description":"The number of results per page (max 100). For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""},"page":{"type":"integer","default":1,"description":"The page number of the results to fetch. For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""}},"required":["org"]},
    call: function(args) {
    var _q = new URLSearchParams();
    if (args["ref"] !== undefined) _q.set("ref", String(args["ref"]));
    if (args["repository_name"] !== undefined) _q.set("repository_name", String(args["repository_name"]));
    if (args["time_period"] !== undefined) _q.set("time_period", String(args["time_period"]));
    if (args["actor_name"] !== undefined) _q.set("actor_name", String(args["actor_name"]));
    if (args["rule_suite_result"] !== undefined) _q.set("rule_suite_result", String(args["rule_suite_result"]));
    if (args["evaluate_status"] !== undefined) _q.set("evaluate_status", String(args["evaluate_status"]));
    if (args["per_page"] !== undefined) _q.set("per_page", String(args["per_page"]));
    if (args["page"] !== undefined) _q.set("page", String(args["page"]));
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/orgs/" + String(args["org"]) + "/rulesets/rule-suites") + _qs, {}, undefined);
    }
  },
  {
    name: "repos/get-org-rule-suite",
    description: "Get an organization rule suite. Gets information about a suite of rule evaluations from within an organization. Params: org (required), rule_suite_id (required).",
    inputSchema: {"type":"object","properties":{"org":{"type":"string","description":"The organization name. The name is not case sensitive."},"rule_suite_id":{"type":"integer","description":"The unique identifier of the rule suite result.\nTo get this ID, you can use [GET /repos/{owner}/{repo}/rulesets/rule-suites](https://docs.github.com/rest/repos/rule-suites#list-repository-rule-suites)\nfor repositories and [GET /orgs/{org}/rulesets/rule-suites](https://docs.github.com/rest/orgs/rule-suites#list-organization-rule-suites)\nfor organizations."}},"required":["org","rule_suite_id"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/orgs/" + String(args["org"]) + "/rulesets/rule-suites/" + String(args["rule_suite_id"])) + _qs, {}, undefined);
    }
  },
  {
    name: "repos/get-org-ruleset",
    description: "Get an organization repository ruleset. Get a repository ruleset for an organization. Params: org (required), ruleset_id (required).",
    inputSchema: {"type":"object","properties":{"org":{"type":"string","description":"The organization name. The name is not case sensitive."},"ruleset_id":{"type":"integer","description":"The ID of the ruleset."}},"required":["org","ruleset_id"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/orgs/" + String(args["org"]) + "/rulesets/" + String(args["ruleset_id"])) + _qs, {}, undefined);
    }
  },
  {
    name: "repos/update-org-ruleset",
    description: "Update an organization repository ruleset. Update a ruleset for an organization. Params: org (required), ruleset_id (required).",
    inputSchema: {"type":"object","properties":{"org":{"type":"string","description":"The organization name. The name is not case sensitive."},"ruleset_id":{"type":"integer","description":"The ID of the ruleset."},"body":{"type":"object","description":"Request body"}},"required":["org","ruleset_id"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("PUT", ("https://api.github.com/orgs/" + String(args["org"]) + "/rulesets/" + String(args["ruleset_id"])) + _qs, {}, args["body"]);
    }
  },
  {
    name: "repos/delete-org-ruleset",
    description: "Delete an organization repository ruleset. Delete a ruleset for an organization. Params: org (required), ruleset_id (required).",
    inputSchema: {"type":"object","properties":{"org":{"type":"string","description":"The organization name. The name is not case sensitive."},"ruleset_id":{"type":"integer","description":"The ID of the ruleset."}},"required":["org","ruleset_id"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("DELETE", ("https://api.github.com/orgs/" + String(args["org"]) + "/rulesets/" + String(args["ruleset_id"])) + _qs, {}, undefined);
    }
  },
  {
    name: "orgs/get-org-ruleset-history",
    description: "Get organization ruleset history. Get the history of an organization ruleset. Params: org (required), ruleset_id (required).",
    inputSchema: {"type":"object","properties":{"org":{"type":"string","description":"The organization name. The name is not case sensitive."},"per_page":{"type":"integer","default":30,"description":"The number of results per page (max 100). For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""},"page":{"type":"integer","default":1,"description":"The page number of the results to fetch. For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""},"ruleset_id":{"type":"integer","description":"The ID of the ruleset."}},"required":["org","ruleset_id"]},
    call: function(args) {
    var _q = new URLSearchParams();
    if (args["per_page"] !== undefined) _q.set("per_page", String(args["per_page"]));
    if (args["page"] !== undefined) _q.set("page", String(args["page"]));
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/orgs/" + String(args["org"]) + "/rulesets/" + String(args["ruleset_id"]) + "/history") + _qs, {}, undefined);
    }
  },
  {
    name: "orgs/get-org-ruleset-version",
    description: "Get organization ruleset version. Get a version of an organization ruleset. Params: org (required), ruleset_id (required), version_id (required).",
    inputSchema: {"type":"object","properties":{"org":{"type":"string","description":"The organization name. The name is not case sensitive."},"ruleset_id":{"type":"integer","description":"The ID of the ruleset."},"version_id":{"type":"integer","description":"The ID of the version"}},"required":["org","ruleset_id","version_id"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/orgs/" + String(args["org"]) + "/rulesets/" + String(args["ruleset_id"]) + "/history/" + String(args["version_id"])) + _qs, {}, undefined);
    }
  },
  {
    name: "secret-scanning/list-alerts-for-org",
    description: "List secret scanning alerts for an organization. Lists secret scanning alerts for eligible repositories in an organization, from newest to oldest. Params: org (required).",
    inputSchema: {"type":"object","properties":{"org":{"type":"string","description":"The organization name. The name is not case sensitive."},"state":{"type":"string","enum":["open","resolved"],"description":"Set to `open` or `resolved` to only list secret scanning alerts in a specific state."},"secret_type":{"type":"string","description":"A comma-separated list of secret types to return. All default secret patterns are returned. To return generic patterns, pass the token name(s) in the parameter. See \"[Supported secret scanning patterns](https://docs.github.com/code-security/secret-scanning/introduction/supported-secret-scanning-patterns#supported-secrets)\" for a complete list of secret types."},"exclude_secret_types":{"type":"string","description":"A comma-separated list of secret types to exclude from the results. All default secret patterns are returned except those matching the specified types. Cannot be combined with the `secret_type` parameter. See \"[Supported secret scanning patterns](https://docs.github.com/code-security/secret-scanning/introduction/supported-secret-scanning-patterns#supported-secrets)\" for a complete list of secret types."},"exclude_providers":{"type":"string","description":"A comma-separated list of provider slugs to exclude from the results.\nProvider slugs use lowercase with underscores (e.g., `github_secret_scanning`, `clojars`).\nYou can find the provider slug in the `provider_slug` field of each alert.\nCannot be combined with the `providers` parameter."},"providers":{"type":"string","description":"A comma-separated list of provider slugs to filter by.\nProvider slugs use lowercase with underscores (e.g., `github_secret_scanning`, `clojars`).\nYou can find the provider slug in the `provider_slug` field of each alert.\nCannot be combined with the `exclude_providers` parameter."},"resolution":{"type":"string","description":"A comma-separated list of resolutions. Only secret scanning alerts with one of these resolutions are listed. Valid resolutions are `false_positive`, `wont_fix`, `revoked`, `pattern_edited`, `pattern_deleted` or `used_in_tests`."},"assignee":{"type":"string","description":"Filters alerts by assignee. Use `*` to get all assigned alerts, `none` to get all unassigned alerts, or a GitHub username to get alerts assigned to a specific user."},"sort":{"type":"string","enum":["created","updated"],"default":"created","description":"The property to sort the results by. `created` means when the alert was created. `updated` means when the alert was updated or resolved."},"direction":{"type":"string","enum":["asc","desc"],"default":"desc","description":"The direction to sort the results by."},"page":{"type":"integer","default":1,"description":"The page number of the results to fetch. For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""},"per_page":{"type":"integer","default":30,"description":"The number of results per page (max 100). For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""},"before":{"type":"string","description":"A cursor, as given in the [Link header](https://docs.github.com/rest/guides/using-pagination-in-the-rest-api#using-link-headers). If specified, the query only searches for events before this cursor. To receive an initial cursor on your first request, include an empty \"before\" query string."},"after":{"type":"string","description":"A cursor, as given in the [Link header](https://docs.github.com/rest/guides/using-pagination-in-the-rest-api#using-link-headers). If specified, the query only searches for events after this cursor.  To receive an initial cursor on your first request, include an empty \"after\" query string."},"validity":{"type":"string","description":"A comma-separated list of validities that, when present, will return alerts that match the validities in this list. Valid options are `active`, `inactive`, and `unknown`."},"is_publicly_leaked":{"type":"boolean","default":false,"description":"A boolean value representing whether or not to filter alerts by the publicly-leaked tag being present."},"is_multi_repo":{"type":"boolean","default":false,"description":"A boolean value representing whether or not to filter alerts by the multi-repo tag being present."},"hide_secret":{"type":"boolean","default":false,"description":"A boolean value representing whether or not to hide literal secrets in the results."},"is_bypassed":{"type":"boolean","description":"A boolean value (`true` or `false`) indicating whether to filter alerts by their push protection bypass status. When set to `true`, only alerts that were created because a push protection rule was bypassed will be returned. When set to `false`, only alerts that were not caused by a push protection bypass will be returned."}},"required":["org"]},
    call: function(args) {
    var _q = new URLSearchParams();
    if (args["state"] !== undefined) _q.set("state", String(args["state"]));
    if (args["secret_type"] !== undefined) _q.set("secret_type", String(args["secret_type"]));
    if (args["exclude_secret_types"] !== undefined) _q.set("exclude_secret_types", String(args["exclude_secret_types"]));
    if (args["exclude_providers"] !== undefined) _q.set("exclude_providers", String(args["exclude_providers"]));
    if (args["providers"] !== undefined) _q.set("providers", String(args["providers"]));
    if (args["resolution"] !== undefined) _q.set("resolution", String(args["resolution"]));
    if (args["assignee"] !== undefined) _q.set("assignee", String(args["assignee"]));
    if (args["sort"] !== undefined) _q.set("sort", String(args["sort"]));
    if (args["direction"] !== undefined) _q.set("direction", String(args["direction"]));
    if (args["page"] !== undefined) _q.set("page", String(args["page"]));
    if (args["per_page"] !== undefined) _q.set("per_page", String(args["per_page"]));
    if (args["before"] !== undefined) _q.set("before", String(args["before"]));
    if (args["after"] !== undefined) _q.set("after", String(args["after"]));
    if (args["validity"] !== undefined) _q.set("validity", String(args["validity"]));
    if (args["is_publicly_leaked"] !== undefined) _q.set("is_publicly_leaked", String(args["is_publicly_leaked"]));
    if (args["is_multi_repo"] !== undefined) _q.set("is_multi_repo", String(args["is_multi_repo"]));
    if (args["hide_secret"] !== undefined) _q.set("hide_secret", String(args["hide_secret"]));
    if (args["is_bypassed"] !== undefined) _q.set("is_bypassed", String(args["is_bypassed"]));
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/orgs/" + String(args["org"]) + "/secret-scanning/alerts") + _qs, {}, undefined);
    }
  },
  {
    name: "secret-scanning/list-org-pattern-configs",
    description: "List organization pattern configurations. Lists the secret scanning pattern configurations for an organization. Params: org (required).",
    inputSchema: {"type":"object","properties":{"org":{"type":"string","description":"The organization name. The name is not case sensitive."}},"required":["org"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/orgs/" + String(args["org"]) + "/secret-scanning/pattern-configurations") + _qs, {}, undefined);
    }
  },
  {
    name: "secret-scanning/update-org-pattern-configs",
    description: "Update organization pattern configurations. Updates the secret scanning pattern configurations for an organization. Params: org (required).",
    inputSchema: {"type":"object","properties":{"org":{"type":"string","description":"The organization name. The name is not case sensitive."},"body":{"type":"object","description":"Request body"}},"required":["org","body"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("PATCH", ("https://api.github.com/orgs/" + String(args["org"]) + "/secret-scanning/pattern-configurations") + _qs, {}, args["body"]);
    }
  },
  {
    name: "security-advisories/list-org-repository-advisories",
    description: "List repository security advisories for an organization. Lists repository security advisories for an organization. Params: org (required).",
    inputSchema: {"type":"object","properties":{"org":{"type":"string","description":"The organization name. The name is not case sensitive."},"direction":{"type":"string","enum":["asc","desc"],"default":"desc","description":"The direction to sort the results by."},"sort":{"type":"string","enum":["created","updated","published"],"default":"created","description":"The property to sort the results by."},"before":{"type":"string","description":"A cursor, as given in the [Link header](https://docs.github.com/rest/guides/using-pagination-in-the-rest-api#using-link-headers). If specified, the query only searches for results before this cursor. For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""},"after":{"type":"string","description":"A cursor, as given in the [Link header](https://docs.github.com/rest/guides/using-pagination-in-the-rest-api#using-link-headers). If specified, the query only searches for results after this cursor. For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""},"per_page":{"type":"integer","minimum":1,"maximum":100,"default":30,"description":"The number of advisories to return per page. For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""},"state":{"type":"string","enum":["triage","draft","published","closed"],"description":"Filter by the state of the repository advisories. Only advisories of this state will be returned."}},"required":["org"]},
    call: function(args) {
    var _q = new URLSearchParams();
    if (args["direction"] !== undefined) _q.set("direction", String(args["direction"]));
    if (args["sort"] !== undefined) _q.set("sort", String(args["sort"]));
    if (args["before"] !== undefined) _q.set("before", String(args["before"]));
    if (args["after"] !== undefined) _q.set("after", String(args["after"]));
    if (args["per_page"] !== undefined) _q.set("per_page", String(args["per_page"]));
    if (args["state"] !== undefined) _q.set("state", String(args["state"]));
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/orgs/" + String(args["org"]) + "/security-advisories") + _qs, {}, undefined);
    }
  },
  {
    name: "orgs/list-security-manager-teams",
    description: "List security manager teams. > [!WARNING]\n> **Closing down notice:** This operation is closing down and will be removed starting January 1, 2026. Params: org (required).",
    inputSchema: {"type":"object","properties":{"org":{"type":"string","description":"The organization name. The name is not case sensitive."}},"required":["org"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/orgs/" + String(args["org"]) + "/security-managers") + _qs, {}, undefined);
    }
  },
  {
    name: "orgs/add-security-manager-team",
    description: "Add a security manager team. > [!WARNING]\n> **Closing down notice:** This operation is closing down and will be removed starting January 1, 2026. Params: org (required), team_slug (required).",
    inputSchema: {"type":"object","properties":{"org":{"type":"string","description":"The organization name. The name is not case sensitive."},"team_slug":{"type":"string","description":"The slug of the team name."}},"required":["org","team_slug"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("PUT", ("https://api.github.com/orgs/" + String(args["org"]) + "/security-managers/teams/" + String(args["team_slug"])) + _qs, {}, undefined);
    }
  },
  {
    name: "orgs/remove-security-manager-team",
    description: "Remove a security manager team. > [!WARNING]\n> **Closing down notice:** This operation is closing down and will be removed starting January 1, 2026. Params: org (required), team_slug (required).",
    inputSchema: {"type":"object","properties":{"org":{"type":"string","description":"The organization name. The name is not case sensitive."},"team_slug":{"type":"string","description":"The slug of the team name."}},"required":["org","team_slug"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("DELETE", ("https://api.github.com/orgs/" + String(args["org"]) + "/security-managers/teams/" + String(args["team_slug"])) + _qs, {}, undefined);
    }
  },
  {
    name: "orgs/get-immutable-releases-settings",
    description: "Get immutable releases settings for an organization. Gets the immutable releases policy for repositories in an organization. Params: org (required).",
    inputSchema: {"type":"object","properties":{"org":{"type":"string","description":"The organization name. The name is not case sensitive."}},"required":["org"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/orgs/" + String(args["org"]) + "/settings/immutable-releases") + _qs, {}, undefined);
    }
  },
  {
    name: "orgs/set-immutable-releases-settings",
    description: "Set immutable releases settings for an organization. Sets the immutable releases policy for repositories in an organization. Params: org (required).",
    inputSchema: {"type":"object","properties":{"org":{"type":"string","description":"The organization name. The name is not case sensitive."},"body":{"type":"object","description":"Request body"}},"required":["org","body"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("PUT", ("https://api.github.com/orgs/" + String(args["org"]) + "/settings/immutable-releases") + _qs, {}, args["body"]);
    }
  },
  {
    name: "orgs/get-immutable-releases-settings-repositories",
    description: "List selected repositories for immutable releases enforcement. List all of the repositories that have been selected for immutable releases enforcement in an organization. Params: org (required).",
    inputSchema: {"type":"object","properties":{"org":{"type":"string","description":"The organization name. The name is not case sensitive."},"page":{"type":"integer","default":1,"description":"The page number of the results to fetch. For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""},"per_page":{"type":"integer","default":30,"description":"The number of results per page (max 100). For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""}},"required":["org"]},
    call: function(args) {
    var _q = new URLSearchParams();
    if (args["page"] !== undefined) _q.set("page", String(args["page"]));
    if (args["per_page"] !== undefined) _q.set("per_page", String(args["per_page"]));
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/orgs/" + String(args["org"]) + "/settings/immutable-releases/repositories") + _qs, {}, undefined);
    }
  },
  {
    name: "orgs/set-immutable-releases-settings-repositories",
    description: "Set selected repositories for immutable releases enforcement. Replaces all repositories that have been selected for immutable releases enforcement in an organization. Params: org (required).",
    inputSchema: {"type":"object","properties":{"org":{"type":"string","description":"The organization name. The name is not case sensitive."},"body":{"type":"object","description":"Request body"}},"required":["org","body"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("PUT", ("https://api.github.com/orgs/" + String(args["org"]) + "/settings/immutable-releases/repositories") + _qs, {}, args["body"]);
    }
  },
  {
    name: "orgs/enable-selected-repository-immutable-releases-organization",
    description: "Enable a selected repository for immutable releases in an organization. Adds a repository to the list of selected repositories that are enforced for immutable releases in an organization. Params: org (required), repository_id (required).",
    inputSchema: {"type":"object","properties":{"org":{"type":"string","description":"The organization name. The name is not case sensitive."},"repository_id":{"type":"integer","description":"The unique identifier of the repository."}},"required":["org","repository_id"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("PUT", ("https://api.github.com/orgs/" + String(args["org"]) + "/settings/immutable-releases/repositories/" + String(args["repository_id"])) + _qs, {}, undefined);
    }
  },
  {
    name: "orgs/disable-selected-repository-immutable-releases-organization",
    description: "Disable a selected repository for immutable releases in an organization. Removes a repository from the list of selected repositories that are enforced for immutable releases in an organization. Params: org (required), repository_id (required).",
    inputSchema: {"type":"object","properties":{"org":{"type":"string","description":"The organization name. The name is not case sensitive."},"repository_id":{"type":"integer","description":"The unique identifier of the repository."}},"required":["org","repository_id"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("DELETE", ("https://api.github.com/orgs/" + String(args["org"]) + "/settings/immutable-releases/repositories/" + String(args["repository_id"])) + _qs, {}, undefined);
    }
  },
  {
    name: "hosted-compute/list-network-configurations-for-org",
    description: "List hosted compute network configurations for an organization. Lists all hosted compute network configurations configured in an organization. Params: org (required).",
    inputSchema: {"type":"object","properties":{"org":{"type":"string","description":"The organization name. The name is not case sensitive."},"per_page":{"type":"integer","default":30,"description":"The number of results per page (max 100). For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""},"page":{"type":"integer","default":1,"description":"The page number of the results to fetch. For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""}},"required":["org"]},
    call: function(args) {
    var _q = new URLSearchParams();
    if (args["per_page"] !== undefined) _q.set("per_page", String(args["per_page"]));
    if (args["page"] !== undefined) _q.set("page", String(args["page"]));
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/orgs/" + String(args["org"]) + "/settings/network-configurations") + _qs, {}, undefined);
    }
  },
  {
    name: "hosted-compute/create-network-configuration-for-org",
    description: "Create a hosted compute network configuration for an organization. Creates a hosted compute network configuration for an organization. Params: org (required).",
    inputSchema: {"type":"object","properties":{"org":{"type":"string","description":"The organization name. The name is not case sensitive."},"body":{"type":"object","description":"Request body"}},"required":["org","body"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("POST", ("https://api.github.com/orgs/" + String(args["org"]) + "/settings/network-configurations") + _qs, {}, args["body"]);
    }
  },
  {
    name: "hosted-compute/get-network-configuration-for-org",
    description: "Get a hosted compute network configuration for an organization. Gets a hosted compute network configuration configured in an organization. Params: org (required), network_configuration_id (required).",
    inputSchema: {"type":"object","properties":{"org":{"type":"string","description":"The organization name. The name is not case sensitive."},"network_configuration_id":{"type":"string","description":"Unique identifier of the hosted compute network configuration."}},"required":["org","network_configuration_id"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/orgs/" + String(args["org"]) + "/settings/network-configurations/" + String(args["network_configuration_id"])) + _qs, {}, undefined);
    }
  },
  {
    name: "hosted-compute/update-network-configuration-for-org",
    description: "Update a hosted compute network configuration for an organization. Updates a hosted compute network configuration for an organization. Params: org (required), network_configuration_id (required).",
    inputSchema: {"type":"object","properties":{"org":{"type":"string","description":"The organization name. The name is not case sensitive."},"network_configuration_id":{"type":"string","description":"Unique identifier of the hosted compute network configuration."},"body":{"type":"object","description":"Request body"}},"required":["org","network_configuration_id","body"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("PATCH", ("https://api.github.com/orgs/" + String(args["org"]) + "/settings/network-configurations/" + String(args["network_configuration_id"])) + _qs, {}, args["body"]);
    }
  },
  {
    name: "hosted-compute/delete-network-configuration-from-org",
    description: "Delete a hosted compute network configuration from an organization. Deletes a hosted compute network configuration from an organization. Params: org (required), network_configuration_id (required).",
    inputSchema: {"type":"object","properties":{"org":{"type":"string","description":"The organization name. The name is not case sensitive."},"network_configuration_id":{"type":"string","description":"Unique identifier of the hosted compute network configuration."}},"required":["org","network_configuration_id"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("DELETE", ("https://api.github.com/orgs/" + String(args["org"]) + "/settings/network-configurations/" + String(args["network_configuration_id"])) + _qs, {}, undefined);
    }
  },
  {
    name: "hosted-compute/get-network-settings-for-org",
    description: "Get a hosted compute network settings resource for an organization. Gets a hosted compute network settings resource configured for an organization. Params: org (required), network_settings_id (required).",
    inputSchema: {"type":"object","properties":{"org":{"type":"string","description":"The organization name. The name is not case sensitive."},"network_settings_id":{"type":"string","description":"Unique identifier of the hosted compute network settings."}},"required":["org","network_settings_id"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/orgs/" + String(args["org"]) + "/settings/network-settings/" + String(args["network_settings_id"])) + _qs, {}, undefined);
    }
  },
  {
    name: "teams/list",
    description: "List teams. Lists all teams in an organization that are visible to the authenticated user. Params: org (required).",
    inputSchema: {"type":"object","properties":{"org":{"type":"string","description":"The organization name. The name is not case sensitive."},"per_page":{"type":"integer","default":30,"description":"The number of results per page (max 100). For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""},"page":{"type":"integer","default":1,"description":"The page number of the results to fetch. For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""},"team_type":{"type":"string","enum":["all","enterprise","organization"],"default":"all","description":"Filter team results by their type. For more information, see \"[What kind of team should I use?](https://docs.github.com/enterprise-cloud@latest/admin/concepts/enterprise-fundamentals/teams-in-an-enterprise#what-kind-of-team-should-i-use)\""}},"required":["org"]},
    call: function(args) {
    var _q = new URLSearchParams();
    if (args["per_page"] !== undefined) _q.set("per_page", String(args["per_page"]));
    if (args["page"] !== undefined) _q.set("page", String(args["page"]));
    if (args["team_type"] !== undefined) _q.set("team_type", String(args["team_type"]));
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/orgs/" + String(args["org"]) + "/teams") + _qs, {}, undefined);
    }
  },
  {
    name: "teams/create",
    description: "Create a team. To create a team, the authenticated user must be a member or owner of `{org}`. Params: org (required).",
    inputSchema: {"type":"object","properties":{"org":{"type":"string","description":"The organization name. The name is not case sensitive."},"body":{"type":"object","description":"Request body"}},"required":["org","body"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("POST", ("https://api.github.com/orgs/" + String(args["org"]) + "/teams") + _qs, {}, args["body"]);
    }
  },
  {
    name: "teams/get-by-name",
    description: "Get a team by name. Gets a team using the team's `slug`. Params: org (required), team_slug (required).",
    inputSchema: {"type":"object","properties":{"org":{"type":"string","description":"The organization name. The name is not case sensitive."},"team_slug":{"type":"string","description":"The slug of the team name."}},"required":["org","team_slug"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/orgs/" + String(args["org"]) + "/teams/" + String(args["team_slug"])) + _qs, {}, undefined);
    }
  },
  {
    name: "teams/update-in-org",
    description: "Update a team. To edit a team, the authenticated user must either be an organization owner or a team maintainer. Params: org (required), team_slug (required).",
    inputSchema: {"type":"object","properties":{"org":{"type":"string","description":"The organization name. The name is not case sensitive."},"team_slug":{"type":"string","description":"The slug of the team name."},"body":{"type":"object","description":"Request body"}},"required":["org","team_slug"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("PATCH", ("https://api.github.com/orgs/" + String(args["org"]) + "/teams/" + String(args["team_slug"])) + _qs, {}, args["body"]);
    }
  },
  {
    name: "teams/delete-in-org",
    description: "Delete a team. To delete a team, the authenticated user must be an organization owner or team maintainer. Params: org (required), team_slug (required).",
    inputSchema: {"type":"object","properties":{"org":{"type":"string","description":"The organization name. The name is not case sensitive."},"team_slug":{"type":"string","description":"The slug of the team name."}},"required":["org","team_slug"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("DELETE", ("https://api.github.com/orgs/" + String(args["org"]) + "/teams/" + String(args["team_slug"])) + _qs, {}, undefined);
    }
  },
  {
    name: "teams/list-pending-invitations-in-org",
    description: "List pending team invitations. The return hash contains a `role` field which refers to the Organization Invitation role and will be one of the following values: `direct_member`, `admin`, `billing_manager`, `hiring_manager`, or `reinstate`. Params: org (required), team_slug (required).",
    inputSchema: {"type":"object","properties":{"org":{"type":"string","description":"The organization name. The name is not case sensitive."},"team_slug":{"type":"string","description":"The slug of the team name."},"per_page":{"type":"integer","default":30,"description":"The number of results per page (max 100). For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""},"page":{"type":"integer","default":1,"description":"The page number of the results to fetch. For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""}},"required":["org","team_slug"]},
    call: function(args) {
    var _q = new URLSearchParams();
    if (args["per_page"] !== undefined) _q.set("per_page", String(args["per_page"]));
    if (args["page"] !== undefined) _q.set("page", String(args["page"]));
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/orgs/" + String(args["org"]) + "/teams/" + String(args["team_slug"]) + "/invitations") + _qs, {}, undefined);
    }
  },
  {
    name: "teams/list-members-in-org",
    description: "List team members. Team members will include the members of child teams. Params: org (required), team_slug (required).",
    inputSchema: {"type":"object","properties":{"org":{"type":"string","description":"The organization name. The name is not case sensitive."},"team_slug":{"type":"string","description":"The slug of the team name."},"role":{"type":"string","enum":["member","maintainer","all"],"default":"all","description":"Filters members returned by their role in the team."},"per_page":{"type":"integer","default":30,"description":"The number of results per page (max 100). For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""},"page":{"type":"integer","default":1,"description":"The page number of the results to fetch. For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""}},"required":["org","team_slug"]},
    call: function(args) {
    var _q = new URLSearchParams();
    if (args["role"] !== undefined) _q.set("role", String(args["role"]));
    if (args["per_page"] !== undefined) _q.set("per_page", String(args["per_page"]));
    if (args["page"] !== undefined) _q.set("page", String(args["page"]));
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/orgs/" + String(args["org"]) + "/teams/" + String(args["team_slug"]) + "/members") + _qs, {}, undefined);
    }
  },
  {
    name: "teams/get-membership-for-user-in-org",
    description: "Get team membership for a user. Team members will include the members of child teams. Params: org (required), team_slug (required), username (required).",
    inputSchema: {"type":"object","properties":{"org":{"type":"string","description":"The organization name. The name is not case sensitive."},"team_slug":{"type":"string","description":"The slug of the team name."},"username":{"type":"string","description":"The handle for the GitHub user account."}},"required":["org","team_slug","username"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/orgs/" + String(args["org"]) + "/teams/" + String(args["team_slug"]) + "/memberships/" + String(args["username"])) + _qs, {}, undefined);
    }
  },
  {
    name: "teams/add-or-update-membership-for-user-in-org",
    description: "Add or update team membership for a user. Adds an organization member to a team. Params: org (required), team_slug (required), username (required).",
    inputSchema: {"type":"object","properties":{"org":{"type":"string","description":"The organization name. The name is not case sensitive."},"team_slug":{"type":"string","description":"The slug of the team name."},"username":{"type":"string","description":"The handle for the GitHub user account."},"body":{"type":"object","description":"Request body"}},"required":["org","team_slug","username"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("PUT", ("https://api.github.com/orgs/" + String(args["org"]) + "/teams/" + String(args["team_slug"]) + "/memberships/" + String(args["username"])) + _qs, {}, args["body"]);
    }
  },
  {
    name: "teams/remove-membership-for-user-in-org",
    description: "Remove team membership for a user. To remove a membership between a user and a team, the authenticated user must have 'admin' permissions to the team or be an owner of the organization that the team is associated with. Params: org (required), team_slug (required), username (required).",
    inputSchema: {"type":"object","properties":{"org":{"type":"string","description":"The organization name. The name is not case sensitive."},"team_slug":{"type":"string","description":"The slug of the team name."},"username":{"type":"string","description":"The handle for the GitHub user account."}},"required":["org","team_slug","username"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("DELETE", ("https://api.github.com/orgs/" + String(args["org"]) + "/teams/" + String(args["team_slug"]) + "/memberships/" + String(args["username"])) + _qs, {}, undefined);
    }
  },
  {
    name: "teams/list-repos-in-org",
    description: "List team repositories. Lists a team's repositories visible to the authenticated user. Params: org (required), team_slug (required).",
    inputSchema: {"type":"object","properties":{"org":{"type":"string","description":"The organization name. The name is not case sensitive."},"team_slug":{"type":"string","description":"The slug of the team name."},"per_page":{"type":"integer","default":30,"description":"The number of results per page (max 100). For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""},"page":{"type":"integer","default":1,"description":"The page number of the results to fetch. For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""}},"required":["org","team_slug"]},
    call: function(args) {
    var _q = new URLSearchParams();
    if (args["per_page"] !== undefined) _q.set("per_page", String(args["per_page"]));
    if (args["page"] !== undefined) _q.set("page", String(args["page"]));
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/orgs/" + String(args["org"]) + "/teams/" + String(args["team_slug"]) + "/repos") + _qs, {}, undefined);
    }
  },
  {
    name: "teams/check-permissions-for-repo-in-org",
    description: "Check team permissions for a repository. Checks whether a team has `admin`, `push`, `maintain`, `triage`, or `pull` permission for a repository. Params: org (required), team_slug (required), owner (required), repo (required).",
    inputSchema: {"type":"object","properties":{"org":{"type":"string","description":"The organization name. The name is not case sensitive."},"team_slug":{"type":"string","description":"The slug of the team name."},"owner":{"type":"string","description":"The account owner of the repository. The name is not case sensitive."},"repo":{"type":"string","description":"The name of the repository without the `.git` extension. The name is not case sensitive."}},"required":["org","team_slug","owner","repo"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/orgs/" + String(args["org"]) + "/teams/" + String(args["team_slug"]) + "/repos/" + String(args["owner"]) + "/" + String(args["repo"])) + _qs, {}, undefined);
    }
  },
  {
    name: "teams/add-or-update-repo-permissions-in-org",
    description: "Add or update team repository permissions. To add a repository to a team or update the team's permission on a repository, the authenticated user must have admin access to the repository, and must be able to see the team. Params: org (required), team_slug (required), owner (required), repo (required).",
    inputSchema: {"type":"object","properties":{"org":{"type":"string","description":"The organization name. The name is not case sensitive."},"team_slug":{"type":"string","description":"The slug of the team name."},"owner":{"type":"string","description":"The account owner of the repository. The name is not case sensitive."},"repo":{"type":"string","description":"The name of the repository without the `.git` extension. The name is not case sensitive."},"body":{"type":"object","description":"Request body"}},"required":["org","team_slug","owner","repo"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("PUT", ("https://api.github.com/orgs/" + String(args["org"]) + "/teams/" + String(args["team_slug"]) + "/repos/" + String(args["owner"]) + "/" + String(args["repo"])) + _qs, {}, args["body"]);
    }
  },
  {
    name: "teams/remove-repo-in-org",
    description: "Remove a repository from a team. If the authenticated user is an organization owner or a team maintainer, they can remove any repositories from the team. Params: org (required), team_slug (required), owner (required), repo (required).",
    inputSchema: {"type":"object","properties":{"org":{"type":"string","description":"The organization name. The name is not case sensitive."},"team_slug":{"type":"string","description":"The slug of the team name."},"owner":{"type":"string","description":"The account owner of the repository. The name is not case sensitive."},"repo":{"type":"string","description":"The name of the repository without the `.git` extension. The name is not case sensitive."}},"required":["org","team_slug","owner","repo"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("DELETE", ("https://api.github.com/orgs/" + String(args["org"]) + "/teams/" + String(args["team_slug"]) + "/repos/" + String(args["owner"]) + "/" + String(args["repo"])) + _qs, {}, undefined);
    }
  },
  {
    name: "teams/list-child-in-org",
    description: "List child teams. Lists the child teams of the team specified by `{team_slug}`. Params: org (required), team_slug (required).",
    inputSchema: {"type":"object","properties":{"org":{"type":"string","description":"The organization name. The name is not case sensitive."},"team_slug":{"type":"string","description":"The slug of the team name."},"per_page":{"type":"integer","default":30,"description":"The number of results per page (max 100). For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""},"page":{"type":"integer","default":1,"description":"The page number of the results to fetch. For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""}},"required":["org","team_slug"]},
    call: function(args) {
    var _q = new URLSearchParams();
    if (args["per_page"] !== undefined) _q.set("per_page", String(args["per_page"]));
    if (args["page"] !== undefined) _q.set("page", String(args["page"]));
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/orgs/" + String(args["org"]) + "/teams/" + String(args["team_slug"]) + "/teams") + _qs, {}, undefined);
    }
  },
  {
    name: "orgs/enable-or-disable-security-product-on-all-org-repos",
    description: "Enable or disable a security feature for an organization. > [!WARNING]\n> **Closing down notice:** The ability to enable or disable a security feature for all eligible repositories in an organization is closing down. Params: org (required), security_product (required), enablement (required).",
    inputSchema: {"type":"object","properties":{"org":{"type":"string","description":"The organization name. The name is not case sensitive."},"security_product":{"type":"string","enum":["dependency_graph","dependabot_alerts","dependabot_security_updates","advanced_security","code_scanning_default_setup","secret_scanning","secret_scanning_push_protection"],"description":"The security feature to enable or disable."},"enablement":{"type":"string","enum":["enable_all","disable_all"],"description":"The action to take.\n\n`enable_all` means to enable the specified security feature for all repositories in the organization.\n`disable_all` means to disable the specified security feature for all repositories in the organization."},"body":{"type":"object","description":"Request body"}},"required":["org","security_product","enablement"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("POST", ("https://api.github.com/orgs/" + String(args["org"]) + "/" + String(args["security_product"]) + "/" + String(args["enablement"])) + _qs, {}, args["body"]);
    }
  },
  {
    name: "rate-limit/get",
    description: "Get rate limit status for the authenticated user. > [!NOTE]\n> Accessing this endpoint does not count against your REST API rate limit.",
    inputSchema: {"type":"object","properties":{},"required":[]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/rate_limit") + _qs, {}, undefined);
    }
  },
  {
    name: "repos/get",
    description: "Get a repository. The `parent` and `source` objects are present when the repository is a fork. Params: owner (required), repo (required).",
    inputSchema: {"type":"object","properties":{"owner":{"type":"string","description":"The account owner of the repository. The name is not case sensitive."},"repo":{"type":"string","description":"The name of the repository without the `.git` extension. The name is not case sensitive."}},"required":["owner","repo"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/repos/" + String(args["owner"]) + "/" + String(args["repo"])) + _qs, {}, undefined);
    }
  },
  {
    name: "repos/update",
    description: "Update a repository. **Note**: To edit a repository's topics, use the [Replace all repository topics](https://docs.github.com/rest/repos/repos#replace-all-repository-topics) endpoint. Params: owner (required), repo (required).",
    inputSchema: {"type":"object","properties":{"owner":{"type":"string","description":"The account owner of the repository. The name is not case sensitive."},"repo":{"type":"string","description":"The name of the repository without the `.git` extension. The name is not case sensitive."},"body":{"type":"object","description":"Request body"}},"required":["owner","repo"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("PATCH", ("https://api.github.com/repos/" + String(args["owner"]) + "/" + String(args["repo"])) + _qs, {}, args["body"]);
    }
  },
  {
    name: "repos/delete",
    description: "Delete a repository. Deleting a repository requires admin access. Params: owner (required), repo (required).",
    inputSchema: {"type":"object","properties":{"owner":{"type":"string","description":"The account owner of the repository. The name is not case sensitive."},"repo":{"type":"string","description":"The name of the repository without the `.git` extension. The name is not case sensitive."}},"required":["owner","repo"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("DELETE", ("https://api.github.com/repos/" + String(args["owner"]) + "/" + String(args["repo"])) + _qs, {}, undefined);
    }
  },
  {
    name: "actions/list-artifacts-for-repo",
    description: "List artifacts for a repository. Lists all artifacts for a repository. Params: owner (required), repo (required).",
    inputSchema: {"type":"object","properties":{"owner":{"type":"string","description":"The account owner of the repository. The name is not case sensitive."},"repo":{"type":"string","description":"The name of the repository without the `.git` extension. The name is not case sensitive."},"per_page":{"type":"integer","default":30,"description":"The number of results per page (max 100). For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""},"page":{"type":"integer","default":1,"description":"The page number of the results to fetch. For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""},"name":{"type":"string","description":"The name field of an artifact. When specified, only artifacts with this name will be returned."}},"required":["owner","repo"]},
    call: function(args) {
    var _q = new URLSearchParams();
    if (args["per_page"] !== undefined) _q.set("per_page", String(args["per_page"]));
    if (args["page"] !== undefined) _q.set("page", String(args["page"]));
    if (args["name"] !== undefined) _q.set("name", String(args["name"]));
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/repos/" + String(args["owner"]) + "/" + String(args["repo"]) + "/actions/artifacts") + _qs, {}, undefined);
    }
  },
  {
    name: "actions/get-artifact",
    description: "Get an artifact. Gets a specific artifact for a workflow run. Params: owner (required), repo (required), artifact_id (required).",
    inputSchema: {"type":"object","properties":{"owner":{"type":"string","description":"The account owner of the repository. The name is not case sensitive."},"repo":{"type":"string","description":"The name of the repository without the `.git` extension. The name is not case sensitive."},"artifact_id":{"type":"integer","description":"The unique identifier of the artifact."}},"required":["owner","repo","artifact_id"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/repos/" + String(args["owner"]) + "/" + String(args["repo"]) + "/actions/artifacts/" + String(args["artifact_id"])) + _qs, {}, undefined);
    }
  },
  {
    name: "actions/delete-artifact",
    description: "Delete an artifact. Deletes an artifact for a workflow run. Params: owner (required), repo (required), artifact_id (required).",
    inputSchema: {"type":"object","properties":{"owner":{"type":"string","description":"The account owner of the repository. The name is not case sensitive."},"repo":{"type":"string","description":"The name of the repository without the `.git` extension. The name is not case sensitive."},"artifact_id":{"type":"integer","description":"The unique identifier of the artifact."}},"required":["owner","repo","artifact_id"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("DELETE", ("https://api.github.com/repos/" + String(args["owner"]) + "/" + String(args["repo"]) + "/actions/artifacts/" + String(args["artifact_id"])) + _qs, {}, undefined);
    }
  },
  {
    name: "actions/download-artifact",
    description: "Download an artifact. Gets a redirect URL to download an archive for a repository. Params: owner (required), repo (required), artifact_id (required), archive_format (required).",
    inputSchema: {"type":"object","properties":{"owner":{"type":"string","description":"The account owner of the repository. The name is not case sensitive."},"repo":{"type":"string","description":"The name of the repository without the `.git` extension. The name is not case sensitive."},"artifact_id":{"type":"integer","description":"The unique identifier of the artifact."},"archive_format":{"type":"string"}},"required":["owner","repo","artifact_id","archive_format"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/repos/" + String(args["owner"]) + "/" + String(args["repo"]) + "/actions/artifacts/" + String(args["artifact_id"]) + "/" + String(args["archive_format"])) + _qs, {}, undefined);
    }
  },
  {
    name: "actions/get-actions-cache-retention-limit-for-repository",
    description: "Get GitHub Actions cache retention limit for a repository. Gets GitHub Actions cache retention limit for a repository. Params: owner (required), repo (required).",
    inputSchema: {"type":"object","properties":{"owner":{"type":"string","description":"The account owner of the repository. The name is not case sensitive."},"repo":{"type":"string","description":"The name of the repository without the `.git` extension. The name is not case sensitive."}},"required":["owner","repo"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/repos/" + String(args["owner"]) + "/" + String(args["repo"]) + "/actions/cache/retention-limit") + _qs, {}, undefined);
    }
  },
  {
    name: "actions/set-actions-cache-retention-limit-for-repository",
    description: "Set GitHub Actions cache retention limit for a repository. Sets GitHub Actions cache retention limit for a repository. Params: owner (required), repo (required).",
    inputSchema: {"type":"object","properties":{"owner":{"type":"string","description":"The account owner of the repository. The name is not case sensitive."},"repo":{"type":"string","description":"The name of the repository without the `.git` extension. The name is not case sensitive."},"body":{"type":"object","description":"Request body"}},"required":["owner","repo","body"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("PUT", ("https://api.github.com/repos/" + String(args["owner"]) + "/" + String(args["repo"]) + "/actions/cache/retention-limit") + _qs, {}, args["body"]);
    }
  },
  {
    name: "actions/get-actions-cache-storage-limit-for-repository",
    description: "Get GitHub Actions cache storage limit for a repository. Gets GitHub Actions cache storage limit for a repository. Params: owner (required), repo (required).",
    inputSchema: {"type":"object","properties":{"owner":{"type":"string","description":"The account owner of the repository. The name is not case sensitive."},"repo":{"type":"string","description":"The name of the repository without the `.git` extension. The name is not case sensitive."}},"required":["owner","repo"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/repos/" + String(args["owner"]) + "/" + String(args["repo"]) + "/actions/cache/storage-limit") + _qs, {}, undefined);
    }
  },
  {
    name: "actions/set-actions-cache-storage-limit-for-repository",
    description: "Set GitHub Actions cache storage limit for a repository. Sets GitHub Actions cache storage limit for a repository. Params: owner (required), repo (required).",
    inputSchema: {"type":"object","properties":{"owner":{"type":"string","description":"The account owner of the repository. The name is not case sensitive."},"repo":{"type":"string","description":"The name of the repository without the `.git` extension. The name is not case sensitive."},"body":{"type":"object","description":"Request body"}},"required":["owner","repo","body"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("PUT", ("https://api.github.com/repos/" + String(args["owner"]) + "/" + String(args["repo"]) + "/actions/cache/storage-limit") + _qs, {}, args["body"]);
    }
  },
  {
    name: "actions/get-actions-cache-usage",
    description: "Get GitHub Actions cache usage for a repository. Gets GitHub Actions cache usage for a repository. Params: owner (required), repo (required).",
    inputSchema: {"type":"object","properties":{"owner":{"type":"string","description":"The account owner of the repository. The name is not case sensitive."},"repo":{"type":"string","description":"The name of the repository without the `.git` extension. The name is not case sensitive."}},"required":["owner","repo"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/repos/" + String(args["owner"]) + "/" + String(args["repo"]) + "/actions/cache/usage") + _qs, {}, undefined);
    }
  },
  {
    name: "actions/get-actions-cache-list",
    description: "List GitHub Actions caches for a repository. Lists the GitHub Actions caches for a repository. Params: owner (required), repo (required).",
    inputSchema: {"type":"object","properties":{"owner":{"type":"string","description":"The account owner of the repository. The name is not case sensitive."},"repo":{"type":"string","description":"The name of the repository without the `.git` extension. The name is not case sensitive."},"per_page":{"type":"integer","default":30,"description":"The number of results per page (max 100). For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""},"page":{"type":"integer","default":1,"description":"The page number of the results to fetch. For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""},"ref":{"type":"string","description":"The full Git reference for narrowing down the cache. The `ref` for a branch should be formatted as `refs/heads/<branch name>`. To reference a pull request use `refs/pull/<number>/merge`."},"key":{"type":"string","description":"An explicit key or prefix for identifying the cache"},"sort":{"type":"string","enum":["created_at","last_accessed_at","size_in_bytes"],"default":"last_accessed_at","description":"The property to sort the results by. `created_at` means when the cache was created. `last_accessed_at` means when the cache was last accessed. `size_in_bytes` is the size of the cache in bytes."},"direction":{"type":"string","enum":["asc","desc"],"default":"desc","description":"The direction to sort the results by."}},"required":["owner","repo"]},
    call: function(args) {
    var _q = new URLSearchParams();
    if (args["per_page"] !== undefined) _q.set("per_page", String(args["per_page"]));
    if (args["page"] !== undefined) _q.set("page", String(args["page"]));
    if (args["ref"] !== undefined) _q.set("ref", String(args["ref"]));
    if (args["key"] !== undefined) _q.set("key", String(args["key"]));
    if (args["sort"] !== undefined) _q.set("sort", String(args["sort"]));
    if (args["direction"] !== undefined) _q.set("direction", String(args["direction"]));
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/repos/" + String(args["owner"]) + "/" + String(args["repo"]) + "/actions/caches") + _qs, {}, undefined);
    }
  },
  {
    name: "actions/delete-actions-cache-by-key",
    description: "Delete GitHub Actions caches for a repository (using a cache key). Deletes one or more GitHub Actions caches for a repository, using a complete cache key. Params: owner (required), repo (required), key (required).",
    inputSchema: {"type":"object","properties":{"owner":{"type":"string","description":"The account owner of the repository. The name is not case sensitive."},"repo":{"type":"string","description":"The name of the repository without the `.git` extension. The name is not case sensitive."},"key":{"type":"string","description":"A key for identifying the cache."},"ref":{"type":"string","description":"The full Git reference for narrowing down the cache. The `ref` for a branch should be formatted as `refs/heads/<branch name>`. To reference a pull request use `refs/pull/<number>/merge`."}},"required":["owner","repo","key"]},
    call: function(args) {
    var _q = new URLSearchParams();
    if (args["key"] !== undefined) _q.set("key", String(args["key"]));
    if (args["ref"] !== undefined) _q.set("ref", String(args["ref"]));
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("DELETE", ("https://api.github.com/repos/" + String(args["owner"]) + "/" + String(args["repo"]) + "/actions/caches") + _qs, {}, undefined);
    }
  },
  {
    name: "actions/delete-actions-cache-by-id",
    description: "Delete a GitHub Actions cache for a repository (using a cache ID). Deletes a GitHub Actions cache for a repository, using a cache ID. Params: owner (required), repo (required), cache_id (required).",
    inputSchema: {"type":"object","properties":{"owner":{"type":"string","description":"The account owner of the repository. The name is not case sensitive."},"repo":{"type":"string","description":"The name of the repository without the `.git` extension. The name is not case sensitive."},"cache_id":{"type":"integer","description":"The unique identifier of the GitHub Actions cache."}},"required":["owner","repo","cache_id"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("DELETE", ("https://api.github.com/repos/" + String(args["owner"]) + "/" + String(args["repo"]) + "/actions/caches/" + String(args["cache_id"])) + _qs, {}, undefined);
    }
  },
  {
    name: "actions/list-concurrency-groups-for-repository",
    description: "List concurrency groups for a repository. Lists the active concurrency groups for a repository. Params: owner (required), repo (required).",
    inputSchema: {"type":"object","properties":{"owner":{"type":"string","description":"The account owner of the repository. The name is not case sensitive."},"repo":{"type":"string","description":"The name of the repository without the `.git` extension. The name is not case sensitive."},"per_page":{"type":"integer","default":30,"description":"The number of results per page (max 100). For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""},"after":{"type":"string","description":"A cursor, as given in the [Link header](https://docs.github.com/rest/guides/using-pagination-in-the-rest-api#using-link-headers). If specified, the query only searches for results after this cursor. For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""}},"required":["owner","repo"]},
    call: function(args) {
    var _q = new URLSearchParams();
    if (args["per_page"] !== undefined) _q.set("per_page", String(args["per_page"]));
    if (args["after"] !== undefined) _q.set("after", String(args["after"]));
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/repos/" + String(args["owner"]) + "/" + String(args["repo"]) + "/actions/concurrency_groups") + _qs, {}, undefined);
    }
  },
  {
    name: "actions/get-concurrency-group-for-repository",
    description: "Get a concurrency group for a repository. Gets a specific concurrency group for a repository, including all instances in the group's queue. Params: owner (required), repo (required), concurrency_group_name (required).",
    inputSchema: {"type":"object","properties":{"owner":{"type":"string","description":"The account owner of the repository. The name is not case sensitive."},"repo":{"type":"string","description":"The name of the repository without the `.git` extension. The name is not case sensitive."},"concurrency_group_name":{"type":"string","description":"The name of the concurrency group."},"ahead_of_run":{"type":"integer","minimum":1,"description":"Filter to items ahead of this workflow run ID in the queue, plus the run itself.\nMatches workflow-level concurrency and reusable-workflow leases held on behalf of\nthe run. Mutually exclusive with `ahead_of_job`."},"ahead_of_job":{"type":"integer","minimum":1,"description":"Filter to items ahead of this job ID in the queue, plus the job itself.\nMatches job-level concurrency and reusable-workflow leases on the job's\nancestor paths. Mutually exclusive with `ahead_of_run`."}},"required":["owner","repo","concurrency_group_name"]},
    call: function(args) {
    var _q = new URLSearchParams();
    if (args["ahead_of_run"] !== undefined) _q.set("ahead_of_run", String(args["ahead_of_run"]));
    if (args["ahead_of_job"] !== undefined) _q.set("ahead_of_job", String(args["ahead_of_job"]));
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/repos/" + String(args["owner"]) + "/" + String(args["repo"]) + "/actions/concurrency_groups/" + String(args["concurrency_group_name"])) + _qs, {}, undefined);
    }
  },
  {
    name: "actions/get-job-for-workflow-run",
    description: "Get a job for a workflow run. Gets a specific job in a workflow run. Params: owner (required), repo (required), job_id (required).",
    inputSchema: {"type":"object","properties":{"owner":{"type":"string","description":"The account owner of the repository. The name is not case sensitive."},"repo":{"type":"string","description":"The name of the repository without the `.git` extension. The name is not case sensitive."},"job_id":{"type":"integer","description":"The unique identifier of the job."}},"required":["owner","repo","job_id"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/repos/" + String(args["owner"]) + "/" + String(args["repo"]) + "/actions/jobs/" + String(args["job_id"])) + _qs, {}, undefined);
    }
  },
  {
    name: "actions/download-job-logs-for-workflow-run",
    description: "Download job logs for a workflow run. Gets a redirect URL to download a plain text file of logs for a workflow job. Params: owner (required), repo (required), job_id (required).",
    inputSchema: {"type":"object","properties":{"owner":{"type":"string","description":"The account owner of the repository. The name is not case sensitive."},"repo":{"type":"string","description":"The name of the repository without the `.git` extension. The name is not case sensitive."},"job_id":{"type":"integer","description":"The unique identifier of the job."}},"required":["owner","repo","job_id"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/repos/" + String(args["owner"]) + "/" + String(args["repo"]) + "/actions/jobs/" + String(args["job_id"]) + "/logs") + _qs, {}, undefined);
    }
  },
  {
    name: "actions/re-run-job-for-workflow-run",
    description: "Re-run a job from a workflow run. Re-run a job and its dependent jobs in a workflow run. Params: owner (required), repo (required), job_id (required).",
    inputSchema: {"type":"object","properties":{"owner":{"type":"string","description":"The account owner of the repository. The name is not case sensitive."},"repo":{"type":"string","description":"The name of the repository without the `.git` extension. The name is not case sensitive."},"job_id":{"type":"integer","description":"The unique identifier of the job."},"body":{"type":"object","description":"Request body"}},"required":["owner","repo","job_id"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("POST", ("https://api.github.com/repos/" + String(args["owner"]) + "/" + String(args["repo"]) + "/actions/jobs/" + String(args["job_id"]) + "/rerun") + _qs, {}, args["body"]);
    }
  },
  {
    name: "actions/get-custom-oidc-sub-claim-for-repo",
    description: "Get the customization template for an OIDC subject claim for a repository. Gets the customization template for an OpenID Connect (OIDC) subject claim. Params: owner (required), repo (required).",
    inputSchema: {"type":"object","properties":{"owner":{"type":"string","description":"The account owner of the repository. The name is not case sensitive."},"repo":{"type":"string","description":"The name of the repository without the `.git` extension. The name is not case sensitive."}},"required":["owner","repo"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/repos/" + String(args["owner"]) + "/" + String(args["repo"]) + "/actions/oidc/customization/sub") + _qs, {}, undefined);
    }
  },
  {
    name: "actions/set-custom-oidc-sub-claim-for-repo",
    description: "Set the customization template for an OIDC subject claim for a repository. Sets the customization template and `opt-in` or `opt-out` flag for an OpenID Connect (OIDC) subject claim for a repository. Params: owner (required), repo (required).",
    inputSchema: {"type":"object","properties":{"owner":{"type":"string","description":"The account owner of the repository. The name is not case sensitive."},"repo":{"type":"string","description":"The name of the repository without the `.git` extension. The name is not case sensitive."},"body":{"type":"object","description":"Request body"}},"required":["owner","repo","body"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("PUT", ("https://api.github.com/repos/" + String(args["owner"]) + "/" + String(args["repo"]) + "/actions/oidc/customization/sub") + _qs, {}, args["body"]);
    }
  },
  {
    name: "actions/list-repo-organization-secrets",
    description: "List repository organization secrets. Lists all organization secrets shared with a repository without revealing their encrypted\nvalues. Params: owner (required), repo (required).",
    inputSchema: {"type":"object","properties":{"owner":{"type":"string","description":"The account owner of the repository. The name is not case sensitive."},"repo":{"type":"string","description":"The name of the repository without the `.git` extension. The name is not case sensitive."},"per_page":{"type":"integer","default":30,"description":"The number of results per page (max 100). For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""},"page":{"type":"integer","default":1,"description":"The page number of the results to fetch. For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""}},"required":["owner","repo"]},
    call: function(args) {
    var _q = new URLSearchParams();
    if (args["per_page"] !== undefined) _q.set("per_page", String(args["per_page"]));
    if (args["page"] !== undefined) _q.set("page", String(args["page"]));
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/repos/" + String(args["owner"]) + "/" + String(args["repo"]) + "/actions/organization-secrets") + _qs, {}, undefined);
    }
  },
  {
    name: "actions/list-repo-organization-variables",
    description: "List repository organization variables. Lists all organization variables shared with a repository. Params: owner (required), repo (required).",
    inputSchema: {"type":"object","properties":{"owner":{"type":"string","description":"The account owner of the repository. The name is not case sensitive."},"repo":{"type":"string","description":"The name of the repository without the `.git` extension. The name is not case sensitive."},"per_page":{"type":"integer","default":10,"description":"The number of results per page (max 30). For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""},"page":{"type":"integer","default":1,"description":"The page number of the results to fetch. For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""}},"required":["owner","repo"]},
    call: function(args) {
    var _q = new URLSearchParams();
    if (args["per_page"] !== undefined) _q.set("per_page", String(args["per_page"]));
    if (args["page"] !== undefined) _q.set("page", String(args["page"]));
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/repos/" + String(args["owner"]) + "/" + String(args["repo"]) + "/actions/organization-variables") + _qs, {}, undefined);
    }
  },
  {
    name: "actions/get-github-actions-permissions-repository",
    description: "Get GitHub Actions permissions for a repository. Gets the GitHub Actions permissions policy for a repository, including whether GitHub Actions is enabled and the actions and reusable workflows allowed to run in the repository. Params: owner (required), repo (required).",
    inputSchema: {"type":"object","properties":{"owner":{"type":"string","description":"The account owner of the repository. The name is not case sensitive."},"repo":{"type":"string","description":"The name of the repository without the `.git` extension. The name is not case sensitive."}},"required":["owner","repo"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/repos/" + String(args["owner"]) + "/" + String(args["repo"]) + "/actions/permissions") + _qs, {}, undefined);
    }
  },
  {
    name: "actions/set-github-actions-permissions-repository",
    description: "Set GitHub Actions permissions for a repository. Sets the GitHub Actions permissions policy for enabling GitHub Actions and allowed actions and reusable workflows in the repository. Params: owner (required), repo (required).",
    inputSchema: {"type":"object","properties":{"owner":{"type":"string","description":"The account owner of the repository. The name is not case sensitive."},"repo":{"type":"string","description":"The name of the repository without the `.git` extension. The name is not case sensitive."},"body":{"type":"object","description":"Request body"}},"required":["owner","repo","body"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("PUT", ("https://api.github.com/repos/" + String(args["owner"]) + "/" + String(args["repo"]) + "/actions/permissions") + _qs, {}, args["body"]);
    }
  },
  {
    name: "actions/get-workflow-access-to-repository",
    description: "Get the level of access for workflows outside of the repository. Gets the level of access that workflows outside of the repository have to actions and reusable workflows in the repository. Params: owner (required), repo (required).",
    inputSchema: {"type":"object","properties":{"owner":{"type":"string","description":"The account owner of the repository. The name is not case sensitive."},"repo":{"type":"string","description":"The name of the repository without the `.git` extension. The name is not case sensitive."}},"required":["owner","repo"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/repos/" + String(args["owner"]) + "/" + String(args["repo"]) + "/actions/permissions/access") + _qs, {}, undefined);
    }
  },
  {
    name: "actions/set-workflow-access-to-repository",
    description: "Set the level of access for workflows outside of the repository. Sets the level of access that workflows outside of the repository have to actions and reusable workflows in the repository. Params: owner (required), repo (required).",
    inputSchema: {"type":"object","properties":{"owner":{"type":"string","description":"The account owner of the repository. The name is not case sensitive."},"repo":{"type":"string","description":"The name of the repository without the `.git` extension. The name is not case sensitive."},"body":{"type":"object","description":"Request body"}},"required":["owner","repo","body"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("PUT", ("https://api.github.com/repos/" + String(args["owner"]) + "/" + String(args["repo"]) + "/actions/permissions/access") + _qs, {}, args["body"]);
    }
  },
  {
    name: "actions/get-artifact-and-log-retention-settings-repository",
    description: "Get artifact and log retention settings for a repository. Gets artifact and log retention settings for a repository. Params: owner (required), repo (required).",
    inputSchema: {"type":"object","properties":{"owner":{"type":"string","description":"The account owner of the repository. The name is not case sensitive."},"repo":{"type":"string","description":"The name of the repository without the `.git` extension. The name is not case sensitive."}},"required":["owner","repo"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/repos/" + String(args["owner"]) + "/" + String(args["repo"]) + "/actions/permissions/artifact-and-log-retention") + _qs, {}, undefined);
    }
  },
  {
    name: "actions/set-artifact-and-log-retention-settings-repository",
    description: "Set artifact and log retention settings for a repository. Sets artifact and log retention settings for a repository. Params: owner (required), repo (required).",
    inputSchema: {"type":"object","properties":{"owner":{"type":"string","description":"The account owner of the repository. The name is not case sensitive."},"repo":{"type":"string","description":"The name of the repository without the `.git` extension. The name is not case sensitive."},"body":{"type":"object","description":"Request body"}},"required":["owner","repo","body"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("PUT", ("https://api.github.com/repos/" + String(args["owner"]) + "/" + String(args["repo"]) + "/actions/permissions/artifact-and-log-retention") + _qs, {}, args["body"]);
    }
  },
  {
    name: "actions/get-fork-pr-contributor-approval-permissions-repository",
    description: "Get fork PR contributor approval permissions for a repository. Gets the fork PR contributor approval policy for a repository. Params: owner (required), repo (required).",
    inputSchema: {"type":"object","properties":{"owner":{"type":"string","description":"The account owner of the repository. The name is not case sensitive."},"repo":{"type":"string","description":"The name of the repository without the `.git` extension. The name is not case sensitive."}},"required":["owner","repo"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/repos/" + String(args["owner"]) + "/" + String(args["repo"]) + "/actions/permissions/fork-pr-contributor-approval") + _qs, {}, undefined);
    }
  },
  {
    name: "actions/set-fork-pr-contributor-approval-permissions-repository",
    description: "Set fork PR contributor approval permissions for a repository. Sets the fork PR contributor approval policy for a repository. Params: owner (required), repo (required).",
    inputSchema: {"type":"object","properties":{"owner":{"type":"string","description":"The account owner of the repository. The name is not case sensitive."},"repo":{"type":"string","description":"The name of the repository without the `.git` extension. The name is not case sensitive."},"body":{"type":"object","description":"Request body"}},"required":["owner","repo","body"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("PUT", ("https://api.github.com/repos/" + String(args["owner"]) + "/" + String(args["repo"]) + "/actions/permissions/fork-pr-contributor-approval") + _qs, {}, args["body"]);
    }
  },
  {
    name: "actions/get-private-repo-fork-pr-workflows-settings-repository",
    description: "Get private repo fork PR workflow settings for a repository. Gets the settings for whether workflows from fork pull requests can run on a private repository. Params: owner (required), repo (required).",
    inputSchema: {"type":"object","properties":{"owner":{"type":"string","description":"The account owner of the repository. The name is not case sensitive."},"repo":{"type":"string","description":"The name of the repository without the `.git` extension. The name is not case sensitive."}},"required":["owner","repo"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/repos/" + String(args["owner"]) + "/" + String(args["repo"]) + "/actions/permissions/fork-pr-workflows-private-repos") + _qs, {}, undefined);
    }
  },
  {
    name: "actions/set-private-repo-fork-pr-workflows-settings-repository",
    description: "Set private repo fork PR workflow settings for a repository. Sets the settings for whether workflows from fork pull requests can run on a private repository. Params: owner (required), repo (required).",
    inputSchema: {"type":"object","properties":{"owner":{"type":"string","description":"The account owner of the repository. The name is not case sensitive."},"repo":{"type":"string","description":"The name of the repository without the `.git` extension. The name is not case sensitive."},"body":{"type":"object","description":"Request body"}},"required":["owner","repo","body"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("PUT", ("https://api.github.com/repos/" + String(args["owner"]) + "/" + String(args["repo"]) + "/actions/permissions/fork-pr-workflows-private-repos") + _qs, {}, args["body"]);
    }
  },
  {
    name: "actions/get-allowed-actions-repository",
    description: "Get allowed actions and reusable workflows for a repository. Gets the settings for selected actions and reusable workflows that are allowed in a repository. Params: owner (required), repo (required).",
    inputSchema: {"type":"object","properties":{"owner":{"type":"string","description":"The account owner of the repository. The name is not case sensitive."},"repo":{"type":"string","description":"The name of the repository without the `.git` extension. The name is not case sensitive."}},"required":["owner","repo"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/repos/" + String(args["owner"]) + "/" + String(args["repo"]) + "/actions/permissions/selected-actions") + _qs, {}, undefined);
    }
  },
  {
    name: "actions/set-allowed-actions-repository",
    description: "Set allowed actions and reusable workflows for a repository. Sets the actions and reusable workflows that are allowed in a repository. Params: owner (required), repo (required).",
    inputSchema: {"type":"object","properties":{"owner":{"type":"string","description":"The account owner of the repository. The name is not case sensitive."},"repo":{"type":"string","description":"The name of the repository without the `.git` extension. The name is not case sensitive."},"body":{"type":"object","description":"Request body"}},"required":["owner","repo"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("PUT", ("https://api.github.com/repos/" + String(args["owner"]) + "/" + String(args["repo"]) + "/actions/permissions/selected-actions") + _qs, {}, args["body"]);
    }
  },
  {
    name: "actions/get-github-actions-default-workflow-permissions-repository",
    description: "Get default workflow permissions for a repository. Gets the default workflow permissions granted to the `GITHUB_TOKEN` when running workflows in a repository,\nas well as if GitHub Actions can submit approving pull request reviews. Params: owner (required), repo (required).",
    inputSchema: {"type":"object","properties":{"owner":{"type":"string","description":"The account owner of the repository. The name is not case sensitive."},"repo":{"type":"string","description":"The name of the repository without the `.git` extension. The name is not case sensitive."}},"required":["owner","repo"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/repos/" + String(args["owner"]) + "/" + String(args["repo"]) + "/actions/permissions/workflow") + _qs, {}, undefined);
    }
  },
  {
    name: "actions/set-github-actions-default-workflow-permissions-repository",
    description: "Set default workflow permissions for a repository. Sets the default workflow permissions granted to the `GITHUB_TOKEN` when running workflows in a repository, and sets if GitHub Actions\ncan submit approving pull request reviews. Params: owner (required), repo (required).",
    inputSchema: {"type":"object","properties":{"owner":{"type":"string","description":"The account owner of the repository. The name is not case sensitive."},"repo":{"type":"string","description":"The name of the repository without the `.git` extension. The name is not case sensitive."},"body":{"type":"object","description":"Request body"}},"required":["owner","repo","body"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("PUT", ("https://api.github.com/repos/" + String(args["owner"]) + "/" + String(args["repo"]) + "/actions/permissions/workflow") + _qs, {}, args["body"]);
    }
  },
  {
    name: "actions/list-self-hosted-runners-for-repo",
    description: "List self-hosted runners for a repository. Lists all self-hosted runners configured in a repository. Params: owner (required), repo (required).",
    inputSchema: {"type":"object","properties":{"name":{"type":"string","description":"The name of a self-hosted runner."},"owner":{"type":"string","description":"The account owner of the repository. The name is not case sensitive."},"repo":{"type":"string","description":"The name of the repository without the `.git` extension. The name is not case sensitive."},"per_page":{"type":"integer","default":30,"description":"The number of results per page (max 100). For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""},"page":{"type":"integer","default":1,"description":"The page number of the results to fetch. For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""}},"required":["owner","repo"]},
    call: function(args) {
    var _q = new URLSearchParams();
    if (args["name"] !== undefined) _q.set("name", String(args["name"]));
    if (args["per_page"] !== undefined) _q.set("per_page", String(args["per_page"]));
    if (args["page"] !== undefined) _q.set("page", String(args["page"]));
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/repos/" + String(args["owner"]) + "/" + String(args["repo"]) + "/actions/runners") + _qs, {}, undefined);
    }
  },
  {
    name: "actions/list-runner-applications-for-repo",
    description: "List runner applications for a repository. Lists binaries for the runner application that you can download and run. Params: owner (required), repo (required).",
    inputSchema: {"type":"object","properties":{"owner":{"type":"string","description":"The account owner of the repository. The name is not case sensitive."},"repo":{"type":"string","description":"The name of the repository without the `.git` extension. The name is not case sensitive."}},"required":["owner","repo"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/repos/" + String(args["owner"]) + "/" + String(args["repo"]) + "/actions/runners/downloads") + _qs, {}, undefined);
    }
  },
  {
    name: "actions/generate-runner-jitconfig-for-repo",
    description: "Create configuration for a just-in-time runner for a repository. Generates a configuration that can be passed to the runner application at startup. Params: owner (required), repo (required).",
    inputSchema: {"type":"object","properties":{"owner":{"type":"string","description":"The account owner of the repository. The name is not case sensitive."},"repo":{"type":"string","description":"The name of the repository without the `.git` extension. The name is not case sensitive."},"body":{"type":"object","description":"Request body"}},"required":["owner","repo","body"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("POST", ("https://api.github.com/repos/" + String(args["owner"]) + "/" + String(args["repo"]) + "/actions/runners/generate-jitconfig") + _qs, {}, args["body"]);
    }
  },
  {
    name: "actions/create-registration-token-for-repo",
    description: "Create a registration token for a repository. Returns a token that you can pass to the `config` script. Params: owner (required), repo (required).",
    inputSchema: {"type":"object","properties":{"owner":{"type":"string","description":"The account owner of the repository. The name is not case sensitive."},"repo":{"type":"string","description":"The name of the repository without the `.git` extension. The name is not case sensitive."}},"required":["owner","repo"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("POST", ("https://api.github.com/repos/" + String(args["owner"]) + "/" + String(args["repo"]) + "/actions/runners/registration-token") + _qs, {}, undefined);
    }
  },
  {
    name: "actions/create-remove-token-for-repo",
    description: "Create a remove token for a repository. Returns a token that you can pass to the `config` script to remove a self-hosted runner from an repository. Params: owner (required), repo (required).",
    inputSchema: {"type":"object","properties":{"owner":{"type":"string","description":"The account owner of the repository. The name is not case sensitive."},"repo":{"type":"string","description":"The name of the repository without the `.git` extension. The name is not case sensitive."}},"required":["owner","repo"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("POST", ("https://api.github.com/repos/" + String(args["owner"]) + "/" + String(args["repo"]) + "/actions/runners/remove-token") + _qs, {}, undefined);
    }
  },
  {
    name: "actions/get-self-hosted-runner-for-repo",
    description: "Get a self-hosted runner for a repository. Gets a specific self-hosted runner configured in a repository. Params: owner (required), repo (required), runner_id (required).",
    inputSchema: {"type":"object","properties":{"owner":{"type":"string","description":"The account owner of the repository. The name is not case sensitive."},"repo":{"type":"string","description":"The name of the repository without the `.git` extension. The name is not case sensitive."},"runner_id":{"type":"integer","description":"Unique identifier of the self-hosted runner."}},"required":["owner","repo","runner_id"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/repos/" + String(args["owner"]) + "/" + String(args["repo"]) + "/actions/runners/" + String(args["runner_id"])) + _qs, {}, undefined);
    }
  },
  {
    name: "actions/delete-self-hosted-runner-from-repo",
    description: "Delete a self-hosted runner from a repository. Forces the removal of a self-hosted runner from a repository. Params: owner (required), repo (required), runner_id (required).",
    inputSchema: {"type":"object","properties":{"owner":{"type":"string","description":"The account owner of the repository. The name is not case sensitive."},"repo":{"type":"string","description":"The name of the repository without the `.git` extension. The name is not case sensitive."},"runner_id":{"type":"integer","description":"Unique identifier of the self-hosted runner."}},"required":["owner","repo","runner_id"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("DELETE", ("https://api.github.com/repos/" + String(args["owner"]) + "/" + String(args["repo"]) + "/actions/runners/" + String(args["runner_id"])) + _qs, {}, undefined);
    }
  },
  {
    name: "actions/list-labels-for-self-hosted-runner-for-repo",
    description: "List labels for a self-hosted runner for a repository. Lists all labels for a self-hosted runner configured in a repository. Params: owner (required), repo (required), runner_id (required).",
    inputSchema: {"type":"object","properties":{"owner":{"type":"string","description":"The account owner of the repository. The name is not case sensitive."},"repo":{"type":"string","description":"The name of the repository without the `.git` extension. The name is not case sensitive."},"runner_id":{"type":"integer","description":"Unique identifier of the self-hosted runner."}},"required":["owner","repo","runner_id"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/repos/" + String(args["owner"]) + "/" + String(args["repo"]) + "/actions/runners/" + String(args["runner_id"]) + "/labels") + _qs, {}, undefined);
    }
  },
  {
    name: "actions/add-custom-labels-to-self-hosted-runner-for-repo",
    description: "Add custom labels to a self-hosted runner for a repository. Adds custom labels to a self-hosted runner configured in a repository. Params: owner (required), repo (required), runner_id (required).",
    inputSchema: {"type":"object","properties":{"owner":{"type":"string","description":"The account owner of the repository. The name is not case sensitive."},"repo":{"type":"string","description":"The name of the repository without the `.git` extension. The name is not case sensitive."},"runner_id":{"type":"integer","description":"Unique identifier of the self-hosted runner."},"body":{"type":"object","description":"Request body"}},"required":["owner","repo","runner_id","body"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("POST", ("https://api.github.com/repos/" + String(args["owner"]) + "/" + String(args["repo"]) + "/actions/runners/" + String(args["runner_id"]) + "/labels") + _qs, {}, args["body"]);
    }
  },
  {
    name: "actions/set-custom-labels-for-self-hosted-runner-for-repo",
    description: "Set custom labels for a self-hosted runner for a repository. Remove all previous custom labels and set the new custom labels for a specific\nself-hosted runner configured in a repository. Params: owner (required), repo (required), runner_id (required).",
    inputSchema: {"type":"object","properties":{"owner":{"type":"string","description":"The account owner of the repository. The name is not case sensitive."},"repo":{"type":"string","description":"The name of the repository without the `.git` extension. The name is not case sensitive."},"runner_id":{"type":"integer","description":"Unique identifier of the self-hosted runner."},"body":{"type":"object","description":"Request body"}},"required":["owner","repo","runner_id","body"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("PUT", ("https://api.github.com/repos/" + String(args["owner"]) + "/" + String(args["repo"]) + "/actions/runners/" + String(args["runner_id"]) + "/labels") + _qs, {}, args["body"]);
    }
  },
  {
    name: "actions/remove-all-custom-labels-from-self-hosted-runner-for-repo",
    description: "Remove all custom labels from a self-hosted runner for a repository. Remove all custom labels from a self-hosted runner configured in a\nrepository. Params: owner (required), repo (required), runner_id (required).",
    inputSchema: {"type":"object","properties":{"owner":{"type":"string","description":"The account owner of the repository. The name is not case sensitive."},"repo":{"type":"string","description":"The name of the repository without the `.git` extension. The name is not case sensitive."},"runner_id":{"type":"integer","description":"Unique identifier of the self-hosted runner."}},"required":["owner","repo","runner_id"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("DELETE", ("https://api.github.com/repos/" + String(args["owner"]) + "/" + String(args["repo"]) + "/actions/runners/" + String(args["runner_id"]) + "/labels") + _qs, {}, undefined);
    }
  },
  {
    name: "actions/remove-custom-label-from-self-hosted-runner-for-repo",
    description: "Remove a custom label from a self-hosted runner for a repository. Remove a custom label from a self-hosted runner configured\nin a repository. Params: owner (required), repo (required), runner_id (required), name (required).",
    inputSchema: {"type":"object","properties":{"owner":{"type":"string","description":"The account owner of the repository. The name is not case sensitive."},"repo":{"type":"string","description":"The name of the repository without the `.git` extension. The name is not case sensitive."},"runner_id":{"type":"integer","description":"Unique identifier of the self-hosted runner."},"name":{"type":"string","description":"The name of a self-hosted runner's custom label."}},"required":["owner","repo","runner_id","name"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("DELETE", ("https://api.github.com/repos/" + String(args["owner"]) + "/" + String(args["repo"]) + "/actions/runners/" + String(args["runner_id"]) + "/labels/" + String(args["name"])) + _qs, {}, undefined);
    }
  },
  {
    name: "actions/list-workflow-runs-for-repo",
    description: "List workflow runs for a repository. Lists all workflow runs for a repository. Params: owner (required), repo (required).",
    inputSchema: {"type":"object","properties":{"owner":{"type":"string","description":"The account owner of the repository. The name is not case sensitive."},"repo":{"type":"string","description":"The name of the repository without the `.git` extension. The name is not case sensitive."},"actor":{"type":"string","description":"Returns someone's workflow runs. Use the login for the user who created the `push` associated with the check suite or workflow run."},"branch":{"type":"string","description":"Returns workflow runs associated with a branch. Use the name of the branch of the `push`."},"event":{"type":"string","description":"Returns workflow run triggered by the event you specify. For example, `push`, `pull_request` or `issue`. For more information, see \"[Events that trigger workflows](https://docs.github.com/actions/automating-your-workflow-with-github-actions/events-that-trigger-workflows).\""},"status":{"type":"string","enum":["completed","action_required","cancelled","failure","neutral","skipped","stale","success","timed_out","in_progress","queued","requested","waiting","pending"],"description":"Returns workflow runs with the check run `status` or `conclusion` that you specify. For example, a conclusion can be `success` or a status can be `in_progress`. Only GitHub Actions can set a status of `waiting`, `pending`, or `requested`."},"per_page":{"type":"integer","default":30,"description":"The number of results per page (max 100). For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""},"page":{"type":"integer","default":1,"description":"The page number of the results to fetch. For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""},"created":{"type":"string","format":"date-time","description":"Returns workflow runs created within the given date-time range. For more information on the syntax, see \"[Understanding the search syntax](https://docs.github.com/search-github/getting-started-with-searching-on-github/understanding-the-search-syntax#query-for-dates).\""},"exclude_pull_requests":{"type":"boolean","default":false,"description":"If `true` pull requests are omitted from the response (empty array)."},"check_suite_id":{"type":"integer","description":"Returns workflow runs with the `check_suite_id` that you specify."},"head_sha":{"type":"string","description":"Only returns workflow runs that are associated with the specified `head_sha`."}},"required":["owner","repo"]},
    call: function(args) {
    var _q = new URLSearchParams();
    if (args["actor"] !== undefined) _q.set("actor", String(args["actor"]));
    if (args["branch"] !== undefined) _q.set("branch", String(args["branch"]));
    if (args["event"] !== undefined) _q.set("event", String(args["event"]));
    if (args["status"] !== undefined) _q.set("status", String(args["status"]));
    if (args["per_page"] !== undefined) _q.set("per_page", String(args["per_page"]));
    if (args["page"] !== undefined) _q.set("page", String(args["page"]));
    if (args["created"] !== undefined) _q.set("created", String(args["created"]));
    if (args["exclude_pull_requests"] !== undefined) _q.set("exclude_pull_requests", String(args["exclude_pull_requests"]));
    if (args["check_suite_id"] !== undefined) _q.set("check_suite_id", String(args["check_suite_id"]));
    if (args["head_sha"] !== undefined) _q.set("head_sha", String(args["head_sha"]));
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/repos/" + String(args["owner"]) + "/" + String(args["repo"]) + "/actions/runs") + _qs, {}, undefined);
    }
  },
  {
    name: "actions/get-workflow-run",
    description: "Get a workflow run. Gets a specific workflow run. Params: owner (required), repo (required), run_id (required).",
    inputSchema: {"type":"object","properties":{"owner":{"type":"string","description":"The account owner of the repository. The name is not case sensitive."},"repo":{"type":"string","description":"The name of the repository without the `.git` extension. The name is not case sensitive."},"run_id":{"type":"integer","description":"The unique identifier of the workflow run."},"exclude_pull_requests":{"type":"boolean","default":false,"description":"If `true` pull requests are omitted from the response (empty array)."}},"required":["owner","repo","run_id"]},
    call: function(args) {
    var _q = new URLSearchParams();
    if (args["exclude_pull_requests"] !== undefined) _q.set("exclude_pull_requests", String(args["exclude_pull_requests"]));
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/repos/" + String(args["owner"]) + "/" + String(args["repo"]) + "/actions/runs/" + String(args["run_id"])) + _qs, {}, undefined);
    }
  },
  {
    name: "actions/delete-workflow-run",
    description: "Delete a workflow run. Deletes a specific workflow run. Params: owner (required), repo (required), run_id (required).",
    inputSchema: {"type":"object","properties":{"owner":{"type":"string","description":"The account owner of the repository. The name is not case sensitive."},"repo":{"type":"string","description":"The name of the repository without the `.git` extension. The name is not case sensitive."},"run_id":{"type":"integer","description":"The unique identifier of the workflow run."}},"required":["owner","repo","run_id"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("DELETE", ("https://api.github.com/repos/" + String(args["owner"]) + "/" + String(args["repo"]) + "/actions/runs/" + String(args["run_id"])) + _qs, {}, undefined);
    }
  },
  {
    name: "actions/get-reviews-for-run",
    description: "Get the review history for a workflow run. Anyone with read access to the repository can use this endpoint. Params: owner (required), repo (required), run_id (required).",
    inputSchema: {"type":"object","properties":{"owner":{"type":"string","description":"The account owner of the repository. The name is not case sensitive."},"repo":{"type":"string","description":"The name of the repository without the `.git` extension. The name is not case sensitive."},"run_id":{"type":"integer","description":"The unique identifier of the workflow run."}},"required":["owner","repo","run_id"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/repos/" + String(args["owner"]) + "/" + String(args["repo"]) + "/actions/runs/" + String(args["run_id"]) + "/approvals") + _qs, {}, undefined);
    }
  },
  {
    name: "actions/approve-workflow-run",
    description: "Approve a workflow run for a fork pull request. Approves a workflow run for a pull request from a public fork of a first time contributor. Params: owner (required), repo (required), run_id (required).",
    inputSchema: {"type":"object","properties":{"owner":{"type":"string","description":"The account owner of the repository. The name is not case sensitive."},"repo":{"type":"string","description":"The name of the repository without the `.git` extension. The name is not case sensitive."},"run_id":{"type":"integer","description":"The unique identifier of the workflow run."}},"required":["owner","repo","run_id"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("POST", ("https://api.github.com/repos/" + String(args["owner"]) + "/" + String(args["repo"]) + "/actions/runs/" + String(args["run_id"]) + "/approve") + _qs, {}, undefined);
    }
  },
  {
    name: "actions/list-workflow-run-artifacts",
    description: "List workflow run artifacts. Lists artifacts for a workflow run. Params: owner (required), repo (required), run_id (required).",
    inputSchema: {"type":"object","properties":{"owner":{"type":"string","description":"The account owner of the repository. The name is not case sensitive."},"repo":{"type":"string","description":"The name of the repository without the `.git` extension. The name is not case sensitive."},"run_id":{"type":"integer","description":"The unique identifier of the workflow run."},"per_page":{"type":"integer","default":30,"description":"The number of results per page (max 100). For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""},"page":{"type":"integer","default":1,"description":"The page number of the results to fetch. For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""},"name":{"type":"string","description":"The name field of an artifact. When specified, only artifacts with this name will be returned."},"direction":{"type":"string","enum":["asc","desc"],"default":"desc","description":"The direction to sort the results by."}},"required":["owner","repo","run_id"]},
    call: function(args) {
    var _q = new URLSearchParams();
    if (args["per_page"] !== undefined) _q.set("per_page", String(args["per_page"]));
    if (args["page"] !== undefined) _q.set("page", String(args["page"]));
    if (args["name"] !== undefined) _q.set("name", String(args["name"]));
    if (args["direction"] !== undefined) _q.set("direction", String(args["direction"]));
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/repos/" + String(args["owner"]) + "/" + String(args["repo"]) + "/actions/runs/" + String(args["run_id"]) + "/artifacts") + _qs, {}, undefined);
    }
  },
  {
    name: "actions/get-workflow-run-attempt",
    description: "Get a workflow run attempt. Gets a specific workflow run attempt. Params: owner (required), repo (required), run_id (required), attempt_number (required).",
    inputSchema: {"type":"object","properties":{"owner":{"type":"string","description":"The account owner of the repository. The name is not case sensitive."},"repo":{"type":"string","description":"The name of the repository without the `.git` extension. The name is not case sensitive."},"run_id":{"type":"integer","description":"The unique identifier of the workflow run."},"attempt_number":{"type":"integer","description":"The attempt number of the workflow run."},"exclude_pull_requests":{"type":"boolean","default":false,"description":"If `true` pull requests are omitted from the response (empty array)."}},"required":["owner","repo","run_id","attempt_number"]},
    call: function(args) {
    var _q = new URLSearchParams();
    if (args["exclude_pull_requests"] !== undefined) _q.set("exclude_pull_requests", String(args["exclude_pull_requests"]));
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/repos/" + String(args["owner"]) + "/" + String(args["repo"]) + "/actions/runs/" + String(args["run_id"]) + "/attempts/" + String(args["attempt_number"])) + _qs, {}, undefined);
    }
  },
  {
    name: "actions/list-jobs-for-workflow-run-attempt",
    description: "List jobs for a workflow run attempt. Lists jobs for a specific workflow run attempt. Params: owner (required), repo (required), run_id (required), attempt_number (required).",
    inputSchema: {"type":"object","properties":{"owner":{"type":"string","description":"The account owner of the repository. The name is not case sensitive."},"repo":{"type":"string","description":"The name of the repository without the `.git` extension. The name is not case sensitive."},"run_id":{"type":"integer","description":"The unique identifier of the workflow run."},"attempt_number":{"type":"integer","description":"The attempt number of the workflow run."},"per_page":{"type":"integer","default":30,"description":"The number of results per page (max 100). For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""},"page":{"type":"integer","default":1,"description":"The page number of the results to fetch. For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""}},"required":["owner","repo","run_id","attempt_number"]},
    call: function(args) {
    var _q = new URLSearchParams();
    if (args["per_page"] !== undefined) _q.set("per_page", String(args["per_page"]));
    if (args["page"] !== undefined) _q.set("page", String(args["page"]));
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/repos/" + String(args["owner"]) + "/" + String(args["repo"]) + "/actions/runs/" + String(args["run_id"]) + "/attempts/" + String(args["attempt_number"]) + "/jobs") + _qs, {}, undefined);
    }
  },
  {
    name: "actions/download-workflow-run-attempt-logs",
    description: "Download workflow run attempt logs. Gets a redirect URL to download an archive of log files for a specific workflow run attempt. Params: owner (required), repo (required), run_id (required), attempt_number (required).",
    inputSchema: {"type":"object","properties":{"owner":{"type":"string","description":"The account owner of the repository. The name is not case sensitive."},"repo":{"type":"string","description":"The name of the repository without the `.git` extension. The name is not case sensitive."},"run_id":{"type":"integer","description":"The unique identifier of the workflow run."},"attempt_number":{"type":"integer","description":"The attempt number of the workflow run."}},"required":["owner","repo","run_id","attempt_number"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/repos/" + String(args["owner"]) + "/" + String(args["repo"]) + "/actions/runs/" + String(args["run_id"]) + "/attempts/" + String(args["attempt_number"]) + "/logs") + _qs, {}, undefined);
    }
  },
  {
    name: "actions/cancel-workflow-run",
    description: "Cancel a workflow run. Cancels a workflow run using its `id`. Params: owner (required), repo (required), run_id (required).",
    inputSchema: {"type":"object","properties":{"owner":{"type":"string","description":"The account owner of the repository. The name is not case sensitive."},"repo":{"type":"string","description":"The name of the repository without the `.git` extension. The name is not case sensitive."},"run_id":{"type":"integer","description":"The unique identifier of the workflow run."}},"required":["owner","repo","run_id"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("POST", ("https://api.github.com/repos/" + String(args["owner"]) + "/" + String(args["repo"]) + "/actions/runs/" + String(args["run_id"]) + "/cancel") + _qs, {}, undefined);
    }
  },
  {
    name: "actions/list-concurrency-groups-for-workflow-run",
    description: "List concurrency groups for a workflow run. Lists all concurrency groups associated with a workflow run or its jobs. Params: owner (required), repo (required), run_id (required).",
    inputSchema: {"type":"object","properties":{"owner":{"type":"string","description":"The account owner of the repository. The name is not case sensitive."},"repo":{"type":"string","description":"The name of the repository without the `.git` extension. The name is not case sensitive."},"run_id":{"type":"integer","description":"The unique identifier of the workflow run."},"per_page":{"type":"integer","default":30,"description":"The number of results per page (max 100). For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""},"before":{"type":"string","description":"A cursor, as given in the [Link header](https://docs.github.com/rest/guides/using-pagination-in-the-rest-api#using-link-headers). If specified, the query only searches for results before this cursor. For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""},"after":{"type":"string","description":"A cursor, as given in the [Link header](https://docs.github.com/rest/guides/using-pagination-in-the-rest-api#using-link-headers). If specified, the query only searches for results after this cursor. For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""}},"required":["owner","repo","run_id"]},
    call: function(args) {
    var _q = new URLSearchParams();
    if (args["per_page"] !== undefined) _q.set("per_page", String(args["per_page"]));
    if (args["before"] !== undefined) _q.set("before", String(args["before"]));
    if (args["after"] !== undefined) _q.set("after", String(args["after"]));
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/repos/" + String(args["owner"]) + "/" + String(args["repo"]) + "/actions/runs/" + String(args["run_id"]) + "/concurrency_groups") + _qs, {}, undefined);
    }
  },
  {
    name: "actions/review-custom-gates-for-run",
    description: "Review custom deployment protection rules for a workflow run. Approve or reject custom deployment protection rules provided by a GitHub App for a workflow run. Params: owner (required), repo (required), run_id (required).",
    inputSchema: {"type":"object","properties":{"owner":{"type":"string","description":"The account owner of the repository. The name is not case sensitive."},"repo":{"type":"string","description":"The name of the repository without the `.git` extension. The name is not case sensitive."},"run_id":{"type":"integer","description":"The unique identifier of the workflow run."},"body":{"type":"object","description":"Request body"}},"required":["owner","repo","run_id","body"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("POST", ("https://api.github.com/repos/" + String(args["owner"]) + "/" + String(args["repo"]) + "/actions/runs/" + String(args["run_id"]) + "/deployment_protection_rule") + _qs, {}, args["body"]);
    }
  },
  {
    name: "actions/force-cancel-workflow-run",
    description: "Force cancel a workflow run. Cancels a workflow run and bypasses conditions that would otherwise cause a workflow execution to continue, such as an `always()` condition on a job. Params: owner (required), repo (required), run_id (required).",
    inputSchema: {"type":"object","properties":{"owner":{"type":"string","description":"The account owner of the repository. The name is not case sensitive."},"repo":{"type":"string","description":"The name of the repository without the `.git` extension. The name is not case sensitive."},"run_id":{"type":"integer","description":"The unique identifier of the workflow run."}},"required":["owner","repo","run_id"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("POST", ("https://api.github.com/repos/" + String(args["owner"]) + "/" + String(args["repo"]) + "/actions/runs/" + String(args["run_id"]) + "/force-cancel") + _qs, {}, undefined);
    }
  },
  {
    name: "actions/list-jobs-for-workflow-run",
    description: "List jobs for a workflow run. Lists jobs for a workflow run. Params: owner (required), repo (required), run_id (required).",
    inputSchema: {"type":"object","properties":{"owner":{"type":"string","description":"The account owner of the repository. The name is not case sensitive."},"repo":{"type":"string","description":"The name of the repository without the `.git` extension. The name is not case sensitive."},"run_id":{"type":"integer","description":"The unique identifier of the workflow run."},"filter":{"type":"string","enum":["latest","all"],"default":"latest","description":"Filters jobs by their `completed_at` timestamp. `latest` returns jobs from the most recent execution of the workflow run. `all` returns all jobs for a workflow run, including from old executions of the workflow run."},"per_page":{"type":"integer","default":30,"description":"The number of results per page (max 100). For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""},"page":{"type":"integer","default":1,"description":"The page number of the results to fetch. For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""}},"required":["owner","repo","run_id"]},
    call: function(args) {
    var _q = new URLSearchParams();
    if (args["filter"] !== undefined) _q.set("filter", String(args["filter"]));
    if (args["per_page"] !== undefined) _q.set("per_page", String(args["per_page"]));
    if (args["page"] !== undefined) _q.set("page", String(args["page"]));
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/repos/" + String(args["owner"]) + "/" + String(args["repo"]) + "/actions/runs/" + String(args["run_id"]) + "/jobs") + _qs, {}, undefined);
    }
  },
  {
    name: "actions/download-workflow-run-logs",
    description: "Download workflow run logs. Gets a redirect URL to download an archive of log files for a workflow run. Params: owner (required), repo (required), run_id (required).",
    inputSchema: {"type":"object","properties":{"owner":{"type":"string","description":"The account owner of the repository. The name is not case sensitive."},"repo":{"type":"string","description":"The name of the repository without the `.git` extension. The name is not case sensitive."},"run_id":{"type":"integer","description":"The unique identifier of the workflow run."}},"required":["owner","repo","run_id"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/repos/" + String(args["owner"]) + "/" + String(args["repo"]) + "/actions/runs/" + String(args["run_id"]) + "/logs") + _qs, {}, undefined);
    }
  },
  {
    name: "actions/delete-workflow-run-logs",
    description: "Delete workflow run logs. Deletes all logs for a workflow run. Params: owner (required), repo (required), run_id (required).",
    inputSchema: {"type":"object","properties":{"owner":{"type":"string","description":"The account owner of the repository. The name is not case sensitive."},"repo":{"type":"string","description":"The name of the repository without the `.git` extension. The name is not case sensitive."},"run_id":{"type":"integer","description":"The unique identifier of the workflow run."}},"required":["owner","repo","run_id"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("DELETE", ("https://api.github.com/repos/" + String(args["owner"]) + "/" + String(args["repo"]) + "/actions/runs/" + String(args["run_id"]) + "/logs") + _qs, {}, undefined);
    }
  },
  {
    name: "actions/get-pending-deployments-for-run",
    description: "Get pending deployments for a workflow run. Get all deployment environments for a workflow run that are waiting for protection rules to pass. Params: owner (required), repo (required), run_id (required).",
    inputSchema: {"type":"object","properties":{"owner":{"type":"string","description":"The account owner of the repository. The name is not case sensitive."},"repo":{"type":"string","description":"The name of the repository without the `.git` extension. The name is not case sensitive."},"run_id":{"type":"integer","description":"The unique identifier of the workflow run."}},"required":["owner","repo","run_id"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/repos/" + String(args["owner"]) + "/" + String(args["repo"]) + "/actions/runs/" + String(args["run_id"]) + "/pending_deployments") + _qs, {}, undefined);
    }
  },
  {
    name: "actions/review-pending-deployments-for-run",
    description: "Review pending deployments for a workflow run. Approve or reject pending deployments that are waiting on approval by a required reviewer. Params: owner (required), repo (required), run_id (required).",
    inputSchema: {"type":"object","properties":{"owner":{"type":"string","description":"The account owner of the repository. The name is not case sensitive."},"repo":{"type":"string","description":"The name of the repository without the `.git` extension. The name is not case sensitive."},"run_id":{"type":"integer","description":"The unique identifier of the workflow run."},"body":{"type":"object","description":"Request body"}},"required":["owner","repo","run_id","body"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("POST", ("https://api.github.com/repos/" + String(args["owner"]) + "/" + String(args["repo"]) + "/actions/runs/" + String(args["run_id"]) + "/pending_deployments") + _qs, {}, args["body"]);
    }
  },
  {
    name: "actions/re-run-workflow",
    description: "Re-run a workflow. Re-runs your workflow run using its `id`. Params: owner (required), repo (required), run_id (required).",
    inputSchema: {"type":"object","properties":{"owner":{"type":"string","description":"The account owner of the repository. The name is not case sensitive."},"repo":{"type":"string","description":"The name of the repository without the `.git` extension. The name is not case sensitive."},"run_id":{"type":"integer","description":"The unique identifier of the workflow run."},"body":{"type":"object","description":"Request body"}},"required":["owner","repo","run_id"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("POST", ("https://api.github.com/repos/" + String(args["owner"]) + "/" + String(args["repo"]) + "/actions/runs/" + String(args["run_id"]) + "/rerun") + _qs, {}, args["body"]);
    }
  },
  {
    name: "actions/re-run-workflow-failed-jobs",
    description: "Re-run failed jobs from a workflow run. Re-run all of the failed jobs and their dependent jobs in a workflow run using the `id` of the workflow run. Params: owner (required), repo (required), run_id (required).",
    inputSchema: {"type":"object","properties":{"owner":{"type":"string","description":"The account owner of the repository. The name is not case sensitive."},"repo":{"type":"string","description":"The name of the repository without the `.git` extension. The name is not case sensitive."},"run_id":{"type":"integer","description":"The unique identifier of the workflow run."},"body":{"type":"object","description":"Request body"}},"required":["owner","repo","run_id"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("POST", ("https://api.github.com/repos/" + String(args["owner"]) + "/" + String(args["repo"]) + "/actions/runs/" + String(args["run_id"]) + "/rerun-failed-jobs") + _qs, {}, args["body"]);
    }
  },
  {
    name: "actions/get-workflow-run-usage",
    description: "Get workflow run usage. > [!WARNING]  \n> This endpoint is in the process of closing down. Params: owner (required), repo (required), run_id (required).",
    inputSchema: {"type":"object","properties":{"owner":{"type":"string","description":"The account owner of the repository. The name is not case sensitive."},"repo":{"type":"string","description":"The name of the repository without the `.git` extension. The name is not case sensitive."},"run_id":{"type":"integer","description":"The unique identifier of the workflow run."}},"required":["owner","repo","run_id"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/repos/" + String(args["owner"]) + "/" + String(args["repo"]) + "/actions/runs/" + String(args["run_id"]) + "/timing") + _qs, {}, undefined);
    }
  },
  {
    name: "actions/list-repo-secrets",
    description: "List repository secrets. Lists all secrets available in a repository without revealing their encrypted\nvalues. Params: owner (required), repo (required).",
    inputSchema: {"type":"object","properties":{"owner":{"type":"string","description":"The account owner of the repository. The name is not case sensitive."},"repo":{"type":"string","description":"The name of the repository without the `.git` extension. The name is not case sensitive."},"per_page":{"type":"integer","default":30,"description":"The number of results per page (max 100). For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""},"page":{"type":"integer","default":1,"description":"The page number of the results to fetch. For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""}},"required":["owner","repo"]},
    call: function(args) {
    var _q = new URLSearchParams();
    if (args["per_page"] !== undefined) _q.set("per_page", String(args["per_page"]));
    if (args["page"] !== undefined) _q.set("page", String(args["page"]));
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/repos/" + String(args["owner"]) + "/" + String(args["repo"]) + "/actions/secrets") + _qs, {}, undefined);
    }
  },
  {
    name: "actions/get-repo-public-key",
    description: "Get a repository public key. Gets your public key, which you need to encrypt secrets. Params: owner (required), repo (required).",
    inputSchema: {"type":"object","properties":{"owner":{"type":"string","description":"The account owner of the repository. The name is not case sensitive."},"repo":{"type":"string","description":"The name of the repository without the `.git` extension. The name is not case sensitive."}},"required":["owner","repo"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/repos/" + String(args["owner"]) + "/" + String(args["repo"]) + "/actions/secrets/public-key") + _qs, {}, undefined);
    }
  },
  {
    name: "actions/get-repo-secret",
    description: "Get a repository secret. Gets a single repository secret without revealing its encrypted value. Params: owner (required), repo (required), secret_name (required).",
    inputSchema: {"type":"object","properties":{"owner":{"type":"string","description":"The account owner of the repository. The name is not case sensitive."},"repo":{"type":"string","description":"The name of the repository without the `.git` extension. The name is not case sensitive."},"secret_name":{"type":"string","description":"The name of the secret."}},"required":["owner","repo","secret_name"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/repos/" + String(args["owner"]) + "/" + String(args["repo"]) + "/actions/secrets/" + String(args["secret_name"])) + _qs, {}, undefined);
    }
  },
  {
    name: "actions/create-or-update-repo-secret",
    description: "Create or update a repository secret. Creates or updates a repository secret with an encrypted value. Params: owner (required), repo (required), secret_name (required).",
    inputSchema: {"type":"object","properties":{"owner":{"type":"string","description":"The account owner of the repository. The name is not case sensitive."},"repo":{"type":"string","description":"The name of the repository without the `.git` extension. The name is not case sensitive."},"secret_name":{"type":"string","description":"The name of the secret."},"body":{"type":"object","description":"Request body"}},"required":["owner","repo","secret_name","body"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("PUT", ("https://api.github.com/repos/" + String(args["owner"]) + "/" + String(args["repo"]) + "/actions/secrets/" + String(args["secret_name"])) + _qs, {}, args["body"]);
    }
  },
  {
    name: "actions/delete-repo-secret",
    description: "Delete a repository secret. Deletes a secret in a repository using the secret name. Params: owner (required), repo (required), secret_name (required).",
    inputSchema: {"type":"object","properties":{"owner":{"type":"string","description":"The account owner of the repository. The name is not case sensitive."},"repo":{"type":"string","description":"The name of the repository without the `.git` extension. The name is not case sensitive."},"secret_name":{"type":"string","description":"The name of the secret."}},"required":["owner","repo","secret_name"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("DELETE", ("https://api.github.com/repos/" + String(args["owner"]) + "/" + String(args["repo"]) + "/actions/secrets/" + String(args["secret_name"])) + _qs, {}, undefined);
    }
  },
  {
    name: "actions/list-repo-variables",
    description: "List repository variables. Lists all repository variables. Params: owner (required), repo (required).",
    inputSchema: {"type":"object","properties":{"owner":{"type":"string","description":"The account owner of the repository. The name is not case sensitive."},"repo":{"type":"string","description":"The name of the repository without the `.git` extension. The name is not case sensitive."},"per_page":{"type":"integer","default":10,"description":"The number of results per page (max 30). For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""},"page":{"type":"integer","default":1,"description":"The page number of the results to fetch. For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""}},"required":["owner","repo"]},
    call: function(args) {
    var _q = new URLSearchParams();
    if (args["per_page"] !== undefined) _q.set("per_page", String(args["per_page"]));
    if (args["page"] !== undefined) _q.set("page", String(args["page"]));
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/repos/" + String(args["owner"]) + "/" + String(args["repo"]) + "/actions/variables") + _qs, {}, undefined);
    }
  },
  {
    name: "actions/create-repo-variable",
    description: "Create a repository variable. Creates a repository variable that you can reference in a GitHub Actions workflow. Params: owner (required), repo (required).",
    inputSchema: {"type":"object","properties":{"owner":{"type":"string","description":"The account owner of the repository. The name is not case sensitive."},"repo":{"type":"string","description":"The name of the repository without the `.git` extension. The name is not case sensitive."},"body":{"type":"object","description":"Request body"}},"required":["owner","repo","body"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("POST", ("https://api.github.com/repos/" + String(args["owner"]) + "/" + String(args["repo"]) + "/actions/variables") + _qs, {}, args["body"]);
    }
  },
  {
    name: "actions/get-repo-variable",
    description: "Get a repository variable. Gets a specific variable in a repository. Params: owner (required), repo (required), name (required).",
    inputSchema: {"type":"object","properties":{"owner":{"type":"string","description":"The account owner of the repository. The name is not case sensitive."},"repo":{"type":"string","description":"The name of the repository without the `.git` extension. The name is not case sensitive."},"name":{"type":"string","description":"The name of the variable."}},"required":["owner","repo","name"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/repos/" + String(args["owner"]) + "/" + String(args["repo"]) + "/actions/variables/" + String(args["name"])) + _qs, {}, undefined);
    }
  },
  {
    name: "actions/update-repo-variable",
    description: "Update a repository variable. Updates a repository variable that you can reference in a GitHub Actions workflow. Params: owner (required), repo (required), name (required).",
    inputSchema: {"type":"object","properties":{"owner":{"type":"string","description":"The account owner of the repository. The name is not case sensitive."},"repo":{"type":"string","description":"The name of the repository without the `.git` extension. The name is not case sensitive."},"name":{"type":"string","description":"The name of the variable."},"body":{"type":"object","description":"Request body"}},"required":["owner","repo","name","body"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("PATCH", ("https://api.github.com/repos/" + String(args["owner"]) + "/" + String(args["repo"]) + "/actions/variables/" + String(args["name"])) + _qs, {}, args["body"]);
    }
  },
  {
    name: "actions/delete-repo-variable",
    description: "Delete a repository variable. Deletes a repository variable using the variable name. Params: owner (required), repo (required), name (required).",
    inputSchema: {"type":"object","properties":{"owner":{"type":"string","description":"The account owner of the repository. The name is not case sensitive."},"repo":{"type":"string","description":"The name of the repository without the `.git` extension. The name is not case sensitive."},"name":{"type":"string","description":"The name of the variable."}},"required":["owner","repo","name"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("DELETE", ("https://api.github.com/repos/" + String(args["owner"]) + "/" + String(args["repo"]) + "/actions/variables/" + String(args["name"])) + _qs, {}, undefined);
    }
  },
  {
    name: "actions/list-repo-workflows",
    description: "List repository workflows. Lists the workflows in a repository. Params: owner (required), repo (required).",
    inputSchema: {"type":"object","properties":{"owner":{"type":"string","description":"The account owner of the repository. The name is not case sensitive."},"repo":{"type":"string","description":"The name of the repository without the `.git` extension. The name is not case sensitive."},"per_page":{"type":"integer","default":30,"description":"The number of results per page (max 100). For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""},"page":{"type":"integer","default":1,"description":"The page number of the results to fetch. For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""}},"required":["owner","repo"]},
    call: function(args) {
    var _q = new URLSearchParams();
    if (args["per_page"] !== undefined) _q.set("per_page", String(args["per_page"]));
    if (args["page"] !== undefined) _q.set("page", String(args["page"]));
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/repos/" + String(args["owner"]) + "/" + String(args["repo"]) + "/actions/workflows") + _qs, {}, undefined);
    }
  },
  {
    name: "actions/get-workflow",
    description: "Get a workflow. Gets a specific workflow. Params: owner (required), repo (required), workflow_id (required).",
    inputSchema: {"type":"object","properties":{"owner":{"type":"string","description":"The account owner of the repository. The name is not case sensitive."},"repo":{"type":"string","description":"The name of the repository without the `.git` extension. The name is not case sensitive."},"workflow_id":{"oneOf":[{"type":"integer"},{"type":"string"}],"description":"The ID of the workflow. You can also pass the workflow file name as a string."}},"required":["owner","repo","workflow_id"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/repos/" + String(args["owner"]) + "/" + String(args["repo"]) + "/actions/workflows/" + String(args["workflow_id"])) + _qs, {}, undefined);
    }
  },
  {
    name: "actions/disable-workflow",
    description: "Disable a workflow. Disables a workflow and sets the `state` of the workflow to `disabled_manually`. Params: owner (required), repo (required), workflow_id (required).",
    inputSchema: {"type":"object","properties":{"owner":{"type":"string","description":"The account owner of the repository. The name is not case sensitive."},"repo":{"type":"string","description":"The name of the repository without the `.git` extension. The name is not case sensitive."},"workflow_id":{"oneOf":[{"type":"integer"},{"type":"string"}],"description":"The ID of the workflow. You can also pass the workflow file name as a string."}},"required":["owner","repo","workflow_id"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("PUT", ("https://api.github.com/repos/" + String(args["owner"]) + "/" + String(args["repo"]) + "/actions/workflows/" + String(args["workflow_id"]) + "/disable") + _qs, {}, undefined);
    }
  },
  {
    name: "actions/create-workflow-dispatch",
    description: "Create a workflow dispatch event. You can use this endpoint to manually trigger a GitHub Actions workflow run. Params: owner (required), repo (required), workflow_id (required).",
    inputSchema: {"type":"object","properties":{"owner":{"type":"string","description":"The account owner of the repository. The name is not case sensitive."},"repo":{"type":"string","description":"The name of the repository without the `.git` extension. The name is not case sensitive."},"workflow_id":{"oneOf":[{"type":"integer"},{"type":"string"}],"description":"The ID of the workflow. You can also pass the workflow file name as a string."},"body":{"type":"object","description":"Request body"}},"required":["owner","repo","workflow_id","body"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("POST", ("https://api.github.com/repos/" + String(args["owner"]) + "/" + String(args["repo"]) + "/actions/workflows/" + String(args["workflow_id"]) + "/dispatches") + _qs, {}, args["body"]);
    }
  },
  {
    name: "actions/enable-workflow",
    description: "Enable a workflow. Enables a workflow and sets the `state` of the workflow to `active`. Params: owner (required), repo (required), workflow_id (required).",
    inputSchema: {"type":"object","properties":{"owner":{"type":"string","description":"The account owner of the repository. The name is not case sensitive."},"repo":{"type":"string","description":"The name of the repository without the `.git` extension. The name is not case sensitive."},"workflow_id":{"oneOf":[{"type":"integer"},{"type":"string"}],"description":"The ID of the workflow. You can also pass the workflow file name as a string."}},"required":["owner","repo","workflow_id"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("PUT", ("https://api.github.com/repos/" + String(args["owner"]) + "/" + String(args["repo"]) + "/actions/workflows/" + String(args["workflow_id"]) + "/enable") + _qs, {}, undefined);
    }
  },
  {
    name: "actions/list-workflow-runs",
    description: "List workflow runs for a workflow. List all workflow runs for a workflow. Params: owner (required), repo (required), workflow_id (required).",
    inputSchema: {"type":"object","properties":{"owner":{"type":"string","description":"The account owner of the repository. The name is not case sensitive."},"repo":{"type":"string","description":"The name of the repository without the `.git` extension. The name is not case sensitive."},"workflow_id":{"oneOf":[{"type":"integer"},{"type":"string"}],"description":"The ID of the workflow. You can also pass the workflow file name as a string."},"actor":{"type":"string","description":"Returns someone's workflow runs. Use the login for the user who created the `push` associated with the check suite or workflow run."},"branch":{"type":"string","description":"Returns workflow runs associated with a branch. Use the name of the branch of the `push`."},"event":{"type":"string","description":"Returns workflow run triggered by the event you specify. For example, `push`, `pull_request` or `issue`. For more information, see \"[Events that trigger workflows](https://docs.github.com/actions/automating-your-workflow-with-github-actions/events-that-trigger-workflows).\""},"status":{"type":"string","enum":["completed","action_required","cancelled","failure","neutral","skipped","stale","success","timed_out","in_progress","queued","requested","waiting","pending"],"description":"Returns workflow runs with the check run `status` or `conclusion` that you specify. For example, a conclusion can be `success` or a status can be `in_progress`. Only GitHub Actions can set a status of `waiting`, `pending`, or `requested`."},"per_page":{"type":"integer","default":30,"description":"The number of results per page (max 100). For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""},"page":{"type":"integer","default":1,"description":"The page number of the results to fetch. For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""},"created":{"type":"string","format":"date-time","description":"Returns workflow runs created within the given date-time range. For more information on the syntax, see \"[Understanding the search syntax](https://docs.github.com/search-github/getting-started-with-searching-on-github/understanding-the-search-syntax#query-for-dates).\""},"exclude_pull_requests":{"type":"boolean","default":false,"description":"If `true` pull requests are omitted from the response (empty array)."},"check_suite_id":{"type":"integer","description":"Returns workflow runs with the `check_suite_id` that you specify."},"head_sha":{"type":"string","description":"Only returns workflow runs that are associated with the specified `head_sha`."}},"required":["owner","repo","workflow_id"]},
    call: function(args) {
    var _q = new URLSearchParams();
    if (args["actor"] !== undefined) _q.set("actor", String(args["actor"]));
    if (args["branch"] !== undefined) _q.set("branch", String(args["branch"]));
    if (args["event"] !== undefined) _q.set("event", String(args["event"]));
    if (args["status"] !== undefined) _q.set("status", String(args["status"]));
    if (args["per_page"] !== undefined) _q.set("per_page", String(args["per_page"]));
    if (args["page"] !== undefined) _q.set("page", String(args["page"]));
    if (args["created"] !== undefined) _q.set("created", String(args["created"]));
    if (args["exclude_pull_requests"] !== undefined) _q.set("exclude_pull_requests", String(args["exclude_pull_requests"]));
    if (args["check_suite_id"] !== undefined) _q.set("check_suite_id", String(args["check_suite_id"]));
    if (args["head_sha"] !== undefined) _q.set("head_sha", String(args["head_sha"]));
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/repos/" + String(args["owner"]) + "/" + String(args["repo"]) + "/actions/workflows/" + String(args["workflow_id"]) + "/runs") + _qs, {}, undefined);
    }
  },
  {
    name: "actions/get-workflow-usage",
    description: "Get workflow usage. > [!WARNING]  \n> This endpoint is in the process of closing down. Params: owner (required), repo (required), workflow_id (required).",
    inputSchema: {"type":"object","properties":{"owner":{"type":"string","description":"The account owner of the repository. The name is not case sensitive."},"repo":{"type":"string","description":"The name of the repository without the `.git` extension. The name is not case sensitive."},"workflow_id":{"oneOf":[{"type":"integer"},{"type":"string"}],"description":"The ID of the workflow. You can also pass the workflow file name as a string."}},"required":["owner","repo","workflow_id"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/repos/" + String(args["owner"]) + "/" + String(args["repo"]) + "/actions/workflows/" + String(args["workflow_id"]) + "/timing") + _qs, {}, undefined);
    }
  },
  {
    name: "repos/list-activities",
    description: "List repository activities. Lists a detailed history of changes to a repository, such as pushes, merges, force pushes, and branch changes, and associates these changes with commits and users. Params: owner (required), repo (required).",
    inputSchema: {"type":"object","properties":{"owner":{"type":"string","description":"The account owner of the repository. The name is not case sensitive."},"repo":{"type":"string","description":"The name of the repository without the `.git` extension. The name is not case sensitive."},"direction":{"type":"string","enum":["asc","desc"],"default":"desc","description":"The direction to sort the results by."},"per_page":{"type":"integer","default":30,"description":"The number of results per page (max 100). For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""},"before":{"type":"string","description":"A cursor, as given in the [Link header](https://docs.github.com/rest/guides/using-pagination-in-the-rest-api#using-link-headers). If specified, the query only searches for results before this cursor. For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""},"after":{"type":"string","description":"A cursor, as given in the [Link header](https://docs.github.com/rest/guides/using-pagination-in-the-rest-api#using-link-headers). If specified, the query only searches for results after this cursor. For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""},"ref":{"type":"string","description":"The Git reference for the activities you want to list.\n\nThe `ref` for a branch can be formatted either as `refs/heads/BRANCH_NAME` or `BRANCH_NAME`, where `BRANCH_NAME` is the name of your branch."},"actor":{"type":"string","description":"The GitHub username to use to filter by the actor who performed the activity."},"time_period":{"type":"string","enum":["day","week","month","quarter","year"],"description":"The time period to filter by.\n\nFor example, `day` will filter for activity that occurred in the past 24 hours, and `week` will filter for activity that occurred in the past 7 days (168 hours)."},"activity_type":{"type":"string","enum":["push","force_push","branch_creation","branch_deletion","pr_merge","merge_queue_merge"],"description":"The activity type to filter by.\n\nFor example, you can choose to filter by \"force_push\", to see all force pushes to the repository."}},"required":["owner","repo"]},
    call: function(args) {
    var _q = new URLSearchParams();
    if (args["direction"] !== undefined) _q.set("direction", String(args["direction"]));
    if (args["per_page"] !== undefined) _q.set("per_page", String(args["per_page"]));
    if (args["before"] !== undefined) _q.set("before", String(args["before"]));
    if (args["after"] !== undefined) _q.set("after", String(args["after"]));
    if (args["ref"] !== undefined) _q.set("ref", String(args["ref"]));
    if (args["actor"] !== undefined) _q.set("actor", String(args["actor"]));
    if (args["time_period"] !== undefined) _q.set("time_period", String(args["time_period"]));
    if (args["activity_type"] !== undefined) _q.set("activity_type", String(args["activity_type"]));
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/repos/" + String(args["owner"]) + "/" + String(args["repo"]) + "/activity") + _qs, {}, undefined);
    }
  },
  {
    name: "agents/list-repo-organization-secrets",
    description: "List repository organization secrets. Lists all organization secrets shared with a repository without revealing their encrypted\nvalues. Params: owner (required), repo (required).",
    inputSchema: {"type":"object","properties":{"owner":{"type":"string","description":"The account owner of the repository. The name is not case sensitive."},"repo":{"type":"string","description":"The name of the repository without the `.git` extension. The name is not case sensitive."},"per_page":{"type":"integer","default":30,"description":"The number of results per page (max 100). For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""},"page":{"type":"integer","default":1,"description":"The page number of the results to fetch. For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""}},"required":["owner","repo"]},
    call: function(args) {
    var _q = new URLSearchParams();
    if (args["per_page"] !== undefined) _q.set("per_page", String(args["per_page"]));
    if (args["page"] !== undefined) _q.set("page", String(args["page"]));
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/repos/" + String(args["owner"]) + "/" + String(args["repo"]) + "/agents/organization-secrets") + _qs, {}, undefined);
    }
  },
  {
    name: "agents/list-repo-organization-variables",
    description: "List repository organization variables. Lists all organization variables shared with a repository. Params: owner (required), repo (required).",
    inputSchema: {"type":"object","properties":{"owner":{"type":"string","description":"The account owner of the repository. The name is not case sensitive."},"repo":{"type":"string","description":"The name of the repository without the `.git` extension. The name is not case sensitive."},"per_page":{"type":"integer","default":10,"description":"The number of results per page (max 30). For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""},"page":{"type":"integer","default":1,"description":"The page number of the results to fetch. For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""}},"required":["owner","repo"]},
    call: function(args) {
    var _q = new URLSearchParams();
    if (args["per_page"] !== undefined) _q.set("per_page", String(args["per_page"]));
    if (args["page"] !== undefined) _q.set("page", String(args["page"]));
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/repos/" + String(args["owner"]) + "/" + String(args["repo"]) + "/agents/organization-variables") + _qs, {}, undefined);
    }
  },
  {
    name: "agents/list-repo-secrets",
    description: "List repository secrets. Lists all secrets available in a repository without revealing their encrypted\nvalues. Params: owner (required), repo (required).",
    inputSchema: {"type":"object","properties":{"owner":{"type":"string","description":"The account owner of the repository. The name is not case sensitive."},"repo":{"type":"string","description":"The name of the repository without the `.git` extension. The name is not case sensitive."},"per_page":{"type":"integer","default":30,"description":"The number of results per page (max 100). For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""},"page":{"type":"integer","default":1,"description":"The page number of the results to fetch. For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""}},"required":["owner","repo"]},
    call: function(args) {
    var _q = new URLSearchParams();
    if (args["per_page"] !== undefined) _q.set("per_page", String(args["per_page"]));
    if (args["page"] !== undefined) _q.set("page", String(args["page"]));
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/repos/" + String(args["owner"]) + "/" + String(args["repo"]) + "/agents/secrets") + _qs, {}, undefined);
    }
  },
  {
    name: "agents/get-repo-public-key",
    description: "Get a repository public key. Gets your public key, which you need to encrypt secrets. Params: owner (required), repo (required).",
    inputSchema: {"type":"object","properties":{"owner":{"type":"string","description":"The account owner of the repository. The name is not case sensitive."},"repo":{"type":"string","description":"The name of the repository without the `.git` extension. The name is not case sensitive."}},"required":["owner","repo"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/repos/" + String(args["owner"]) + "/" + String(args["repo"]) + "/agents/secrets/public-key") + _qs, {}, undefined);
    }
  },
  {
    name: "agents/get-repo-secret",
    description: "Get a repository secret. Gets a single repository secret without revealing its encrypted value. Params: owner (required), repo (required), secret_name (required).",
    inputSchema: {"type":"object","properties":{"owner":{"type":"string","description":"The account owner of the repository. The name is not case sensitive."},"repo":{"type":"string","description":"The name of the repository without the `.git` extension. The name is not case sensitive."},"secret_name":{"type":"string","description":"The name of the secret."}},"required":["owner","repo","secret_name"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/repos/" + String(args["owner"]) + "/" + String(args["repo"]) + "/agents/secrets/" + String(args["secret_name"])) + _qs, {}, undefined);
    }
  },
  {
    name: "agents/create-or-update-repo-secret",
    description: "Create or update a repository secret. Creates or updates a repository secret with an encrypted value. Params: owner (required), repo (required), secret_name (required).",
    inputSchema: {"type":"object","properties":{"owner":{"type":"string","description":"The account owner of the repository. The name is not case sensitive."},"repo":{"type":"string","description":"The name of the repository without the `.git` extension. The name is not case sensitive."},"secret_name":{"type":"string","description":"The name of the secret."},"body":{"type":"object","description":"Request body"}},"required":["owner","repo","secret_name","body"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("PUT", ("https://api.github.com/repos/" + String(args["owner"]) + "/" + String(args["repo"]) + "/agents/secrets/" + String(args["secret_name"])) + _qs, {}, args["body"]);
    }
  },
  {
    name: "agents/delete-repo-secret",
    description: "Delete a repository secret. Deletes a secret in a repository using the secret name. Params: owner (required), repo (required), secret_name (required).",
    inputSchema: {"type":"object","properties":{"owner":{"type":"string","description":"The account owner of the repository. The name is not case sensitive."},"repo":{"type":"string","description":"The name of the repository without the `.git` extension. The name is not case sensitive."},"secret_name":{"type":"string","description":"The name of the secret."}},"required":["owner","repo","secret_name"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("DELETE", ("https://api.github.com/repos/" + String(args["owner"]) + "/" + String(args["repo"]) + "/agents/secrets/" + String(args["secret_name"])) + _qs, {}, undefined);
    }
  },
  {
    name: "agents/list-repo-variables",
    description: "List repository variables. Lists all repository variables. Params: owner (required), repo (required).",
    inputSchema: {"type":"object","properties":{"owner":{"type":"string","description":"The account owner of the repository. The name is not case sensitive."},"repo":{"type":"string","description":"The name of the repository without the `.git` extension. The name is not case sensitive."},"per_page":{"type":"integer","default":10,"description":"The number of results per page (max 30). For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""},"page":{"type":"integer","default":1,"description":"The page number of the results to fetch. For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""}},"required":["owner","repo"]},
    call: function(args) {
    var _q = new URLSearchParams();
    if (args["per_page"] !== undefined) _q.set("per_page", String(args["per_page"]));
    if (args["page"] !== undefined) _q.set("page", String(args["page"]));
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/repos/" + String(args["owner"]) + "/" + String(args["repo"]) + "/agents/variables") + _qs, {}, undefined);
    }
  },
  {
    name: "agents/create-repo-variable",
    description: "Create a repository variable. Creates a repository variable that you can reference in a GitHub Actions workflow. Params: owner (required), repo (required).",
    inputSchema: {"type":"object","properties":{"owner":{"type":"string","description":"The account owner of the repository. The name is not case sensitive."},"repo":{"type":"string","description":"The name of the repository without the `.git` extension. The name is not case sensitive."},"body":{"type":"object","description":"Request body"}},"required":["owner","repo","body"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("POST", ("https://api.github.com/repos/" + String(args["owner"]) + "/" + String(args["repo"]) + "/agents/variables") + _qs, {}, args["body"]);
    }
  },
  {
    name: "agents/get-repo-variable",
    description: "Get a repository variable. Gets a specific variable in a repository. Params: owner (required), repo (required), name (required).",
    inputSchema: {"type":"object","properties":{"owner":{"type":"string","description":"The account owner of the repository. The name is not case sensitive."},"repo":{"type":"string","description":"The name of the repository without the `.git` extension. The name is not case sensitive."},"name":{"type":"string","description":"The name of the variable."}},"required":["owner","repo","name"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/repos/" + String(args["owner"]) + "/" + String(args["repo"]) + "/agents/variables/" + String(args["name"])) + _qs, {}, undefined);
    }
  },
  {
    name: "agents/update-repo-variable",
    description: "Update a repository variable. Updates a repository variable that you can reference in a GitHub Actions workflow. Params: owner (required), repo (required), name (required).",
    inputSchema: {"type":"object","properties":{"owner":{"type":"string","description":"The account owner of the repository. The name is not case sensitive."},"repo":{"type":"string","description":"The name of the repository without the `.git` extension. The name is not case sensitive."},"name":{"type":"string","description":"The name of the variable."},"body":{"type":"object","description":"Request body"}},"required":["owner","repo","name","body"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("PATCH", ("https://api.github.com/repos/" + String(args["owner"]) + "/" + String(args["repo"]) + "/agents/variables/" + String(args["name"])) + _qs, {}, args["body"]);
    }
  },
  {
    name: "agents/delete-repo-variable",
    description: "Delete a repository variable. Deletes a repository variable using the variable name. Params: owner (required), repo (required), name (required).",
    inputSchema: {"type":"object","properties":{"owner":{"type":"string","description":"The account owner of the repository. The name is not case sensitive."},"repo":{"type":"string","description":"The name of the repository without the `.git` extension. The name is not case sensitive."},"name":{"type":"string","description":"The name of the variable."}},"required":["owner","repo","name"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("DELETE", ("https://api.github.com/repos/" + String(args["owner"]) + "/" + String(args["repo"]) + "/agents/variables/" + String(args["name"])) + _qs, {}, undefined);
    }
  },
  {
    name: "issues/list-assignees",
    description: "List assignees. Lists the [available assignees](https://docs.github.com/articles/assigning-issues-and-pull-requests-to-other-github-users/) for issues in a repository. Params: owner (required), repo (required).",
    inputSchema: {"type":"object","properties":{"owner":{"type":"string","description":"The account owner of the repository. The name is not case sensitive."},"repo":{"type":"string","description":"The name of the repository without the `.git` extension. The name is not case sensitive."},"per_page":{"type":"integer","default":30,"description":"The number of results per page (max 100). For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""},"page":{"type":"integer","default":1,"description":"The page number of the results to fetch. For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""}},"required":["owner","repo"]},
    call: function(args) {
    var _q = new URLSearchParams();
    if (args["per_page"] !== undefined) _q.set("per_page", String(args["per_page"]));
    if (args["page"] !== undefined) _q.set("page", String(args["page"]));
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/repos/" + String(args["owner"]) + "/" + String(args["repo"]) + "/assignees") + _qs, {}, undefined);
    }
  },
  {
    name: "issues/check-user-can-be-assigned",
    description: "Check if a user can be assigned. Checks if a user has permission to be assigned to an issue in this repository. Params: owner (required), repo (required), assignee (required).",
    inputSchema: {"type":"object","properties":{"owner":{"type":"string","description":"The account owner of the repository. The name is not case sensitive."},"repo":{"type":"string","description":"The name of the repository without the `.git` extension. The name is not case sensitive."},"assignee":{"type":"string"}},"required":["owner","repo","assignee"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/repos/" + String(args["owner"]) + "/" + String(args["repo"]) + "/assignees/" + String(args["assignee"])) + _qs, {}, undefined);
    }
  },
  {
    name: "repos/create-attestation",
    description: "Create an attestation. Store an artifact attestation and associate it with a repository. Params: owner (required), repo (required).",
    inputSchema: {"type":"object","properties":{"owner":{"type":"string","description":"The account owner of the repository. The name is not case sensitive."},"repo":{"type":"string","description":"The name of the repository without the `.git` extension. The name is not case sensitive."},"body":{"type":"object","description":"Request body"}},"required":["owner","repo","body"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("POST", ("https://api.github.com/repos/" + String(args["owner"]) + "/" + String(args["repo"]) + "/attestations") + _qs, {}, args["body"]);
    }
  },
  {
    name: "repos/list-attestations",
    description: "List attestations. List a collection of artifact attestations with a given subject digest that are associated with a repository. Params: owner (required), repo (required), subject_digest (required).",
    inputSchema: {"type":"object","properties":{"owner":{"type":"string","description":"The account owner of the repository. The name is not case sensitive."},"repo":{"type":"string","description":"The name of the repository without the `.git` extension. The name is not case sensitive."},"per_page":{"type":"integer","default":30,"description":"The number of results per page (max 100). For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""},"before":{"type":"string","description":"A cursor, as given in the [Link header](https://docs.github.com/rest/guides/using-pagination-in-the-rest-api#using-link-headers). If specified, the query only searches for results before this cursor. For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""},"after":{"type":"string","description":"A cursor, as given in the [Link header](https://docs.github.com/rest/guides/using-pagination-in-the-rest-api#using-link-headers). If specified, the query only searches for results after this cursor. For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""},"subject_digest":{"type":"string","description":"The parameter should be set to the attestation's subject's SHA256 digest, in the form `sha256:HEX_DIGEST`."},"predicate_type":{"type":"string","description":"Optional filter for fetching attestations with a given predicate type.\nThis option accepts `provenance`, `sbom`, `release`, or freeform text\nfor custom predicate types."}},"required":["owner","repo","subject_digest"]},
    call: function(args) {
    var _q = new URLSearchParams();
    if (args["per_page"] !== undefined) _q.set("per_page", String(args["per_page"]));
    if (args["before"] !== undefined) _q.set("before", String(args["before"]));
    if (args["after"] !== undefined) _q.set("after", String(args["after"]));
    if (args["predicate_type"] !== undefined) _q.set("predicate_type", String(args["predicate_type"]));
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/repos/" + String(args["owner"]) + "/" + String(args["repo"]) + "/attestations/" + String(args["subject_digest"])) + _qs, {}, undefined);
    }
  },
  {
    name: "repos/list-autolinks",
    description: "Get all autolinks of a repository. Gets all autolinks that are configured for a repository. Params: owner (required), repo (required).",
    inputSchema: {"type":"object","properties":{"owner":{"type":"string","description":"The account owner of the repository. The name is not case sensitive."},"repo":{"type":"string","description":"The name of the repository without the `.git` extension. The name is not case sensitive."}},"required":["owner","repo"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/repos/" + String(args["owner"]) + "/" + String(args["repo"]) + "/autolinks") + _qs, {}, undefined);
    }
  },
  {
    name: "repos/create-autolink",
    description: "Create an autolink reference for a repository. Users with admin access to the repository can create an autolink. Params: owner (required), repo (required).",
    inputSchema: {"type":"object","properties":{"owner":{"type":"string","description":"The account owner of the repository. The name is not case sensitive."},"repo":{"type":"string","description":"The name of the repository without the `.git` extension. The name is not case sensitive."},"body":{"type":"object","description":"Request body"}},"required":["owner","repo","body"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("POST", ("https://api.github.com/repos/" + String(args["owner"]) + "/" + String(args["repo"]) + "/autolinks") + _qs, {}, args["body"]);
    }
  },
  {
    name: "repos/get-autolink",
    description: "Get an autolink reference of a repository. This returns a single autolink reference by ID that was configured for the given repository. Params: owner (required), repo (required), autolink_id (required).",
    inputSchema: {"type":"object","properties":{"owner":{"type":"string","description":"The account owner of the repository. The name is not case sensitive."},"repo":{"type":"string","description":"The name of the repository without the `.git` extension. The name is not case sensitive."},"autolink_id":{"type":"integer","description":"The unique identifier of the autolink."}},"required":["owner","repo","autolink_id"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/repos/" + String(args["owner"]) + "/" + String(args["repo"]) + "/autolinks/" + String(args["autolink_id"])) + _qs, {}, undefined);
    }
  },
  {
    name: "repos/delete-autolink",
    description: "Delete an autolink reference from a repository. This deletes a single autolink reference by ID that was configured for the given repository. Params: owner (required), repo (required), autolink_id (required).",
    inputSchema: {"type":"object","properties":{"owner":{"type":"string","description":"The account owner of the repository. The name is not case sensitive."},"repo":{"type":"string","description":"The name of the repository without the `.git` extension. The name is not case sensitive."},"autolink_id":{"type":"integer","description":"The unique identifier of the autolink."}},"required":["owner","repo","autolink_id"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("DELETE", ("https://api.github.com/repos/" + String(args["owner"]) + "/" + String(args["repo"]) + "/autolinks/" + String(args["autolink_id"])) + _qs, {}, undefined);
    }
  },
  {
    name: "repos/check-automated-security-fixes",
    description: "Check if Dependabot security updates are enabled for a repository. Shows whether Dependabot security updates are enabled, disabled or paused for a repository. Params: owner (required), repo (required).",
    inputSchema: {"type":"object","properties":{"owner":{"type":"string","description":"The account owner of the repository. The name is not case sensitive."},"repo":{"type":"string","description":"The name of the repository without the `.git` extension. The name is not case sensitive."}},"required":["owner","repo"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/repos/" + String(args["owner"]) + "/" + String(args["repo"]) + "/automated-security-fixes") + _qs, {}, undefined);
    }
  },
  {
    name: "repos/enable-automated-security-fixes",
    description: "Enable Dependabot security updates. Enables Dependabot security updates for a repository. Params: owner (required), repo (required).",
    inputSchema: {"type":"object","properties":{"owner":{"type":"string","description":"The account owner of the repository. The name is not case sensitive."},"repo":{"type":"string","description":"The name of the repository without the `.git` extension. The name is not case sensitive."}},"required":["owner","repo"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("PUT", ("https://api.github.com/repos/" + String(args["owner"]) + "/" + String(args["repo"]) + "/automated-security-fixes") + _qs, {}, undefined);
    }
  },
  {
    name: "repos/disable-automated-security-fixes",
    description: "Disable Dependabot security updates. Disables Dependabot security updates for a repository. Params: owner (required), repo (required).",
    inputSchema: {"type":"object","properties":{"owner":{"type":"string","description":"The account owner of the repository. The name is not case sensitive."},"repo":{"type":"string","description":"The name of the repository without the `.git` extension. The name is not case sensitive."}},"required":["owner","repo"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("DELETE", ("https://api.github.com/repos/" + String(args["owner"]) + "/" + String(args["repo"]) + "/automated-security-fixes") + _qs, {}, undefined);
    }
  },
  {
    name: "repos/list-branches",
    description: "List branches. Params: owner (required), repo (required).",
    inputSchema: {"type":"object","properties":{"owner":{"type":"string","description":"The account owner of the repository. The name is not case sensitive."},"repo":{"type":"string","description":"The name of the repository without the `.git` extension. The name is not case sensitive."},"protected":{"type":"boolean","description":"Setting to `true` returns only branches protected by branch protections or rulesets. When set to `false`, only unprotected branches are returned. Omitting this parameter returns all branches."},"per_page":{"type":"integer","default":30,"description":"The number of results per page (max 100). For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""},"page":{"type":"integer","default":1,"description":"The page number of the results to fetch. For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""}},"required":["owner","repo"]},
    call: function(args) {
    var _q = new URLSearchParams();
    if (args["protected"] !== undefined) _q.set("protected", String(args["protected"]));
    if (args["per_page"] !== undefined) _q.set("per_page", String(args["per_page"]));
    if (args["page"] !== undefined) _q.set("page", String(args["page"]));
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/repos/" + String(args["owner"]) + "/" + String(args["repo"]) + "/branches") + _qs, {}, undefined);
    }
  },
  {
    name: "repos/get-branch",
    description: "Get a branch. Params: owner (required), repo (required), branch (required).",
    inputSchema: {"type":"object","properties":{"owner":{"type":"string","description":"The account owner of the repository. The name is not case sensitive."},"repo":{"type":"string","description":"The name of the repository without the `.git` extension. The name is not case sensitive."},"branch":{"type":"string","description":"The name of the branch. Cannot contain wildcard characters. To use wildcard characters in branch names, use [the GraphQL API](https://docs.github.com/graphql)."}},"required":["owner","repo","branch"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/repos/" + String(args["owner"]) + "/" + String(args["repo"]) + "/branches/" + String(args["branch"])) + _qs, {}, undefined);
    }
  },
  {
    name: "repos/get-branch-protection",
    description: "Get branch protection. Protected branches are available in public repositories with GitHub Free and GitHub Free for organizations, and in public and private repositories with GitHub Pro, GitHub Team, GitHub Enterprise Cloud, and GitHub Enterprise Server. Params: owner (required), repo (required), branch (required).",
    inputSchema: {"type":"object","properties":{"owner":{"type":"string","description":"The account owner of the repository. The name is not case sensitive."},"repo":{"type":"string","description":"The name of the repository without the `.git` extension. The name is not case sensitive."},"branch":{"type":"string","description":"The name of the branch. Cannot contain wildcard characters. To use wildcard characters in branch names, use [the GraphQL API](https://docs.github.com/graphql)."}},"required":["owner","repo","branch"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/repos/" + String(args["owner"]) + "/" + String(args["repo"]) + "/branches/" + String(args["branch"]) + "/protection") + _qs, {}, undefined);
    }
  },
  {
    name: "repos/update-branch-protection",
    description: "Update branch protection. Protected branches are available in public repositories with GitHub Free and GitHub Free for organizations, and in public and private repositories with GitHub Pro, GitHub Team, GitHub Enterprise Cloud, and GitHub Enterprise Server. Params: owner (required), repo (required), branch (required).",
    inputSchema: {"type":"object","properties":{"owner":{"type":"string","description":"The account owner of the repository. The name is not case sensitive."},"repo":{"type":"string","description":"The name of the repository without the `.git` extension. The name is not case sensitive."},"branch":{"type":"string","description":"The name of the branch. Cannot contain wildcard characters. To use wildcard characters in branch names, use [the GraphQL API](https://docs.github.com/graphql)."},"body":{"type":"object","description":"Request body"}},"required":["owner","repo","branch","body"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("PUT", ("https://api.github.com/repos/" + String(args["owner"]) + "/" + String(args["repo"]) + "/branches/" + String(args["branch"]) + "/protection") + _qs, {}, args["body"]);
    }
  },
  {
    name: "repos/delete-branch-protection",
    description: "Delete branch protection. Protected branches are available in public repositories with GitHub Free and GitHub Free for organizations, and in public and private repositories with GitHub Pro, GitHub Team, GitHub Enterprise Cloud, and GitHub Enterprise Server. Params: owner (required), repo (required), branch (required).",
    inputSchema: {"type":"object","properties":{"owner":{"type":"string","description":"The account owner of the repository. The name is not case sensitive."},"repo":{"type":"string","description":"The name of the repository without the `.git` extension. The name is not case sensitive."},"branch":{"type":"string","description":"The name of the branch. Cannot contain wildcard characters. To use wildcard characters in branch names, use [the GraphQL API](https://docs.github.com/graphql)."}},"required":["owner","repo","branch"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("DELETE", ("https://api.github.com/repos/" + String(args["owner"]) + "/" + String(args["repo"]) + "/branches/" + String(args["branch"]) + "/protection") + _qs, {}, undefined);
    }
  },
  {
    name: "repos/get-admin-branch-protection",
    description: "Get admin branch protection. Protected branches are available in public repositories with GitHub Free and GitHub Free for organizations, and in public and private repositories with GitHub Pro, GitHub Team, GitHub Enterprise Cloud, and GitHub Enterprise Server. Params: owner (required), repo (required), branch (required).",
    inputSchema: {"type":"object","properties":{"owner":{"type":"string","description":"The account owner of the repository. The name is not case sensitive."},"repo":{"type":"string","description":"The name of the repository without the `.git` extension. The name is not case sensitive."},"branch":{"type":"string","description":"The name of the branch. Cannot contain wildcard characters. To use wildcard characters in branch names, use [the GraphQL API](https://docs.github.com/graphql)."}},"required":["owner","repo","branch"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/repos/" + String(args["owner"]) + "/" + String(args["repo"]) + "/branches/" + String(args["branch"]) + "/protection/enforce_admins") + _qs, {}, undefined);
    }
  },
  {
    name: "repos/set-admin-branch-protection",
    description: "Set admin branch protection. Protected branches are available in public repositories with GitHub Free and GitHub Free for organizations, and in public and private repositories with GitHub Pro, GitHub Team, GitHub Enterprise Cloud, and GitHub Enterprise Server. Params: owner (required), repo (required), branch (required).",
    inputSchema: {"type":"object","properties":{"owner":{"type":"string","description":"The account owner of the repository. The name is not case sensitive."},"repo":{"type":"string","description":"The name of the repository without the `.git` extension. The name is not case sensitive."},"branch":{"type":"string","description":"The name of the branch. Cannot contain wildcard characters. To use wildcard characters in branch names, use [the GraphQL API](https://docs.github.com/graphql)."}},"required":["owner","repo","branch"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("POST", ("https://api.github.com/repos/" + String(args["owner"]) + "/" + String(args["repo"]) + "/branches/" + String(args["branch"]) + "/protection/enforce_admins") + _qs, {}, undefined);
    }
  },
  {
    name: "repos/delete-admin-branch-protection",
    description: "Delete admin branch protection. Protected branches are available in public repositories with GitHub Free and GitHub Free for organizations, and in public and private repositories with GitHub Pro, GitHub Team, GitHub Enterprise Cloud, and GitHub Enterprise Server. Params: owner (required), repo (required), branch (required).",
    inputSchema: {"type":"object","properties":{"owner":{"type":"string","description":"The account owner of the repository. The name is not case sensitive."},"repo":{"type":"string","description":"The name of the repository without the `.git` extension. The name is not case sensitive."},"branch":{"type":"string","description":"The name of the branch. Cannot contain wildcard characters. To use wildcard characters in branch names, use [the GraphQL API](https://docs.github.com/graphql)."}},"required":["owner","repo","branch"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("DELETE", ("https://api.github.com/repos/" + String(args["owner"]) + "/" + String(args["repo"]) + "/branches/" + String(args["branch"]) + "/protection/enforce_admins") + _qs, {}, undefined);
    }
  },
  {
    name: "repos/get-pull-request-review-protection",
    description: "Get pull request review protection. Protected branches are available in public repositories with GitHub Free and GitHub Free for organizations, and in public and private repositories with GitHub Pro, GitHub Team, GitHub Enterprise Cloud, and GitHub Enterprise Server. Params: owner (required), repo (required), branch (required).",
    inputSchema: {"type":"object","properties":{"owner":{"type":"string","description":"The account owner of the repository. The name is not case sensitive."},"repo":{"type":"string","description":"The name of the repository without the `.git` extension. The name is not case sensitive."},"branch":{"type":"string","description":"The name of the branch. Cannot contain wildcard characters. To use wildcard characters in branch names, use [the GraphQL API](https://docs.github.com/graphql)."}},"required":["owner","repo","branch"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/repos/" + String(args["owner"]) + "/" + String(args["repo"]) + "/branches/" + String(args["branch"]) + "/protection/required_pull_request_reviews") + _qs, {}, undefined);
    }
  },
  {
    name: "repos/update-pull-request-review-protection",
    description: "Update pull request review protection. Protected branches are available in public repositories with GitHub Free and GitHub Free for organizations, and in public and private repositories with GitHub Pro, GitHub Team, GitHub Enterprise Cloud, and GitHub Enterprise Server. Params: owner (required), repo (required), branch (required).",
    inputSchema: {"type":"object","properties":{"owner":{"type":"string","description":"The account owner of the repository. The name is not case sensitive."},"repo":{"type":"string","description":"The name of the repository without the `.git` extension. The name is not case sensitive."},"branch":{"type":"string","description":"The name of the branch. Cannot contain wildcard characters. To use wildcard characters in branch names, use [the GraphQL API](https://docs.github.com/graphql)."},"body":{"type":"object","description":"Request body"}},"required":["owner","repo","branch"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("PATCH", ("https://api.github.com/repos/" + String(args["owner"]) + "/" + String(args["repo"]) + "/branches/" + String(args["branch"]) + "/protection/required_pull_request_reviews") + _qs, {}, args["body"]);
    }
  },
  {
    name: "repos/delete-pull-request-review-protection",
    description: "Delete pull request review protection. Protected branches are available in public repositories with GitHub Free and GitHub Free for organizations, and in public and private repositories with GitHub Pro, GitHub Team, GitHub Enterprise Cloud, and GitHub Enterprise Server. Params: owner (required), repo (required), branch (required).",
    inputSchema: {"type":"object","properties":{"owner":{"type":"string","description":"The account owner of the repository. The name is not case sensitive."},"repo":{"type":"string","description":"The name of the repository without the `.git` extension. The name is not case sensitive."},"branch":{"type":"string","description":"The name of the branch. Cannot contain wildcard characters. To use wildcard characters in branch names, use [the GraphQL API](https://docs.github.com/graphql)."}},"required":["owner","repo","branch"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("DELETE", ("https://api.github.com/repos/" + String(args["owner"]) + "/" + String(args["repo"]) + "/branches/" + String(args["branch"]) + "/protection/required_pull_request_reviews") + _qs, {}, undefined);
    }
  },
  {
    name: "repos/get-commit-signature-protection",
    description: "Get commit signature protection. Protected branches are available in public repositories with GitHub Free and GitHub Free for organizations, and in public and private repositories with GitHub Pro, GitHub Team, GitHub Enterprise Cloud, and GitHub Enterprise Server. Params: owner (required), repo (required), branch (required).",
    inputSchema: {"type":"object","properties":{"owner":{"type":"string","description":"The account owner of the repository. The name is not case sensitive."},"repo":{"type":"string","description":"The name of the repository without the `.git` extension. The name is not case sensitive."},"branch":{"type":"string","description":"The name of the branch. Cannot contain wildcard characters. To use wildcard characters in branch names, use [the GraphQL API](https://docs.github.com/graphql)."}},"required":["owner","repo","branch"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/repos/" + String(args["owner"]) + "/" + String(args["repo"]) + "/branches/" + String(args["branch"]) + "/protection/required_signatures") + _qs, {}, undefined);
    }
  },
  {
    name: "repos/create-commit-signature-protection",
    description: "Create commit signature protection. Protected branches are available in public repositories with GitHub Free and GitHub Free for organizations, and in public and private repositories with GitHub Pro, GitHub Team, GitHub Enterprise Cloud, and GitHub Enterprise Server. Params: owner (required), repo (required), branch (required).",
    inputSchema: {"type":"object","properties":{"owner":{"type":"string","description":"The account owner of the repository. The name is not case sensitive."},"repo":{"type":"string","description":"The name of the repository without the `.git` extension. The name is not case sensitive."},"branch":{"type":"string","description":"The name of the branch. Cannot contain wildcard characters. To use wildcard characters in branch names, use [the GraphQL API](https://docs.github.com/graphql)."}},"required":["owner","repo","branch"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("POST", ("https://api.github.com/repos/" + String(args["owner"]) + "/" + String(args["repo"]) + "/branches/" + String(args["branch"]) + "/protection/required_signatures") + _qs, {}, undefined);
    }
  },
  {
    name: "repos/delete-commit-signature-protection",
    description: "Delete commit signature protection. Protected branches are available in public repositories with GitHub Free and GitHub Free for organizations, and in public and private repositories with GitHub Pro, GitHub Team, GitHub Enterprise Cloud, and GitHub Enterprise Server. Params: owner (required), repo (required), branch (required).",
    inputSchema: {"type":"object","properties":{"owner":{"type":"string","description":"The account owner of the repository. The name is not case sensitive."},"repo":{"type":"string","description":"The name of the repository without the `.git` extension. The name is not case sensitive."},"branch":{"type":"string","description":"The name of the branch. Cannot contain wildcard characters. To use wildcard characters in branch names, use [the GraphQL API](https://docs.github.com/graphql)."}},"required":["owner","repo","branch"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("DELETE", ("https://api.github.com/repos/" + String(args["owner"]) + "/" + String(args["repo"]) + "/branches/" + String(args["branch"]) + "/protection/required_signatures") + _qs, {}, undefined);
    }
  },
  {
    name: "repos/get-status-checks-protection",
    description: "Get status checks protection. Protected branches are available in public repositories with GitHub Free and GitHub Free for organizations, and in public and private repositories with GitHub Pro, GitHub Team, GitHub Enterprise Cloud, and GitHub Enterprise Server. Params: owner (required), repo (required), branch (required).",
    inputSchema: {"type":"object","properties":{"owner":{"type":"string","description":"The account owner of the repository. The name is not case sensitive."},"repo":{"type":"string","description":"The name of the repository without the `.git` extension. The name is not case sensitive."},"branch":{"type":"string","description":"The name of the branch. Cannot contain wildcard characters. To use wildcard characters in branch names, use [the GraphQL API](https://docs.github.com/graphql)."}},"required":["owner","repo","branch"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/repos/" + String(args["owner"]) + "/" + String(args["repo"]) + "/branches/" + String(args["branch"]) + "/protection/required_status_checks") + _qs, {}, undefined);
    }
  },
  {
    name: "repos/update-status-check-protection",
    description: "Update status check protection. Protected branches are available in public repositories with GitHub Free and GitHub Free for organizations, and in public and private repositories with GitHub Pro, GitHub Team, GitHub Enterprise Cloud, and GitHub Enterprise Server. Params: owner (required), repo (required), branch (required).",
    inputSchema: {"type":"object","properties":{"owner":{"type":"string","description":"The account owner of the repository. The name is not case sensitive."},"repo":{"type":"string","description":"The name of the repository without the `.git` extension. The name is not case sensitive."},"branch":{"type":"string","description":"The name of the branch. Cannot contain wildcard characters. To use wildcard characters in branch names, use [the GraphQL API](https://docs.github.com/graphql)."},"body":{"type":"object","description":"Request body"}},"required":["owner","repo","branch"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("PATCH", ("https://api.github.com/repos/" + String(args["owner"]) + "/" + String(args["repo"]) + "/branches/" + String(args["branch"]) + "/protection/required_status_checks") + _qs, {}, args["body"]);
    }
  },
  {
    name: "repos/remove-status-check-protection",
    description: "Remove status check protection. Protected branches are available in public repositories with GitHub Free and GitHub Free for organizations, and in public and private repositories with GitHub Pro, GitHub Team, GitHub Enterprise Cloud, and GitHub Enterprise Server. Params: owner (required), repo (required), branch (required).",
    inputSchema: {"type":"object","properties":{"owner":{"type":"string","description":"The account owner of the repository. The name is not case sensitive."},"repo":{"type":"string","description":"The name of the repository without the `.git` extension. The name is not case sensitive."},"branch":{"type":"string","description":"The name of the branch. Cannot contain wildcard characters. To use wildcard characters in branch names, use [the GraphQL API](https://docs.github.com/graphql)."}},"required":["owner","repo","branch"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("DELETE", ("https://api.github.com/repos/" + String(args["owner"]) + "/" + String(args["repo"]) + "/branches/" + String(args["branch"]) + "/protection/required_status_checks") + _qs, {}, undefined);
    }
  },
  {
    name: "repos/get-all-status-check-contexts",
    description: "Get all status check contexts. Protected branches are available in public repositories with GitHub Free and GitHub Free for organizations, and in public and private repositories with GitHub Pro, GitHub Team, GitHub Enterprise Cloud, and GitHub Enterprise Server. Params: owner (required), repo (required), branch (required).",
    inputSchema: {"type":"object","properties":{"owner":{"type":"string","description":"The account owner of the repository. The name is not case sensitive."},"repo":{"type":"string","description":"The name of the repository without the `.git` extension. The name is not case sensitive."},"branch":{"type":"string","description":"The name of the branch. Cannot contain wildcard characters. To use wildcard characters in branch names, use [the GraphQL API](https://docs.github.com/graphql)."}},"required":["owner","repo","branch"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/repos/" + String(args["owner"]) + "/" + String(args["repo"]) + "/branches/" + String(args["branch"]) + "/protection/required_status_checks/contexts") + _qs, {}, undefined);
    }
  },
  {
    name: "repos/add-status-check-contexts",
    description: "Add status check contexts. Protected branches are available in public repositories with GitHub Free and GitHub Free for organizations, and in public and private repositories with GitHub Pro, GitHub Team, GitHub Enterprise Cloud, and GitHub Enterprise Server. Params: owner (required), repo (required), branch (required).",
    inputSchema: {"type":"object","properties":{"owner":{"type":"string","description":"The account owner of the repository. The name is not case sensitive."},"repo":{"type":"string","description":"The name of the repository without the `.git` extension. The name is not case sensitive."},"branch":{"type":"string","description":"The name of the branch. Cannot contain wildcard characters. To use wildcard characters in branch names, use [the GraphQL API](https://docs.github.com/graphql)."},"body":{"type":"object","description":"Request body"}},"required":["owner","repo","branch"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("POST", ("https://api.github.com/repos/" + String(args["owner"]) + "/" + String(args["repo"]) + "/branches/" + String(args["branch"]) + "/protection/required_status_checks/contexts") + _qs, {}, args["body"]);
    }
  },
  {
    name: "repos/set-status-check-contexts",
    description: "Set status check contexts. Protected branches are available in public repositories with GitHub Free and GitHub Free for organizations, and in public and private repositories with GitHub Pro, GitHub Team, GitHub Enterprise Cloud, and GitHub Enterprise Server. Params: owner (required), repo (required), branch (required).",
    inputSchema: {"type":"object","properties":{"owner":{"type":"string","description":"The account owner of the repository. The name is not case sensitive."},"repo":{"type":"string","description":"The name of the repository without the `.git` extension. The name is not case sensitive."},"branch":{"type":"string","description":"The name of the branch. Cannot contain wildcard characters. To use wildcard characters in branch names, use [the GraphQL API](https://docs.github.com/graphql)."},"body":{"type":"object","description":"Request body"}},"required":["owner","repo","branch"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("PUT", ("https://api.github.com/repos/" + String(args["owner"]) + "/" + String(args["repo"]) + "/branches/" + String(args["branch"]) + "/protection/required_status_checks/contexts") + _qs, {}, args["body"]);
    }
  },
  {
    name: "repos/remove-status-check-contexts",
    description: "Remove status check contexts. Protected branches are available in public repositories with GitHub Free and GitHub Free for organizations, and in public and private repositories with GitHub Pro, GitHub Team, GitHub Enterprise Cloud, and GitHub Enterprise Server. Params: owner (required), repo (required), branch (required).",
    inputSchema: {"type":"object","properties":{"owner":{"type":"string","description":"The account owner of the repository. The name is not case sensitive."},"repo":{"type":"string","description":"The name of the repository without the `.git` extension. The name is not case sensitive."},"branch":{"type":"string","description":"The name of the branch. Cannot contain wildcard characters. To use wildcard characters in branch names, use [the GraphQL API](https://docs.github.com/graphql)."},"body":{"type":"object","description":"Request body"}},"required":["owner","repo","branch"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("DELETE", ("https://api.github.com/repos/" + String(args["owner"]) + "/" + String(args["repo"]) + "/branches/" + String(args["branch"]) + "/protection/required_status_checks/contexts") + _qs, {}, args["body"]);
    }
  },
  {
    name: "repos/get-access-restrictions",
    description: "Get access restrictions. Protected branches are available in public repositories with GitHub Free and GitHub Free for organizations, and in public and private repositories with GitHub Pro, GitHub Team, GitHub Enterprise Cloud, and GitHub Enterprise Server. Params: owner (required), repo (required), branch (required).",
    inputSchema: {"type":"object","properties":{"owner":{"type":"string","description":"The account owner of the repository. The name is not case sensitive."},"repo":{"type":"string","description":"The name of the repository without the `.git` extension. The name is not case sensitive."},"branch":{"type":"string","description":"The name of the branch. Cannot contain wildcard characters. To use wildcard characters in branch names, use [the GraphQL API](https://docs.github.com/graphql)."}},"required":["owner","repo","branch"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/repos/" + String(args["owner"]) + "/" + String(args["repo"]) + "/branches/" + String(args["branch"]) + "/protection/restrictions") + _qs, {}, undefined);
    }
  },
  {
    name: "repos/delete-access-restrictions",
    description: "Delete access restrictions. Protected branches are available in public repositories with GitHub Free and GitHub Free for organizations, and in public and private repositories with GitHub Pro, GitHub Team, GitHub Enterprise Cloud, and GitHub Enterprise Server. Params: owner (required), repo (required), branch (required).",
    inputSchema: {"type":"object","properties":{"owner":{"type":"string","description":"The account owner of the repository. The name is not case sensitive."},"repo":{"type":"string","description":"The name of the repository without the `.git` extension. The name is not case sensitive."},"branch":{"type":"string","description":"The name of the branch. Cannot contain wildcard characters. To use wildcard characters in branch names, use [the GraphQL API](https://docs.github.com/graphql)."}},"required":["owner","repo","branch"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("DELETE", ("https://api.github.com/repos/" + String(args["owner"]) + "/" + String(args["repo"]) + "/branches/" + String(args["branch"]) + "/protection/restrictions") + _qs, {}, undefined);
    }
  },
  {
    name: "repos/get-apps-with-access-to-protected-branch",
    description: "Get apps with access to the protected branch. Protected branches are available in public repositories with GitHub Free and GitHub Free for organizations, and in public and private repositories with GitHub Pro, GitHub Team, GitHub Enterprise Cloud, and GitHub Enterprise Server. Params: owner (required), repo (required), branch (required).",
    inputSchema: {"type":"object","properties":{"owner":{"type":"string","description":"The account owner of the repository. The name is not case sensitive."},"repo":{"type":"string","description":"The name of the repository without the `.git` extension. The name is not case sensitive."},"branch":{"type":"string","description":"The name of the branch. Cannot contain wildcard characters. To use wildcard characters in branch names, use [the GraphQL API](https://docs.github.com/graphql)."}},"required":["owner","repo","branch"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/repos/" + String(args["owner"]) + "/" + String(args["repo"]) + "/branches/" + String(args["branch"]) + "/protection/restrictions/apps") + _qs, {}, undefined);
    }
  },
  {
    name: "repos/add-app-access-restrictions",
    description: "Add app access restrictions. Protected branches are available in public repositories with GitHub Free and GitHub Free for organizations, and in public and private repositories with GitHub Pro, GitHub Team, GitHub Enterprise Cloud, and GitHub Enterprise Server. Params: owner (required), repo (required), branch (required).",
    inputSchema: {"type":"object","properties":{"owner":{"type":"string","description":"The account owner of the repository. The name is not case sensitive."},"repo":{"type":"string","description":"The name of the repository without the `.git` extension. The name is not case sensitive."},"branch":{"type":"string","description":"The name of the branch. Cannot contain wildcard characters. To use wildcard characters in branch names, use [the GraphQL API](https://docs.github.com/graphql)."},"body":{"type":"object","description":"Request body"}},"required":["owner","repo","branch","body"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("POST", ("https://api.github.com/repos/" + String(args["owner"]) + "/" + String(args["repo"]) + "/branches/" + String(args["branch"]) + "/protection/restrictions/apps") + _qs, {}, args["body"]);
    }
  },
  {
    name: "repos/set-app-access-restrictions",
    description: "Set app access restrictions. Protected branches are available in public repositories with GitHub Free and GitHub Free for organizations, and in public and private repositories with GitHub Pro, GitHub Team, GitHub Enterprise Cloud, and GitHub Enterprise Server. Params: owner (required), repo (required), branch (required).",
    inputSchema: {"type":"object","properties":{"owner":{"type":"string","description":"The account owner of the repository. The name is not case sensitive."},"repo":{"type":"string","description":"The name of the repository without the `.git` extension. The name is not case sensitive."},"branch":{"type":"string","description":"The name of the branch. Cannot contain wildcard characters. To use wildcard characters in branch names, use [the GraphQL API](https://docs.github.com/graphql)."},"body":{"type":"object","description":"Request body"}},"required":["owner","repo","branch","body"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("PUT", ("https://api.github.com/repos/" + String(args["owner"]) + "/" + String(args["repo"]) + "/branches/" + String(args["branch"]) + "/protection/restrictions/apps") + _qs, {}, args["body"]);
    }
  },
  {
    name: "repos/remove-app-access-restrictions",
    description: "Remove app access restrictions. Protected branches are available in public repositories with GitHub Free and GitHub Free for organizations, and in public and private repositories with GitHub Pro, GitHub Team, GitHub Enterprise Cloud, and GitHub Enterprise Server. Params: owner (required), repo (required), branch (required).",
    inputSchema: {"type":"object","properties":{"owner":{"type":"string","description":"The account owner of the repository. The name is not case sensitive."},"repo":{"type":"string","description":"The name of the repository without the `.git` extension. The name is not case sensitive."},"branch":{"type":"string","description":"The name of the branch. Cannot contain wildcard characters. To use wildcard characters in branch names, use [the GraphQL API](https://docs.github.com/graphql)."},"body":{"type":"object","description":"Request body"}},"required":["owner","repo","branch","body"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("DELETE", ("https://api.github.com/repos/" + String(args["owner"]) + "/" + String(args["repo"]) + "/branches/" + String(args["branch"]) + "/protection/restrictions/apps") + _qs, {}, args["body"]);
    }
  },
  {
    name: "repos/get-teams-with-access-to-protected-branch",
    description: "Get teams with access to the protected branch. Protected branches are available in public repositories with GitHub Free and GitHub Free for organizations, and in public and private repositories with GitHub Pro, GitHub Team, GitHub Enterprise Cloud, and GitHub Enterprise Server. Params: owner (required), repo (required), branch (required).",
    inputSchema: {"type":"object","properties":{"owner":{"type":"string","description":"The account owner of the repository. The name is not case sensitive."},"repo":{"type":"string","description":"The name of the repository without the `.git` extension. The name is not case sensitive."},"branch":{"type":"string","description":"The name of the branch. Cannot contain wildcard characters. To use wildcard characters in branch names, use [the GraphQL API](https://docs.github.com/graphql)."}},"required":["owner","repo","branch"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/repos/" + String(args["owner"]) + "/" + String(args["repo"]) + "/branches/" + String(args["branch"]) + "/protection/restrictions/teams") + _qs, {}, undefined);
    }
  },
  {
    name: "repos/add-team-access-restrictions",
    description: "Add team access restrictions. Protected branches are available in public repositories with GitHub Free and GitHub Free for organizations, and in public and private repositories with GitHub Pro, GitHub Team, GitHub Enterprise Cloud, and GitHub Enterprise Server. Params: owner (required), repo (required), branch (required).",
    inputSchema: {"type":"object","properties":{"owner":{"type":"string","description":"The account owner of the repository. The name is not case sensitive."},"repo":{"type":"string","description":"The name of the repository without the `.git` extension. The name is not case sensitive."},"branch":{"type":"string","description":"The name of the branch. Cannot contain wildcard characters. To use wildcard characters in branch names, use [the GraphQL API](https://docs.github.com/graphql)."},"body":{"type":"object","description":"Request body"}},"required":["owner","repo","branch"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("POST", ("https://api.github.com/repos/" + String(args["owner"]) + "/" + String(args["repo"]) + "/branches/" + String(args["branch"]) + "/protection/restrictions/teams") + _qs, {}, args["body"]);
    }
  },
  {
    name: "repos/set-team-access-restrictions",
    description: "Set team access restrictions. Protected branches are available in public repositories with GitHub Free and GitHub Free for organizations, and in public and private repositories with GitHub Pro, GitHub Team, GitHub Enterprise Cloud, and GitHub Enterprise Server. Params: owner (required), repo (required), branch (required).",
    inputSchema: {"type":"object","properties":{"owner":{"type":"string","description":"The account owner of the repository. The name is not case sensitive."},"repo":{"type":"string","description":"The name of the repository without the `.git` extension. The name is not case sensitive."},"branch":{"type":"string","description":"The name of the branch. Cannot contain wildcard characters. To use wildcard characters in branch names, use [the GraphQL API](https://docs.github.com/graphql)."},"body":{"type":"object","description":"Request body"}},"required":["owner","repo","branch"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("PUT", ("https://api.github.com/repos/" + String(args["owner"]) + "/" + String(args["repo"]) + "/branches/" + String(args["branch"]) + "/protection/restrictions/teams") + _qs, {}, args["body"]);
    }
  },
  {
    name: "repos/remove-team-access-restrictions",
    description: "Remove team access restrictions. Protected branches are available in public repositories with GitHub Free and GitHub Free for organizations, and in public and private repositories with GitHub Pro, GitHub Team, GitHub Enterprise Cloud, and GitHub Enterprise Server. Params: owner (required), repo (required), branch (required).",
    inputSchema: {"type":"object","properties":{"owner":{"type":"string","description":"The account owner of the repository. The name is not case sensitive."},"repo":{"type":"string","description":"The name of the repository without the `.git` extension. The name is not case sensitive."},"branch":{"type":"string","description":"The name of the branch. Cannot contain wildcard characters. To use wildcard characters in branch names, use [the GraphQL API](https://docs.github.com/graphql)."},"body":{"type":"object","description":"Request body"}},"required":["owner","repo","branch"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("DELETE", ("https://api.github.com/repos/" + String(args["owner"]) + "/" + String(args["repo"]) + "/branches/" + String(args["branch"]) + "/protection/restrictions/teams") + _qs, {}, args["body"]);
    }
  },
  {
    name: "repos/get-users-with-access-to-protected-branch",
    description: "Get users with access to the protected branch. Protected branches are available in public repositories with GitHub Free and GitHub Free for organizations, and in public and private repositories with GitHub Pro, GitHub Team, GitHub Enterprise Cloud, and GitHub Enterprise Server. Params: owner (required), repo (required), branch (required).",
    inputSchema: {"type":"object","properties":{"owner":{"type":"string","description":"The account owner of the repository. The name is not case sensitive."},"repo":{"type":"string","description":"The name of the repository without the `.git` extension. The name is not case sensitive."},"branch":{"type":"string","description":"The name of the branch. Cannot contain wildcard characters. To use wildcard characters in branch names, use [the GraphQL API](https://docs.github.com/graphql)."}},"required":["owner","repo","branch"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/repos/" + String(args["owner"]) + "/" + String(args["repo"]) + "/branches/" + String(args["branch"]) + "/protection/restrictions/users") + _qs, {}, undefined);
    }
  },
  {
    name: "repos/add-user-access-restrictions",
    description: "Add user access restrictions. Protected branches are available in public repositories with GitHub Free and GitHub Free for organizations, and in public and private repositories with GitHub Pro, GitHub Team, GitHub Enterprise Cloud, and GitHub Enterprise Server. Params: owner (required), repo (required), branch (required).",
    inputSchema: {"type":"object","properties":{"owner":{"type":"string","description":"The account owner of the repository. The name is not case sensitive."},"repo":{"type":"string","description":"The name of the repository without the `.git` extension. The name is not case sensitive."},"branch":{"type":"string","description":"The name of the branch. Cannot contain wildcard characters. To use wildcard characters in branch names, use [the GraphQL API](https://docs.github.com/graphql)."},"body":{"type":"object","description":"Request body"}},"required":["owner","repo","branch","body"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("POST", ("https://api.github.com/repos/" + String(args["owner"]) + "/" + String(args["repo"]) + "/branches/" + String(args["branch"]) + "/protection/restrictions/users") + _qs, {}, args["body"]);
    }
  },
  {
    name: "repos/set-user-access-restrictions",
    description: "Set user access restrictions. Protected branches are available in public repositories with GitHub Free and GitHub Free for organizations, and in public and private repositories with GitHub Pro, GitHub Team, GitHub Enterprise Cloud, and GitHub Enterprise Server. Params: owner (required), repo (required), branch (required).",
    inputSchema: {"type":"object","properties":{"owner":{"type":"string","description":"The account owner of the repository. The name is not case sensitive."},"repo":{"type":"string","description":"The name of the repository without the `.git` extension. The name is not case sensitive."},"branch":{"type":"string","description":"The name of the branch. Cannot contain wildcard characters. To use wildcard characters in branch names, use [the GraphQL API](https://docs.github.com/graphql)."},"body":{"type":"object","description":"Request body"}},"required":["owner","repo","branch","body"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("PUT", ("https://api.github.com/repos/" + String(args["owner"]) + "/" + String(args["repo"]) + "/branches/" + String(args["branch"]) + "/protection/restrictions/users") + _qs, {}, args["body"]);
    }
  },
  {
    name: "repos/remove-user-access-restrictions",
    description: "Remove user access restrictions. Protected branches are available in public repositories with GitHub Free and GitHub Free for organizations, and in public and private repositories with GitHub Pro, GitHub Team, GitHub Enterprise Cloud, and GitHub Enterprise Server. Params: owner (required), repo (required), branch (required).",
    inputSchema: {"type":"object","properties":{"owner":{"type":"string","description":"The account owner of the repository. The name is not case sensitive."},"repo":{"type":"string","description":"The name of the repository without the `.git` extension. The name is not case sensitive."},"branch":{"type":"string","description":"The name of the branch. Cannot contain wildcard characters. To use wildcard characters in branch names, use [the GraphQL API](https://docs.github.com/graphql)."},"body":{"type":"object","description":"Request body"}},"required":["owner","repo","branch","body"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("DELETE", ("https://api.github.com/repos/" + String(args["owner"]) + "/" + String(args["repo"]) + "/branches/" + String(args["branch"]) + "/protection/restrictions/users") + _qs, {}, args["body"]);
    }
  },
  {
    name: "repos/rename-branch",
    description: "Rename a branch. Renames a branch in a repository. Params: owner (required), repo (required), branch (required).",
    inputSchema: {"type":"object","properties":{"owner":{"type":"string","description":"The account owner of the repository. The name is not case sensitive."},"repo":{"type":"string","description":"The name of the repository without the `.git` extension. The name is not case sensitive."},"branch":{"type":"string","description":"The name of the branch. Cannot contain wildcard characters. To use wildcard characters in branch names, use [the GraphQL API](https://docs.github.com/graphql)."},"body":{"type":"object","description":"Request body"}},"required":["owner","repo","branch","body"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("POST", ("https://api.github.com/repos/" + String(args["owner"]) + "/" + String(args["repo"]) + "/branches/" + String(args["branch"]) + "/rename") + _qs, {}, args["body"]);
    }
  },
  {
    name: "checks/create",
    description: "Create a check run. Creates a new check run for a specific commit in a repository. Params: owner (required), repo (required).",
    inputSchema: {"type":"object","properties":{"owner":{"type":"string","description":"The account owner of the repository. The name is not case sensitive."},"repo":{"type":"string","description":"The name of the repository without the `.git` extension. The name is not case sensitive."},"body":{"type":"object","description":"Request body"}},"required":["owner","repo","body"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("POST", ("https://api.github.com/repos/" + String(args["owner"]) + "/" + String(args["repo"]) + "/check-runs") + _qs, {}, args["body"]);
    }
  },
  {
    name: "checks/get",
    description: "Get a check run. Gets a single check run using its `id`. Params: owner (required), repo (required), check_run_id (required).",
    inputSchema: {"type":"object","properties":{"owner":{"type":"string","description":"The account owner of the repository. The name is not case sensitive."},"repo":{"type":"string","description":"The name of the repository without the `.git` extension. The name is not case sensitive."},"check_run_id":{"type":"integer","description":"The unique identifier of the check run."}},"required":["owner","repo","check_run_id"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/repos/" + String(args["owner"]) + "/" + String(args["repo"]) + "/check-runs/" + String(args["check_run_id"])) + _qs, {}, undefined);
    }
  },
  {
    name: "checks/update",
    description: "Update a check run. Updates a check run for a specific commit in a repository. Params: owner (required), repo (required), check_run_id (required).",
    inputSchema: {"type":"object","properties":{"owner":{"type":"string","description":"The account owner of the repository. The name is not case sensitive."},"repo":{"type":"string","description":"The name of the repository without the `.git` extension. The name is not case sensitive."},"check_run_id":{"type":"integer","description":"The unique identifier of the check run."},"body":{"type":"object","description":"Request body"}},"required":["owner","repo","check_run_id","body"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("PATCH", ("https://api.github.com/repos/" + String(args["owner"]) + "/" + String(args["repo"]) + "/check-runs/" + String(args["check_run_id"])) + _qs, {}, args["body"]);
    }
  },
  {
    name: "checks/list-annotations",
    description: "List check run annotations. Lists annotations for a check run using the annotation `id`. Params: owner (required), repo (required), check_run_id (required).",
    inputSchema: {"type":"object","properties":{"owner":{"type":"string","description":"The account owner of the repository. The name is not case sensitive."},"repo":{"type":"string","description":"The name of the repository without the `.git` extension. The name is not case sensitive."},"check_run_id":{"type":"integer","description":"The unique identifier of the check run."},"per_page":{"type":"integer","default":30,"description":"The number of results per page (max 100). For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""},"page":{"type":"integer","default":1,"description":"The page number of the results to fetch. For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""}},"required":["owner","repo","check_run_id"]},
    call: function(args) {
    var _q = new URLSearchParams();
    if (args["per_page"] !== undefined) _q.set("per_page", String(args["per_page"]));
    if (args["page"] !== undefined) _q.set("page", String(args["page"]));
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/repos/" + String(args["owner"]) + "/" + String(args["repo"]) + "/check-runs/" + String(args["check_run_id"]) + "/annotations") + _qs, {}, undefined);
    }
  },
  {
    name: "checks/rerequest-run",
    description: "Rerequest a check run. Triggers GitHub to rerequest an existing check run, without pushing new code to a repository. Params: owner (required), repo (required), check_run_id (required).",
    inputSchema: {"type":"object","properties":{"owner":{"type":"string","description":"The account owner of the repository. The name is not case sensitive."},"repo":{"type":"string","description":"The name of the repository without the `.git` extension. The name is not case sensitive."},"check_run_id":{"type":"integer","description":"The unique identifier of the check run."}},"required":["owner","repo","check_run_id"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("POST", ("https://api.github.com/repos/" + String(args["owner"]) + "/" + String(args["repo"]) + "/check-runs/" + String(args["check_run_id"]) + "/rerequest") + _qs, {}, undefined);
    }
  },
  {
    name: "checks/create-suite",
    description: "Create a check suite. Creates a check suite manually. Params: owner (required), repo (required).",
    inputSchema: {"type":"object","properties":{"owner":{"type":"string","description":"The account owner of the repository. The name is not case sensitive."},"repo":{"type":"string","description":"The name of the repository without the `.git` extension. The name is not case sensitive."},"body":{"type":"object","description":"Request body"}},"required":["owner","repo","body"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("POST", ("https://api.github.com/repos/" + String(args["owner"]) + "/" + String(args["repo"]) + "/check-suites") + _qs, {}, args["body"]);
    }
  },
  {
    name: "checks/set-suites-preferences",
    description: "Update repository preferences for check suites. Changes the default automatic flow when creating check suites. Params: owner (required), repo (required).",
    inputSchema: {"type":"object","properties":{"owner":{"type":"string","description":"The account owner of the repository. The name is not case sensitive."},"repo":{"type":"string","description":"The name of the repository without the `.git` extension. The name is not case sensitive."},"body":{"type":"object","description":"Request body"}},"required":["owner","repo","body"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("PATCH", ("https://api.github.com/repos/" + String(args["owner"]) + "/" + String(args["repo"]) + "/check-suites/preferences") + _qs, {}, args["body"]);
    }
  },
  {
    name: "checks/get-suite",
    description: "Get a check suite. Gets a single check suite using its `id`. Params: owner (required), repo (required), check_suite_id (required).",
    inputSchema: {"type":"object","properties":{"owner":{"type":"string","description":"The account owner of the repository. The name is not case sensitive."},"repo":{"type":"string","description":"The name of the repository without the `.git` extension. The name is not case sensitive."},"check_suite_id":{"type":"integer","description":"The unique identifier of the check suite."}},"required":["owner","repo","check_suite_id"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/repos/" + String(args["owner"]) + "/" + String(args["repo"]) + "/check-suites/" + String(args["check_suite_id"])) + _qs, {}, undefined);
    }
  },
  {
    name: "checks/list-for-suite",
    description: "List check runs in a check suite. Lists check runs for a check suite using its `id`. Params: owner (required), repo (required), check_suite_id (required).",
    inputSchema: {"type":"object","properties":{"owner":{"type":"string","description":"The account owner of the repository. The name is not case sensitive."},"repo":{"type":"string","description":"The name of the repository without the `.git` extension. The name is not case sensitive."},"check_suite_id":{"type":"integer","description":"The unique identifier of the check suite."},"check_name":{"type":"string","description":"Returns check runs with the specified `name`."},"status":{"type":"string","enum":["queued","in_progress","completed"],"description":"Returns check runs with the specified `status`."},"filter":{"type":"string","enum":["latest","all"],"default":"latest","description":"Filters check runs by their `completed_at` timestamp. `latest` returns the most recent check runs."},"per_page":{"type":"integer","default":30,"description":"The number of results per page (max 100). For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""},"page":{"type":"integer","default":1,"description":"The page number of the results to fetch. For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""}},"required":["owner","repo","check_suite_id"]},
    call: function(args) {
    var _q = new URLSearchParams();
    if (args["check_name"] !== undefined) _q.set("check_name", String(args["check_name"]));
    if (args["status"] !== undefined) _q.set("status", String(args["status"]));
    if (args["filter"] !== undefined) _q.set("filter", String(args["filter"]));
    if (args["per_page"] !== undefined) _q.set("per_page", String(args["per_page"]));
    if (args["page"] !== undefined) _q.set("page", String(args["page"]));
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/repos/" + String(args["owner"]) + "/" + String(args["repo"]) + "/check-suites/" + String(args["check_suite_id"]) + "/check-runs") + _qs, {}, undefined);
    }
  },
  {
    name: "checks/rerequest-suite",
    description: "Rerequest a check suite. Triggers GitHub to rerequest an existing check suite, without pushing new code to a repository. Params: owner (required), repo (required), check_suite_id (required).",
    inputSchema: {"type":"object","properties":{"owner":{"type":"string","description":"The account owner of the repository. The name is not case sensitive."},"repo":{"type":"string","description":"The name of the repository without the `.git` extension. The name is not case sensitive."},"check_suite_id":{"type":"integer","description":"The unique identifier of the check suite."}},"required":["owner","repo","check_suite_id"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("POST", ("https://api.github.com/repos/" + String(args["owner"]) + "/" + String(args["repo"]) + "/check-suites/" + String(args["check_suite_id"]) + "/rerequest") + _qs, {}, undefined);
    }
  },
  {
    name: "code-quality/get-setup",
    description: "Get a code quality setup configuration. Gets a code quality setup configuration. Params: owner (required), repo (required).",
    inputSchema: {"type":"object","properties":{"owner":{"type":"string","description":"The account owner of the repository. The name is not case sensitive."},"repo":{"type":"string","description":"The name of the repository without the `.git` extension. The name is not case sensitive."}},"required":["owner","repo"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/repos/" + String(args["owner"]) + "/" + String(args["repo"]) + "/code-quality/setup") + _qs, {}, undefined);
    }
  },
  {
    name: "code-quality/update-setup",
    description: "Update a code quality setup configuration. Updates a code quality setup configuration. Params: owner (required), repo (required).",
    inputSchema: {"type":"object","properties":{"owner":{"type":"string","description":"The account owner of the repository. The name is not case sensitive."},"repo":{"type":"string","description":"The name of the repository without the `.git` extension. The name is not case sensitive."},"body":{"type":"object","description":"Request body"}},"required":["owner","repo","body"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("PATCH", ("https://api.github.com/repos/" + String(args["owner"]) + "/" + String(args["repo"]) + "/code-quality/setup") + _qs, {}, args["body"]);
    }
  },
  {
    name: "code-scanning/list-alerts-for-repo",
    description: "List code scanning alerts for a repository. Lists code scanning alerts. Params: owner (required), repo (required).",
    inputSchema: {"type":"object","properties":{"owner":{"type":"string","description":"The account owner of the repository. The name is not case sensitive."},"repo":{"type":"string","description":"The name of the repository without the `.git` extension. The name is not case sensitive."},"tool_name":{"$ref":"#/components/schemas/code-scanning-analysis-tool-name","description":"The name of a code scanning tool. Only results by this tool will be listed. You can specify the tool by using either `tool_name` or `tool_guid`, but not both."},"tool_guid":{"$ref":"#/components/schemas/code-scanning-analysis-tool-guid","description":"The GUID of a code scanning tool. Only results by this tool will be listed. Note that some code scanning tools may not include a GUID in their analysis data. You can specify the tool by using either `tool_guid` or `tool_name`, but not both."},"page":{"type":"integer","default":1,"description":"The page number of the results to fetch. For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""},"per_page":{"type":"integer","default":30,"description":"The number of results per page (max 100). For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""},"ref":{"$ref":"#/components/schemas/code-scanning-ref","description":"The Git reference for the results you want to list. The `ref` for a branch can be formatted either as `refs/heads/<branch name>` or simply `<branch name>`. To reference a pull request use `refs/pull/<number>/merge`."},"pr":{"type":"integer","description":"The number of the pull request for the results you want to list."},"direction":{"type":"string","enum":["asc","desc"],"default":"desc","description":"The direction to sort the results by."},"before":{"type":"string","description":"A cursor, as given in the [Link header](https://docs.github.com/rest/guides/using-pagination-in-the-rest-api#using-link-headers). If specified, the query only searches for results before this cursor. For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""},"after":{"type":"string","description":"A cursor, as given in the [Link header](https://docs.github.com/rest/guides/using-pagination-in-the-rest-api#using-link-headers). If specified, the query only searches for results after this cursor. For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""},"sort":{"type":"string","enum":["created","updated"],"default":"created","description":"The property by which to sort the results."},"state":{"$ref":"#/components/schemas/code-scanning-alert-state-query","description":"If specified, only code scanning alerts with this state will be returned."},"severity":{"$ref":"#/components/schemas/code-scanning-alert-severity","description":"If specified, only code scanning alerts with this severity will be returned."},"assignees":{"type":"string","description":"Filter alerts by assignees. Provide a comma-separated list of user handles (e.g., `octocat` or `octocat,hubot`).\nUse `*` to list alerts with at least one assignee or `none` to list alerts with no assignees.\n"}},"required":["owner","repo"]},
    call: function(args) {
    var _q = new URLSearchParams();
    if (args["tool_name"] !== undefined) _q.set("tool_name", String(args["tool_name"]));
    if (args["tool_guid"] !== undefined) _q.set("tool_guid", String(args["tool_guid"]));
    if (args["page"] !== undefined) _q.set("page", String(args["page"]));
    if (args["per_page"] !== undefined) _q.set("per_page", String(args["per_page"]));
    if (args["ref"] !== undefined) _q.set("ref", String(args["ref"]));
    if (args["pr"] !== undefined) _q.set("pr", String(args["pr"]));
    if (args["direction"] !== undefined) _q.set("direction", String(args["direction"]));
    if (args["before"] !== undefined) _q.set("before", String(args["before"]));
    if (args["after"] !== undefined) _q.set("after", String(args["after"]));
    if (args["sort"] !== undefined) _q.set("sort", String(args["sort"]));
    if (args["state"] !== undefined) _q.set("state", String(args["state"]));
    if (args["severity"] !== undefined) _q.set("severity", String(args["severity"]));
    if (args["assignees"] !== undefined) _q.set("assignees", String(args["assignees"]));
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/repos/" + String(args["owner"]) + "/" + String(args["repo"]) + "/code-scanning/alerts") + _qs, {}, undefined);
    }
  },
  {
    name: "code-scanning/get-alert",
    description: "Get a code scanning alert. Gets a single code scanning alert. Params: owner (required), repo (required), alert_number (required).",
    inputSchema: {"type":"object","properties":{"owner":{"type":"string","description":"The account owner of the repository. The name is not case sensitive."},"repo":{"type":"string","description":"The name of the repository without the `.git` extension. The name is not case sensitive."},"alert_number":{"$ref":"#/components/schemas/alert-number","description":"The number that identifies an alert. You can find this at the end of the URL for a code scanning alert within GitHub, and in the `number` field in the response from the `GET /repos/{owner}/{repo}/code-scanning/alerts` operation."}},"required":["owner","repo","alert_number"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/repos/" + String(args["owner"]) + "/" + String(args["repo"]) + "/code-scanning/alerts/" + String(args["alert_number"])) + _qs, {}, undefined);
    }
  },
  {
    name: "code-scanning/update-alert",
    description: "Update a code scanning alert. Updates the status of a single code scanning alert. Params: owner (required), repo (required), alert_number (required).",
    inputSchema: {"type":"object","properties":{"owner":{"type":"string","description":"The account owner of the repository. The name is not case sensitive."},"repo":{"type":"string","description":"The name of the repository without the `.git` extension. The name is not case sensitive."},"alert_number":{"$ref":"#/components/schemas/alert-number","description":"The number that identifies an alert. You can find this at the end of the URL for a code scanning alert within GitHub, and in the `number` field in the response from the `GET /repos/{owner}/{repo}/code-scanning/alerts` operation."},"body":{"type":"object","description":"Request body"}},"required":["owner","repo","alert_number","body"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("PATCH", ("https://api.github.com/repos/" + String(args["owner"]) + "/" + String(args["repo"]) + "/code-scanning/alerts/" + String(args["alert_number"])) + _qs, {}, args["body"]);
    }
  },
  {
    name: "code-scanning/get-autofix",
    description: "Get the status of an autofix for a code scanning alert. Gets the status and description of an autofix for a code scanning alert on the repository's default branch. Params: owner (required), repo (required), alert_number (required).",
    inputSchema: {"type":"object","properties":{"owner":{"type":"string","description":"The account owner of the repository. The name is not case sensitive."},"repo":{"type":"string","description":"The name of the repository without the `.git` extension. The name is not case sensitive."},"alert_number":{"$ref":"#/components/schemas/alert-number","description":"The number that identifies an alert. You can find this at the end of the URL for a code scanning alert within GitHub, and in the `number` field in the response from the `GET /repos/{owner}/{repo}/code-scanning/alerts` operation."}},"required":["owner","repo","alert_number"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/repos/" + String(args["owner"]) + "/" + String(args["repo"]) + "/code-scanning/alerts/" + String(args["alert_number"]) + "/autofix") + _qs, {}, undefined);
    }
  },
  {
    name: "code-scanning/create-autofix",
    description: "Create an autofix for a code scanning alert. Creates an autofix for a code scanning alert. Params: owner (required), repo (required), alert_number (required).",
    inputSchema: {"type":"object","properties":{"owner":{"type":"string","description":"The account owner of the repository. The name is not case sensitive."},"repo":{"type":"string","description":"The name of the repository without the `.git` extension. The name is not case sensitive."},"alert_number":{"$ref":"#/components/schemas/alert-number","description":"The number that identifies an alert. You can find this at the end of the URL for a code scanning alert within GitHub, and in the `number` field in the response from the `GET /repos/{owner}/{repo}/code-scanning/alerts` operation."}},"required":["owner","repo","alert_number"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("POST", ("https://api.github.com/repos/" + String(args["owner"]) + "/" + String(args["repo"]) + "/code-scanning/alerts/" + String(args["alert_number"]) + "/autofix") + _qs, {}, undefined);
    }
  },
  {
    name: "code-scanning/commit-autofix",
    description: "Commit an autofix for a code scanning alert. Commits an autofix for a code scanning alert from the repository's default branch. Params: owner (required), repo (required), alert_number (required).",
    inputSchema: {"type":"object","properties":{"owner":{"type":"string","description":"The account owner of the repository. The name is not case sensitive."},"repo":{"type":"string","description":"The name of the repository without the `.git` extension. The name is not case sensitive."},"alert_number":{"$ref":"#/components/schemas/alert-number","description":"The number that identifies an alert. You can find this at the end of the URL for a code scanning alert within GitHub, and in the `number` field in the response from the `GET /repos/{owner}/{repo}/code-scanning/alerts` operation."},"body":{"type":"object","description":"Request body"}},"required":["owner","repo","alert_number"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("POST", ("https://api.github.com/repos/" + String(args["owner"]) + "/" + String(args["repo"]) + "/code-scanning/alerts/" + String(args["alert_number"]) + "/autofix/commits") + _qs, {}, args["body"]);
    }
  },
  {
    name: "code-scanning/list-alert-instances",
    description: "List instances of a code scanning alert. Lists all instances of the specified code scanning alert. Params: owner (required), repo (required), alert_number (required).",
    inputSchema: {"type":"object","properties":{"owner":{"type":"string","description":"The account owner of the repository. The name is not case sensitive."},"repo":{"type":"string","description":"The name of the repository without the `.git` extension. The name is not case sensitive."},"alert_number":{"$ref":"#/components/schemas/alert-number","description":"The number that identifies an alert. You can find this at the end of the URL for a code scanning alert within GitHub, and in the `number` field in the response from the `GET /repos/{owner}/{repo}/code-scanning/alerts` operation."},"page":{"type":"integer","default":1,"description":"The page number of the results to fetch. For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""},"per_page":{"type":"integer","default":30,"description":"The number of results per page (max 100). For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""},"ref":{"$ref":"#/components/schemas/code-scanning-ref","description":"The Git reference for the results you want to list. The `ref` for a branch can be formatted either as `refs/heads/<branch name>` or simply `<branch name>`. To reference a pull request use `refs/pull/<number>/merge`."},"pr":{"type":"integer","description":"The number of the pull request for the results you want to list."}},"required":["owner","repo","alert_number"]},
    call: function(args) {
    var _q = new URLSearchParams();
    if (args["page"] !== undefined) _q.set("page", String(args["page"]));
    if (args["per_page"] !== undefined) _q.set("per_page", String(args["per_page"]));
    if (args["ref"] !== undefined) _q.set("ref", String(args["ref"]));
    if (args["pr"] !== undefined) _q.set("pr", String(args["pr"]));
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/repos/" + String(args["owner"]) + "/" + String(args["repo"]) + "/code-scanning/alerts/" + String(args["alert_number"]) + "/instances") + _qs, {}, undefined);
    }
  },
  {
    name: "code-scanning/list-recent-analyses",
    description: "List code scanning analyses for a repository. Lists the details of all code scanning analyses for a repository,\nstarting with the most recent. Params: owner (required), repo (required).",
    inputSchema: {"type":"object","properties":{"owner":{"type":"string","description":"The account owner of the repository. The name is not case sensitive."},"repo":{"type":"string","description":"The name of the repository without the `.git` extension. The name is not case sensitive."},"tool_name":{"$ref":"#/components/schemas/code-scanning-analysis-tool-name","description":"The name of a code scanning tool. Only results by this tool will be listed. You can specify the tool by using either `tool_name` or `tool_guid`, but not both."},"tool_guid":{"$ref":"#/components/schemas/code-scanning-analysis-tool-guid","description":"The GUID of a code scanning tool. Only results by this tool will be listed. Note that some code scanning tools may not include a GUID in their analysis data. You can specify the tool by using either `tool_guid` or `tool_name`, but not both."},"page":{"type":"integer","default":1,"description":"The page number of the results to fetch. For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""},"per_page":{"type":"integer","default":30,"description":"The number of results per page (max 100). For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""},"pr":{"type":"integer","description":"The number of the pull request for the results you want to list."},"ref":{"$ref":"#/components/schemas/code-scanning-ref","description":"The Git reference for the analyses you want to list. The `ref` for a branch can be formatted either as `refs/heads/<branch name>` or simply `<branch name>`. To reference a pull request use `refs/pull/<number>/merge`."},"sarif_id":{"$ref":"#/components/schemas/code-scanning-analysis-sarif-id","description":"Filter analyses belonging to the same SARIF upload."},"direction":{"type":"string","enum":["asc","desc"],"default":"desc","description":"The direction to sort the results by."},"sort":{"type":"string","enum":["created"],"default":"created","description":"The property by which to sort the results."}},"required":["owner","repo"]},
    call: function(args) {
    var _q = new URLSearchParams();
    if (args["tool_name"] !== undefined) _q.set("tool_name", String(args["tool_name"]));
    if (args["tool_guid"] !== undefined) _q.set("tool_guid", String(args["tool_guid"]));
    if (args["page"] !== undefined) _q.set("page", String(args["page"]));
    if (args["per_page"] !== undefined) _q.set("per_page", String(args["per_page"]));
    if (args["pr"] !== undefined) _q.set("pr", String(args["pr"]));
    if (args["ref"] !== undefined) _q.set("ref", String(args["ref"]));
    if (args["sarif_id"] !== undefined) _q.set("sarif_id", String(args["sarif_id"]));
    if (args["direction"] !== undefined) _q.set("direction", String(args["direction"]));
    if (args["sort"] !== undefined) _q.set("sort", String(args["sort"]));
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/repos/" + String(args["owner"]) + "/" + String(args["repo"]) + "/code-scanning/analyses") + _qs, {}, undefined);
    }
  },
  {
    name: "code-scanning/get-analysis",
    description: "Get a code scanning analysis for a repository. Gets a specified code scanning analysis for a repository. Params: owner (required), repo (required), analysis_id (required).",
    inputSchema: {"type":"object","properties":{"owner":{"type":"string","description":"The account owner of the repository. The name is not case sensitive."},"repo":{"type":"string","description":"The name of the repository without the `.git` extension. The name is not case sensitive."},"analysis_id":{"type":"integer","description":"The ID of the analysis, as returned from the `GET /repos/{owner}/{repo}/code-scanning/analyses` operation."}},"required":["owner","repo","analysis_id"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/repos/" + String(args["owner"]) + "/" + String(args["repo"]) + "/code-scanning/analyses/" + String(args["analysis_id"])) + _qs, {}, undefined);
    }
  },
  {
    name: "code-scanning/delete-analysis",
    description: "Delete a code scanning analysis from a repository. Deletes a specified code scanning analysis from a repository. Params: owner (required), repo (required), analysis_id (required).",
    inputSchema: {"type":"object","properties":{"owner":{"type":"string","description":"The account owner of the repository. The name is not case sensitive."},"repo":{"type":"string","description":"The name of the repository without the `.git` extension. The name is not case sensitive."},"analysis_id":{"type":"integer","description":"The ID of the analysis, as returned from the `GET /repos/{owner}/{repo}/code-scanning/analyses` operation."},"confirm_delete":{"type":"string","nullable":true,"description":"Allow deletion if the specified analysis is the last in a set. If you attempt to delete the final analysis in a set without setting this parameter to `true`, you'll get a 400 response with the message: `Analysis is last of its type and deletion may result in the loss of historical alert data. Please specify confirm_delete.`"}},"required":["owner","repo","analysis_id"]},
    call: function(args) {
    var _q = new URLSearchParams();
    if (args["confirm_delete"] !== undefined) _q.set("confirm_delete", String(args["confirm_delete"]));
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("DELETE", ("https://api.github.com/repos/" + String(args["owner"]) + "/" + String(args["repo"]) + "/code-scanning/analyses/" + String(args["analysis_id"])) + _qs, {}, undefined);
    }
  },
  {
    name: "code-scanning/list-codeql-databases",
    description: "List CodeQL databases for a repository. Lists the CodeQL databases that are available in a repository. Params: owner (required), repo (required).",
    inputSchema: {"type":"object","properties":{"owner":{"type":"string","description":"The account owner of the repository. The name is not case sensitive."},"repo":{"type":"string","description":"The name of the repository without the `.git` extension. The name is not case sensitive."}},"required":["owner","repo"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/repos/" + String(args["owner"]) + "/" + String(args["repo"]) + "/code-scanning/codeql/databases") + _qs, {}, undefined);
    }
  },
  {
    name: "code-scanning/get-codeql-database",
    description: "Get a CodeQL database for a repository. Gets a CodeQL database for a language in a repository. Params: owner (required), repo (required), language (required).",
    inputSchema: {"type":"object","properties":{"owner":{"type":"string","description":"The account owner of the repository. The name is not case sensitive."},"repo":{"type":"string","description":"The name of the repository without the `.git` extension. The name is not case sensitive."},"language":{"type":"string","description":"The language of the CodeQL database."}},"required":["owner","repo","language"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/repos/" + String(args["owner"]) + "/" + String(args["repo"]) + "/code-scanning/codeql/databases/" + String(args["language"])) + _qs, {}, undefined);
    }
  },
  {
    name: "code-scanning/delete-codeql-database",
    description: "Delete a CodeQL database. Deletes a CodeQL database for a language in a repository. Params: owner (required), repo (required), language (required).",
    inputSchema: {"type":"object","properties":{"owner":{"type":"string","description":"The account owner of the repository. The name is not case sensitive."},"repo":{"type":"string","description":"The name of the repository without the `.git` extension. The name is not case sensitive."},"language":{"type":"string","description":"The language of the CodeQL database."}},"required":["owner","repo","language"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("DELETE", ("https://api.github.com/repos/" + String(args["owner"]) + "/" + String(args["repo"]) + "/code-scanning/codeql/databases/" + String(args["language"])) + _qs, {}, undefined);
    }
  },
  {
    name: "code-scanning/create-variant-analysis",
    description: "Create a CodeQL variant analysis. Creates a new CodeQL variant analysis, which will run a CodeQL query against one or more repositories. Params: owner (required), repo (required).",
    inputSchema: {"type":"object","properties":{"owner":{"type":"string","description":"The account owner of the repository. The name is not case sensitive."},"repo":{"type":"string","description":"The name of the repository without the `.git` extension. The name is not case sensitive."},"body":{"type":"object","description":"Request body"}},"required":["owner","repo","body"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("POST", ("https://api.github.com/repos/" + String(args["owner"]) + "/" + String(args["repo"]) + "/code-scanning/codeql/variant-analyses") + _qs, {}, args["body"]);
    }
  },
  {
    name: "code-scanning/get-variant-analysis",
    description: "Get the summary of a CodeQL variant analysis. Gets the summary of a CodeQL variant analysis. Params: owner (required), repo (required), codeql_variant_analysis_id (required).",
    inputSchema: {"type":"object","properties":{"owner":{"type":"string","description":"The account owner of the repository. The name is not case sensitive."},"repo":{"type":"string","description":"The name of the repository without the `.git` extension. The name is not case sensitive."},"codeql_variant_analysis_id":{"type":"integer","description":"The unique identifier of the variant analysis."}},"required":["owner","repo","codeql_variant_analysis_id"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/repos/" + String(args["owner"]) + "/" + String(args["repo"]) + "/code-scanning/codeql/variant-analyses/" + String(args["codeql_variant_analysis_id"])) + _qs, {}, undefined);
    }
  },
  {
    name: "code-scanning/get-variant-analysis-repo-task",
    description: "Get the analysis status of a repository in a CodeQL variant analysis. Gets the analysis status of a repository in a CodeQL variant analysis. Params: owner (required), repo (required), codeql_variant_analysis_id (required), repo_owner (required), repo_name (required).",
    inputSchema: {"type":"object","properties":{"owner":{"type":"string","description":"The account owner of the repository. The name is not case sensitive."},"repo":{"type":"string","description":"The name of the controller repository."},"codeql_variant_analysis_id":{"type":"integer","description":"The ID of the variant analysis."},"repo_owner":{"type":"string","description":"The account owner of the variant analysis repository. The name is not case sensitive."},"repo_name":{"type":"string","description":"The name of the variant analysis repository."}},"required":["owner","repo","codeql_variant_analysis_id","repo_owner","repo_name"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/repos/" + String(args["owner"]) + "/" + String(args["repo"]) + "/code-scanning/codeql/variant-analyses/" + String(args["codeql_variant_analysis_id"]) + "/repos/" + String(args["repo_owner"]) + "/" + String(args["repo_name"])) + _qs, {}, undefined);
    }
  },
  {
    name: "code-scanning/get-default-setup",
    description: "Get a code scanning default setup configuration. Gets a code scanning default setup configuration. Params: owner (required), repo (required).",
    inputSchema: {"type":"object","properties":{"owner":{"type":"string","description":"The account owner of the repository. The name is not case sensitive."},"repo":{"type":"string","description":"The name of the repository without the `.git` extension. The name is not case sensitive."}},"required":["owner","repo"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/repos/" + String(args["owner"]) + "/" + String(args["repo"]) + "/code-scanning/default-setup") + _qs, {}, undefined);
    }
  },
  {
    name: "code-scanning/update-default-setup",
    description: "Update a code scanning default setup configuration. Updates a code scanning default setup configuration. Params: owner (required), repo (required).",
    inputSchema: {"type":"object","properties":{"owner":{"type":"string","description":"The account owner of the repository. The name is not case sensitive."},"repo":{"type":"string","description":"The name of the repository without the `.git` extension. The name is not case sensitive."},"body":{"type":"object","description":"Request body"}},"required":["owner","repo","body"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("PATCH", ("https://api.github.com/repos/" + String(args["owner"]) + "/" + String(args["repo"]) + "/code-scanning/default-setup") + _qs, {}, args["body"]);
    }
  },
  {
    name: "code-scanning/upload-sarif",
    description: "Upload an analysis as SARIF data. Uploads SARIF data containing the results of a code scanning analysis to make the results available in a repository. Params: owner (required), repo (required).",
    inputSchema: {"type":"object","properties":{"owner":{"type":"string","description":"The account owner of the repository. The name is not case sensitive."},"repo":{"type":"string","description":"The name of the repository without the `.git` extension. The name is not case sensitive."},"body":{"type":"object","description":"Request body"}},"required":["owner","repo","body"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("POST", ("https://api.github.com/repos/" + String(args["owner"]) + "/" + String(args["repo"]) + "/code-scanning/sarifs") + _qs, {}, args["body"]);
    }
  },
  {
    name: "code-scanning/get-sarif",
    description: "Get information about a SARIF upload. Gets information about a SARIF upload, including the status and the URL of the analysis that was uploaded so that you can retrieve details of the analysis. Params: owner (required), repo (required), sarif_id (required).",
    inputSchema: {"type":"object","properties":{"owner":{"type":"string","description":"The account owner of the repository. The name is not case sensitive."},"repo":{"type":"string","description":"The name of the repository without the `.git` extension. The name is not case sensitive."},"sarif_id":{"type":"string","description":"The SARIF ID obtained after uploading."}},"required":["owner","repo","sarif_id"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/repos/" + String(args["owner"]) + "/" + String(args["repo"]) + "/code-scanning/sarifs/" + String(args["sarif_id"])) + _qs, {}, undefined);
    }
  },
  {
    name: "code-security/get-configuration-for-repository",
    description: "Get the code security configuration associated with a repository. Get the code security configuration that manages a repository's code security settings. Params: owner (required), repo (required).",
    inputSchema: {"type":"object","properties":{"owner":{"type":"string","description":"The account owner of the repository. The name is not case sensitive."},"repo":{"type":"string","description":"The name of the repository without the `.git` extension. The name is not case sensitive."}},"required":["owner","repo"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/repos/" + String(args["owner"]) + "/" + String(args["repo"]) + "/code-security-configuration") + _qs, {}, undefined);
    }
  },
  {
    name: "repos/codeowners-errors",
    description: "List CODEOWNERS errors. List any syntax errors that are detected in the CODEOWNERS\nfile. Params: owner (required), repo (required).",
    inputSchema: {"type":"object","properties":{"owner":{"type":"string","description":"The account owner of the repository. The name is not case sensitive."},"repo":{"type":"string","description":"The name of the repository without the `.git` extension. The name is not case sensitive."},"ref":{"type":"string","description":"A branch, tag or commit name used to determine which version of the CODEOWNERS file to use. Default: the repository's default branch (e.g. `main`)"}},"required":["owner","repo"]},
    call: function(args) {
    var _q = new URLSearchParams();
    if (args["ref"] !== undefined) _q.set("ref", String(args["ref"]));
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/repos/" + String(args["owner"]) + "/" + String(args["repo"]) + "/codeowners/errors") + _qs, {}, undefined);
    }
  },
  {
    name: "codespaces/list-in-repository-for-authenticated-user",
    description: "List codespaces in a repository for the authenticated user. Lists the codespaces associated to a specified repository and the authenticated user. Params: owner (required), repo (required).",
    inputSchema: {"type":"object","properties":{"per_page":{"type":"integer","default":30,"description":"The number of results per page (max 100). For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""},"page":{"type":"integer","default":1,"description":"The page number of the results to fetch. For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""},"owner":{"type":"string","description":"The account owner of the repository. The name is not case sensitive."},"repo":{"type":"string","description":"The name of the repository without the `.git` extension. The name is not case sensitive."}},"required":["owner","repo"]},
    call: function(args) {
    var _q = new URLSearchParams();
    if (args["per_page"] !== undefined) _q.set("per_page", String(args["per_page"]));
    if (args["page"] !== undefined) _q.set("page", String(args["page"]));
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/repos/" + String(args["owner"]) + "/" + String(args["repo"]) + "/codespaces") + _qs, {}, undefined);
    }
  },
  {
    name: "codespaces/create-with-repo-for-authenticated-user",
    description: "Create a codespace in a repository. Creates a codespace owned by the authenticated user in the specified repository. Params: owner (required), repo (required).",
    inputSchema: {"type":"object","properties":{"owner":{"type":"string","description":"The account owner of the repository. The name is not case sensitive."},"repo":{"type":"string","description":"The name of the repository without the `.git` extension. The name is not case sensitive."},"body":{"type":"object","description":"Request body"}},"required":["owner","repo","body"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("POST", ("https://api.github.com/repos/" + String(args["owner"]) + "/" + String(args["repo"]) + "/codespaces") + _qs, {}, args["body"]);
    }
  },
  {
    name: "codespaces/list-devcontainers-in-repository-for-authenticated-user",
    description: "List devcontainer configurations in a repository for the authenticated user. Lists the devcontainer.json files associated with a specified repository and the authenticated user. Params: owner (required), repo (required).",
    inputSchema: {"type":"object","properties":{"per_page":{"type":"integer","default":30,"description":"The number of results per page (max 100). For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""},"page":{"type":"integer","default":1,"description":"The page number of the results to fetch. For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""},"owner":{"type":"string","description":"The account owner of the repository. The name is not case sensitive."},"repo":{"type":"string","description":"The name of the repository without the `.git` extension. The name is not case sensitive."}},"required":["owner","repo"]},
    call: function(args) {
    var _q = new URLSearchParams();
    if (args["per_page"] !== undefined) _q.set("per_page", String(args["per_page"]));
    if (args["page"] !== undefined) _q.set("page", String(args["page"]));
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/repos/" + String(args["owner"]) + "/" + String(args["repo"]) + "/codespaces/devcontainers") + _qs, {}, undefined);
    }
  },
  {
    name: "codespaces/repo-machines-for-authenticated-user",
    description: "List available machine types for a repository. List the machine types available for a given repository based on its configuration. Params: owner (required), repo (required).",
    inputSchema: {"type":"object","properties":{"owner":{"type":"string","description":"The account owner of the repository. The name is not case sensitive."},"repo":{"type":"string","description":"The name of the repository without the `.git` extension. The name is not case sensitive."},"location":{"type":"string","example":"WestUs2","description":"The location to check for available machines. Assigned by IP if not provided."},"client_ip":{"type":"string","description":"IP for location auto-detection when proxying a request"},"ref":{"type":"string","example":"main","description":"The branch or commit to check for prebuild availability and devcontainer restrictions."}},"required":["owner","repo"]},
    call: function(args) {
    var _q = new URLSearchParams();
    if (args["location"] !== undefined) _q.set("location", String(args["location"]));
    if (args["client_ip"] !== undefined) _q.set("client_ip", String(args["client_ip"]));
    if (args["ref"] !== undefined) _q.set("ref", String(args["ref"]));
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/repos/" + String(args["owner"]) + "/" + String(args["repo"]) + "/codespaces/machines") + _qs, {}, undefined);
    }
  },
  {
    name: "codespaces/pre-flight-with-repo-for-authenticated-user",
    description: "Get default attributes for a codespace. Gets the default attributes for codespaces created by the user with the repository. Params: owner (required), repo (required).",
    inputSchema: {"type":"object","properties":{"owner":{"type":"string","description":"The account owner of the repository. The name is not case sensitive."},"repo":{"type":"string","description":"The name of the repository without the `.git` extension. The name is not case sensitive."},"ref":{"type":"string","example":"main","description":"The branch or commit to check for a default devcontainer path. If not specified, the default branch will be checked."},"client_ip":{"type":"string","example":"1.2.3.4","description":"An alternative IP for default location auto-detection, such as when proxying a request."}},"required":["owner","repo"]},
    call: function(args) {
    var _q = new URLSearchParams();
    if (args["ref"] !== undefined) _q.set("ref", String(args["ref"]));
    if (args["client_ip"] !== undefined) _q.set("client_ip", String(args["client_ip"]));
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/repos/" + String(args["owner"]) + "/" + String(args["repo"]) + "/codespaces/new") + _qs, {}, undefined);
    }
  },
  {
    name: "codespaces/check-permissions-for-devcontainer",
    description: "Check if permissions defined by a devcontainer have been accepted by the authenticated user. Checks whether the permissions defined by a given devcontainer configuration have been accepted by the authenticated user. Params: owner (required), repo (required), ref (required), devcontainer_path (required).",
    inputSchema: {"type":"object","properties":{"owner":{"type":"string","description":"The account owner of the repository. The name is not case sensitive."},"repo":{"type":"string","description":"The name of the repository without the `.git` extension. The name is not case sensitive."},"ref":{"type":"string","example":"master","description":"The git reference that points to the location of the devcontainer configuration to use for the permission check. The value of `ref` will typically be a branch name (`heads/BRANCH_NAME`). For more information, see \"[Git References](https://git-scm.com/book/en/v2/Git-Internals-Git-References)\" in the Git documentation."},"devcontainer_path":{"type":"string","example":".devcontainer/example/devcontainer.json","description":"Path to the devcontainer.json configuration to use for the permission check."}},"required":["owner","repo","ref","devcontainer_path"]},
    call: function(args) {
    var _q = new URLSearchParams();
    if (args["ref"] !== undefined) _q.set("ref", String(args["ref"]));
    if (args["devcontainer_path"] !== undefined) _q.set("devcontainer_path", String(args["devcontainer_path"]));
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/repos/" + String(args["owner"]) + "/" + String(args["repo"]) + "/codespaces/permissions_check") + _qs, {}, undefined);
    }
  },
  {
    name: "codespaces/list-repo-secrets",
    description: "List repository secrets. Lists all development environment secrets available in a repository without revealing their encrypted\nvalues. Params: owner (required), repo (required).",
    inputSchema: {"type":"object","properties":{"owner":{"type":"string","description":"The account owner of the repository. The name is not case sensitive."},"repo":{"type":"string","description":"The name of the repository without the `.git` extension. The name is not case sensitive."},"per_page":{"type":"integer","default":30,"description":"The number of results per page (max 100). For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""},"page":{"type":"integer","default":1,"description":"The page number of the results to fetch. For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""}},"required":["owner","repo"]},
    call: function(args) {
    var _q = new URLSearchParams();
    if (args["per_page"] !== undefined) _q.set("per_page", String(args["per_page"]));
    if (args["page"] !== undefined) _q.set("page", String(args["page"]));
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/repos/" + String(args["owner"]) + "/" + String(args["repo"]) + "/codespaces/secrets") + _qs, {}, undefined);
    }
  },
  {
    name: "codespaces/get-repo-public-key",
    description: "Get a repository public key. Gets your public key, which you need to encrypt secrets. Params: owner (required), repo (required).",
    inputSchema: {"type":"object","properties":{"owner":{"type":"string","description":"The account owner of the repository. The name is not case sensitive."},"repo":{"type":"string","description":"The name of the repository without the `.git` extension. The name is not case sensitive."}},"required":["owner","repo"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/repos/" + String(args["owner"]) + "/" + String(args["repo"]) + "/codespaces/secrets/public-key") + _qs, {}, undefined);
    }
  },
  {
    name: "codespaces/get-repo-secret",
    description: "Get a repository secret. Gets a single repository development environment secret without revealing its encrypted value. Params: owner (required), repo (required), secret_name (required).",
    inputSchema: {"type":"object","properties":{"owner":{"type":"string","description":"The account owner of the repository. The name is not case sensitive."},"repo":{"type":"string","description":"The name of the repository without the `.git` extension. The name is not case sensitive."},"secret_name":{"type":"string","description":"The name of the secret."}},"required":["owner","repo","secret_name"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/repos/" + String(args["owner"]) + "/" + String(args["repo"]) + "/codespaces/secrets/" + String(args["secret_name"])) + _qs, {}, undefined);
    }
  },
  {
    name: "codespaces/create-or-update-repo-secret",
    description: "Create or update a repository secret. Creates or updates a repository development environment secret with an encrypted value. Params: owner (required), repo (required), secret_name (required).",
    inputSchema: {"type":"object","properties":{"owner":{"type":"string","description":"The account owner of the repository. The name is not case sensitive."},"repo":{"type":"string","description":"The name of the repository without the `.git` extension. The name is not case sensitive."},"secret_name":{"type":"string","description":"The name of the secret."},"body":{"type":"object","description":"Request body"}},"required":["owner","repo","secret_name","body"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("PUT", ("https://api.github.com/repos/" + String(args["owner"]) + "/" + String(args["repo"]) + "/codespaces/secrets/" + String(args["secret_name"])) + _qs, {}, args["body"]);
    }
  },
  {
    name: "codespaces/delete-repo-secret",
    description: "Delete a repository secret. Deletes a development environment secret in a repository using the secret name. Params: owner (required), repo (required), secret_name (required).",
    inputSchema: {"type":"object","properties":{"owner":{"type":"string","description":"The account owner of the repository. The name is not case sensitive."},"repo":{"type":"string","description":"The name of the repository without the `.git` extension. The name is not case sensitive."},"secret_name":{"type":"string","description":"The name of the secret."}},"required":["owner","repo","secret_name"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("DELETE", ("https://api.github.com/repos/" + String(args["owner"]) + "/" + String(args["repo"]) + "/codespaces/secrets/" + String(args["secret_name"])) + _qs, {}, undefined);
    }
  },
  {
    name: "repos/list-collaborators",
    description: "List repository collaborators. For organization-owned repositories, the list of collaborators includes outside collaborators, organization members that are direct collaborators, organization members with access through team memberships, organization members with access through default organization permissions, and organization owners. Params: owner (required), repo (required).",
    inputSchema: {"type":"object","properties":{"owner":{"type":"string","description":"The account owner of the repository. The name is not case sensitive."},"repo":{"type":"string","description":"The name of the repository without the `.git` extension. The name is not case sensitive."},"affiliation":{"type":"string","enum":["outside","direct","all"],"default":"all","description":"Filter collaborators returned by their affiliation. `outside` means all outside collaborators of an organization-owned repository. `direct` means all collaborators with permissions to an organization-owned repository, regardless of organization membership status. `all` means all collaborators the authenticated user can see."},"permission":{"type":"string","enum":["pull","triage","push","maintain","admin"],"description":"Filter collaborators by the permissions they have on the repository. If not specified, all collaborators will be returned."},"per_page":{"type":"integer","default":30,"description":"The number of results per page (max 100). For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""},"page":{"type":"integer","default":1,"description":"The page number of the results to fetch. For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""}},"required":["owner","repo"]},
    call: function(args) {
    var _q = new URLSearchParams();
    if (args["affiliation"] !== undefined) _q.set("affiliation", String(args["affiliation"]));
    if (args["permission"] !== undefined) _q.set("permission", String(args["permission"]));
    if (args["per_page"] !== undefined) _q.set("per_page", String(args["per_page"]));
    if (args["page"] !== undefined) _q.set("page", String(args["page"]));
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/repos/" + String(args["owner"]) + "/" + String(args["repo"]) + "/collaborators") + _qs, {}, undefined);
    }
  },
  {
    name: "repos/check-collaborator",
    description: "Check if a user is a repository collaborator. For organization-owned repositories, the list of collaborators includes outside collaborators, organization members that are direct collaborators, organization members with access through team memberships, organization members with access through default organization permissions, and organization owners. Params: owner (required), repo (required), username (required).",
    inputSchema: {"type":"object","properties":{"owner":{"type":"string","description":"The account owner of the repository. The name is not case sensitive."},"repo":{"type":"string","description":"The name of the repository without the `.git` extension. The name is not case sensitive."},"username":{"type":"string","description":"The handle for the GitHub user account."}},"required":["owner","repo","username"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/repos/" + String(args["owner"]) + "/" + String(args["repo"]) + "/collaborators/" + String(args["username"])) + _qs, {}, undefined);
    }
  },
  {
    name: "repos/add-collaborator",
    description: "Add a repository collaborator. Add a user to a repository with a specified level of access. Params: owner (required), repo (required), username (required).",
    inputSchema: {"type":"object","properties":{"owner":{"type":"string","description":"The account owner of the repository. The name is not case sensitive."},"repo":{"type":"string","description":"The name of the repository without the `.git` extension. The name is not case sensitive."},"username":{"type":"string","description":"The handle for the GitHub user account."},"body":{"type":"object","description":"Request body"}},"required":["owner","repo","username"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("PUT", ("https://api.github.com/repos/" + String(args["owner"]) + "/" + String(args["repo"]) + "/collaborators/" + String(args["username"])) + _qs, {}, args["body"]);
    }
  },
  {
    name: "repos/remove-collaborator",
    description: "Remove a repository collaborator. Removes a collaborator from a repository. Params: owner (required), repo (required), username (required).",
    inputSchema: {"type":"object","properties":{"owner":{"type":"string","description":"The account owner of the repository. The name is not case sensitive."},"repo":{"type":"string","description":"The name of the repository without the `.git` extension. The name is not case sensitive."},"username":{"type":"string","description":"The handle for the GitHub user account."}},"required":["owner","repo","username"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("DELETE", ("https://api.github.com/repos/" + String(args["owner"]) + "/" + String(args["repo"]) + "/collaborators/" + String(args["username"])) + _qs, {}, undefined);
    }
  },
  {
    name: "repos/get-collaborator-permission-level",
    description: "Get repository permissions for a user. Checks the repository permission and role of a collaborator. Params: owner (required), repo (required), username (required).",
    inputSchema: {"type":"object","properties":{"owner":{"type":"string","description":"The account owner of the repository. The name is not case sensitive."},"repo":{"type":"string","description":"The name of the repository without the `.git` extension. The name is not case sensitive."},"username":{"type":"string","description":"The handle for the GitHub user account."}},"required":["owner","repo","username"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/repos/" + String(args["owner"]) + "/" + String(args["repo"]) + "/collaborators/" + String(args["username"]) + "/permission") + _qs, {}, undefined);
    }
  },
  {
    name: "repos/list-commit-comments-for-repo",
    description: "List commit comments for a repository. Lists the commit comments for a specified repository. Params: owner (required), repo (required).",
    inputSchema: {"type":"object","properties":{"owner":{"type":"string","description":"The account owner of the repository. The name is not case sensitive."},"repo":{"type":"string","description":"The name of the repository without the `.git` extension. The name is not case sensitive."},"per_page":{"type":"integer","default":30,"description":"The number of results per page (max 100). For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""},"page":{"type":"integer","default":1,"description":"The page number of the results to fetch. For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""}},"required":["owner","repo"]},
    call: function(args) {
    var _q = new URLSearchParams();
    if (args["per_page"] !== undefined) _q.set("per_page", String(args["per_page"]));
    if (args["page"] !== undefined) _q.set("page", String(args["page"]));
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/repos/" + String(args["owner"]) + "/" + String(args["repo"]) + "/comments") + _qs, {}, undefined);
    }
  },
  {
    name: "repos/get-commit-comment",
    description: "Get a commit comment. Gets a specified commit comment. Params: owner (required), repo (required), comment_id (required).",
    inputSchema: {"type":"object","properties":{"owner":{"type":"string","description":"The account owner of the repository. The name is not case sensitive."},"repo":{"type":"string","description":"The name of the repository without the `.git` extension. The name is not case sensitive."},"comment_id":{"type":"integer","format":"int64","description":"The unique identifier of the comment."}},"required":["owner","repo","comment_id"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/repos/" + String(args["owner"]) + "/" + String(args["repo"]) + "/comments/" + String(args["comment_id"])) + _qs, {}, undefined);
    }
  },
  {
    name: "repos/update-commit-comment",
    description: "Update a commit comment. Updates the contents of a specified commit comment. Params: owner (required), repo (required), comment_id (required).",
    inputSchema: {"type":"object","properties":{"owner":{"type":"string","description":"The account owner of the repository. The name is not case sensitive."},"repo":{"type":"string","description":"The name of the repository without the `.git` extension. The name is not case sensitive."},"comment_id":{"type":"integer","format":"int64","description":"The unique identifier of the comment."},"body":{"type":"object","description":"Request body"}},"required":["owner","repo","comment_id","body"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("PATCH", ("https://api.github.com/repos/" + String(args["owner"]) + "/" + String(args["repo"]) + "/comments/" + String(args["comment_id"])) + _qs, {}, args["body"]);
    }
  },
  {
    name: "repos/delete-commit-comment",
    description: "Delete a commit comment. Params: owner (required), repo (required), comment_id (required).",
    inputSchema: {"type":"object","properties":{"owner":{"type":"string","description":"The account owner of the repository. The name is not case sensitive."},"repo":{"type":"string","description":"The name of the repository without the `.git` extension. The name is not case sensitive."},"comment_id":{"type":"integer","format":"int64","description":"The unique identifier of the comment."}},"required":["owner","repo","comment_id"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("DELETE", ("https://api.github.com/repos/" + String(args["owner"]) + "/" + String(args["repo"]) + "/comments/" + String(args["comment_id"])) + _qs, {}, undefined);
    }
  },
  {
    name: "reactions/list-for-commit-comment",
    description: "List reactions for a commit comment. List the reactions to a [commit comment](https://docs.github.com/rest/commits/comments#get-a-commit-comment). Params: owner (required), repo (required), comment_id (required).",
    inputSchema: {"type":"object","properties":{"owner":{"type":"string","description":"The account owner of the repository. The name is not case sensitive."},"repo":{"type":"string","description":"The name of the repository without the `.git` extension. The name is not case sensitive."},"comment_id":{"type":"integer","format":"int64","description":"The unique identifier of the comment."},"content":{"type":"string","enum":["+1","-1","laugh","confused","heart","hooray","rocket","eyes"],"description":"Returns a single [reaction type](https://docs.github.com/rest/reactions/reactions#about-reactions). Omit this parameter to list all reactions to a commit comment."},"per_page":{"type":"integer","default":30,"description":"The number of results per page (max 100). For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""},"page":{"type":"integer","default":1,"description":"The page number of the results to fetch. For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""}},"required":["owner","repo","comment_id"]},
    call: function(args) {
    var _q = new URLSearchParams();
    if (args["content"] !== undefined) _q.set("content", String(args["content"]));
    if (args["per_page"] !== undefined) _q.set("per_page", String(args["per_page"]));
    if (args["page"] !== undefined) _q.set("page", String(args["page"]));
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/repos/" + String(args["owner"]) + "/" + String(args["repo"]) + "/comments/" + String(args["comment_id"]) + "/reactions") + _qs, {}, undefined);
    }
  },
  {
    name: "reactions/create-for-commit-comment",
    description: "Create reaction for a commit comment. Create a reaction to a [commit comment](https://docs.github.com/rest/commits/comments#get-a-commit-comment). Params: owner (required), repo (required), comment_id (required).",
    inputSchema: {"type":"object","properties":{"owner":{"type":"string","description":"The account owner of the repository. The name is not case sensitive."},"repo":{"type":"string","description":"The name of the repository without the `.git` extension. The name is not case sensitive."},"comment_id":{"type":"integer","format":"int64","description":"The unique identifier of the comment."},"body":{"type":"object","description":"Request body"}},"required":["owner","repo","comment_id","body"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("POST", ("https://api.github.com/repos/" + String(args["owner"]) + "/" + String(args["repo"]) + "/comments/" + String(args["comment_id"]) + "/reactions") + _qs, {}, args["body"]);
    }
  },
  {
    name: "reactions/delete-for-commit-comment",
    description: "Delete a commit comment reaction. > [!NOTE]\n> You can also specify a repository by `repository_id` using the route `DELETE /repositories/:repository_id/comments/:comment_id/reactions/:reaction_id`. Params: owner (required), repo (required), comment_id (required), reaction_id (required).",
    inputSchema: {"type":"object","properties":{"owner":{"type":"string","description":"The account owner of the repository. The name is not case sensitive."},"repo":{"type":"string","description":"The name of the repository without the `.git` extension. The name is not case sensitive."},"comment_id":{"type":"integer","format":"int64","description":"The unique identifier of the comment."},"reaction_id":{"type":"integer","description":"The unique identifier of the reaction."}},"required":["owner","repo","comment_id","reaction_id"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("DELETE", ("https://api.github.com/repos/" + String(args["owner"]) + "/" + String(args["repo"]) + "/comments/" + String(args["comment_id"]) + "/reactions/" + String(args["reaction_id"])) + _qs, {}, undefined);
    }
  },
  {
    name: "repos/list-commits",
    description: "List commits. **Signature verification object**\n\nThe response will include a `verification` object that describes the result of verifying the commit's signature. Params: owner (required), repo (required).",
    inputSchema: {"type":"object","properties":{"owner":{"type":"string","description":"The account owner of the repository. The name is not case sensitive."},"repo":{"type":"string","description":"The name of the repository without the `.git` extension. The name is not case sensitive."},"sha":{"type":"string","description":"SHA or branch to start listing commits from. Default: the repository’s default branch (usually `main`)."},"path":{"type":"string","description":"Only commits containing this file path will be returned."},"author":{"type":"string","description":"GitHub username or email address to use to filter by commit author."},"committer":{"type":"string","description":"GitHub username or email address to use to filter by commit committer."},"since":{"type":"string","format":"date-time","description":"Only show results that were last updated after the given time. This is a timestamp in [ISO 8601](https://en.wikipedia.org/wiki/ISO_8601) format: `YYYY-MM-DDTHH:MM:SSZ`. Due to limitations of Git, timestamps must be between 1970-01-01 and 2099-12-31 (inclusive) or unexpected results may be returned."},"until":{"type":"string","format":"date-time","description":"Only commits before this date will be returned. This is a timestamp in [ISO 8601](https://en.wikipedia.org/wiki/ISO_8601) format: `YYYY-MM-DDTHH:MM:SSZ`. Due to limitations of Git, timestamps must be between 1970-01-01 and 2099-12-31 (inclusive) or unexpected results may be returned."},"per_page":{"type":"integer","default":30,"description":"The number of results per page (max 100). For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""},"page":{"type":"integer","default":1,"description":"The page number of the results to fetch. For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""}},"required":["owner","repo"]},
    call: function(args) {
    var _q = new URLSearchParams();
    if (args["sha"] !== undefined) _q.set("sha", String(args["sha"]));
    if (args["path"] !== undefined) _q.set("path", String(args["path"]));
    if (args["author"] !== undefined) _q.set("author", String(args["author"]));
    if (args["committer"] !== undefined) _q.set("committer", String(args["committer"]));
    if (args["since"] !== undefined) _q.set("since", String(args["since"]));
    if (args["until"] !== undefined) _q.set("until", String(args["until"]));
    if (args["per_page"] !== undefined) _q.set("per_page", String(args["per_page"]));
    if (args["page"] !== undefined) _q.set("page", String(args["page"]));
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/repos/" + String(args["owner"]) + "/" + String(args["repo"]) + "/commits") + _qs, {}, undefined);
    }
  },
  {
    name: "repos/list-branches-for-head-commit",
    description: "List branches for HEAD commit. Protected branches are available in public repositories with GitHub Free and GitHub Free for organizations, and in public and private repositories with GitHub Pro, GitHub Team, GitHub Enterprise Cloud, and GitHub Enterprise Server. Params: owner (required), repo (required), commit_sha (required).",
    inputSchema: {"type":"object","properties":{"owner":{"type":"string","description":"The account owner of the repository. The name is not case sensitive."},"repo":{"type":"string","description":"The name of the repository without the `.git` extension. The name is not case sensitive."},"commit_sha":{"type":"string","description":"The SHA of the commit."}},"required":["owner","repo","commit_sha"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/repos/" + String(args["owner"]) + "/" + String(args["repo"]) + "/commits/" + String(args["commit_sha"]) + "/branches-where-head") + _qs, {}, undefined);
    }
  },
  {
    name: "repos/list-comments-for-commit",
    description: "List commit comments. Lists the comments for a specified commit. Params: owner (required), repo (required), commit_sha (required).",
    inputSchema: {"type":"object","properties":{"owner":{"type":"string","description":"The account owner of the repository. The name is not case sensitive."},"repo":{"type":"string","description":"The name of the repository without the `.git` extension. The name is not case sensitive."},"commit_sha":{"type":"string","description":"The SHA of the commit."},"per_page":{"type":"integer","default":30,"description":"The number of results per page (max 100). For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""},"page":{"type":"integer","default":1,"description":"The page number of the results to fetch. For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""}},"required":["owner","repo","commit_sha"]},
    call: function(args) {
    var _q = new URLSearchParams();
    if (args["per_page"] !== undefined) _q.set("per_page", String(args["per_page"]));
    if (args["page"] !== undefined) _q.set("page", String(args["page"]));
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/repos/" + String(args["owner"]) + "/" + String(args["repo"]) + "/commits/" + String(args["commit_sha"]) + "/comments") + _qs, {}, undefined);
    }
  },
  {
    name: "repos/create-commit-comment",
    description: "Create a commit comment. Create a comment for a commit using its `:commit_sha`. Params: owner (required), repo (required), commit_sha (required).",
    inputSchema: {"type":"object","properties":{"owner":{"type":"string","description":"The account owner of the repository. The name is not case sensitive."},"repo":{"type":"string","description":"The name of the repository without the `.git` extension. The name is not case sensitive."},"commit_sha":{"type":"string","description":"The SHA of the commit."},"body":{"type":"object","description":"Request body"}},"required":["owner","repo","commit_sha","body"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("POST", ("https://api.github.com/repos/" + String(args["owner"]) + "/" + String(args["repo"]) + "/commits/" + String(args["commit_sha"]) + "/comments") + _qs, {}, args["body"]);
    }
  },
  {
    name: "repos/list-pull-requests-associated-with-commit",
    description: "List pull requests associated with a commit. Lists the merged pull request that introduced the commit to the repository. Params: owner (required), repo (required), commit_sha (required).",
    inputSchema: {"type":"object","properties":{"owner":{"type":"string","description":"The account owner of the repository. The name is not case sensitive."},"repo":{"type":"string","description":"The name of the repository without the `.git` extension. The name is not case sensitive."},"commit_sha":{"type":"string","description":"The SHA of the commit."},"per_page":{"type":"integer","default":30,"description":"The number of results per page (max 100). For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""},"page":{"type":"integer","default":1,"description":"The page number of the results to fetch. For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""}},"required":["owner","repo","commit_sha"]},
    call: function(args) {
    var _q = new URLSearchParams();
    if (args["per_page"] !== undefined) _q.set("per_page", String(args["per_page"]));
    if (args["page"] !== undefined) _q.set("page", String(args["page"]));
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/repos/" + String(args["owner"]) + "/" + String(args["repo"]) + "/commits/" + String(args["commit_sha"]) + "/pulls") + _qs, {}, undefined);
    }
  },
  {
    name: "repos/get-commit",
    description: "Get a commit. Returns the contents of a single commit reference. Params: owner (required), repo (required), ref (required).",
    inputSchema: {"type":"object","properties":{"owner":{"type":"string","description":"The account owner of the repository. The name is not case sensitive."},"repo":{"type":"string","description":"The name of the repository without the `.git` extension. The name is not case sensitive."},"page":{"type":"integer","default":1,"description":"The page number of the results to fetch. For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""},"per_page":{"type":"integer","default":30,"description":"The number of results per page (max 100). For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""},"ref":{"type":"string","description":"The commit reference. Can be a commit SHA, branch name (`heads/BRANCH_NAME`), or tag name (`tags/TAG_NAME`). For more information, see \"[Git References](https://git-scm.com/book/en/v2/Git-Internals-Git-References)\" in the Git documentation."}},"required":["owner","repo","ref"]},
    call: function(args) {
    var _q = new URLSearchParams();
    if (args["page"] !== undefined) _q.set("page", String(args["page"]));
    if (args["per_page"] !== undefined) _q.set("per_page", String(args["per_page"]));
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/repos/" + String(args["owner"]) + "/" + String(args["repo"]) + "/commits/" + String(args["ref"])) + _qs, {}, undefined);
    }
  },
  {
    name: "checks/list-for-ref",
    description: "List check runs for a Git reference. Lists check runs for a commit ref. Params: owner (required), repo (required), ref (required).",
    inputSchema: {"type":"object","properties":{"owner":{"type":"string","description":"The account owner of the repository. The name is not case sensitive."},"repo":{"type":"string","description":"The name of the repository without the `.git` extension. The name is not case sensitive."},"ref":{"type":"string","description":"The commit reference. Can be a commit SHA, branch name (`heads/BRANCH_NAME`), or tag name (`tags/TAG_NAME`). For more information, see \"[Git References](https://git-scm.com/book/en/v2/Git-Internals-Git-References)\" in the Git documentation."},"check_name":{"type":"string","description":"Returns check runs with the specified `name`."},"status":{"type":"string","enum":["queued","in_progress","completed"],"description":"Returns check runs with the specified `status`."},"filter":{"type":"string","enum":["latest","all"],"default":"latest","description":"Filters check runs by their `completed_at` timestamp. `latest` returns the most recent check runs."},"per_page":{"type":"integer","default":30,"description":"The number of results per page (max 100). For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""},"page":{"type":"integer","default":1,"description":"The page number of the results to fetch. For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""},"app_id":{"type":"integer"}},"required":["owner","repo","ref"]},
    call: function(args) {
    var _q = new URLSearchParams();
    if (args["check_name"] !== undefined) _q.set("check_name", String(args["check_name"]));
    if (args["status"] !== undefined) _q.set("status", String(args["status"]));
    if (args["filter"] !== undefined) _q.set("filter", String(args["filter"]));
    if (args["per_page"] !== undefined) _q.set("per_page", String(args["per_page"]));
    if (args["page"] !== undefined) _q.set("page", String(args["page"]));
    if (args["app_id"] !== undefined) _q.set("app_id", String(args["app_id"]));
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/repos/" + String(args["owner"]) + "/" + String(args["repo"]) + "/commits/" + String(args["ref"]) + "/check-runs") + _qs, {}, undefined);
    }
  },
  {
    name: "checks/list-suites-for-ref",
    description: "List check suites for a Git reference. Lists check suites for a commit `ref`. Params: owner (required), repo (required), ref (required).",
    inputSchema: {"type":"object","properties":{"owner":{"type":"string","description":"The account owner of the repository. The name is not case sensitive."},"repo":{"type":"string","description":"The name of the repository without the `.git` extension. The name is not case sensitive."},"ref":{"type":"string","description":"The commit reference. Can be a commit SHA, branch name (`heads/BRANCH_NAME`), or tag name (`tags/TAG_NAME`). For more information, see \"[Git References](https://git-scm.com/book/en/v2/Git-Internals-Git-References)\" in the Git documentation."},"app_id":{"type":"integer","description":"Filters check suites by GitHub App `id`."},"check_name":{"type":"string","description":"Returns check runs with the specified `name`."},"per_page":{"type":"integer","default":30,"description":"The number of results per page (max 100). For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""},"page":{"type":"integer","default":1,"description":"The page number of the results to fetch. For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""}},"required":["owner","repo","ref"]},
    call: function(args) {
    var _q = new URLSearchParams();
    if (args["app_id"] !== undefined) _q.set("app_id", String(args["app_id"]));
    if (args["check_name"] !== undefined) _q.set("check_name", String(args["check_name"]));
    if (args["per_page"] !== undefined) _q.set("per_page", String(args["per_page"]));
    if (args["page"] !== undefined) _q.set("page", String(args["page"]));
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/repos/" + String(args["owner"]) + "/" + String(args["repo"]) + "/commits/" + String(args["ref"]) + "/check-suites") + _qs, {}, undefined);
    }
  },
  {
    name: "repos/get-combined-status-for-ref",
    description: "Get the combined status for a specific reference. Users with pull access in a repository can access a combined view of commit statuses for a given ref. Params: owner (required), repo (required), ref (required).",
    inputSchema: {"type":"object","properties":{"owner":{"type":"string","description":"The account owner of the repository. The name is not case sensitive."},"repo":{"type":"string","description":"The name of the repository without the `.git` extension. The name is not case sensitive."},"ref":{"type":"string","description":"The commit reference. Can be a commit SHA, branch name (`heads/BRANCH_NAME`), or tag name (`tags/TAG_NAME`). For more information, see \"[Git References](https://git-scm.com/book/en/v2/Git-Internals-Git-References)\" in the Git documentation."},"per_page":{"type":"integer","default":30,"description":"The number of results per page (max 100). For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""},"page":{"type":"integer","default":1,"description":"The page number of the results to fetch. For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""}},"required":["owner","repo","ref"]},
    call: function(args) {
    var _q = new URLSearchParams();
    if (args["per_page"] !== undefined) _q.set("per_page", String(args["per_page"]));
    if (args["page"] !== undefined) _q.set("page", String(args["page"]));
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/repos/" + String(args["owner"]) + "/" + String(args["repo"]) + "/commits/" + String(args["ref"]) + "/status") + _qs, {}, undefined);
    }
  },
  {
    name: "repos/list-commit-statuses-for-ref",
    description: "List commit statuses for a reference. Users with pull access in a repository can view commit statuses for a given ref. Params: owner (required), repo (required), ref (required).",
    inputSchema: {"type":"object","properties":{"owner":{"type":"string","description":"The account owner of the repository. The name is not case sensitive."},"repo":{"type":"string","description":"The name of the repository without the `.git` extension. The name is not case sensitive."},"ref":{"type":"string","description":"The commit reference. Can be a commit SHA, branch name (`heads/BRANCH_NAME`), or tag name (`tags/TAG_NAME`). For more information, see \"[Git References](https://git-scm.com/book/en/v2/Git-Internals-Git-References)\" in the Git documentation."},"per_page":{"type":"integer","default":30,"description":"The number of results per page (max 100). For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""},"page":{"type":"integer","default":1,"description":"The page number of the results to fetch. For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""}},"required":["owner","repo","ref"]},
    call: function(args) {
    var _q = new URLSearchParams();
    if (args["per_page"] !== undefined) _q.set("per_page", String(args["per_page"]));
    if (args["page"] !== undefined) _q.set("page", String(args["page"]));
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/repos/" + String(args["owner"]) + "/" + String(args["repo"]) + "/commits/" + String(args["ref"]) + "/statuses") + _qs, {}, undefined);
    }
  },
  {
    name: "repos/get-community-profile-metrics",
    description: "Get community profile metrics. Returns all community profile metrics for a repository. Params: owner (required), repo (required).",
    inputSchema: {"type":"object","properties":{"owner":{"type":"string","description":"The account owner of the repository. The name is not case sensitive."},"repo":{"type":"string","description":"The name of the repository without the `.git` extension. The name is not case sensitive."}},"required":["owner","repo"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/repos/" + String(args["owner"]) + "/" + String(args["repo"]) + "/community/profile") + _qs, {}, undefined);
    }
  },
  {
    name: "repos/compare-commits",
    description: "Compare two commits. Compares two commits against one another. Params: owner (required), repo (required), basehead (required).",
    inputSchema: {"type":"object","properties":{"owner":{"type":"string","description":"The account owner of the repository. The name is not case sensitive."},"repo":{"type":"string","description":"The name of the repository without the `.git` extension. The name is not case sensitive."},"page":{"type":"integer","default":1,"description":"The page number of the results to fetch. For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""},"per_page":{"type":"integer","default":30,"description":"The number of results per page (max 100). For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""},"basehead":{"type":"string","description":"The base branch and head branch to compare. This parameter expects the format `BASE...HEAD`. Both must be branch names in `repo`. To compare with a branch that exists in a different repository in the same network as `repo`, the `basehead` parameter expects the format `USERNAME:BASE...USERNAME:HEAD`."}},"required":["owner","repo","basehead"]},
    call: function(args) {
    var _q = new URLSearchParams();
    if (args["page"] !== undefined) _q.set("page", String(args["page"]));
    if (args["per_page"] !== undefined) _q.set("per_page", String(args["per_page"]));
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/repos/" + String(args["owner"]) + "/" + String(args["repo"]) + "/compare/" + String(args["basehead"])) + _qs, {}, undefined);
    }
  },
  {
    name: "repos/get-content",
    description: "Get repository content. Gets the contents of a file or directory in a repository. Params: owner (required), repo (required), path (required).",
    inputSchema: {"type":"object","properties":{"owner":{"type":"string","description":"The account owner of the repository. The name is not case sensitive."},"repo":{"type":"string","description":"The name of the repository without the `.git` extension. The name is not case sensitive."},"path":{"type":"string","description":"path parameter"},"ref":{"type":"string","description":"The name of the commit/branch/tag. Default: the repository’s default branch."},"body":{"type":"object","description":"Request body"}},"required":["owner","repo","path"]},
    call: function(args) {
    var _q = new URLSearchParams();
    if (args["ref"] !== undefined) _q.set("ref", String(args["ref"]));
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/repos/" + String(args["owner"]) + "/" + String(args["repo"]) + "/contents/" + String(args["path"])) + _qs, {}, args["body"]);
    }
  },
  {
    name: "repos/create-or-update-file-contents",
    description: "Create or update file contents. Creates a new file or replaces an existing file in a repository. Params: owner (required), repo (required), path (required).",
    inputSchema: {"type":"object","properties":{"owner":{"type":"string","description":"The account owner of the repository. The name is not case sensitive."},"repo":{"type":"string","description":"The name of the repository without the `.git` extension. The name is not case sensitive."},"path":{"type":"string","description":"path parameter"},"body":{"type":"object","description":"Request body"}},"required":["owner","repo","path","body"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("PUT", ("https://api.github.com/repos/" + String(args["owner"]) + "/" + String(args["repo"]) + "/contents/" + String(args["path"])) + _qs, {}, args["body"]);
    }
  },
  {
    name: "repos/delete-file",
    description: "Delete a file. Deletes a file in a repository. Params: owner (required), repo (required), path (required).",
    inputSchema: {"type":"object","properties":{"owner":{"type":"string","description":"The account owner of the repository. The name is not case sensitive."},"repo":{"type":"string","description":"The name of the repository without the `.git` extension. The name is not case sensitive."},"path":{"type":"string","description":"path parameter"},"body":{"type":"object","description":"Request body"}},"required":["owner","repo","path","body"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("DELETE", ("https://api.github.com/repos/" + String(args["owner"]) + "/" + String(args["repo"]) + "/contents/" + String(args["path"])) + _qs, {}, args["body"]);
    }
  },
  {
    name: "repos/list-contributors",
    description: "List repository contributors. Lists contributors to the specified repository and sorts them by the number of commits per contributor in descending order. Params: owner (required), repo (required).",
    inputSchema: {"type":"object","properties":{"owner":{"type":"string","description":"The account owner of the repository. The name is not case sensitive."},"repo":{"type":"string","description":"The name of the repository without the `.git` extension. The name is not case sensitive."},"anon":{"type":"string","description":"Set to `1` or `true` to include anonymous contributors in results."},"per_page":{"type":"integer","default":30,"description":"The number of results per page (max 100). For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""},"page":{"type":"integer","default":1,"description":"The page number of the results to fetch. For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""}},"required":["owner","repo"]},
    call: function(args) {
    var _q = new URLSearchParams();
    if (args["anon"] !== undefined) _q.set("anon", String(args["anon"]));
    if (args["per_page"] !== undefined) _q.set("per_page", String(args["per_page"]));
    if (args["page"] !== undefined) _q.set("page", String(args["page"]));
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/repos/" + String(args["owner"]) + "/" + String(args["repo"]) + "/contributors") + _qs, {}, undefined);
    }
  },
  {
    name: "copilot/get-copilot-cloud-agent-configuration",
    description: "Get Copilot cloud agent configuration for a repository. > [!NOTE]\n> This endpoint is in public preview and is subject to change. Params: owner (required), repo (required).",
    inputSchema: {"type":"object","properties":{"owner":{"type":"string","description":"The account owner of the repository. The name is not case sensitive."},"repo":{"type":"string","description":"The name of the repository without the `.git` extension. The name is not case sensitive."}},"required":["owner","repo"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/repos/" + String(args["owner"]) + "/" + String(args["repo"]) + "/copilot/cloud-agent/configuration") + _qs, {}, undefined);
    }
  },
  {
    name: "dependabot/list-alerts-for-repo",
    description: "List Dependabot alerts for a repository. OAuth app tokens and personal access tokens (classic) need the `security_events` scope to use this endpoint. Params: owner (required), repo (required).",
    inputSchema: {"type":"object","properties":{"owner":{"type":"string","description":"The account owner of the repository. The name is not case sensitive."},"repo":{"type":"string","description":"The name of the repository without the `.git` extension. The name is not case sensitive."},"classification":{"type":"string","description":"A comma-separated list of vulnerability classifications. If specified, only alerts for vulnerabilities with these classifications will be returned.\n\nCan be: `malware`, `general`"},"state":{"type":"string","description":"A comma-separated list of states. If specified, only alerts with these states will be returned.\n\nCan be: `auto_dismissed`, `dismissed`, `fixed`, `open`"},"severity":{"type":"string","description":"A comma-separated list of severities. If specified, only alerts with these severities will be returned.\n\nCan be: `low`, `medium`, `high`, `critical`"},"ecosystem":{"type":"string","description":"A comma-separated list of ecosystems. If specified, only alerts for these ecosystems will be returned.\n\nCan be: `composer`, `go`, `maven`, `npm`, `nuget`, `pip`, `pub`, `rubygems`, `rust`"},"package":{"type":"string","description":"A comma-separated list of package names. If specified, only alerts for these packages will be returned."},"manifest":{"type":"string","description":"A comma-separated list of full manifest paths. If specified, only alerts for these manifests will be returned."},"epss_percentage":{"type":"string","description":"CVE Exploit Prediction Scoring System (EPSS) percentage. Can be specified as:\n- An exact number (`n`)\n- Comparators such as `>n`, `<n`, `>=n`, `<=n`\n- A range like `n..n`, where `n` is a number from 0.0 to 1.0\n\nFilters the list of alerts based on EPSS percentages. If specified, only alerts with the provided EPSS percentages will be returned."},"has":{"oneOf":[{"type":"string"},{"type":"array","items":{"type":"string","enum":["patch"]}}],"description":"Filters the list of alerts based on whether the alert has the given value. If specified, only alerts meeting this criterion will be returned.\nMultiple `has` filters can be passed to filter for alerts that have all of the values. Currently, only `patch` is supported."},"assignee":{"type":"string","description":"Filter alerts by assignees.\nProvide a comma-separated list of user handles (e.g., `octocat` or `octocat,hubot`) to return alerts assigned to any of the specified users.\nUse `*` to list alerts with at least one assignee or `none` to list alerts with no assignees."},"scope":{"type":"string","enum":["development","runtime"],"description":"The scope of the vulnerable dependency. If specified, only alerts with this scope will be returned."},"sort":{"type":"string","enum":["created","updated","epss_percentage"],"default":"created","description":"The property by which to sort the results.\n`created` means when the alert was created.\n`updated` means when the alert's state last changed.\n`epss_percentage` sorts alerts by the Exploit Prediction Scoring System (EPSS) percentage."},"direction":{"type":"string","enum":["asc","desc"],"default":"desc","description":"The direction to sort the results by."},"before":{"type":"string","description":"A cursor, as given in the [Link header](https://docs.github.com/rest/guides/using-pagination-in-the-rest-api#using-link-headers). If specified, the query only searches for results before this cursor. For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""},"after":{"type":"string","description":"A cursor, as given in the [Link header](https://docs.github.com/rest/guides/using-pagination-in-the-rest-api#using-link-headers). If specified, the query only searches for results after this cursor. For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""},"per_page":{"type":"integer","default":30,"description":"The number of results per page (max 100). For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""}},"required":["owner","repo"]},
    call: function(args) {
    var _q = new URLSearchParams();
    if (args["classification"] !== undefined) _q.set("classification", String(args["classification"]));
    if (args["state"] !== undefined) _q.set("state", String(args["state"]));
    if (args["severity"] !== undefined) _q.set("severity", String(args["severity"]));
    if (args["ecosystem"] !== undefined) _q.set("ecosystem", String(args["ecosystem"]));
    if (args["package"] !== undefined) _q.set("package", String(args["package"]));
    if (args["manifest"] !== undefined) _q.set("manifest", String(args["manifest"]));
    if (args["epss_percentage"] !== undefined) _q.set("epss_percentage", String(args["epss_percentage"]));
    if (args["has"] !== undefined) _q.set("has", String(args["has"]));
    if (args["assignee"] !== undefined) _q.set("assignee", String(args["assignee"]));
    if (args["scope"] !== undefined) _q.set("scope", String(args["scope"]));
    if (args["sort"] !== undefined) _q.set("sort", String(args["sort"]));
    if (args["direction"] !== undefined) _q.set("direction", String(args["direction"]));
    if (args["before"] !== undefined) _q.set("before", String(args["before"]));
    if (args["after"] !== undefined) _q.set("after", String(args["after"]));
    if (args["per_page"] !== undefined) _q.set("per_page", String(args["per_page"]));
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/repos/" + String(args["owner"]) + "/" + String(args["repo"]) + "/dependabot/alerts") + _qs, {}, undefined);
    }
  },
  {
    name: "dependabot/get-alert",
    description: "Get a Dependabot alert. OAuth app tokens and personal access tokens (classic) need the `security_events` scope to use this endpoint. Params: owner (required), repo (required), alert_number (required).",
    inputSchema: {"type":"object","properties":{"owner":{"type":"string","description":"The account owner of the repository. The name is not case sensitive."},"repo":{"type":"string","description":"The name of the repository without the `.git` extension. The name is not case sensitive."},"alert_number":{"$ref":"#/components/schemas/alert-number","description":"The number that identifies a Dependabot alert in its repository.\nYou can find this at the end of the URL for a Dependabot alert within GitHub,\nor in `number` fields in the response from the\n`GET /repos/{owner}/{repo}/dependabot/alerts` operation."}},"required":["owner","repo","alert_number"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/repos/" + String(args["owner"]) + "/" + String(args["repo"]) + "/dependabot/alerts/" + String(args["alert_number"])) + _qs, {}, undefined);
    }
  },
  {
    name: "dependabot/update-alert",
    description: "Update a Dependabot alert. The authenticated user must have access to security alerts for the repository to use this endpoint. Params: owner (required), repo (required), alert_number (required).",
    inputSchema: {"type":"object","properties":{"owner":{"type":"string","description":"The account owner of the repository. The name is not case sensitive."},"repo":{"type":"string","description":"The name of the repository without the `.git` extension. The name is not case sensitive."},"alert_number":{"$ref":"#/components/schemas/alert-number","description":"The number that identifies a Dependabot alert in its repository.\nYou can find this at the end of the URL for a Dependabot alert within GitHub,\nor in `number` fields in the response from the\n`GET /repos/{owner}/{repo}/dependabot/alerts` operation."},"body":{"type":"object","description":"Request body"}},"required":["owner","repo","alert_number","body"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("PATCH", ("https://api.github.com/repos/" + String(args["owner"]) + "/" + String(args["repo"]) + "/dependabot/alerts/" + String(args["alert_number"])) + _qs, {}, args["body"]);
    }
  },
  {
    name: "dependabot/list-repo-secrets",
    description: "List repository secrets. Lists all secrets available in a repository without revealing their encrypted\nvalues. Params: owner (required), repo (required).",
    inputSchema: {"type":"object","properties":{"owner":{"type":"string","description":"The account owner of the repository. The name is not case sensitive."},"repo":{"type":"string","description":"The name of the repository without the `.git` extension. The name is not case sensitive."},"per_page":{"type":"integer","default":30,"description":"The number of results per page (max 100). For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""},"page":{"type":"integer","default":1,"description":"The page number of the results to fetch. For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""}},"required":["owner","repo"]},
    call: function(args) {
    var _q = new URLSearchParams();
    if (args["per_page"] !== undefined) _q.set("per_page", String(args["per_page"]));
    if (args["page"] !== undefined) _q.set("page", String(args["page"]));
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/repos/" + String(args["owner"]) + "/" + String(args["repo"]) + "/dependabot/secrets") + _qs, {}, undefined);
    }
  },
  {
    name: "dependabot/get-repo-public-key",
    description: "Get a repository public key. Gets your public key, which you need to encrypt secrets. Params: owner (required), repo (required).",
    inputSchema: {"type":"object","properties":{"owner":{"type":"string","description":"The account owner of the repository. The name is not case sensitive."},"repo":{"type":"string","description":"The name of the repository without the `.git` extension. The name is not case sensitive."}},"required":["owner","repo"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/repos/" + String(args["owner"]) + "/" + String(args["repo"]) + "/dependabot/secrets/public-key") + _qs, {}, undefined);
    }
  },
  {
    name: "dependabot/get-repo-secret",
    description: "Get a repository secret. Gets a single repository secret without revealing its encrypted value. Params: owner (required), repo (required), secret_name (required).",
    inputSchema: {"type":"object","properties":{"owner":{"type":"string","description":"The account owner of the repository. The name is not case sensitive."},"repo":{"type":"string","description":"The name of the repository without the `.git` extension. The name is not case sensitive."},"secret_name":{"type":"string","description":"The name of the secret."}},"required":["owner","repo","secret_name"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/repos/" + String(args["owner"]) + "/" + String(args["repo"]) + "/dependabot/secrets/" + String(args["secret_name"])) + _qs, {}, undefined);
    }
  },
  {
    name: "dependabot/create-or-update-repo-secret",
    description: "Create or update a repository secret. Creates or updates a repository secret with an encrypted value. Params: owner (required), repo (required), secret_name (required).",
    inputSchema: {"type":"object","properties":{"owner":{"type":"string","description":"The account owner of the repository. The name is not case sensitive."},"repo":{"type":"string","description":"The name of the repository without the `.git` extension. The name is not case sensitive."},"secret_name":{"type":"string","description":"The name of the secret."},"body":{"type":"object","description":"Request body"}},"required":["owner","repo","secret_name","body"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("PUT", ("https://api.github.com/repos/" + String(args["owner"]) + "/" + String(args["repo"]) + "/dependabot/secrets/" + String(args["secret_name"])) + _qs, {}, args["body"]);
    }
  },
  {
    name: "dependabot/delete-repo-secret",
    description: "Delete a repository secret. Deletes a secret in a repository using the secret name. Params: owner (required), repo (required), secret_name (required).",
    inputSchema: {"type":"object","properties":{"owner":{"type":"string","description":"The account owner of the repository. The name is not case sensitive."},"repo":{"type":"string","description":"The name of the repository without the `.git` extension. The name is not case sensitive."},"secret_name":{"type":"string","description":"The name of the secret."}},"required":["owner","repo","secret_name"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("DELETE", ("https://api.github.com/repos/" + String(args["owner"]) + "/" + String(args["repo"]) + "/dependabot/secrets/" + String(args["secret_name"])) + _qs, {}, undefined);
    }
  },
  {
    name: "dependency-graph/diff-range",
    description: "Get a diff of the dependencies between commits. Gets the diff of the dependency changes between two commits of a repository, based on the changes to the dependency manifests made in those commits. Params: owner (required), repo (required), basehead (required).",
    inputSchema: {"type":"object","properties":{"owner":{"type":"string","description":"The account owner of the repository. The name is not case sensitive."},"repo":{"type":"string","description":"The name of the repository without the `.git` extension. The name is not case sensitive."},"basehead":{"type":"string","description":"The base and head Git revisions to compare. The Git revisions will be resolved to commit SHAs. Named revisions will be resolved to their corresponding HEAD commits, and an appropriate merge base will be determined. This parameter expects the format `{base}...{head}`."},"name":{"type":"string","description":"The full path, relative to the repository root, of the dependency manifest file."}},"required":["owner","repo","basehead"]},
    call: function(args) {
    var _q = new URLSearchParams();
    if (args["name"] !== undefined) _q.set("name", String(args["name"]));
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/repos/" + String(args["owner"]) + "/" + String(args["repo"]) + "/dependency-graph/compare/" + String(args["basehead"])) + _qs, {}, undefined);
    }
  },
  {
    name: "dependency-graph/export-sbom",
    description: "Export a software bill of materials (SBOM) for a repository. Exports the software bill of materials (SBOM) for a repository in SPDX JSON format. Params: owner (required), repo (required).",
    inputSchema: {"type":"object","properties":{"owner":{"type":"string","description":"The account owner of the repository. The name is not case sensitive."},"repo":{"type":"string","description":"The name of the repository without the `.git` extension. The name is not case sensitive."}},"required":["owner","repo"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/repos/" + String(args["owner"]) + "/" + String(args["repo"]) + "/dependency-graph/sbom") + _qs, {}, undefined);
    }
  },
  {
    name: "dependency-graph/fetch-sbom-report",
    description: "Fetch a software bill of materials (SBOM) for a repository. Fetches a previously generated software bill of materials (SBOM) for a repository. Params: owner (required), repo (required), sbom_uuid (required).",
    inputSchema: {"type":"object","properties":{"owner":{"type":"string","description":"The account owner of the repository. The name is not case sensitive."},"repo":{"type":"string","description":"The name of the repository without the `.git` extension. The name is not case sensitive."},"sbom_uuid":{"type":"string","description":"The unique identifier of the SBOM export."}},"required":["owner","repo","sbom_uuid"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/repos/" + String(args["owner"]) + "/" + String(args["repo"]) + "/dependency-graph/sbom/fetch-report/" + String(args["sbom_uuid"])) + _qs, {}, undefined);
    }
  },
  {
    name: "dependency-graph/generate-sbom-report",
    description: "Request generation of a software bill of materials (SBOM) for a repository. Triggers a job to generate a software bill of materials (SBOM) for a repository in SPDX JSON format. Params: owner (required), repo (required).",
    inputSchema: {"type":"object","properties":{"owner":{"type":"string","description":"The account owner of the repository. The name is not case sensitive."},"repo":{"type":"string","description":"The name of the repository without the `.git` extension. The name is not case sensitive."}},"required":["owner","repo"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/repos/" + String(args["owner"]) + "/" + String(args["repo"]) + "/dependency-graph/sbom/generate-report") + _qs, {}, undefined);
    }
  },
  {
    name: "dependency-graph/create-repository-snapshot",
    description: "Create a snapshot of dependencies for a repository. Create a new snapshot of a repository's dependencies. Params: owner (required), repo (required).",
    inputSchema: {"type":"object","properties":{"owner":{"type":"string","description":"The account owner of the repository. The name is not case sensitive."},"repo":{"type":"string","description":"The name of the repository without the `.git` extension. The name is not case sensitive."},"body":{"type":"object","description":"Request body"}},"required":["owner","repo","body"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("POST", ("https://api.github.com/repos/" + String(args["owner"]) + "/" + String(args["repo"]) + "/dependency-graph/snapshots") + _qs, {}, args["body"]);
    }
  },
  {
    name: "repos/list-deployments",
    description: "List deployments. Simple filtering of deployments is available via query parameters:. Params: owner (required), repo (required).",
    inputSchema: {"type":"object","properties":{"owner":{"type":"string","description":"The account owner of the repository. The name is not case sensitive."},"repo":{"type":"string","description":"The name of the repository without the `.git` extension. The name is not case sensitive."},"sha":{"type":"string","default":"none","description":"The SHA recorded at creation time."},"ref":{"type":"string","default":"none","description":"The name of the ref. This can be a branch, tag, or SHA."},"task":{"type":"string","default":"none","description":"The name of the task for the deployment (e.g., `deploy` or `deploy:migrations`)."},"environment":{"type":"string","default":"none","nullable":true,"description":"The name of the environment that was deployed to (e.g., `staging` or `production`)."},"per_page":{"type":"integer","default":30,"description":"The number of results per page (max 100). For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""},"page":{"type":"integer","default":1,"description":"The page number of the results to fetch. For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""}},"required":["owner","repo"]},
    call: function(args) {
    var _q = new URLSearchParams();
    if (args["sha"] !== undefined) _q.set("sha", String(args["sha"]));
    if (args["ref"] !== undefined) _q.set("ref", String(args["ref"]));
    if (args["task"] !== undefined) _q.set("task", String(args["task"]));
    if (args["environment"] !== undefined) _q.set("environment", String(args["environment"]));
    if (args["per_page"] !== undefined) _q.set("per_page", String(args["per_page"]));
    if (args["page"] !== undefined) _q.set("page", String(args["page"]));
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/repos/" + String(args["owner"]) + "/" + String(args["repo"]) + "/deployments") + _qs, {}, undefined);
    }
  },
  {
    name: "repos/create-deployment",
    description: "Create a deployment. Deployments offer a few configurable parameters with certain defaults. Params: owner (required), repo (required).",
    inputSchema: {"type":"object","properties":{"owner":{"type":"string","description":"The account owner of the repository. The name is not case sensitive."},"repo":{"type":"string","description":"The name of the repository without the `.git` extension. The name is not case sensitive."},"body":{"type":"object","description":"Request body"}},"required":["owner","repo","body"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("POST", ("https://api.github.com/repos/" + String(args["owner"]) + "/" + String(args["repo"]) + "/deployments") + _qs, {}, args["body"]);
    }
  },
  {
    name: "repos/get-deployment",
    description: "Get a deployment. Params: owner (required), repo (required), deployment_id (required).",
    inputSchema: {"type":"object","properties":{"owner":{"type":"string","description":"The account owner of the repository. The name is not case sensitive."},"repo":{"type":"string","description":"The name of the repository without the `.git` extension. The name is not case sensitive."},"deployment_id":{"type":"integer","description":"deployment_id parameter"}},"required":["owner","repo","deployment_id"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/repos/" + String(args["owner"]) + "/" + String(args["repo"]) + "/deployments/" + String(args["deployment_id"])) + _qs, {}, undefined);
    }
  },
  {
    name: "repos/delete-deployment",
    description: "Delete a deployment. If the repository only has one deployment, you can delete the deployment regardless of its status. Params: owner (required), repo (required), deployment_id (required).",
    inputSchema: {"type":"object","properties":{"owner":{"type":"string","description":"The account owner of the repository. The name is not case sensitive."},"repo":{"type":"string","description":"The name of the repository without the `.git` extension. The name is not case sensitive."},"deployment_id":{"type":"integer","description":"deployment_id parameter"}},"required":["owner","repo","deployment_id"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("DELETE", ("https://api.github.com/repos/" + String(args["owner"]) + "/" + String(args["repo"]) + "/deployments/" + String(args["deployment_id"])) + _qs, {}, undefined);
    }
  },
  {
    name: "repos/list-deployment-statuses",
    description: "List deployment statuses. Users with pull access can view deployment statuses for a deployment:. Params: owner (required), repo (required), deployment_id (required).",
    inputSchema: {"type":"object","properties":{"owner":{"type":"string","description":"The account owner of the repository. The name is not case sensitive."},"repo":{"type":"string","description":"The name of the repository without the `.git` extension. The name is not case sensitive."},"deployment_id":{"type":"integer","description":"deployment_id parameter"},"per_page":{"type":"integer","default":30,"description":"The number of results per page (max 100). For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""},"page":{"type":"integer","default":1,"description":"The page number of the results to fetch. For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""}},"required":["owner","repo","deployment_id"]},
    call: function(args) {
    var _q = new URLSearchParams();
    if (args["per_page"] !== undefined) _q.set("per_page", String(args["per_page"]));
    if (args["page"] !== undefined) _q.set("page", String(args["page"]));
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/repos/" + String(args["owner"]) + "/" + String(args["repo"]) + "/deployments/" + String(args["deployment_id"]) + "/statuses") + _qs, {}, undefined);
    }
  },
  {
    name: "repos/create-deployment-status",
    description: "Create a deployment status. Users with `push` access can create deployment statuses for a given deployment. Params: owner (required), repo (required), deployment_id (required).",
    inputSchema: {"type":"object","properties":{"owner":{"type":"string","description":"The account owner of the repository. The name is not case sensitive."},"repo":{"type":"string","description":"The name of the repository without the `.git` extension. The name is not case sensitive."},"deployment_id":{"type":"integer","description":"deployment_id parameter"},"body":{"type":"object","description":"Request body"}},"required":["owner","repo","deployment_id","body"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("POST", ("https://api.github.com/repos/" + String(args["owner"]) + "/" + String(args["repo"]) + "/deployments/" + String(args["deployment_id"]) + "/statuses") + _qs, {}, args["body"]);
    }
  },
  {
    name: "repos/get-deployment-status",
    description: "Get a deployment status. Users with pull access can view a deployment status for a deployment:. Params: owner (required), repo (required), deployment_id (required), status_id (required).",
    inputSchema: {"type":"object","properties":{"owner":{"type":"string","description":"The account owner of the repository. The name is not case sensitive."},"repo":{"type":"string","description":"The name of the repository without the `.git` extension. The name is not case sensitive."},"deployment_id":{"type":"integer","description":"deployment_id parameter"},"status_id":{"type":"integer"}},"required":["owner","repo","deployment_id","status_id"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/repos/" + String(args["owner"]) + "/" + String(args["repo"]) + "/deployments/" + String(args["deployment_id"]) + "/statuses/" + String(args["status_id"])) + _qs, {}, undefined);
    }
  },
  {
    name: "repos/create-dispatch-event",
    description: "Create a repository dispatch event. You can use this endpoint to trigger a webhook event called `repository_dispatch` when you want activity that happens outside of GitHub to trigger a GitHub Actions workflow or GitHub App webhook. Params: owner (required), repo (required).",
    inputSchema: {"type":"object","properties":{"owner":{"type":"string","description":"The account owner of the repository. The name is not case sensitive."},"repo":{"type":"string","description":"The name of the repository without the `.git` extension. The name is not case sensitive."},"body":{"type":"object","description":"Request body"}},"required":["owner","repo","body"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("POST", ("https://api.github.com/repos/" + String(args["owner"]) + "/" + String(args["repo"]) + "/dispatches") + _qs, {}, args["body"]);
    }
  },
  {
    name: "repos/get-all-environments",
    description: "List environments. Lists the environments for a repository. Params: owner (required), repo (required).",
    inputSchema: {"type":"object","properties":{"owner":{"type":"string","description":"The account owner of the repository. The name is not case sensitive."},"repo":{"type":"string","description":"The name of the repository without the `.git` extension. The name is not case sensitive."},"per_page":{"type":"integer","default":30,"description":"The number of results per page (max 100). For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""},"page":{"type":"integer","default":1,"description":"The page number of the results to fetch. For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""}},"required":["owner","repo"]},
    call: function(args) {
    var _q = new URLSearchParams();
    if (args["per_page"] !== undefined) _q.set("per_page", String(args["per_page"]));
    if (args["page"] !== undefined) _q.set("page", String(args["page"]));
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/repos/" + String(args["owner"]) + "/" + String(args["repo"]) + "/environments") + _qs, {}, undefined);
    }
  },
  {
    name: "repos/get-environment",
    description: "Get an environment. > [!NOTE]\n> To get information about name patterns that branches must match in order to deploy to this environment, see \"[Get a deployment branch policy](/rest/deployments/branch-policies#get-a-deployment-branch-policy).\"\n\nAnyone with read access to the repository can use this endpoint. Params: owner (required), repo (required), environment_name (required).",
    inputSchema: {"type":"object","properties":{"owner":{"type":"string","description":"The account owner of the repository. The name is not case sensitive."},"repo":{"type":"string","description":"The name of the repository without the `.git` extension. The name is not case sensitive."},"environment_name":{"type":"string","description":"The name of the environment. The name must be URL encoded. For example, any slashes in the name must be replaced with `%2F`."}},"required":["owner","repo","environment_name"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/repos/" + String(args["owner"]) + "/" + String(args["repo"]) + "/environments/" + String(args["environment_name"])) + _qs, {}, undefined);
    }
  },
  {
    name: "repos/create-or-update-environment",
    description: "Create or update an environment. Create or update an environment with protection rules, such as required reviewers. Params: owner (required), repo (required), environment_name (required).",
    inputSchema: {"type":"object","properties":{"owner":{"type":"string","description":"The account owner of the repository. The name is not case sensitive."},"repo":{"type":"string","description":"The name of the repository without the `.git` extension. The name is not case sensitive."},"environment_name":{"type":"string","description":"The name of the environment. The name must be URL encoded. For example, any slashes in the name must be replaced with `%2F`."},"body":{"type":"object","description":"Request body"}},"required":["owner","repo","environment_name"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("PUT", ("https://api.github.com/repos/" + String(args["owner"]) + "/" + String(args["repo"]) + "/environments/" + String(args["environment_name"])) + _qs, {}, args["body"]);
    }
  },
  {
    name: "repos/delete-an-environment",
    description: "Delete an environment. OAuth app tokens and personal access tokens (classic) need the `repo` scope to use this endpoint. Params: owner (required), repo (required), environment_name (required).",
    inputSchema: {"type":"object","properties":{"owner":{"type":"string","description":"The account owner of the repository. The name is not case sensitive."},"repo":{"type":"string","description":"The name of the repository without the `.git` extension. The name is not case sensitive."},"environment_name":{"type":"string","description":"The name of the environment. The name must be URL encoded. For example, any slashes in the name must be replaced with `%2F`."}},"required":["owner","repo","environment_name"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("DELETE", ("https://api.github.com/repos/" + String(args["owner"]) + "/" + String(args["repo"]) + "/environments/" + String(args["environment_name"])) + _qs, {}, undefined);
    }
  },
  {
    name: "repos/list-deployment-branch-policies",
    description: "List deployment branch policies. Lists the deployment branch policies for an environment. Params: owner (required), repo (required), environment_name (required).",
    inputSchema: {"type":"object","properties":{"owner":{"type":"string","description":"The account owner of the repository. The name is not case sensitive."},"repo":{"type":"string","description":"The name of the repository without the `.git` extension. The name is not case sensitive."},"environment_name":{"type":"string","description":"The name of the environment. The name must be URL encoded. For example, any slashes in the name must be replaced with `%2F`."},"per_page":{"type":"integer","default":30,"description":"The number of results per page (max 100). For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""},"page":{"type":"integer","default":1,"description":"The page number of the results to fetch. For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""}},"required":["owner","repo","environment_name"]},
    call: function(args) {
    var _q = new URLSearchParams();
    if (args["per_page"] !== undefined) _q.set("per_page", String(args["per_page"]));
    if (args["page"] !== undefined) _q.set("page", String(args["page"]));
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/repos/" + String(args["owner"]) + "/" + String(args["repo"]) + "/environments/" + String(args["environment_name"]) + "/deployment-branch-policies") + _qs, {}, undefined);
    }
  },
  {
    name: "repos/create-deployment-branch-policy",
    description: "Create a deployment branch policy. Creates a deployment branch or tag policy for an environment. Params: owner (required), repo (required), environment_name (required).",
    inputSchema: {"type":"object","properties":{"owner":{"type":"string","description":"The account owner of the repository. The name is not case sensitive."},"repo":{"type":"string","description":"The name of the repository without the `.git` extension. The name is not case sensitive."},"environment_name":{"type":"string","description":"The name of the environment. The name must be URL encoded. For example, any slashes in the name must be replaced with `%2F`."},"body":{"type":"object","description":"Request body"}},"required":["owner","repo","environment_name","body"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("POST", ("https://api.github.com/repos/" + String(args["owner"]) + "/" + String(args["repo"]) + "/environments/" + String(args["environment_name"]) + "/deployment-branch-policies") + _qs, {}, args["body"]);
    }
  },
  {
    name: "repos/get-deployment-branch-policy",
    description: "Get a deployment branch policy. Gets a deployment branch or tag policy for an environment. Params: owner (required), repo (required), environment_name (required), branch_policy_id (required).",
    inputSchema: {"type":"object","properties":{"owner":{"type":"string","description":"The account owner of the repository. The name is not case sensitive."},"repo":{"type":"string","description":"The name of the repository without the `.git` extension. The name is not case sensitive."},"environment_name":{"type":"string","description":"The name of the environment. The name must be URL encoded. For example, any slashes in the name must be replaced with `%2F`."},"branch_policy_id":{"type":"integer","description":"The unique identifier of the branch policy."}},"required":["owner","repo","environment_name","branch_policy_id"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/repos/" + String(args["owner"]) + "/" + String(args["repo"]) + "/environments/" + String(args["environment_name"]) + "/deployment-branch-policies/" + String(args["branch_policy_id"])) + _qs, {}, undefined);
    }
  },
  {
    name: "repos/update-deployment-branch-policy",
    description: "Update a deployment branch policy. Updates a deployment branch or tag policy for an environment. Params: owner (required), repo (required), environment_name (required), branch_policy_id (required).",
    inputSchema: {"type":"object","properties":{"owner":{"type":"string","description":"The account owner of the repository. The name is not case sensitive."},"repo":{"type":"string","description":"The name of the repository without the `.git` extension. The name is not case sensitive."},"environment_name":{"type":"string","description":"The name of the environment. The name must be URL encoded. For example, any slashes in the name must be replaced with `%2F`."},"branch_policy_id":{"type":"integer","description":"The unique identifier of the branch policy."},"body":{"type":"object","description":"Request body"}},"required":["owner","repo","environment_name","branch_policy_id","body"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("PUT", ("https://api.github.com/repos/" + String(args["owner"]) + "/" + String(args["repo"]) + "/environments/" + String(args["environment_name"]) + "/deployment-branch-policies/" + String(args["branch_policy_id"])) + _qs, {}, args["body"]);
    }
  },
  {
    name: "repos/delete-deployment-branch-policy",
    description: "Delete a deployment branch policy. Deletes a deployment branch or tag policy for an environment. Params: owner (required), repo (required), environment_name (required), branch_policy_id (required).",
    inputSchema: {"type":"object","properties":{"owner":{"type":"string","description":"The account owner of the repository. The name is not case sensitive."},"repo":{"type":"string","description":"The name of the repository without the `.git` extension. The name is not case sensitive."},"environment_name":{"type":"string","description":"The name of the environment. The name must be URL encoded. For example, any slashes in the name must be replaced with `%2F`."},"branch_policy_id":{"type":"integer","description":"The unique identifier of the branch policy."}},"required":["owner","repo","environment_name","branch_policy_id"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("DELETE", ("https://api.github.com/repos/" + String(args["owner"]) + "/" + String(args["repo"]) + "/environments/" + String(args["environment_name"]) + "/deployment-branch-policies/" + String(args["branch_policy_id"])) + _qs, {}, undefined);
    }
  },
  {
    name: "repos/get-all-deployment-protection-rules",
    description: "Get all deployment protection rules for an environment. Gets all custom deployment protection rules that are enabled for an environment. Params: environment_name (required), repo (required), owner (required).",
    inputSchema: {"type":"object","properties":{"environment_name":{"type":"string","description":"The name of the environment. The name must be URL encoded. For example, any slashes in the name must be replaced with `%2F`."},"repo":{"type":"string","description":"The name of the repository without the `.git` extension. The name is not case sensitive."},"owner":{"type":"string","description":"The account owner of the repository. The name is not case sensitive."}},"required":["environment_name","repo","owner"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/repos/" + String(args["owner"]) + "/" + String(args["repo"]) + "/environments/" + String(args["environment_name"]) + "/deployment_protection_rules") + _qs, {}, undefined);
    }
  },
  {
    name: "repos/create-deployment-protection-rule",
    description: "Create a custom deployment protection rule on an environment. Enable a custom deployment protection rule for an environment. Params: environment_name (required), repo (required), owner (required).",
    inputSchema: {"type":"object","properties":{"environment_name":{"type":"string","description":"The name of the environment. The name must be URL encoded. For example, any slashes in the name must be replaced with `%2F`."},"repo":{"type":"string","description":"The name of the repository without the `.git` extension. The name is not case sensitive."},"owner":{"type":"string","description":"The account owner of the repository. The name is not case sensitive."},"body":{"type":"object","description":"Request body"}},"required":["environment_name","repo","owner","body"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("POST", ("https://api.github.com/repos/" + String(args["owner"]) + "/" + String(args["repo"]) + "/environments/" + String(args["environment_name"]) + "/deployment_protection_rules") + _qs, {}, args["body"]);
    }
  },
  {
    name: "repos/list-custom-deployment-rule-integrations",
    description: "List custom deployment rule integrations available for an environment. Gets all custom deployment protection rule integrations that are available for an environment. Params: environment_name (required), repo (required), owner (required).",
    inputSchema: {"type":"object","properties":{"environment_name":{"type":"string","description":"The name of the environment. The name must be URL encoded. For example, any slashes in the name must be replaced with `%2F`."},"repo":{"type":"string","description":"The name of the repository without the `.git` extension. The name is not case sensitive."},"owner":{"type":"string","description":"The account owner of the repository. The name is not case sensitive."},"page":{"type":"integer","default":1,"description":"The page number of the results to fetch. For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""},"per_page":{"type":"integer","default":30,"description":"The number of results per page (max 100). For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""}},"required":["environment_name","repo","owner"]},
    call: function(args) {
    var _q = new URLSearchParams();
    if (args["page"] !== undefined) _q.set("page", String(args["page"]));
    if (args["per_page"] !== undefined) _q.set("per_page", String(args["per_page"]));
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/repos/" + String(args["owner"]) + "/" + String(args["repo"]) + "/environments/" + String(args["environment_name"]) + "/deployment_protection_rules/apps") + _qs, {}, undefined);
    }
  },
  {
    name: "repos/get-custom-deployment-protection-rule",
    description: "Get a custom deployment protection rule. Gets an enabled custom deployment protection rule for an environment. Params: owner (required), repo (required), environment_name (required), protection_rule_id (required).",
    inputSchema: {"type":"object","properties":{"owner":{"type":"string","description":"The account owner of the repository. The name is not case sensitive."},"repo":{"type":"string","description":"The name of the repository without the `.git` extension. The name is not case sensitive."},"environment_name":{"type":"string","description":"The name of the environment. The name must be URL encoded. For example, any slashes in the name must be replaced with `%2F`."},"protection_rule_id":{"type":"integer","description":"The unique identifier of the protection rule."}},"required":["owner","repo","environment_name","protection_rule_id"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/repos/" + String(args["owner"]) + "/" + String(args["repo"]) + "/environments/" + String(args["environment_name"]) + "/deployment_protection_rules/" + String(args["protection_rule_id"])) + _qs, {}, undefined);
    }
  },
  {
    name: "repos/disable-deployment-protection-rule",
    description: "Disable a custom protection rule for an environment. Disables a custom deployment protection rule for an environment. Params: environment_name (required), repo (required), owner (required), protection_rule_id (required).",
    inputSchema: {"type":"object","properties":{"environment_name":{"type":"string","description":"The name of the environment. The name must be URL encoded. For example, any slashes in the name must be replaced with `%2F`."},"repo":{"type":"string","description":"The name of the repository without the `.git` extension. The name is not case sensitive."},"owner":{"type":"string","description":"The account owner of the repository. The name is not case sensitive."},"protection_rule_id":{"type":"integer","description":"The unique identifier of the protection rule."}},"required":["environment_name","repo","owner","protection_rule_id"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("DELETE", ("https://api.github.com/repos/" + String(args["owner"]) + "/" + String(args["repo"]) + "/environments/" + String(args["environment_name"]) + "/deployment_protection_rules/" + String(args["protection_rule_id"])) + _qs, {}, undefined);
    }
  },
  {
    name: "actions/list-environment-secrets",
    description: "List environment secrets. Lists all secrets available in an environment without revealing their\nencrypted values. Params: owner (required), repo (required), environment_name (required).",
    inputSchema: {"type":"object","properties":{"owner":{"type":"string","description":"The account owner of the repository. The name is not case sensitive."},"repo":{"type":"string","description":"The name of the repository without the `.git` extension. The name is not case sensitive."},"environment_name":{"type":"string","description":"The name of the environment. The name must be URL encoded. For example, any slashes in the name must be replaced with `%2F`."},"per_page":{"type":"integer","default":30,"description":"The number of results per page (max 100). For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""},"page":{"type":"integer","default":1,"description":"The page number of the results to fetch. For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""}},"required":["owner","repo","environment_name"]},
    call: function(args) {
    var _q = new URLSearchParams();
    if (args["per_page"] !== undefined) _q.set("per_page", String(args["per_page"]));
    if (args["page"] !== undefined) _q.set("page", String(args["page"]));
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/repos/" + String(args["owner"]) + "/" + String(args["repo"]) + "/environments/" + String(args["environment_name"]) + "/secrets") + _qs, {}, undefined);
    }
  },
  {
    name: "actions/get-environment-public-key",
    description: "Get an environment public key. Get the public key for an environment, which you need to encrypt environment\nsecrets. Params: owner (required), repo (required), environment_name (required).",
    inputSchema: {"type":"object","properties":{"owner":{"type":"string","description":"The account owner of the repository. The name is not case sensitive."},"repo":{"type":"string","description":"The name of the repository without the `.git` extension. The name is not case sensitive."},"environment_name":{"type":"string","description":"The name of the environment. The name must be URL encoded. For example, any slashes in the name must be replaced with `%2F`."}},"required":["owner","repo","environment_name"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/repos/" + String(args["owner"]) + "/" + String(args["repo"]) + "/environments/" + String(args["environment_name"]) + "/secrets/public-key") + _qs, {}, undefined);
    }
  },
  {
    name: "actions/get-environment-secret",
    description: "Get an environment secret. Gets a single environment secret without revealing its encrypted value. Params: owner (required), repo (required), environment_name (required), secret_name (required).",
    inputSchema: {"type":"object","properties":{"owner":{"type":"string","description":"The account owner of the repository. The name is not case sensitive."},"repo":{"type":"string","description":"The name of the repository without the `.git` extension. The name is not case sensitive."},"environment_name":{"type":"string","description":"The name of the environment. The name must be URL encoded. For example, any slashes in the name must be replaced with `%2F`."},"secret_name":{"type":"string","description":"The name of the secret."}},"required":["owner","repo","environment_name","secret_name"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/repos/" + String(args["owner"]) + "/" + String(args["repo"]) + "/environments/" + String(args["environment_name"]) + "/secrets/" + String(args["secret_name"])) + _qs, {}, undefined);
    }
  },
  {
    name: "actions/create-or-update-environment-secret",
    description: "Create or update an environment secret. Creates or updates an environment secret with an encrypted value. Params: owner (required), repo (required), environment_name (required), secret_name (required).",
    inputSchema: {"type":"object","properties":{"owner":{"type":"string","description":"The account owner of the repository. The name is not case sensitive."},"repo":{"type":"string","description":"The name of the repository without the `.git` extension. The name is not case sensitive."},"environment_name":{"type":"string","description":"The name of the environment. The name must be URL encoded. For example, any slashes in the name must be replaced with `%2F`."},"secret_name":{"type":"string","description":"The name of the secret."},"body":{"type":"object","description":"Request body"}},"required":["owner","repo","environment_name","secret_name","body"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("PUT", ("https://api.github.com/repos/" + String(args["owner"]) + "/" + String(args["repo"]) + "/environments/" + String(args["environment_name"]) + "/secrets/" + String(args["secret_name"])) + _qs, {}, args["body"]);
    }
  },
  {
    name: "actions/delete-environment-secret",
    description: "Delete an environment secret. Deletes a secret in an environment using the secret name. Params: owner (required), repo (required), environment_name (required), secret_name (required).",
    inputSchema: {"type":"object","properties":{"owner":{"type":"string","description":"The account owner of the repository. The name is not case sensitive."},"repo":{"type":"string","description":"The name of the repository without the `.git` extension. The name is not case sensitive."},"environment_name":{"type":"string","description":"The name of the environment. The name must be URL encoded. For example, any slashes in the name must be replaced with `%2F`."},"secret_name":{"type":"string","description":"The name of the secret."}},"required":["owner","repo","environment_name","secret_name"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("DELETE", ("https://api.github.com/repos/" + String(args["owner"]) + "/" + String(args["repo"]) + "/environments/" + String(args["environment_name"]) + "/secrets/" + String(args["secret_name"])) + _qs, {}, undefined);
    }
  },
  {
    name: "actions/list-environment-variables",
    description: "List environment variables. Lists all environment variables. Params: owner (required), repo (required), environment_name (required).",
    inputSchema: {"type":"object","properties":{"owner":{"type":"string","description":"The account owner of the repository. The name is not case sensitive."},"repo":{"type":"string","description":"The name of the repository without the `.git` extension. The name is not case sensitive."},"environment_name":{"type":"string","description":"The name of the environment. The name must be URL encoded. For example, any slashes in the name must be replaced with `%2F`."},"per_page":{"type":"integer","default":10,"description":"The number of results per page (max 30). For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""},"page":{"type":"integer","default":1,"description":"The page number of the results to fetch. For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""}},"required":["owner","repo","environment_name"]},
    call: function(args) {
    var _q = new URLSearchParams();
    if (args["per_page"] !== undefined) _q.set("per_page", String(args["per_page"]));
    if (args["page"] !== undefined) _q.set("page", String(args["page"]));
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/repos/" + String(args["owner"]) + "/" + String(args["repo"]) + "/environments/" + String(args["environment_name"]) + "/variables") + _qs, {}, undefined);
    }
  },
  {
    name: "actions/create-environment-variable",
    description: "Create an environment variable. Create an environment variable that you can reference in a GitHub Actions workflow. Params: owner (required), repo (required), environment_name (required).",
    inputSchema: {"type":"object","properties":{"owner":{"type":"string","description":"The account owner of the repository. The name is not case sensitive."},"repo":{"type":"string","description":"The name of the repository without the `.git` extension. The name is not case sensitive."},"environment_name":{"type":"string","description":"The name of the environment. The name must be URL encoded. For example, any slashes in the name must be replaced with `%2F`."},"body":{"type":"object","description":"Request body"}},"required":["owner","repo","environment_name","body"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("POST", ("https://api.github.com/repos/" + String(args["owner"]) + "/" + String(args["repo"]) + "/environments/" + String(args["environment_name"]) + "/variables") + _qs, {}, args["body"]);
    }
  },
  {
    name: "actions/get-environment-variable",
    description: "Get an environment variable. Gets a specific variable in an environment. Params: owner (required), repo (required), environment_name (required), name (required).",
    inputSchema: {"type":"object","properties":{"owner":{"type":"string","description":"The account owner of the repository. The name is not case sensitive."},"repo":{"type":"string","description":"The name of the repository without the `.git` extension. The name is not case sensitive."},"environment_name":{"type":"string","description":"The name of the environment. The name must be URL encoded. For example, any slashes in the name must be replaced with `%2F`."},"name":{"type":"string","description":"The name of the variable."}},"required":["owner","repo","environment_name","name"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/repos/" + String(args["owner"]) + "/" + String(args["repo"]) + "/environments/" + String(args["environment_name"]) + "/variables/" + String(args["name"])) + _qs, {}, undefined);
    }
  },
  {
    name: "actions/update-environment-variable",
    description: "Update an environment variable. Updates an environment variable that you can reference in a GitHub Actions workflow. Params: owner (required), repo (required), name (required), environment_name (required).",
    inputSchema: {"type":"object","properties":{"owner":{"type":"string","description":"The account owner of the repository. The name is not case sensitive."},"repo":{"type":"string","description":"The name of the repository without the `.git` extension. The name is not case sensitive."},"name":{"type":"string","description":"The name of the variable."},"environment_name":{"type":"string","description":"The name of the environment. The name must be URL encoded. For example, any slashes in the name must be replaced with `%2F`."},"body":{"type":"object","description":"Request body"}},"required":["owner","repo","name","environment_name","body"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("PATCH", ("https://api.github.com/repos/" + String(args["owner"]) + "/" + String(args["repo"]) + "/environments/" + String(args["environment_name"]) + "/variables/" + String(args["name"])) + _qs, {}, args["body"]);
    }
  },
  {
    name: "actions/delete-environment-variable",
    description: "Delete an environment variable. Deletes an environment variable using the variable name. Params: owner (required), repo (required), name (required), environment_name (required).",
    inputSchema: {"type":"object","properties":{"owner":{"type":"string","description":"The account owner of the repository. The name is not case sensitive."},"repo":{"type":"string","description":"The name of the repository without the `.git` extension. The name is not case sensitive."},"name":{"type":"string","description":"The name of the variable."},"environment_name":{"type":"string","description":"The name of the environment. The name must be URL encoded. For example, any slashes in the name must be replaced with `%2F`."}},"required":["owner","repo","name","environment_name"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("DELETE", ("https://api.github.com/repos/" + String(args["owner"]) + "/" + String(args["repo"]) + "/environments/" + String(args["environment_name"]) + "/variables/" + String(args["name"])) + _qs, {}, undefined);
    }
  },
  {
    name: "activity/list-repo-events",
    description: "List repository events. > [!NOTE]\n> This API is not built to serve real-time use cases. Params: owner (required), repo (required).",
    inputSchema: {"type":"object","properties":{"owner":{"type":"string","description":"The account owner of the repository. The name is not case sensitive."},"repo":{"type":"string","description":"The name of the repository without the `.git` extension. The name is not case sensitive."},"per_page":{"type":"integer","default":30,"description":"The number of results per page (max 100). For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""},"page":{"type":"integer","default":1,"description":"The page number of the results to fetch. For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""}},"required":["owner","repo"]},
    call: function(args) {
    var _q = new URLSearchParams();
    if (args["per_page"] !== undefined) _q.set("per_page", String(args["per_page"]));
    if (args["page"] !== undefined) _q.set("page", String(args["page"]));
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/repos/" + String(args["owner"]) + "/" + String(args["repo"]) + "/events") + _qs, {}, undefined);
    }
  },
  {
    name: "repos/list-forks",
    description: "List forks. Params: owner (required), repo (required).",
    inputSchema: {"type":"object","properties":{"owner":{"type":"string","description":"The account owner of the repository. The name is not case sensitive."},"repo":{"type":"string","description":"The name of the repository without the `.git` extension. The name is not case sensitive."},"sort":{"type":"string","enum":["newest","oldest","stargazers","watchers"],"default":"newest","description":"The sort order. `stargazers` will sort by star count."},"per_page":{"type":"integer","default":30,"description":"The number of results per page (max 100). For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""},"page":{"type":"integer","default":1,"description":"The page number of the results to fetch. For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""}},"required":["owner","repo"]},
    call: function(args) {
    var _q = new URLSearchParams();
    if (args["sort"] !== undefined) _q.set("sort", String(args["sort"]));
    if (args["per_page"] !== undefined) _q.set("per_page", String(args["per_page"]));
    if (args["page"] !== undefined) _q.set("page", String(args["page"]));
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/repos/" + String(args["owner"]) + "/" + String(args["repo"]) + "/forks") + _qs, {}, undefined);
    }
  },
  {
    name: "repos/create-fork",
    description: "Create a fork. Create a fork for the authenticated user. Params: owner (required), repo (required).",
    inputSchema: {"type":"object","properties":{"owner":{"type":"string","description":"The account owner of the repository. The name is not case sensitive."},"repo":{"type":"string","description":"The name of the repository without the `.git` extension. The name is not case sensitive."},"body":{"type":"object","description":"Request body"}},"required":["owner","repo"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("POST", ("https://api.github.com/repos/" + String(args["owner"]) + "/" + String(args["repo"]) + "/forks") + _qs, {}, args["body"]);
    }
  },
  {
    name: "git/create-blob",
    description: "Create a blob. Params: owner (required), repo (required).",
    inputSchema: {"type":"object","properties":{"owner":{"type":"string","description":"The account owner of the repository. The name is not case sensitive."},"repo":{"type":"string","description":"The name of the repository without the `.git` extension. The name is not case sensitive."},"body":{"type":"object","description":"Request body"}},"required":["owner","repo","body"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("POST", ("https://api.github.com/repos/" + String(args["owner"]) + "/" + String(args["repo"]) + "/git/blobs") + _qs, {}, args["body"]);
    }
  },
  {
    name: "git/get-blob",
    description: "Get a blob. The `content` in the response will always be Base64 encoded. Params: owner (required), repo (required), file_sha (required).",
    inputSchema: {"type":"object","properties":{"owner":{"type":"string","description":"The account owner of the repository. The name is not case sensitive."},"repo":{"type":"string","description":"The name of the repository without the `.git` extension. The name is not case sensitive."},"file_sha":{"type":"string"}},"required":["owner","repo","file_sha"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/repos/" + String(args["owner"]) + "/" + String(args["repo"]) + "/git/blobs/" + String(args["file_sha"])) + _qs, {}, undefined);
    }
  },
  {
    name: "git/create-commit",
    description: "Create a commit. Creates a new Git [commit object](https://git-scm.com/book/en/v2/Git-Internals-Git-Objects). Params: owner (required), repo (required).",
    inputSchema: {"type":"object","properties":{"owner":{"type":"string","description":"The account owner of the repository. The name is not case sensitive."},"repo":{"type":"string","description":"The name of the repository without the `.git` extension. The name is not case sensitive."},"body":{"type":"object","description":"Request body"}},"required":["owner","repo","body"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("POST", ("https://api.github.com/repos/" + String(args["owner"]) + "/" + String(args["repo"]) + "/git/commits") + _qs, {}, args["body"]);
    }
  },
  {
    name: "git/get-commit",
    description: "Get a commit object. Gets a Git [commit object](https://git-scm.com/book/en/v2/Git-Internals-Git-Objects). Params: owner (required), repo (required), commit_sha (required).",
    inputSchema: {"type":"object","properties":{"owner":{"type":"string","description":"The account owner of the repository. The name is not case sensitive."},"repo":{"type":"string","description":"The name of the repository without the `.git` extension. The name is not case sensitive."},"commit_sha":{"type":"string","description":"The SHA of the commit."}},"required":["owner","repo","commit_sha"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/repos/" + String(args["owner"]) + "/" + String(args["repo"]) + "/git/commits/" + String(args["commit_sha"])) + _qs, {}, undefined);
    }
  },
  {
    name: "git/list-matching-refs",
    description: "List matching references. Returns an array of references from your Git database that match the supplied name. Params: owner (required), repo (required), ref (required).",
    inputSchema: {"type":"object","properties":{"owner":{"type":"string","description":"The account owner of the repository. The name is not case sensitive."},"repo":{"type":"string","description":"The name of the repository without the `.git` extension. The name is not case sensitive."},"ref":{"type":"string","description":"The Git reference. For more information, see \"[Git References](https://git-scm.com/book/en/v2/Git-Internals-Git-References)\" in the Git documentation."}},"required":["owner","repo","ref"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/repos/" + String(args["owner"]) + "/" + String(args["repo"]) + "/git/matching-refs/" + String(args["ref"])) + _qs, {}, undefined);
    }
  },
  {
    name: "git/get-ref",
    description: "Get a reference. Returns a single reference from your Git database. Params: owner (required), repo (required), ref (required).",
    inputSchema: {"type":"object","properties":{"owner":{"type":"string","description":"The account owner of the repository. The name is not case sensitive."},"repo":{"type":"string","description":"The name of the repository without the `.git` extension. The name is not case sensitive."},"ref":{"type":"string","description":"The Git reference. For more information, see \"[Git References](https://git-scm.com/book/en/v2/Git-Internals-Git-References)\" in the Git documentation."}},"required":["owner","repo","ref"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/repos/" + String(args["owner"]) + "/" + String(args["repo"]) + "/git/ref/" + String(args["ref"])) + _qs, {}, undefined);
    }
  },
  {
    name: "git/create-ref",
    description: "Create a reference. Creates a reference for your repository. Params: owner (required), repo (required).",
    inputSchema: {"type":"object","properties":{"owner":{"type":"string","description":"The account owner of the repository. The name is not case sensitive."},"repo":{"type":"string","description":"The name of the repository without the `.git` extension. The name is not case sensitive."},"body":{"type":"object","description":"Request body"}},"required":["owner","repo","body"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("POST", ("https://api.github.com/repos/" + String(args["owner"]) + "/" + String(args["repo"]) + "/git/refs") + _qs, {}, args["body"]);
    }
  },
  {
    name: "git/update-ref",
    description: "Update a reference. Updates the provided reference to point to a new SHA. Params: owner (required), repo (required), ref (required).",
    inputSchema: {"type":"object","properties":{"owner":{"type":"string","description":"The account owner of the repository. The name is not case sensitive."},"repo":{"type":"string","description":"The name of the repository without the `.git` extension. The name is not case sensitive."},"ref":{"type":"string","description":"The Git reference. For more information, see \"[Git References](https://git-scm.com/book/en/v2/Git-Internals-Git-References)\" in the Git documentation."},"body":{"type":"object","description":"Request body"}},"required":["owner","repo","ref","body"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("PATCH", ("https://api.github.com/repos/" + String(args["owner"]) + "/" + String(args["repo"]) + "/git/refs/" + String(args["ref"])) + _qs, {}, args["body"]);
    }
  },
  {
    name: "git/delete-ref",
    description: "Delete a reference. Deletes the provided reference. Params: owner (required), repo (required), ref (required).",
    inputSchema: {"type":"object","properties":{"owner":{"type":"string","description":"The account owner of the repository. The name is not case sensitive."},"repo":{"type":"string","description":"The name of the repository without the `.git` extension. The name is not case sensitive."},"ref":{"type":"string","description":"The Git reference. For more information, see \"[Git References](https://git-scm.com/book/en/v2/Git-Internals-Git-References)\" in the Git documentation."}},"required":["owner","repo","ref"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("DELETE", ("https://api.github.com/repos/" + String(args["owner"]) + "/" + String(args["repo"]) + "/git/refs/" + String(args["ref"])) + _qs, {}, undefined);
    }
  },
  {
    name: "git/create-tag",
    description: "Create a tag object. Note that creating a tag object does not create the reference that makes a tag in Git. Params: owner (required), repo (required).",
    inputSchema: {"type":"object","properties":{"owner":{"type":"string","description":"The account owner of the repository. The name is not case sensitive."},"repo":{"type":"string","description":"The name of the repository without the `.git` extension. The name is not case sensitive."},"body":{"type":"object","description":"Request body"}},"required":["owner","repo","body"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("POST", ("https://api.github.com/repos/" + String(args["owner"]) + "/" + String(args["repo"]) + "/git/tags") + _qs, {}, args["body"]);
    }
  },
  {
    name: "git/get-tag",
    description: "Get a tag. **Signature verification object**\n\nThe response will include a `verification` object that describes the result of verifying the commit's signature. Params: owner (required), repo (required), tag_sha (required).",
    inputSchema: {"type":"object","properties":{"owner":{"type":"string","description":"The account owner of the repository. The name is not case sensitive."},"repo":{"type":"string","description":"The name of the repository without the `.git` extension. The name is not case sensitive."},"tag_sha":{"type":"string"}},"required":["owner","repo","tag_sha"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/repos/" + String(args["owner"]) + "/" + String(args["repo"]) + "/git/tags/" + String(args["tag_sha"])) + _qs, {}, undefined);
    }
  },
  {
    name: "git/create-tree",
    description: "Create a tree. The tree creation API accepts nested entries. Params: owner (required), repo (required).",
    inputSchema: {"type":"object","properties":{"owner":{"type":"string","description":"The account owner of the repository. The name is not case sensitive."},"repo":{"type":"string","description":"The name of the repository without the `.git` extension. The name is not case sensitive."},"body":{"type":"object","description":"Request body"}},"required":["owner","repo","body"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("POST", ("https://api.github.com/repos/" + String(args["owner"]) + "/" + String(args["repo"]) + "/git/trees") + _qs, {}, args["body"]);
    }
  },
  {
    name: "git/get-tree",
    description: "Get a tree. Returns a single tree using the SHA1 value or ref name for that tree. Params: owner (required), repo (required), tree_sha (required).",
    inputSchema: {"type":"object","properties":{"owner":{"type":"string","description":"The account owner of the repository. The name is not case sensitive."},"repo":{"type":"string","description":"The name of the repository without the `.git` extension. The name is not case sensitive."},"tree_sha":{"type":"string","description":"The SHA1 value or ref (branch or tag) name of the tree."},"recursive":{"type":"string","description":"Setting this parameter to any value returns the objects or subtrees referenced by the tree specified in `:tree_sha`. For example, setting `recursive` to any of the following will enable returning objects or subtrees: `0`, `1`, `\"true\"`, and `\"false\"`. Omit this parameter to prevent recursively returning objects or subtrees."}},"required":["owner","repo","tree_sha"]},
    call: function(args) {
    var _q = new URLSearchParams();
    if (args["recursive"] !== undefined) _q.set("recursive", String(args["recursive"]));
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/repos/" + String(args["owner"]) + "/" + String(args["repo"]) + "/git/trees/" + String(args["tree_sha"])) + _qs, {}, undefined);
    }
  },
  {
    name: "repos/get-hash-algorithm",
    description: "Get the hash algorithm for a repository. Returns the hash algorithm used to store repository objects. Params: owner (required), repo (required).",
    inputSchema: {"type":"object","properties":{"owner":{"type":"string","description":"The account owner of the repository. The name is not case sensitive."},"repo":{"type":"string","description":"The name of the repository without the `.git` extension. The name is not case sensitive."}},"required":["owner","repo"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/repos/" + String(args["owner"]) + "/" + String(args["repo"]) + "/hash-algorithm") + _qs, {}, undefined);
    }
  },
  {
    name: "repos/list-webhooks",
    description: "List repository webhooks. Lists webhooks for a repository. Params: owner (required), repo (required).",
    inputSchema: {"type":"object","properties":{"owner":{"type":"string","description":"The account owner of the repository. The name is not case sensitive."},"repo":{"type":"string","description":"The name of the repository without the `.git` extension. The name is not case sensitive."},"per_page":{"type":"integer","default":30,"description":"The number of results per page (max 100). For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""},"page":{"type":"integer","default":1,"description":"The page number of the results to fetch. For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""}},"required":["owner","repo"]},
    call: function(args) {
    var _q = new URLSearchParams();
    if (args["per_page"] !== undefined) _q.set("per_page", String(args["per_page"]));
    if (args["page"] !== undefined) _q.set("page", String(args["page"]));
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/repos/" + String(args["owner"]) + "/" + String(args["repo"]) + "/hooks") + _qs, {}, undefined);
    }
  },
  {
    name: "repos/create-webhook",
    description: "Create a repository webhook. Repositories can have multiple webhooks installed. Params: owner (required), repo (required).",
    inputSchema: {"type":"object","properties":{"owner":{"type":"string","description":"The account owner of the repository. The name is not case sensitive."},"repo":{"type":"string","description":"The name of the repository without the `.git` extension. The name is not case sensitive."},"body":{"type":"object","description":"Request body"}},"required":["owner","repo"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("POST", ("https://api.github.com/repos/" + String(args["owner"]) + "/" + String(args["repo"]) + "/hooks") + _qs, {}, args["body"]);
    }
  },
  {
    name: "repos/get-webhook",
    description: "Get a repository webhook. Returns a webhook configured in a repository. Params: owner (required), repo (required), hook_id (required).",
    inputSchema: {"type":"object","properties":{"owner":{"type":"string","description":"The account owner of the repository. The name is not case sensitive."},"repo":{"type":"string","description":"The name of the repository without the `.git` extension. The name is not case sensitive."},"hook_id":{"type":"integer","description":"The unique identifier of the hook. You can find this value in the `X-GitHub-Hook-ID` header of a webhook delivery."}},"required":["owner","repo","hook_id"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/repos/" + String(args["owner"]) + "/" + String(args["repo"]) + "/hooks/" + String(args["hook_id"])) + _qs, {}, undefined);
    }
  },
  {
    name: "repos/update-webhook",
    description: "Update a repository webhook. Updates a webhook configured in a repository. Params: owner (required), repo (required), hook_id (required).",
    inputSchema: {"type":"object","properties":{"owner":{"type":"string","description":"The account owner of the repository. The name is not case sensitive."},"repo":{"type":"string","description":"The name of the repository without the `.git` extension. The name is not case sensitive."},"hook_id":{"type":"integer","description":"The unique identifier of the hook. You can find this value in the `X-GitHub-Hook-ID` header of a webhook delivery."},"body":{"type":"object","description":"Request body"}},"required":["owner","repo","hook_id","body"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("PATCH", ("https://api.github.com/repos/" + String(args["owner"]) + "/" + String(args["repo"]) + "/hooks/" + String(args["hook_id"])) + _qs, {}, args["body"]);
    }
  },
  {
    name: "repos/delete-webhook",
    description: "Delete a repository webhook. Delete a webhook for an organization. Params: owner (required), repo (required), hook_id (required).",
    inputSchema: {"type":"object","properties":{"owner":{"type":"string","description":"The account owner of the repository. The name is not case sensitive."},"repo":{"type":"string","description":"The name of the repository without the `.git` extension. The name is not case sensitive."},"hook_id":{"type":"integer","description":"The unique identifier of the hook. You can find this value in the `X-GitHub-Hook-ID` header of a webhook delivery."}},"required":["owner","repo","hook_id"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("DELETE", ("https://api.github.com/repos/" + String(args["owner"]) + "/" + String(args["repo"]) + "/hooks/" + String(args["hook_id"])) + _qs, {}, undefined);
    }
  },
  {
    name: "repos/get-webhook-config-for-repo",
    description: "Get a webhook configuration for a repository. Returns the webhook configuration for a repository. Params: owner (required), repo (required), hook_id (required).",
    inputSchema: {"type":"object","properties":{"owner":{"type":"string","description":"The account owner of the repository. The name is not case sensitive."},"repo":{"type":"string","description":"The name of the repository without the `.git` extension. The name is not case sensitive."},"hook_id":{"type":"integer","description":"The unique identifier of the hook. You can find this value in the `X-GitHub-Hook-ID` header of a webhook delivery."}},"required":["owner","repo","hook_id"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/repos/" + String(args["owner"]) + "/" + String(args["repo"]) + "/hooks/" + String(args["hook_id"]) + "/config") + _qs, {}, undefined);
    }
  },
  {
    name: "repos/update-webhook-config-for-repo",
    description: "Update a webhook configuration for a repository. Updates the webhook configuration for a repository. Params: owner (required), repo (required), hook_id (required).",
    inputSchema: {"type":"object","properties":{"owner":{"type":"string","description":"The account owner of the repository. The name is not case sensitive."},"repo":{"type":"string","description":"The name of the repository without the `.git` extension. The name is not case sensitive."},"hook_id":{"type":"integer","description":"The unique identifier of the hook. You can find this value in the `X-GitHub-Hook-ID` header of a webhook delivery."},"body":{"type":"object","description":"Request body"}},"required":["owner","repo","hook_id"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("PATCH", ("https://api.github.com/repos/" + String(args["owner"]) + "/" + String(args["repo"]) + "/hooks/" + String(args["hook_id"]) + "/config") + _qs, {}, args["body"]);
    }
  },
  {
    name: "repos/list-webhook-deliveries",
    description: "List deliveries for a repository webhook. Returns a list of webhook deliveries for a webhook configured in a repository. Params: owner (required), repo (required), hook_id (required).",
    inputSchema: {"type":"object","properties":{"owner":{"type":"string","description":"The account owner of the repository. The name is not case sensitive."},"repo":{"type":"string","description":"The name of the repository without the `.git` extension. The name is not case sensitive."},"hook_id":{"type":"integer","description":"The unique identifier of the hook. You can find this value in the `X-GitHub-Hook-ID` header of a webhook delivery."},"per_page":{"type":"integer","default":30,"description":"The number of results per page (max 100). For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""},"cursor":{"type":"string","description":"Used for pagination: the starting delivery from which the page of deliveries is fetched. Refer to the `link` header for the next and previous page cursors."},"status":{"type":"string","enum":["success","failure"],"description":"Returns webhook deliveries filtered by delivery outcome classification based on `status_code` range. A `status` of `success` returns deliveries with a `status_code` in the 200-399 range (inclusive). A `status` of `failure` returns deliveries with a `status_code` in the 400-599 range (inclusive)."}},"required":["owner","repo","hook_id"]},
    call: function(args) {
    var _q = new URLSearchParams();
    if (args["per_page"] !== undefined) _q.set("per_page", String(args["per_page"]));
    if (args["cursor"] !== undefined) _q.set("cursor", String(args["cursor"]));
    if (args["status"] !== undefined) _q.set("status", String(args["status"]));
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/repos/" + String(args["owner"]) + "/" + String(args["repo"]) + "/hooks/" + String(args["hook_id"]) + "/deliveries") + _qs, {}, undefined);
    }
  },
  {
    name: "repos/get-webhook-delivery",
    description: "Get a delivery for a repository webhook. Returns a delivery for a webhook configured in a repository. Params: owner (required), repo (required), hook_id (required), delivery_id (required).",
    inputSchema: {"type":"object","properties":{"owner":{"type":"string","description":"The account owner of the repository. The name is not case sensitive."},"repo":{"type":"string","description":"The name of the repository without the `.git` extension. The name is not case sensitive."},"hook_id":{"type":"integer","description":"The unique identifier of the hook. You can find this value in the `X-GitHub-Hook-ID` header of a webhook delivery."},"delivery_id":{"type":"integer"}},"required":["owner","repo","hook_id","delivery_id"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/repos/" + String(args["owner"]) + "/" + String(args["repo"]) + "/hooks/" + String(args["hook_id"]) + "/deliveries/" + String(args["delivery_id"])) + _qs, {}, undefined);
    }
  },
  {
    name: "repos/redeliver-webhook-delivery",
    description: "Redeliver a delivery for a repository webhook. Redeliver a webhook delivery for a webhook configured in a repository. Params: owner (required), repo (required), hook_id (required), delivery_id (required).",
    inputSchema: {"type":"object","properties":{"owner":{"type":"string","description":"The account owner of the repository. The name is not case sensitive."},"repo":{"type":"string","description":"The name of the repository without the `.git` extension. The name is not case sensitive."},"hook_id":{"type":"integer","description":"The unique identifier of the hook. You can find this value in the `X-GitHub-Hook-ID` header of a webhook delivery."},"delivery_id":{"type":"integer"}},"required":["owner","repo","hook_id","delivery_id"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("POST", ("https://api.github.com/repos/" + String(args["owner"]) + "/" + String(args["repo"]) + "/hooks/" + String(args["hook_id"]) + "/deliveries/" + String(args["delivery_id"]) + "/attempts") + _qs, {}, undefined);
    }
  },
  {
    name: "repos/ping-webhook",
    description: "Ping a repository webhook. This will trigger a [ping event](https://docs.github.com/webhooks/#ping-event) to be sent to the hook. Params: owner (required), repo (required), hook_id (required).",
    inputSchema: {"type":"object","properties":{"owner":{"type":"string","description":"The account owner of the repository. The name is not case sensitive."},"repo":{"type":"string","description":"The name of the repository without the `.git` extension. The name is not case sensitive."},"hook_id":{"type":"integer","description":"The unique identifier of the hook. You can find this value in the `X-GitHub-Hook-ID` header of a webhook delivery."}},"required":["owner","repo","hook_id"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("POST", ("https://api.github.com/repos/" + String(args["owner"]) + "/" + String(args["repo"]) + "/hooks/" + String(args["hook_id"]) + "/pings") + _qs, {}, undefined);
    }
  },
  {
    name: "repos/test-push-webhook",
    description: "Test the push repository webhook. This will trigger the hook with the latest push to the current repository if the hook is subscribed to `push` events. Params: owner (required), repo (required), hook_id (required).",
    inputSchema: {"type":"object","properties":{"owner":{"type":"string","description":"The account owner of the repository. The name is not case sensitive."},"repo":{"type":"string","description":"The name of the repository without the `.git` extension. The name is not case sensitive."},"hook_id":{"type":"integer","description":"The unique identifier of the hook. You can find this value in the `X-GitHub-Hook-ID` header of a webhook delivery."}},"required":["owner","repo","hook_id"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("POST", ("https://api.github.com/repos/" + String(args["owner"]) + "/" + String(args["repo"]) + "/hooks/" + String(args["hook_id"]) + "/tests") + _qs, {}, undefined);
    }
  },
  {
    name: "repos/check-immutable-releases",
    description: "Check if immutable releases are enabled for a repository. Shows whether immutable releases are enabled or disabled. Params: owner (required), repo (required).",
    inputSchema: {"type":"object","properties":{"owner":{"type":"string","description":"The account owner of the repository. The name is not case sensitive."},"repo":{"type":"string","description":"The name of the repository without the `.git` extension. The name is not case sensitive."}},"required":["owner","repo"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/repos/" + String(args["owner"]) + "/" + String(args["repo"]) + "/immutable-releases") + _qs, {}, undefined);
    }
  },
  {
    name: "repos/enable-immutable-releases",
    description: "Enable immutable releases. Enables immutable releases for a repository. Params: owner (required), repo (required).",
    inputSchema: {"type":"object","properties":{"owner":{"type":"string","description":"The account owner of the repository. The name is not case sensitive."},"repo":{"type":"string","description":"The name of the repository without the `.git` extension. The name is not case sensitive."}},"required":["owner","repo"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("PUT", ("https://api.github.com/repos/" + String(args["owner"]) + "/" + String(args["repo"]) + "/immutable-releases") + _qs, {}, undefined);
    }
  },
  {
    name: "repos/disable-immutable-releases",
    description: "Disable immutable releases. Disables immutable releases for a repository. Params: owner (required), repo (required).",
    inputSchema: {"type":"object","properties":{"owner":{"type":"string","description":"The account owner of the repository. The name is not case sensitive."},"repo":{"type":"string","description":"The name of the repository without the `.git` extension. The name is not case sensitive."}},"required":["owner","repo"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("DELETE", ("https://api.github.com/repos/" + String(args["owner"]) + "/" + String(args["repo"]) + "/immutable-releases") + _qs, {}, undefined);
    }
  },
  {
    name: "migrations/get-import-status",
    description: "Get an import status. View the progress of an import. Params: owner (required), repo (required).",
    inputSchema: {"type":"object","properties":{"owner":{"type":"string","description":"The account owner of the repository. The name is not case sensitive."},"repo":{"type":"string","description":"The name of the repository without the `.git` extension. The name is not case sensitive."}},"required":["owner","repo"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/repos/" + String(args["owner"]) + "/" + String(args["repo"]) + "/import") + _qs, {}, undefined);
    }
  },
  {
    name: "migrations/start-import",
    description: "Start an import. Start a source import to a GitHub repository using GitHub Importer. Params: owner (required), repo (required).",
    inputSchema: {"type":"object","properties":{"owner":{"type":"string","description":"The account owner of the repository. The name is not case sensitive."},"repo":{"type":"string","description":"The name of the repository without the `.git` extension. The name is not case sensitive."},"body":{"type":"object","description":"Request body"}},"required":["owner","repo","body"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("PUT", ("https://api.github.com/repos/" + String(args["owner"]) + "/" + String(args["repo"]) + "/import") + _qs, {}, args["body"]);
    }
  },
  {
    name: "migrations/update-import",
    description: "Update an import. An import can be updated with credentials or a project choice by passing in the appropriate parameters in this API\nrequest. Params: owner (required), repo (required).",
    inputSchema: {"type":"object","properties":{"owner":{"type":"string","description":"The account owner of the repository. The name is not case sensitive."},"repo":{"type":"string","description":"The name of the repository without the `.git` extension. The name is not case sensitive."},"body":{"type":"object","description":"Request body"}},"required":["owner","repo"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("PATCH", ("https://api.github.com/repos/" + String(args["owner"]) + "/" + String(args["repo"]) + "/import") + _qs, {}, args["body"]);
    }
  },
  {
    name: "migrations/cancel-import",
    description: "Cancel an import. Stop an import for a repository. Params: owner (required), repo (required).",
    inputSchema: {"type":"object","properties":{"owner":{"type":"string","description":"The account owner of the repository. The name is not case sensitive."},"repo":{"type":"string","description":"The name of the repository without the `.git` extension. The name is not case sensitive."}},"required":["owner","repo"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("DELETE", ("https://api.github.com/repos/" + String(args["owner"]) + "/" + String(args["repo"]) + "/import") + _qs, {}, undefined);
    }
  },
  {
    name: "migrations/get-commit-authors",
    description: "Get commit authors. Each type of source control system represents authors in a different way. Params: owner (required), repo (required).",
    inputSchema: {"type":"object","properties":{"owner":{"type":"string","description":"The account owner of the repository. The name is not case sensitive."},"repo":{"type":"string","description":"The name of the repository without the `.git` extension. The name is not case sensitive."},"since":{"type":"integer","description":"A user ID. Only return users with an ID greater than this ID."}},"required":["owner","repo"]},
    call: function(args) {
    var _q = new URLSearchParams();
    if (args["since"] !== undefined) _q.set("since", String(args["since"]));
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/repos/" + String(args["owner"]) + "/" + String(args["repo"]) + "/import/authors") + _qs, {}, undefined);
    }
  },
  {
    name: "migrations/map-commit-author",
    description: "Map a commit author. Update an author's identity for the import. Params: owner (required), repo (required), author_id (required).",
    inputSchema: {"type":"object","properties":{"owner":{"type":"string","description":"The account owner of the repository. The name is not case sensitive."},"repo":{"type":"string","description":"The name of the repository without the `.git` extension. The name is not case sensitive."},"author_id":{"type":"integer"},"body":{"type":"object","description":"Request body"}},"required":["owner","repo","author_id"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("PATCH", ("https://api.github.com/repos/" + String(args["owner"]) + "/" + String(args["repo"]) + "/import/authors/" + String(args["author_id"])) + _qs, {}, args["body"]);
    }
  },
  {
    name: "migrations/get-large-files",
    description: "Get large files. List files larger than 100MB found during the import\n\n> [!WARNING]\n> **Endpoint closing down notice:** Due to very low levels of usage and available alternatives, this endpoint is closing down and will no longer be available from 00:00 UTC on April 12, 2024. Params: owner (required), repo (required).",
    inputSchema: {"type":"object","properties":{"owner":{"type":"string","description":"The account owner of the repository. The name is not case sensitive."},"repo":{"type":"string","description":"The name of the repository without the `.git` extension. The name is not case sensitive."}},"required":["owner","repo"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/repos/" + String(args["owner"]) + "/" + String(args["repo"]) + "/import/large_files") + _qs, {}, undefined);
    }
  },
  {
    name: "migrations/set-lfs-preference",
    description: "Update Git LFS preference. You can import repositories from Subversion, Mercurial, and TFS that include files larger than 100MB. Params: owner (required), repo (required).",
    inputSchema: {"type":"object","properties":{"owner":{"type":"string","description":"The account owner of the repository. The name is not case sensitive."},"repo":{"type":"string","description":"The name of the repository without the `.git` extension. The name is not case sensitive."},"body":{"type":"object","description":"Request body"}},"required":["owner","repo","body"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("PATCH", ("https://api.github.com/repos/" + String(args["owner"]) + "/" + String(args["repo"]) + "/import/lfs") + _qs, {}, args["body"]);
    }
  },
  {
    name: "apps/get-repo-installation",
    description: "Get a repository installation for the authenticated app. Enables an authenticated GitHub App to find the repository's installation information. Params: owner (required), repo (required).",
    inputSchema: {"type":"object","properties":{"owner":{"type":"string","description":"The account owner of the repository. The name is not case sensitive."},"repo":{"type":"string","description":"The name of the repository without the `.git` extension. The name is not case sensitive."}},"required":["owner","repo"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/repos/" + String(args["owner"]) + "/" + String(args["repo"]) + "/installation") + _qs, {}, undefined);
    }
  },
  {
    name: "interactions/get-restrictions-for-repo",
    description: "Get interaction restrictions for a repository. Shows which type of GitHub user can interact with this repository and when the restriction expires. Params: owner (required), repo (required).",
    inputSchema: {"type":"object","properties":{"owner":{"type":"string","description":"The account owner of the repository. The name is not case sensitive."},"repo":{"type":"string","description":"The name of the repository without the `.git` extension. The name is not case sensitive."}},"required":["owner","repo"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/repos/" + String(args["owner"]) + "/" + String(args["repo"]) + "/interaction-limits") + _qs, {}, undefined);
    }
  },
  {
    name: "interactions/set-restrictions-for-repo",
    description: "Set interaction restrictions for a repository. Temporarily restricts interactions to a certain type of GitHub user within the given repository. Params: owner (required), repo (required).",
    inputSchema: {"type":"object","properties":{"owner":{"type":"string","description":"The account owner of the repository. The name is not case sensitive."},"repo":{"type":"string","description":"The name of the repository without the `.git` extension. The name is not case sensitive."},"body":{"type":"object","description":"Request body"}},"required":["owner","repo","body"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("PUT", ("https://api.github.com/repos/" + String(args["owner"]) + "/" + String(args["repo"]) + "/interaction-limits") + _qs, {}, args["body"]);
    }
  },
  {
    name: "interactions/remove-restrictions-for-repo",
    description: "Remove interaction restrictions for a repository. Removes all interaction restrictions from the given repository. Params: owner (required), repo (required).",
    inputSchema: {"type":"object","properties":{"owner":{"type":"string","description":"The account owner of the repository. The name is not case sensitive."},"repo":{"type":"string","description":"The name of the repository without the `.git` extension. The name is not case sensitive."}},"required":["owner","repo"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("DELETE", ("https://api.github.com/repos/" + String(args["owner"]) + "/" + String(args["repo"]) + "/interaction-limits") + _qs, {}, undefined);
    }
  },
  {
    name: "interactions/get-pull-request-bypass-list-for-repo",
    description: "Get pull request creation cap bypass list for a repository. Lists the users that are on the pull request creation cap bypass list for a\nrepository. Params: owner (required), repo (required).",
    inputSchema: {"type":"object","properties":{"owner":{"type":"string","description":"The account owner of the repository. The name is not case sensitive."},"repo":{"type":"string","description":"The name of the repository without the `.git` extension. The name is not case sensitive."}},"required":["owner","repo"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/repos/" + String(args["owner"]) + "/" + String(args["repo"]) + "/interaction-limits/pulls/bypass-list") + _qs, {}, undefined);
    }
  },
  {
    name: "interactions/set-pull-request-bypass-list-for-repo",
    description: "Add users to the pull request creation cap bypass list for a repository. Adds users to the pull request creation cap bypass list for a repository. Params: owner (required), repo (required).",
    inputSchema: {"type":"object","properties":{"owner":{"type":"string","description":"The account owner of the repository. The name is not case sensitive."},"repo":{"type":"string","description":"The name of the repository without the `.git` extension. The name is not case sensitive."},"body":{"type":"object","description":"Request body"}},"required":["owner","repo","body"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("PUT", ("https://api.github.com/repos/" + String(args["owner"]) + "/" + String(args["repo"]) + "/interaction-limits/pulls/bypass-list") + _qs, {}, args["body"]);
    }
  },
  {
    name: "interactions/remove-pull-request-bypass-list-for-repo",
    description: "Remove users from the pull request creation cap bypass list for a repository. Removes users from the pull request creation cap bypass list for a repository. Params: owner (required), repo (required).",
    inputSchema: {"type":"object","properties":{"owner":{"type":"string","description":"The account owner of the repository. The name is not case sensitive."},"repo":{"type":"string","description":"The name of the repository without the `.git` extension. The name is not case sensitive."},"body":{"type":"object","description":"Request body"}},"required":["owner","repo","body"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("DELETE", ("https://api.github.com/repos/" + String(args["owner"]) + "/" + String(args["repo"]) + "/interaction-limits/pulls/bypass-list") + _qs, {}, args["body"]);
    }
  },
  {
    name: "repos/list-invitations",
    description: "List repository invitations. When authenticating as a user with admin rights to a repository, this endpoint will list all currently open repository invitations. Params: owner (required), repo (required).",
    inputSchema: {"type":"object","properties":{"owner":{"type":"string","description":"The account owner of the repository. The name is not case sensitive."},"repo":{"type":"string","description":"The name of the repository without the `.git` extension. The name is not case sensitive."},"per_page":{"type":"integer","default":30,"description":"The number of results per page (max 100). For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""},"page":{"type":"integer","default":1,"description":"The page number of the results to fetch. For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""}},"required":["owner","repo"]},
    call: function(args) {
    var _q = new URLSearchParams();
    if (args["per_page"] !== undefined) _q.set("per_page", String(args["per_page"]));
    if (args["page"] !== undefined) _q.set("page", String(args["page"]));
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/repos/" + String(args["owner"]) + "/" + String(args["repo"]) + "/invitations") + _qs, {}, undefined);
    }
  },
  {
    name: "repos/update-invitation",
    description: "Update a repository invitation. Params: owner (required), repo (required), invitation_id (required).",
    inputSchema: {"type":"object","properties":{"owner":{"type":"string","description":"The account owner of the repository. The name is not case sensitive."},"repo":{"type":"string","description":"The name of the repository without the `.git` extension. The name is not case sensitive."},"invitation_id":{"type":"integer","description":"The unique identifier of the invitation."},"body":{"type":"object","description":"Request body"}},"required":["owner","repo","invitation_id"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("PATCH", ("https://api.github.com/repos/" + String(args["owner"]) + "/" + String(args["repo"]) + "/invitations/" + String(args["invitation_id"])) + _qs, {}, args["body"]);
    }
  },
  {
    name: "repos/delete-invitation",
    description: "Delete a repository invitation. Params: owner (required), repo (required), invitation_id (required).",
    inputSchema: {"type":"object","properties":{"owner":{"type":"string","description":"The account owner of the repository. The name is not case sensitive."},"repo":{"type":"string","description":"The name of the repository without the `.git` extension. The name is not case sensitive."},"invitation_id":{"type":"integer","description":"The unique identifier of the invitation."}},"required":["owner","repo","invitation_id"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("DELETE", ("https://api.github.com/repos/" + String(args["owner"]) + "/" + String(args["repo"]) + "/invitations/" + String(args["invitation_id"])) + _qs, {}, undefined);
    }
  },
  {
    name: "repos/list-issue-types",
    description: "List issue types for a repository. Lists issue types available for a repository (inherited from its organization owner, with any per-repository overrides applied). Params: owner (required), repo (required).",
    inputSchema: {"type":"object","properties":{"owner":{"type":"string","description":"The account owner of the repository. The name is not case sensitive."},"repo":{"type":"string","description":"The name of the repository without the `.git` extension. The name is not case sensitive."}},"required":["owner","repo"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/repos/" + String(args["owner"]) + "/" + String(args["repo"]) + "/issue-types") + _qs, {}, undefined);
    }
  },
  {
    name: "issues/list-for-repo",
    description: "List repository issues. List issues in a repository. Params: owner (required), repo (required).",
    inputSchema: {"type":"object","properties":{"owner":{"type":"string","description":"The account owner of the repository. The name is not case sensitive."},"repo":{"type":"string","description":"The name of the repository without the `.git` extension. The name is not case sensitive."},"milestone":{"type":"string","description":"If an `integer` is passed, it should refer to a milestone by its `number` field. If the string `*` is passed, issues with any milestone are accepted. If the string `none` is passed, issues without milestones are returned."},"state":{"type":"string","enum":["open","closed","all"],"default":"open","description":"Indicates the state of the issues to return."},"assignee":{"type":"string","description":"Can be the name of a user. Pass in `none` for issues with no assigned user, and `*` for issues assigned to any user."},"type":{"type":"string","description":"Can be the name of an issue type. If the string `*` is passed, issues with any type are accepted. If the string `none` is passed, issues without type are returned."},"creator":{"type":"string","description":"The user that created the issue."},"mentioned":{"type":"string","description":"A user that's mentioned in the issue."},"issue_field_values":{"type":"string","description":"A comma-separated list of issue field filters in `field_slug:value` format.\nOnly issues matching all specified field values are returned.\nRequires issue fields to be enabled for the repository. Issue fields are\nnot available for user-owned repositories, and field availability for\norganization-owned public repositories depends on the organization's\nvisibility settings. For example, `priority:Urgent,severity:High` filters\nissues where the `priority` field is `Urgent` AND the `severity` field is\n`High`."},"labels":{"type":"string","description":"A list of comma separated label names. Example: `bug,ui,@high`"},"sort":{"type":"string","enum":["created","updated","comments"],"default":"created","description":"What to sort results by."},"direction":{"type":"string","enum":["asc","desc"],"default":"desc","description":"The direction to sort the results by."},"since":{"type":"string","format":"date-time","description":"Only show results that were last updated after the given time. This is a timestamp in [ISO 8601](https://en.wikipedia.org/wiki/ISO_8601) format: `YYYY-MM-DDTHH:MM:SSZ`."},"per_page":{"type":"integer","default":30,"description":"The number of results per page (max 100). For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""},"page":{"type":"integer","default":1,"description":"The page number of the results to fetch. For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""}},"required":["owner","repo"]},
    call: function(args) {
    var _q = new URLSearchParams();
    if (args["milestone"] !== undefined) _q.set("milestone", String(args["milestone"]));
    if (args["state"] !== undefined) _q.set("state", String(args["state"]));
    if (args["assignee"] !== undefined) _q.set("assignee", String(args["assignee"]));
    if (args["type"] !== undefined) _q.set("type", String(args["type"]));
    if (args["creator"] !== undefined) _q.set("creator", String(args["creator"]));
    if (args["mentioned"] !== undefined) _q.set("mentioned", String(args["mentioned"]));
    if (args["issue_field_values"] !== undefined) _q.set("issue_field_values", String(args["issue_field_values"]));
    if (args["labels"] !== undefined) _q.set("labels", String(args["labels"]));
    if (args["sort"] !== undefined) _q.set("sort", String(args["sort"]));
    if (args["direction"] !== undefined) _q.set("direction", String(args["direction"]));
    if (args["since"] !== undefined) _q.set("since", String(args["since"]));
    if (args["per_page"] !== undefined) _q.set("per_page", String(args["per_page"]));
    if (args["page"] !== undefined) _q.set("page", String(args["page"]));
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/repos/" + String(args["owner"]) + "/" + String(args["repo"]) + "/issues") + _qs, {}, undefined);
    }
  },
  {
    name: "issues/create",
    description: "Create an issue. Any user with pull access to a repository can create an issue. Params: owner (required), repo (required).",
    inputSchema: {"type":"object","properties":{"owner":{"type":"string","description":"The account owner of the repository. The name is not case sensitive."},"repo":{"type":"string","description":"The name of the repository without the `.git` extension. The name is not case sensitive."},"body":{"type":"object","description":"Request body"}},"required":["owner","repo","body"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("POST", ("https://api.github.com/repos/" + String(args["owner"]) + "/" + String(args["repo"]) + "/issues") + _qs, {}, args["body"]);
    }
  },
  {
    name: "issues/list-comments-for-repo",
    description: "List issue comments for a repository. You can use the REST API to list comments on issues and pull requests for a repository. Params: owner (required), repo (required).",
    inputSchema: {"type":"object","properties":{"owner":{"type":"string","description":"The account owner of the repository. The name is not case sensitive."},"repo":{"type":"string","description":"The name of the repository without the `.git` extension. The name is not case sensitive."},"sort":{"type":"string","enum":["created","updated"],"default":"created","description":"The property to sort the results by."},"direction":{"type":"string","enum":["asc","desc"],"description":"Either `asc` or `desc`. Ignored without the `sort` parameter."},"since":{"type":"string","format":"date-time","description":"Only show results that were last updated after the given time. This is a timestamp in [ISO 8601](https://en.wikipedia.org/wiki/ISO_8601) format: `YYYY-MM-DDTHH:MM:SSZ`."},"per_page":{"type":"integer","default":30,"description":"The number of results per page (max 100). For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""},"page":{"type":"integer","default":1,"description":"The page number of the results to fetch. For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""}},"required":["owner","repo"]},
    call: function(args) {
    var _q = new URLSearchParams();
    if (args["sort"] !== undefined) _q.set("sort", String(args["sort"]));
    if (args["direction"] !== undefined) _q.set("direction", String(args["direction"]));
    if (args["since"] !== undefined) _q.set("since", String(args["since"]));
    if (args["per_page"] !== undefined) _q.set("per_page", String(args["per_page"]));
    if (args["page"] !== undefined) _q.set("page", String(args["page"]));
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/repos/" + String(args["owner"]) + "/" + String(args["repo"]) + "/issues/comments") + _qs, {}, undefined);
    }
  },
  {
    name: "issues/get-comment",
    description: "Get an issue comment. You can use the REST API to get comments on issues and pull requests. Params: owner (required), repo (required), comment_id (required).",
    inputSchema: {"type":"object","properties":{"owner":{"type":"string","description":"The account owner of the repository. The name is not case sensitive."},"repo":{"type":"string","description":"The name of the repository without the `.git` extension. The name is not case sensitive."},"comment_id":{"type":"integer","format":"int64","description":"The unique identifier of the comment."}},"required":["owner","repo","comment_id"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/repos/" + String(args["owner"]) + "/" + String(args["repo"]) + "/issues/comments/" + String(args["comment_id"])) + _qs, {}, undefined);
    }
  },
  {
    name: "issues/update-comment",
    description: "Update an issue comment. You can use the REST API to update comments on issues and pull requests. Params: owner (required), repo (required), comment_id (required).",
    inputSchema: {"type":"object","properties":{"owner":{"type":"string","description":"The account owner of the repository. The name is not case sensitive."},"repo":{"type":"string","description":"The name of the repository without the `.git` extension. The name is not case sensitive."},"comment_id":{"type":"integer","format":"int64","description":"The unique identifier of the comment."},"body":{"type":"object","description":"Request body"}},"required":["owner","repo","comment_id","body"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("PATCH", ("https://api.github.com/repos/" + String(args["owner"]) + "/" + String(args["repo"]) + "/issues/comments/" + String(args["comment_id"])) + _qs, {}, args["body"]);
    }
  },
  {
    name: "issues/delete-comment",
    description: "Delete an issue comment. You can use the REST API to delete comments on issues and pull requests. Params: owner (required), repo (required), comment_id (required).",
    inputSchema: {"type":"object","properties":{"owner":{"type":"string","description":"The account owner of the repository. The name is not case sensitive."},"repo":{"type":"string","description":"The name of the repository without the `.git` extension. The name is not case sensitive."},"comment_id":{"type":"integer","format":"int64","description":"The unique identifier of the comment."}},"required":["owner","repo","comment_id"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("DELETE", ("https://api.github.com/repos/" + String(args["owner"]) + "/" + String(args["repo"]) + "/issues/comments/" + String(args["comment_id"])) + _qs, {}, undefined);
    }
  },
  {
    name: "issues/pin-comment",
    description: "Pin an issue comment. You can use the REST API to pin comments on issues. Params: owner (required), repo (required), comment_id (required).",
    inputSchema: {"type":"object","properties":{"owner":{"type":"string","description":"The account owner of the repository. The name is not case sensitive."},"repo":{"type":"string","description":"The name of the repository without the `.git` extension. The name is not case sensitive."},"comment_id":{"type":"integer","format":"int64","description":"The unique identifier of the comment."}},"required":["owner","repo","comment_id"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("PUT", ("https://api.github.com/repos/" + String(args["owner"]) + "/" + String(args["repo"]) + "/issues/comments/" + String(args["comment_id"]) + "/pin") + _qs, {}, undefined);
    }
  },
  {
    name: "issues/unpin-comment",
    description: "Unpin an issue comment. You can use the REST API to unpin comments on issues. Params: owner (required), repo (required), comment_id (required).",
    inputSchema: {"type":"object","properties":{"owner":{"type":"string","description":"The account owner of the repository. The name is not case sensitive."},"repo":{"type":"string","description":"The name of the repository without the `.git` extension. The name is not case sensitive."},"comment_id":{"type":"integer","format":"int64","description":"The unique identifier of the comment."}},"required":["owner","repo","comment_id"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("DELETE", ("https://api.github.com/repos/" + String(args["owner"]) + "/" + String(args["repo"]) + "/issues/comments/" + String(args["comment_id"]) + "/pin") + _qs, {}, undefined);
    }
  },
  {
    name: "reactions/list-for-issue-comment",
    description: "List reactions for an issue comment. List the reactions to an [issue comment](https://docs.github.com/rest/issues/comments#get-an-issue-comment). Params: owner (required), repo (required), comment_id (required).",
    inputSchema: {"type":"object","properties":{"owner":{"type":"string","description":"The account owner of the repository. The name is not case sensitive."},"repo":{"type":"string","description":"The name of the repository without the `.git` extension. The name is not case sensitive."},"comment_id":{"type":"integer","format":"int64","description":"The unique identifier of the comment."},"content":{"type":"string","enum":["+1","-1","laugh","confused","heart","hooray","rocket","eyes"],"description":"Returns a single [reaction type](https://docs.github.com/rest/reactions/reactions#about-reactions). Omit this parameter to list all reactions to an issue comment."},"per_page":{"type":"integer","default":30,"description":"The number of results per page (max 100). For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""},"page":{"type":"integer","default":1,"description":"The page number of the results to fetch. For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""}},"required":["owner","repo","comment_id"]},
    call: function(args) {
    var _q = new URLSearchParams();
    if (args["content"] !== undefined) _q.set("content", String(args["content"]));
    if (args["per_page"] !== undefined) _q.set("per_page", String(args["per_page"]));
    if (args["page"] !== undefined) _q.set("page", String(args["page"]));
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/repos/" + String(args["owner"]) + "/" + String(args["repo"]) + "/issues/comments/" + String(args["comment_id"]) + "/reactions") + _qs, {}, undefined);
    }
  },
  {
    name: "reactions/create-for-issue-comment",
    description: "Create reaction for an issue comment. Create a reaction to an [issue comment](https://docs.github.com/rest/issues/comments#get-an-issue-comment). Params: owner (required), repo (required), comment_id (required).",
    inputSchema: {"type":"object","properties":{"owner":{"type":"string","description":"The account owner of the repository. The name is not case sensitive."},"repo":{"type":"string","description":"The name of the repository without the `.git` extension. The name is not case sensitive."},"comment_id":{"type":"integer","format":"int64","description":"The unique identifier of the comment."},"body":{"type":"object","description":"Request body"}},"required":["owner","repo","comment_id","body"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("POST", ("https://api.github.com/repos/" + String(args["owner"]) + "/" + String(args["repo"]) + "/issues/comments/" + String(args["comment_id"]) + "/reactions") + _qs, {}, args["body"]);
    }
  },
  {
    name: "reactions/delete-for-issue-comment",
    description: "Delete an issue comment reaction. > [!NOTE]\n> You can also specify a repository by `repository_id` using the route `DELETE delete /repositories/:repository_id/issues/comments/:comment_id/reactions/:reaction_id`. Params: owner (required), repo (required), comment_id (required), reaction_id (required).",
    inputSchema: {"type":"object","properties":{"owner":{"type":"string","description":"The account owner of the repository. The name is not case sensitive."},"repo":{"type":"string","description":"The name of the repository without the `.git` extension. The name is not case sensitive."},"comment_id":{"type":"integer","format":"int64","description":"The unique identifier of the comment."},"reaction_id":{"type":"integer","description":"The unique identifier of the reaction."}},"required":["owner","repo","comment_id","reaction_id"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("DELETE", ("https://api.github.com/repos/" + String(args["owner"]) + "/" + String(args["repo"]) + "/issues/comments/" + String(args["comment_id"]) + "/reactions/" + String(args["reaction_id"])) + _qs, {}, undefined);
    }
  },
  {
    name: "issues/list-events-for-repo",
    description: "List issue events for a repository. Lists events for a repository. Params: owner (required), repo (required).",
    inputSchema: {"type":"object","properties":{"owner":{"type":"string","description":"The account owner of the repository. The name is not case sensitive."},"repo":{"type":"string","description":"The name of the repository without the `.git` extension. The name is not case sensitive."},"per_page":{"type":"integer","default":30,"description":"The number of results per page (max 100). For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""},"page":{"type":"integer","default":1,"description":"The page number of the results to fetch. For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""}},"required":["owner","repo"]},
    call: function(args) {
    var _q = new URLSearchParams();
    if (args["per_page"] !== undefined) _q.set("per_page", String(args["per_page"]));
    if (args["page"] !== undefined) _q.set("page", String(args["page"]));
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/repos/" + String(args["owner"]) + "/" + String(args["repo"]) + "/issues/events") + _qs, {}, undefined);
    }
  },
  {
    name: "issues/get-event",
    description: "Get an issue event. Gets a single event by the event id. Params: owner (required), repo (required), event_id (required).",
    inputSchema: {"type":"object","properties":{"owner":{"type":"string","description":"The account owner of the repository. The name is not case sensitive."},"repo":{"type":"string","description":"The name of the repository without the `.git` extension. The name is not case sensitive."},"event_id":{"type":"integer"}},"required":["owner","repo","event_id"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/repos/" + String(args["owner"]) + "/" + String(args["repo"]) + "/issues/events/" + String(args["event_id"])) + _qs, {}, undefined);
    }
  },
  {
    name: "issues/get",
    description: "Get an issue. The API returns a [`301 Moved Permanently` status](https://docs.github.com/rest/guides/best-practices-for-using-the-rest-api#follow-redirects) if the issue was\n[transferred](https://docs.github.com/articles/transferring-an-issue-to-another-repository/) to another repository. Params: owner (required), repo (required), issue_number (required).",
    inputSchema: {"type":"object","properties":{"owner":{"type":"string","description":"The account owner of the repository. The name is not case sensitive."},"repo":{"type":"string","description":"The name of the repository without the `.git` extension. The name is not case sensitive."},"issue_number":{"type":"integer","description":"The number that identifies the issue."}},"required":["owner","repo","issue_number"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/repos/" + String(args["owner"]) + "/" + String(args["repo"]) + "/issues/" + String(args["issue_number"])) + _qs, {}, undefined);
    }
  },
  {
    name: "issues/update",
    description: "Update an issue. Issue owners and users with push access or Triage role can edit an issue. Params: owner (required), repo (required), issue_number (required).",
    inputSchema: {"type":"object","properties":{"owner":{"type":"string","description":"The account owner of the repository. The name is not case sensitive."},"repo":{"type":"string","description":"The name of the repository without the `.git` extension. The name is not case sensitive."},"issue_number":{"type":"integer","description":"The number that identifies the issue."},"body":{"type":"object","description":"Request body"}},"required":["owner","repo","issue_number"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("PATCH", ("https://api.github.com/repos/" + String(args["owner"]) + "/" + String(args["repo"]) + "/issues/" + String(args["issue_number"])) + _qs, {}, args["body"]);
    }
  },
  {
    name: "issues/add-assignees",
    description: "Add assignees to an issue. Adds up to 10 assignees to an issue. Params: owner (required), repo (required), issue_number (required).",
    inputSchema: {"type":"object","properties":{"owner":{"type":"string","description":"The account owner of the repository. The name is not case sensitive."},"repo":{"type":"string","description":"The name of the repository without the `.git` extension. The name is not case sensitive."},"issue_number":{"type":"integer","description":"The number that identifies the issue."},"body":{"type":"object","description":"Request body"}},"required":["owner","repo","issue_number"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("POST", ("https://api.github.com/repos/" + String(args["owner"]) + "/" + String(args["repo"]) + "/issues/" + String(args["issue_number"]) + "/assignees") + _qs, {}, args["body"]);
    }
  },
  {
    name: "issues/remove-assignees",
    description: "Remove assignees from an issue. Removes one or more assignees from an issue. Params: owner (required), repo (required), issue_number (required).",
    inputSchema: {"type":"object","properties":{"owner":{"type":"string","description":"The account owner of the repository. The name is not case sensitive."},"repo":{"type":"string","description":"The name of the repository without the `.git` extension. The name is not case sensitive."},"issue_number":{"type":"integer","description":"The number that identifies the issue."},"body":{"type":"object","description":"Request body"}},"required":["owner","repo","issue_number"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("DELETE", ("https://api.github.com/repos/" + String(args["owner"]) + "/" + String(args["repo"]) + "/issues/" + String(args["issue_number"]) + "/assignees") + _qs, {}, args["body"]);
    }
  },
  {
    name: "issues/check-user-can-be-assigned-to-issue",
    description: "Check if a user can be assigned to a issue. Checks if a user has permission to be assigned to a specific issue. Params: owner (required), repo (required), issue_number (required), assignee (required).",
    inputSchema: {"type":"object","properties":{"owner":{"type":"string","description":"The account owner of the repository. The name is not case sensitive."},"repo":{"type":"string","description":"The name of the repository without the `.git` extension. The name is not case sensitive."},"issue_number":{"type":"integer","description":"The number that identifies the issue."},"assignee":{"type":"string"}},"required":["owner","repo","issue_number","assignee"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/repos/" + String(args["owner"]) + "/" + String(args["repo"]) + "/issues/" + String(args["issue_number"]) + "/assignees/" + String(args["assignee"])) + _qs, {}, undefined);
    }
  },
  {
    name: "issues/list-comments",
    description: "List issue comments. You can use the REST API to list comments on issues and pull requests. Params: owner (required), repo (required), issue_number (required).",
    inputSchema: {"type":"object","properties":{"owner":{"type":"string","description":"The account owner of the repository. The name is not case sensitive."},"repo":{"type":"string","description":"The name of the repository without the `.git` extension. The name is not case sensitive."},"issue_number":{"type":"integer","description":"The number that identifies the issue."},"since":{"type":"string","format":"date-time","description":"Only show results that were last updated after the given time. This is a timestamp in [ISO 8601](https://en.wikipedia.org/wiki/ISO_8601) format: `YYYY-MM-DDTHH:MM:SSZ`."},"per_page":{"type":"integer","default":30,"description":"The number of results per page (max 100). For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""},"page":{"type":"integer","default":1,"description":"The page number of the results to fetch. For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""}},"required":["owner","repo","issue_number"]},
    call: function(args) {
    var _q = new URLSearchParams();
    if (args["since"] !== undefined) _q.set("since", String(args["since"]));
    if (args["per_page"] !== undefined) _q.set("per_page", String(args["per_page"]));
    if (args["page"] !== undefined) _q.set("page", String(args["page"]));
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/repos/" + String(args["owner"]) + "/" + String(args["repo"]) + "/issues/" + String(args["issue_number"]) + "/comments") + _qs, {}, undefined);
    }
  },
  {
    name: "issues/create-comment",
    description: "Create an issue comment. You can use the REST API to create comments on issues and pull requests. Params: owner (required), repo (required), issue_number (required).",
    inputSchema: {"type":"object","properties":{"owner":{"type":"string","description":"The account owner of the repository. The name is not case sensitive."},"repo":{"type":"string","description":"The name of the repository without the `.git` extension. The name is not case sensitive."},"issue_number":{"type":"integer","description":"The number that identifies the issue."},"body":{"type":"object","description":"Request body"}},"required":["owner","repo","issue_number","body"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("POST", ("https://api.github.com/repos/" + String(args["owner"]) + "/" + String(args["repo"]) + "/issues/" + String(args["issue_number"]) + "/comments") + _qs, {}, args["body"]);
    }
  },
  {
    name: "issues/list-dependencies-blocked-by",
    description: "List dependencies an issue is blocked by. You can use the REST API to list the dependencies an issue is blocked by. Params: owner (required), repo (required), issue_number (required).",
    inputSchema: {"type":"object","properties":{"owner":{"type":"string","description":"The account owner of the repository. The name is not case sensitive."},"repo":{"type":"string","description":"The name of the repository without the `.git` extension. The name is not case sensitive."},"issue_number":{"type":"integer","description":"The number that identifies the issue."},"per_page":{"type":"integer","default":30,"description":"The number of results per page (max 100). For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""},"page":{"type":"integer","default":1,"description":"The page number of the results to fetch. For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""}},"required":["owner","repo","issue_number"]},
    call: function(args) {
    var _q = new URLSearchParams();
    if (args["per_page"] !== undefined) _q.set("per_page", String(args["per_page"]));
    if (args["page"] !== undefined) _q.set("page", String(args["page"]));
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/repos/" + String(args["owner"]) + "/" + String(args["repo"]) + "/issues/" + String(args["issue_number"]) + "/dependencies/blocked_by") + _qs, {}, undefined);
    }
  },
  {
    name: "issues/add-blocked-by-dependency",
    description: "Add a dependency an issue is blocked by. You can use the REST API to add a 'blocked by' relationship to an issue. Params: owner (required), repo (required), issue_number (required).",
    inputSchema: {"type":"object","properties":{"owner":{"type":"string","description":"The account owner of the repository. The name is not case sensitive."},"repo":{"type":"string","description":"The name of the repository without the `.git` extension. The name is not case sensitive."},"issue_number":{"type":"integer","description":"The number that identifies the issue."},"body":{"type":"object","description":"Request body"}},"required":["owner","repo","issue_number","body"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("POST", ("https://api.github.com/repos/" + String(args["owner"]) + "/" + String(args["repo"]) + "/issues/" + String(args["issue_number"]) + "/dependencies/blocked_by") + _qs, {}, args["body"]);
    }
  },
  {
    name: "issues/remove-dependency-blocked-by",
    description: "Remove dependency an issue is blocked by. You can use the REST API to remove a dependency that an issue is blocked by. Params: owner (required), repo (required), issue_number (required), issue_id (required).",
    inputSchema: {"type":"object","properties":{"owner":{"type":"string","description":"The account owner of the repository. The name is not case sensitive."},"repo":{"type":"string","description":"The name of the repository without the `.git` extension. The name is not case sensitive."},"issue_number":{"type":"integer","description":"The number that identifies the issue."},"issue_id":{"type":"integer","description":"The id of the blocking issue to remove as a dependency"}},"required":["owner","repo","issue_number","issue_id"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("DELETE", ("https://api.github.com/repos/" + String(args["owner"]) + "/" + String(args["repo"]) + "/issues/" + String(args["issue_number"]) + "/dependencies/blocked_by/" + String(args["issue_id"])) + _qs, {}, undefined);
    }
  },
  {
    name: "issues/list-dependencies-blocking",
    description: "List dependencies an issue is blocking. You can use the REST API to list the dependencies an issue is blocking. Params: owner (required), repo (required), issue_number (required).",
    inputSchema: {"type":"object","properties":{"owner":{"type":"string","description":"The account owner of the repository. The name is not case sensitive."},"repo":{"type":"string","description":"The name of the repository without the `.git` extension. The name is not case sensitive."},"issue_number":{"type":"integer","description":"The number that identifies the issue."},"per_page":{"type":"integer","default":30,"description":"The number of results per page (max 100). For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""},"page":{"type":"integer","default":1,"description":"The page number of the results to fetch. For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""}},"required":["owner","repo","issue_number"]},
    call: function(args) {
    var _q = new URLSearchParams();
    if (args["per_page"] !== undefined) _q.set("per_page", String(args["per_page"]));
    if (args["page"] !== undefined) _q.set("page", String(args["page"]));
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/repos/" + String(args["owner"]) + "/" + String(args["repo"]) + "/issues/" + String(args["issue_number"]) + "/dependencies/blocking") + _qs, {}, undefined);
    }
  },
  {
    name: "issues/list-events",
    description: "List issue events. Lists all events for an issue. Params: owner (required), repo (required), issue_number (required).",
    inputSchema: {"type":"object","properties":{"owner":{"type":"string","description":"The account owner of the repository. The name is not case sensitive."},"repo":{"type":"string","description":"The name of the repository without the `.git` extension. The name is not case sensitive."},"issue_number":{"type":"integer","description":"The number that identifies the issue."},"per_page":{"type":"integer","default":30,"description":"The number of results per page (max 100). For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""},"page":{"type":"integer","default":1,"description":"The page number of the results to fetch. For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""}},"required":["owner","repo","issue_number"]},
    call: function(args) {
    var _q = new URLSearchParams();
    if (args["per_page"] !== undefined) _q.set("per_page", String(args["per_page"]));
    if (args["page"] !== undefined) _q.set("page", String(args["page"]));
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/repos/" + String(args["owner"]) + "/" + String(args["repo"]) + "/issues/" + String(args["issue_number"]) + "/events") + _qs, {}, undefined);
    }
  },
  {
    name: "issues/list-issue-field-values-for-issue",
    description: "List issue field values for an issue. Lists all issue field values for an issue. Params: owner (required), repo (required), issue_number (required).",
    inputSchema: {"type":"object","properties":{"owner":{"type":"string","description":"The account owner of the repository. The name is not case sensitive."},"repo":{"type":"string","description":"The name of the repository without the `.git` extension. The name is not case sensitive."},"issue_number":{"type":"integer","description":"The number that identifies the issue."},"per_page":{"type":"integer","default":30,"description":"The number of results per page (max 100). For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""},"page":{"type":"integer","default":1,"description":"The page number of the results to fetch. For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""}},"required":["owner","repo","issue_number"]},
    call: function(args) {
    var _q = new URLSearchParams();
    if (args["per_page"] !== undefined) _q.set("per_page", String(args["per_page"]));
    if (args["page"] !== undefined) _q.set("page", String(args["page"]));
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/repos/" + String(args["owner"]) + "/" + String(args["repo"]) + "/issues/" + String(args["issue_number"]) + "/issue-field-values") + _qs, {}, undefined);
    }
  },
  {
    name: "issues/add-issue-field-values",
    description: "Add issue field values to an issue. Add custom field values to an issue. Params: owner (required), repo (required), issue_number (required).",
    inputSchema: {"type":"object","properties":{"owner":{"type":"string","description":"The account owner of the repository. The name is not case sensitive."},"repo":{"type":"string","description":"The name of the repository without the `.git` extension. The name is not case sensitive."},"issue_number":{"type":"integer","description":"The number that identifies the issue."},"body":{"type":"object","description":"Request body"}},"required":["owner","repo","issue_number","body"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("POST", ("https://api.github.com/repos/" + String(args["owner"]) + "/" + String(args["repo"]) + "/issues/" + String(args["issue_number"]) + "/issue-field-values") + _qs, {}, args["body"]);
    }
  },
  {
    name: "issues/set-issue-field-values",
    description: "Set issue field values for an issue. Set custom field values for an issue, replacing any existing values. Params: owner (required), repo (required), issue_number (required).",
    inputSchema: {"type":"object","properties":{"owner":{"type":"string","description":"The account owner of the repository. The name is not case sensitive."},"repo":{"type":"string","description":"The name of the repository without the `.git` extension. The name is not case sensitive."},"issue_number":{"type":"integer","description":"The number that identifies the issue."},"body":{"type":"object","description":"Request body"}},"required":["owner","repo","issue_number","body"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("PUT", ("https://api.github.com/repos/" + String(args["owner"]) + "/" + String(args["repo"]) + "/issues/" + String(args["issue_number"]) + "/issue-field-values") + _qs, {}, args["body"]);
    }
  },
  {
    name: "issues/delete-issue-field-value",
    description: "Delete an issue field value from an issue. Remove a specific custom field value from an issue. Params: owner (required), repo (required), issue_number (required), issue_field_id (required).",
    inputSchema: {"type":"object","properties":{"owner":{"type":"string","description":"The account owner of the repository. The name is not case sensitive."},"repo":{"type":"string","description":"The name of the repository without the `.git` extension. The name is not case sensitive."},"issue_number":{"type":"integer","description":"The number that identifies the issue."},"issue_field_id":{"type":"integer","description":"The unique identifier of the issue field."}},"required":["owner","repo","issue_number","issue_field_id"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("DELETE", ("https://api.github.com/repos/" + String(args["owner"]) + "/" + String(args["repo"]) + "/issues/" + String(args["issue_number"]) + "/issue-field-values/" + String(args["issue_field_id"])) + _qs, {}, undefined);
    }
  },
  {
    name: "issues/list-labels-on-issue",
    description: "List labels for an issue. Lists all labels for an issue. Params: owner (required), repo (required), issue_number (required).",
    inputSchema: {"type":"object","properties":{"owner":{"type":"string","description":"The account owner of the repository. The name is not case sensitive."},"repo":{"type":"string","description":"The name of the repository without the `.git` extension. The name is not case sensitive."},"issue_number":{"type":"integer","description":"The number that identifies the issue."},"per_page":{"type":"integer","default":30,"description":"The number of results per page (max 100). For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""},"page":{"type":"integer","default":1,"description":"The page number of the results to fetch. For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""}},"required":["owner","repo","issue_number"]},
    call: function(args) {
    var _q = new URLSearchParams();
    if (args["per_page"] !== undefined) _q.set("per_page", String(args["per_page"]));
    if (args["page"] !== undefined) _q.set("page", String(args["page"]));
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/repos/" + String(args["owner"]) + "/" + String(args["repo"]) + "/issues/" + String(args["issue_number"]) + "/labels") + _qs, {}, undefined);
    }
  },
  {
    name: "issues/add-labels",
    description: "Add labels to an issue. Adds labels to an issue. Params: owner (required), repo (required), issue_number (required).",
    inputSchema: {"type":"object","properties":{"owner":{"type":"string","description":"The account owner of the repository. The name is not case sensitive."},"repo":{"type":"string","description":"The name of the repository without the `.git` extension. The name is not case sensitive."},"issue_number":{"type":"integer","description":"The number that identifies the issue."},"body":{"type":"object","description":"Request body"}},"required":["owner","repo","issue_number"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("POST", ("https://api.github.com/repos/" + String(args["owner"]) + "/" + String(args["repo"]) + "/issues/" + String(args["issue_number"]) + "/labels") + _qs, {}, args["body"]);
    }
  },
  {
    name: "issues/set-labels",
    description: "Set labels for an issue. Removes any previous labels and sets the new labels for an issue. Params: owner (required), repo (required), issue_number (required).",
    inputSchema: {"type":"object","properties":{"owner":{"type":"string","description":"The account owner of the repository. The name is not case sensitive."},"repo":{"type":"string","description":"The name of the repository without the `.git` extension. The name is not case sensitive."},"issue_number":{"type":"integer","description":"The number that identifies the issue."},"body":{"type":"object","description":"Request body"}},"required":["owner","repo","issue_number"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("PUT", ("https://api.github.com/repos/" + String(args["owner"]) + "/" + String(args["repo"]) + "/issues/" + String(args["issue_number"]) + "/labels") + _qs, {}, args["body"]);
    }
  },
  {
    name: "issues/remove-all-labels",
    description: "Remove all labels from an issue. Removes all labels from an issue. Params: owner (required), repo (required), issue_number (required).",
    inputSchema: {"type":"object","properties":{"owner":{"type":"string","description":"The account owner of the repository. The name is not case sensitive."},"repo":{"type":"string","description":"The name of the repository without the `.git` extension. The name is not case sensitive."},"issue_number":{"type":"integer","description":"The number that identifies the issue."}},"required":["owner","repo","issue_number"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("DELETE", ("https://api.github.com/repos/" + String(args["owner"]) + "/" + String(args["repo"]) + "/issues/" + String(args["issue_number"]) + "/labels") + _qs, {}, undefined);
    }
  },
  {
    name: "issues/remove-label",
    description: "Remove a label from an issue. Removes the specified label from the issue, and returns the remaining labels on the issue. Params: owner (required), repo (required), issue_number (required), name (required).",
    inputSchema: {"type":"object","properties":{"owner":{"type":"string","description":"The account owner of the repository. The name is not case sensitive."},"repo":{"type":"string","description":"The name of the repository without the `.git` extension. The name is not case sensitive."},"issue_number":{"type":"integer","description":"The number that identifies the issue."},"name":{"type":"string"}},"required":["owner","repo","issue_number","name"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("DELETE", ("https://api.github.com/repos/" + String(args["owner"]) + "/" + String(args["repo"]) + "/issues/" + String(args["issue_number"]) + "/labels/" + String(args["name"])) + _qs, {}, undefined);
    }
  },
  {
    name: "issues/lock",
    description: "Lock an issue. Users with push access can lock an issue or pull request's conversation. Params: owner (required), repo (required), issue_number (required).",
    inputSchema: {"type":"object","properties":{"owner":{"type":"string","description":"The account owner of the repository. The name is not case sensitive."},"repo":{"type":"string","description":"The name of the repository without the `.git` extension. The name is not case sensitive."},"issue_number":{"type":"integer","description":"The number that identifies the issue."},"body":{"type":"object","description":"Request body"}},"required":["owner","repo","issue_number"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("PUT", ("https://api.github.com/repos/" + String(args["owner"]) + "/" + String(args["repo"]) + "/issues/" + String(args["issue_number"]) + "/lock") + _qs, {}, args["body"]);
    }
  },
  {
    name: "issues/unlock",
    description: "Unlock an issue. Users with push access can unlock an issue's conversation. Params: owner (required), repo (required), issue_number (required).",
    inputSchema: {"type":"object","properties":{"owner":{"type":"string","description":"The account owner of the repository. The name is not case sensitive."},"repo":{"type":"string","description":"The name of the repository without the `.git` extension. The name is not case sensitive."},"issue_number":{"type":"integer","description":"The number that identifies the issue."}},"required":["owner","repo","issue_number"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("DELETE", ("https://api.github.com/repos/" + String(args["owner"]) + "/" + String(args["repo"]) + "/issues/" + String(args["issue_number"]) + "/lock") + _qs, {}, undefined);
    }
  },
  {
    name: "issues/get-parent",
    description: "Get parent issue. You can use the REST API to get the parent issue of a sub-issue. Params: owner (required), repo (required), issue_number (required).",
    inputSchema: {"type":"object","properties":{"owner":{"type":"string","description":"The account owner of the repository. The name is not case sensitive."},"repo":{"type":"string","description":"The name of the repository without the `.git` extension. The name is not case sensitive."},"issue_number":{"type":"integer","description":"The number that identifies the issue."}},"required":["owner","repo","issue_number"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/repos/" + String(args["owner"]) + "/" + String(args["repo"]) + "/issues/" + String(args["issue_number"]) + "/parent") + _qs, {}, undefined);
    }
  },
  {
    name: "reactions/list-for-issue",
    description: "List reactions for an issue. List the reactions to an [issue](https://docs.github.com/rest/issues/issues#get-an-issue). Params: owner (required), repo (required), issue_number (required).",
    inputSchema: {"type":"object","properties":{"owner":{"type":"string","description":"The account owner of the repository. The name is not case sensitive."},"repo":{"type":"string","description":"The name of the repository without the `.git` extension. The name is not case sensitive."},"issue_number":{"type":"integer","description":"The number that identifies the issue."},"content":{"type":"string","enum":["+1","-1","laugh","confused","heart","hooray","rocket","eyes"],"description":"Returns a single [reaction type](https://docs.github.com/rest/reactions/reactions#about-reactions). Omit this parameter to list all reactions to an issue."},"per_page":{"type":"integer","default":30,"description":"The number of results per page (max 100). For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""},"page":{"type":"integer","default":1,"description":"The page number of the results to fetch. For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""}},"required":["owner","repo","issue_number"]},
    call: function(args) {
    var _q = new URLSearchParams();
    if (args["content"] !== undefined) _q.set("content", String(args["content"]));
    if (args["per_page"] !== undefined) _q.set("per_page", String(args["per_page"]));
    if (args["page"] !== undefined) _q.set("page", String(args["page"]));
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/repos/" + String(args["owner"]) + "/" + String(args["repo"]) + "/issues/" + String(args["issue_number"]) + "/reactions") + _qs, {}, undefined);
    }
  },
  {
    name: "reactions/create-for-issue",
    description: "Create reaction for an issue. Create a reaction to an [issue](https://docs.github.com/rest/issues/issues#get-an-issue). Params: owner (required), repo (required), issue_number (required).",
    inputSchema: {"type":"object","properties":{"owner":{"type":"string","description":"The account owner of the repository. The name is not case sensitive."},"repo":{"type":"string","description":"The name of the repository without the `.git` extension. The name is not case sensitive."},"issue_number":{"type":"integer","description":"The number that identifies the issue."},"body":{"type":"object","description":"Request body"}},"required":["owner","repo","issue_number","body"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("POST", ("https://api.github.com/repos/" + String(args["owner"]) + "/" + String(args["repo"]) + "/issues/" + String(args["issue_number"]) + "/reactions") + _qs, {}, args["body"]);
    }
  },
  {
    name: "reactions/delete-for-issue",
    description: "Delete an issue reaction. > [!NOTE]\n> You can also specify a repository by `repository_id` using the route `DELETE /repositories/:repository_id/issues/:issue_number/reactions/:reaction_id`. Params: owner (required), repo (required), issue_number (required), reaction_id (required).",
    inputSchema: {"type":"object","properties":{"owner":{"type":"string","description":"The account owner of the repository. The name is not case sensitive."},"repo":{"type":"string","description":"The name of the repository without the `.git` extension. The name is not case sensitive."},"issue_number":{"type":"integer","description":"The number that identifies the issue."},"reaction_id":{"type":"integer","description":"The unique identifier of the reaction."}},"required":["owner","repo","issue_number","reaction_id"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("DELETE", ("https://api.github.com/repos/" + String(args["owner"]) + "/" + String(args["repo"]) + "/issues/" + String(args["issue_number"]) + "/reactions/" + String(args["reaction_id"])) + _qs, {}, undefined);
    }
  },
  {
    name: "issues/remove-sub-issue",
    description: "Remove sub-issue. You can use the REST API to remove a sub-issue from an issue. Params: owner (required), repo (required), issue_number (required).",
    inputSchema: {"type":"object","properties":{"owner":{"type":"string","description":"The account owner of the repository. The name is not case sensitive."},"repo":{"type":"string","description":"The name of the repository without the `.git` extension. The name is not case sensitive."},"issue_number":{"type":"integer","description":"The number that identifies the issue."},"body":{"type":"object","description":"Request body"}},"required":["owner","repo","issue_number","body"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("DELETE", ("https://api.github.com/repos/" + String(args["owner"]) + "/" + String(args["repo"]) + "/issues/" + String(args["issue_number"]) + "/sub_issue") + _qs, {}, args["body"]);
    }
  },
  {
    name: "issues/list-sub-issues",
    description: "List sub-issues. You can use the REST API to list the sub-issues on an issue. Params: owner (required), repo (required), issue_number (required).",
    inputSchema: {"type":"object","properties":{"owner":{"type":"string","description":"The account owner of the repository. The name is not case sensitive."},"repo":{"type":"string","description":"The name of the repository without the `.git` extension. The name is not case sensitive."},"issue_number":{"type":"integer","description":"The number that identifies the issue."},"per_page":{"type":"integer","default":30,"description":"The number of results per page (max 100). For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""},"page":{"type":"integer","default":1,"description":"The page number of the results to fetch. For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""}},"required":["owner","repo","issue_number"]},
    call: function(args) {
    var _q = new URLSearchParams();
    if (args["per_page"] !== undefined) _q.set("per_page", String(args["per_page"]));
    if (args["page"] !== undefined) _q.set("page", String(args["page"]));
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/repos/" + String(args["owner"]) + "/" + String(args["repo"]) + "/issues/" + String(args["issue_number"]) + "/sub_issues") + _qs, {}, undefined);
    }
  },
  {
    name: "issues/add-sub-issue",
    description: "Add sub-issue. You can use the REST API to add sub-issues to issues. Params: owner (required), repo (required), issue_number (required).",
    inputSchema: {"type":"object","properties":{"owner":{"type":"string","description":"The account owner of the repository. The name is not case sensitive."},"repo":{"type":"string","description":"The name of the repository without the `.git` extension. The name is not case sensitive."},"issue_number":{"type":"integer","description":"The number that identifies the issue."},"body":{"type":"object","description":"Request body"}},"required":["owner","repo","issue_number","body"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("POST", ("https://api.github.com/repos/" + String(args["owner"]) + "/" + String(args["repo"]) + "/issues/" + String(args["issue_number"]) + "/sub_issues") + _qs, {}, args["body"]);
    }
  },
  {
    name: "issues/reprioritize-sub-issue",
    description: "Reprioritize sub-issue. You can use the REST API to reprioritize a sub-issue to a different position in the parent list. Params: owner (required), repo (required), issue_number (required).",
    inputSchema: {"type":"object","properties":{"owner":{"type":"string","description":"The account owner of the repository. The name is not case sensitive."},"repo":{"type":"string","description":"The name of the repository without the `.git` extension. The name is not case sensitive."},"issue_number":{"type":"integer","description":"The number that identifies the issue."},"body":{"type":"object","description":"Request body"}},"required":["owner","repo","issue_number","body"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("PATCH", ("https://api.github.com/repos/" + String(args["owner"]) + "/" + String(args["repo"]) + "/issues/" + String(args["issue_number"]) + "/sub_issues/priority") + _qs, {}, args["body"]);
    }
  },
  {
    name: "issues/list-events-for-timeline",
    description: "List timeline events for an issue. List all timeline events for an issue. Params: owner (required), repo (required), issue_number (required).",
    inputSchema: {"type":"object","properties":{"owner":{"type":"string","description":"The account owner of the repository. The name is not case sensitive."},"repo":{"type":"string","description":"The name of the repository without the `.git` extension. The name is not case sensitive."},"issue_number":{"type":"integer","description":"The number that identifies the issue."},"per_page":{"type":"integer","default":30,"description":"The number of results per page (max 100). For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""},"page":{"type":"integer","default":1,"description":"The page number of the results to fetch. For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""}},"required":["owner","repo","issue_number"]},
    call: function(args) {
    var _q = new URLSearchParams();
    if (args["per_page"] !== undefined) _q.set("per_page", String(args["per_page"]));
    if (args["page"] !== undefined) _q.set("page", String(args["page"]));
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/repos/" + String(args["owner"]) + "/" + String(args["repo"]) + "/issues/" + String(args["issue_number"]) + "/timeline") + _qs, {}, undefined);
    }
  },
  {
    name: "repos/list-deploy-keys",
    description: "List deploy keys. Params: owner (required), repo (required).",
    inputSchema: {"type":"object","properties":{"owner":{"type":"string","description":"The account owner of the repository. The name is not case sensitive."},"repo":{"type":"string","description":"The name of the repository without the `.git` extension. The name is not case sensitive."},"per_page":{"type":"integer","default":30,"description":"The number of results per page (max 100). For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""},"page":{"type":"integer","default":1,"description":"The page number of the results to fetch. For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""}},"required":["owner","repo"]},
    call: function(args) {
    var _q = new URLSearchParams();
    if (args["per_page"] !== undefined) _q.set("per_page", String(args["per_page"]));
    if (args["page"] !== undefined) _q.set("page", String(args["page"]));
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/repos/" + String(args["owner"]) + "/" + String(args["repo"]) + "/keys") + _qs, {}, undefined);
    }
  },
  {
    name: "repos/create-deploy-key",
    description: "Create a deploy key. You can create a read-only deploy key. Params: owner (required), repo (required).",
    inputSchema: {"type":"object","properties":{"owner":{"type":"string","description":"The account owner of the repository. The name is not case sensitive."},"repo":{"type":"string","description":"The name of the repository without the `.git` extension. The name is not case sensitive."},"body":{"type":"object","description":"Request body"}},"required":["owner","repo","body"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("POST", ("https://api.github.com/repos/" + String(args["owner"]) + "/" + String(args["repo"]) + "/keys") + _qs, {}, args["body"]);
    }
  },
  {
    name: "repos/get-deploy-key",
    description: "Get a deploy key. Params: owner (required), repo (required), key_id (required).",
    inputSchema: {"type":"object","properties":{"owner":{"type":"string","description":"The account owner of the repository. The name is not case sensitive."},"repo":{"type":"string","description":"The name of the repository without the `.git` extension. The name is not case sensitive."},"key_id":{"type":"integer","description":"The unique identifier of the key."}},"required":["owner","repo","key_id"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/repos/" + String(args["owner"]) + "/" + String(args["repo"]) + "/keys/" + String(args["key_id"])) + _qs, {}, undefined);
    }
  },
  {
    name: "repos/delete-deploy-key",
    description: "Delete a deploy key. Deploy keys are immutable. Params: owner (required), repo (required), key_id (required).",
    inputSchema: {"type":"object","properties":{"owner":{"type":"string","description":"The account owner of the repository. The name is not case sensitive."},"repo":{"type":"string","description":"The name of the repository without the `.git` extension. The name is not case sensitive."},"key_id":{"type":"integer","description":"The unique identifier of the key."}},"required":["owner","repo","key_id"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("DELETE", ("https://api.github.com/repos/" + String(args["owner"]) + "/" + String(args["repo"]) + "/keys/" + String(args["key_id"])) + _qs, {}, undefined);
    }
  },
  {
    name: "issues/list-labels-for-repo",
    description: "List labels for a repository. Lists all labels for a repository. Params: owner (required), repo (required).",
    inputSchema: {"type":"object","properties":{"owner":{"type":"string","description":"The account owner of the repository. The name is not case sensitive."},"repo":{"type":"string","description":"The name of the repository without the `.git` extension. The name is not case sensitive."},"per_page":{"type":"integer","default":30,"description":"The number of results per page (max 100). For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""},"page":{"type":"integer","default":1,"description":"The page number of the results to fetch. For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""}},"required":["owner","repo"]},
    call: function(args) {
    var _q = new URLSearchParams();
    if (args["per_page"] !== undefined) _q.set("per_page", String(args["per_page"]));
    if (args["page"] !== undefined) _q.set("page", String(args["page"]));
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/repos/" + String(args["owner"]) + "/" + String(args["repo"]) + "/labels") + _qs, {}, undefined);
    }
  },
  {
    name: "issues/create-label",
    description: "Create a label. Creates a label for the specified repository with the given name and color. Params: owner (required), repo (required).",
    inputSchema: {"type":"object","properties":{"owner":{"type":"string","description":"The account owner of the repository. The name is not case sensitive."},"repo":{"type":"string","description":"The name of the repository without the `.git` extension. The name is not case sensitive."},"body":{"type":"object","description":"Request body"}},"required":["owner","repo","body"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("POST", ("https://api.github.com/repos/" + String(args["owner"]) + "/" + String(args["repo"]) + "/labels") + _qs, {}, args["body"]);
    }
  },
  {
    name: "issues/get-label",
    description: "Get a label. Gets a label using the given name. Params: owner (required), repo (required), name (required).",
    inputSchema: {"type":"object","properties":{"owner":{"type":"string","description":"The account owner of the repository. The name is not case sensitive."},"repo":{"type":"string","description":"The name of the repository without the `.git` extension. The name is not case sensitive."},"name":{"type":"string"}},"required":["owner","repo","name"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/repos/" + String(args["owner"]) + "/" + String(args["repo"]) + "/labels/" + String(args["name"])) + _qs, {}, undefined);
    }
  },
  {
    name: "issues/update-label",
    description: "Update a label. Updates a label using the given label name. Params: owner (required), repo (required), name (required).",
    inputSchema: {"type":"object","properties":{"owner":{"type":"string","description":"The account owner of the repository. The name is not case sensitive."},"repo":{"type":"string","description":"The name of the repository without the `.git` extension. The name is not case sensitive."},"name":{"type":"string"},"body":{"type":"object","description":"Request body"}},"required":["owner","repo","name"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("PATCH", ("https://api.github.com/repos/" + String(args["owner"]) + "/" + String(args["repo"]) + "/labels/" + String(args["name"])) + _qs, {}, args["body"]);
    }
  },
  {
    name: "issues/delete-label",
    description: "Delete a label. Deletes a label using the given label name. Params: owner (required), repo (required), name (required).",
    inputSchema: {"type":"object","properties":{"owner":{"type":"string","description":"The account owner of the repository. The name is not case sensitive."},"repo":{"type":"string","description":"The name of the repository without the `.git` extension. The name is not case sensitive."},"name":{"type":"string"}},"required":["owner","repo","name"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("DELETE", ("https://api.github.com/repos/" + String(args["owner"]) + "/" + String(args["repo"]) + "/labels/" + String(args["name"])) + _qs, {}, undefined);
    }
  },
  {
    name: "repos/list-languages",
    description: "List repository languages. Lists languages for the specified repository. Params: owner (required), repo (required).",
    inputSchema: {"type":"object","properties":{"owner":{"type":"string","description":"The account owner of the repository. The name is not case sensitive."},"repo":{"type":"string","description":"The name of the repository without the `.git` extension. The name is not case sensitive."}},"required":["owner","repo"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/repos/" + String(args["owner"]) + "/" + String(args["repo"]) + "/languages") + _qs, {}, undefined);
    }
  },
  {
    name: "licenses/get-for-repo",
    description: "Get the license for a repository. This method returns the contents of the repository's license file, if one is detected. Params: owner (required), repo (required).",
    inputSchema: {"type":"object","properties":{"owner":{"type":"string","description":"The account owner of the repository. The name is not case sensitive."},"repo":{"type":"string","description":"The name of the repository without the `.git` extension. The name is not case sensitive."},"ref":{"$ref":"#/components/schemas/code-scanning-ref","description":"The Git reference for the results you want to list. The `ref` for a branch can be formatted either as `refs/heads/<branch name>` or simply `<branch name>`. To reference a pull request use `refs/pull/<number>/merge`."}},"required":["owner","repo"]},
    call: function(args) {
    var _q = new URLSearchParams();
    if (args["ref"] !== undefined) _q.set("ref", String(args["ref"]));
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/repos/" + String(args["owner"]) + "/" + String(args["repo"]) + "/license") + _qs, {}, undefined);
    }
  },
  {
    name: "repos/merge-upstream",
    description: "Sync a fork branch with the upstream repository. Sync a branch of a forked repository to keep it up-to-date with the upstream repository. Params: owner (required), repo (required).",
    inputSchema: {"type":"object","properties":{"owner":{"type":"string","description":"The account owner of the repository. The name is not case sensitive."},"repo":{"type":"string","description":"The name of the repository without the `.git` extension. The name is not case sensitive."},"body":{"type":"object","description":"Request body"}},"required":["owner","repo","body"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("POST", ("https://api.github.com/repos/" + String(args["owner"]) + "/" + String(args["repo"]) + "/merge-upstream") + _qs, {}, args["body"]);
    }
  },
  {
    name: "repos/merge",
    description: "Merge a branch. Params: owner (required), repo (required).",
    inputSchema: {"type":"object","properties":{"owner":{"type":"string","description":"The account owner of the repository. The name is not case sensitive."},"repo":{"type":"string","description":"The name of the repository without the `.git` extension. The name is not case sensitive."},"body":{"type":"object","description":"Request body"}},"required":["owner","repo","body"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("POST", ("https://api.github.com/repos/" + String(args["owner"]) + "/" + String(args["repo"]) + "/merges") + _qs, {}, args["body"]);
    }
  },
  {
    name: "issues/list-milestones",
    description: "List milestones. Lists milestones for a repository. Params: owner (required), repo (required).",
    inputSchema: {"type":"object","properties":{"owner":{"type":"string","description":"The account owner of the repository. The name is not case sensitive."},"repo":{"type":"string","description":"The name of the repository without the `.git` extension. The name is not case sensitive."},"state":{"type":"string","enum":["open","closed","all"],"default":"open","description":"The state of the milestone. Either `open`, `closed`, or `all`."},"sort":{"type":"string","enum":["due_on","completeness"],"default":"due_on","description":"What to sort results by. Either `due_on` or `completeness`."},"direction":{"type":"string","enum":["asc","desc"],"default":"asc","description":"The direction of the sort. Either `asc` or `desc`."},"per_page":{"type":"integer","default":30,"description":"The number of results per page (max 100). For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""},"page":{"type":"integer","default":1,"description":"The page number of the results to fetch. For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""}},"required":["owner","repo"]},
    call: function(args) {
    var _q = new URLSearchParams();
    if (args["state"] !== undefined) _q.set("state", String(args["state"]));
    if (args["sort"] !== undefined) _q.set("sort", String(args["sort"]));
    if (args["direction"] !== undefined) _q.set("direction", String(args["direction"]));
    if (args["per_page"] !== undefined) _q.set("per_page", String(args["per_page"]));
    if (args["page"] !== undefined) _q.set("page", String(args["page"]));
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/repos/" + String(args["owner"]) + "/" + String(args["repo"]) + "/milestones") + _qs, {}, undefined);
    }
  },
  {
    name: "issues/create-milestone",
    description: "Create a milestone. Creates a milestone. Params: owner (required), repo (required).",
    inputSchema: {"type":"object","properties":{"owner":{"type":"string","description":"The account owner of the repository. The name is not case sensitive."},"repo":{"type":"string","description":"The name of the repository without the `.git` extension. The name is not case sensitive."},"body":{"type":"object","description":"Request body"}},"required":["owner","repo","body"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("POST", ("https://api.github.com/repos/" + String(args["owner"]) + "/" + String(args["repo"]) + "/milestones") + _qs, {}, args["body"]);
    }
  },
  {
    name: "issues/get-milestone",
    description: "Get a milestone. Gets a milestone using the given milestone number. Params: owner (required), repo (required), milestone_number (required).",
    inputSchema: {"type":"object","properties":{"owner":{"type":"string","description":"The account owner of the repository. The name is not case sensitive."},"repo":{"type":"string","description":"The name of the repository without the `.git` extension. The name is not case sensitive."},"milestone_number":{"type":"integer","description":"The number that identifies the milestone."}},"required":["owner","repo","milestone_number"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/repos/" + String(args["owner"]) + "/" + String(args["repo"]) + "/milestones/" + String(args["milestone_number"])) + _qs, {}, undefined);
    }
  },
  {
    name: "issues/update-milestone",
    description: "Update a milestone. Params: owner (required), repo (required), milestone_number (required).",
    inputSchema: {"type":"object","properties":{"owner":{"type":"string","description":"The account owner of the repository. The name is not case sensitive."},"repo":{"type":"string","description":"The name of the repository without the `.git` extension. The name is not case sensitive."},"milestone_number":{"type":"integer","description":"The number that identifies the milestone."},"body":{"type":"object","description":"Request body"}},"required":["owner","repo","milestone_number"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("PATCH", ("https://api.github.com/repos/" + String(args["owner"]) + "/" + String(args["repo"]) + "/milestones/" + String(args["milestone_number"])) + _qs, {}, args["body"]);
    }
  },
  {
    name: "issues/delete-milestone",
    description: "Delete a milestone. Deletes a milestone using the given milestone number. Params: owner (required), repo (required), milestone_number (required).",
    inputSchema: {"type":"object","properties":{"owner":{"type":"string","description":"The account owner of the repository. The name is not case sensitive."},"repo":{"type":"string","description":"The name of the repository without the `.git` extension. The name is not case sensitive."},"milestone_number":{"type":"integer","description":"The number that identifies the milestone."}},"required":["owner","repo","milestone_number"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("DELETE", ("https://api.github.com/repos/" + String(args["owner"]) + "/" + String(args["repo"]) + "/milestones/" + String(args["milestone_number"])) + _qs, {}, undefined);
    }
  },
  {
    name: "issues/list-labels-for-milestone",
    description: "List labels for issues in a milestone. Lists labels for issues in a milestone. Params: owner (required), repo (required), milestone_number (required).",
    inputSchema: {"type":"object","properties":{"owner":{"type":"string","description":"The account owner of the repository. The name is not case sensitive."},"repo":{"type":"string","description":"The name of the repository without the `.git` extension. The name is not case sensitive."},"milestone_number":{"type":"integer","description":"The number that identifies the milestone."},"per_page":{"type":"integer","default":30,"description":"The number of results per page (max 100). For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""},"page":{"type":"integer","default":1,"description":"The page number of the results to fetch. For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""}},"required":["owner","repo","milestone_number"]},
    call: function(args) {
    var _q = new URLSearchParams();
    if (args["per_page"] !== undefined) _q.set("per_page", String(args["per_page"]));
    if (args["page"] !== undefined) _q.set("page", String(args["page"]));
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/repos/" + String(args["owner"]) + "/" + String(args["repo"]) + "/milestones/" + String(args["milestone_number"]) + "/labels") + _qs, {}, undefined);
    }
  },
  {
    name: "activity/list-repo-notifications-for-authenticated-user",
    description: "List repository notifications for the authenticated user. Lists all notifications for the current user in the specified repository. Params: owner (required), repo (required).",
    inputSchema: {"type":"object","properties":{"owner":{"type":"string","description":"The account owner of the repository. The name is not case sensitive."},"repo":{"type":"string","description":"The name of the repository without the `.git` extension. The name is not case sensitive."},"all":{"type":"boolean","default":false,"description":"If `true`, show notifications marked as read."},"participating":{"type":"boolean","default":false,"description":"If `true`, only shows notifications in which the user is directly participating or mentioned."},"since":{"type":"string","format":"date-time","description":"Only show results that were last updated after the given time. This is a timestamp in [ISO 8601](https://en.wikipedia.org/wiki/ISO_8601) format: `YYYY-MM-DDTHH:MM:SSZ`."},"before":{"type":"string","format":"date-time","description":"Only show notifications updated before the given time. This is a timestamp in [ISO 8601](https://en.wikipedia.org/wiki/ISO_8601) format: `YYYY-MM-DDTHH:MM:SSZ`."},"per_page":{"type":"integer","default":30,"description":"The number of results per page (max 100). For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""},"page":{"type":"integer","default":1,"description":"The page number of the results to fetch. For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""}},"required":["owner","repo"]},
    call: function(args) {
    var _q = new URLSearchParams();
    if (args["all"] !== undefined) _q.set("all", String(args["all"]));
    if (args["participating"] !== undefined) _q.set("participating", String(args["participating"]));
    if (args["since"] !== undefined) _q.set("since", String(args["since"]));
    if (args["before"] !== undefined) _q.set("before", String(args["before"]));
    if (args["per_page"] !== undefined) _q.set("per_page", String(args["per_page"]));
    if (args["page"] !== undefined) _q.set("page", String(args["page"]));
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/repos/" + String(args["owner"]) + "/" + String(args["repo"]) + "/notifications") + _qs, {}, undefined);
    }
  },
  {
    name: "activity/mark-repo-notifications-as-read",
    description: "Mark repository notifications as read. Marks all notifications in a repository as \"read\" for the current user. Params: owner (required), repo (required).",
    inputSchema: {"type":"object","properties":{"owner":{"type":"string","description":"The account owner of the repository. The name is not case sensitive."},"repo":{"type":"string","description":"The name of the repository without the `.git` extension. The name is not case sensitive."},"body":{"type":"object","description":"Request body"}},"required":["owner","repo"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("PUT", ("https://api.github.com/repos/" + String(args["owner"]) + "/" + String(args["repo"]) + "/notifications") + _qs, {}, args["body"]);
    }
  },
  {
    name: "repos/get-pages",
    description: "Get a GitHub Pages site. Gets information about a GitHub Pages site. Params: owner (required), repo (required).",
    inputSchema: {"type":"object","properties":{"owner":{"type":"string","description":"The account owner of the repository. The name is not case sensitive."},"repo":{"type":"string","description":"The name of the repository without the `.git` extension. The name is not case sensitive."}},"required":["owner","repo"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/repos/" + String(args["owner"]) + "/" + String(args["repo"]) + "/pages") + _qs, {}, undefined);
    }
  },
  {
    name: "repos/create-pages-site",
    description: "Create a GitHub Pages site. Configures a GitHub Pages site. Params: owner (required), repo (required).",
    inputSchema: {"type":"object","properties":{"owner":{"type":"string","description":"The account owner of the repository. The name is not case sensitive."},"repo":{"type":"string","description":"The name of the repository without the `.git` extension. The name is not case sensitive."},"body":{"type":"object","description":"Request body"}},"required":["owner","repo","body"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("POST", ("https://api.github.com/repos/" + String(args["owner"]) + "/" + String(args["repo"]) + "/pages") + _qs, {}, args["body"]);
    }
  },
  {
    name: "repos/update-information-about-pages-site",
    description: "Update information about a GitHub Pages site. Updates information for a GitHub Pages site. Params: owner (required), repo (required).",
    inputSchema: {"type":"object","properties":{"owner":{"type":"string","description":"The account owner of the repository. The name is not case sensitive."},"repo":{"type":"string","description":"The name of the repository without the `.git` extension. The name is not case sensitive."},"body":{"type":"object","description":"Request body"}},"required":["owner","repo","body"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("PUT", ("https://api.github.com/repos/" + String(args["owner"]) + "/" + String(args["repo"]) + "/pages") + _qs, {}, args["body"]);
    }
  },
  {
    name: "repos/delete-pages-site",
    description: "Delete a GitHub Pages site. Deletes a GitHub Pages site. Params: owner (required), repo (required).",
    inputSchema: {"type":"object","properties":{"owner":{"type":"string","description":"The account owner of the repository. The name is not case sensitive."},"repo":{"type":"string","description":"The name of the repository without the `.git` extension. The name is not case sensitive."}},"required":["owner","repo"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("DELETE", ("https://api.github.com/repos/" + String(args["owner"]) + "/" + String(args["repo"]) + "/pages") + _qs, {}, undefined);
    }
  },
  {
    name: "repos/list-pages-builds",
    description: "List GitHub Pages builds. Lists builts of a GitHub Pages site. Params: owner (required), repo (required).",
    inputSchema: {"type":"object","properties":{"owner":{"type":"string","description":"The account owner of the repository. The name is not case sensitive."},"repo":{"type":"string","description":"The name of the repository without the `.git` extension. The name is not case sensitive."},"per_page":{"type":"integer","default":30,"description":"The number of results per page (max 100). For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""},"page":{"type":"integer","default":1,"description":"The page number of the results to fetch. For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""}},"required":["owner","repo"]},
    call: function(args) {
    var _q = new URLSearchParams();
    if (args["per_page"] !== undefined) _q.set("per_page", String(args["per_page"]));
    if (args["page"] !== undefined) _q.set("page", String(args["page"]));
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/repos/" + String(args["owner"]) + "/" + String(args["repo"]) + "/pages/builds") + _qs, {}, undefined);
    }
  },
  {
    name: "repos/request-pages-build",
    description: "Request a GitHub Pages build. You can request that your site be built from the latest revision on the default branch. Params: owner (required), repo (required).",
    inputSchema: {"type":"object","properties":{"owner":{"type":"string","description":"The account owner of the repository. The name is not case sensitive."},"repo":{"type":"string","description":"The name of the repository without the `.git` extension. The name is not case sensitive."}},"required":["owner","repo"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("POST", ("https://api.github.com/repos/" + String(args["owner"]) + "/" + String(args["repo"]) + "/pages/builds") + _qs, {}, undefined);
    }
  },
  {
    name: "repos/get-latest-pages-build",
    description: "Get latest Pages build. Gets information about the single most recent build of a GitHub Pages site. Params: owner (required), repo (required).",
    inputSchema: {"type":"object","properties":{"owner":{"type":"string","description":"The account owner of the repository. The name is not case sensitive."},"repo":{"type":"string","description":"The name of the repository without the `.git` extension. The name is not case sensitive."}},"required":["owner","repo"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/repos/" + String(args["owner"]) + "/" + String(args["repo"]) + "/pages/builds/latest") + _qs, {}, undefined);
    }
  },
  {
    name: "repos/get-pages-build",
    description: "Get GitHub Pages build. Gets information about a GitHub Pages build. Params: owner (required), repo (required), build_id (required).",
    inputSchema: {"type":"object","properties":{"owner":{"type":"string","description":"The account owner of the repository. The name is not case sensitive."},"repo":{"type":"string","description":"The name of the repository without the `.git` extension. The name is not case sensitive."},"build_id":{"type":"integer"}},"required":["owner","repo","build_id"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/repos/" + String(args["owner"]) + "/" + String(args["repo"]) + "/pages/builds/" + String(args["build_id"])) + _qs, {}, undefined);
    }
  },
  {
    name: "repos/create-pages-deployment",
    description: "Create a GitHub Pages deployment. Create a GitHub Pages deployment for a repository. Params: owner (required), repo (required).",
    inputSchema: {"type":"object","properties":{"owner":{"type":"string","description":"The account owner of the repository. The name is not case sensitive."},"repo":{"type":"string","description":"The name of the repository without the `.git` extension. The name is not case sensitive."},"body":{"type":"object","description":"Request body"}},"required":["owner","repo","body"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("POST", ("https://api.github.com/repos/" + String(args["owner"]) + "/" + String(args["repo"]) + "/pages/deployments") + _qs, {}, args["body"]);
    }
  },
  {
    name: "repos/get-pages-deployment",
    description: "Get the status of a GitHub Pages deployment. Gets the current status of a GitHub Pages deployment. Params: owner (required), repo (required), pages_deployment_id (required).",
    inputSchema: {"type":"object","properties":{"owner":{"type":"string","description":"The account owner of the repository. The name is not case sensitive."},"repo":{"type":"string","description":"The name of the repository without the `.git` extension. The name is not case sensitive."},"pages_deployment_id":{"oneOf":[{"type":"integer"},{"type":"string"}],"description":"The ID of the Pages deployment. You can also give the commit SHA of the deployment."}},"required":["owner","repo","pages_deployment_id"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/repos/" + String(args["owner"]) + "/" + String(args["repo"]) + "/pages/deployments/" + String(args["pages_deployment_id"])) + _qs, {}, undefined);
    }
  },
  {
    name: "repos/cancel-pages-deployment",
    description: "Cancel a GitHub Pages deployment. Cancels a GitHub Pages deployment. Params: owner (required), repo (required), pages_deployment_id (required).",
    inputSchema: {"type":"object","properties":{"owner":{"type":"string","description":"The account owner of the repository. The name is not case sensitive."},"repo":{"type":"string","description":"The name of the repository without the `.git` extension. The name is not case sensitive."},"pages_deployment_id":{"oneOf":[{"type":"integer"},{"type":"string"}],"description":"The ID of the Pages deployment. You can also give the commit SHA of the deployment."}},"required":["owner","repo","pages_deployment_id"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("POST", ("https://api.github.com/repos/" + String(args["owner"]) + "/" + String(args["repo"]) + "/pages/deployments/" + String(args["pages_deployment_id"]) + "/cancel") + _qs, {}, undefined);
    }
  },
  {
    name: "repos/get-pages-health-check",
    description: "Get a DNS health check for GitHub Pages. Gets a health check of the DNS settings for the `CNAME` record configured for a repository's GitHub Pages. Params: owner (required), repo (required).",
    inputSchema: {"type":"object","properties":{"owner":{"type":"string","description":"The account owner of the repository. The name is not case sensitive."},"repo":{"type":"string","description":"The name of the repository without the `.git` extension. The name is not case sensitive."}},"required":["owner","repo"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/repos/" + String(args["owner"]) + "/" + String(args["repo"]) + "/pages/health") + _qs, {}, undefined);
    }
  },
  {
    name: "repos/check-private-vulnerability-reporting",
    description: "Check if private vulnerability reporting is enabled for a repository. Returns a boolean indicating whether or not private vulnerability reporting is enabled for the repository. Params: owner (required), repo (required).",
    inputSchema: {"type":"object","properties":{"owner":{"type":"string","description":"The account owner of the repository. The name is not case sensitive."},"repo":{"type":"string","description":"The name of the repository without the `.git` extension. The name is not case sensitive."}},"required":["owner","repo"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/repos/" + String(args["owner"]) + "/" + String(args["repo"]) + "/private-vulnerability-reporting") + _qs, {}, undefined);
    }
  },
  {
    name: "repos/enable-private-vulnerability-reporting",
    description: "Enable private vulnerability reporting for a repository. Enables private vulnerability reporting for a repository. Params: owner (required), repo (required).",
    inputSchema: {"type":"object","properties":{"owner":{"type":"string","description":"The account owner of the repository. The name is not case sensitive."},"repo":{"type":"string","description":"The name of the repository without the `.git` extension. The name is not case sensitive."}},"required":["owner","repo"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("PUT", ("https://api.github.com/repos/" + String(args["owner"]) + "/" + String(args["repo"]) + "/private-vulnerability-reporting") + _qs, {}, undefined);
    }
  },
  {
    name: "repos/disable-private-vulnerability-reporting",
    description: "Disable private vulnerability reporting for a repository. Disables private vulnerability reporting for a repository. Params: owner (required), repo (required).",
    inputSchema: {"type":"object","properties":{"owner":{"type":"string","description":"The account owner of the repository. The name is not case sensitive."},"repo":{"type":"string","description":"The name of the repository without the `.git` extension. The name is not case sensitive."}},"required":["owner","repo"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("DELETE", ("https://api.github.com/repos/" + String(args["owner"]) + "/" + String(args["repo"]) + "/private-vulnerability-reporting") + _qs, {}, undefined);
    }
  },
  {
    name: "repos/custom-properties-for-repos-get-repository-values",
    description: "Get all custom property values for a repository. Gets all custom property values that are set for a repository. Params: owner (required), repo (required).",
    inputSchema: {"type":"object","properties":{"owner":{"type":"string","description":"The account owner of the repository. The name is not case sensitive."},"repo":{"type":"string","description":"The name of the repository without the `.git` extension. The name is not case sensitive."}},"required":["owner","repo"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/repos/" + String(args["owner"]) + "/" + String(args["repo"]) + "/properties/values") + _qs, {}, undefined);
    }
  },
  {
    name: "repos/custom-properties-for-repos-create-or-update-repository-values",
    description: "Create or update custom property values for a repository. Create new or update existing custom property values for a repository. Params: owner (required), repo (required).",
    inputSchema: {"type":"object","properties":{"owner":{"type":"string","description":"The account owner of the repository. The name is not case sensitive."},"repo":{"type":"string","description":"The name of the repository without the `.git` extension. The name is not case sensitive."},"body":{"type":"object","description":"Request body"}},"required":["owner","repo","body"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("PATCH", ("https://api.github.com/repos/" + String(args["owner"]) + "/" + String(args["repo"]) + "/properties/values") + _qs, {}, args["body"]);
    }
  },
  {
    name: "pulls/list",
    description: "List pull requests. Lists pull requests in a specified repository. Params: owner (required), repo (required).",
    inputSchema: {"type":"object","properties":{"owner":{"type":"string","description":"The account owner of the repository. The name is not case sensitive."},"repo":{"type":"string","description":"The name of the repository without the `.git` extension. The name is not case sensitive."},"state":{"type":"string","enum":["open","closed","all"],"default":"open","description":"Either `open`, `closed`, or `all` to filter by state."},"head":{"type":"string","description":"Filter pulls by head user or head organization and branch name in the format of `user:ref-name` or `organization:ref-name`. For example: `github:new-script-format` or `octocat:test-branch`."},"base":{"type":"string","description":"Filter pulls by base branch name. Example: `gh-pages`."},"sort":{"type":"string","enum":["created","updated","popularity","long-running"],"default":"created","description":"What to sort results by. `popularity` will sort by the number of comments. `long-running` will sort by date created and will limit the results to pull requests that have been open for more than a month and have had activity within the past month."},"direction":{"type":"string","enum":["asc","desc"],"description":"The direction of the sort. Default: `desc` when sort is `created` or sort is not specified, otherwise `asc`."},"per_page":{"type":"integer","default":30,"description":"The number of results per page (max 100). For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""},"page":{"type":"integer","default":1,"description":"The page number of the results to fetch. For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""}},"required":["owner","repo"]},
    call: function(args) {
    var _q = new URLSearchParams();
    if (args["state"] !== undefined) _q.set("state", String(args["state"]));
    if (args["head"] !== undefined) _q.set("head", String(args["head"]));
    if (args["base"] !== undefined) _q.set("base", String(args["base"]));
    if (args["sort"] !== undefined) _q.set("sort", String(args["sort"]));
    if (args["direction"] !== undefined) _q.set("direction", String(args["direction"]));
    if (args["per_page"] !== undefined) _q.set("per_page", String(args["per_page"]));
    if (args["page"] !== undefined) _q.set("page", String(args["page"]));
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/repos/" + String(args["owner"]) + "/" + String(args["repo"]) + "/pulls") + _qs, {}, undefined);
    }
  },
  {
    name: "pulls/create",
    description: "Create a pull request. Draft pull requests are available in public repositories with GitHub Free and GitHub Free for organizations, GitHub Pro, and legacy per-repository billing plans, and in public and private repositories with GitHub Team and GitHub Enterprise Cloud. Params: owner (required), repo (required).",
    inputSchema: {"type":"object","properties":{"owner":{"type":"string","description":"The account owner of the repository. The name is not case sensitive."},"repo":{"type":"string","description":"The name of the repository without the `.git` extension. The name is not case sensitive."},"body":{"type":"object","description":"Request body"}},"required":["owner","repo","body"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("POST", ("https://api.github.com/repos/" + String(args["owner"]) + "/" + String(args["repo"]) + "/pulls") + _qs, {}, args["body"]);
    }
  },
  {
    name: "pulls/list-review-comments-for-repo",
    description: "List review comments in a repository. Lists review comments for all pull requests in a repository. Params: owner (required), repo (required).",
    inputSchema: {"type":"object","properties":{"owner":{"type":"string","description":"The account owner of the repository. The name is not case sensitive."},"repo":{"type":"string","description":"The name of the repository without the `.git` extension. The name is not case sensitive."},"sort":{"type":"string","enum":["created","updated","created_at"]},"direction":{"type":"string","enum":["asc","desc"],"description":"The direction to sort results. Ignored without `sort` parameter."},"since":{"type":"string","format":"date-time","description":"Only show results that were last updated after the given time. This is a timestamp in [ISO 8601](https://en.wikipedia.org/wiki/ISO_8601) format: `YYYY-MM-DDTHH:MM:SSZ`."},"per_page":{"type":"integer","default":30,"description":"The number of results per page (max 100). For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""},"page":{"type":"integer","default":1,"description":"The page number of the results to fetch. For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""}},"required":["owner","repo"]},
    call: function(args) {
    var _q = new URLSearchParams();
    if (args["sort"] !== undefined) _q.set("sort", String(args["sort"]));
    if (args["direction"] !== undefined) _q.set("direction", String(args["direction"]));
    if (args["since"] !== undefined) _q.set("since", String(args["since"]));
    if (args["per_page"] !== undefined) _q.set("per_page", String(args["per_page"]));
    if (args["page"] !== undefined) _q.set("page", String(args["page"]));
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/repos/" + String(args["owner"]) + "/" + String(args["repo"]) + "/pulls/comments") + _qs, {}, undefined);
    }
  },
  {
    name: "pulls/get-review-comment",
    description: "Get a review comment for a pull request. Provides details for a specified review comment. Params: owner (required), repo (required), comment_id (required).",
    inputSchema: {"type":"object","properties":{"owner":{"type":"string","description":"The account owner of the repository. The name is not case sensitive."},"repo":{"type":"string","description":"The name of the repository without the `.git` extension. The name is not case sensitive."},"comment_id":{"type":"integer","format":"int64","description":"The unique identifier of the comment."}},"required":["owner","repo","comment_id"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/repos/" + String(args["owner"]) + "/" + String(args["repo"]) + "/pulls/comments/" + String(args["comment_id"])) + _qs, {}, undefined);
    }
  },
  {
    name: "pulls/update-review-comment",
    description: "Update a review comment for a pull request. Edits the content of a specified review comment. Params: owner (required), repo (required), comment_id (required).",
    inputSchema: {"type":"object","properties":{"owner":{"type":"string","description":"The account owner of the repository. The name is not case sensitive."},"repo":{"type":"string","description":"The name of the repository without the `.git` extension. The name is not case sensitive."},"comment_id":{"type":"integer","format":"int64","description":"The unique identifier of the comment."},"body":{"type":"object","description":"Request body"}},"required":["owner","repo","comment_id","body"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("PATCH", ("https://api.github.com/repos/" + String(args["owner"]) + "/" + String(args["repo"]) + "/pulls/comments/" + String(args["comment_id"])) + _qs, {}, args["body"]);
    }
  },
  {
    name: "pulls/delete-review-comment",
    description: "Delete a review comment for a pull request. Deletes a review comment. Params: owner (required), repo (required), comment_id (required).",
    inputSchema: {"type":"object","properties":{"owner":{"type":"string","description":"The account owner of the repository. The name is not case sensitive."},"repo":{"type":"string","description":"The name of the repository without the `.git` extension. The name is not case sensitive."},"comment_id":{"type":"integer","format":"int64","description":"The unique identifier of the comment."}},"required":["owner","repo","comment_id"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("DELETE", ("https://api.github.com/repos/" + String(args["owner"]) + "/" + String(args["repo"]) + "/pulls/comments/" + String(args["comment_id"])) + _qs, {}, undefined);
    }
  },
  {
    name: "reactions/list-for-pull-request-review-comment",
    description: "List reactions for a pull request review comment. List the reactions to a [pull request review comment](https://docs.github.com/rest/pulls/comments#get-a-review-comment-for-a-pull-request). Params: owner (required), repo (required), comment_id (required).",
    inputSchema: {"type":"object","properties":{"owner":{"type":"string","description":"The account owner of the repository. The name is not case sensitive."},"repo":{"type":"string","description":"The name of the repository without the `.git` extension. The name is not case sensitive."},"comment_id":{"type":"integer","format":"int64","description":"The unique identifier of the comment."},"content":{"type":"string","enum":["+1","-1","laugh","confused","heart","hooray","rocket","eyes"],"description":"Returns a single [reaction type](https://docs.github.com/rest/reactions/reactions#about-reactions). Omit this parameter to list all reactions to a pull request review comment."},"per_page":{"type":"integer","default":30,"description":"The number of results per page (max 100). For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""},"page":{"type":"integer","default":1,"description":"The page number of the results to fetch. For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""}},"required":["owner","repo","comment_id"]},
    call: function(args) {
    var _q = new URLSearchParams();
    if (args["content"] !== undefined) _q.set("content", String(args["content"]));
    if (args["per_page"] !== undefined) _q.set("per_page", String(args["per_page"]));
    if (args["page"] !== undefined) _q.set("page", String(args["page"]));
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/repos/" + String(args["owner"]) + "/" + String(args["repo"]) + "/pulls/comments/" + String(args["comment_id"]) + "/reactions") + _qs, {}, undefined);
    }
  },
  {
    name: "reactions/create-for-pull-request-review-comment",
    description: "Create reaction for a pull request review comment. Create a reaction to a [pull request review comment](https://docs.github.com/rest/pulls/comments#get-a-review-comment-for-a-pull-request). Params: owner (required), repo (required), comment_id (required).",
    inputSchema: {"type":"object","properties":{"owner":{"type":"string","description":"The account owner of the repository. The name is not case sensitive."},"repo":{"type":"string","description":"The name of the repository without the `.git` extension. The name is not case sensitive."},"comment_id":{"type":"integer","format":"int64","description":"The unique identifier of the comment."},"body":{"type":"object","description":"Request body"}},"required":["owner","repo","comment_id","body"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("POST", ("https://api.github.com/repos/" + String(args["owner"]) + "/" + String(args["repo"]) + "/pulls/comments/" + String(args["comment_id"]) + "/reactions") + _qs, {}, args["body"]);
    }
  },
  {
    name: "reactions/delete-for-pull-request-comment",
    description: "Delete a pull request comment reaction. > [!NOTE]\n> You can also specify a repository by `repository_id` using the route `DELETE /repositories/:repository_id/pulls/comments/:comment_id/reactions/:reaction_id.`\n\nDelete a reaction to a [pull request review comment](https://docs.github.com/rest/pulls/comments#get-a-review-comment-for-a-pull-request). Params: owner (required), repo (required), comment_id (required), reaction_id (required).",
    inputSchema: {"type":"object","properties":{"owner":{"type":"string","description":"The account owner of the repository. The name is not case sensitive."},"repo":{"type":"string","description":"The name of the repository without the `.git` extension. The name is not case sensitive."},"comment_id":{"type":"integer","format":"int64","description":"The unique identifier of the comment."},"reaction_id":{"type":"integer","description":"The unique identifier of the reaction."}},"required":["owner","repo","comment_id","reaction_id"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("DELETE", ("https://api.github.com/repos/" + String(args["owner"]) + "/" + String(args["repo"]) + "/pulls/comments/" + String(args["comment_id"]) + "/reactions/" + String(args["reaction_id"])) + _qs, {}, undefined);
    }
  },
  {
    name: "pulls/get",
    description: "Get a pull request. Draft pull requests are available in public repositories with GitHub Free and GitHub Free for organizations, GitHub Pro, and legacy per-repository billing plans, and in public and private repositories with GitHub Team and GitHub Enterprise Cloud. Params: owner (required), repo (required), pull_number (required).",
    inputSchema: {"type":"object","properties":{"owner":{"type":"string","description":"The account owner of the repository. The name is not case sensitive."},"repo":{"type":"string","description":"The name of the repository without the `.git` extension. The name is not case sensitive."},"pull_number":{"type":"integer","description":"The number that identifies the pull request."}},"required":["owner","repo","pull_number"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/repos/" + String(args["owner"]) + "/" + String(args["repo"]) + "/pulls/" + String(args["pull_number"])) + _qs, {}, undefined);
    }
  },
  {
    name: "pulls/update",
    description: "Update a pull request. Draft pull requests are available in public repositories with GitHub Free and GitHub Free for organizations, GitHub Pro, and legacy per-repository billing plans, and in public and private repositories with GitHub Team and GitHub Enterprise Cloud. Params: owner (required), repo (required), pull_number (required).",
    inputSchema: {"type":"object","properties":{"owner":{"type":"string","description":"The account owner of the repository. The name is not case sensitive."},"repo":{"type":"string","description":"The name of the repository without the `.git` extension. The name is not case sensitive."},"pull_number":{"type":"integer","description":"The number that identifies the pull request."},"body":{"type":"object","description":"Request body"}},"required":["owner","repo","pull_number"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("PATCH", ("https://api.github.com/repos/" + String(args["owner"]) + "/" + String(args["repo"]) + "/pulls/" + String(args["pull_number"])) + _qs, {}, args["body"]);
    }
  },
  {
    name: "codespaces/create-with-pr-for-authenticated-user",
    description: "Create a codespace from a pull request. Creates a codespace owned by the authenticated user for the specified pull request. Params: owner (required), repo (required), pull_number (required).",
    inputSchema: {"type":"object","properties":{"owner":{"type":"string","description":"The account owner of the repository. The name is not case sensitive."},"repo":{"type":"string","description":"The name of the repository without the `.git` extension. The name is not case sensitive."},"pull_number":{"type":"integer","description":"The number that identifies the pull request."},"body":{"type":"object","description":"Request body"}},"required":["owner","repo","pull_number","body"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("POST", ("https://api.github.com/repos/" + String(args["owner"]) + "/" + String(args["repo"]) + "/pulls/" + String(args["pull_number"]) + "/codespaces") + _qs, {}, args["body"]);
    }
  },
  {
    name: "pulls/list-review-comments",
    description: "List review comments on a pull request. Lists all review comments for a specified pull request. Params: owner (required), repo (required), pull_number (required).",
    inputSchema: {"type":"object","properties":{"owner":{"type":"string","description":"The account owner of the repository. The name is not case sensitive."},"repo":{"type":"string","description":"The name of the repository without the `.git` extension. The name is not case sensitive."},"pull_number":{"type":"integer","description":"The number that identifies the pull request."},"sort":{"type":"string","enum":["created","updated"],"default":"created","description":"The property to sort the results by."},"direction":{"type":"string","enum":["asc","desc"],"description":"The direction to sort results. Ignored without `sort` parameter."},"since":{"type":"string","format":"date-time","description":"Only show results that were last updated after the given time. This is a timestamp in [ISO 8601](https://en.wikipedia.org/wiki/ISO_8601) format: `YYYY-MM-DDTHH:MM:SSZ`."},"per_page":{"type":"integer","default":30,"description":"The number of results per page (max 100). For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""},"page":{"type":"integer","default":1,"description":"The page number of the results to fetch. For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""}},"required":["owner","repo","pull_number"]},
    call: function(args) {
    var _q = new URLSearchParams();
    if (args["sort"] !== undefined) _q.set("sort", String(args["sort"]));
    if (args["direction"] !== undefined) _q.set("direction", String(args["direction"]));
    if (args["since"] !== undefined) _q.set("since", String(args["since"]));
    if (args["per_page"] !== undefined) _q.set("per_page", String(args["per_page"]));
    if (args["page"] !== undefined) _q.set("page", String(args["page"]));
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/repos/" + String(args["owner"]) + "/" + String(args["repo"]) + "/pulls/" + String(args["pull_number"]) + "/comments") + _qs, {}, undefined);
    }
  },
  {
    name: "pulls/create-review-comment",
    description: "Create a review comment for a pull request. Creates a review comment on the diff of a specified pull request. Params: owner (required), repo (required), pull_number (required).",
    inputSchema: {"type":"object","properties":{"owner":{"type":"string","description":"The account owner of the repository. The name is not case sensitive."},"repo":{"type":"string","description":"The name of the repository without the `.git` extension. The name is not case sensitive."},"pull_number":{"type":"integer","description":"The number that identifies the pull request."},"body":{"type":"object","description":"Request body"}},"required":["owner","repo","pull_number","body"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("POST", ("https://api.github.com/repos/" + String(args["owner"]) + "/" + String(args["repo"]) + "/pulls/" + String(args["pull_number"]) + "/comments") + _qs, {}, args["body"]);
    }
  },
  {
    name: "pulls/create-reply-for-review-comment",
    description: "Create a reply for a review comment. Creates a reply to a review comment for a pull request. Params: owner (required), repo (required), pull_number (required), comment_id (required).",
    inputSchema: {"type":"object","properties":{"owner":{"type":"string","description":"The account owner of the repository. The name is not case sensitive."},"repo":{"type":"string","description":"The name of the repository without the `.git` extension. The name is not case sensitive."},"pull_number":{"type":"integer","description":"The number that identifies the pull request."},"comment_id":{"type":"integer","format":"int64","description":"The unique identifier of the comment."},"body":{"type":"object","description":"Request body"}},"required":["owner","repo","pull_number","comment_id","body"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("POST", ("https://api.github.com/repos/" + String(args["owner"]) + "/" + String(args["repo"]) + "/pulls/" + String(args["pull_number"]) + "/comments/" + String(args["comment_id"]) + "/replies") + _qs, {}, args["body"]);
    }
  },
  {
    name: "pulls/list-commits",
    description: "List commits on a pull request. Lists a maximum of 250 commits for a pull request. Params: owner (required), repo (required), pull_number (required).",
    inputSchema: {"type":"object","properties":{"owner":{"type":"string","description":"The account owner of the repository. The name is not case sensitive."},"repo":{"type":"string","description":"The name of the repository without the `.git` extension. The name is not case sensitive."},"pull_number":{"type":"integer","description":"The number that identifies the pull request."},"per_page":{"type":"integer","default":30,"description":"The number of results per page (max 100). For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""},"page":{"type":"integer","default":1,"description":"The page number of the results to fetch. For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""}},"required":["owner","repo","pull_number"]},
    call: function(args) {
    var _q = new URLSearchParams();
    if (args["per_page"] !== undefined) _q.set("per_page", String(args["per_page"]));
    if (args["page"] !== undefined) _q.set("page", String(args["page"]));
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/repos/" + String(args["owner"]) + "/" + String(args["repo"]) + "/pulls/" + String(args["pull_number"]) + "/commits") + _qs, {}, undefined);
    }
  },
  {
    name: "pulls/list-files",
    description: "List pull requests files. Lists the files in a specified pull request. Params: owner (required), repo (required), pull_number (required).",
    inputSchema: {"type":"object","properties":{"owner":{"type":"string","description":"The account owner of the repository. The name is not case sensitive."},"repo":{"type":"string","description":"The name of the repository without the `.git` extension. The name is not case sensitive."},"pull_number":{"type":"integer","description":"The number that identifies the pull request."},"per_page":{"type":"integer","default":30,"description":"The number of results per page (max 100). For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""},"page":{"type":"integer","default":1,"description":"The page number of the results to fetch. For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""}},"required":["owner","repo","pull_number"]},
    call: function(args) {
    var _q = new URLSearchParams();
    if (args["per_page"] !== undefined) _q.set("per_page", String(args["per_page"]));
    if (args["page"] !== undefined) _q.set("page", String(args["page"]));
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/repos/" + String(args["owner"]) + "/" + String(args["repo"]) + "/pulls/" + String(args["pull_number"]) + "/files") + _qs, {}, undefined);
    }
  },
  {
    name: "pulls/check-if-merged",
    description: "Check if a pull request has been merged. Checks if a pull request has been merged into the base branch. Params: owner (required), repo (required), pull_number (required).",
    inputSchema: {"type":"object","properties":{"owner":{"type":"string","description":"The account owner of the repository. The name is not case sensitive."},"repo":{"type":"string","description":"The name of the repository without the `.git` extension. The name is not case sensitive."},"pull_number":{"type":"integer","description":"The number that identifies the pull request."}},"required":["owner","repo","pull_number"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/repos/" + String(args["owner"]) + "/" + String(args["repo"]) + "/pulls/" + String(args["pull_number"]) + "/merge") + _qs, {}, undefined);
    }
  },
  {
    name: "pulls/merge",
    description: "Merge a pull request. Merges a pull request into the base branch. Params: owner (required), repo (required), pull_number (required).",
    inputSchema: {"type":"object","properties":{"owner":{"type":"string","description":"The account owner of the repository. The name is not case sensitive."},"repo":{"type":"string","description":"The name of the repository without the `.git` extension. The name is not case sensitive."},"pull_number":{"type":"integer","description":"The number that identifies the pull request."},"body":{"type":"object","description":"Request body"}},"required":["owner","repo","pull_number"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("PUT", ("https://api.github.com/repos/" + String(args["owner"]) + "/" + String(args["repo"]) + "/pulls/" + String(args["pull_number"]) + "/merge") + _qs, {}, args["body"]);
    }
  },
  {
    name: "pulls/list-requested-reviewers",
    description: "Get all requested reviewers for a pull request. Gets the users or teams whose review is requested for a pull request. Params: owner (required), repo (required), pull_number (required).",
    inputSchema: {"type":"object","properties":{"owner":{"type":"string","description":"The account owner of the repository. The name is not case sensitive."},"repo":{"type":"string","description":"The name of the repository without the `.git` extension. The name is not case sensitive."},"pull_number":{"type":"integer","description":"The number that identifies the pull request."}},"required":["owner","repo","pull_number"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/repos/" + String(args["owner"]) + "/" + String(args["repo"]) + "/pulls/" + String(args["pull_number"]) + "/requested_reviewers") + _qs, {}, undefined);
    }
  },
  {
    name: "pulls/request-reviewers",
    description: "Request reviewers for a pull request. Requests reviews for a pull request from a given set of users and/or teams. Params: owner (required), repo (required), pull_number (required).",
    inputSchema: {"type":"object","properties":{"owner":{"type":"string","description":"The account owner of the repository. The name is not case sensitive."},"repo":{"type":"string","description":"The name of the repository without the `.git` extension. The name is not case sensitive."},"pull_number":{"type":"integer","description":"The number that identifies the pull request."},"body":{"type":"object","description":"Request body"}},"required":["owner","repo","pull_number"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("POST", ("https://api.github.com/repos/" + String(args["owner"]) + "/" + String(args["repo"]) + "/pulls/" + String(args["pull_number"]) + "/requested_reviewers") + _qs, {}, args["body"]);
    }
  },
  {
    name: "pulls/remove-requested-reviewers",
    description: "Remove requested reviewers from a pull request. Removes review requests from a pull request for a given set of users and/or teams. Params: owner (required), repo (required), pull_number (required).",
    inputSchema: {"type":"object","properties":{"owner":{"type":"string","description":"The account owner of the repository. The name is not case sensitive."},"repo":{"type":"string","description":"The name of the repository without the `.git` extension. The name is not case sensitive."},"pull_number":{"type":"integer","description":"The number that identifies the pull request."},"body":{"type":"object","description":"Request body"}},"required":["owner","repo","pull_number","body"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("DELETE", ("https://api.github.com/repos/" + String(args["owner"]) + "/" + String(args["repo"]) + "/pulls/" + String(args["pull_number"]) + "/requested_reviewers") + _qs, {}, args["body"]);
    }
  },
  {
    name: "pulls/list-reviews",
    description: "List reviews for a pull request. Lists all reviews for a specified pull request. Params: owner (required), repo (required), pull_number (required).",
    inputSchema: {"type":"object","properties":{"owner":{"type":"string","description":"The account owner of the repository. The name is not case sensitive."},"repo":{"type":"string","description":"The name of the repository without the `.git` extension. The name is not case sensitive."},"pull_number":{"type":"integer","description":"The number that identifies the pull request."},"per_page":{"type":"integer","default":30,"description":"The number of results per page (max 100). For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""},"page":{"type":"integer","default":1,"description":"The page number of the results to fetch. For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""}},"required":["owner","repo","pull_number"]},
    call: function(args) {
    var _q = new URLSearchParams();
    if (args["per_page"] !== undefined) _q.set("per_page", String(args["per_page"]));
    if (args["page"] !== undefined) _q.set("page", String(args["page"]));
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/repos/" + String(args["owner"]) + "/" + String(args["repo"]) + "/pulls/" + String(args["pull_number"]) + "/reviews") + _qs, {}, undefined);
    }
  },
  {
    name: "pulls/create-review",
    description: "Create a review for a pull request. Creates a review on a specified pull request. Params: owner (required), repo (required), pull_number (required).",
    inputSchema: {"type":"object","properties":{"owner":{"type":"string","description":"The account owner of the repository. The name is not case sensitive."},"repo":{"type":"string","description":"The name of the repository without the `.git` extension. The name is not case sensitive."},"pull_number":{"type":"integer","description":"The number that identifies the pull request."},"body":{"type":"object","description":"Request body"}},"required":["owner","repo","pull_number"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("POST", ("https://api.github.com/repos/" + String(args["owner"]) + "/" + String(args["repo"]) + "/pulls/" + String(args["pull_number"]) + "/reviews") + _qs, {}, args["body"]);
    }
  },
  {
    name: "pulls/get-review",
    description: "Get a review for a pull request. Retrieves a pull request review by its ID. Params: owner (required), repo (required), pull_number (required), review_id (required).",
    inputSchema: {"type":"object","properties":{"owner":{"type":"string","description":"The account owner of the repository. The name is not case sensitive."},"repo":{"type":"string","description":"The name of the repository without the `.git` extension. The name is not case sensitive."},"pull_number":{"type":"integer","description":"The number that identifies the pull request."},"review_id":{"type":"integer","description":"The unique identifier of the review."}},"required":["owner","repo","pull_number","review_id"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/repos/" + String(args["owner"]) + "/" + String(args["repo"]) + "/pulls/" + String(args["pull_number"]) + "/reviews/" + String(args["review_id"])) + _qs, {}, undefined);
    }
  },
  {
    name: "pulls/update-review",
    description: "Update a review for a pull request. Updates the contents of a specified review summary comment. Params: owner (required), repo (required), pull_number (required), review_id (required).",
    inputSchema: {"type":"object","properties":{"owner":{"type":"string","description":"The account owner of the repository. The name is not case sensitive."},"repo":{"type":"string","description":"The name of the repository without the `.git` extension. The name is not case sensitive."},"pull_number":{"type":"integer","description":"The number that identifies the pull request."},"review_id":{"type":"integer","description":"The unique identifier of the review."},"body":{"type":"object","description":"Request body"}},"required":["owner","repo","pull_number","review_id","body"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("PUT", ("https://api.github.com/repos/" + String(args["owner"]) + "/" + String(args["repo"]) + "/pulls/" + String(args["pull_number"]) + "/reviews/" + String(args["review_id"])) + _qs, {}, args["body"]);
    }
  },
  {
    name: "pulls/delete-pending-review",
    description: "Delete a pending review for a pull request. Deletes a pull request review that has not been submitted. Params: owner (required), repo (required), pull_number (required), review_id (required).",
    inputSchema: {"type":"object","properties":{"owner":{"type":"string","description":"The account owner of the repository. The name is not case sensitive."},"repo":{"type":"string","description":"The name of the repository without the `.git` extension. The name is not case sensitive."},"pull_number":{"type":"integer","description":"The number that identifies the pull request."},"review_id":{"type":"integer","description":"The unique identifier of the review."}},"required":["owner","repo","pull_number","review_id"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("DELETE", ("https://api.github.com/repos/" + String(args["owner"]) + "/" + String(args["repo"]) + "/pulls/" + String(args["pull_number"]) + "/reviews/" + String(args["review_id"])) + _qs, {}, undefined);
    }
  },
  {
    name: "pulls/list-comments-for-review",
    description: "List comments for a pull request review. Lists comments for a specific pull request review. Params: owner (required), repo (required), pull_number (required), review_id (required).",
    inputSchema: {"type":"object","properties":{"owner":{"type":"string","description":"The account owner of the repository. The name is not case sensitive."},"repo":{"type":"string","description":"The name of the repository without the `.git` extension. The name is not case sensitive."},"pull_number":{"type":"integer","description":"The number that identifies the pull request."},"review_id":{"type":"integer","description":"The unique identifier of the review."},"per_page":{"type":"integer","default":30,"description":"The number of results per page (max 100). For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""},"page":{"type":"integer","default":1,"description":"The page number of the results to fetch. For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""}},"required":["owner","repo","pull_number","review_id"]},
    call: function(args) {
    var _q = new URLSearchParams();
    if (args["per_page"] !== undefined) _q.set("per_page", String(args["per_page"]));
    if (args["page"] !== undefined) _q.set("page", String(args["page"]));
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/repos/" + String(args["owner"]) + "/" + String(args["repo"]) + "/pulls/" + String(args["pull_number"]) + "/reviews/" + String(args["review_id"]) + "/comments") + _qs, {}, undefined);
    }
  },
  {
    name: "pulls/dismiss-review",
    description: "Dismiss a review for a pull request. Dismisses a specified review on a pull request. Params: owner (required), repo (required), pull_number (required), review_id (required).",
    inputSchema: {"type":"object","properties":{"owner":{"type":"string","description":"The account owner of the repository. The name is not case sensitive."},"repo":{"type":"string","description":"The name of the repository without the `.git` extension. The name is not case sensitive."},"pull_number":{"type":"integer","description":"The number that identifies the pull request."},"review_id":{"type":"integer","description":"The unique identifier of the review."},"body":{"type":"object","description":"Request body"}},"required":["owner","repo","pull_number","review_id","body"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("PUT", ("https://api.github.com/repos/" + String(args["owner"]) + "/" + String(args["repo"]) + "/pulls/" + String(args["pull_number"]) + "/reviews/" + String(args["review_id"]) + "/dismissals") + _qs, {}, args["body"]);
    }
  },
  {
    name: "pulls/submit-review",
    description: "Submit a review for a pull request. Submits a pending review for a pull request. Params: owner (required), repo (required), pull_number (required), review_id (required).",
    inputSchema: {"type":"object","properties":{"owner":{"type":"string","description":"The account owner of the repository. The name is not case sensitive."},"repo":{"type":"string","description":"The name of the repository without the `.git` extension. The name is not case sensitive."},"pull_number":{"type":"integer","description":"The number that identifies the pull request."},"review_id":{"type":"integer","description":"The unique identifier of the review."},"body":{"type":"object","description":"Request body"}},"required":["owner","repo","pull_number","review_id","body"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("POST", ("https://api.github.com/repos/" + String(args["owner"]) + "/" + String(args["repo"]) + "/pulls/" + String(args["pull_number"]) + "/reviews/" + String(args["review_id"]) + "/events") + _qs, {}, args["body"]);
    }
  },
  {
    name: "pulls/update-branch",
    description: "Update a pull request branch. Updates the pull request branch with the latest upstream changes by merging HEAD from the base branch into the pull request branch. Params: owner (required), repo (required), pull_number (required).",
    inputSchema: {"type":"object","properties":{"owner":{"type":"string","description":"The account owner of the repository. The name is not case sensitive."},"repo":{"type":"string","description":"The name of the repository without the `.git` extension. The name is not case sensitive."},"pull_number":{"type":"integer","description":"The number that identifies the pull request."},"body":{"type":"object","description":"Request body"}},"required":["owner","repo","pull_number"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("PUT", ("https://api.github.com/repos/" + String(args["owner"]) + "/" + String(args["repo"]) + "/pulls/" + String(args["pull_number"]) + "/update-branch") + _qs, {}, args["body"]);
    }
  },
  {
    name: "repos/get-readme",
    description: "Get a repository README. Gets the preferred README for a repository. Params: owner (required), repo (required).",
    inputSchema: {"type":"object","properties":{"owner":{"type":"string","description":"The account owner of the repository. The name is not case sensitive."},"repo":{"type":"string","description":"The name of the repository without the `.git` extension. The name is not case sensitive."},"ref":{"type":"string","description":"The name of the commit/branch/tag. Default: the repository’s default branch."}},"required":["owner","repo"]},
    call: function(args) {
    var _q = new URLSearchParams();
    if (args["ref"] !== undefined) _q.set("ref", String(args["ref"]));
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/repos/" + String(args["owner"]) + "/" + String(args["repo"]) + "/readme") + _qs, {}, undefined);
    }
  },
  {
    name: "repos/get-readme-in-directory",
    description: "Get a repository README for a directory. Gets the README from a repository directory. Params: owner (required), repo (required), dir (required).",
    inputSchema: {"type":"object","properties":{"owner":{"type":"string","description":"The account owner of the repository. The name is not case sensitive."},"repo":{"type":"string","description":"The name of the repository without the `.git` extension. The name is not case sensitive."},"dir":{"type":"string","description":"The alternate path to look for a README file"},"ref":{"type":"string","description":"The name of the commit/branch/tag. Default: the repository’s default branch."}},"required":["owner","repo","dir"]},
    call: function(args) {
    var _q = new URLSearchParams();
    if (args["ref"] !== undefined) _q.set("ref", String(args["ref"]));
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/repos/" + String(args["owner"]) + "/" + String(args["repo"]) + "/readme/" + String(args["dir"])) + _qs, {}, undefined);
    }
  },
  {
    name: "repos/list-releases",
    description: "List releases. This returns a list of releases, which does not include regular Git tags that have not been associated with a release. Params: owner (required), repo (required).",
    inputSchema: {"type":"object","properties":{"owner":{"type":"string","description":"The account owner of the repository. The name is not case sensitive."},"repo":{"type":"string","description":"The name of the repository without the `.git` extension. The name is not case sensitive."},"per_page":{"type":"integer","default":30,"description":"The number of results per page (max 100). For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""},"page":{"type":"integer","default":1,"description":"The page number of the results to fetch. For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""}},"required":["owner","repo"]},
    call: function(args) {
    var _q = new URLSearchParams();
    if (args["per_page"] !== undefined) _q.set("per_page", String(args["per_page"]));
    if (args["page"] !== undefined) _q.set("page", String(args["page"]));
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/repos/" + String(args["owner"]) + "/" + String(args["repo"]) + "/releases") + _qs, {}, undefined);
    }
  },
  {
    name: "repos/create-release",
    description: "Create a release. Users with push access to the repository can create a release. Params: owner (required), repo (required).",
    inputSchema: {"type":"object","properties":{"owner":{"type":"string","description":"The account owner of the repository. The name is not case sensitive."},"repo":{"type":"string","description":"The name of the repository without the `.git` extension. The name is not case sensitive."},"body":{"type":"object","description":"Request body"}},"required":["owner","repo","body"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("POST", ("https://api.github.com/repos/" + String(args["owner"]) + "/" + String(args["repo"]) + "/releases") + _qs, {}, args["body"]);
    }
  },
  {
    name: "repos/get-release-asset",
    description: "Get a release asset. To download the asset's binary content:\n\n- If within a browser, fetch the location specified in the `browser_download_url` key provided in the response. Params: owner (required), repo (required), asset_id (required).",
    inputSchema: {"type":"object","properties":{"owner":{"type":"string","description":"The account owner of the repository. The name is not case sensitive."},"repo":{"type":"string","description":"The name of the repository without the `.git` extension. The name is not case sensitive."},"asset_id":{"type":"integer","description":"The unique identifier of the asset."}},"required":["owner","repo","asset_id"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/repos/" + String(args["owner"]) + "/" + String(args["repo"]) + "/releases/assets/" + String(args["asset_id"])) + _qs, {}, undefined);
    }
  },
  {
    name: "repos/update-release-asset",
    description: "Update a release asset. Users with push access to the repository can edit a release asset. Params: owner (required), repo (required), asset_id (required).",
    inputSchema: {"type":"object","properties":{"owner":{"type":"string","description":"The account owner of the repository. The name is not case sensitive."},"repo":{"type":"string","description":"The name of the repository without the `.git` extension. The name is not case sensitive."},"asset_id":{"type":"integer","description":"The unique identifier of the asset."},"body":{"type":"object","description":"Request body"}},"required":["owner","repo","asset_id"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("PATCH", ("https://api.github.com/repos/" + String(args["owner"]) + "/" + String(args["repo"]) + "/releases/assets/" + String(args["asset_id"])) + _qs, {}, args["body"]);
    }
  },
  {
    name: "repos/delete-release-asset",
    description: "Delete a release asset. Params: owner (required), repo (required), asset_id (required).",
    inputSchema: {"type":"object","properties":{"owner":{"type":"string","description":"The account owner of the repository. The name is not case sensitive."},"repo":{"type":"string","description":"The name of the repository without the `.git` extension. The name is not case sensitive."},"asset_id":{"type":"integer","description":"The unique identifier of the asset."}},"required":["owner","repo","asset_id"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("DELETE", ("https://api.github.com/repos/" + String(args["owner"]) + "/" + String(args["repo"]) + "/releases/assets/" + String(args["asset_id"])) + _qs, {}, undefined);
    }
  },
  {
    name: "repos/generate-release-notes",
    description: "Generate release notes content for a release. Generate a name and body describing a [release](https://docs.github.com/rest/releases/releases#get-a-release). Params: owner (required), repo (required).",
    inputSchema: {"type":"object","properties":{"owner":{"type":"string","description":"The account owner of the repository. The name is not case sensitive."},"repo":{"type":"string","description":"The name of the repository without the `.git` extension. The name is not case sensitive."},"body":{"type":"object","description":"Request body"}},"required":["owner","repo","body"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("POST", ("https://api.github.com/repos/" + String(args["owner"]) + "/" + String(args["repo"]) + "/releases/generate-notes") + _qs, {}, args["body"]);
    }
  },
  {
    name: "repos/get-latest-release",
    description: "Get the latest release. View the latest published full release for the repository. Params: owner (required), repo (required).",
    inputSchema: {"type":"object","properties":{"owner":{"type":"string","description":"The account owner of the repository. The name is not case sensitive."},"repo":{"type":"string","description":"The name of the repository without the `.git` extension. The name is not case sensitive."}},"required":["owner","repo"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/repos/" + String(args["owner"]) + "/" + String(args["repo"]) + "/releases/latest") + _qs, {}, undefined);
    }
  },
  {
    name: "repos/get-release-by-tag",
    description: "Get a release by tag name. Get a published release with the specified tag. Params: owner (required), repo (required), tag (required).",
    inputSchema: {"type":"object","properties":{"owner":{"type":"string","description":"The account owner of the repository. The name is not case sensitive."},"repo":{"type":"string","description":"The name of the repository without the `.git` extension. The name is not case sensitive."},"tag":{"type":"string","description":"tag parameter"}},"required":["owner","repo","tag"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/repos/" + String(args["owner"]) + "/" + String(args["repo"]) + "/releases/tags/" + String(args["tag"])) + _qs, {}, undefined);
    }
  },
  {
    name: "repos/get-release",
    description: "Get a release. Gets a public release with the specified release ID. Params: owner (required), repo (required), release_id (required).",
    inputSchema: {"type":"object","properties":{"owner":{"type":"string","description":"The account owner of the repository. The name is not case sensitive."},"repo":{"type":"string","description":"The name of the repository without the `.git` extension. The name is not case sensitive."},"release_id":{"type":"integer","description":"The unique identifier of the release."}},"required":["owner","repo","release_id"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/repos/" + String(args["owner"]) + "/" + String(args["repo"]) + "/releases/" + String(args["release_id"])) + _qs, {}, undefined);
    }
  },
  {
    name: "repos/update-release",
    description: "Update a release. Users with push access to the repository can edit a release. Params: owner (required), repo (required), release_id (required).",
    inputSchema: {"type":"object","properties":{"owner":{"type":"string","description":"The account owner of the repository. The name is not case sensitive."},"repo":{"type":"string","description":"The name of the repository without the `.git` extension. The name is not case sensitive."},"release_id":{"type":"integer","description":"The unique identifier of the release."},"body":{"type":"object","description":"Request body"}},"required":["owner","repo","release_id"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("PATCH", ("https://api.github.com/repos/" + String(args["owner"]) + "/" + String(args["repo"]) + "/releases/" + String(args["release_id"])) + _qs, {}, args["body"]);
    }
  },
  {
    name: "repos/delete-release",
    description: "Delete a release. Users with push access to the repository can delete a release. Params: owner (required), repo (required), release_id (required).",
    inputSchema: {"type":"object","properties":{"owner":{"type":"string","description":"The account owner of the repository. The name is not case sensitive."},"repo":{"type":"string","description":"The name of the repository without the `.git` extension. The name is not case sensitive."},"release_id":{"type":"integer","description":"The unique identifier of the release."}},"required":["owner","repo","release_id"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("DELETE", ("https://api.github.com/repos/" + String(args["owner"]) + "/" + String(args["repo"]) + "/releases/" + String(args["release_id"])) + _qs, {}, undefined);
    }
  },
  {
    name: "repos/list-release-assets",
    description: "List release assets. Params: owner (required), repo (required), release_id (required).",
    inputSchema: {"type":"object","properties":{"owner":{"type":"string","description":"The account owner of the repository. The name is not case sensitive."},"repo":{"type":"string","description":"The name of the repository without the `.git` extension. The name is not case sensitive."},"release_id":{"type":"integer","description":"The unique identifier of the release."},"per_page":{"type":"integer","default":30,"description":"The number of results per page (max 100). For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""},"page":{"type":"integer","default":1,"description":"The page number of the results to fetch. For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""}},"required":["owner","repo","release_id"]},
    call: function(args) {
    var _q = new URLSearchParams();
    if (args["per_page"] !== undefined) _q.set("per_page", String(args["per_page"]));
    if (args["page"] !== undefined) _q.set("page", String(args["page"]));
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/repos/" + String(args["owner"]) + "/" + String(args["repo"]) + "/releases/" + String(args["release_id"]) + "/assets") + _qs, {}, undefined);
    }
  },
  {
    name: "repos/upload-release-asset",
    description: "Upload a release asset. This endpoint makes use of a [Hypermedia relation](https://docs.github.com/rest/using-the-rest-api/getting-started-with-the-rest-api#hypermedia) to determine which URL to access. Params: owner (required), repo (required), release_id (required), name (required).",
    inputSchema: {"type":"object","properties":{"owner":{"type":"string","description":"The account owner of the repository. The name is not case sensitive."},"repo":{"type":"string","description":"The name of the repository without the `.git` extension. The name is not case sensitive."},"release_id":{"type":"integer","description":"The unique identifier of the release."},"name":{"type":"string"},"label":{"type":"string"},"body":{"type":"object","description":"Request body"}},"required":["owner","repo","release_id","name"]},
    call: function(args) {
    var _q = new URLSearchParams();
    if (args["name"] !== undefined) _q.set("name", String(args["name"]));
    if (args["label"] !== undefined) _q.set("label", String(args["label"]));
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("POST", ("https://api.github.com/repos/" + String(args["owner"]) + "/" + String(args["repo"]) + "/releases/" + String(args["release_id"]) + "/assets") + _qs, {}, args["body"]);
    }
  },
  {
    name: "reactions/list-for-release",
    description: "List reactions for a release. List the reactions to a [release](https://docs.github.com/rest/releases/releases#get-a-release). Params: owner (required), repo (required), release_id (required).",
    inputSchema: {"type":"object","properties":{"owner":{"type":"string","description":"The account owner of the repository. The name is not case sensitive."},"repo":{"type":"string","description":"The name of the repository without the `.git` extension. The name is not case sensitive."},"release_id":{"type":"integer","description":"The unique identifier of the release."},"content":{"type":"string","enum":["+1","laugh","heart","hooray","rocket","eyes"],"description":"Returns a single [reaction type](https://docs.github.com/rest/reactions/reactions#about-reactions). Omit this parameter to list all reactions to a release."},"per_page":{"type":"integer","default":30,"description":"The number of results per page (max 100). For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""},"page":{"type":"integer","default":1,"description":"The page number of the results to fetch. For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""}},"required":["owner","repo","release_id"]},
    call: function(args) {
    var _q = new URLSearchParams();
    if (args["content"] !== undefined) _q.set("content", String(args["content"]));
    if (args["per_page"] !== undefined) _q.set("per_page", String(args["per_page"]));
    if (args["page"] !== undefined) _q.set("page", String(args["page"]));
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/repos/" + String(args["owner"]) + "/" + String(args["repo"]) + "/releases/" + String(args["release_id"]) + "/reactions") + _qs, {}, undefined);
    }
  },
  {
    name: "reactions/create-for-release",
    description: "Create reaction for a release. Create a reaction to a [release](https://docs.github.com/rest/releases/releases#get-a-release). Params: owner (required), repo (required), release_id (required).",
    inputSchema: {"type":"object","properties":{"owner":{"type":"string","description":"The account owner of the repository. The name is not case sensitive."},"repo":{"type":"string","description":"The name of the repository without the `.git` extension. The name is not case sensitive."},"release_id":{"type":"integer","description":"The unique identifier of the release."},"body":{"type":"object","description":"Request body"}},"required":["owner","repo","release_id","body"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("POST", ("https://api.github.com/repos/" + String(args["owner"]) + "/" + String(args["repo"]) + "/releases/" + String(args["release_id"]) + "/reactions") + _qs, {}, args["body"]);
    }
  },
  {
    name: "reactions/delete-for-release",
    description: "Delete a release reaction. > [!NOTE]\n> You can also specify a repository by `repository_id` using the route `DELETE delete /repositories/:repository_id/releases/:release_id/reactions/:reaction_id`. Params: owner (required), repo (required), release_id (required), reaction_id (required).",
    inputSchema: {"type":"object","properties":{"owner":{"type":"string","description":"The account owner of the repository. The name is not case sensitive."},"repo":{"type":"string","description":"The name of the repository without the `.git` extension. The name is not case sensitive."},"release_id":{"type":"integer","description":"The unique identifier of the release."},"reaction_id":{"type":"integer","description":"The unique identifier of the reaction."}},"required":["owner","repo","release_id","reaction_id"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("DELETE", ("https://api.github.com/repos/" + String(args["owner"]) + "/" + String(args["repo"]) + "/releases/" + String(args["release_id"]) + "/reactions/" + String(args["reaction_id"])) + _qs, {}, undefined);
    }
  },
  {
    name: "repos/get-branch-rules",
    description: "Get rules for a branch. Returns all active rules that apply to the specified branch. Params: owner (required), repo (required), branch (required).",
    inputSchema: {"type":"object","properties":{"owner":{"type":"string","description":"The account owner of the repository. The name is not case sensitive."},"repo":{"type":"string","description":"The name of the repository without the `.git` extension. The name is not case sensitive."},"branch":{"type":"string","description":"The name of the branch. Cannot contain wildcard characters. To use wildcard characters in branch names, use [the GraphQL API](https://docs.github.com/graphql)."},"per_page":{"type":"integer","default":30,"description":"The number of results per page (max 100). For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""},"page":{"type":"integer","default":1,"description":"The page number of the results to fetch. For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""}},"required":["owner","repo","branch"]},
    call: function(args) {
    var _q = new URLSearchParams();
    if (args["per_page"] !== undefined) _q.set("per_page", String(args["per_page"]));
    if (args["page"] !== undefined) _q.set("page", String(args["page"]));
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/repos/" + String(args["owner"]) + "/" + String(args["repo"]) + "/rules/branches/" + String(args["branch"])) + _qs, {}, undefined);
    }
  },
  {
    name: "repos/get-repo-rulesets",
    description: "Get all repository rulesets. Get all the rulesets for a repository. Params: owner (required), repo (required).",
    inputSchema: {"type":"object","properties":{"owner":{"type":"string","description":"The account owner of the repository. The name is not case sensitive."},"repo":{"type":"string","description":"The name of the repository without the `.git` extension. The name is not case sensitive."},"per_page":{"type":"integer","default":30,"description":"The number of results per page (max 100). For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""},"page":{"type":"integer","default":1,"description":"The page number of the results to fetch. For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""},"includes_parents":{"type":"boolean","default":true,"description":"Include rulesets configured at higher levels that apply to this repository"},"targets":{"type":"string","example":"branch,tag,push","description":"A comma-separated list of rule targets to filter by.\nIf provided, only rulesets that apply to the specified targets will be returned.\nFor example, `branch,tag,push`.\n"}},"required":["owner","repo"]},
    call: function(args) {
    var _q = new URLSearchParams();
    if (args["per_page"] !== undefined) _q.set("per_page", String(args["per_page"]));
    if (args["page"] !== undefined) _q.set("page", String(args["page"]));
    if (args["includes_parents"] !== undefined) _q.set("includes_parents", String(args["includes_parents"]));
    if (args["targets"] !== undefined) _q.set("targets", String(args["targets"]));
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/repos/" + String(args["owner"]) + "/" + String(args["repo"]) + "/rulesets") + _qs, {}, undefined);
    }
  },
  {
    name: "repos/create-repo-ruleset",
    description: "Create a repository ruleset. Create a ruleset for a repository. Params: owner (required), repo (required).",
    inputSchema: {"type":"object","properties":{"owner":{"type":"string","description":"The account owner of the repository. The name is not case sensitive."},"repo":{"type":"string","description":"The name of the repository without the `.git` extension. The name is not case sensitive."},"body":{"type":"object","description":"Request body"}},"required":["owner","repo","body"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("POST", ("https://api.github.com/repos/" + String(args["owner"]) + "/" + String(args["repo"]) + "/rulesets") + _qs, {}, args["body"]);
    }
  },
  {
    name: "repos/get-repo-rule-suites",
    description: "List repository rule suites. Lists suites of rule evaluations at the repository level. Params: owner (required), repo (required).",
    inputSchema: {"type":"object","properties":{"owner":{"type":"string","description":"The account owner of the repository. The name is not case sensitive."},"repo":{"type":"string","description":"The name of the repository without the `.git` extension. The name is not case sensitive."},"ref":{"type":"string","description":"The name of the ref. Cannot contain wildcard characters. Optionally prefix with `refs/heads/` to limit to branches or `refs/tags/` to limit to tags. Omit the prefix to search across all refs. When specified, only rule evaluations triggered for this ref will be returned."},"time_period":{"type":"string","enum":["hour","day","week","month"],"default":"day","description":"The time period to filter by.\n\nFor example, `day` will filter for rule suites that occurred in the past 24 hours, and `week` will filter for rule suites that occurred in the past 7 days (168 hours)."},"actor_name":{"type":"string","description":"The handle for the GitHub user account to filter on. When specified, only rule evaluations triggered by this actor will be returned."},"rule_suite_result":{"type":"string","enum":["pass","fail","bypass","all"],"default":"all","description":"The rule suite results to filter on. When specified, only suites with this result will be returned."},"evaluate_status":{"type":"string","enum":["all","active","evaluate"],"default":"all","description":"The evaluate status to filter on. When specified, only rule suites resulting from rulesets with the specified evaluate status will be returned.\n  - `all` - all rule suites will be returned.\n  - `active` - only rule suites resulting from rulesets in active (non-evaluate) mode will be returned.\n  - `evaluate` - only rule suites resulting from rulesets in evaluate mode will be returned."},"per_page":{"type":"integer","default":30,"description":"The number of results per page (max 100). For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""},"page":{"type":"integer","default":1,"description":"The page number of the results to fetch. For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""}},"required":["owner","repo"]},
    call: function(args) {
    var _q = new URLSearchParams();
    if (args["ref"] !== undefined) _q.set("ref", String(args["ref"]));
    if (args["time_period"] !== undefined) _q.set("time_period", String(args["time_period"]));
    if (args["actor_name"] !== undefined) _q.set("actor_name", String(args["actor_name"]));
    if (args["rule_suite_result"] !== undefined) _q.set("rule_suite_result", String(args["rule_suite_result"]));
    if (args["evaluate_status"] !== undefined) _q.set("evaluate_status", String(args["evaluate_status"]));
    if (args["per_page"] !== undefined) _q.set("per_page", String(args["per_page"]));
    if (args["page"] !== undefined) _q.set("page", String(args["page"]));
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/repos/" + String(args["owner"]) + "/" + String(args["repo"]) + "/rulesets/rule-suites") + _qs, {}, undefined);
    }
  },
  {
    name: "repos/get-repo-rule-suite",
    description: "Get a repository rule suite. Gets information about a suite of rule evaluations from within a repository. Params: owner (required), repo (required), rule_suite_id (required).",
    inputSchema: {"type":"object","properties":{"owner":{"type":"string","description":"The account owner of the repository. The name is not case sensitive."},"repo":{"type":"string","description":"The name of the repository without the `.git` extension. The name is not case sensitive."},"rule_suite_id":{"type":"integer","description":"The unique identifier of the rule suite result.\nTo get this ID, you can use [GET /repos/{owner}/{repo}/rulesets/rule-suites](https://docs.github.com/rest/repos/rule-suites#list-repository-rule-suites)\nfor repositories and [GET /orgs/{org}/rulesets/rule-suites](https://docs.github.com/rest/orgs/rule-suites#list-organization-rule-suites)\nfor organizations."}},"required":["owner","repo","rule_suite_id"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/repos/" + String(args["owner"]) + "/" + String(args["repo"]) + "/rulesets/rule-suites/" + String(args["rule_suite_id"])) + _qs, {}, undefined);
    }
  },
  {
    name: "repos/get-repo-ruleset",
    description: "Get a repository ruleset. Get a ruleset for a repository. Params: owner (required), repo (required), ruleset_id (required).",
    inputSchema: {"type":"object","properties":{"owner":{"type":"string","description":"The account owner of the repository. The name is not case sensitive."},"repo":{"type":"string","description":"The name of the repository without the `.git` extension. The name is not case sensitive."},"ruleset_id":{"type":"integer","description":"The ID of the ruleset."},"includes_parents":{"type":"boolean","default":true,"description":"Include rulesets configured at higher levels that apply to this repository"}},"required":["owner","repo","ruleset_id"]},
    call: function(args) {
    var _q = new URLSearchParams();
    if (args["includes_parents"] !== undefined) _q.set("includes_parents", String(args["includes_parents"]));
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/repos/" + String(args["owner"]) + "/" + String(args["repo"]) + "/rulesets/" + String(args["ruleset_id"])) + _qs, {}, undefined);
    }
  },
  {
    name: "repos/update-repo-ruleset",
    description: "Update a repository ruleset. Update a ruleset for a repository. Params: owner (required), repo (required), ruleset_id (required).",
    inputSchema: {"type":"object","properties":{"owner":{"type":"string","description":"The account owner of the repository. The name is not case sensitive."},"repo":{"type":"string","description":"The name of the repository without the `.git` extension. The name is not case sensitive."},"ruleset_id":{"type":"integer","description":"The ID of the ruleset."},"body":{"type":"object","description":"Request body"}},"required":["owner","repo","ruleset_id"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("PUT", ("https://api.github.com/repos/" + String(args["owner"]) + "/" + String(args["repo"]) + "/rulesets/" + String(args["ruleset_id"])) + _qs, {}, args["body"]);
    }
  },
  {
    name: "repos/delete-repo-ruleset",
    description: "Delete a repository ruleset. Delete a ruleset for a repository. Params: owner (required), repo (required), ruleset_id (required).",
    inputSchema: {"type":"object","properties":{"owner":{"type":"string","description":"The account owner of the repository. The name is not case sensitive."},"repo":{"type":"string","description":"The name of the repository without the `.git` extension. The name is not case sensitive."},"ruleset_id":{"type":"integer","description":"The ID of the ruleset."}},"required":["owner","repo","ruleset_id"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("DELETE", ("https://api.github.com/repos/" + String(args["owner"]) + "/" + String(args["repo"]) + "/rulesets/" + String(args["ruleset_id"])) + _qs, {}, undefined);
    }
  },
  {
    name: "repos/get-repo-ruleset-history",
    description: "Get repository ruleset history. Get the history of a repository ruleset. Params: owner (required), repo (required), ruleset_id (required).",
    inputSchema: {"type":"object","properties":{"owner":{"type":"string","description":"The account owner of the repository. The name is not case sensitive."},"repo":{"type":"string","description":"The name of the repository without the `.git` extension. The name is not case sensitive."},"per_page":{"type":"integer","default":30,"description":"The number of results per page (max 100). For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""},"page":{"type":"integer","default":1,"description":"The page number of the results to fetch. For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""},"ruleset_id":{"type":"integer","description":"The ID of the ruleset."}},"required":["owner","repo","ruleset_id"]},
    call: function(args) {
    var _q = new URLSearchParams();
    if (args["per_page"] !== undefined) _q.set("per_page", String(args["per_page"]));
    if (args["page"] !== undefined) _q.set("page", String(args["page"]));
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/repos/" + String(args["owner"]) + "/" + String(args["repo"]) + "/rulesets/" + String(args["ruleset_id"]) + "/history") + _qs, {}, undefined);
    }
  },
  {
    name: "repos/get-repo-ruleset-version",
    description: "Get repository ruleset version. Get a version of a repository ruleset. Params: owner (required), repo (required), ruleset_id (required), version_id (required).",
    inputSchema: {"type":"object","properties":{"owner":{"type":"string","description":"The account owner of the repository. The name is not case sensitive."},"repo":{"type":"string","description":"The name of the repository without the `.git` extension. The name is not case sensitive."},"ruleset_id":{"type":"integer","description":"The ID of the ruleset."},"version_id":{"type":"integer","description":"The ID of the version"}},"required":["owner","repo","ruleset_id","version_id"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/repos/" + String(args["owner"]) + "/" + String(args["repo"]) + "/rulesets/" + String(args["ruleset_id"]) + "/history/" + String(args["version_id"])) + _qs, {}, undefined);
    }
  },
  {
    name: "secret-scanning/list-alerts-for-repo",
    description: "List secret scanning alerts for a repository. Lists secret scanning alerts for an eligible repository, from newest to oldest. Params: owner (required), repo (required).",
    inputSchema: {"type":"object","properties":{"owner":{"type":"string","description":"The account owner of the repository. The name is not case sensitive."},"repo":{"type":"string","description":"The name of the repository without the `.git` extension. The name is not case sensitive."},"state":{"type":"string","enum":["open","resolved"],"description":"Set to `open` or `resolved` to only list secret scanning alerts in a specific state."},"secret_type":{"type":"string","description":"A comma-separated list of secret types to return. All default secret patterns are returned. To return generic patterns, pass the token name(s) in the parameter. See \"[Supported secret scanning patterns](https://docs.github.com/code-security/secret-scanning/introduction/supported-secret-scanning-patterns#supported-secrets)\" for a complete list of secret types."},"exclude_secret_types":{"type":"string","description":"A comma-separated list of secret types to exclude from the results. All default secret patterns are returned except those matching the specified types. Cannot be combined with the `secret_type` parameter. See \"[Supported secret scanning patterns](https://docs.github.com/code-security/secret-scanning/introduction/supported-secret-scanning-patterns#supported-secrets)\" for a complete list of secret types."},"exclude_providers":{"type":"string","description":"A comma-separated list of provider slugs to exclude from the results.\nProvider slugs use lowercase with underscores (e.g., `github_secret_scanning`, `clojars`).\nYou can find the provider slug in the `provider_slug` field of each alert.\nCannot be combined with the `providers` parameter."},"providers":{"type":"string","description":"A comma-separated list of provider slugs to filter by.\nProvider slugs use lowercase with underscores (e.g., `github_secret_scanning`, `clojars`).\nYou can find the provider slug in the `provider_slug` field of each alert.\nCannot be combined with the `exclude_providers` parameter."},"resolution":{"type":"string","description":"A comma-separated list of resolutions. Only secret scanning alerts with one of these resolutions are listed. Valid resolutions are `false_positive`, `wont_fix`, `revoked`, `pattern_edited`, `pattern_deleted` or `used_in_tests`."},"assignee":{"type":"string","description":"Filters alerts by assignee. Use `*` to get all assigned alerts, `none` to get all unassigned alerts, or a GitHub username to get alerts assigned to a specific user."},"sort":{"type":"string","enum":["created","updated"],"default":"created","description":"The property to sort the results by. `created` means when the alert was created. `updated` means when the alert was updated or resolved."},"direction":{"type":"string","enum":["asc","desc"],"default":"desc","description":"The direction to sort the results by."},"page":{"type":"integer","default":1,"description":"The page number of the results to fetch. For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""},"per_page":{"type":"integer","default":30,"description":"The number of results per page (max 100). For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""},"before":{"type":"string","description":"A cursor, as given in the [Link header](https://docs.github.com/rest/guides/using-pagination-in-the-rest-api#using-link-headers). If specified, the query only searches for events before this cursor. To receive an initial cursor on your first request, include an empty \"before\" query string."},"after":{"type":"string","description":"A cursor, as given in the [Link header](https://docs.github.com/rest/guides/using-pagination-in-the-rest-api#using-link-headers). If specified, the query only searches for events after this cursor.  To receive an initial cursor on your first request, include an empty \"after\" query string."},"validity":{"type":"string","description":"A comma-separated list of validities that, when present, will return alerts that match the validities in this list. Valid options are `active`, `inactive`, and `unknown`."},"is_publicly_leaked":{"type":"boolean","default":false,"description":"A boolean value representing whether or not to filter alerts by the publicly-leaked tag being present."},"is_multi_repo":{"type":"boolean","default":false,"description":"A boolean value representing whether or not to filter alerts by the multi-repo tag being present."},"hide_secret":{"type":"boolean","default":false,"description":"A boolean value representing whether or not to hide literal secrets in the results."},"is_bypassed":{"type":"boolean","description":"A boolean value (`true` or `false`) indicating whether to filter alerts by their push protection bypass status. When set to `true`, only alerts that were created because a push protection rule was bypassed will be returned. When set to `false`, only alerts that were not caused by a push protection bypass will be returned."}},"required":["owner","repo"]},
    call: function(args) {
    var _q = new URLSearchParams();
    if (args["state"] !== undefined) _q.set("state", String(args["state"]));
    if (args["secret_type"] !== undefined) _q.set("secret_type", String(args["secret_type"]));
    if (args["exclude_secret_types"] !== undefined) _q.set("exclude_secret_types", String(args["exclude_secret_types"]));
    if (args["exclude_providers"] !== undefined) _q.set("exclude_providers", String(args["exclude_providers"]));
    if (args["providers"] !== undefined) _q.set("providers", String(args["providers"]));
    if (args["resolution"] !== undefined) _q.set("resolution", String(args["resolution"]));
    if (args["assignee"] !== undefined) _q.set("assignee", String(args["assignee"]));
    if (args["sort"] !== undefined) _q.set("sort", String(args["sort"]));
    if (args["direction"] !== undefined) _q.set("direction", String(args["direction"]));
    if (args["page"] !== undefined) _q.set("page", String(args["page"]));
    if (args["per_page"] !== undefined) _q.set("per_page", String(args["per_page"]));
    if (args["before"] !== undefined) _q.set("before", String(args["before"]));
    if (args["after"] !== undefined) _q.set("after", String(args["after"]));
    if (args["validity"] !== undefined) _q.set("validity", String(args["validity"]));
    if (args["is_publicly_leaked"] !== undefined) _q.set("is_publicly_leaked", String(args["is_publicly_leaked"]));
    if (args["is_multi_repo"] !== undefined) _q.set("is_multi_repo", String(args["is_multi_repo"]));
    if (args["hide_secret"] !== undefined) _q.set("hide_secret", String(args["hide_secret"]));
    if (args["is_bypassed"] !== undefined) _q.set("is_bypassed", String(args["is_bypassed"]));
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/repos/" + String(args["owner"]) + "/" + String(args["repo"]) + "/secret-scanning/alerts") + _qs, {}, undefined);
    }
  },
  {
    name: "secret-scanning/get-alert",
    description: "Get a secret scanning alert. Gets a single secret scanning alert detected in an eligible repository. Params: owner (required), repo (required), alert_number (required).",
    inputSchema: {"type":"object","properties":{"owner":{"type":"string","description":"The account owner of the repository. The name is not case sensitive."},"repo":{"type":"string","description":"The name of the repository without the `.git` extension. The name is not case sensitive."},"alert_number":{"$ref":"#/components/schemas/alert-number","description":"The number that identifies an alert. You can find this at the end of the URL for a code scanning alert within GitHub, and in the `number` field in the response from the `GET /repos/{owner}/{repo}/code-scanning/alerts` operation."},"hide_secret":{"type":"boolean","default":false,"description":"A boolean value representing whether or not to hide literal secrets in the results."}},"required":["owner","repo","alert_number"]},
    call: function(args) {
    var _q = new URLSearchParams();
    if (args["hide_secret"] !== undefined) _q.set("hide_secret", String(args["hide_secret"]));
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/repos/" + String(args["owner"]) + "/" + String(args["repo"]) + "/secret-scanning/alerts/" + String(args["alert_number"])) + _qs, {}, undefined);
    }
  },
  {
    name: "secret-scanning/update-alert",
    description: "Update a secret scanning alert. Updates the status of a secret scanning alert in an eligible repository. Params: owner (required), repo (required), alert_number (required).",
    inputSchema: {"type":"object","properties":{"owner":{"type":"string","description":"The account owner of the repository. The name is not case sensitive."},"repo":{"type":"string","description":"The name of the repository without the `.git` extension. The name is not case sensitive."},"alert_number":{"$ref":"#/components/schemas/alert-number","description":"The number that identifies an alert. You can find this at the end of the URL for a code scanning alert within GitHub, and in the `number` field in the response from the `GET /repos/{owner}/{repo}/code-scanning/alerts` operation."},"body":{"type":"object","description":"Request body"}},"required":["owner","repo","alert_number","body"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("PATCH", ("https://api.github.com/repos/" + String(args["owner"]) + "/" + String(args["repo"]) + "/secret-scanning/alerts/" + String(args["alert_number"])) + _qs, {}, args["body"]);
    }
  },
  {
    name: "secret-scanning/list-locations-for-alert",
    description: "List locations for a secret scanning alert. Lists all locations for a given secret scanning alert for an eligible repository. Params: owner (required), repo (required), alert_number (required).",
    inputSchema: {"type":"object","properties":{"owner":{"type":"string","description":"The account owner of the repository. The name is not case sensitive."},"repo":{"type":"string","description":"The name of the repository without the `.git` extension. The name is not case sensitive."},"alert_number":{"$ref":"#/components/schemas/alert-number","description":"The number that identifies an alert. You can find this at the end of the URL for a code scanning alert within GitHub, and in the `number` field in the response from the `GET /repos/{owner}/{repo}/code-scanning/alerts` operation."},"page":{"type":"integer","default":1,"description":"The page number of the results to fetch. For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""},"per_page":{"type":"integer","default":30,"description":"The number of results per page (max 100). For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""}},"required":["owner","repo","alert_number"]},
    call: function(args) {
    var _q = new URLSearchParams();
    if (args["page"] !== undefined) _q.set("page", String(args["page"]));
    if (args["per_page"] !== undefined) _q.set("per_page", String(args["per_page"]));
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/repos/" + String(args["owner"]) + "/" + String(args["repo"]) + "/secret-scanning/alerts/" + String(args["alert_number"]) + "/locations") + _qs, {}, undefined);
    }
  },
  {
    name: "secret-scanning/create-push-protection-bypass",
    description: "Create a push protection bypass. Creates a bypass for a previously push protected secret. Params: owner (required), repo (required).",
    inputSchema: {"type":"object","properties":{"owner":{"type":"string","description":"The account owner of the repository. The name is not case sensitive."},"repo":{"type":"string","description":"The name of the repository without the `.git` extension. The name is not case sensitive."},"body":{"type":"object","description":"Request body"}},"required":["owner","repo","body"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("POST", ("https://api.github.com/repos/" + String(args["owner"]) + "/" + String(args["repo"]) + "/secret-scanning/push-protection-bypasses") + _qs, {}, args["body"]);
    }
  },
  {
    name: "secret-scanning/get-scan-history",
    description: "Get secret scanning scan history for a repository. Lists the latest default incremental and backfill scans by type for a repository. Params: owner (required), repo (required).",
    inputSchema: {"type":"object","properties":{"owner":{"type":"string","description":"The account owner of the repository. The name is not case sensitive."},"repo":{"type":"string","description":"The name of the repository without the `.git` extension. The name is not case sensitive."}},"required":["owner","repo"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/repos/" + String(args["owner"]) + "/" + String(args["repo"]) + "/secret-scanning/scan-history") + _qs, {}, undefined);
    }
  },
  {
    name: "security-advisories/list-repository-advisories",
    description: "List repository security advisories. Lists security advisories in a repository. Params: owner (required), repo (required).",
    inputSchema: {"type":"object","properties":{"owner":{"type":"string","description":"The account owner of the repository. The name is not case sensitive."},"repo":{"type":"string","description":"The name of the repository without the `.git` extension. The name is not case sensitive."},"direction":{"type":"string","enum":["asc","desc"],"default":"desc","description":"The direction to sort the results by."},"sort":{"type":"string","enum":["created","updated","published"],"default":"created","description":"The property to sort the results by."},"before":{"type":"string","description":"A cursor, as given in the [Link header](https://docs.github.com/rest/guides/using-pagination-in-the-rest-api#using-link-headers). If specified, the query only searches for results before this cursor. For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""},"after":{"type":"string","description":"A cursor, as given in the [Link header](https://docs.github.com/rest/guides/using-pagination-in-the-rest-api#using-link-headers). If specified, the query only searches for results after this cursor. For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""},"per_page":{"type":"integer","minimum":1,"maximum":100,"default":30,"description":"The number of advisories to return per page. For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""},"state":{"type":"string","enum":["triage","draft","published","closed"],"description":"Filter by state of the repository advisories. Only advisories of this state will be returned."}},"required":["owner","repo"]},
    call: function(args) {
    var _q = new URLSearchParams();
    if (args["direction"] !== undefined) _q.set("direction", String(args["direction"]));
    if (args["sort"] !== undefined) _q.set("sort", String(args["sort"]));
    if (args["before"] !== undefined) _q.set("before", String(args["before"]));
    if (args["after"] !== undefined) _q.set("after", String(args["after"]));
    if (args["per_page"] !== undefined) _q.set("per_page", String(args["per_page"]));
    if (args["state"] !== undefined) _q.set("state", String(args["state"]));
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/repos/" + String(args["owner"]) + "/" + String(args["repo"]) + "/security-advisories") + _qs, {}, undefined);
    }
  },
  {
    name: "security-advisories/create-repository-advisory",
    description: "Create a repository security advisory. Creates a new repository security advisory. Params: owner (required), repo (required).",
    inputSchema: {"type":"object","properties":{"owner":{"type":"string","description":"The account owner of the repository. The name is not case sensitive."},"repo":{"type":"string","description":"The name of the repository without the `.git` extension. The name is not case sensitive."},"body":{"type":"object","description":"Request body"}},"required":["owner","repo","body"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("POST", ("https://api.github.com/repos/" + String(args["owner"]) + "/" + String(args["repo"]) + "/security-advisories") + _qs, {}, args["body"]);
    }
  },
  {
    name: "security-advisories/create-private-vulnerability-report",
    description: "Privately report a security vulnerability. Report a security vulnerability to the maintainers of the repository. Params: owner (required), repo (required).",
    inputSchema: {"type":"object","properties":{"owner":{"type":"string","description":"The account owner of the repository. The name is not case sensitive."},"repo":{"type":"string","description":"The name of the repository without the `.git` extension. The name is not case sensitive."},"body":{"type":"object","description":"Request body"}},"required":["owner","repo","body"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("POST", ("https://api.github.com/repos/" + String(args["owner"]) + "/" + String(args["repo"]) + "/security-advisories/reports") + _qs, {}, args["body"]);
    }
  },
  {
    name: "security-advisories/get-repository-advisory",
    description: "Get a repository security advisory. Get a repository security advisory using its GitHub Security Advisory (GHSA) identifier. Params: owner (required), repo (required), ghsa_id (required).",
    inputSchema: {"type":"object","properties":{"owner":{"type":"string","description":"The account owner of the repository. The name is not case sensitive."},"repo":{"type":"string","description":"The name of the repository without the `.git` extension. The name is not case sensitive."},"ghsa_id":{"type":"string","description":"The GHSA (GitHub Security Advisory) identifier of the advisory."}},"required":["owner","repo","ghsa_id"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/repos/" + String(args["owner"]) + "/" + String(args["repo"]) + "/security-advisories/" + String(args["ghsa_id"])) + _qs, {}, undefined);
    }
  },
  {
    name: "security-advisories/update-repository-advisory",
    description: "Update a repository security advisory. Update a repository security advisory using its GitHub Security Advisory (GHSA) identifier. Params: owner (required), repo (required), ghsa_id (required).",
    inputSchema: {"type":"object","properties":{"owner":{"type":"string","description":"The account owner of the repository. The name is not case sensitive."},"repo":{"type":"string","description":"The name of the repository without the `.git` extension. The name is not case sensitive."},"ghsa_id":{"type":"string","description":"The GHSA (GitHub Security Advisory) identifier of the advisory."},"body":{"type":"object","description":"Request body"}},"required":["owner","repo","ghsa_id","body"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("PATCH", ("https://api.github.com/repos/" + String(args["owner"]) + "/" + String(args["repo"]) + "/security-advisories/" + String(args["ghsa_id"])) + _qs, {}, args["body"]);
    }
  },
  {
    name: "security-advisories/create-repository-advisory-cve-request",
    description: "Request a CVE for a repository security advisory. If you want a CVE identification number for the security vulnerability in your project, and don't already have one, you can request a CVE identification number from GitHub. Params: owner (required), repo (required), ghsa_id (required).",
    inputSchema: {"type":"object","properties":{"owner":{"type":"string","description":"The account owner of the repository. The name is not case sensitive."},"repo":{"type":"string","description":"The name of the repository without the `.git` extension. The name is not case sensitive."},"ghsa_id":{"type":"string","description":"The GHSA (GitHub Security Advisory) identifier of the advisory."}},"required":["owner","repo","ghsa_id"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("POST", ("https://api.github.com/repos/" + String(args["owner"]) + "/" + String(args["repo"]) + "/security-advisories/" + String(args["ghsa_id"]) + "/cve") + _qs, {}, undefined);
    }
  },
  {
    name: "security-advisories/create-fork",
    description: "Create a temporary private fork. Create a temporary private fork to collaborate on fixing a security vulnerability in your repository. Params: owner (required), repo (required), ghsa_id (required).",
    inputSchema: {"type":"object","properties":{"owner":{"type":"string","description":"The account owner of the repository. The name is not case sensitive."},"repo":{"type":"string","description":"The name of the repository without the `.git` extension. The name is not case sensitive."},"ghsa_id":{"type":"string","description":"The GHSA (GitHub Security Advisory) identifier of the advisory."}},"required":["owner","repo","ghsa_id"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("POST", ("https://api.github.com/repos/" + String(args["owner"]) + "/" + String(args["repo"]) + "/security-advisories/" + String(args["ghsa_id"]) + "/forks") + _qs, {}, undefined);
    }
  },
  {
    name: "activity/list-stargazers-for-repo",
    description: "List stargazers. Lists the people that have starred the repository. Params: owner (required), repo (required).",
    inputSchema: {"type":"object","properties":{"owner":{"type":"string","description":"The account owner of the repository. The name is not case sensitive."},"repo":{"type":"string","description":"The name of the repository without the `.git` extension. The name is not case sensitive."},"per_page":{"type":"integer","default":30,"description":"The number of results per page (max 100). For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""},"page":{"type":"integer","default":1,"description":"The page number of the results to fetch. For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""}},"required":["owner","repo"]},
    call: function(args) {
    var _q = new URLSearchParams();
    if (args["per_page"] !== undefined) _q.set("per_page", String(args["per_page"]));
    if (args["page"] !== undefined) _q.set("page", String(args["page"]));
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/repos/" + String(args["owner"]) + "/" + String(args["repo"]) + "/stargazers") + _qs, {}, undefined);
    }
  },
  {
    name: "repos/get-code-frequency-stats",
    description: "Get the weekly commit activity. Returns a weekly aggregate of the number of additions and deletions pushed to a repository. Params: owner (required), repo (required).",
    inputSchema: {"type":"object","properties":{"owner":{"type":"string","description":"The account owner of the repository. The name is not case sensitive."},"repo":{"type":"string","description":"The name of the repository without the `.git` extension. The name is not case sensitive."}},"required":["owner","repo"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/repos/" + String(args["owner"]) + "/" + String(args["repo"]) + "/stats/code_frequency") + _qs, {}, undefined);
    }
  },
  {
    name: "repos/get-commit-activity-stats",
    description: "Get the last year of commit activity. Returns the last year of commit activity grouped by week. Params: owner (required), repo (required).",
    inputSchema: {"type":"object","properties":{"owner":{"type":"string","description":"The account owner of the repository. The name is not case sensitive."},"repo":{"type":"string","description":"The name of the repository without the `.git` extension. The name is not case sensitive."}},"required":["owner","repo"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/repos/" + String(args["owner"]) + "/" + String(args["repo"]) + "/stats/commit_activity") + _qs, {}, undefined);
    }
  },
  {
    name: "repos/get-contributors-stats",
    description: "Get all contributor commit activity. Returns the `total` number of commits authored by the contributor. Params: owner (required), repo (required).",
    inputSchema: {"type":"object","properties":{"owner":{"type":"string","description":"The account owner of the repository. The name is not case sensitive."},"repo":{"type":"string","description":"The name of the repository without the `.git` extension. The name is not case sensitive."}},"required":["owner","repo"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/repos/" + String(args["owner"]) + "/" + String(args["repo"]) + "/stats/contributors") + _qs, {}, undefined);
    }
  },
  {
    name: "repos/get-participation-stats",
    description: "Get the weekly commit count. Returns the total commit counts for the `owner` and total commit counts in `all`. Params: owner (required), repo (required).",
    inputSchema: {"type":"object","properties":{"owner":{"type":"string","description":"The account owner of the repository. The name is not case sensitive."},"repo":{"type":"string","description":"The name of the repository without the `.git` extension. The name is not case sensitive."}},"required":["owner","repo"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/repos/" + String(args["owner"]) + "/" + String(args["repo"]) + "/stats/participation") + _qs, {}, undefined);
    }
  },
  {
    name: "repos/get-punch-card-stats",
    description: "Get the hourly commit count for each day. Each array contains the day number, hour number, and number of commits:\n\n*   `0-6`: Sunday - Saturday\n*   `0-23`: Hour of day\n*   Number of commits\n\nFor example, `[2, 14, 25]` indicates that there were 25 total commits, during the 2:00pm hour on Tuesdays. Params: owner (required), repo (required).",
    inputSchema: {"type":"object","properties":{"owner":{"type":"string","description":"The account owner of the repository. The name is not case sensitive."},"repo":{"type":"string","description":"The name of the repository without the `.git` extension. The name is not case sensitive."}},"required":["owner","repo"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/repos/" + String(args["owner"]) + "/" + String(args["repo"]) + "/stats/punch_card") + _qs, {}, undefined);
    }
  },
  {
    name: "repos/create-commit-status",
    description: "Create a commit status. Users with push access in a repository can create commit statuses for a given SHA. Params: owner (required), repo (required), sha (required).",
    inputSchema: {"type":"object","properties":{"owner":{"type":"string","description":"The account owner of the repository. The name is not case sensitive."},"repo":{"type":"string","description":"The name of the repository without the `.git` extension. The name is not case sensitive."},"sha":{"type":"string"},"body":{"type":"object","description":"Request body"}},"required":["owner","repo","sha","body"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("POST", ("https://api.github.com/repos/" + String(args["owner"]) + "/" + String(args["repo"]) + "/statuses/" + String(args["sha"])) + _qs, {}, args["body"]);
    }
  },
  {
    name: "activity/list-watchers-for-repo",
    description: "List watchers. Lists the people watching the specified repository. Params: owner (required), repo (required).",
    inputSchema: {"type":"object","properties":{"owner":{"type":"string","description":"The account owner of the repository. The name is not case sensitive."},"repo":{"type":"string","description":"The name of the repository without the `.git` extension. The name is not case sensitive."},"per_page":{"type":"integer","default":30,"description":"The number of results per page (max 100). For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""},"page":{"type":"integer","default":1,"description":"The page number of the results to fetch. For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""}},"required":["owner","repo"]},
    call: function(args) {
    var _q = new URLSearchParams();
    if (args["per_page"] !== undefined) _q.set("per_page", String(args["per_page"]));
    if (args["page"] !== undefined) _q.set("page", String(args["page"]));
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/repos/" + String(args["owner"]) + "/" + String(args["repo"]) + "/subscribers") + _qs, {}, undefined);
    }
  },
  {
    name: "activity/get-repo-subscription",
    description: "Get a repository subscription. Gets information about whether the authenticated user is subscribed to the repository. Params: owner (required), repo (required).",
    inputSchema: {"type":"object","properties":{"owner":{"type":"string","description":"The account owner of the repository. The name is not case sensitive."},"repo":{"type":"string","description":"The name of the repository without the `.git` extension. The name is not case sensitive."}},"required":["owner","repo"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/repos/" + String(args["owner"]) + "/" + String(args["repo"]) + "/subscription") + _qs, {}, undefined);
    }
  },
  {
    name: "activity/set-repo-subscription",
    description: "Set a repository subscription. If you would like to watch a repository, set `subscribed` to `true`. Params: owner (required), repo (required).",
    inputSchema: {"type":"object","properties":{"owner":{"type":"string","description":"The account owner of the repository. The name is not case sensitive."},"repo":{"type":"string","description":"The name of the repository without the `.git` extension. The name is not case sensitive."},"body":{"type":"object","description":"Request body"}},"required":["owner","repo"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("PUT", ("https://api.github.com/repos/" + String(args["owner"]) + "/" + String(args["repo"]) + "/subscription") + _qs, {}, args["body"]);
    }
  },
  {
    name: "activity/delete-repo-subscription",
    description: "Delete a repository subscription. This endpoint should only be used to stop watching a repository. Params: owner (required), repo (required).",
    inputSchema: {"type":"object","properties":{"owner":{"type":"string","description":"The account owner of the repository. The name is not case sensitive."},"repo":{"type":"string","description":"The name of the repository without the `.git` extension. The name is not case sensitive."}},"required":["owner","repo"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("DELETE", ("https://api.github.com/repos/" + String(args["owner"]) + "/" + String(args["repo"]) + "/subscription") + _qs, {}, undefined);
    }
  },
  {
    name: "repos/list-tags",
    description: "List repository tags. Params: owner (required), repo (required).",
    inputSchema: {"type":"object","properties":{"owner":{"type":"string","description":"The account owner of the repository. The name is not case sensitive."},"repo":{"type":"string","description":"The name of the repository without the `.git` extension. The name is not case sensitive."},"per_page":{"type":"integer","default":30,"description":"The number of results per page (max 100). For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""},"page":{"type":"integer","default":1,"description":"The page number of the results to fetch. For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""}},"required":["owner","repo"]},
    call: function(args) {
    var _q = new URLSearchParams();
    if (args["per_page"] !== undefined) _q.set("per_page", String(args["per_page"]));
    if (args["page"] !== undefined) _q.set("page", String(args["page"]));
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/repos/" + String(args["owner"]) + "/" + String(args["repo"]) + "/tags") + _qs, {}, undefined);
    }
  },
  {
    name: "repos/download-tarball-archive",
    description: "Download a repository archive (tar). Gets a redirect URL to download a tar archive for a repository. Params: owner (required), repo (required), ref (required).",
    inputSchema: {"type":"object","properties":{"owner":{"type":"string","description":"The account owner of the repository. The name is not case sensitive."},"repo":{"type":"string","description":"The name of the repository without the `.git` extension. The name is not case sensitive."},"ref":{"type":"string"}},"required":["owner","repo","ref"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/repos/" + String(args["owner"]) + "/" + String(args["repo"]) + "/tarball/" + String(args["ref"])) + _qs, {}, undefined);
    }
  },
  {
    name: "repos/list-teams",
    description: "List repository teams. Lists the teams that have access to the specified repository and that are also visible to the authenticated user. Params: owner (required), repo (required).",
    inputSchema: {"type":"object","properties":{"owner":{"type":"string","description":"The account owner of the repository. The name is not case sensitive."},"repo":{"type":"string","description":"The name of the repository without the `.git` extension. The name is not case sensitive."},"per_page":{"type":"integer","default":30,"description":"The number of results per page (max 100). For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""},"page":{"type":"integer","default":1,"description":"The page number of the results to fetch. For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""}},"required":["owner","repo"]},
    call: function(args) {
    var _q = new URLSearchParams();
    if (args["per_page"] !== undefined) _q.set("per_page", String(args["per_page"]));
    if (args["page"] !== undefined) _q.set("page", String(args["page"]));
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/repos/" + String(args["owner"]) + "/" + String(args["repo"]) + "/teams") + _qs, {}, undefined);
    }
  },
  {
    name: "repos/get-all-topics",
    description: "Get all repository topics. Params: owner (required), repo (required).",
    inputSchema: {"type":"object","properties":{"owner":{"type":"string","description":"The account owner of the repository. The name is not case sensitive."},"repo":{"type":"string","description":"The name of the repository without the `.git` extension. The name is not case sensitive."},"page":{"type":"integer","default":1,"description":"The page number of the results to fetch. For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""},"per_page":{"type":"integer","default":30,"description":"The number of results per page (max 100). For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""}},"required":["owner","repo"]},
    call: function(args) {
    var _q = new URLSearchParams();
    if (args["page"] !== undefined) _q.set("page", String(args["page"]));
    if (args["per_page"] !== undefined) _q.set("per_page", String(args["per_page"]));
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/repos/" + String(args["owner"]) + "/" + String(args["repo"]) + "/topics") + _qs, {}, undefined);
    }
  },
  {
    name: "repos/replace-all-topics",
    description: "Replace all repository topics. Params: owner (required), repo (required).",
    inputSchema: {"type":"object","properties":{"owner":{"type":"string","description":"The account owner of the repository. The name is not case sensitive."},"repo":{"type":"string","description":"The name of the repository without the `.git` extension. The name is not case sensitive."},"body":{"type":"object","description":"Request body"}},"required":["owner","repo","body"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("PUT", ("https://api.github.com/repos/" + String(args["owner"]) + "/" + String(args["repo"]) + "/topics") + _qs, {}, args["body"]);
    }
  },
  {
    name: "repos/get-clones",
    description: "Get repository clones. Get the total number of clones and breakdown per day or week for the last 14 days. Params: owner (required), repo (required).",
    inputSchema: {"type":"object","properties":{"owner":{"type":"string","description":"The account owner of the repository. The name is not case sensitive."},"repo":{"type":"string","description":"The name of the repository without the `.git` extension. The name is not case sensitive."},"per":{"type":"string","enum":["day","week"],"default":"day","description":"The time frame to display results for."}},"required":["owner","repo"]},
    call: function(args) {
    var _q = new URLSearchParams();
    if (args["per"] !== undefined) _q.set("per", String(args["per"]));
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/repos/" + String(args["owner"]) + "/" + String(args["repo"]) + "/traffic/clones") + _qs, {}, undefined);
    }
  },
  {
    name: "repos/get-top-paths",
    description: "Get top referral paths. Get the top 10 popular contents over the last 14 days. Params: owner (required), repo (required).",
    inputSchema: {"type":"object","properties":{"owner":{"type":"string","description":"The account owner of the repository. The name is not case sensitive."},"repo":{"type":"string","description":"The name of the repository without the `.git` extension. The name is not case sensitive."}},"required":["owner","repo"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/repos/" + String(args["owner"]) + "/" + String(args["repo"]) + "/traffic/popular/paths") + _qs, {}, undefined);
    }
  },
  {
    name: "repos/get-top-referrers",
    description: "Get top referral sources. Get the top 10 referrers over the last 14 days. Params: owner (required), repo (required).",
    inputSchema: {"type":"object","properties":{"owner":{"type":"string","description":"The account owner of the repository. The name is not case sensitive."},"repo":{"type":"string","description":"The name of the repository without the `.git` extension. The name is not case sensitive."}},"required":["owner","repo"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/repos/" + String(args["owner"]) + "/" + String(args["repo"]) + "/traffic/popular/referrers") + _qs, {}, undefined);
    }
  },
  {
    name: "repos/get-views",
    description: "Get page views. Get the total number of views and breakdown per day or week for the last 14 days. Params: owner (required), repo (required).",
    inputSchema: {"type":"object","properties":{"owner":{"type":"string","description":"The account owner of the repository. The name is not case sensitive."},"repo":{"type":"string","description":"The name of the repository without the `.git` extension. The name is not case sensitive."},"per":{"type":"string","enum":["day","week"],"default":"day","description":"The time frame to display results for."}},"required":["owner","repo"]},
    call: function(args) {
    var _q = new URLSearchParams();
    if (args["per"] !== undefined) _q.set("per", String(args["per"]));
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/repos/" + String(args["owner"]) + "/" + String(args["repo"]) + "/traffic/views") + _qs, {}, undefined);
    }
  },
  {
    name: "repos/transfer",
    description: "Transfer a repository. A transfer request will need to be accepted by the new owner when transferring a personal repository to another user. Params: owner (required), repo (required).",
    inputSchema: {"type":"object","properties":{"owner":{"type":"string","description":"The account owner of the repository. The name is not case sensitive."},"repo":{"type":"string","description":"The name of the repository without the `.git` extension. The name is not case sensitive."},"body":{"type":"object","description":"Request body"}},"required":["owner","repo","body"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("POST", ("https://api.github.com/repos/" + String(args["owner"]) + "/" + String(args["repo"]) + "/transfer") + _qs, {}, args["body"]);
    }
  },
  {
    name: "repos/check-vulnerability-alerts",
    description: "Check if vulnerability alerts are enabled for a repository. Shows whether dependency alerts are enabled or disabled for a repository. Params: owner (required), repo (required).",
    inputSchema: {"type":"object","properties":{"owner":{"type":"string","description":"The account owner of the repository. The name is not case sensitive."},"repo":{"type":"string","description":"The name of the repository without the `.git` extension. The name is not case sensitive."}},"required":["owner","repo"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/repos/" + String(args["owner"]) + "/" + String(args["repo"]) + "/vulnerability-alerts") + _qs, {}, undefined);
    }
  },
  {
    name: "repos/enable-vulnerability-alerts",
    description: "Enable vulnerability alerts. Enables dependency alerts and the dependency graph for a repository. Params: owner (required), repo (required).",
    inputSchema: {"type":"object","properties":{"owner":{"type":"string","description":"The account owner of the repository. The name is not case sensitive."},"repo":{"type":"string","description":"The name of the repository without the `.git` extension. The name is not case sensitive."}},"required":["owner","repo"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("PUT", ("https://api.github.com/repos/" + String(args["owner"]) + "/" + String(args["repo"]) + "/vulnerability-alerts") + _qs, {}, undefined);
    }
  },
  {
    name: "repos/disable-vulnerability-alerts",
    description: "Disable vulnerability alerts. Disables dependency alerts and the dependency graph for a repository. Params: owner (required), repo (required).",
    inputSchema: {"type":"object","properties":{"owner":{"type":"string","description":"The account owner of the repository. The name is not case sensitive."},"repo":{"type":"string","description":"The name of the repository without the `.git` extension. The name is not case sensitive."}},"required":["owner","repo"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("DELETE", ("https://api.github.com/repos/" + String(args["owner"]) + "/" + String(args["repo"]) + "/vulnerability-alerts") + _qs, {}, undefined);
    }
  },
  {
    name: "repos/download-zipball-archive",
    description: "Download a repository archive (zip). Gets a redirect URL to download a zip archive for a repository. Params: owner (required), repo (required), ref (required).",
    inputSchema: {"type":"object","properties":{"owner":{"type":"string","description":"The account owner of the repository. The name is not case sensitive."},"repo":{"type":"string","description":"The name of the repository without the `.git` extension. The name is not case sensitive."},"ref":{"type":"string"}},"required":["owner","repo","ref"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/repos/" + String(args["owner"]) + "/" + String(args["repo"]) + "/zipball/" + String(args["ref"])) + _qs, {}, undefined);
    }
  },
  {
    name: "repos/create-using-template",
    description: "Create a repository using a template. Creates a new repository using a repository template. Params: template_owner (required), template_repo (required).",
    inputSchema: {"type":"object","properties":{"template_owner":{"type":"string","description":"The account owner of the template repository. The name is not case sensitive."},"template_repo":{"type":"string","description":"The name of the template repository without the `.git` extension. The name is not case sensitive."},"body":{"type":"object","description":"Request body"}},"required":["template_owner","template_repo","body"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("POST", ("https://api.github.com/repos/" + String(args["template_owner"]) + "/" + String(args["template_repo"]) + "/generate") + _qs, {}, args["body"]);
    }
  },
  {
    name: "repos/list-public",
    description: "List public repositories. Lists all public repositories in the order that they were created.",
    inputSchema: {"type":"object","properties":{"since":{"type":"integer","description":"A repository ID. Only return repositories with an ID greater than this ID."}},"required":[]},
    call: function(args) {
    var _q = new URLSearchParams();
    if (args["since"] !== undefined) _q.set("since", String(args["since"]));
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/repositories") + _qs, {}, undefined);
    }
  },
  {
    name: "search/code",
    description: "Search code. Searches for query terms inside of a file. Params: q (required).",
    inputSchema: {"type":"object","properties":{"q":{"type":"string","description":"The query contains one or more search keywords and qualifiers. Qualifiers allow you to limit your search to specific areas of GitHub. The REST API supports the same qualifiers as the web interface for GitHub. To learn more about the format of the query, see [Constructing a search query](https://docs.github.com/rest/search/search#constructing-a-search-query). See \"[Searching code](https://docs.github.com/search-github/searching-on-github/searching-code)\" for a detailed list of qualifiers."},"sort":{"type":"string","enum":["indexed"],"description":"**This field is closing down.** Sorts the results of your query. Can only be `indexed`, which indicates how recently a file has been indexed by the GitHub search infrastructure. Default: [best match](https://docs.github.com/rest/search/search#ranking-search-results)"},"order":{"type":"string","enum":["desc","asc"],"default":"desc","description":"**This field is closing down.** Determines whether the first search result returned is the highest number of matches (`desc`) or lowest number of matches (`asc`). This parameter is ignored unless you provide `sort`. "},"per_page":{"type":"integer","default":30,"description":"The number of results per page (max 100). For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""},"page":{"type":"integer","default":1,"description":"The page number of the results to fetch. For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""}},"required":["q"]},
    call: function(args) {
    var _q = new URLSearchParams();
    if (args["q"] !== undefined) _q.set("q", String(args["q"]));
    if (args["sort"] !== undefined) _q.set("sort", String(args["sort"]));
    if (args["order"] !== undefined) _q.set("order", String(args["order"]));
    if (args["per_page"] !== undefined) _q.set("per_page", String(args["per_page"]));
    if (args["page"] !== undefined) _q.set("page", String(args["page"]));
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/search/code") + _qs, {}, undefined);
    }
  },
  {
    name: "search/commits",
    description: "Search commits. Find commits via various criteria on the default branch (usually `main`). Params: q (required).",
    inputSchema: {"type":"object","properties":{"q":{"type":"string","description":"The query contains one or more search keywords and qualifiers. Qualifiers allow you to limit your search to specific areas of GitHub. The REST API supports the same qualifiers as the web interface for GitHub. To learn more about the format of the query, see [Constructing a search query](https://docs.github.com/rest/search/search#constructing-a-search-query). See \"[Searching commits](https://docs.github.com/search-github/searching-on-github/searching-commits)\" for a detailed list of qualifiers."},"sort":{"type":"string","enum":["author-date","committer-date"],"description":"Sorts the results of your query by `author-date` or `committer-date`. Default: [best match](https://docs.github.com/rest/search/search#ranking-search-results)"},"order":{"type":"string","enum":["desc","asc"],"default":"desc","description":"Determines whether the first search result returned is the highest number of matches (`desc`) or lowest number of matches (`asc`). This parameter is ignored unless you provide `sort`."},"per_page":{"type":"integer","default":30,"description":"The number of results per page (max 100). For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""},"page":{"type":"integer","default":1,"description":"The page number of the results to fetch. For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""}},"required":["q"]},
    call: function(args) {
    var _q = new URLSearchParams();
    if (args["q"] !== undefined) _q.set("q", String(args["q"]));
    if (args["sort"] !== undefined) _q.set("sort", String(args["sort"]));
    if (args["order"] !== undefined) _q.set("order", String(args["order"]));
    if (args["per_page"] !== undefined) _q.set("per_page", String(args["per_page"]));
    if (args["page"] !== undefined) _q.set("page", String(args["page"]));
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/search/commits") + _qs, {}, undefined);
    }
  },
  {
    name: "search/issues-and-pull-requests",
    description: "Search issues and pull requests. Find issues by state and keyword. Params: q (required).",
    inputSchema: {"type":"object","properties":{"q":{"type":"string","description":"The query contains one or more search keywords and qualifiers. Qualifiers allow you to limit your search to specific areas of GitHub. The REST API supports the same qualifiers as the web interface for GitHub. To learn more about the format of the query, see [Constructing a search query](https://docs.github.com/rest/search/search#constructing-a-search-query). See \"[Searching issues and pull requests](https://docs.github.com/search-github/searching-on-github/searching-issues-and-pull-requests)\" for a detailed list of qualifiers."},"sort":{"type":"string","enum":["comments","reactions","reactions-+1","reactions--1","reactions-smile","reactions-thinking_face","reactions-heart","reactions-tada","interactions","created","updated"],"description":"Sorts the results of your query by the number of `comments`, `reactions`, `reactions-+1`, `reactions--1`, `reactions-smile`, `reactions-thinking_face`, `reactions-heart`, `reactions-tada`, or `interactions`. You can also sort results by how recently the items were `created` or `updated`, Default: [best match](https://docs.github.com/rest/search/search#ranking-search-results)"},"order":{"type":"string","enum":["desc","asc"],"default":"desc","description":"Determines whether the first search result returned is the highest number of matches (`desc`) or lowest number of matches (`asc`). This parameter is ignored unless you provide `sort`."},"per_page":{"type":"integer","default":30,"description":"The number of results per page (max 100). For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""},"page":{"type":"integer","default":1,"description":"The page number of the results to fetch. For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""},"advanced_search":{"type":"string","description":"Set to `true` to use advanced search.\nExample: `http://api.github.com/search/issues?q={query}&advanced_search=true`"},"search_type":{"type":"string","enum":["semantic","hybrid"],"description":"The type of search to perform on issues. When not specified, the default is lexical search.\n\n- `semantic` — performs a pure semantic (vector) search using embedding-based understanding.\n- `hybrid` — combines semantic search with lexical search for best results.\n\nSemantic and hybrid search require authentication and are rate limited to 10 requests per minute.\nOnly applies to issue searches (`/search/issues`)."}},"required":["q"]},
    call: function(args) {
    var _q = new URLSearchParams();
    if (args["q"] !== undefined) _q.set("q", String(args["q"]));
    if (args["sort"] !== undefined) _q.set("sort", String(args["sort"]));
    if (args["order"] !== undefined) _q.set("order", String(args["order"]));
    if (args["per_page"] !== undefined) _q.set("per_page", String(args["per_page"]));
    if (args["page"] !== undefined) _q.set("page", String(args["page"]));
    if (args["advanced_search"] !== undefined) _q.set("advanced_search", String(args["advanced_search"]));
    if (args["search_type"] !== undefined) _q.set("search_type", String(args["search_type"]));
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/search/issues") + _qs, {}, undefined);
    }
  },
  {
    name: "search/labels",
    description: "Search labels. Find labels in a repository with names or descriptions that match search keywords. Params: repository_id (required), q (required).",
    inputSchema: {"type":"object","properties":{"repository_id":{"type":"integer","description":"The id of the repository."},"q":{"type":"string","description":"The search keywords. This endpoint does not accept qualifiers in the query. To learn more about the format of the query, see [Constructing a search query](https://docs.github.com/rest/search/search#constructing-a-search-query)."},"sort":{"type":"string","enum":["created","updated"],"description":"Sorts the results of your query by when the label was `created` or `updated`. Default: [best match](https://docs.github.com/rest/search/search#ranking-search-results)"},"order":{"type":"string","enum":["desc","asc"],"default":"desc","description":"Determines whether the first search result returned is the highest number of matches (`desc`) or lowest number of matches (`asc`). This parameter is ignored unless you provide `sort`."},"per_page":{"type":"integer","default":30,"description":"The number of results per page (max 100). For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""},"page":{"type":"integer","default":1,"description":"The page number of the results to fetch. For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""}},"required":["repository_id","q"]},
    call: function(args) {
    var _q = new URLSearchParams();
    if (args["repository_id"] !== undefined) _q.set("repository_id", String(args["repository_id"]));
    if (args["q"] !== undefined) _q.set("q", String(args["q"]));
    if (args["sort"] !== undefined) _q.set("sort", String(args["sort"]));
    if (args["order"] !== undefined) _q.set("order", String(args["order"]));
    if (args["per_page"] !== undefined) _q.set("per_page", String(args["per_page"]));
    if (args["page"] !== undefined) _q.set("page", String(args["page"]));
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/search/labels") + _qs, {}, undefined);
    }
  },
  {
    name: "search/repos",
    description: "Search repositories. Find repositories via various criteria. Params: q (required).",
    inputSchema: {"type":"object","properties":{"q":{"type":"string","description":"The query contains one or more search keywords and qualifiers. Qualifiers allow you to limit your search to specific areas of GitHub. The REST API supports the same qualifiers as the web interface for GitHub. To learn more about the format of the query, see [Constructing a search query](https://docs.github.com/rest/search/search#constructing-a-search-query). See \"[Searching for repositories](https://docs.github.com/articles/searching-for-repositories/)\" for a detailed list of qualifiers."},"sort":{"type":"string","enum":["stars","forks","help-wanted-issues","updated"],"description":"Sorts the results of your query by number of `stars`, `forks`, or `help-wanted-issues` or how recently the items were `updated`. Default: [best match](https://docs.github.com/rest/search/search#ranking-search-results)"},"order":{"type":"string","enum":["desc","asc"],"default":"desc","description":"Determines whether the first search result returned is the highest number of matches (`desc`) or lowest number of matches (`asc`). This parameter is ignored unless you provide `sort`."},"per_page":{"type":"integer","default":30,"description":"The number of results per page (max 100). For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""},"page":{"type":"integer","default":1,"description":"The page number of the results to fetch. For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""}},"required":["q"]},
    call: function(args) {
    var _q = new URLSearchParams();
    if (args["q"] !== undefined) _q.set("q", String(args["q"]));
    if (args["sort"] !== undefined) _q.set("sort", String(args["sort"]));
    if (args["order"] !== undefined) _q.set("order", String(args["order"]));
    if (args["per_page"] !== undefined) _q.set("per_page", String(args["per_page"]));
    if (args["page"] !== undefined) _q.set("page", String(args["page"]));
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/search/repositories") + _qs, {}, undefined);
    }
  },
  {
    name: "search/topics",
    description: "Search topics. Find topics via various criteria. Params: q (required).",
    inputSchema: {"type":"object","properties":{"q":{"type":"string","description":"The query contains one or more search keywords and qualifiers. Qualifiers allow you to limit your search to specific areas of GitHub. The REST API supports the same qualifiers as the web interface for GitHub. To learn more about the format of the query, see [Constructing a search query](https://docs.github.com/rest/search/search#constructing-a-search-query)."},"per_page":{"type":"integer","default":30,"description":"The number of results per page (max 100). For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""},"page":{"type":"integer","default":1,"description":"The page number of the results to fetch. For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""}},"required":["q"]},
    call: function(args) {
    var _q = new URLSearchParams();
    if (args["q"] !== undefined) _q.set("q", String(args["q"]));
    if (args["per_page"] !== undefined) _q.set("per_page", String(args["per_page"]));
    if (args["page"] !== undefined) _q.set("page", String(args["page"]));
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/search/topics") + _qs, {}, undefined);
    }
  },
  {
    name: "search/users",
    description: "Search users. Find users via various criteria. Params: q (required).",
    inputSchema: {"type":"object","properties":{"q":{"type":"string","description":"The query contains one or more search keywords and qualifiers. Qualifiers allow you to limit your search to specific areas of GitHub. The REST API supports the same qualifiers as the web interface for GitHub. To learn more about the format of the query, see [Constructing a search query](https://docs.github.com/rest/search/search#constructing-a-search-query). See \"[Searching users](https://docs.github.com/search-github/searching-on-github/searching-users)\" for a detailed list of qualifiers."},"sort":{"type":"string","enum":["followers","repositories","joined"],"description":"Sorts the results of your query by number of `followers` or `repositories`, or when the person `joined` GitHub. Default: [best match](https://docs.github.com/rest/search/search#ranking-search-results)"},"order":{"type":"string","enum":["desc","asc"],"default":"desc","description":"Determines whether the first search result returned is the highest number of matches (`desc`) or lowest number of matches (`asc`). This parameter is ignored unless you provide `sort`."},"per_page":{"type":"integer","default":30,"description":"The number of results per page (max 100). For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""},"page":{"type":"integer","default":1,"description":"The page number of the results to fetch. For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""}},"required":["q"]},
    call: function(args) {
    var _q = new URLSearchParams();
    if (args["q"] !== undefined) _q.set("q", String(args["q"]));
    if (args["sort"] !== undefined) _q.set("sort", String(args["sort"]));
    if (args["order"] !== undefined) _q.set("order", String(args["order"]));
    if (args["per_page"] !== undefined) _q.set("per_page", String(args["per_page"]));
    if (args["page"] !== undefined) _q.set("page", String(args["page"]));
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/search/users") + _qs, {}, undefined);
    }
  },
  {
    name: "teams/get-legacy",
    description: "Get a team (Legacy). > [!WARNING]\n> **Endpoint closing down notice:** This endpoint route is closing down and will be removed from the Teams API. Params: team_id (required).",
    inputSchema: {"type":"object","properties":{"team_id":{"type":"integer","description":"The unique identifier of the team."}},"required":["team_id"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/teams/" + String(args["team_id"])) + _qs, {}, undefined);
    }
  },
  {
    name: "teams/update-legacy",
    description: "Update a team (Legacy). > [!WARNING]\n> **Endpoint closing down notice:** This endpoint route is closing down and will be removed from the Teams API. Params: team_id (required).",
    inputSchema: {"type":"object","properties":{"team_id":{"type":"integer","description":"The unique identifier of the team."},"body":{"type":"object","description":"Request body"}},"required":["team_id","body"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("PATCH", ("https://api.github.com/teams/" + String(args["team_id"])) + _qs, {}, args["body"]);
    }
  },
  {
    name: "teams/delete-legacy",
    description: "Delete a team (Legacy). > [!WARNING]\n> **Endpoint closing down notice:** This endpoint route is closing down and will be removed from the Teams API. Params: team_id (required).",
    inputSchema: {"type":"object","properties":{"team_id":{"type":"integer","description":"The unique identifier of the team."}},"required":["team_id"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("DELETE", ("https://api.github.com/teams/" + String(args["team_id"])) + _qs, {}, undefined);
    }
  },
  {
    name: "teams/list-pending-invitations-legacy",
    description: "List pending team invitations (Legacy). > [!WARNING]\n> **Endpoint closing down notice:** This endpoint route is closing down and will be removed from the Teams API. Params: team_id (required).",
    inputSchema: {"type":"object","properties":{"team_id":{"type":"integer","description":"The unique identifier of the team."},"per_page":{"type":"integer","default":30,"description":"The number of results per page (max 100). For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""},"page":{"type":"integer","default":1,"description":"The page number of the results to fetch. For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""}},"required":["team_id"]},
    call: function(args) {
    var _q = new URLSearchParams();
    if (args["per_page"] !== undefined) _q.set("per_page", String(args["per_page"]));
    if (args["page"] !== undefined) _q.set("page", String(args["page"]));
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/teams/" + String(args["team_id"]) + "/invitations") + _qs, {}, undefined);
    }
  },
  {
    name: "teams/list-members-legacy",
    description: "List team members (Legacy). > [!WARNING]\n> **Endpoint closing down notice:** This endpoint route is closing down and will be removed from the Teams API. Params: team_id (required).",
    inputSchema: {"type":"object","properties":{"team_id":{"type":"integer","description":"The unique identifier of the team."},"role":{"type":"string","enum":["member","maintainer","all"],"default":"all","description":"Filters members returned by their role in the team."},"per_page":{"type":"integer","default":30,"description":"The number of results per page (max 100). For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""},"page":{"type":"integer","default":1,"description":"The page number of the results to fetch. For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""}},"required":["team_id"]},
    call: function(args) {
    var _q = new URLSearchParams();
    if (args["role"] !== undefined) _q.set("role", String(args["role"]));
    if (args["per_page"] !== undefined) _q.set("per_page", String(args["per_page"]));
    if (args["page"] !== undefined) _q.set("page", String(args["page"]));
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/teams/" + String(args["team_id"]) + "/members") + _qs, {}, undefined);
    }
  },
  {
    name: "teams/get-member-legacy",
    description: "Get team member (Legacy). The \"Get team member\" endpoint (described below) is closing down. Params: team_id (required), username (required).",
    inputSchema: {"type":"object","properties":{"team_id":{"type":"integer","description":"The unique identifier of the team."},"username":{"type":"string","description":"The handle for the GitHub user account."}},"required":["team_id","username"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/teams/" + String(args["team_id"]) + "/members/" + String(args["username"])) + _qs, {}, undefined);
    }
  },
  {
    name: "teams/add-member-legacy",
    description: "Add team member (Legacy). The \"Add team member\" endpoint (described below) is closing down. Params: team_id (required), username (required).",
    inputSchema: {"type":"object","properties":{"team_id":{"type":"integer","description":"The unique identifier of the team."},"username":{"type":"string","description":"The handle for the GitHub user account."}},"required":["team_id","username"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("PUT", ("https://api.github.com/teams/" + String(args["team_id"]) + "/members/" + String(args["username"])) + _qs, {}, undefined);
    }
  },
  {
    name: "teams/remove-member-legacy",
    description: "Remove team member (Legacy). The \"Remove team member\" endpoint (described below) is closing down. Params: team_id (required), username (required).",
    inputSchema: {"type":"object","properties":{"team_id":{"type":"integer","description":"The unique identifier of the team."},"username":{"type":"string","description":"The handle for the GitHub user account."}},"required":["team_id","username"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("DELETE", ("https://api.github.com/teams/" + String(args["team_id"]) + "/members/" + String(args["username"])) + _qs, {}, undefined);
    }
  },
  {
    name: "teams/get-membership-for-user-legacy",
    description: "Get team membership for a user (Legacy). > [!WARNING]\n> **Endpoint closing down notice:** This endpoint route is closing down and will be removed from the Teams API. Params: team_id (required), username (required).",
    inputSchema: {"type":"object","properties":{"team_id":{"type":"integer","description":"The unique identifier of the team."},"username":{"type":"string","description":"The handle for the GitHub user account."}},"required":["team_id","username"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/teams/" + String(args["team_id"]) + "/memberships/" + String(args["username"])) + _qs, {}, undefined);
    }
  },
  {
    name: "teams/add-or-update-membership-for-user-legacy",
    description: "Add or update team membership for a user (Legacy). > [!WARNING]\n> **Endpoint closing down notice:** This endpoint route is closing down and will be removed from the Teams API. Params: team_id (required), username (required).",
    inputSchema: {"type":"object","properties":{"team_id":{"type":"integer","description":"The unique identifier of the team."},"username":{"type":"string","description":"The handle for the GitHub user account."},"body":{"type":"object","description":"Request body"}},"required":["team_id","username"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("PUT", ("https://api.github.com/teams/" + String(args["team_id"]) + "/memberships/" + String(args["username"])) + _qs, {}, args["body"]);
    }
  },
  {
    name: "teams/remove-membership-for-user-legacy",
    description: "Remove team membership for a user (Legacy). > [!WARNING]\n> **Endpoint closing down notice:** This endpoint route is closing down and will be removed from the Teams API. Params: team_id (required), username (required).",
    inputSchema: {"type":"object","properties":{"team_id":{"type":"integer","description":"The unique identifier of the team."},"username":{"type":"string","description":"The handle for the GitHub user account."}},"required":["team_id","username"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("DELETE", ("https://api.github.com/teams/" + String(args["team_id"]) + "/memberships/" + String(args["username"])) + _qs, {}, undefined);
    }
  },
  {
    name: "teams/list-repos-legacy",
    description: "List team repositories (Legacy). > [!WARNING]\n> **Endpoint closing down notice:** This endpoint route is closing down and will be removed from the Teams API. Params: team_id (required).",
    inputSchema: {"type":"object","properties":{"team_id":{"type":"integer","description":"The unique identifier of the team."},"per_page":{"type":"integer","default":30,"description":"The number of results per page (max 100). For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""},"page":{"type":"integer","default":1,"description":"The page number of the results to fetch. For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""}},"required":["team_id"]},
    call: function(args) {
    var _q = new URLSearchParams();
    if (args["per_page"] !== undefined) _q.set("per_page", String(args["per_page"]));
    if (args["page"] !== undefined) _q.set("page", String(args["page"]));
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/teams/" + String(args["team_id"]) + "/repos") + _qs, {}, undefined);
    }
  },
  {
    name: "teams/check-permissions-for-repo-legacy",
    description: "Check team permissions for a repository (Legacy). > [!WARNING]\n> **Endpoint closing down notice:** This endpoint route is closing down and will be removed from the Teams API. Params: team_id (required), owner (required), repo (required).",
    inputSchema: {"type":"object","properties":{"team_id":{"type":"integer","description":"The unique identifier of the team."},"owner":{"type":"string","description":"The account owner of the repository. The name is not case sensitive."},"repo":{"type":"string","description":"The name of the repository without the `.git` extension. The name is not case sensitive."}},"required":["team_id","owner","repo"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/teams/" + String(args["team_id"]) + "/repos/" + String(args["owner"]) + "/" + String(args["repo"])) + _qs, {}, undefined);
    }
  },
  {
    name: "teams/add-or-update-repo-permissions-legacy",
    description: "Add or update team repository permissions (Legacy). > [!WARNING]\n> **Endpoint closing down notice:** This endpoint route is closing down and will be removed from the Teams API. Params: team_id (required), owner (required), repo (required).",
    inputSchema: {"type":"object","properties":{"team_id":{"type":"integer","description":"The unique identifier of the team."},"owner":{"type":"string","description":"The account owner of the repository. The name is not case sensitive."},"repo":{"type":"string","description":"The name of the repository without the `.git` extension. The name is not case sensitive."},"body":{"type":"object","description":"Request body"}},"required":["team_id","owner","repo"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("PUT", ("https://api.github.com/teams/" + String(args["team_id"]) + "/repos/" + String(args["owner"]) + "/" + String(args["repo"])) + _qs, {}, args["body"]);
    }
  },
  {
    name: "teams/remove-repo-legacy",
    description: "Remove a repository from a team (Legacy). > [!WARNING]\n> **Endpoint closing down notice:** This endpoint route is closing down and will be removed from the Teams API. Params: team_id (required), owner (required), repo (required).",
    inputSchema: {"type":"object","properties":{"team_id":{"type":"integer","description":"The unique identifier of the team."},"owner":{"type":"string","description":"The account owner of the repository. The name is not case sensitive."},"repo":{"type":"string","description":"The name of the repository without the `.git` extension. The name is not case sensitive."}},"required":["team_id","owner","repo"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("DELETE", ("https://api.github.com/teams/" + String(args["team_id"]) + "/repos/" + String(args["owner"]) + "/" + String(args["repo"])) + _qs, {}, undefined);
    }
  },
  {
    name: "teams/list-child-legacy",
    description: "List child teams (Legacy). > [!WARNING]\n> **Endpoint closing down notice:** This endpoint route is closing down and will be removed from the Teams API. Params: team_id (required).",
    inputSchema: {"type":"object","properties":{"team_id":{"type":"integer","description":"The unique identifier of the team."},"per_page":{"type":"integer","default":30,"description":"The number of results per page (max 100). For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""},"page":{"type":"integer","default":1,"description":"The page number of the results to fetch. For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""}},"required":["team_id"]},
    call: function(args) {
    var _q = new URLSearchParams();
    if (args["per_page"] !== undefined) _q.set("per_page", String(args["per_page"]));
    if (args["page"] !== undefined) _q.set("page", String(args["page"]));
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/teams/" + String(args["team_id"]) + "/teams") + _qs, {}, undefined);
    }
  },
  {
    name: "users/get-authenticated",
    description: "Get the authenticated user. OAuth app tokens and personal access tokens (classic) need the `user` scope in order for the response to include private profile information.",
    inputSchema: {"type":"object","properties":{},"required":[]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/user") + _qs, {}, undefined);
    }
  },
  {
    name: "users/update-authenticated",
    description: "Update the authenticated user. **Note:** If your email is set to private and you send an `email` parameter as part of this request to update your profile, your privacy settings are still enforced: the email address will not be displayed on your public profile or via the API.",
    inputSchema: {"type":"object","properties":{"body":{"type":"object","description":"Request body"}},"required":[]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("PATCH", ("https://api.github.com/user") + _qs, {}, args["body"]);
    }
  },
  {
    name: "users/list-blocked-by-authenticated-user",
    description: "List users blocked by the authenticated user. List the users you've blocked on your personal account.",
    inputSchema: {"type":"object","properties":{"per_page":{"type":"integer","default":30,"description":"The number of results per page (max 100). For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""},"page":{"type":"integer","default":1,"description":"The page number of the results to fetch. For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""}},"required":[]},
    call: function(args) {
    var _q = new URLSearchParams();
    if (args["per_page"] !== undefined) _q.set("per_page", String(args["per_page"]));
    if (args["page"] !== undefined) _q.set("page", String(args["page"]));
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/user/blocks") + _qs, {}, undefined);
    }
  },
  {
    name: "users/check-blocked",
    description: "Check if a user is blocked by the authenticated user. Returns a 204 if the given user is blocked by the authenticated user. Params: username (required).",
    inputSchema: {"type":"object","properties":{"username":{"type":"string","description":"The handle for the GitHub user account."}},"required":["username"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/user/blocks/" + String(args["username"])) + _qs, {}, undefined);
    }
  },
  {
    name: "users/block",
    description: "Block a user. Blocks the given user and returns a 204. Params: username (required).",
    inputSchema: {"type":"object","properties":{"username":{"type":"string","description":"The handle for the GitHub user account."}},"required":["username"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("PUT", ("https://api.github.com/user/blocks/" + String(args["username"])) + _qs, {}, undefined);
    }
  },
  {
    name: "users/unblock",
    description: "Unblock a user. Unblocks the given user and returns a 204. Params: username (required).",
    inputSchema: {"type":"object","properties":{"username":{"type":"string","description":"The handle for the GitHub user account."}},"required":["username"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("DELETE", ("https://api.github.com/user/blocks/" + String(args["username"])) + _qs, {}, undefined);
    }
  },
  {
    name: "codespaces/list-for-authenticated-user",
    description: "List codespaces for the authenticated user. Lists the authenticated user's codespaces.",
    inputSchema: {"type":"object","properties":{"per_page":{"type":"integer","default":30,"description":"The number of results per page (max 100). For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""},"page":{"type":"integer","default":1,"description":"The page number of the results to fetch. For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""},"repository_id":{"type":"integer","description":"ID of the Repository to filter on"}},"required":[]},
    call: function(args) {
    var _q = new URLSearchParams();
    if (args["per_page"] !== undefined) _q.set("per_page", String(args["per_page"]));
    if (args["page"] !== undefined) _q.set("page", String(args["page"]));
    if (args["repository_id"] !== undefined) _q.set("repository_id", String(args["repository_id"]));
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/user/codespaces") + _qs, {}, undefined);
    }
  },
  {
    name: "codespaces/create-for-authenticated-user",
    description: "Create a codespace for the authenticated user. Creates a new codespace, owned by the authenticated user.",
    inputSchema: {"type":"object","properties":{"body":{"type":"object","description":"Request body"}},"required":["body"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("POST", ("https://api.github.com/user/codespaces") + _qs, {}, args["body"]);
    }
  },
  {
    name: "codespaces/list-secrets-for-authenticated-user",
    description: "List secrets for the authenticated user. Lists all development environment secrets available for a user's codespaces without revealing their\nencrypted values.",
    inputSchema: {"type":"object","properties":{"per_page":{"type":"integer","default":30,"description":"The number of results per page (max 100). For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""},"page":{"type":"integer","default":1,"description":"The page number of the results to fetch. For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""}},"required":[]},
    call: function(args) {
    var _q = new URLSearchParams();
    if (args["per_page"] !== undefined) _q.set("per_page", String(args["per_page"]));
    if (args["page"] !== undefined) _q.set("page", String(args["page"]));
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/user/codespaces/secrets") + _qs, {}, undefined);
    }
  },
  {
    name: "codespaces/get-public-key-for-authenticated-user",
    description: "Get public key for the authenticated user. Gets your public key, which you need to encrypt secrets.",
    inputSchema: {"type":"object","properties":{},"required":[]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/user/codespaces/secrets/public-key") + _qs, {}, undefined);
    }
  },
  {
    name: "codespaces/get-secret-for-authenticated-user",
    description: "Get a secret for the authenticated user. Gets a development environment secret available to a user's codespaces without revealing its encrypted value. Params: secret_name (required).",
    inputSchema: {"type":"object","properties":{"secret_name":{"type":"string","description":"The name of the secret."}},"required":["secret_name"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/user/codespaces/secrets/" + String(args["secret_name"])) + _qs, {}, undefined);
    }
  },
  {
    name: "codespaces/create-or-update-secret-for-authenticated-user",
    description: "Create or update a secret for the authenticated user. Creates or updates a development environment secret for a user's codespace with an encrypted value. Params: secret_name (required).",
    inputSchema: {"type":"object","properties":{"secret_name":{"type":"string","description":"The name of the secret."},"body":{"type":"object","description":"Request body"}},"required":["secret_name","body"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("PUT", ("https://api.github.com/user/codespaces/secrets/" + String(args["secret_name"])) + _qs, {}, args["body"]);
    }
  },
  {
    name: "codespaces/delete-secret-for-authenticated-user",
    description: "Delete a secret for the authenticated user. Deletes a development environment secret from a user's codespaces using the secret name. Params: secret_name (required).",
    inputSchema: {"type":"object","properties":{"secret_name":{"type":"string","description":"The name of the secret."}},"required":["secret_name"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("DELETE", ("https://api.github.com/user/codespaces/secrets/" + String(args["secret_name"])) + _qs, {}, undefined);
    }
  },
  {
    name: "codespaces/list-repositories-for-secret-for-authenticated-user",
    description: "List selected repositories for a user secret. List the repositories that have been granted the ability to use a user's development environment secret. Params: secret_name (required).",
    inputSchema: {"type":"object","properties":{"secret_name":{"type":"string","description":"The name of the secret."}},"required":["secret_name"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/user/codespaces/secrets/" + String(args["secret_name"]) + "/repositories") + _qs, {}, undefined);
    }
  },
  {
    name: "codespaces/set-repositories-for-secret-for-authenticated-user",
    description: "Set selected repositories for a user secret. Select the repositories that will use a user's development environment secret. Params: secret_name (required).",
    inputSchema: {"type":"object","properties":{"secret_name":{"type":"string","description":"The name of the secret."},"body":{"type":"object","description":"Request body"}},"required":["secret_name","body"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("PUT", ("https://api.github.com/user/codespaces/secrets/" + String(args["secret_name"]) + "/repositories") + _qs, {}, args["body"]);
    }
  },
  {
    name: "codespaces/add-repository-for-secret-for-authenticated-user",
    description: "Add a selected repository to a user secret. Adds a repository to the selected repositories for a user's development environment secret. Params: secret_name (required), repository_id (required).",
    inputSchema: {"type":"object","properties":{"secret_name":{"type":"string","description":"The name of the secret."},"repository_id":{"type":"integer"}},"required":["secret_name","repository_id"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("PUT", ("https://api.github.com/user/codespaces/secrets/" + String(args["secret_name"]) + "/repositories/" + String(args["repository_id"])) + _qs, {}, undefined);
    }
  },
  {
    name: "codespaces/remove-repository-for-secret-for-authenticated-user",
    description: "Remove a selected repository from a user secret. Removes a repository from the selected repositories for a user's development environment secret. Params: secret_name (required), repository_id (required).",
    inputSchema: {"type":"object","properties":{"secret_name":{"type":"string","description":"The name of the secret."},"repository_id":{"type":"integer"}},"required":["secret_name","repository_id"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("DELETE", ("https://api.github.com/user/codespaces/secrets/" + String(args["secret_name"]) + "/repositories/" + String(args["repository_id"])) + _qs, {}, undefined);
    }
  },
  {
    name: "codespaces/get-for-authenticated-user",
    description: "Get a codespace for the authenticated user. Gets information about a user's codespace. Params: codespace_name (required).",
    inputSchema: {"type":"object","properties":{"codespace_name":{"type":"string","description":"The name of the codespace."}},"required":["codespace_name"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/user/codespaces/" + String(args["codespace_name"])) + _qs, {}, undefined);
    }
  },
  {
    name: "codespaces/update-for-authenticated-user",
    description: "Update a codespace for the authenticated user. Updates a codespace owned by the authenticated user. Params: codespace_name (required).",
    inputSchema: {"type":"object","properties":{"codespace_name":{"type":"string","description":"The name of the codespace."},"body":{"type":"object","description":"Request body"}},"required":["codespace_name"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("PATCH", ("https://api.github.com/user/codespaces/" + String(args["codespace_name"])) + _qs, {}, args["body"]);
    }
  },
  {
    name: "codespaces/delete-for-authenticated-user",
    description: "Delete a codespace for the authenticated user. Deletes a user's codespace. Params: codespace_name (required).",
    inputSchema: {"type":"object","properties":{"codespace_name":{"type":"string","description":"The name of the codespace."}},"required":["codespace_name"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("DELETE", ("https://api.github.com/user/codespaces/" + String(args["codespace_name"])) + _qs, {}, undefined);
    }
  },
  {
    name: "codespaces/export-for-authenticated-user",
    description: "Export a codespace for the authenticated user. Triggers an export of the specified codespace and returns a URL and ID where the status of the export can be monitored. Params: codespace_name (required).",
    inputSchema: {"type":"object","properties":{"codespace_name":{"type":"string","description":"The name of the codespace."}},"required":["codespace_name"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("POST", ("https://api.github.com/user/codespaces/" + String(args["codespace_name"]) + "/exports") + _qs, {}, undefined);
    }
  },
  {
    name: "codespaces/get-export-details-for-authenticated-user",
    description: "Get details about a codespace export. Gets information about an export of a codespace. Params: codespace_name (required), export_id (required).",
    inputSchema: {"type":"object","properties":{"codespace_name":{"type":"string","description":"The name of the codespace."},"export_id":{"type":"string","description":"The ID of the export operation, or `latest`. Currently only `latest` is currently supported."}},"required":["codespace_name","export_id"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/user/codespaces/" + String(args["codespace_name"]) + "/exports/" + String(args["export_id"])) + _qs, {}, undefined);
    }
  },
  {
    name: "codespaces/codespace-machines-for-authenticated-user",
    description: "List machine types for a codespace. List the machine types a codespace can transition to use. Params: codespace_name (required).",
    inputSchema: {"type":"object","properties":{"codespace_name":{"type":"string","description":"The name of the codespace."}},"required":["codespace_name"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/user/codespaces/" + String(args["codespace_name"]) + "/machines") + _qs, {}, undefined);
    }
  },
  {
    name: "codespaces/publish-for-authenticated-user",
    description: "Create a repository from an unpublished codespace. Publishes an unpublished codespace, creating a new repository and assigning it to the codespace. Params: codespace_name (required).",
    inputSchema: {"type":"object","properties":{"codespace_name":{"type":"string","description":"The name of the codespace."},"body":{"type":"object","description":"Request body"}},"required":["codespace_name","body"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("POST", ("https://api.github.com/user/codespaces/" + String(args["codespace_name"]) + "/publish") + _qs, {}, args["body"]);
    }
  },
  {
    name: "codespaces/start-for-authenticated-user",
    description: "Start a codespace for the authenticated user. Starts a user's codespace. Params: codespace_name (required).",
    inputSchema: {"type":"object","properties":{"codespace_name":{"type":"string","description":"The name of the codespace."}},"required":["codespace_name"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("POST", ("https://api.github.com/user/codespaces/" + String(args["codespace_name"]) + "/start") + _qs, {}, undefined);
    }
  },
  {
    name: "codespaces/stop-for-authenticated-user",
    description: "Stop a codespace for the authenticated user. Stops a user's codespace. Params: codespace_name (required).",
    inputSchema: {"type":"object","properties":{"codespace_name":{"type":"string","description":"The name of the codespace."}},"required":["codespace_name"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("POST", ("https://api.github.com/user/codespaces/" + String(args["codespace_name"]) + "/stop") + _qs, {}, undefined);
    }
  },
  {
    name: "packages/list-docker-migration-conflicting-packages-for-authenticated-user",
    description: "Get list of conflicting packages during Docker migration for authenticated-user. Lists all packages that are owned by the authenticated user within the user's namespace, and that encountered a conflict during a Docker migration.",
    inputSchema: {"type":"object","properties":{},"required":[]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/user/docker/conflicts") + _qs, {}, undefined);
    }
  },
  {
    name: "users/set-primary-email-visibility-for-authenticated-user",
    description: "Set primary email visibility for the authenticated user. Sets the visibility for your primary email addresses.",
    inputSchema: {"type":"object","properties":{"body":{"type":"object","description":"Request body"}},"required":["body"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("PATCH", ("https://api.github.com/user/email/visibility") + _qs, {}, args["body"]);
    }
  },
  {
    name: "users/list-emails-for-authenticated-user",
    description: "List email addresses for the authenticated user. Lists all of your email addresses, and specifies which one is visible\nto the public.",
    inputSchema: {"type":"object","properties":{"per_page":{"type":"integer","default":30,"description":"The number of results per page (max 100). For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""},"page":{"type":"integer","default":1,"description":"The page number of the results to fetch. For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""}},"required":[]},
    call: function(args) {
    var _q = new URLSearchParams();
    if (args["per_page"] !== undefined) _q.set("per_page", String(args["per_page"]));
    if (args["page"] !== undefined) _q.set("page", String(args["page"]));
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/user/emails") + _qs, {}, undefined);
    }
  },
  {
    name: "users/add-email-for-authenticated-user",
    description: "Add an email address for the authenticated user. OAuth app tokens and personal access tokens (classic) need the `user` scope to use this endpoint.",
    inputSchema: {"type":"object","properties":{"body":{"type":"object","description":"Request body"}},"required":[]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("POST", ("https://api.github.com/user/emails") + _qs, {}, args["body"]);
    }
  },
  {
    name: "users/delete-email-for-authenticated-user",
    description: "Delete an email address for the authenticated user. OAuth app tokens and personal access tokens (classic) need the `user` scope to use this endpoint.",
    inputSchema: {"type":"object","properties":{"body":{"type":"object","description":"Request body"}},"required":[]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("DELETE", ("https://api.github.com/user/emails") + _qs, {}, args["body"]);
    }
  },
  {
    name: "users/list-followers-for-authenticated-user",
    description: "List followers of the authenticated user. Lists the people following the authenticated user.",
    inputSchema: {"type":"object","properties":{"per_page":{"type":"integer","default":30,"description":"The number of results per page (max 100). For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""},"page":{"type":"integer","default":1,"description":"The page number of the results to fetch. For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""}},"required":[]},
    call: function(args) {
    var _q = new URLSearchParams();
    if (args["per_page"] !== undefined) _q.set("per_page", String(args["per_page"]));
    if (args["page"] !== undefined) _q.set("page", String(args["page"]));
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/user/followers") + _qs, {}, undefined);
    }
  },
  {
    name: "users/list-followed-by-authenticated-user",
    description: "List the people the authenticated user follows. Lists the people who the authenticated user follows.",
    inputSchema: {"type":"object","properties":{"per_page":{"type":"integer","default":30,"description":"The number of results per page (max 100). For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""},"page":{"type":"integer","default":1,"description":"The page number of the results to fetch. For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""}},"required":[]},
    call: function(args) {
    var _q = new URLSearchParams();
    if (args["per_page"] !== undefined) _q.set("per_page", String(args["per_page"]));
    if (args["page"] !== undefined) _q.set("page", String(args["page"]));
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/user/following") + _qs, {}, undefined);
    }
  },
  {
    name: "users/check-person-is-followed-by-authenticated",
    description: "Check if a person is followed by the authenticated user. Params: username (required).",
    inputSchema: {"type":"object","properties":{"username":{"type":"string","description":"The handle for the GitHub user account."}},"required":["username"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/user/following/" + String(args["username"])) + _qs, {}, undefined);
    }
  },
  {
    name: "users/follow",
    description: "Follow a user. Note that you'll need to set `Content-Length` to zero when calling out to this endpoint. Params: username (required).",
    inputSchema: {"type":"object","properties":{"username":{"type":"string","description":"The handle for the GitHub user account."}},"required":["username"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("PUT", ("https://api.github.com/user/following/" + String(args["username"])) + _qs, {}, undefined);
    }
  },
  {
    name: "users/unfollow",
    description: "Unfollow a user. OAuth app tokens and personal access tokens (classic) need the `user:follow` scope to use this endpoint. Params: username (required).",
    inputSchema: {"type":"object","properties":{"username":{"type":"string","description":"The handle for the GitHub user account."}},"required":["username"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("DELETE", ("https://api.github.com/user/following/" + String(args["username"])) + _qs, {}, undefined);
    }
  },
  {
    name: "users/list-gpg-keys-for-authenticated-user",
    description: "List GPG keys for the authenticated user. Lists the current user's GPG keys.",
    inputSchema: {"type":"object","properties":{"per_page":{"type":"integer","default":30,"description":"The number of results per page (max 100). For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""},"page":{"type":"integer","default":1,"description":"The page number of the results to fetch. For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""}},"required":[]},
    call: function(args) {
    var _q = new URLSearchParams();
    if (args["per_page"] !== undefined) _q.set("per_page", String(args["per_page"]));
    if (args["page"] !== undefined) _q.set("page", String(args["page"]));
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/user/gpg_keys") + _qs, {}, undefined);
    }
  },
  {
    name: "users/create-gpg-key-for-authenticated-user",
    description: "Create a GPG key for the authenticated user. Adds a GPG key to the authenticated user's GitHub account.",
    inputSchema: {"type":"object","properties":{"body":{"type":"object","description":"Request body"}},"required":["body"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("POST", ("https://api.github.com/user/gpg_keys") + _qs, {}, args["body"]);
    }
  },
  {
    name: "users/get-gpg-key-for-authenticated-user",
    description: "Get a GPG key for the authenticated user. View extended details for a single GPG key. Params: gpg_key_id (required).",
    inputSchema: {"type":"object","properties":{"gpg_key_id":{"type":"integer","description":"The unique identifier of the GPG key."}},"required":["gpg_key_id"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/user/gpg_keys/" + String(args["gpg_key_id"])) + _qs, {}, undefined);
    }
  },
  {
    name: "users/delete-gpg-key-for-authenticated-user",
    description: "Delete a GPG key for the authenticated user. Removes a GPG key from the authenticated user's GitHub account. Params: gpg_key_id (required).",
    inputSchema: {"type":"object","properties":{"gpg_key_id":{"type":"integer","description":"The unique identifier of the GPG key."}},"required":["gpg_key_id"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("DELETE", ("https://api.github.com/user/gpg_keys/" + String(args["gpg_key_id"])) + _qs, {}, undefined);
    }
  },
  {
    name: "apps/list-installations-for-authenticated-user",
    description: "List app installations accessible to the user access token. Lists installations of your GitHub App that the authenticated user has explicit permission (`:read`, `:write`, or `:admin`) to access.",
    inputSchema: {"type":"object","properties":{"per_page":{"type":"integer","default":30,"description":"The number of results per page (max 100). For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""},"page":{"type":"integer","default":1,"description":"The page number of the results to fetch. For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""}},"required":[]},
    call: function(args) {
    var _q = new URLSearchParams();
    if (args["per_page"] !== undefined) _q.set("per_page", String(args["per_page"]));
    if (args["page"] !== undefined) _q.set("page", String(args["page"]));
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/user/installations") + _qs, {}, undefined);
    }
  },
  {
    name: "apps/list-installation-repos-for-authenticated-user",
    description: "List repositories accessible to the user access token. List repositories that the authenticated user has explicit permission (`:read`, `:write`, or `:admin`) to access for an installation. Params: installation_id (required).",
    inputSchema: {"type":"object","properties":{"installation_id":{"type":"integer","description":"The unique identifier of the installation."},"per_page":{"type":"integer","default":30,"description":"The number of results per page (max 100). For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""},"page":{"type":"integer","default":1,"description":"The page number of the results to fetch. For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""}},"required":["installation_id"]},
    call: function(args) {
    var _q = new URLSearchParams();
    if (args["per_page"] !== undefined) _q.set("per_page", String(args["per_page"]));
    if (args["page"] !== undefined) _q.set("page", String(args["page"]));
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/user/installations/" + String(args["installation_id"]) + "/repositories") + _qs, {}, undefined);
    }
  },
  {
    name: "apps/add-repo-to-installation-for-authenticated-user",
    description: "Add a repository to an app installation. Add a single repository to an installation. Params: installation_id (required), repository_id (required).",
    inputSchema: {"type":"object","properties":{"installation_id":{"type":"integer","description":"The unique identifier of the installation."},"repository_id":{"type":"integer","description":"The unique identifier of the repository."}},"required":["installation_id","repository_id"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("PUT", ("https://api.github.com/user/installations/" + String(args["installation_id"]) + "/repositories/" + String(args["repository_id"])) + _qs, {}, undefined);
    }
  },
  {
    name: "apps/remove-repo-from-installation-for-authenticated-user",
    description: "Remove a repository from an app installation. Remove a single repository from an installation. Params: installation_id (required), repository_id (required).",
    inputSchema: {"type":"object","properties":{"installation_id":{"type":"integer","description":"The unique identifier of the installation."},"repository_id":{"type":"integer","description":"The unique identifier of the repository."}},"required":["installation_id","repository_id"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("DELETE", ("https://api.github.com/user/installations/" + String(args["installation_id"]) + "/repositories/" + String(args["repository_id"])) + _qs, {}, undefined);
    }
  },
  {
    name: "interactions/get-restrictions-for-authenticated-user",
    description: "Get interaction restrictions for your public repositories. Shows which type of GitHub user can interact with your public repositories and when the restriction expires.",
    inputSchema: {"type":"object","properties":{},"required":[]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/user/interaction-limits") + _qs, {}, undefined);
    }
  },
  {
    name: "interactions/set-restrictions-for-authenticated-user",
    description: "Set interaction restrictions for your public repositories. Temporarily restricts which type of GitHub user can interact with your public repositories.",
    inputSchema: {"type":"object","properties":{"body":{"type":"object","description":"Request body"}},"required":["body"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("PUT", ("https://api.github.com/user/interaction-limits") + _qs, {}, args["body"]);
    }
  },
  {
    name: "interactions/remove-restrictions-for-authenticated-user",
    description: "Remove interaction restrictions from your public repositories. Removes any interaction restrictions from your public repositories.",
    inputSchema: {"type":"object","properties":{},"required":[]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("DELETE", ("https://api.github.com/user/interaction-limits") + _qs, {}, undefined);
    }
  },
  {
    name: "issues/list-for-authenticated-user",
    description: "List user account issues assigned to the authenticated user. List issues across owned and member repositories assigned to the authenticated user.",
    inputSchema: {"type":"object","properties":{"filter":{"type":"string","enum":["assigned","created","mentioned","subscribed","repos","all"],"default":"assigned","description":"Indicates which sorts of issues to return. `assigned` means issues assigned to you. `created` means issues created by you. `mentioned` means issues mentioning you. `subscribed` means issues you're subscribed to updates for. `all` or `repos` means all issues you can see, regardless of participation or creation."},"state":{"type":"string","enum":["open","closed","all"],"default":"open","description":"Indicates the state of the issues to return."},"labels":{"type":"string","description":"A list of comma separated label names. Example: `bug,ui,@high`"},"sort":{"type":"string","enum":["created","updated","comments"],"default":"created","description":"What to sort results by."},"direction":{"type":"string","enum":["asc","desc"],"default":"desc","description":"The direction to sort the results by."},"since":{"type":"string","format":"date-time","description":"Only show results that were last updated after the given time. This is a timestamp in [ISO 8601](https://en.wikipedia.org/wiki/ISO_8601) format: `YYYY-MM-DDTHH:MM:SSZ`."},"per_page":{"type":"integer","default":30,"description":"The number of results per page (max 100). For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""},"page":{"type":"integer","default":1,"description":"The page number of the results to fetch. For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""}},"required":[]},
    call: function(args) {
    var _q = new URLSearchParams();
    if (args["filter"] !== undefined) _q.set("filter", String(args["filter"]));
    if (args["state"] !== undefined) _q.set("state", String(args["state"]));
    if (args["labels"] !== undefined) _q.set("labels", String(args["labels"]));
    if (args["sort"] !== undefined) _q.set("sort", String(args["sort"]));
    if (args["direction"] !== undefined) _q.set("direction", String(args["direction"]));
    if (args["since"] !== undefined) _q.set("since", String(args["since"]));
    if (args["per_page"] !== undefined) _q.set("per_page", String(args["per_page"]));
    if (args["page"] !== undefined) _q.set("page", String(args["page"]));
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/user/issues") + _qs, {}, undefined);
    }
  },
  {
    name: "users/list-public-ssh-keys-for-authenticated-user",
    description: "List public SSH keys for the authenticated user. Lists the public SSH keys for the authenticated user's GitHub account.",
    inputSchema: {"type":"object","properties":{"per_page":{"type":"integer","default":30,"description":"The number of results per page (max 100). For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""},"page":{"type":"integer","default":1,"description":"The page number of the results to fetch. For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""}},"required":[]},
    call: function(args) {
    var _q = new URLSearchParams();
    if (args["per_page"] !== undefined) _q.set("per_page", String(args["per_page"]));
    if (args["page"] !== undefined) _q.set("page", String(args["page"]));
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/user/keys") + _qs, {}, undefined);
    }
  },
  {
    name: "users/create-public-ssh-key-for-authenticated-user",
    description: "Create a public SSH key for the authenticated user. Adds a public SSH key to the authenticated user's GitHub account.",
    inputSchema: {"type":"object","properties":{"body":{"type":"object","description":"Request body"}},"required":["body"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("POST", ("https://api.github.com/user/keys") + _qs, {}, args["body"]);
    }
  },
  {
    name: "users/get-public-ssh-key-for-authenticated-user",
    description: "Get a public SSH key for the authenticated user. View extended details for a single public SSH key. Params: key_id (required).",
    inputSchema: {"type":"object","properties":{"key_id":{"type":"integer","description":"The unique identifier of the key."}},"required":["key_id"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/user/keys/" + String(args["key_id"])) + _qs, {}, undefined);
    }
  },
  {
    name: "users/delete-public-ssh-key-for-authenticated-user",
    description: "Delete a public SSH key for the authenticated user. Removes a public SSH key from the authenticated user's GitHub account. Params: key_id (required).",
    inputSchema: {"type":"object","properties":{"key_id":{"type":"integer","description":"The unique identifier of the key."}},"required":["key_id"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("DELETE", ("https://api.github.com/user/keys/" + String(args["key_id"])) + _qs, {}, undefined);
    }
  },
  {
    name: "apps/list-subscriptions-for-authenticated-user",
    description: "List subscriptions for the authenticated user. Lists the active subscriptions for the authenticated user.",
    inputSchema: {"type":"object","properties":{"per_page":{"type":"integer","default":30,"description":"The number of results per page (max 100). For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""},"page":{"type":"integer","default":1,"description":"The page number of the results to fetch. For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""}},"required":[]},
    call: function(args) {
    var _q = new URLSearchParams();
    if (args["per_page"] !== undefined) _q.set("per_page", String(args["per_page"]));
    if (args["page"] !== undefined) _q.set("page", String(args["page"]));
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/user/marketplace_purchases") + _qs, {}, undefined);
    }
  },
  {
    name: "apps/list-subscriptions-for-authenticated-user-stubbed",
    description: "List subscriptions for the authenticated user (stubbed). Lists the active subscriptions for the authenticated user.",
    inputSchema: {"type":"object","properties":{"per_page":{"type":"integer","default":30,"description":"The number of results per page (max 100). For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""},"page":{"type":"integer","default":1,"description":"The page number of the results to fetch. For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""}},"required":[]},
    call: function(args) {
    var _q = new URLSearchParams();
    if (args["per_page"] !== undefined) _q.set("per_page", String(args["per_page"]));
    if (args["page"] !== undefined) _q.set("page", String(args["page"]));
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/user/marketplace_purchases/stubbed") + _qs, {}, undefined);
    }
  },
  {
    name: "orgs/list-memberships-for-authenticated-user",
    description: "List organization memberships for the authenticated user. Lists all of the authenticated user's organization memberships.",
    inputSchema: {"type":"object","properties":{"state":{"type":"string","enum":["active","pending"],"description":"Indicates the state of the memberships to return. If not specified, the API returns both active and pending memberships."},"per_page":{"type":"integer","default":30,"description":"The number of results per page (max 100). For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""},"page":{"type":"integer","default":1,"description":"The page number of the results to fetch. For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""}},"required":[]},
    call: function(args) {
    var _q = new URLSearchParams();
    if (args["state"] !== undefined) _q.set("state", String(args["state"]));
    if (args["per_page"] !== undefined) _q.set("per_page", String(args["per_page"]));
    if (args["page"] !== undefined) _q.set("page", String(args["page"]));
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/user/memberships/orgs") + _qs, {}, undefined);
    }
  },
  {
    name: "orgs/get-membership-for-authenticated-user",
    description: "Get an organization membership for the authenticated user. If the authenticated user is an active or pending member of the organization, this endpoint will return the user's membership. Params: org (required).",
    inputSchema: {"type":"object","properties":{"org":{"type":"string","description":"The organization name. The name is not case sensitive."}},"required":["org"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/user/memberships/orgs/" + String(args["org"])) + _qs, {}, undefined);
    }
  },
  {
    name: "orgs/update-membership-for-authenticated-user",
    description: "Update an organization membership for the authenticated user. Converts the authenticated user to an active member of the organization, if that user has a pending invitation from the organization. Params: org (required).",
    inputSchema: {"type":"object","properties":{"org":{"type":"string","description":"The organization name. The name is not case sensitive."},"body":{"type":"object","description":"Request body"}},"required":["org","body"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("PATCH", ("https://api.github.com/user/memberships/orgs/" + String(args["org"])) + _qs, {}, args["body"]);
    }
  },
  {
    name: "migrations/list-for-authenticated-user",
    description: "List user migrations. Lists all migrations a user has started.",
    inputSchema: {"type":"object","properties":{"per_page":{"type":"integer","default":30,"description":"The number of results per page (max 100). For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""},"page":{"type":"integer","default":1,"description":"The page number of the results to fetch. For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""}},"required":[]},
    call: function(args) {
    var _q = new URLSearchParams();
    if (args["per_page"] !== undefined) _q.set("per_page", String(args["per_page"]));
    if (args["page"] !== undefined) _q.set("page", String(args["page"]));
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/user/migrations") + _qs, {}, undefined);
    }
  },
  {
    name: "migrations/start-for-authenticated-user",
    description: "Start a user migration. Initiates the generation of a user migration archive.",
    inputSchema: {"type":"object","properties":{"body":{"type":"object","description":"Request body"}},"required":["body"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("POST", ("https://api.github.com/user/migrations") + _qs, {}, args["body"]);
    }
  },
  {
    name: "migrations/get-status-for-authenticated-user",
    description: "Get a user migration status. Fetches a single user migration. Params: migration_id (required).",
    inputSchema: {"type":"object","properties":{"migration_id":{"type":"integer","description":"The unique identifier of the migration."},"exclude":{"type":"array","items":{"type":"string"}}},"required":["migration_id"]},
    call: function(args) {
    var _q = new URLSearchParams();
    if (args["exclude"] !== undefined) _q.set("exclude", String(args["exclude"]));
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/user/migrations/" + String(args["migration_id"])) + _qs, {}, undefined);
    }
  },
  {
    name: "migrations/get-archive-for-authenticated-user",
    description: "Download a user migration archive. Fetches the URL to download the migration archive as a `tar.gz` file. Params: migration_id (required).",
    inputSchema: {"type":"object","properties":{"migration_id":{"type":"integer","description":"The unique identifier of the migration."}},"required":["migration_id"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/user/migrations/" + String(args["migration_id"]) + "/archive") + _qs, {}, undefined);
    }
  },
  {
    name: "migrations/delete-archive-for-authenticated-user",
    description: "Delete a user migration archive. Deletes a previous migration archive. Params: migration_id (required).",
    inputSchema: {"type":"object","properties":{"migration_id":{"type":"integer","description":"The unique identifier of the migration."}},"required":["migration_id"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("DELETE", ("https://api.github.com/user/migrations/" + String(args["migration_id"]) + "/archive") + _qs, {}, undefined);
    }
  },
  {
    name: "migrations/unlock-repo-for-authenticated-user",
    description: "Unlock a user repository. Unlocks a repository. Params: migration_id (required), repo_name (required).",
    inputSchema: {"type":"object","properties":{"migration_id":{"type":"integer","description":"The unique identifier of the migration."},"repo_name":{"type":"string","description":"repo_name parameter"}},"required":["migration_id","repo_name"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("DELETE", ("https://api.github.com/user/migrations/" + String(args["migration_id"]) + "/repos/" + String(args["repo_name"]) + "/lock") + _qs, {}, undefined);
    }
  },
  {
    name: "migrations/list-repos-for-authenticated-user",
    description: "List repositories for a user migration. Lists all the repositories for this user migration. Params: migration_id (required).",
    inputSchema: {"type":"object","properties":{"migration_id":{"type":"integer","description":"The unique identifier of the migration."},"per_page":{"type":"integer","default":30,"description":"The number of results per page (max 100). For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""},"page":{"type":"integer","default":1,"description":"The page number of the results to fetch. For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""}},"required":["migration_id"]},
    call: function(args) {
    var _q = new URLSearchParams();
    if (args["per_page"] !== undefined) _q.set("per_page", String(args["per_page"]));
    if (args["page"] !== undefined) _q.set("page", String(args["page"]));
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/user/migrations/" + String(args["migration_id"]) + "/repositories") + _qs, {}, undefined);
    }
  },
  {
    name: "orgs/list-for-authenticated-user",
    description: "List organizations for the authenticated user.",
    inputSchema: {"type":"object","properties":{"per_page":{"type":"integer","default":30,"description":"The number of results per page (max 100). For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""},"page":{"type":"integer","default":1,"description":"The page number of the results to fetch. For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""}},"required":[]},
    call: function(args) {
    var _q = new URLSearchParams();
    if (args["per_page"] !== undefined) _q.set("per_page", String(args["per_page"]));
    if (args["page"] !== undefined) _q.set("page", String(args["page"]));
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/user/orgs") + _qs, {}, undefined);
    }
  },
  {
    name: "packages/list-packages-for-authenticated-user",
    description: "List packages for the authenticated user's namespace. Lists packages owned by the authenticated user within the user's namespace. Params: package_type (required).",
    inputSchema: {"type":"object","properties":{"package_type":{"type":"string","enum":["npm","maven","rubygems","docker","nuget","container"],"description":"The type of supported package. Packages in GitHub's Gradle registry have the type `maven`. Docker images pushed to GitHub's Container registry (`ghcr.io`) have the type `container`. You can use the type `docker` to find images that were pushed to GitHub's Docker registry (`docker.pkg.github.com`), even if these have now been migrated to the Container registry."},"visibility":{"type":"string","enum":["public","private","internal"],"description":"The selected visibility of the packages.  This parameter is optional and only filters an existing result set.\n\nThe `internal` visibility is only supported for GitHub Packages registries that allow for granular permissions. For other ecosystems `internal` is synonymous with `private`.\nFor the list of GitHub Packages registries that support granular permissions, see \"[About permissions for GitHub Packages](https://docs.github.com/packages/learn-github-packages/about-permissions-for-github-packages#granular-permissions-for-userorganization-scoped-packages).\""},"page":{"type":"integer","default":1,"description":"The page number of the results to fetch. For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""},"per_page":{"type":"integer","default":30,"description":"The number of results per page (max 100). For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""}},"required":["package_type"]},
    call: function(args) {
    var _q = new URLSearchParams();
    if (args["package_type"] !== undefined) _q.set("package_type", String(args["package_type"]));
    if (args["visibility"] !== undefined) _q.set("visibility", String(args["visibility"]));
    if (args["page"] !== undefined) _q.set("page", String(args["page"]));
    if (args["per_page"] !== undefined) _q.set("per_page", String(args["per_page"]));
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/user/packages") + _qs, {}, undefined);
    }
  },
  {
    name: "packages/get-package-for-authenticated-user",
    description: "Get a package for the authenticated user. Gets a specific package for a package owned by the authenticated user. Params: package_type (required), package_name (required).",
    inputSchema: {"type":"object","properties":{"package_type":{"type":"string","enum":["npm","maven","rubygems","docker","nuget","container"],"description":"The type of supported package. Packages in GitHub's Gradle registry have the type `maven`. Docker images pushed to GitHub's Container registry (`ghcr.io`) have the type `container`. You can use the type `docker` to find images that were pushed to GitHub's Docker registry (`docker.pkg.github.com`), even if these have now been migrated to the Container registry."},"package_name":{"type":"string","description":"The name of the package."}},"required":["package_type","package_name"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/user/packages/" + String(args["package_type"]) + "/" + String(args["package_name"])) + _qs, {}, undefined);
    }
  },
  {
    name: "packages/delete-package-for-authenticated-user",
    description: "Delete a package for the authenticated user. Deletes a package owned by the authenticated user. Params: package_type (required), package_name (required).",
    inputSchema: {"type":"object","properties":{"package_type":{"type":"string","enum":["npm","maven","rubygems","docker","nuget","container"],"description":"The type of supported package. Packages in GitHub's Gradle registry have the type `maven`. Docker images pushed to GitHub's Container registry (`ghcr.io`) have the type `container`. You can use the type `docker` to find images that were pushed to GitHub's Docker registry (`docker.pkg.github.com`), even if these have now been migrated to the Container registry."},"package_name":{"type":"string","description":"The name of the package."}},"required":["package_type","package_name"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("DELETE", ("https://api.github.com/user/packages/" + String(args["package_type"]) + "/" + String(args["package_name"])) + _qs, {}, undefined);
    }
  },
  {
    name: "packages/restore-package-for-authenticated-user",
    description: "Restore a package for the authenticated user. Restores a package owned by the authenticated user. Params: package_type (required), package_name (required).",
    inputSchema: {"type":"object","properties":{"package_type":{"type":"string","enum":["npm","maven","rubygems","docker","nuget","container"],"description":"The type of supported package. Packages in GitHub's Gradle registry have the type `maven`. Docker images pushed to GitHub's Container registry (`ghcr.io`) have the type `container`. You can use the type `docker` to find images that were pushed to GitHub's Docker registry (`docker.pkg.github.com`), even if these have now been migrated to the Container registry."},"package_name":{"type":"string","description":"The name of the package."},"token":{"type":"string","description":"package token"}},"required":["package_type","package_name"]},
    call: function(args) {
    var _q = new URLSearchParams();
    if (args["token"] !== undefined) _q.set("token", String(args["token"]));
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("POST", ("https://api.github.com/user/packages/" + String(args["package_type"]) + "/" + String(args["package_name"]) + "/restore") + _qs, {}, undefined);
    }
  },
  {
    name: "packages/get-all-package-versions-for-package-owned-by-authenticated-user",
    description: "List package versions for a package owned by the authenticated user. Lists package versions for a package owned by the authenticated user. Params: package_type (required), package_name (required).",
    inputSchema: {"type":"object","properties":{"package_type":{"type":"string","enum":["npm","maven","rubygems","docker","nuget","container"],"description":"The type of supported package. Packages in GitHub's Gradle registry have the type `maven`. Docker images pushed to GitHub's Container registry (`ghcr.io`) have the type `container`. You can use the type `docker` to find images that were pushed to GitHub's Docker registry (`docker.pkg.github.com`), even if these have now been migrated to the Container registry."},"package_name":{"type":"string","description":"The name of the package."},"page":{"type":"integer","default":1,"description":"The page number of the results to fetch. For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""},"per_page":{"type":"integer","default":30,"description":"The number of results per page (max 100). For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""},"state":{"type":"string","enum":["active","deleted"],"default":"active","description":"The state of the package, either active or deleted."}},"required":["package_type","package_name"]},
    call: function(args) {
    var _q = new URLSearchParams();
    if (args["page"] !== undefined) _q.set("page", String(args["page"]));
    if (args["per_page"] !== undefined) _q.set("per_page", String(args["per_page"]));
    if (args["state"] !== undefined) _q.set("state", String(args["state"]));
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/user/packages/" + String(args["package_type"]) + "/" + String(args["package_name"]) + "/versions") + _qs, {}, undefined);
    }
  },
  {
    name: "packages/get-package-version-for-authenticated-user",
    description: "Get a package version for the authenticated user. Gets a specific package version for a package owned by the authenticated user. Params: package_type (required), package_name (required), package_version_id (required).",
    inputSchema: {"type":"object","properties":{"package_type":{"type":"string","enum":["npm","maven","rubygems","docker","nuget","container"],"description":"The type of supported package. Packages in GitHub's Gradle registry have the type `maven`. Docker images pushed to GitHub's Container registry (`ghcr.io`) have the type `container`. You can use the type `docker` to find images that were pushed to GitHub's Docker registry (`docker.pkg.github.com`), even if these have now been migrated to the Container registry."},"package_name":{"type":"string","description":"The name of the package."},"package_version_id":{"type":"integer","description":"Unique identifier of the package version."}},"required":["package_type","package_name","package_version_id"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/user/packages/" + String(args["package_type"]) + "/" + String(args["package_name"]) + "/versions/" + String(args["package_version_id"])) + _qs, {}, undefined);
    }
  },
  {
    name: "packages/delete-package-version-for-authenticated-user",
    description: "Delete a package version for the authenticated user. Deletes a specific package version for a package owned by the authenticated user. Params: package_type (required), package_name (required), package_version_id (required).",
    inputSchema: {"type":"object","properties":{"package_type":{"type":"string","enum":["npm","maven","rubygems","docker","nuget","container"],"description":"The type of supported package. Packages in GitHub's Gradle registry have the type `maven`. Docker images pushed to GitHub's Container registry (`ghcr.io`) have the type `container`. You can use the type `docker` to find images that were pushed to GitHub's Docker registry (`docker.pkg.github.com`), even if these have now been migrated to the Container registry."},"package_name":{"type":"string","description":"The name of the package."},"package_version_id":{"type":"integer","description":"Unique identifier of the package version."}},"required":["package_type","package_name","package_version_id"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("DELETE", ("https://api.github.com/user/packages/" + String(args["package_type"]) + "/" + String(args["package_name"]) + "/versions/" + String(args["package_version_id"])) + _qs, {}, undefined);
    }
  },
  {
    name: "packages/restore-package-version-for-authenticated-user",
    description: "Restore a package version for the authenticated user. Restores a package version owned by the authenticated user. Params: package_type (required), package_name (required), package_version_id (required).",
    inputSchema: {"type":"object","properties":{"package_type":{"type":"string","enum":["npm","maven","rubygems","docker","nuget","container"],"description":"The type of supported package. Packages in GitHub's Gradle registry have the type `maven`. Docker images pushed to GitHub's Container registry (`ghcr.io`) have the type `container`. You can use the type `docker` to find images that were pushed to GitHub's Docker registry (`docker.pkg.github.com`), even if these have now been migrated to the Container registry."},"package_name":{"type":"string","description":"The name of the package."},"package_version_id":{"type":"integer","description":"Unique identifier of the package version."}},"required":["package_type","package_name","package_version_id"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("POST", ("https://api.github.com/user/packages/" + String(args["package_type"]) + "/" + String(args["package_name"]) + "/versions/" + String(args["package_version_id"]) + "/restore") + _qs, {}, undefined);
    }
  },
  {
    name: "users/list-public-emails-for-authenticated-user",
    description: "List public email addresses for the authenticated user. Lists your publicly visible email address, which you can set with the\n[Set primary email visibility for the authenticated user](https://docs.github.com/rest/users/emails#set-primary-email-visibility-for-the-authenticated-user)\nendpoint.",
    inputSchema: {"type":"object","properties":{"per_page":{"type":"integer","default":30,"description":"The number of results per page (max 100). For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""},"page":{"type":"integer","default":1,"description":"The page number of the results to fetch. For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""}},"required":[]},
    call: function(args) {
    var _q = new URLSearchParams();
    if (args["per_page"] !== undefined) _q.set("per_page", String(args["per_page"]));
    if (args["page"] !== undefined) _q.set("page", String(args["page"]));
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/user/public_emails") + _qs, {}, undefined);
    }
  },
  {
    name: "repos/list-for-authenticated-user",
    description: "List repositories for the authenticated user. Lists repositories that the authenticated user has explicit permission (`:read`, `:write`, or `:admin`) to access.",
    inputSchema: {"type":"object","properties":{"visibility":{"type":"string","enum":["all","public","private"],"default":"all","description":"Limit results to repositories with the specified visibility."},"affiliation":{"type":"string","default":"owner,collaborator,organization_member","description":"Comma-separated list of values. Can include:  \n * `owner`: Repositories that are owned by the authenticated user.  \n * `collaborator`: Repositories that the user has been added to as a collaborator.  \n * `organization_member`: Repositories that the user has access to through being a member of an organization. This includes every repository on every team that the user is on."},"type":{"type":"string","enum":["all","owner","public","private","member"],"default":"all","description":"Limit results to repositories of the specified type. Will cause a `422` error if used in the same request as **visibility** or **affiliation**."},"sort":{"type":"string","enum":["created","updated","pushed","full_name"],"default":"full_name","description":"The property to sort the results by."},"direction":{"type":"string","enum":["asc","desc"],"description":"The order to sort by. Default: `asc` when using `full_name`, otherwise `desc`."},"per_page":{"type":"integer","default":30,"description":"The number of results per page (max 100). For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""},"page":{"type":"integer","default":1,"description":"The page number of the results to fetch. For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""},"since":{"type":"string","format":"date-time","description":"Only show repositories updated after the given time. This is a timestamp in [ISO 8601](https://en.wikipedia.org/wiki/ISO_8601) format: `YYYY-MM-DDTHH:MM:SSZ`."},"before":{"type":"string","format":"date-time","description":"Only show repositories updated before the given time. This is a timestamp in [ISO 8601](https://en.wikipedia.org/wiki/ISO_8601) format: `YYYY-MM-DDTHH:MM:SSZ`."}},"required":[]},
    call: function(args) {
    var _q = new URLSearchParams();
    if (args["visibility"] !== undefined) _q.set("visibility", String(args["visibility"]));
    if (args["affiliation"] !== undefined) _q.set("affiliation", String(args["affiliation"]));
    if (args["type"] !== undefined) _q.set("type", String(args["type"]));
    if (args["sort"] !== undefined) _q.set("sort", String(args["sort"]));
    if (args["direction"] !== undefined) _q.set("direction", String(args["direction"]));
    if (args["per_page"] !== undefined) _q.set("per_page", String(args["per_page"]));
    if (args["page"] !== undefined) _q.set("page", String(args["page"]));
    if (args["since"] !== undefined) _q.set("since", String(args["since"]));
    if (args["before"] !== undefined) _q.set("before", String(args["before"]));
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/user/repos") + _qs, {}, undefined);
    }
  },
  {
    name: "repos/create-for-authenticated-user",
    description: "Create a repository for the authenticated user. Creates a new repository for the authenticated user.",
    inputSchema: {"type":"object","properties":{"body":{"type":"object","description":"Request body"}},"required":["body"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("POST", ("https://api.github.com/user/repos") + _qs, {}, args["body"]);
    }
  },
  {
    name: "repos/list-invitations-for-authenticated-user",
    description: "List repository invitations for the authenticated user. When authenticating as a user, this endpoint will list all currently open repository invitations for that user.",
    inputSchema: {"type":"object","properties":{"per_page":{"type":"integer","default":30,"description":"The number of results per page (max 100). For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""},"page":{"type":"integer","default":1,"description":"The page number of the results to fetch. For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""}},"required":[]},
    call: function(args) {
    var _q = new URLSearchParams();
    if (args["per_page"] !== undefined) _q.set("per_page", String(args["per_page"]));
    if (args["page"] !== undefined) _q.set("page", String(args["page"]));
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/user/repository_invitations") + _qs, {}, undefined);
    }
  },
  {
    name: "repos/accept-invitation-for-authenticated-user",
    description: "Accept a repository invitation. Params: invitation_id (required).",
    inputSchema: {"type":"object","properties":{"invitation_id":{"type":"integer","description":"The unique identifier of the invitation."}},"required":["invitation_id"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("PATCH", ("https://api.github.com/user/repository_invitations/" + String(args["invitation_id"])) + _qs, {}, undefined);
    }
  },
  {
    name: "repos/decline-invitation-for-authenticated-user",
    description: "Decline a repository invitation. Params: invitation_id (required).",
    inputSchema: {"type":"object","properties":{"invitation_id":{"type":"integer","description":"The unique identifier of the invitation."}},"required":["invitation_id"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("DELETE", ("https://api.github.com/user/repository_invitations/" + String(args["invitation_id"])) + _qs, {}, undefined);
    }
  },
  {
    name: "users/list-social-accounts-for-authenticated-user",
    description: "List social accounts for the authenticated user. Lists all of your social accounts.",
    inputSchema: {"type":"object","properties":{"per_page":{"type":"integer","default":30,"description":"The number of results per page (max 100). For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""},"page":{"type":"integer","default":1,"description":"The page number of the results to fetch. For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""}},"required":[]},
    call: function(args) {
    var _q = new URLSearchParams();
    if (args["per_page"] !== undefined) _q.set("per_page", String(args["per_page"]));
    if (args["page"] !== undefined) _q.set("page", String(args["page"]));
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/user/social_accounts") + _qs, {}, undefined);
    }
  },
  {
    name: "users/add-social-account-for-authenticated-user",
    description: "Add social accounts for the authenticated user. Add one or more social accounts to the authenticated user's profile.",
    inputSchema: {"type":"object","properties":{"body":{"type":"object","description":"Request body"}},"required":["body"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("POST", ("https://api.github.com/user/social_accounts") + _qs, {}, args["body"]);
    }
  },
  {
    name: "users/delete-social-account-for-authenticated-user",
    description: "Delete social accounts for the authenticated user. Deletes one or more social accounts from the authenticated user's profile.",
    inputSchema: {"type":"object","properties":{"body":{"type":"object","description":"Request body"}},"required":["body"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("DELETE", ("https://api.github.com/user/social_accounts") + _qs, {}, args["body"]);
    }
  },
  {
    name: "users/list-ssh-signing-keys-for-authenticated-user",
    description: "List SSH signing keys for the authenticated user. Lists the SSH signing keys for the authenticated user's GitHub account.",
    inputSchema: {"type":"object","properties":{"per_page":{"type":"integer","default":30,"description":"The number of results per page (max 100). For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""},"page":{"type":"integer","default":1,"description":"The page number of the results to fetch. For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""}},"required":[]},
    call: function(args) {
    var _q = new URLSearchParams();
    if (args["per_page"] !== undefined) _q.set("per_page", String(args["per_page"]));
    if (args["page"] !== undefined) _q.set("page", String(args["page"]));
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/user/ssh_signing_keys") + _qs, {}, undefined);
    }
  },
  {
    name: "users/create-ssh-signing-key-for-authenticated-user",
    description: "Create a SSH signing key for the authenticated user. Creates an SSH signing key for the authenticated user's GitHub account.",
    inputSchema: {"type":"object","properties":{"body":{"type":"object","description":"Request body"}},"required":["body"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("POST", ("https://api.github.com/user/ssh_signing_keys") + _qs, {}, args["body"]);
    }
  },
  {
    name: "users/get-ssh-signing-key-for-authenticated-user",
    description: "Get an SSH signing key for the authenticated user. Gets extended details for an SSH signing key. Params: ssh_signing_key_id (required).",
    inputSchema: {"type":"object","properties":{"ssh_signing_key_id":{"type":"integer","description":"The unique identifier of the SSH signing key."}},"required":["ssh_signing_key_id"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/user/ssh_signing_keys/" + String(args["ssh_signing_key_id"])) + _qs, {}, undefined);
    }
  },
  {
    name: "users/delete-ssh-signing-key-for-authenticated-user",
    description: "Delete an SSH signing key for the authenticated user. Deletes an SSH signing key from the authenticated user's GitHub account. Params: ssh_signing_key_id (required).",
    inputSchema: {"type":"object","properties":{"ssh_signing_key_id":{"type":"integer","description":"The unique identifier of the SSH signing key."}},"required":["ssh_signing_key_id"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("DELETE", ("https://api.github.com/user/ssh_signing_keys/" + String(args["ssh_signing_key_id"])) + _qs, {}, undefined);
    }
  },
  {
    name: "activity/list-repos-starred-by-authenticated-user",
    description: "List repositories starred by the authenticated user. Lists repositories the authenticated user has starred.",
    inputSchema: {"type":"object","properties":{"sort":{"type":"string","enum":["created","updated"],"default":"created","description":"The property to sort the results by. `created` means when the repository was starred. `updated` means when the repository was last pushed to."},"direction":{"type":"string","enum":["asc","desc"],"default":"desc","description":"The direction to sort the results by."},"per_page":{"type":"integer","default":30,"description":"The number of results per page (max 100). For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""},"page":{"type":"integer","default":1,"description":"The page number of the results to fetch. For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""}},"required":[]},
    call: function(args) {
    var _q = new URLSearchParams();
    if (args["sort"] !== undefined) _q.set("sort", String(args["sort"]));
    if (args["direction"] !== undefined) _q.set("direction", String(args["direction"]));
    if (args["per_page"] !== undefined) _q.set("per_page", String(args["per_page"]));
    if (args["page"] !== undefined) _q.set("page", String(args["page"]));
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/user/starred") + _qs, {}, undefined);
    }
  },
  {
    name: "activity/check-repo-is-starred-by-authenticated-user",
    description: "Check if a repository is starred by the authenticated user. Whether the authenticated user has starred the repository. Params: owner (required), repo (required).",
    inputSchema: {"type":"object","properties":{"owner":{"type":"string","description":"The account owner of the repository. The name is not case sensitive."},"repo":{"type":"string","description":"The name of the repository without the `.git` extension. The name is not case sensitive."}},"required":["owner","repo"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/user/starred/" + String(args["owner"]) + "/" + String(args["repo"])) + _qs, {}, undefined);
    }
  },
  {
    name: "activity/star-repo-for-authenticated-user",
    description: "Star a repository for the authenticated user. Note that you'll need to set `Content-Length` to zero when calling out to this endpoint. Params: owner (required), repo (required).",
    inputSchema: {"type":"object","properties":{"owner":{"type":"string","description":"The account owner of the repository. The name is not case sensitive."},"repo":{"type":"string","description":"The name of the repository without the `.git` extension. The name is not case sensitive."}},"required":["owner","repo"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("PUT", ("https://api.github.com/user/starred/" + String(args["owner"]) + "/" + String(args["repo"])) + _qs, {}, undefined);
    }
  },
  {
    name: "activity/unstar-repo-for-authenticated-user",
    description: "Unstar a repository for the authenticated user. Unstar a repository that the authenticated user has previously starred. Params: owner (required), repo (required).",
    inputSchema: {"type":"object","properties":{"owner":{"type":"string","description":"The account owner of the repository. The name is not case sensitive."},"repo":{"type":"string","description":"The name of the repository without the `.git` extension. The name is not case sensitive."}},"required":["owner","repo"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("DELETE", ("https://api.github.com/user/starred/" + String(args["owner"]) + "/" + String(args["repo"])) + _qs, {}, undefined);
    }
  },
  {
    name: "activity/list-watched-repos-for-authenticated-user",
    description: "List repositories watched by the authenticated user. Lists repositories the authenticated user is watching.",
    inputSchema: {"type":"object","properties":{"per_page":{"type":"integer","default":30,"description":"The number of results per page (max 100). For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""},"page":{"type":"integer","default":1,"description":"The page number of the results to fetch. For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""}},"required":[]},
    call: function(args) {
    var _q = new URLSearchParams();
    if (args["per_page"] !== undefined) _q.set("per_page", String(args["per_page"]));
    if (args["page"] !== undefined) _q.set("page", String(args["page"]));
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/user/subscriptions") + _qs, {}, undefined);
    }
  },
  {
    name: "teams/list-for-authenticated-user",
    description: "List teams for the authenticated user. List all of the teams across all of the organizations to which the authenticated\nuser belongs.",
    inputSchema: {"type":"object","properties":{"per_page":{"type":"integer","default":30,"description":"The number of results per page (max 100). For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""},"page":{"type":"integer","default":1,"description":"The page number of the results to fetch. For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""}},"required":[]},
    call: function(args) {
    var _q = new URLSearchParams();
    if (args["per_page"] !== undefined) _q.set("per_page", String(args["per_page"]));
    if (args["page"] !== undefined) _q.set("page", String(args["page"]));
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/user/teams") + _qs, {}, undefined);
    }
  },
  {
    name: "users/get-by-id",
    description: "Get a user using their ID. Provides publicly available information about someone with a GitHub account. Params: account_id (required).",
    inputSchema: {"type":"object","properties":{"account_id":{"type":"integer","description":"account_id parameter"}},"required":["account_id"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/user/" + String(args["account_id"])) + _qs, {}, undefined);
    }
  },
  {
    name: "projects/create-draft-item-for-authenticated-user",
    description: "Create draft item for user owned project. Create draft issue item for the specified user owned project. Params: user_id (required), project_number (required).",
    inputSchema: {"type":"object","properties":{"user_id":{"type":"string","description":"The unique identifier of the user."},"project_number":{"type":"integer","description":"The project's number."},"body":{"type":"object","description":"Details of the draft item to create in the project."}},"required":["user_id","project_number","body"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("POST", ("https://api.github.com/user/" + String(args["user_id"]) + "/projectsV2/" + String(args["project_number"]) + "/drafts") + _qs, {}, args["body"]);
    }
  },
  {
    name: "users/list",
    description: "List users. Lists all users, in the order that they signed up on GitHub.",
    inputSchema: {"type":"object","properties":{"since":{"type":"integer","description":"A user ID. Only return users with an ID greater than this ID."},"per_page":{"type":"integer","default":30,"description":"The number of results per page (max 100). For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""}},"required":[]},
    call: function(args) {
    var _q = new URLSearchParams();
    if (args["since"] !== undefined) _q.set("since", String(args["since"]));
    if (args["per_page"] !== undefined) _q.set("per_page", String(args["per_page"]));
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/users") + _qs, {}, undefined);
    }
  },
  {
    name: "projects/create-view-for-user",
    description: "Create a view for a user-owned project. Create a new view in a user-owned project. Params: user_id (required), project_number (required).",
    inputSchema: {"type":"object","properties":{"user_id":{"type":"string","description":"The unique identifier of the user."},"project_number":{"type":"integer","description":"The project's number."},"body":{"type":"object","description":"Request body"}},"required":["user_id","project_number","body"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("POST", ("https://api.github.com/users/" + String(args["user_id"]) + "/projectsV2/" + String(args["project_number"]) + "/views") + _qs, {}, args["body"]);
    }
  },
  {
    name: "users/get-by-username",
    description: "Get a user. Provides publicly available information about someone with a GitHub account. Params: username (required).",
    inputSchema: {"type":"object","properties":{"username":{"type":"string","description":"The handle for the GitHub user account."}},"required":["username"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/users/" + String(args["username"])) + _qs, {}, undefined);
    }
  },
  {
    name: "users/list-attestations-bulk",
    description: "List attestations by bulk subject digests. List a collection of artifact attestations associated with any entry in a list of subject digests owned by a user. Params: username (required).",
    inputSchema: {"type":"object","properties":{"per_page":{"type":"integer","default":30,"description":"The number of results per page (max 100). For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""},"before":{"type":"string","description":"A cursor, as given in the [Link header](https://docs.github.com/rest/guides/using-pagination-in-the-rest-api#using-link-headers). If specified, the query only searches for results before this cursor. For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""},"after":{"type":"string","description":"A cursor, as given in the [Link header](https://docs.github.com/rest/guides/using-pagination-in-the-rest-api#using-link-headers). If specified, the query only searches for results after this cursor. For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""},"username":{"type":"string","description":"The handle for the GitHub user account."},"body":{"type":"object","description":"Request body"}},"required":["username","body"]},
    call: function(args) {
    var _q = new URLSearchParams();
    if (args["per_page"] !== undefined) _q.set("per_page", String(args["per_page"]));
    if (args["before"] !== undefined) _q.set("before", String(args["before"]));
    if (args["after"] !== undefined) _q.set("after", String(args["after"]));
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("POST", ("https://api.github.com/users/" + String(args["username"]) + "/attestations/bulk-list") + _qs, {}, args["body"]);
    }
  },
  {
    name: "users/delete-attestations-bulk",
    description: "Delete attestations in bulk. Delete artifact attestations in bulk by either subject digests or unique ID. Params: username (required).",
    inputSchema: {"type":"object","properties":{"username":{"type":"string","description":"The handle for the GitHub user account."},"body":{"type":"object","description":"Request body"}},"required":["username","body"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("POST", ("https://api.github.com/users/" + String(args["username"]) + "/attestations/delete-request") + _qs, {}, args["body"]);
    }
  },
  {
    name: "users/delete-attestations-by-subject-digest",
    description: "Delete attestations by subject digest. Delete an artifact attestation by subject digest. Params: username (required), subject_digest (required).",
    inputSchema: {"type":"object","properties":{"username":{"type":"string","description":"The handle for the GitHub user account."},"subject_digest":{"type":"string","description":"Subject Digest"}},"required":["username","subject_digest"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("DELETE", ("https://api.github.com/users/" + String(args["username"]) + "/attestations/digest/" + String(args["subject_digest"])) + _qs, {}, undefined);
    }
  },
  {
    name: "users/delete-attestations-by-id",
    description: "Delete attestations by ID. Delete an artifact attestation by unique ID that is associated with a repository owned by a user. Params: username (required), attestation_id (required).",
    inputSchema: {"type":"object","properties":{"username":{"type":"string","description":"The handle for the GitHub user account."},"attestation_id":{"type":"integer","description":"Attestation ID"}},"required":["username","attestation_id"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("DELETE", ("https://api.github.com/users/" + String(args["username"]) + "/attestations/" + String(args["attestation_id"])) + _qs, {}, undefined);
    }
  },
  {
    name: "users/list-attestations",
    description: "List attestations. List a collection of artifact attestations with a given subject digest that are associated with repositories owned by a user. Params: username (required), subject_digest (required).",
    inputSchema: {"type":"object","properties":{"per_page":{"type":"integer","default":30,"description":"The number of results per page (max 100). For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""},"before":{"type":"string","description":"A cursor, as given in the [Link header](https://docs.github.com/rest/guides/using-pagination-in-the-rest-api#using-link-headers). If specified, the query only searches for results before this cursor. For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""},"after":{"type":"string","description":"A cursor, as given in the [Link header](https://docs.github.com/rest/guides/using-pagination-in-the-rest-api#using-link-headers). If specified, the query only searches for results after this cursor. For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""},"username":{"type":"string","description":"The handle for the GitHub user account."},"subject_digest":{"type":"string","description":"Subject Digest"},"predicate_type":{"type":"string","description":"Optional filter for fetching attestations with a given predicate type.\nThis option accepts `provenance`, `sbom`, `release`, or freeform text\nfor custom predicate types."}},"required":["username","subject_digest"]},
    call: function(args) {
    var _q = new URLSearchParams();
    if (args["per_page"] !== undefined) _q.set("per_page", String(args["per_page"]));
    if (args["before"] !== undefined) _q.set("before", String(args["before"]));
    if (args["after"] !== undefined) _q.set("after", String(args["after"]));
    if (args["predicate_type"] !== undefined) _q.set("predicate_type", String(args["predicate_type"]));
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/users/" + String(args["username"]) + "/attestations/" + String(args["subject_digest"])) + _qs, {}, undefined);
    }
  },
  {
    name: "copilot-spaces/list-for-user",
    description: "List Copilot Spaces for a user. Lists Copilot Spaces owned by a user. Params: username (required).",
    inputSchema: {"type":"object","properties":{"username":{"type":"string","description":"The handle for the GitHub user account."},"per_page":{"type":"integer","minimum":1,"maximum":100,"default":30,"description":"The number of results per page (max 100)."},"before":{"type":"string","description":"A cursor, as given in the [Link header](https://docs.github.com/rest/guides/using-pagination-in-the-rest-api#using-link-headers). If specified, the query only searches for results before this cursor."},"after":{"type":"string","description":"A cursor, as given in the [Link header](https://docs.github.com/rest/guides/using-pagination-in-the-rest-api#using-link-headers). If specified, the query only searches for results after this cursor."}},"required":["username"]},
    call: function(args) {
    var _q = new URLSearchParams();
    if (args["per_page"] !== undefined) _q.set("per_page", String(args["per_page"]));
    if (args["before"] !== undefined) _q.set("before", String(args["before"]));
    if (args["after"] !== undefined) _q.set("after", String(args["after"]));
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/users/" + String(args["username"]) + "/copilot-spaces") + _qs, {}, undefined);
    }
  },
  {
    name: "copilot-spaces/create-for-user",
    description: "Create a Copilot Space for a user. Creates a new Copilot Space owned by a user. Params: username (required).",
    inputSchema: {"type":"object","properties":{"username":{"type":"string","description":"The handle for the GitHub user account."},"body":{"type":"object","description":"Request body"}},"required":["username","body"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("POST", ("https://api.github.com/users/" + String(args["username"]) + "/copilot-spaces") + _qs, {}, args["body"]);
    }
  },
  {
    name: "copilot-spaces/get-for-user",
    description: "Get a Copilot Space for a user. Gets details about a specific Copilot Space owned by a user. Params: username (required), space_number (required).",
    inputSchema: {"type":"object","properties":{"username":{"type":"string","description":"The handle for the GitHub user account."},"space_number":{"type":"integer","description":"The unique identifier of the Copilot Space."}},"required":["username","space_number"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/users/" + String(args["username"]) + "/copilot-spaces/" + String(args["space_number"])) + _qs, {}, undefined);
    }
  },
  {
    name: "copilot-spaces/update-for-user",
    description: "Set a Copilot Space for a user. Updates a Copilot Space owned by a user. Params: username (required), space_number (required).",
    inputSchema: {"type":"object","properties":{"username":{"type":"string","description":"The handle for the GitHub user account."},"space_number":{"type":"integer","description":"The unique identifier of the Copilot Space."},"body":{"type":"object","description":"Request body"}},"required":["username","space_number","body"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("PUT", ("https://api.github.com/users/" + String(args["username"]) + "/copilot-spaces/" + String(args["space_number"])) + _qs, {}, args["body"]);
    }
  },
  {
    name: "copilot-spaces/delete-for-user",
    description: "Delete a Copilot Space for a user. Deletes a Copilot Space owned by a user. Params: username (required), space_number (required).",
    inputSchema: {"type":"object","properties":{"username":{"type":"string","description":"The handle for the GitHub user account."},"space_number":{"type":"integer","description":"The unique identifier of the Copilot Space."}},"required":["username","space_number"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("DELETE", ("https://api.github.com/users/" + String(args["username"]) + "/copilot-spaces/" + String(args["space_number"])) + _qs, {}, undefined);
    }
  },
  {
    name: "copilot-spaces/list-collaborators-for-user",
    description: "List collaborators for a Copilot Space for a user. Lists all collaborators for a specific Copilot Space owned by a user. Params: username (required), space_number (required).",
    inputSchema: {"type":"object","properties":{"username":{"type":"string","description":"The handle for the GitHub user account."},"space_number":{"type":"integer","description":"The unique identifier of the Copilot Space."}},"required":["username","space_number"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/users/" + String(args["username"]) + "/copilot-spaces/" + String(args["space_number"]) + "/collaborators") + _qs, {}, undefined);
    }
  },
  {
    name: "copilot-spaces/add-collaborator-for-user",
    description: "Add a collaborator to a Copilot Space for a user. Adds a collaborator to a specific Copilot Space owned by a user. Params: username (required), space_number (required).",
    inputSchema: {"type":"object","properties":{"username":{"type":"string","description":"The handle for the GitHub user account."},"space_number":{"type":"integer","description":"The unique identifier of the Copilot Space."},"body":{"type":"object","description":"Request body"}},"required":["username","space_number","body"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("POST", ("https://api.github.com/users/" + String(args["username"]) + "/copilot-spaces/" + String(args["space_number"]) + "/collaborators") + _qs, {}, args["body"]);
    }
  },
  {
    name: "copilot-spaces/update-collaborator-for-user",
    description: "Set a collaborator role for a Copilot Space for a user. Updates the role of a collaborator for a specific Copilot Space owned by a user. Params: username (required), space_number (required), actor_type (required), actor_identifier (required).",
    inputSchema: {"type":"object","properties":{"username":{"type":"string","description":"The handle for the GitHub user account."},"space_number":{"type":"integer","description":"The unique identifier of the Copilot Space."},"actor_type":{"type":"string","enum":["User","Team"],"description":"The type of actor (must be `User` for user-owned spaces; `Team` will be rejected)."},"actor_identifier":{"type":"string","description":"The username of the collaborator. The numeric user ID is also accepted."},"body":{"type":"object","description":"Request body"}},"required":["username","space_number","actor_type","actor_identifier","body"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("PUT", ("https://api.github.com/users/" + String(args["username"]) + "/copilot-spaces/" + String(args["space_number"]) + "/collaborators/" + String(args["actor_type"]) + "/" + String(args["actor_identifier"])) + _qs, {}, args["body"]);
    }
  },
  {
    name: "copilot-spaces/remove-collaborator-for-user",
    description: "Remove a collaborator from a Copilot Space for a user. Removes a collaborator from a specific Copilot Space owned by a user. Params: username (required), space_number (required), actor_type (required), actor_identifier (required).",
    inputSchema: {"type":"object","properties":{"username":{"type":"string","description":"The handle for the GitHub user account."},"space_number":{"type":"integer","description":"The unique identifier of the Copilot Space."},"actor_type":{"type":"string","enum":["User","Team"],"description":"The type of actor (must be `User` for user-owned spaces; `Team` will be rejected)."},"actor_identifier":{"type":"string","description":"The username of the collaborator. The numeric user ID is also accepted."}},"required":["username","space_number","actor_type","actor_identifier"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("DELETE", ("https://api.github.com/users/" + String(args["username"]) + "/copilot-spaces/" + String(args["space_number"]) + "/collaborators/" + String(args["actor_type"]) + "/" + String(args["actor_identifier"])) + _qs, {}, undefined);
    }
  },
  {
    name: "copilot-spaces/list-resources-for-user",
    description: "List resources for a Copilot Space for a user. Lists all resources attached to a specific Copilot Space owned by a user. Params: username (required), space_number (required).",
    inputSchema: {"type":"object","properties":{"username":{"type":"string","description":"The handle for the GitHub user account."},"space_number":{"type":"integer","description":"The unique identifier of the Copilot Space."}},"required":["username","space_number"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/users/" + String(args["username"]) + "/copilot-spaces/" + String(args["space_number"]) + "/resources") + _qs, {}, undefined);
    }
  },
  {
    name: "copilot-spaces/create-resource-for-user",
    description: "Create a resource for a Copilot Space for a user. Creates a new resource in a specific Copilot Space owned by a user. Params: username (required), space_number (required).",
    inputSchema: {"type":"object","properties":{"username":{"type":"string","description":"The handle for the GitHub user account."},"space_number":{"type":"integer","description":"The unique identifier of the Copilot Space."},"body":{"type":"object","description":"Request body"}},"required":["username","space_number","body"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("POST", ("https://api.github.com/users/" + String(args["username"]) + "/copilot-spaces/" + String(args["space_number"]) + "/resources") + _qs, {}, args["body"]);
    }
  },
  {
    name: "copilot-spaces/get-resource-for-user",
    description: "Get a resource for a Copilot Space for a user. Gets a specific resource attached to a Copilot Space owned by a user. Params: username (required), space_number (required), space_resource_id (required).",
    inputSchema: {"type":"object","properties":{"username":{"type":"string","description":"The handle for the GitHub user account."},"space_number":{"type":"integer","description":"The unique identifier of the Copilot Space."},"space_resource_id":{"type":"integer","description":"The unique identifier of the resource."}},"required":["username","space_number","space_resource_id"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/users/" + String(args["username"]) + "/copilot-spaces/" + String(args["space_number"]) + "/resources/" + String(args["space_resource_id"])) + _qs, {}, undefined);
    }
  },
  {
    name: "copilot-spaces/update-resource-for-user",
    description: "Set a resource for a Copilot Space for a user. Updates the metadata of a resource in a specific Copilot Space owned by a user. Params: username (required), space_number (required), space_resource_id (required).",
    inputSchema: {"type":"object","properties":{"username":{"type":"string","description":"The handle for the GitHub user account."},"space_number":{"type":"integer","description":"The unique identifier of the Copilot Space."},"space_resource_id":{"type":"integer","description":"The unique identifier of the resource."},"body":{"type":"object","description":"Request body"}},"required":["username","space_number","space_resource_id","body"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("PUT", ("https://api.github.com/users/" + String(args["username"]) + "/copilot-spaces/" + String(args["space_number"]) + "/resources/" + String(args["space_resource_id"])) + _qs, {}, args["body"]);
    }
  },
  {
    name: "copilot-spaces/delete-resource-for-user",
    description: "Delete a resource from a Copilot Space for a user. Deletes a resource from a specific Copilot Space owned by a user. Params: username (required), space_number (required), space_resource_id (required).",
    inputSchema: {"type":"object","properties":{"username":{"type":"string","description":"The handle for the GitHub user account."},"space_number":{"type":"integer","description":"The unique identifier of the Copilot Space."},"space_resource_id":{"type":"integer","description":"The unique identifier of the resource."}},"required":["username","space_number","space_resource_id"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("DELETE", ("https://api.github.com/users/" + String(args["username"]) + "/copilot-spaces/" + String(args["space_number"]) + "/resources/" + String(args["space_resource_id"])) + _qs, {}, undefined);
    }
  },
  {
    name: "packages/list-docker-migration-conflicting-packages-for-user",
    description: "Get list of conflicting packages during Docker migration for user. Lists all packages that are in a specific user's namespace, that the requesting user has access to, and that encountered a conflict during Docker migration. Params: username (required).",
    inputSchema: {"type":"object","properties":{"username":{"type":"string","description":"The handle for the GitHub user account."}},"required":["username"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/users/" + String(args["username"]) + "/docker/conflicts") + _qs, {}, undefined);
    }
  },
  {
    name: "activity/list-events-for-authenticated-user",
    description: "List events for the authenticated user. If you are authenticated as the given user, you will see your private events. Params: username (required).",
    inputSchema: {"type":"object","properties":{"username":{"type":"string","description":"The handle for the GitHub user account."},"per_page":{"type":"integer","default":30,"description":"The number of results per page (max 100). For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""},"page":{"type":"integer","default":1,"description":"The page number of the results to fetch. For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""}},"required":["username"]},
    call: function(args) {
    var _q = new URLSearchParams();
    if (args["per_page"] !== undefined) _q.set("per_page", String(args["per_page"]));
    if (args["page"] !== undefined) _q.set("page", String(args["page"]));
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/users/" + String(args["username"]) + "/events") + _qs, {}, undefined);
    }
  },
  {
    name: "activity/list-org-events-for-authenticated-user",
    description: "List organization events for the authenticated user. This is the user's organization dashboard. Params: username (required), org (required).",
    inputSchema: {"type":"object","properties":{"username":{"type":"string","description":"The handle for the GitHub user account."},"org":{"type":"string","description":"The organization name. The name is not case sensitive."},"per_page":{"type":"integer","default":30,"description":"The number of results per page (max 100). For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""},"page":{"type":"integer","default":1,"description":"The page number of the results to fetch. For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""}},"required":["username","org"]},
    call: function(args) {
    var _q = new URLSearchParams();
    if (args["per_page"] !== undefined) _q.set("per_page", String(args["per_page"]));
    if (args["page"] !== undefined) _q.set("page", String(args["page"]));
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/users/" + String(args["username"]) + "/events/orgs/" + String(args["org"])) + _qs, {}, undefined);
    }
  },
  {
    name: "activity/list-public-events-for-user",
    description: "List public events for a user. > [!NOTE]\n> This API is not built to serve real-time use cases. Params: username (required).",
    inputSchema: {"type":"object","properties":{"username":{"type":"string","description":"The handle for the GitHub user account."},"per_page":{"type":"integer","default":30,"description":"The number of results per page (max 100). For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""},"page":{"type":"integer","default":1,"description":"The page number of the results to fetch. For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""}},"required":["username"]},
    call: function(args) {
    var _q = new URLSearchParams();
    if (args["per_page"] !== undefined) _q.set("per_page", String(args["per_page"]));
    if (args["page"] !== undefined) _q.set("page", String(args["page"]));
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/users/" + String(args["username"]) + "/events/public") + _qs, {}, undefined);
    }
  },
  {
    name: "users/list-followers-for-user",
    description: "List followers of a user. Lists the people following the specified user. Params: username (required).",
    inputSchema: {"type":"object","properties":{"username":{"type":"string","description":"The handle for the GitHub user account."},"per_page":{"type":"integer","default":30,"description":"The number of results per page (max 100). For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""},"page":{"type":"integer","default":1,"description":"The page number of the results to fetch. For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""}},"required":["username"]},
    call: function(args) {
    var _q = new URLSearchParams();
    if (args["per_page"] !== undefined) _q.set("per_page", String(args["per_page"]));
    if (args["page"] !== undefined) _q.set("page", String(args["page"]));
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/users/" + String(args["username"]) + "/followers") + _qs, {}, undefined);
    }
  },
  {
    name: "users/list-following-for-user",
    description: "List the people a user follows. Lists the people who the specified user follows. Params: username (required).",
    inputSchema: {"type":"object","properties":{"username":{"type":"string","description":"The handle for the GitHub user account."},"per_page":{"type":"integer","default":30,"description":"The number of results per page (max 100). For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""},"page":{"type":"integer","default":1,"description":"The page number of the results to fetch. For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""}},"required":["username"]},
    call: function(args) {
    var _q = new URLSearchParams();
    if (args["per_page"] !== undefined) _q.set("per_page", String(args["per_page"]));
    if (args["page"] !== undefined) _q.set("page", String(args["page"]));
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/users/" + String(args["username"]) + "/following") + _qs, {}, undefined);
    }
  },
  {
    name: "users/check-following-for-user",
    description: "Check if a user follows another user. Params: username (required), target_user (required).",
    inputSchema: {"type":"object","properties":{"username":{"type":"string","description":"The handle for the GitHub user account."},"target_user":{"type":"string"}},"required":["username","target_user"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/users/" + String(args["username"]) + "/following/" + String(args["target_user"])) + _qs, {}, undefined);
    }
  },
  {
    name: "gists/list-for-user",
    description: "List gists for a user. Lists public gists for the specified user:. Params: username (required).",
    inputSchema: {"type":"object","properties":{"username":{"type":"string","description":"The handle for the GitHub user account."},"since":{"type":"string","format":"date-time","description":"Only show results that were last updated after the given time. This is a timestamp in [ISO 8601](https://en.wikipedia.org/wiki/ISO_8601) format: `YYYY-MM-DDTHH:MM:SSZ`."},"per_page":{"type":"integer","default":30,"description":"The number of results per page (max 100). For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""},"page":{"type":"integer","default":1,"description":"The page number of the results to fetch. For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""}},"required":["username"]},
    call: function(args) {
    var _q = new URLSearchParams();
    if (args["since"] !== undefined) _q.set("since", String(args["since"]));
    if (args["per_page"] !== undefined) _q.set("per_page", String(args["per_page"]));
    if (args["page"] !== undefined) _q.set("page", String(args["page"]));
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/users/" + String(args["username"]) + "/gists") + _qs, {}, undefined);
    }
  },
  {
    name: "users/list-gpg-keys-for-user",
    description: "List GPG keys for a user. Lists the GPG keys for a user. Params: username (required).",
    inputSchema: {"type":"object","properties":{"username":{"type":"string","description":"The handle for the GitHub user account."},"per_page":{"type":"integer","default":30,"description":"The number of results per page (max 100). For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""},"page":{"type":"integer","default":1,"description":"The page number of the results to fetch. For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""}},"required":["username"]},
    call: function(args) {
    var _q = new URLSearchParams();
    if (args["per_page"] !== undefined) _q.set("per_page", String(args["per_page"]));
    if (args["page"] !== undefined) _q.set("page", String(args["page"]));
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/users/" + String(args["username"]) + "/gpg_keys") + _qs, {}, undefined);
    }
  },
  {
    name: "users/get-context-for-user",
    description: "Get contextual information for a user. Provides hovercard information. Params: username (required).",
    inputSchema: {"type":"object","properties":{"username":{"type":"string","description":"The handle for the GitHub user account."},"subject_type":{"type":"string","enum":["organization","repository","issue","pull_request"],"description":"Identifies which additional information you'd like to receive about the person's hovercard. Can be `organization`, `repository`, `issue`, `pull_request`. **Required** when using `subject_id`."},"subject_id":{"type":"string","description":"Uses the ID for the `subject_type` you specified. **Required** when using `subject_type`."}},"required":["username"]},
    call: function(args) {
    var _q = new URLSearchParams();
    if (args["subject_type"] !== undefined) _q.set("subject_type", String(args["subject_type"]));
    if (args["subject_id"] !== undefined) _q.set("subject_id", String(args["subject_id"]));
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/users/" + String(args["username"]) + "/hovercard") + _qs, {}, undefined);
    }
  },
  {
    name: "apps/get-user-installation",
    description: "Get a user installation for the authenticated app. Enables an authenticated GitHub App to find the user’s installation information. Params: username (required).",
    inputSchema: {"type":"object","properties":{"username":{"type":"string","description":"The handle for the GitHub user account."}},"required":["username"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/users/" + String(args["username"]) + "/installation") + _qs, {}, undefined);
    }
  },
  {
    name: "users/list-public-keys-for-user",
    description: "List public keys for a user. Lists the _verified_ public SSH keys for a user. Params: username (required).",
    inputSchema: {"type":"object","properties":{"username":{"type":"string","description":"The handle for the GitHub user account."},"per_page":{"type":"integer","default":30,"description":"The number of results per page (max 100). For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""},"page":{"type":"integer","default":1,"description":"The page number of the results to fetch. For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""}},"required":["username"]},
    call: function(args) {
    var _q = new URLSearchParams();
    if (args["per_page"] !== undefined) _q.set("per_page", String(args["per_page"]));
    if (args["page"] !== undefined) _q.set("page", String(args["page"]));
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/users/" + String(args["username"]) + "/keys") + _qs, {}, undefined);
    }
  },
  {
    name: "orgs/list-for-user",
    description: "List organizations for a user. List [public organization memberships](https://docs.github.com/articles/publicizing-or-concealing-organization-membership) for the specified user. Params: username (required).",
    inputSchema: {"type":"object","properties":{"username":{"type":"string","description":"The handle for the GitHub user account."},"per_page":{"type":"integer","default":30,"description":"The number of results per page (max 100). For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""},"page":{"type":"integer","default":1,"description":"The page number of the results to fetch. For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""}},"required":["username"]},
    call: function(args) {
    var _q = new URLSearchParams();
    if (args["per_page"] !== undefined) _q.set("per_page", String(args["per_page"]));
    if (args["page"] !== undefined) _q.set("page", String(args["page"]));
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/users/" + String(args["username"]) + "/orgs") + _qs, {}, undefined);
    }
  },
  {
    name: "packages/list-packages-for-user",
    description: "List packages for a user. Lists all packages in a user's namespace for which the requesting user has access. Params: package_type (required), username (required).",
    inputSchema: {"type":"object","properties":{"package_type":{"type":"string","enum":["npm","maven","rubygems","docker","nuget","container"],"description":"The type of supported package. Packages in GitHub's Gradle registry have the type `maven`. Docker images pushed to GitHub's Container registry (`ghcr.io`) have the type `container`. You can use the type `docker` to find images that were pushed to GitHub's Docker registry (`docker.pkg.github.com`), even if these have now been migrated to the Container registry."},"visibility":{"type":"string","enum":["public","private","internal"],"description":"The selected visibility of the packages.  This parameter is optional and only filters an existing result set.\n\nThe `internal` visibility is only supported for GitHub Packages registries that allow for granular permissions. For other ecosystems `internal` is synonymous with `private`.\nFor the list of GitHub Packages registries that support granular permissions, see \"[About permissions for GitHub Packages](https://docs.github.com/packages/learn-github-packages/about-permissions-for-github-packages#granular-permissions-for-userorganization-scoped-packages).\""},"username":{"type":"string","description":"The handle for the GitHub user account."},"page":{"type":"integer","default":1,"description":"The page number of the results to fetch. For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""},"per_page":{"type":"integer","default":30,"description":"The number of results per page (max 100). For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""}},"required":["package_type","username"]},
    call: function(args) {
    var _q = new URLSearchParams();
    if (args["package_type"] !== undefined) _q.set("package_type", String(args["package_type"]));
    if (args["visibility"] !== undefined) _q.set("visibility", String(args["visibility"]));
    if (args["page"] !== undefined) _q.set("page", String(args["page"]));
    if (args["per_page"] !== undefined) _q.set("per_page", String(args["per_page"]));
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/users/" + String(args["username"]) + "/packages") + _qs, {}, undefined);
    }
  },
  {
    name: "packages/get-package-for-user",
    description: "Get a package for a user. Gets a specific package metadata for a public package owned by a user. Params: package_type (required), package_name (required), username (required).",
    inputSchema: {"type":"object","properties":{"package_type":{"type":"string","enum":["npm","maven","rubygems","docker","nuget","container"],"description":"The type of supported package. Packages in GitHub's Gradle registry have the type `maven`. Docker images pushed to GitHub's Container registry (`ghcr.io`) have the type `container`. You can use the type `docker` to find images that were pushed to GitHub's Docker registry (`docker.pkg.github.com`), even if these have now been migrated to the Container registry."},"package_name":{"type":"string","description":"The name of the package."},"username":{"type":"string","description":"The handle for the GitHub user account."}},"required":["package_type","package_name","username"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/users/" + String(args["username"]) + "/packages/" + String(args["package_type"]) + "/" + String(args["package_name"])) + _qs, {}, undefined);
    }
  },
  {
    name: "packages/delete-package-for-user",
    description: "Delete a package for a user. Deletes an entire package for a user. Params: package_type (required), package_name (required), username (required).",
    inputSchema: {"type":"object","properties":{"package_type":{"type":"string","enum":["npm","maven","rubygems","docker","nuget","container"],"description":"The type of supported package. Packages in GitHub's Gradle registry have the type `maven`. Docker images pushed to GitHub's Container registry (`ghcr.io`) have the type `container`. You can use the type `docker` to find images that were pushed to GitHub's Docker registry (`docker.pkg.github.com`), even if these have now been migrated to the Container registry."},"package_name":{"type":"string","description":"The name of the package."},"username":{"type":"string","description":"The handle for the GitHub user account."}},"required":["package_type","package_name","username"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("DELETE", ("https://api.github.com/users/" + String(args["username"]) + "/packages/" + String(args["package_type"]) + "/" + String(args["package_name"])) + _qs, {}, undefined);
    }
  },
  {
    name: "packages/restore-package-for-user",
    description: "Restore a package for a user. Restores an entire package for a user. Params: package_type (required), package_name (required), username (required).",
    inputSchema: {"type":"object","properties":{"package_type":{"type":"string","enum":["npm","maven","rubygems","docker","nuget","container"],"description":"The type of supported package. Packages in GitHub's Gradle registry have the type `maven`. Docker images pushed to GitHub's Container registry (`ghcr.io`) have the type `container`. You can use the type `docker` to find images that were pushed to GitHub's Docker registry (`docker.pkg.github.com`), even if these have now been migrated to the Container registry."},"package_name":{"type":"string","description":"The name of the package."},"username":{"type":"string","description":"The handle for the GitHub user account."},"token":{"type":"string","description":"package token"}},"required":["package_type","package_name","username"]},
    call: function(args) {
    var _q = new URLSearchParams();
    if (args["token"] !== undefined) _q.set("token", String(args["token"]));
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("POST", ("https://api.github.com/users/" + String(args["username"]) + "/packages/" + String(args["package_type"]) + "/" + String(args["package_name"]) + "/restore") + _qs, {}, undefined);
    }
  },
  {
    name: "packages/get-all-package-versions-for-package-owned-by-user",
    description: "List package versions for a package owned by a user. Lists package versions for a public package owned by a specified user. Params: package_type (required), package_name (required), username (required).",
    inputSchema: {"type":"object","properties":{"package_type":{"type":"string","enum":["npm","maven","rubygems","docker","nuget","container"],"description":"The type of supported package. Packages in GitHub's Gradle registry have the type `maven`. Docker images pushed to GitHub's Container registry (`ghcr.io`) have the type `container`. You can use the type `docker` to find images that were pushed to GitHub's Docker registry (`docker.pkg.github.com`), even if these have now been migrated to the Container registry."},"package_name":{"type":"string","description":"The name of the package."},"username":{"type":"string","description":"The handle for the GitHub user account."}},"required":["package_type","package_name","username"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/users/" + String(args["username"]) + "/packages/" + String(args["package_type"]) + "/" + String(args["package_name"]) + "/versions") + _qs, {}, undefined);
    }
  },
  {
    name: "packages/get-package-version-for-user",
    description: "Get a package version for a user. Gets a specific package version for a public package owned by a specified user. Params: package_type (required), package_name (required), package_version_id (required), username (required).",
    inputSchema: {"type":"object","properties":{"package_type":{"type":"string","enum":["npm","maven","rubygems","docker","nuget","container"],"description":"The type of supported package. Packages in GitHub's Gradle registry have the type `maven`. Docker images pushed to GitHub's Container registry (`ghcr.io`) have the type `container`. You can use the type `docker` to find images that were pushed to GitHub's Docker registry (`docker.pkg.github.com`), even if these have now been migrated to the Container registry."},"package_name":{"type":"string","description":"The name of the package."},"package_version_id":{"type":"integer","description":"Unique identifier of the package version."},"username":{"type":"string","description":"The handle for the GitHub user account."}},"required":["package_type","package_name","package_version_id","username"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/users/" + String(args["username"]) + "/packages/" + String(args["package_type"]) + "/" + String(args["package_name"]) + "/versions/" + String(args["package_version_id"])) + _qs, {}, undefined);
    }
  },
  {
    name: "packages/delete-package-version-for-user",
    description: "Delete package version for a user. Deletes a specific package version for a user. Params: package_type (required), package_name (required), username (required), package_version_id (required).",
    inputSchema: {"type":"object","properties":{"package_type":{"type":"string","enum":["npm","maven","rubygems","docker","nuget","container"],"description":"The type of supported package. Packages in GitHub's Gradle registry have the type `maven`. Docker images pushed to GitHub's Container registry (`ghcr.io`) have the type `container`. You can use the type `docker` to find images that were pushed to GitHub's Docker registry (`docker.pkg.github.com`), even if these have now been migrated to the Container registry."},"package_name":{"type":"string","description":"The name of the package."},"username":{"type":"string","description":"The handle for the GitHub user account."},"package_version_id":{"type":"integer","description":"Unique identifier of the package version."}},"required":["package_type","package_name","username","package_version_id"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("DELETE", ("https://api.github.com/users/" + String(args["username"]) + "/packages/" + String(args["package_type"]) + "/" + String(args["package_name"]) + "/versions/" + String(args["package_version_id"])) + _qs, {}, undefined);
    }
  },
  {
    name: "packages/restore-package-version-for-user",
    description: "Restore package version for a user. Restores a specific package version for a user. Params: package_type (required), package_name (required), username (required), package_version_id (required).",
    inputSchema: {"type":"object","properties":{"package_type":{"type":"string","enum":["npm","maven","rubygems","docker","nuget","container"],"description":"The type of supported package. Packages in GitHub's Gradle registry have the type `maven`. Docker images pushed to GitHub's Container registry (`ghcr.io`) have the type `container`. You can use the type `docker` to find images that were pushed to GitHub's Docker registry (`docker.pkg.github.com`), even if these have now been migrated to the Container registry."},"package_name":{"type":"string","description":"The name of the package."},"username":{"type":"string","description":"The handle for the GitHub user account."},"package_version_id":{"type":"integer","description":"Unique identifier of the package version."}},"required":["package_type","package_name","username","package_version_id"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("POST", ("https://api.github.com/users/" + String(args["username"]) + "/packages/" + String(args["package_type"]) + "/" + String(args["package_name"]) + "/versions/" + String(args["package_version_id"]) + "/restore") + _qs, {}, undefined);
    }
  },
  {
    name: "projects/list-for-user",
    description: "List projects for user. List all projects owned by a specific user accessible by the authenticated user. Params: username (required).",
    inputSchema: {"type":"object","properties":{"username":{"type":"string","description":"The handle for the GitHub user account."},"q":{"type":"string","description":"Limit results to projects of the specified type."},"before":{"type":"string","description":"A cursor, as given in the [Link header](https://docs.github.com/rest/guides/using-pagination-in-the-rest-api#using-link-headers). If specified, the query only searches for results before this cursor. For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""},"after":{"type":"string","description":"A cursor, as given in the [Link header](https://docs.github.com/rest/guides/using-pagination-in-the-rest-api#using-link-headers). If specified, the query only searches for results after this cursor. For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""},"per_page":{"type":"integer","default":30,"description":"The number of results per page (max 100). For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""}},"required":["username"]},
    call: function(args) {
    var _q = new URLSearchParams();
    if (args["q"] !== undefined) _q.set("q", String(args["q"]));
    if (args["before"] !== undefined) _q.set("before", String(args["before"]));
    if (args["after"] !== undefined) _q.set("after", String(args["after"]));
    if (args["per_page"] !== undefined) _q.set("per_page", String(args["per_page"]));
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/users/" + String(args["username"]) + "/projectsV2") + _qs, {}, undefined);
    }
  },
  {
    name: "projects/get-for-user",
    description: "Get project for user. Get a specific user-owned project. Params: project_number (required), username (required).",
    inputSchema: {"type":"object","properties":{"project_number":{"type":"integer","description":"The project's number."},"username":{"type":"string","description":"The handle for the GitHub user account."}},"required":["project_number","username"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/users/" + String(args["username"]) + "/projectsV2/" + String(args["project_number"])) + _qs, {}, undefined);
    }
  },
  {
    name: "projects/list-fields-for-user",
    description: "List project fields for user. List all fields for a specific user-owned project. Params: project_number (required), username (required).",
    inputSchema: {"type":"object","properties":{"project_number":{"type":"integer","description":"The project's number."},"username":{"type":"string","description":"The handle for the GitHub user account."},"per_page":{"type":"integer","default":30,"description":"The number of results per page (max 100). For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""},"before":{"type":"string","description":"A cursor, as given in the [Link header](https://docs.github.com/rest/guides/using-pagination-in-the-rest-api#using-link-headers). If specified, the query only searches for results before this cursor. For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""},"after":{"type":"string","description":"A cursor, as given in the [Link header](https://docs.github.com/rest/guides/using-pagination-in-the-rest-api#using-link-headers). If specified, the query only searches for results after this cursor. For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""}},"required":["project_number","username"]},
    call: function(args) {
    var _q = new URLSearchParams();
    if (args["per_page"] !== undefined) _q.set("per_page", String(args["per_page"]));
    if (args["before"] !== undefined) _q.set("before", String(args["before"]));
    if (args["after"] !== undefined) _q.set("after", String(args["after"]));
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/users/" + String(args["username"]) + "/projectsV2/" + String(args["project_number"]) + "/fields") + _qs, {}, undefined);
    }
  },
  {
    name: "projects/add-field-for-user",
    description: "Add field to user owned project. Add a field to a specified user owned project. Params: username (required), project_number (required).",
    inputSchema: {"type":"object","properties":{"username":{"type":"string","description":"The handle for the GitHub user account."},"project_number":{"type":"integer","description":"The project's number."},"body":{"type":"object","description":"Request body"}},"required":["username","project_number","body"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("POST", ("https://api.github.com/users/" + String(args["username"]) + "/projectsV2/" + String(args["project_number"]) + "/fields") + _qs, {}, args["body"]);
    }
  },
  {
    name: "projects/get-field-for-user",
    description: "Get project field for user. Get a specific field for a user-owned project. Params: project_number (required), field_id (required), username (required).",
    inputSchema: {"type":"object","properties":{"project_number":{"type":"integer","description":"The project's number."},"field_id":{"type":"integer","description":"The unique identifier of the field."},"username":{"type":"string","description":"The handle for the GitHub user account."}},"required":["project_number","field_id","username"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/users/" + String(args["username"]) + "/projectsV2/" + String(args["project_number"]) + "/fields/" + String(args["field_id"])) + _qs, {}, undefined);
    }
  },
  {
    name: "projects/list-items-for-user",
    description: "List items for a user owned project. List all items for a specific user-owned project accessible by the authenticated user. Params: project_number (required), username (required).",
    inputSchema: {"type":"object","properties":{"project_number":{"type":"integer","description":"The project's number."},"username":{"type":"string","description":"The handle for the GitHub user account."},"before":{"type":"string","description":"A cursor, as given in the [Link header](https://docs.github.com/rest/guides/using-pagination-in-the-rest-api#using-link-headers). If specified, the query only searches for results before this cursor. For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""},"after":{"type":"string","description":"A cursor, as given in the [Link header](https://docs.github.com/rest/guides/using-pagination-in-the-rest-api#using-link-headers). If specified, the query only searches for results after this cursor. For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""},"per_page":{"type":"integer","default":30,"description":"The number of results per page (max 100). For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""},"q":{"type":"string","description":"Search query to filter items, see [Filtering projects](https://docs.github.com/issues/planning-and-tracking-with-projects/customizing-views-in-your-project/filtering-projects) for more information."},"fields":{"oneOf":[{"type":"string"},{"type":"array","maxItems":50,"items":{"type":"string"}}],"description":"Limit results to specific fields, by their IDs. If not specified, the title field will be returned.\n\nExample: `fields[]=123&fields[]=456&fields[]=789` or `fields=123,456,789`"}},"required":["project_number","username"]},
    call: function(args) {
    var _q = new URLSearchParams();
    if (args["before"] !== undefined) _q.set("before", String(args["before"]));
    if (args["after"] !== undefined) _q.set("after", String(args["after"]));
    if (args["per_page"] !== undefined) _q.set("per_page", String(args["per_page"]));
    if (args["q"] !== undefined) _q.set("q", String(args["q"]));
    if (args["fields"] !== undefined) _q.set("fields", String(args["fields"]));
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/users/" + String(args["username"]) + "/projectsV2/" + String(args["project_number"]) + "/items") + _qs, {}, undefined);
    }
  },
  {
    name: "projects/add-item-for-user",
    description: "Add item to user owned project. Add an issue or pull request item to the specified user owned project. Params: username (required), project_number (required).",
    inputSchema: {"type":"object","properties":{"username":{"type":"string","description":"The handle for the GitHub user account."},"project_number":{"type":"integer","description":"The project's number."},"body":{"type":"object","description":"Details of the item to add to the project. You can specify either the unique ID or the repository owner, name, and issue/PR number."}},"required":["username","project_number","body"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("POST", ("https://api.github.com/users/" + String(args["username"]) + "/projectsV2/" + String(args["project_number"]) + "/items") + _qs, {}, args["body"]);
    }
  },
  {
    name: "projects/get-user-item",
    description: "Get an item for a user owned project. Get a specific item from a user-owned project. Params: project_number (required), username (required), item_id (required).",
    inputSchema: {"type":"object","properties":{"project_number":{"type":"integer","description":"The project's number."},"username":{"type":"string","description":"The handle for the GitHub user account."},"item_id":{"type":"integer","description":"The unique identifier of the project item."},"fields":{"oneOf":[{"type":"string"},{"type":"array","maxItems":50,"items":{"type":"string"}}],"description":"Limit results to specific fields, by their IDs. If not specified, the title field will be returned.\n\nExample: fields[]=123&fields[]=456&fields[]=789 or fields=123,456,789"}},"required":["project_number","username","item_id"]},
    call: function(args) {
    var _q = new URLSearchParams();
    if (args["fields"] !== undefined) _q.set("fields", String(args["fields"]));
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/users/" + String(args["username"]) + "/projectsV2/" + String(args["project_number"]) + "/items/" + String(args["item_id"])) + _qs, {}, undefined);
    }
  },
  {
    name: "projects/update-item-for-user",
    description: "Update project item for user. Update a specific item in a user-owned project. Params: project_number (required), username (required), item_id (required).",
    inputSchema: {"type":"object","properties":{"project_number":{"type":"integer","description":"The project's number."},"username":{"type":"string","description":"The handle for the GitHub user account."},"item_id":{"type":"integer","description":"The unique identifier of the project item."},"body":{"type":"object","description":"Field updates to apply to the project item. Only text, number, date, single select, and iteration fields are supported."}},"required":["project_number","username","item_id","body"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("PATCH", ("https://api.github.com/users/" + String(args["username"]) + "/projectsV2/" + String(args["project_number"]) + "/items/" + String(args["item_id"])) + _qs, {}, args["body"]);
    }
  },
  {
    name: "projects/delete-item-for-user",
    description: "Delete project item for user. Delete a specific item from a user-owned project. Params: project_number (required), username (required), item_id (required).",
    inputSchema: {"type":"object","properties":{"project_number":{"type":"integer","description":"The project's number."},"username":{"type":"string","description":"The handle for the GitHub user account."},"item_id":{"type":"integer","description":"The unique identifier of the project item."}},"required":["project_number","username","item_id"]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("DELETE", ("https://api.github.com/users/" + String(args["username"]) + "/projectsV2/" + String(args["project_number"]) + "/items/" + String(args["item_id"])) + _qs, {}, undefined);
    }
  },
  {
    name: "projects/list-view-items-for-user",
    description: "List items for a user project view. List items in a user project with the saved view's filter applied. Params: project_number (required), username (required), view_number (required).",
    inputSchema: {"type":"object","properties":{"project_number":{"type":"integer","description":"The project's number."},"username":{"type":"string","description":"The handle for the GitHub user account."},"view_number":{"type":"integer","description":"The number that identifies the project view."},"fields":{"oneOf":[{"type":"string"},{"type":"array","maxItems":50,"items":{"type":"string"}}],"description":"Limit results to specific fields, by their IDs. If not specified, the\ntitle field will be returned.\n\nExample: `fields[]=123&fields[]=456&fields[]=789` or `fields=123,456,789`"},"before":{"type":"string","description":"A cursor, as given in the [Link header](https://docs.github.com/rest/guides/using-pagination-in-the-rest-api#using-link-headers). If specified, the query only searches for results before this cursor. For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""},"after":{"type":"string","description":"A cursor, as given in the [Link header](https://docs.github.com/rest/guides/using-pagination-in-the-rest-api#using-link-headers). If specified, the query only searches for results after this cursor. For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""},"per_page":{"type":"integer","default":30,"description":"The number of results per page (max 100). For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""}},"required":["project_number","username","view_number"]},
    call: function(args) {
    var _q = new URLSearchParams();
    if (args["fields"] !== undefined) _q.set("fields", String(args["fields"]));
    if (args["before"] !== undefined) _q.set("before", String(args["before"]));
    if (args["after"] !== undefined) _q.set("after", String(args["after"]));
    if (args["per_page"] !== undefined) _q.set("per_page", String(args["per_page"]));
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/users/" + String(args["username"]) + "/projectsV2/" + String(args["project_number"]) + "/views/" + String(args["view_number"]) + "/items") + _qs, {}, undefined);
    }
  },
  {
    name: "activity/list-received-events-for-user",
    description: "List events received by the authenticated user. These are events that you've received by watching repositories and following users. Params: username (required).",
    inputSchema: {"type":"object","properties":{"username":{"type":"string","description":"The handle for the GitHub user account."},"per_page":{"type":"integer","default":30,"description":"The number of results per page (max 100). For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""},"page":{"type":"integer","default":1,"description":"The page number of the results to fetch. For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""}},"required":["username"]},
    call: function(args) {
    var _q = new URLSearchParams();
    if (args["per_page"] !== undefined) _q.set("per_page", String(args["per_page"]));
    if (args["page"] !== undefined) _q.set("page", String(args["page"]));
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/users/" + String(args["username"]) + "/received_events") + _qs, {}, undefined);
    }
  },
  {
    name: "activity/list-received-public-events-for-user",
    description: "List public events received by a user. > [!NOTE]\n> This API is not built to serve real-time use cases. Params: username (required).",
    inputSchema: {"type":"object","properties":{"username":{"type":"string","description":"The handle for the GitHub user account."},"per_page":{"type":"integer","default":30,"description":"The number of results per page (max 100). For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""},"page":{"type":"integer","default":1,"description":"The page number of the results to fetch. For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""}},"required":["username"]},
    call: function(args) {
    var _q = new URLSearchParams();
    if (args["per_page"] !== undefined) _q.set("per_page", String(args["per_page"]));
    if (args["page"] !== undefined) _q.set("page", String(args["page"]));
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/users/" + String(args["username"]) + "/received_events/public") + _qs, {}, undefined);
    }
  },
  {
    name: "repos/list-for-user",
    description: "List repositories for a user. Lists public repositories for the specified user. Params: username (required).",
    inputSchema: {"type":"object","properties":{"username":{"type":"string","description":"The handle for the GitHub user account."},"type":{"type":"string","enum":["all","owner","member"],"default":"owner","description":"Limit results to repositories of the specified type."},"sort":{"type":"string","enum":["created","updated","pushed","full_name"],"default":"full_name","description":"The property to sort the results by."},"direction":{"type":"string","enum":["asc","desc"],"description":"The order to sort by. Default: `asc` when using `full_name`, otherwise `desc`."},"per_page":{"type":"integer","default":30,"description":"The number of results per page (max 100). For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""},"page":{"type":"integer","default":1,"description":"The page number of the results to fetch. For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""}},"required":["username"]},
    call: function(args) {
    var _q = new URLSearchParams();
    if (args["type"] !== undefined) _q.set("type", String(args["type"]));
    if (args["sort"] !== undefined) _q.set("sort", String(args["sort"]));
    if (args["direction"] !== undefined) _q.set("direction", String(args["direction"]));
    if (args["per_page"] !== undefined) _q.set("per_page", String(args["per_page"]));
    if (args["page"] !== undefined) _q.set("page", String(args["page"]));
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/users/" + String(args["username"]) + "/repos") + _qs, {}, undefined);
    }
  },
  {
    name: "billing/get-github-billing-ai-credit-usage-report-user",
    description: "Get billing AI credit usage report for a user. Gets a report of AI credit usage for a user. Params: username (required).",
    inputSchema: {"type":"object","properties":{"username":{"type":"string","description":"The handle for the GitHub user account."},"year":{"type":"integer","description":"If specified, only return results for a single year. The value of `year` is an integer with four digits representing a year. For example, `2025`. Default value is the current year."},"month":{"type":"integer","description":"If specified, only return results for a single month. The value of `month` is an integer between `1` and `12`. Default value is the current month. If no year is specified the default `year` is used."},"day":{"type":"integer","description":"If specified, only return results for a single day. The value of `day` is an integer between `1` and `31`. If no `year` or `month` is specified, the default `year` and `month` are used."},"model":{"type":"string","description":"The model name to query usage for. The name is not case sensitive."},"product":{"type":"string","description":"The product name to query usage for. The name is not case sensitive."}},"required":["username"]},
    call: function(args) {
    var _q = new URLSearchParams();
    if (args["year"] !== undefined) _q.set("year", String(args["year"]));
    if (args["month"] !== undefined) _q.set("month", String(args["month"]));
    if (args["day"] !== undefined) _q.set("day", String(args["day"]));
    if (args["model"] !== undefined) _q.set("model", String(args["model"]));
    if (args["product"] !== undefined) _q.set("product", String(args["product"]));
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/users/" + String(args["username"]) + "/settings/billing/ai_credit/usage") + _qs, {}, undefined);
    }
  },
  {
    name: "billing/get-github-billing-premium-request-usage-report-user",
    description: "Get billing premium request usage report for a user. Gets a report of premium request usage for a user. Params: username (required).",
    inputSchema: {"type":"object","properties":{"username":{"type":"string","description":"The handle for the GitHub user account."},"year":{"type":"integer","description":"If specified, only return results for a single year. The value of `year` is an integer with four digits representing a year. For example, `2025`. Default value is the current year."},"month":{"type":"integer","description":"If specified, only return results for a single month. The value of `month` is an integer between `1` and `12`. Default value is the current month. If no year is specified the default `year` is used."},"day":{"type":"integer","description":"If specified, only return results for a single day. The value of `day` is an integer between `1` and `31`. If no `year` or `month` is specified, the default `year` and `month` are used."},"model":{"type":"string","description":"The model name to query usage for. The name is not case sensitive."},"product":{"type":"string","description":"The product name to query usage for. The name is not case sensitive."}},"required":["username"]},
    call: function(args) {
    var _q = new URLSearchParams();
    if (args["year"] !== undefined) _q.set("year", String(args["year"]));
    if (args["month"] !== undefined) _q.set("month", String(args["month"]));
    if (args["day"] !== undefined) _q.set("day", String(args["day"]));
    if (args["model"] !== undefined) _q.set("model", String(args["model"]));
    if (args["product"] !== undefined) _q.set("product", String(args["product"]));
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/users/" + String(args["username"]) + "/settings/billing/premium_request/usage") + _qs, {}, undefined);
    }
  },
  {
    name: "billing/get-github-billing-usage-report-user",
    description: "Get billing usage report for a user. Gets a report of the total usage for a user. Params: username (required).",
    inputSchema: {"type":"object","properties":{"username":{"type":"string","description":"The handle for the GitHub user account."},"year":{"type":"integer","description":"If specified, only return results for a single year. The value of `year` is an integer with four digits representing a year. For example, `2025`. Default value is the current year."},"month":{"type":"integer","description":"If specified, only return results for a single month. The value of `month` is an integer between `1` and `12`. If no year is specified the default `year` is used."},"day":{"type":"integer","description":"If specified, only return results for a single day. The value of `day` is an integer between `1` and `31`. If no `year` or `month` is specified, the default `year` and `month` are used."}},"required":["username"]},
    call: function(args) {
    var _q = new URLSearchParams();
    if (args["year"] !== undefined) _q.set("year", String(args["year"]));
    if (args["month"] !== undefined) _q.set("month", String(args["month"]));
    if (args["day"] !== undefined) _q.set("day", String(args["day"]));
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/users/" + String(args["username"]) + "/settings/billing/usage") + _qs, {}, undefined);
    }
  },
  {
    name: "billing/get-github-billing-usage-summary-report-user",
    description: "Get billing usage summary for a user. > [!NOTE]\n> This endpoint is in public preview and is subject to change. Params: username (required).",
    inputSchema: {"type":"object","properties":{"username":{"type":"string","description":"The handle for the GitHub user account."},"year":{"type":"integer","description":"If specified, only return results for a single year. The value of `year` is an integer with four digits representing a year. For example, `2025`. Default value is the current year."},"month":{"type":"integer","description":"If specified, only return results for a single month. The value of `month` is an integer between `1` and `12`. Default value is the current month. If no year is specified the default `year` is used."},"day":{"type":"integer","description":"If specified, only return results for a single day. The value of `day` is an integer between `1` and `31`. If no `year` or `month` is specified, the default `year` and `month` are used."},"repository":{"type":"string","description":"The repository name to query for usage in the format owner/repository."},"product":{"type":"string","description":"The product name to query usage for. The name is not case sensitive."},"sku":{"type":"string","description":"The SKU to query for usage."}},"required":["username"]},
    call: function(args) {
    var _q = new URLSearchParams();
    if (args["year"] !== undefined) _q.set("year", String(args["year"]));
    if (args["month"] !== undefined) _q.set("month", String(args["month"]));
    if (args["day"] !== undefined) _q.set("day", String(args["day"]));
    if (args["repository"] !== undefined) _q.set("repository", String(args["repository"]));
    if (args["product"] !== undefined) _q.set("product", String(args["product"]));
    if (args["sku"] !== undefined) _q.set("sku", String(args["sku"]));
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/users/" + String(args["username"]) + "/settings/billing/usage/summary") + _qs, {}, undefined);
    }
  },
  {
    name: "users/list-social-accounts-for-user",
    description: "List social accounts for a user. Lists social media accounts for a user. Params: username (required).",
    inputSchema: {"type":"object","properties":{"username":{"type":"string","description":"The handle for the GitHub user account."},"per_page":{"type":"integer","default":30,"description":"The number of results per page (max 100). For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""},"page":{"type":"integer","default":1,"description":"The page number of the results to fetch. For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""}},"required":["username"]},
    call: function(args) {
    var _q = new URLSearchParams();
    if (args["per_page"] !== undefined) _q.set("per_page", String(args["per_page"]));
    if (args["page"] !== undefined) _q.set("page", String(args["page"]));
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/users/" + String(args["username"]) + "/social_accounts") + _qs, {}, undefined);
    }
  },
  {
    name: "users/list-ssh-signing-keys-for-user",
    description: "List SSH signing keys for a user. Lists the SSH signing keys for a user. Params: username (required).",
    inputSchema: {"type":"object","properties":{"username":{"type":"string","description":"The handle for the GitHub user account."},"per_page":{"type":"integer","default":30,"description":"The number of results per page (max 100). For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""},"page":{"type":"integer","default":1,"description":"The page number of the results to fetch. For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""}},"required":["username"]},
    call: function(args) {
    var _q = new URLSearchParams();
    if (args["per_page"] !== undefined) _q.set("per_page", String(args["per_page"]));
    if (args["page"] !== undefined) _q.set("page", String(args["page"]));
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/users/" + String(args["username"]) + "/ssh_signing_keys") + _qs, {}, undefined);
    }
  },
  {
    name: "activity/list-repos-starred-by-user",
    description: "List repositories starred by a user. Lists repositories a user has starred. Params: username (required).",
    inputSchema: {"type":"object","properties":{"username":{"type":"string","description":"The handle for the GitHub user account."},"sort":{"type":"string","enum":["created","updated"],"default":"created","description":"The property to sort the results by. `created` means when the repository was starred. `updated` means when the repository was last pushed to."},"direction":{"type":"string","enum":["asc","desc"],"default":"desc","description":"The direction to sort the results by."},"per_page":{"type":"integer","default":30,"description":"The number of results per page (max 100). For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""},"page":{"type":"integer","default":1,"description":"The page number of the results to fetch. For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""}},"required":["username"]},
    call: function(args) {
    var _q = new URLSearchParams();
    if (args["sort"] !== undefined) _q.set("sort", String(args["sort"]));
    if (args["direction"] !== undefined) _q.set("direction", String(args["direction"]));
    if (args["per_page"] !== undefined) _q.set("per_page", String(args["per_page"]));
    if (args["page"] !== undefined) _q.set("page", String(args["page"]));
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/users/" + String(args["username"]) + "/starred") + _qs, {}, undefined);
    }
  },
  {
    name: "activity/list-repos-watched-by-user",
    description: "List repositories watched by a user. Lists repositories a user is watching. Params: username (required).",
    inputSchema: {"type":"object","properties":{"username":{"type":"string","description":"The handle for the GitHub user account."},"per_page":{"type":"integer","default":30,"description":"The number of results per page (max 100). For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""},"page":{"type":"integer","default":1,"description":"The page number of the results to fetch. For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""}},"required":["username"]},
    call: function(args) {
    var _q = new URLSearchParams();
    if (args["per_page"] !== undefined) _q.set("per_page", String(args["per_page"]));
    if (args["page"] !== undefined) _q.set("page", String(args["page"]));
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/users/" + String(args["username"]) + "/subscriptions") + _qs, {}, undefined);
    }
  },
  {
    name: "meta/get-all-versions",
    description: "Get all API versions. Get all supported GitHub API versions.",
    inputSchema: {"type":"object","properties":{},"required":[]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/versions") + _qs, {}, undefined);
    }
  },
  {
    name: "meta/get-zen",
    description: "Get the Zen of GitHub. Get a random sentence from the Zen of GitHub.",
    inputSchema: {"type":"object","properties":{},"required":[]},
    call: function(args) {
    var _q = new URLSearchParams();
    var _qs = _q.toString() ? "?" + _q.toString() : "";
      return _request("GET", ("https://api.github.com/zen") + _qs, {}, undefined);
    }
  }
];

// --- MCP Protocol ---
var _buf = "";
process.stdin.setEncoding("utf8");
process.stdin.on("data", function(chunk) {
  _buf += chunk;
  var lines = _buf.split("\n");
  _buf = lines.pop() || "";
  for (var i = 0; i < lines.length; i++) {
    var line = lines[i];
    if (!line.trim()) continue;
    var msg;
    try { msg = JSON.parse(line); } catch(e) { continue; }
    _handle(msg).catch(function(e) { process.stderr.write(e.message + "\n"); });
  }
});

function _send(obj) { process.stdout.write(JSON.stringify(obj) + "\n"); }

function _handle(msg) {
  var id = msg.id;
  var method = msg.method;
  var params = msg.params;
  if (method === "initialize") {
    _send({ jsonrpc: "2.0", id: id, result: { protocolVersion: "2024-11-05", capabilities: { tools: {} }, serverInfo: { name: "github-v3-rest-api", version: "1.1.4" } } });
    return Promise.resolve();
  } else if (method === "notifications/initialized") {
    return Promise.resolve();
  } else if (method === "tools/list") {
    _send({ jsonrpc: "2.0", id: id, result: { tools: _TOOLS.map(function(t) { return { name: t.name, description: t.description, inputSchema: t.inputSchema }; }) } });
    return Promise.resolve();
  } else if (method === "tools/call") {
    var toolName = params && params.name;
    var tool = null;
    for (var i = 0; i < _TOOLS.length; i++) {
      if (_TOOLS[i].name === toolName) { tool = _TOOLS[i]; break; }
    }
    if (!tool) {
      _send({ jsonrpc: "2.0", id: id, error: { code: -32601, message: "Unknown tool: " + toolName } });
      return Promise.resolve();
    }
    return Promise.resolve(tool.call((params && params.arguments) || {})).then(function(result) {
      _send({ jsonrpc: "2.0", id: id, result: { content: [{ type: "text", text: JSON.stringify(result, null, 2) }] } });
    }).catch(function(e) {
      _send({ jsonrpc: "2.0", id: id, result: { content: [{ type: "text", text: "Error: " + e.message }], isError: true } });
    });
  } else if (id !== undefined) {
    _send({ jsonrpc: "2.0", id: id, error: { code: -32601, message: "Method not found: " + method } });
    return Promise.resolve();
  }
  return Promise.resolve();
}
