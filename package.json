{
  "name": "apidiom",
  "version": "0.4.0",
  "description": "Turn any API into an MCP server in one command.",
  "type": "commonjs",
  "bin": {
    "apidiom": "dist/cli.js"
  },
  "scripts": {
    "build": "tsup",
    "pack:smoke": "node scripts/pack-smoke.cjs --clean && npm pack --pack-destination .tmp/pack-smoke --cache .tmp/pack-smoke-cache --silent && node scripts/pack-smoke.cjs",
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "files": [
    "dist"
  ],
  "keywords": [
    "mcp",
    "model-context-protocol",
    "openapi",
    "ai",
    "llm",
    "api",
    "claude",
    "tool-calling",
    "ai-agents",
    "code-generator",
    "cli"
  ],
  "license": "MIT",
  "homepage": "https://github.com/poyhi/apidiom",
  "repository": {
    "type": "git",
    "url": "https://github.com/poyhi/apidiom.git"
  },
  "dependencies": {
    "commander": "^12.0.0",
    "js-yaml": "^4.1.0"
  },
  "devDependencies": {
    "@types/js-yaml": "^4.0.9",
    "@types/node": "^20.0.0",
    "tsup": "^8.0.0",
    "typescript": "^5.0.0",
    "vitest": "^4.1.9"
  },
  "engines": {
    "node": ">=18"
  }
}
