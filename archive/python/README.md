# Python Archive

This directory preserves the previous Python-era tests and project config.

The active project is TypeScript-first:

```bash
npm run build
npm run typecheck
npm test
```

Do not use files here as active verification unless the Python target is
explicitly reopened.
